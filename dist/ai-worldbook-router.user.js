// ==UserScript==
// @name         AI Worldbook Router - Tavern Helper
// @namespace    https://github.com/jiandanhaoyun/haoyunAll-Memories
// @version      1.0.6-userscript
// @description  AI worldbook routing and lightweight memory graph console
// @author       zmer
// @match        *://*/*
// @grant        none
// @run-at       document-idle
// ==/UserScript==
(function () {
    'use strict';

    const AI_WBR_INSTANCE_KEY = '__AI_WORLDBOOK_ROUTER_USERSCRIPT_LOADED__';

    function getHostWindow() {
        try {
            if (window.parent && window.parent !== window && window.parent.document) {
                return window.parent;
            }
        } catch (_) {}
        return window;
    }

    const hostWindow = getHostWindow();
    const hostDocument = hostWindow.document;
    if (!hostDocument || hostWindow[AI_WBR_INSTANCE_KEY]) {
        return;
    }
    hostWindow[AI_WBR_INSTANCE_KEY] = true;

    const moduleSource = "const hostWindow = window;\nconst hostDocument = document;\nconst AI_WBR_SETTINGS_HTML = \"<div id=\\\"ai_worldbook_router_settings\\\" class=\\\"ai-worldbook-router-settings\\\">\\r\\n    <div class=\\\"inline-drawer\\\">\\r\\n        <div class=\\\"inline-drawer-toggle inline-drawer-header\\\">\\r\\n            <b>世界书读取</b>\\r\\n            <div class=\\\"inline-drawer-icon fa-solid fa-circle-chevron-down down\\\"></div>\\r\\n        </div>\\r\\n        <div class=\\\"inline-drawer-content\\\">\\r\\n            <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_enabled\\\">\\r\\n                <input id=\\\"ai_wbr_enabled\\\" type=\\\"checkbox\\\" />\\r\\n                <span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"总开关。开启后会在正式生成前筛选本轮相关世界书并注入 prompt。\\\"></span>\\r\\n                <span>启用前置 AI 世界书路由</span>\\r\\n            </label>\\r\\n            <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_debug\\\">\\r\\n                <input id=\\\"ai_wbr_debug\\\" type=\\\"checkbox\\\" />\\r\\n                <span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"在浏览器控制台和本面板显示候选、选择原因与注入长度，方便排查。\\\"></span>\\r\\n                <span>调试模式</span>\\r\\n            </label>\\r\\n            <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_entry_diagnostics\\\">\\r\\n                <input id=\\\"ai_wbr_entry_diagnostics\\\" type=\\\"checkbox\\\" />\\r\\n                <span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"开启后，点击菜单入口或右侧记忆按钮时会显示入口诊断层；关闭后仅在加载失败时自动显示。\\\"></span>\\r\\n                <span>显示入口诊断层</span>\\r\\n            </label>\\r\\n            <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_floating_button_enabled\\\">\\r\\n                <input id=\\\"ai_wbr_floating_button_enabled\\\" type=\\\"checkbox\\\" />\\r\\n                <span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"控制右侧记忆图谱悬浮按钮是否显示。关闭后仍可从扩展入口打开控制台。\\\"></span>\\r\\n                <span>显示记忆悬浮按钮</span>\\r\\n            </label>\\r\\n            <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_keyword_recall\\\">\\r\\n                <input id=\\\"ai_wbr_keyword_recall\\\" type=\\\"checkbox\\\" />\\r\\n                <span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"用最后用户消息、最近上下文和可选状态匹配世界书 keys，先缩小候选范围。推荐开启。\\\"></span>\\r\\n                <span>启用关键词粗召回</span>\\r\\n            </label>\\r\\n            <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_use_mvu\\\">\\r\\n                <input id=\\\"ai_wbr_use_mvu\\\" type=\\\"checkbox\\\" />\\r\\n                <span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"尝试把变量卡表格、MVU、Tavern Helper 等状态数据纳入读取范围，并把地点、NPC、阶段等状态加入匹配与判断。读不到会静默跳过。\\\"></span>\\r\\n                <span>变量卡表格纳入读取范围</span>\\r\\n            </label>\\r\\n            <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_allow_constant\\\">\\r\\n                <input id=\\\"ai_wbr_allow_constant\\\" type=\\\"checkbox\\\" />\\r\\n                <span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"默认跳过 constant 条目，避免和原生世界书常驻注入重复。开启后，本插件也会把常驻条目读进候选范围。\\\"></span>\\r\\n                <span>允许读取常驻条目</span>\\r\\n            </label>\\r\\n\\r\\n            <div class=\\\"m-t-1\\\">\\r\\n                <label><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"这里显示当前聊天/角色下实际生效的世界书。勾选表示允许参与前置召回；取消勾选后，这本世界书将不会被本插件搜索。\\\"></span>当前生效世界书</label>\\r\\n                <div id=\\\"ai_wbr_active_world_items\\\" class=\\\"ai-wbr-world-list\\\"></div>\\r\\n            </div>\\r\\n\\r\\n            <div class=\\\"m-t-1\\\">\\r\\n                <label for=\\\"ai_wbr_title_block_input\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"按标题拦截世界书条目。输入一个标题关键词后点添加；命中的条目不会进入本插件的候选、AI 选择和注入。\\\"></span>标题拦截规则</label>\\r\\n                <div class=\\\"ai-wbr-token-editor\\\">\\r\\n                    <input id=\\\"ai_wbr_title_block_input\\\" class=\\\"text_pole\\\" type=\\\"text\\\" placeholder=\\\"例如：TavernDB-ACU-WrapperEnd\\\" />\\r\\n                    <button id=\\\"ai_wbr_title_block_add\\\" class=\\\"menu_button\\\" type=\\\"button\\\">添加</button>\\r\\n                </div>\\r\\n                <div id=\\\"ai_wbr_title_block_items\\\" class=\\\"ai-wbr-token-list\\\"></div>\\r\\n            </div>\\r\\n\\r\\n            <div class=\\\"ai-wbr-section\\\">\\r\\n                <div class=\\\"ai-wbr-section-title\\\">\\r\\n                    <b>独立路由模型</b>\\r\\n                </div>\\r\\n                <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_router_use_separate_model\\\">\\r\\n                    <input id=\\\"ai_wbr_router_use_separate_model\\\" type=\\\"checkbox\\\" />\\r\\n                    <span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"开启后，前置 AI 精选不走当前主模型，而走下面单独配置的 OpenAI-compatible 接口。\\\"></span>\\r\\n                    <span>启用独立模型</span>\\r\\n                </label>\\r\\n\\r\\n                <div class=\\\"ai-wbr-grid\\\">\\r\\n                    <label for=\\\"ai_wbr_router_api_url\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"独立路由模型的接口地址。通常填到 /v1，例如 http://127.0.0.1:8000/v1 。\\\"></span>API URL</label>\\r\\n                    <input id=\\\"ai_wbr_router_api_url\\\" class=\\\"text_pole\\\" type=\\\"text\\\" placeholder=\\\"https://example.com/v1\\\" />\\r\\n\\r\\n                    <label for=\\\"ai_wbr_router_api_key\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"用于拉取模型和执行前置 AI 选择的 API Key，仅保存在此插件设置里。\\\"></span>API Key</label>\\r\\n                    <input id=\\\"ai_wbr_router_api_key\\\" class=\\\"text_pole\\\" type=\\\"password\\\" placeholder=\\\"sk-...\\\" />\\r\\n\\r\\n                    <label for=\\\"ai_wbr_router_model\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"当前用于前置世界书精选的模型。先点“拉取模型”，再从下拉里选择。\\\"></span>路由模型</label>\\r\\n                    <select id=\\\"ai_wbr_router_model\\\" class=\\\"text_pole\\\">\\r\\n                        <option value=\\\"\\\">未选择</option>\\r\\n                    </select>\\r\\n                </div>\\r\\n\\r\\n                <div class=\\\"ai-wbr-actions\\\">\\r\\n                    <button id=\\\"ai_wbr_fetch_models\\\" class=\\\"menu_button\\\">拉取模型</button>\\r\\n                    <span id=\\\"ai_wbr_router_status\\\" class=\\\"ai-wbr-status\\\">未连接</span>\\r\\n                </div>\\r\\n            </div>\\r\\n\\r\\n            <div class=\\\"ai-wbr-grid\\\">\\r\\n                <label for=\\\"ai_wbr_max_candidates\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"本插件先用本地规则从世界书里召回候选，再把候选交给前置 AI 精选。这里控制候选上限。\\\"></span>最大候选条目数</label>\\r\\n                <input id=\\\"ai_wbr_max_candidates\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"1\\\" max=\\\"100\\\" step=\\\"1\\\" />\\r\\n\\r\\n                <label for=\\\"ai_wbr_max_selected\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"前置 AI 或 fallback 最多选择多少条注入本轮 prompt。\\\"></span>最终注入条目数</label>\\r\\n                <input id=\\\"ai_wbr_max_selected\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"1\\\" max=\\\"50\\\" step=\\\"1\\\" />\\r\\n\\r\\n                <label for=\\\"ai_wbr_max_chars\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"最终世界书注入块的字符上限，防止条目过长挤占上下文。\\\"></span>最大注入字符数</label>\\r\\n                <input id=\\\"ai_wbr_max_chars\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"100\\\" max=\\\"30000\\\" step=\\\"100\\\" />\\r\\n\\r\\n                <label for=\\\"ai_wbr_scan_messages\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"用于关键词匹配和 AI 判断的最近聊天消息数量。剧情连续时可适当调高。\\\"></span>扫描最近消息数</label>\\r\\n                <input id=\\\"ai_wbr_scan_messages\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"1\\\" max=\\\"50\\\" step=\\\"1\\\" />\\r\\n\\r\\n                <label for=\\\"ai_wbr_main_history_ai_turns\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"限制主模型正式生成时保留的历史 AI 回复楼数。仅计算 assistant 回复，不计算用户消息；角色卡、破限、系统提示、世界书注入不受影响。填 0 表示不限制。\\\"></span>主模型历史AI楼层数</label>\\r\\n                <input id=\\\"ai_wbr_main_history_ai_turns\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"0\\\" max=\\\"100\\\" step=\\\"1\\\" />\\r\\n\\r\\n                <label for=\\\"ai_wbr_position\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"控制筛选出的世界书块插入 prompt 的位置。通常推荐 In-chat 深度注入。\\\"></span>注入位置</label>\\r\\n                <select id=\\\"ai_wbr_position\\\" class=\\\"text_pole\\\">\\r\\n                    <option value=\\\"1\\\">In-chat 深度注入</option>\\r\\n                    <option value=\\\"0\\\">故事字符串后</option>\\r\\n                    <option value=\\\"2\\\">故事字符串前</option>\\r\\n                </select>\\r\\n\\r\\n                <label for=\\\"ai_wbr_depth\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"In-chat 注入时距离最新消息的深度。数字越小通常越靠近最新上下文，影响越强。\\\"></span>注入深度</label>\\r\\n                <input id=\\\"ai_wbr_depth\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"0\\\" max=\\\"1000\\\" step=\\\"1\\\" />\\r\\n\\r\\n                <label for=\\\"ai_wbr_role\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"In-chat 注入使用的消息角色。一般推荐 system。\\\"></span>In-chat 角色</label>\\r\\n                <select id=\\\"ai_wbr_role\\\" class=\\\"text_pole\\\">\\r\\n                    <option value=\\\"0\\\">system</option>\\r\\n                    <option value=\\\"1\\\">user</option>\\r\\n                    <option value=\\\"2\\\">assistant</option>\\r\\n                </select>\\r\\n\\r\\n                <label for=\\\"ai_wbr_ai_response_length\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"限制前置 AI 输出 JSON 的最大长度。这里只做插件侧保护，不强行卡死在 1024。候选很多或某些接口吞吐保守时可调高。\\\"></span>AI 判定最大回复 tokens</label>\\r\\n                <input id=\\\"ai_wbr_ai_response_length\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"32\\\" max=\\\"16384\\\" step=\\\"16\\\" />\\r\\n\\r\\n                <label for=\\\"ai_wbr_router_retries\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"当前置 AI 调用或 JSON 解析失败时，额外自动重试几次。重试时会抛出反转标记；全部失败时会抛出红色 × 并红光闪三下。\\\"></span>前置 AI 重试次数</label>\\r\\n                <input id=\\\"ai_wbr_router_retries\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"0\\\" max=\\\"5\\\" step=\\\"1\\\" />\\r\\n\\r\\n                <label for=\\\"ai_wbr_router_request_max_tokens\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"前置世界书路由请求实际发送给模型的 max_tokens 上限。独立模型和非独立模型都使用这个值。\\\"></span>前置请求 tokens 上限</label>\\r\\n                <input id=\\\"ai_wbr_router_request_max_tokens\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"32\\\" max=\\\"100000\\\" step=\\\"16\\\" />\\r\\n            </div>\\r\\n\\r\\n            <div class=\\\"m-t-1\\\">\\r\\n                <label for=\\\"ai_wbr_system_prompt\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"约束前置 AI 如何选择条目以及必须输出严格 JSON。通常不用改。\\\"></span>前置 AI 系统提示</label>\\r\\n                <textarea id=\\\"ai_wbr_system_prompt\\\" class=\\\"text_pole textarea_compact ai-wbr-prompt\\\" rows=\\\"5\\\"></textarea>\\r\\n            </div>\\r\\n\\r\\n            <div class=\\\"ai-wbr-section\\\" id=\\\"ai_wbr_memory_section\\\">\\r\\n                <div class=\\\"ai-wbr-section-title\\\">\\r\\n                    <b>记忆存储与整理</b>\\r\\n                </div>\\r\\n                <div class=\\\"ai-wbr-memory-band\\\">\\r\\n                    <div class=\\\"ai-wbr-memory-band-metrics\\\" id=\\\"ai_wbr_memory_band_metrics\\\">\\r\\n                        <span class=\\\"ai-wbr-status\\\" id=\\\"ai_wbr_memory_status\\\">未启用</span>\\r\\n                    </div>\\r\\n                    <div class=\\\"ai-wbr-memory-band-actions\\\">\\r\\n                        <button id=\\\"ai_wbr_memory_run_now\\\" class=\\\"menu_button ai-wbr-memory-band-primary\\\" type=\\\"button\\\">立即实时整理</button>\\r\\n                        <button id=\\\"ai_wbr_memory_summary_now\\\" class=\\\"menu_button ai-wbr-memory-band-primary\\\" type=\\\"button\\\">立即归纳整理</button>\\r\\n                        <button id=\\\"ai_wbr_memory_accept_all\\\" class=\\\"menu_button\\\" type=\\\"button\\\">确认全部更新</button>\\r\\n                        <button id=\\\"ai_wbr_memory_reject_all\\\" class=\\\"menu_button ai-wbr-memory-band-muted\\\" type=\\\"button\\\">清空待确认</button>\\r\\n                        <button id=\\\"ai_wbr_memory_save_json\\\" class=\\\"menu_button ai-wbr-memory-band-muted\\\" type=\\\"button\\\">保存 JSON</button>\\r\\n                        <button id=\\\"ai_wbr_memory_clear\\\" class=\\\"menu_button ai-wbr-memory-band-danger\\\" type=\\\"button\\\">清空记忆</button>\\r\\n                    </div>\\r\\n                </div>\\r\\n                <div class=\\\"ai-wbr-memory-activity\\\">\\r\\n                    <div id=\\\"ai_wbr_memory_dashboard\\\" class=\\\"ai-wbr-memory-dashboard\\\"></div>\\r\\n                    <details class=\\\"ai-wbr-memory-fold ai-wbr-memory-review-fold\\\">\\r\\n                        <summary>\\r\\n                            <span>待确认更新</span>\\r\\n                            <small id=\\\"ai_wbr_memory_review_badge\\\">暂无待确认。</small>\\r\\n                        </summary>\\r\\n                        <div id=\\\"ai_wbr_memory_review_queue\\\" class=\\\"ai-wbr-memory-review-queue ai-wbr-memory-fold-body\\\"></div>\\r\\n                    </details>\\r\\n                </div>\\r\\n                <details class=\\\"ai-wbr-memory-fold ai-wbr-memory-settings-fold\\\">\\r\\n                    <summary>\\r\\n                        <span><i class=\\\"fa-solid fa-toggle-on\\\"></i> 核心开关</span>\\r\\n                        <small>启用记忆、实时整理、归纳和路由参与。</small>\\r\\n                    </summary>\\r\\n                    <div class=\\\"ai-wbr-memory-fold-body\\\">\\r\\n                        <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_memory_enabled\\\">\\r\\n                            <input id=\\\"ai_wbr_memory_enabled\\\" type=\\\"checkbox\\\" />\\r\\n                            <span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"启用后把聊天中长期有价值的信息写入当前聊天/角色对应的记忆容器。\\\"></span>\\r\\n                            <span>启用记忆存储</span>\\r\\n                        </label>\\r\\n                        <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_memory_realtime_enabled\\\">\\r\\n                            <input id=\\\"ai_wbr_memory_realtime_enabled\\\" type=\\\"checkbox\\\" />\\r\\n                            <span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"开启后每轮回复结束都会读取最近几条楼层，实时补充刚发生的事实、状态和关系。\\\"></span>\\r\\n                            <span>启用实时整理（每轮更新）</span>\\r\\n                        </label>\\r\\n                        <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_memory_summary_enabled\\\">\\r\\n                            <input id=\\\"ai_wbr_memory_summary_enabled\\\" type=\\\"checkbox\\\" />\\r\\n                            <span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"开启后按间隔读取更长一段上下文，用于压缩、归纳和修正长期记忆。\\\"></span>\\r\\n                            <span>启用间隔归纳</span>\\r\\n                        </label>\\r\\n                        <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_memory_inject_to_router\\\">\\r\\n                            <input id=\\\"ai_wbr_memory_inject_to_router\\\" type=\\\"checkbox\\\" />\\r\\n                            <span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"把当前记忆状态摘要加入前置世界书路由输入，帮助下一轮更稳定命中相关条目。\\\"></span>\\r\\n                            <span>记忆状态参与世界书路由</span>\\r\\n                        </label>\\r\\n                        <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_memory_review_required\\\">\\r\\n                            <input id=\\\"ai_wbr_memory_review_required\\\" type=\\\"checkbox\\\" />\\r\\n                            <span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"开启后，AI 自动整理出的记忆更新会先进入待确认队列；你确认后才会写入长期记忆图谱。\\\"></span>\\r\\n                            <span>记忆更新需要确认</span>\\r\\n                        </label>\\r\\n                    </div>\\r\\n                </details>\\r\\n\\r\\n                <details class=\\\"ai-wbr-memory-fold\\\">\\r\\n                    <summary>\\r\\n                        <span><i class=\\\"fa-solid fa-sliders\\\"></i> 整理参数</span>\\r\\n                        <small>控制实时、间隔归纳和图谱容量。</small>\\r\\n                    </summary>\\r\\n                    <div class=\\\"ai-wbr-memory-fold-body ai-wbr-grid\\\">\\r\\n                        <label for=\\\"ai_wbr_memory_realtime_scan_messages\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"实时整理读取最近多少条聊天消息。建议 3-6 条，用于每轮快速更新，不承担长期压缩。\\\"></span>实时整理读取消息数</label>\\r\\n                        <input id=\\\"ai_wbr_memory_realtime_scan_messages\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"2\\\" max=\\\"40\\\" step=\\\"1\\\" />\\r\\n\\r\\n                        <label for=\\\"ai_wbr_memory_summary_interval_messages\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"间隔归纳每隔多少条聊天消息触发一次。填 20 表示每 20 条聊天楼层进行一次长期归纳。\\\"></span>间隔归纳消息数</label>\\r\\n                        <input id=\\\"ai_wbr_memory_summary_interval_messages\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"1\\\" max=\\\"100\\\" step=\\\"1\\\" />\\r\\n\\r\\n                        <label for=\\\"ai_wbr_memory_summary_scan_messages\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"间隔归纳读取最近多少条聊天消息。建议比实时整理更大，用于压缩阶段性剧情和关系变化。\\\"></span>间隔归纳读取消息数</label>\\r\\n                        <input id=\\\"ai_wbr_memory_summary_scan_messages\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"2\\\" max=\\\"40\\\" step=\\\"1\\\" />\\r\\n\\r\\n                        <label for=\\\"ai_wbr_memory_max_nodes\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"记忆图谱最多保留多少个节点，超出后优先保留重要度较高、最近更新的节点。\\\"></span>最大记忆节点数</label>\\r\\n                        <input id=\\\"ai_wbr_memory_max_nodes\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"5\\\" max=\\\"200\\\" step=\\\"1\\\" />\\r\\n\\r\\n                        <label for=\\\"ai_wbr_memory_max_links\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"记忆图谱最多保留多少条关系边。\\\"></span>最大记忆关系数</label>\\r\\n                        <input id=\\\"ai_wbr_memory_max_links\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"0\\\" max=\\\"400\\\" step=\\\"1\\\" />\\r\\n\\r\\n                        <label for=\\\"ai_wbr_memory_request_max_tokens\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"后置记忆整理请求实际发送给模型的 max_tokens 上限。普通整理和重试都使用这个值。\\\"></span>后置请求 tokens 上限</label>\\r\\n                        <input id=\\\"ai_wbr_memory_request_max_tokens\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"32\\\" max=\\\"100000\\\" step=\\\"16\\\" />\\r\\n\\r\\n                        <label for=\\\"ai_wbr_memory_retries\\\"><span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"后置记忆整理失败、空回或解析失败时，额外自动重试几次。\\\"></span>后置记忆重试次数</label>\\r\\n                        <input id=\\\"ai_wbr_memory_retries\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"0\\\" max=\\\"10\\\" step=\\\"1\\\" />\\r\\n                    </div>\\r\\n                </details>\\r\\n\\r\\n                <details class=\\\"ai-wbr-memory-fold ai-wbr-bookshelf-fold\\\">\\r\\n                    <summary>\\r\\n                        <span><i class=\\\"fa-solid fa-book-bookmark\\\"></i> 书架向量库</span>\\r\\n                        <small>导入 TXT 或自动记忆书后，可按设置自动构建向量召回。</small>\\n                    </summary>\\r\\n                    <div id=\\\"ai_wbr_bookshelf_panel\\\" class=\\\"ai-wbr-memory-fold-body ai-wbr-bookshelf-panel\\\">\\r\\n                        <div class=\\\"ai-wbr-bookshelf-switches\\\">\\r\\n                            <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_bookshelf_enabled\\\">\\r\\n                                <input id=\\\"ai_wbr_bookshelf_enabled\\\" type=\\\"checkbox\\\" />\\r\\n                                <span>启用书架补充召回</span>\\r\\n                            </label>\\r\\n                            <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_bookshelf_auto_inject\\\">\\r\\n                                <input id=\\\"ai_wbr_bookshelf_auto_inject\\\" type=\\\"checkbox\\\" />\\r\\n                                <span>生成前自动注入向量召回</span>\\r\\n                            </label>\\r\\n                            <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_bookshelf_auto_memory_book\\\">\\n                                <input id=\\\"ai_wbr_bookshelf_auto_memory_book\\\" type=\\\"checkbox\\\" />\\n                                <span>自动维护当前聊天记忆书</span>\\n                            </label>\\n                            <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_bookshelf_auto_memory_book_vectorize\\\">\\n                                <input id=\\\"ai_wbr_bookshelf_auto_memory_book_vectorize\\\" type=\\\"checkbox\\\" />\\n                                <span>记忆书生成后自动向量化</span>\\n                            </label>\\n                            <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_bookshelf_memory_vector\\\">\\n                                <input id=\\\"ai_wbr_bookshelf_memory_vector\\\" type=\\\"checkbox\\\" />\\n                                <span>图谱记忆参与向量召回</span>\\n                            </label>\\r\\n                            <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_bookshelf_only_bound\\\">\\r\\n                                <input id=\\\"ai_wbr_bookshelf_only_bound\\\" type=\\\"checkbox\\\" />\\r\\n                                <span>只使用已绑定书籍</span>\\r\\n                            </label>\\r\\n                            <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_bookshelf_allow_global\\\">\\r\\n                                <input id=\\\"ai_wbr_bookshelf_allow_global\\\" type=\\\"checkbox\\\" />\\r\\n                                <span>允许全局书籍参与召回</span>\\r\\n                            </label>\\r\\n                        </div>\\r\\n\\r\\n                        <div class=\\\"ai-wbr-bookshelf-import\\\">\\r\\n                            <input id=\\\"ai_wbr_bookshelf_file\\\" type=\\\"file\\\" accept=\\\".txt,text/plain\\\" multiple hidden />\\r\\n                            <select id=\\\"ai_wbr_bookshelf_import_type\\\" class=\\\"text_pole\\\">\\r\\n                                <option value=\\\"character\\\">人物档案</option>\\r\\n                                <option value=\\\"world\\\">世界观</option>\\r\\n                                <option value=\\\"plot\\\">剧情记录</option>\\r\\n                                <option value=\\\"rule\\\">规则设定</option>\\r\\n                                <option value=\\\"other\\\">其他资料</option>\\r\\n                            </select>\\r\\n                            <select id=\\\"ai_wbr_bookshelf_import_binding\\\" class=\\\"text_pole\\\">\\r\\n                                <option value=\\\"none\\\">仅保存，不绑定</option>\\r\\n                                <option value=\\\"character\\\">绑定当前角色卡</option>\\r\\n                                <option value=\\\"chat\\\">绑定当前聊天</option>\\r\\n                                <option value=\\\"global\\\">设为全局补充</option>\\r\\n                            </select>\\r\\n                            <button id=\\\"ai_wbr_bookshelf_import\\\" class=\\\"menu_button\\\" type=\\\"button\\\">导入 TXT</button>\\r\\n                        </div>\\r\\n\\r\\n                        <div class=\\\"ai-wbr-grid ai-wbr-bookshelf-config\\\">\\r\\n                            <label for=\\\"ai_wbr_bookshelf_embedding_mode\\\">Embedding 模式</label>\\r\\n                            <select id=\\\"ai_wbr_bookshelf_embedding_mode\\\" class=\\\"text_pole\\\">\\r\\n                                <option value=\\\"api\\\">API 向量模型</option>\\r\\n                                <option value=\\\"browser-local\\\">浏览器本地模型</option>\\r\\n                            </select>\\r\\n                            <label for=\\\"ai_wbr_bookshelf_api_url\\\">API 地址</label>\\r\\n                            <input id=\\\"ai_wbr_bookshelf_api_url\\\" class=\\\"text_pole\\\" type=\\\"text\\\" placeholder=\\\"https://example.com/v1 或 /v1/embeddings\\\" />\\r\\n                            <label for=\\\"ai_wbr_bookshelf_api_key\\\">API Key</label>\\r\\n                            <input id=\\\"ai_wbr_bookshelf_api_key\\\" class=\\\"text_pole\\\" type=\\\"password\\\" placeholder=\\\"sk-...\\\" />\\r\\n                            <label for=\\\"ai_wbr_bookshelf_api_model\\\">API 模型名</label>\\r\\n                            <input id=\\\"ai_wbr_bookshelf_api_model\\\" class=\\\"text_pole\\\" type=\\\"text\\\" placeholder=\\\"text-embedding-3-small\\\" />\\r\\n                            <label for=\\\"ai_wbr_bookshelf_local_model\\\">本地模型 ID</label>\\r\\n                            <input id=\\\"ai_wbr_bookshelf_local_model\\\" class=\\\"text_pole\\\" type=\\\"text\\\" placeholder=\\\"Xenova/paraphrase-multilingual-MiniLM-L12-v2\\\" />\\r\\n                            <label for=\\\"ai_wbr_bookshelf_memory_vector_max\\\">图谱召回数量</label>\\r\\n                            <input id=\\\"ai_wbr_bookshelf_memory_vector_max\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"1\\\" max=\\\"12\\\" step=\\\"1\\\" />\\r\\n                            <label for=\\\"ai_wbr_bookshelf_max_chunks\\\">TXT 召回数量</label>\\r\\n                            <input id=\\\"ai_wbr_bookshelf_max_chunks\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"1\\\" max=\\\"12\\\" step=\\\"1\\\" />\\r\\n                            <label for=\\\"ai_wbr_bookshelf_max_chars\\\">每段最大字数</label>\\r\\n                            <input id=\\\"ai_wbr_bookshelf_max_chars\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"120\\\" max=\\\"2000\\\" step=\\\"20\\\" />\\r\\n                            <label for=\\\"ai_wbr_bookshelf_min_score\\\">最低相似度</label>\\r\\n                            <input id=\\\"ai_wbr_bookshelf_min_score\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"0\\\" max=\\\"1\\\" step=\\\"0.05\\\" />\\r\\n                        </div>\\r\\n                        <div class=\\\"ai-wbr-bookshelf-actions\\\">\\r\\n                            <button id=\\\"ai_wbr_bookshelf_test_provider\\\" class=\\\"menu_button\\\" type=\\\"button\\\">测试向量模型</button>\\r\\n                            <button id=\\\"ai_wbr_bookshelf_load_local\\\" class=\\\"menu_button\\\" type=\\\"button\\\">下载/加载本地模型</button>\\r\\n                            <button id=\\\"ai_wbr_bookshelf_build_memory_books\\\" class=\\\"menu_button\\\" type=\\\"button\\\">刷新当前聊天记忆书</button>\\r\\n                            <button id=\\\"ai_wbr_bookshelf_vectorize_memory\\\" class=\\\"menu_button\\\" type=\\\"button\\\">同步图谱向量</button>\\r\\n                            <button id=\\\"ai_wbr_bookshelf_reset_memory_vectors\\\" class=\\\"menu_button\\\" type=\\\"button\\\">重置图谱向量</button>\\r\\n                            <span id=\\\"ai_wbr_bookshelf_model_status\\\" class=\\\"ai-wbr-status\\\">未测试</span>\\r\\n                        </div>\\r\\n\\r\\n                        <div id=\\\"ai_wbr_bookshelf_status\\\" class=\\\"ai-wbr-memory-history-status\\\">书架待命。记忆书生成后会按设置自动向量化；TXT 导入仍可在书籍详情中手动向量化。</div>\\n\\r\\n                        <div class=\\\"ai-wbr-bookshelf-layout\\\">\\r\\n                            <section>\\r\\n                                <div class=\\\"ai-wbr-memory-subtitle\\\"><b>绑定范围</b></div>\\r\\n                                <div id=\\\"ai_wbr_bookshelf_scope\\\" class=\\\"ai-wbr-bookshelf-scope\\\"></div>\\r\\n                            </section>\\r\\n                            <section>\\r\\n                                <div class=\\\"ai-wbr-memory-subtitle\\\"><b>书籍列表</b></div>\\r\\n                                <div id=\\\"ai_wbr_bookshelf_books\\\" class=\\\"ai-wbr-bookshelf-books\\\"></div>\\r\\n                            </section>\\r\\n                            <section>\\r\\n                                <div class=\\\"ai-wbr-memory-subtitle\\\"><b>书籍详情</b></div>\\r\\n                                <div id=\\\"ai_wbr_bookshelf_detail\\\" class=\\\"ai-wbr-bookshelf-detail\\\"></div>\\r\\n                            </section>\\r\\n                        </div>\\r\\n\\r\\n                        <div class=\\\"ai-wbr-bookshelf-test\\\">\\r\\n                            <div class=\\\"ai-wbr-memory-subtitle\\\"><b>召回测试</b></div>\\r\\n                            <textarea id=\\\"ai_wbr_bookshelf_test_query\\\" class=\\\"text_pole\\\" rows=\\\"3\\\" placeholder=\\\"输入一句当前剧情问题，测试绑定书籍会召回哪些片段。\\\"></textarea>\\r\\n                            <div class=\\\"ai-wbr-bookshelf-actions\\\">\\r\\n                                <button id=\\\"ai_wbr_bookshelf_test\\\" class=\\\"menu_button\\\" type=\\\"button\\\">测试召回</button>\\r\\n                            </div>\\r\\n                            <div id=\\\"ai_wbr_bookshelf_results\\\" class=\\\"ai-wbr-bookshelf-results\\\"></div>\\r\\n                        </div>\\r\\n                    </div>\\r\\n                </details>\\r\\n\\r\\n                <details class=\\\"ai-wbr-memory-fold\\\">\\r\\n                    <summary>\\r\\n                        <span><i class=\\\"fa-solid fa-clock-rotate-left\\\"></i> 历史聊天补录</span>\\r\\n                        <small>把已经存在的旧楼层按范围整理进当前记忆。</small>\\r\\n                    </summary>\\r\\n                    <div class=\\\"ai-wbr-memory-fold-body ai-wbr-memory-history-import\\\">\\r\\n                    <div class=\\\"ai-wbr-memory-history-grid\\\">\\r\\n                        <label for=\\\"ai_wbr_memory_history_start_floor\\\">起始楼层</label>\\r\\n                        <input id=\\\"ai_wbr_memory_history_start_floor\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"1\\\" step=\\\"1\\\" placeholder=\\\"1\\\" />\\r\\n                        <label for=\\\"ai_wbr_memory_history_end_floor\\\">结束楼层</label>\\r\\n                        <input id=\\\"ai_wbr_memory_history_end_floor\\\" class=\\\"text_pole\\\" type=\\\"number\\\" min=\\\"1\\\" step=\\\"1\\\" placeholder=\\\"当前最新\\\" />\\r\\n                        <label for=\\\"ai_wbr_memory_history_mode\\\">整理方式</label>\\r\\n                        <select id=\\\"ai_wbr_memory_history_mode\\\" class=\\\"text_pole\\\">\\r\\n                            <option value=\\\"history\\\">补录为长期记忆</option>\\r\\n                            <option value=\\\"summary\\\">补录为阶段摘要</option>\\r\\n                        </select>\\r\\n                    </div>\\r\\n                    <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_memory_history_skip_done\\\">\\r\\n                        <input id=\\\"ai_wbr_memory_history_skip_done\\\" type=\\\"checkbox\\\" />\\r\\n                        <span>跳过已补录过的楼层范围</span>\\r\\n                    </label>\\r\\n                    <div class=\\\"ai-wbr-memory-history-actions\\\">\\r\\n                        <button class=\\\"menu_button\\\" type=\\\"button\\\" data-ai-wbr-history-range=\\\"all\\\">全部聊天</button>\\r\\n                        <button class=\\\"menu_button\\\" type=\\\"button\\\" data-ai-wbr-history-range=\\\"20\\\">最近 20 层</button>\\r\\n                        <button class=\\\"menu_button\\\" type=\\\"button\\\" data-ai-wbr-history-range=\\\"50\\\">最近 50 层</button>\\r\\n                        <button id=\\\"ai_wbr_memory_history_preview\\\" class=\\\"menu_button\\\" type=\\\"button\\\">预览范围</button>\\r\\n                        <button id=\\\"ai_wbr_memory_history_import\\\" class=\\\"menu_button\\\" type=\\\"button\\\">整理写入</button>\\r\\n                    </div>\\r\\n                    <div id=\\\"ai_wbr_memory_history_status\\\" class=\\\"ai-wbr-memory-history-status\\\">尚未选择楼层范围。</div>\\r\\n                    <div id=\\\"ai_wbr_memory_history_preview_box\\\" class=\\\"ai-wbr-memory-history-preview\\\"></div>\\r\\n                    </div>\\r\\n                </details>\\r\\n\\r\\n                <details class=\\\"ai-wbr-memory-fold\\\">\\r\\n                    <summary>\\r\\n                        <span><i class=\\\"fa-solid fa-table-list\\\"></i> 状态字段</span>\\r\\n                        <small>编辑当前位置、目标、阶段和自定义状态。</small>\\r\\n                    </summary>\\r\\n                    <div id=\\\"ai_wbr_memory_state_editor\\\" class=\\\"ai-wbr-memory-state-editor ai-wbr-memory-fold-body\\\"></div>\\r\\n                </details>\\r\\n\\r\\n                <details class=\\\"ai-wbr-memory-fold\\\">\\r\\n                    <summary>\\r\\n                        <span><i class=\\\"fa-solid fa-code\\\"></i> 高级 JSON</span>\\r\\n                        <small>直接查看或手动保存记忆图谱 JSON。</small>\\r\\n                    </summary>\\r\\n                    <div class=\\\"ai-wbr-router-raw-block ai-wbr-memory-fold-body\\\">\\r\\n                        <div><b>记忆图谱 JSON</b></div>\\r\\n                        <textarea id=\\\"ai_wbr_memory_json\\\" class=\\\"text_pole ai-wbr-memory-json\\\" rows=\\\"9\\\"></textarea>\\r\\n                    </div>\\r\\n                </details>\\r\\n\\r\\n                <details class=\\\"ai-wbr-memory-fold ai-wbr-memory-debug-fold\\\">\\r\\n                    <summary>\\r\\n                        <span><i class=\\\"fa-solid fa-bug\\\"></i> 调试信息</span>\\r\\n                        <small>后置记忆 + 前置 AI 的 Prompt、返回与解析错误。</small>\\r\\n                    </summary>\\r\\n                    <div id=\\\"ai_wbr_memory_debug_panel\\\" class=\\\"ai-wbr-memory-debug-panel ai-wbr-memory-fold-body\\\">\\r\\n                        <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_memory_debug\\\">\\r\\n                            <input id=\\\"ai_wbr_memory_debug\\\" type=\\\"checkbox\\\" />\\r\\n                            <span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"显示后置记忆整理发给 AI 的 Prompt、AI 原始返回和解析错误，方便排查。\\\"></span>\\r\\n                            <span>记忆调试模式</span>\\r\\n                        </label>\\r\\n                        <label class=\\\"checkbox_label\\\" for=\\\"ai_wbr_memory_scope_debug\\\">\\r\\n                            <input id=\\\"ai_wbr_memory_scope_debug\\\" type=\\\"checkbox\\\" />\\r\\n                            <span class=\\\"fa-solid fa-circle-info ai-wbr-help\\\" title=\\\"在浏览器控制台输出切卡/切聊天、读取记忆、保存记忆和面板重绘的详细日志。\\\"></span>\\r\\n                            <span>记忆作用域控制台调试</span>\\r\\n                        </label>\\r\\n                    <div class=\\\"ai-wbr-router-raw-block ai-wbr-memory-raw-block\\\">\\r\\n                        <div><b>后置记忆 Prompt</b></div>\\r\\n                        <pre id=\\\"ai_wbr_memory_prompt\\\" class=\\\"ai-wbr-router-raw\\\">尚无后置记忆 Prompt</pre>\\r\\n                    </div>\\r\\n                    <div class=\\\"ai-wbr-router-raw-block ai-wbr-memory-raw-block\\\">\\r\\n                        <div><b>后置记忆 AI 原始返回</b></div>\\r\\n                        <pre id=\\\"ai_wbr_memory_raw\\\" class=\\\"ai-wbr-router-raw\\\">尚无后置记忆返回</pre>\\r\\n                    </div>\\r\\n                    <div class=\\\"ai-wbr-router-raw-block ai-wbr-memory-raw-block\\\">\\r\\n                        <div><b>后置记忆解析错误</b></div>\\r\\n                        <pre id=\\\"ai_wbr_memory_error\\\" class=\\\"ai-wbr-router-raw\\\">尚无错误</pre>\\r\\n                    </div>\\r\\n\\r\\n                    <div class=\\\"ai-wbr-memory-debug-divider\\\"></div>\\r\\n                    <div class=\\\"ai-wbr-memory-debug-group\\\">前置 AI 与注入</div>\\r\\n                    <div><b>最近一次选择</b></div>\\r\\n                    <div id=\\\"ai_wbr_last_summary\\\" class=\\\"menu_button disabled\\\">尚无路由记录</div>\\r\\n                    <div id=\\\"ai_wbr_last_items\\\" class=\\\"ai-wbr-last-items\\\"></div>\\r\\n                    <div class=\\\"ai-wbr-router-raw-block\\\">\\r\\n                        <div><b>本轮最终注入文本</b></div>\\r\\n                        <pre id=\\\"ai_wbr_injection_text\\\" class=\\\"ai-wbr-router-raw\\\">尚无本轮注入记录</pre>\\r\\n                    </div>\\r\\n                    <div class=\\\"ai-wbr-router-raw-block\\\">\\r\\n                        <div><b>发给前置 AI 的 Prompt</b></div>\\r\\n                        <pre id=\\\"ai_wbr_router_prompt\\\" class=\\\"ai-wbr-router-raw\\\">尚无前置 AI Prompt 记录</pre>\\r\\n                    </div>\\r\\n                    <div class=\\\"ai-wbr-router-raw-block\\\">\\r\\n                        <div><b>前置 AI 返回</b></div>\\r\\n                        <pre id=\\\"ai_wbr_router_raw\\\" class=\\\"ai-wbr-router-raw\\\">尚无前置 AI 返回记录</pre>\\r\\n                    </div>\\r\\n                    </div>\\r\\n                </details>\\r\\n            </div>\\r\\n\\r\\n            <div class=\\\"ai-wbr-section ai-wbr-graph-section ai-wbr-graph-workspace\\\" id=\\\"ai_wbr_memory_graph_section\\\">\\r\\n                <div class=\\\"ai-wbr-graph-shell\\\">\\r\\n                    <aside id=\\\"ai_wbr_memory_preview_panel\\\" class=\\\"ai-wbr-memory-preview-panel\\\"></aside>\\r\\n                    <div id=\\\"ai_wbr_memory_graph\\\" class=\\\"ai-wbr-memory-graph ai-wbr-memory-graph-large\\\"></div>\\r\\n                    <button class=\\\"menu_button ai-wbr-graph-fab-fit ai-wbr-graph-fit-trigger\\\" type=\\\"button\\\" title=\\\"适配视图\\\"><i class=\\\"fa-solid fa-expand\\\"></i></button>\\r\\n                    <aside id=\\\"ai_wbr_memory_detail_drawer\\\" class=\\\"ai-wbr-memory-detail-drawer\\\">\\r\\n                        <div class=\\\"ai-wbr-memory-detail-empty\\\">点击图谱中的记忆卡片或关系线查看详情。</div>\\r\\n                    </aside>\\r\\n                </div>\\r\\n                <div id=\\\"ai_wbr_memory_node_editor\\\" class=\\\"ai-wbr-memory-node-editor ai-wbr-hidden-editor\\\"></div>\\r\\n                <div id=\\\"ai_wbr_memory_edge_editor\\\" class=\\\"ai-wbr-memory-edge-editor ai-wbr-hidden-editor\\\"></div>\\r\\n                <div id=\\\"ai_wbr_memory_nodes\\\" class=\\\"ai-wbr-memory-list ai-wbr-graph-list-collapsed\\\"></div>\\r\\n                <div id=\\\"ai_wbr_memory_links\\\" class=\\\"ai-wbr-memory-list ai-wbr-graph-list-collapsed\\\"></div>\\r\\n            </div>\\r\\n\\r\\n        </div>\\r\\n    </div>\\r\\n</div>\\r\\n\";\nconst AI_WBR_STYLE_CSS = \".ai-wbr-fab {\\n    position: fixed;\\n    right: 1.2rem;\\n    bottom: calc(1.2rem + env(safe-area-inset-bottom, 0px));\\n    width: 3.2rem;\\r\\n    height: 3.2rem;\\r\\n    border-radius: 999px;\\r\\n    background:\\r\\n        linear-gradient(135deg, rgba(85, 133, 201, 0.28), rgba(123, 216, 255, 0.16)),\\r\\n        color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(15, 17, 22, 0.82)) 86%, transparent 14%);\\r\\n    border: 1px solid rgba(176, 225, 255, 0.42);\\r\\n    box-shadow:\\r\\n        0 10px 24px rgba(0, 0, 0, 0.2),\\r\\n        0 0 14px rgba(125, 212, 255, 0.18);\\r\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 88%, #d7f5ff 12%);\\r\\n    display: flex;\\r\\n    align-items: center;\\r\\n    justify-content: center;\\r\\n    font-size: 1.4rem;\\r\\n    z-index: 999999;\\n    cursor: pointer;\\n    touch-action: none;\\n    -webkit-tap-highlight-color: transparent;\\n    user-select: none;\\n    backdrop-filter: blur(8px);\\n    transition: transform 0.25s cubic-bezier(0.2, 0.9, 0.25, 1.15), box-shadow 0.25s ease, opacity 0.3s ease;\\r\\n    opacity: 0;\\r\\n    transform: translateY(12px);\\r\\n    animation: ai-wbr-fab-enter 0.4s cubic-bezier(0.2, 0.9, 0.25, 1.15) 0.3s forwards;\\r\\n}\\r\\n\\r\\n.ai-wbr-fab:hover {\\r\\n    transform: scale(1.08);\\r\\n    box-shadow:\\r\\n        0 12px 28px rgba(0, 0, 0, 0.3),\\r\\n        0 0 20px rgba(125, 212, 255, 0.35);\\r\\n}\\r\\n\\r\\n.ai-wbr-fab:active {\\n    transform: scale(0.95);\\n}\\n\\n.ai-wbr-fab.idle {\\n    opacity: 0.72;\\n}\\n\\n.ai-wbr-fab.ready,\\n.ai-wbr-fab.active {\\n    border-color: rgba(125, 212, 255, 0.62);\\n}\\n\\n.ai-wbr-fab.ok {\\n    border-color: rgba(91, 214, 143, 0.72);\\n    box-shadow: 0 12px 28px rgba(0, 0, 0, 0.3), 0 0 22px rgba(91, 214, 143, 0.26);\\n}\\n\\n.ai-wbr-fab.warn {\\n    border-color: rgba(255, 196, 87, 0.74);\\n    box-shadow: 0 12px 28px rgba(0, 0, 0, 0.3), 0 0 22px rgba(255, 196, 87, 0.24);\\n}\\n\\n.ai-wbr-fab.error {\\n    border-color: rgba(255, 100, 100, 0.76);\\n    box-shadow: 0 12px 28px rgba(0, 0, 0, 0.3), 0 0 22px rgba(255, 100, 100, 0.28);\\n}\\n\\n.ai-wbr-fab.ai-wbr-fab-runtime {\\n    overflow: visible;\\n    opacity: 1;\\n    transform: none;\\n}\\n\\n.ai-wbr-fab.ai-wbr-fab-runtime.running {\\n    border-color: rgba(125, 212, 255, 0.78);\\n    box-shadow:\\n        0 12px 30px rgba(0, 0, 0, 0.34),\\n        0 0 0 0 rgba(125, 212, 255, 0.42),\\n        0 0 24px rgba(125, 212, 255, 0.34);\\n    animation: ai-wbr-fab-runtime-pulse 1.25s ease-in-out infinite;\\n}\\n\\n.ai-wbr-fab.ai-wbr-fab-runtime.success {\\n    border-color: rgba(91, 214, 143, 0.78);\\n    box-shadow: 0 12px 28px rgba(0, 0, 0, 0.3), 0 0 24px rgba(91, 214, 143, 0.3);\\n}\\n\\n.ai-wbr-fab.ai-wbr-fab-runtime.failed {\\n    border-color: rgba(255, 100, 100, 0.8);\\n    box-shadow: 0 12px 28px rgba(0, 0, 0, 0.3), 0 0 24px rgba(255, 100, 100, 0.32);\\n}\\n\\n.ai-wbr-fab.ai-wbr-fab-runtime[data-runtime-label]::after {\\n    content: attr(data-runtime-label);\\n    position: absolute;\\n    right: 0;\\n    bottom: calc(100% + 0.25rem);\\n    max-width: 5.8rem;\\n    padding: 0.12rem 0.38rem;\\n    border-radius: 999px;\\n    border: 1px solid rgba(176, 225, 255, 0.28);\\n    background: rgba(10, 15, 24, 0.88);\\n    color: #d9f6ff;\\n    font-size: 0.68rem;\\n    line-height: 1.25;\\n    white-space: nowrap;\\n    pointer-events: none;\\n    box-shadow: 0 6px 14px rgba(0, 0, 0, 0.26);\\n}\\n\\n@keyframes ai-wbr-fab-enter {\\n    to {\\n        opacity: 1;\\n        transform: translateY(0);\\n    }\\n}\\n\\n@keyframes ai-wbr-fab-runtime-pulse {\\n    0% {\\n        box-shadow:\\n            0 12px 30px rgba(0, 0, 0, 0.34),\\n            0 0 0 0 rgba(125, 212, 255, 0.38),\\n            0 0 22px rgba(125, 212, 255, 0.3);\\n    }\\n    70% {\\n        box-shadow:\\n            0 12px 30px rgba(0, 0, 0, 0.34),\\n            0 0 0 0.55rem rgba(125, 212, 255, 0),\\n            0 0 22px rgba(125, 212, 255, 0.22);\\n    }\\n    100% {\\n        box-shadow:\\n            0 12px 30px rgba(0, 0, 0, 0.34),\\n            0 0 0 0 rgba(125, 212, 255, 0),\\n            0 0 22px rgba(125, 212, 255, 0.3);\\n    }\\n}\\n\\r\\n.ai-wbr-floating-window {\\n    position: fixed;\\n    bottom: 5.5rem;\\n    right: 1.2rem;\\r\\n    width: clamp(20rem, 92vw, 46rem);\\r\\n    height: clamp(24rem, 88vh, 80vh);\\r\\n    background:\\r\\n        radial-gradient(circle at 50% 35%, rgba(92, 130, 255, 0.08), transparent 48%),\\r\\n        radial-gradient(circle at 32% 24%, rgba(58, 228, 193, 0.05), transparent 34%),\\r\\n        color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(10, 12, 20, 0.96)) 96%, #07101f 4%);\\r\\n    border: 1px solid rgba(176, 225, 255, 0.35);\\r\\n    border-radius: 14px;\\r\\n    box-shadow:\\r\\n        0 20px 40px rgba(0, 0, 0, 0.4),\\r\\n        0 0 24px rgba(125, 212, 255, 0.12);\\r\\n    backdrop-filter: blur(12px);\\r\\n    z-index: 1000000;\\n    display: flex;\\r\\n    flex-direction: column;\\r\\n    overflow: hidden;\\r\\n    /* initial state for open animation */\\r\\n    opacity: 0;\\r\\n    transform: scale(0.92) translateY(8px);\\n    visibility: hidden;\\n}\\n\\n.ai-wbr-floating-window.ai-wbr-floating-window-graph {\\n    width: min(96vw, 72rem);\\n    height: min(90dvh, 48rem);\\n}\\n\\n.ai-wbr-floating-window.open {\\n    visibility: visible;\\n    animation: ai-wbr-window-open 0.32s cubic-bezier(0.2, 0.9, 0.25, 1.15) forwards;\\n}\\n\\n.ai-wbr-floating-window.open[style] {\\n    pointer-events: auto;\\n}\\n\\r\\n.ai-wbr-floating-window.closing {\\r\\n    animation: ai-wbr-window-close 0.22s cubic-bezier(0.4, 0, 0.6, 1) forwards;\\r\\n    pointer-events: none;\\r\\n}\\r\\n\\r\\n@keyframes ai-wbr-window-open {\\r\\n    from {\\r\\n        opacity: 0;\\r\\n        transform: scale(0.92) translateY(8px);\\r\\n    }\\r\\n    to {\\r\\n        opacity: 1;\\r\\n        transform: scale(1) translateY(0);\\r\\n    }\\r\\n}\\r\\n\\r\\n@keyframes ai-wbr-window-close {\\r\\n    from {\\r\\n        opacity: 1;\\r\\n        transform: scale(1);\\r\\n    }\\r\\n    to {\\r\\n        opacity: 0;\\r\\n        transform: scale(0.94);\\r\\n    }\\r\\n}\\r\\n\\r\\n.ai-wbr-floating-header {\\r\\n    display: flex;\\r\\n    align-items: center;\\r\\n    justify-content: space-between;\\r\\n    padding: 0.75rem 1rem;\\r\\n    border-bottom: 1px solid rgba(255, 255, 255, 0.08);\\r\\n    background: rgba(0, 0, 0, 0.15);\\r\\n    cursor: grab;\\r\\n    user-select: none;\\r\\n}\\r\\n\\r\\n.ai-wbr-floating-header:active {\\r\\n    cursor: grabbing;\\r\\n}\\r\\n\\r\\n.ai-wbr-floating-title {\\n    font-weight: 600;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 90%, #a4e1ff 10%);\\n    display: flex;\\n    align-items: center;\\n    gap: 0.5rem;\\r\\n}\\r\\n\\r\\n.ai-wbr-floating-title i {\\r\\n    opacity: 0.7;\\r\\n}\\r\\n\\r\\n.ai-wbr-floating-close {\\r\\n    cursor: pointer;\\r\\n    opacity: 0.7;\\r\\n    transition: opacity 0.2s, transform 0.2s;\\r\\n    width: 24px;\\r\\n    height: 24px;\\r\\n    display: flex;\\r\\n    align-items: center;\\r\\n    justify-content: center;\\r\\n    border-radius: 4px;\\r\\n}\\r\\n\\r\\n.ai-wbr-floating-close:hover {\\r\\n    opacity: 1;\\r\\n    background: rgba(255, 255, 255, 0.1);\\r\\n}\\r\\n\\r\\n.ai-wbr-floating-close:active {\\r\\n    transform: scale(0.9);\\r\\n}\\r\\n\\r\\n.ai-wbr-floating-content {\\n    flex: 1;\\n    min-height: 0;\\n    overflow-y: auto;\\n    padding: 1rem;\\n    scrollbar-width: thin;\\n    scrollbar-color: rgba(255, 255, 255, 0.2) transparent;\\n}\\n\\n.ai-wbr-floating-window-graph .ai-wbr-floating-content {\\n    padding: 0;\\n    overflow: hidden;\\n}\\n\\n.ai-wbr-floating-content::-webkit-scrollbar {\\r\\n    width: 6px;\\r\\n}\\r\\n\\r\\n.ai-wbr-floating-content::-webkit-scrollbar-thumb {\\r\\n    background-color: rgba(255, 255, 255, 0.2);\\r\\n    border-radius: 3px;\\r\\n}\\r\\n\\r\\n/* Adjust memory section inside floating window to fit nicely without outer border */\\r\\n.ai-wbr-floating-content .ai-wbr-section {\\n    border: none;\\n    background: transparent;\\n    padding: 0;\\n    margin: 0;\\n}\\n\\n.ai-wbr-console-status {\\n    display: inline-flex;\\n    align-items: center;\\n    padding: 0.12rem 0.45rem;\\n    border-radius: 999px;\\n    font-size: 0.72rem;\\n    font-weight: 500;\\n    background: rgba(255, 255, 255, 0.08);\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 72%, #b9eaff 28%);\\n}\\n\\n.ai-wbr-console-status.ok,\\n.ai-wbr-console-status.ready {\\n    color: #b9f4d1;\\n    background: rgba(91, 214, 143, 0.12);\\n}\\n\\n.ai-wbr-console-status.warn {\\n    color: #ffe0a3;\\n    background: rgba(255, 196, 87, 0.12);\\n}\\n\\n.ai-wbr-console-status.error {\\n    color: #ffb9b9;\\n    background: rgba(255, 100, 100, 0.14);\\n}\\n\\n.ai-wbr-console-status.running {\\n    color: #d9f6ff;\\n    background: rgba(125, 212, 255, 0.13);\\n}\\n\\n.ai-wbr-console-status.success {\\n    color: #b9f4d1;\\n    background: rgba(91, 214, 143, 0.13);\\n}\\n\\n.ai-wbr-console-status.failed {\\n    color: #ffb9b9;\\n    background: rgba(255, 100, 100, 0.14);\\n}\\n\\n.ai-wbr-console-tabs {\\n    display: flex;\\n    align-items: center;\\n    gap: 0.25rem;\\n    padding: 0.45rem 0.65rem;\\n    border-bottom: 1px solid rgba(255, 255, 255, 0.08);\\n    background: rgba(0, 0, 0, 0.1);\\n    overflow-x: auto;\\n}\\n\\n.ai-wbr-console-tab {\\n    border: 1px solid transparent;\\n    border-radius: 6px;\\n    background: transparent;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 74%, transparent 26%);\\n    padding: 0.32rem 0.55rem;\\n    white-space: nowrap;\\n    cursor: pointer;\\n}\\n\\n.ai-wbr-console-tab:hover {\\n    background: rgba(255, 255, 255, 0.08);\\n}\\n\\n.ai-wbr-console-tab.active {\\n    border-color: rgba(176, 225, 255, 0.34);\\n    background: rgba(125, 212, 255, 0.13);\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 90%, #bfeeff 10%);\\n}\\n\\n.ai-wbr-console-body {\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.85rem;\\n    min-height: 0;\\n}\\n\\n.ai-wbr-console-body-graph {\\n    gap: 0;\\n    height: 100%;\\n    min-height: 0;\\n}\\n\\n.ai-wbr-console-parking {\\n    display: none !important;\\n}\\n\\n.ai-wbr-console-hero {\\n    display: flex;\\n    align-items: flex-start;\\n    justify-content: space-between;\\n    gap: 1rem;\\n    padding: 0.9rem;\\n    border: 1px solid rgba(176, 225, 255, 0.18);\\n    border-radius: 10px;\\n    background: rgba(255, 255, 255, 0.045);\\n}\\n\\n.ai-wbr-console-kicker {\\n    font-size: 0.72rem;\\n    text-transform: uppercase;\\n    opacity: 0.7;\\n    letter-spacing: 0.04em;\\n}\\n\\n.ai-wbr-console-hero h3 {\\n    margin: 0.16rem 0 0.32rem;\\n    font-size: 1.05rem;\\n}\\n\\n.ai-wbr-console-hero p {\\n    margin: 0;\\n    opacity: 0.78;\\n    line-height: 1.45;\\n}\\n\\n.ai-wbr-console-status-pill {\\n    display: inline-flex;\\n    align-items: center;\\n    gap: 0.35rem;\\n    padding: 0.38rem 0.62rem;\\n    border-radius: 999px;\\n    background: rgba(125, 212, 255, 0.12);\\n    color: #d7f5ff;\\n    white-space: nowrap;\\n}\\n\\n.ai-wbr-console-status-pill.ok {\\n    background: rgba(91, 214, 143, 0.14);\\n    color: #b9f4d1;\\n}\\n\\n.ai-wbr-console-status-pill.warn {\\n    background: rgba(255, 196, 87, 0.14);\\n    color: #ffe0a3;\\n}\\n\\n.ai-wbr-console-status-pill.error {\\n    background: rgba(255, 100, 100, 0.16);\\n    color: #ffb9b9;\\n}\\n\\n.ai-wbr-console-status-pill.idle {\\n    background: rgba(255, 255, 255, 0.08);\\n    color: rgba(255, 255, 255, 0.68);\\n}\\n\\n.ai-wbr-console-stats {\\n    display: grid;\\n    grid-template-columns: repeat(auto-fit, minmax(7.5rem, 1fr));\\n    gap: 0.55rem;\\n}\\n\\n.ai-wbr-console-stat,\\n.ai-wbr-console-entry,\\n.ai-wbr-console-empty,\\n.ai-wbr-console-alert {\\n    border: 1px solid rgba(176, 225, 255, 0.16);\\n    border-radius: 8px;\\n    background: rgba(255, 255, 255, 0.045);\\n}\\n\\n.ai-wbr-console-stat {\\n    padding: 0.72rem;\\n}\\n\\n.ai-wbr-console-stat-value {\\n    font-size: 1.1rem;\\n    font-weight: 700;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 90%, #bfeeff 10%);\\n}\\n\\n.ai-wbr-console-stat-label {\\n    margin-top: 0.18rem;\\n    font-size: 0.76rem;\\n    opacity: 0.68;\\n}\\n\\n.ai-wbr-console-actions,\\n.ai-wbr-console-section-head {\\n    display: flex;\\n    align-items: center;\\n    justify-content: space-between;\\n    gap: 0.55rem;\\n    flex-wrap: wrap;\\n}\\n\\n.ai-wbr-console-section-title {\\n    font-weight: 700;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 88%, #bfeeff 12%);\\n}\\n\\n.ai-wbr-console-entry-list {\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.55rem;\\n}\\n\\n.ai-wbr-console-entry {\\n    padding: 0.72rem;\\n}\\n\\n.ai-wbr-console-entry.selected {\\n    border-color: rgba(91, 214, 143, 0.36);\\n    background: rgba(91, 214, 143, 0.07);\\n}\\n\\n.ai-wbr-console-entry-head {\\n    display: flex;\\n    align-items: center;\\n    justify-content: space-between;\\n    gap: 0.75rem;\\n}\\n\\n.ai-wbr-console-entry-head span {\\n    font-size: 0.72rem;\\n    opacity: 0.75;\\n}\\n\\n.ai-wbr-console-entry-meta,\\n.ai-wbr-console-entry-keys,\\n.ai-wbr-console-entry small {\\n    display: block;\\n    margin-top: 0.25rem;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 64%, transparent 36%);\\n    font-size: 0.78rem;\\n}\\n\\n.ai-wbr-console-entry p {\\n    margin: 0.45rem 0 0;\\n    line-height: 1.45;\\n    opacity: 0.82;\\n}\\n\\n.ai-wbr-console-empty,\\n.ai-wbr-console-alert {\\n    padding: 0.9rem;\\n    opacity: 0.82;\\n}\\n\\n.ai-wbr-console-alert.error {\\n    border-color: rgba(255, 100, 100, 0.34);\\n    background: rgba(255, 100, 100, 0.08);\\n    color: #ffb9b9;\\n}\\n\\n.ai-wbr-console-pre {\\n    max-height: 48vh;\\n    overflow: auto;\\n    white-space: pre-wrap;\\n    word-break: break-word;\\n    padding: 0.85rem;\\n    border: 1px solid rgba(176, 225, 255, 0.16);\\n    border-radius: 8px;\\n    background: rgba(0, 0, 0, 0.18);\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 86%, #d7f5ff 14%);\\n}\\n\\n.ai-wbr-console-pre.error {\\n    color: #ffb9b9;\\n}\\n\\n.ai-wbr-console-form {\\n    display: grid;\\n    grid-template-columns: minmax(7rem, 0.4fr) minmax(12rem, 1fr);\\n    gap: 0.65rem 0.75rem;\\n    align-items: center;\\n}\\n\\n.ai-wbr-console-form .checkbox_label,\\n.ai-wbr-console-form .ai-wbr-console-actions {\\n    grid-column: 1 / -1;\\n}\\n\\r\\n@media (max-width: 640px) {\\n    .ai-wbr-fab {\\n        right: 0.85rem;\\n        bottom: calc(5rem + env(safe-area-inset-bottom, 0px));\\n    }\\n\\n    .ai-wbr-floating-window {\\n        width: calc(100vw - 1rem);\\n        height: min(82dvh, 40rem);\\n        right: 0.5rem;\\n        bottom: calc(4.5rem + env(safe-area-inset-bottom, 0px));\\n        border-radius: 12px;\\n    }\\n}\\n\\r\\n.ai-worldbook-router-settings .inline-drawer-content {\\r\\n    padding: 0.75rem;\\r\\n    display: flex;\\r\\n    flex-direction: column;\\r\\n    gap: 0.5rem;\\r\\n}\\r\\n\\r\\n/* 统一修复按钮竖排问题 */\\r\\n.ai-worldbook-router-settings .menu_button {\\r\\n    display: inline-flex;\\r\\n    flex-direction: row;\\r\\n    align-items: center;\\r\\n    justify-content: center;\\r\\n    white-space: nowrap;\\r\\n    width: auto;\\r\\n    writing-mode: horizontal-tb;\\r\\n    padding: 0.35rem 0.75rem;\\r\\n    box-sizing: border-box;\\r\\n}\\r\\n\\r\\n.ai-wbr-actions .menu_button {\\r\\n    min-width: 6.5rem;\\r\\n}\\r\\n\\r\\n.ai-wbr-token-editor .menu_button {\\r\\n    min-width: 4.5rem;\\r\\n    flex-shrink: 0;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-add-state .menu_button {\\r\\n    min-width: 5.5rem;\\r\\n    align-self: stretch;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-popover-actions .menu_button {\\r\\n    min-width: 3.4rem;\\r\\n    padding: 0.22rem 0.36rem;\\r\\n    font-size: 0.78rem;\\r\\n}\\r\\n\\r\\n#ai_wbr_fx_layer {\\r\\n    position: fixed;\\r\\n    inset: 0;\\r\\n    z-index: 30000;\\r\\n    pointer-events: none;\\r\\n}\\r\\n\\r\\n.ai-wbr-book-underline {\\r\\n    position: fixed;\\r\\n    width: 3.5rem;\\r\\n    height: 0.62rem;\\r\\n    transform: translateX(-50%);\\r\\n    border-radius: 999px;\\r\\n    background:\\r\\n        radial-gradient(circle at center, rgba(153, 225, 255, 0.9) 0%, rgba(96, 199, 255, 0.5) 34%, rgba(120, 211, 255, 0) 76%);\\r\\n    filter: blur(0.2rem);\\r\\n    opacity: 0.98;\\r\\n    box-shadow:\\r\\n        0 0 14px rgba(117, 210, 255, 0.48),\\r\\n        0 0 24px rgba(117, 210, 255, 0.26);\\r\\n    animation: ai-wbr-book-pulse 1.45s ease-in-out infinite;\\r\\n}\\r\\n\\r\\n.ai-wbr-book-underline.ai-wbr-book-underline-error {\\r\\n    background:\\r\\n        radial-gradient(circle at center, rgba(255, 124, 124, 0.96) 0%, rgba(255, 82, 82, 0.62) 34%, rgba(255, 82, 82, 0) 76%);\\r\\n    box-shadow:\\r\\n        0 0 16px rgba(255, 82, 82, 0.62),\\r\\n        0 0 28px rgba(255, 82, 82, 0.32);\\r\\n    animation:\\r\\n        ai-wbr-book-pulse 0.34s ease-in-out 3,\\r\\n        ai-wbr-error-flash 0.34s ease-in-out 3;\\r\\n}\\r\\n\\r\\n.ai-wbr-book-underline.ai-wbr-book-underline-memory {\\r\\n    background:\\r\\n        radial-gradient(circle at center, rgba(255, 134, 134, 0.92) 0%, rgba(255, 90, 90, 0.58) 34%, rgba(255, 90, 90, 0) 76%);\\r\\n    box-shadow:\\r\\n        0 0 14px rgba(255, 109, 109, 0.5),\\r\\n        0 0 24px rgba(255, 109, 109, 0.28);\\r\\n}\\r\\n\\r\\n.ai-wbr-book-underline::after {\\r\\n    content: '';\\r\\n    position: absolute;\\r\\n    left: 50%;\\r\\n    top: 50%;\\r\\n    width: 3.8rem;\\r\\n    height: 0.15rem;\\r\\n    border-radius: 999px;\\r\\n    background: linear-gradient(90deg,\\r\\n        rgba(255, 255, 255, 0) 0%,\\r\\n        rgba(167, 225, 255, 0.34) 24%,\\r\\n        rgba(255, 255, 255, 1) 50%,\\r\\n        rgba(167, 225, 255, 0.34) 76%,\\r\\n        rgba(255, 255, 255, 0) 100%);\\r\\n    filter: blur(0.04rem);\\r\\n    transform: translate(-50%, -50%) translateX(-1.15rem);\\r\\n    animation: ai-wbr-book-sheen 1.05s linear infinite;\\r\\n}\\r\\n\\r\\n.ai-wbr-book-underline::before {\\r\\n    content: '';\\r\\n    position: absolute;\\r\\n    left: 50%;\\r\\n    top: 50%;\\r\\n    width: 2.4rem;\\r\\n    height: 0.1rem;\\r\\n    border-radius: 999px;\\r\\n    background: rgba(255, 255, 255, 0.92);\\r\\n    box-shadow: 0 0 10px rgba(255, 255, 255, 0.3);\\r\\n    transform: translate(-50%, -50%);\\r\\n}\\r\\n\\r\\n.ai-wbr-book-underline.ai-wbr-book-underline-error::before {\\r\\n    background: rgba(255, 238, 238, 0.96);\\r\\n    box-shadow: 0 0 10px rgba(255, 144, 144, 0.42);\\r\\n}\\r\\n\\r\\n.ai-wbr-book-underline.ai-wbr-book-underline-error::after {\\r\\n    background: linear-gradient(90deg,\\r\\n        rgba(255, 255, 255, 0) 0%,\\r\\n        rgba(255, 152, 152, 0.4) 24%,\\r\\n        rgba(255, 244, 244, 1) 50%,\\r\\n        rgba(255, 152, 152, 0.4) 76%,\\r\\n        rgba(255, 255, 255, 0) 100%);\\r\\n}\\r\\n\\r\\n.ai-wbr-book-underline.ai-wbr-book-underline-memory::before {\\r\\n    background: rgba(255, 242, 242, 0.96);\\r\\n    box-shadow: 0 0 10px rgba(255, 144, 144, 0.42);\\r\\n}\\r\\n\\r\\n.ai-wbr-book-underline.ai-wbr-book-underline-memory::after {\\r\\n    background: linear-gradient(90deg,\\r\\n        rgba(255, 255, 255, 0) 0%,\\r\\n        rgba(255, 152, 152, 0.4) 24%,\\r\\n        rgba(255, 244, 244, 1) 50%,\\r\\n        rgba(255, 152, 152, 0.4) 76%,\\r\\n        rgba(255, 255, 255, 0) 100%);\\r\\n}\\r\\n\\r\\n.ai-wbr-entry-burst {\\r\\n    position: fixed;\\r\\n    transform: translate(-50%, -50%);\\r\\n    padding: 0.28rem 0.58rem;\\r\\n    border: 1px solid rgba(176, 225, 255, 0.42);\\r\\n    border-radius: 999px;\\r\\n    background:\\r\\n        linear-gradient(135deg, rgba(85, 133, 201, 0.28), rgba(123, 216, 255, 0.16)),\\r\\n        color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(15, 17, 22, 0.82)) 86%, transparent 14%);\\r\\n    box-shadow:\\r\\n        0 10px 24px rgba(0, 0, 0, 0.2),\\r\\n        0 0 14px rgba(125, 212, 255, 0.18);\\r\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 88%, #d7f5ff 12%);\\r\\n    font-size: 0.85rem;\\r\\n    line-height: 1.1;\\r\\n    white-space: nowrap;\\r\\n    backdrop-filter: blur(8px);\\r\\n    opacity: 0;\\r\\n    animation: ai-wbr-entry-burst 1.5s cubic-bezier(0.2, 0.76, 0.18, 1) forwards;\\r\\n    animation-delay: var(--burst-delay, 0ms);\\r\\n}\\r\\n\\r\\n.ai-wbr-entry-burst.ai-wbr-entry-burst-memory {\\r\\n    border: 1px solid rgba(255, 178, 178, 0.44);\\r\\n    background:\\r\\n        linear-gradient(135deg, rgba(201, 85, 85, 0.28), rgba(255, 123, 123, 0.16)),\\r\\n        color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(15, 17, 22, 0.82)) 86%, transparent 14%);\\r\\n    box-shadow:\\r\\n        0 10px 24px rgba(0, 0, 0, 0.2),\\r\\n        0 0 14px rgba(255, 122, 122, 0.2);\\r\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 88%, #ffe1e1 12%);\\r\\n}\\r\\n\\r\\n.ai-wbr-status-burst {\\r\\n    position: fixed;\\r\\n    transform: translate(-50%, -50%);\\r\\n    font-size: 1.2rem;\\r\\n    line-height: 1;\\r\\n    font-weight: 700;\\r\\n    text-shadow: 0 0 10px rgba(255, 255, 255, 0.22);\\r\\n    opacity: 0;\\r\\n    animation: ai-wbr-status-burst 0.96s cubic-bezier(0.2, 0.78, 0.2, 1) forwards;\\r\\n}\\r\\n\\r\\n.ai-wbr-status-burst-retry {\\r\\n    color: #9edfff;\\r\\n}\\r\\n\\r\\n.ai-wbr-status-burst-fail {\\r\\n    color: #ff7676;\\r\\n}\\r\\n\\r\\n.ai-wbr-status-burst-memory {\\r\\n    color: #ff9e9e;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-orbit {\\r\\n    position: fixed;\\r\\n    width: 3.6rem;\\r\\n    height: 3.6rem;\\r\\n    transform: translate(-50%, -50%);\\r\\n    border-radius: 999px;\\r\\n    background:\\r\\n        radial-gradient(circle at center, rgba(220, 184, 255, 0.18) 0%, rgba(172, 119, 255, 0.1) 42%, rgba(172, 119, 255, 0) 72%);\\r\\n    filter: drop-shadow(0 0 14px rgba(192, 139, 255, 0.42));\\r\\n    animation: ai-wbr-memory-orbit-pulse 1.2s ease-in-out infinite;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-orbit span {\\r\\n    position: absolute;\\r\\n    left: 50%;\\r\\n    top: 50%;\\r\\n    width: 0.44rem;\\r\\n    height: 0.44rem;\\r\\n    margin: -0.22rem 0 0 -0.22rem;\\r\\n    border-radius: 999px;\\r\\n    background: #e7d0ff;\\r\\n    box-shadow: 0 0 12px rgba(223, 194, 255, 0.86);\\r\\n    transform-origin: 0 0;\\r\\n    animation: ai-wbr-memory-orbit-dot 1.25s linear infinite;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-orbit span:nth-child(2) {\\r\\n    animation-delay: -0.42s;\\r\\n    background: #aee9ff;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-orbit span:nth-child(3) {\\r\\n    animation-delay: -0.84s;\\r\\n    background: #ffd6ef;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-orbit.ai-wbr-memory-orbit-done {\\r\\n    animation: ai-wbr-memory-done 0.62s ease-out forwards;\\r\\n}\\r\\n\\r\\n.ai-wbr-grid {\\r\\n    display: grid;\\r\\n    grid-template-columns: 1fr minmax(6rem, 12rem);\\r\\n    gap: 0.6rem 0.75rem;\\r\\n    align-items: center;\\r\\n    margin-top: 0.75rem;\\r\\n}\\r\\n\\r\\n.ai-wbr-help {\\r\\n    margin-right: 0.35rem;\\r\\n    color: var(--SmartThemeQuoteColor);\\r\\n    cursor: help;\\r\\n}\\r\\n\\r\\n.ai-worldbook-router-settings .checkbox_label .ai-wbr-help {\\r\\n    flex: 0 0 auto;\\r\\n}\\r\\n\\r\\n.ai-wbr-section {\\r\\n    margin-top: 0.9rem;\\r\\n    padding-top: 0.75rem;\\r\\n    border-top: 1px solid var(--SmartThemeBorderColor);\\r\\n}\\r\\n\\r\\n.ai-wbr-section-title {\\r\\n    margin-bottom: 0.45rem;\\r\\n}\\r\\n\\r\\n.ai-wbr-actions {\\r\\n    display: flex;\\r\\n    align-items: center;\\r\\n    gap: 0.6rem;\\r\\n    margin-top: 0.65rem;\\r\\n    flex-wrap: wrap;\\r\\n}\\r\\n\\r\\n.ai-wbr-status {\\r\\n    opacity: 0.85;\\r\\n    font-size: 0.95em;\\r\\n    writing-mode: horizontal-tb;\\r\\n    white-space: nowrap;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-graph {\\n    position: relative;\\n    margin-top: 0.75rem;\\n    border: 1px solid rgba(151, 207, 255, 0.32);\\n    border-radius: 14px;\\n    background:\\n        radial-gradient(circle at 50% 35%, rgba(92, 130, 255, 0.18), transparent 48%),\\n        radial-gradient(circle at 32% 24%, rgba(58, 228, 193, 0.12), transparent 34%),\\n        radial-gradient(circle at 72% 68%, rgba(255, 190, 110, 0.08), transparent 36%),\\n        linear-gradient(rgba(255, 255, 255, 0.045) 1px, transparent 1px),\\n        linear-gradient(90deg, rgba(255, 255, 255, 0.045) 1px, transparent 1px),\\n        color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(10, 12, 20, 0.96)) 92%, #07101f 8%);\\n    background-size: auto, auto, auto, 22px 22px, 22px 22px, auto;\\n    box-shadow:\\n        inset 0 0 28px rgba(125, 212, 255, 0.08),\\n        0 14px 26px rgba(0, 0, 0, 0.22);\\n    overflow: hidden;\\n    min-height: 16rem;\\n    height: clamp(18rem, 42vw, 22rem);\\n}\\n\\n.ai-wbr-graph-section .ai-wbr-memory-graph,\\n.ai-wbr-memory-graph-large {\\n    min-height: 26rem;\\n    height: clamp(28rem, 62vh, 46rem);\\n}\\n\\n.ai-wbr-graph-workspace {\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0;\\n    flex: 1 1 auto;\\n    height: 100%;\\n    min-height: 0;\\n}\\n\\n.ai-wbr-memory-graph-fullscreen-open {\\n    overflow: hidden !important;\\n}\\n\\n.ai-wbr-graph-workspace.ai-wbr-memory-graph-fullscreen {\\n    position: fixed !important;\\n    inset: 0 !important;\\n    z-index: 2147483646 !important;\\n    width: 100vw !important;\\n    height: 100dvh !important;\\n    max-width: 100vw !important;\\n    max-height: 100dvh !important;\\n    margin: 0 !important;\\n    padding: 0 !important;\\n    border: none !important;\\n    border-radius: 0 !important;\\n    background:\\n        radial-gradient(circle at 50% 28%, rgba(92, 130, 255, 0.12), transparent 46%),\\n        color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(8, 10, 18, 0.98)) 94%, #050914 6%) !important;\\n    box-shadow: none !important;\\n}\\n\\n.ai-wbr-graph-workspace.ai-wbr-memory-graph-fullscreen .ai-wbr-graph-topbar {\\n    flex: 0 0 auto;\\n    padding: 0.65rem 0.8rem;\\n    background: rgba(8, 12, 22, 0.92);\\n    backdrop-filter: blur(10px);\\n}\\n\\n.ai-wbr-graph-workspace.ai-wbr-memory-graph-fullscreen .ai-wbr-graph-shell {\\n    flex: 1 1 auto;\\n    min-height: 0;\\n}\\n\\n.ai-wbr-graph-workspace.ai-wbr-memory-graph-fullscreen .ai-wbr-memory-graph {\\n    min-height: 0;\\n    height: 100%;\\n}\\n\\n.ai-wbr-graph-workspace.ai-wbr-memory-graph-fullscreen .ai-wbr-memory-graph-canvas {\\n    display: grid;\\n    place-items: center;\\n}\\n\\n.ai-wbr-graph-workspace.ai-wbr-memory-graph-fullscreen .ai-wbr-memory-graph-canvas svg {\\n    width: 100%;\\n    height: 100%;\\n}\\n\\n.ai-wbr-graph-topbar {\\n    display: flex;\\n    align-items: center;\\n    justify-content: space-between;\\n    gap: 1rem;\\n    padding: 0.85rem 1rem;\\n    border-bottom: 1px solid rgba(176, 225, 255, 0.16);\\n    background: rgba(0, 0, 0, 0.18);\\n    flex: 0 0 auto;\\n}\\n\\n.ai-wbr-graph-kicker {\\n    font-size: 0.72rem;\\n    opacity: 0.62;\\n    text-transform: uppercase;\\n    letter-spacing: 0.08em;\\n}\\n\\n.ai-wbr-graph-title {\\n    font-size: 1.05rem;\\n    font-weight: 800;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 88%, #bfeeff 12%);\\n}\\n\\n.ai-wbr-graph-topbar-actions {\\n    display: flex;\\n    align-items: center;\\n    gap: 0.5rem;\\n    flex-wrap: wrap;\\n    justify-content: flex-end;\\n}\\n\\n.ai-wbr-graph-shell {\\n    position: relative;\\n    display: block;\\n    height: 100%;\\n    min-height: 0;\\n    flex: 1 1 auto;\\n    overflow: hidden;\\n}\\n\\n.ai-wbr-memory-preview-panel {\\n    position: absolute;\\n    z-index: 5;\\n    inset: 0 auto 0 0;\\n    width: min(22rem, 86%);\\n    min-width: 0;\\n    transform: translateX(-102%);\\n    transition: transform 160ms ease;\\n    overflow: auto;\\n    border-right: 1px solid rgba(176, 225, 255, 0.18);\\n    background:\\n        linear-gradient(180deg, rgba(84, 226, 166, 0.055), rgba(125, 212, 255, 0.035)),\\n        color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(12, 16, 24, 0.96)) 94%, #08101d 6%);\\n    padding: 0.85rem;\\n    box-shadow: 18px 0 34px rgba(0, 0, 0, 0.28);\\n}\\n\\n.ai-wbr-graph-shell.preview-open .ai-wbr-memory-preview-panel {\\n    transform: translateX(0);\\n}\\n\\n.ai-wbr-memory-rule-drawer {\\n    position: absolute;\\n    z-index: 7;\\n    inset: 0 0 0 auto;\\n    width: min(28rem, 92vw);\\n    min-width: 0;\\n    transform: translateX(102%);\\n    transition: transform 180ms ease;\\n    overflow: auto;\\n    border-left: 1px solid rgba(176, 225, 255, 0.2);\\n    background:\\n        linear-gradient(180deg, rgba(125, 212, 255, 0.07), rgba(84, 226, 166, 0.035)),\\n        color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(12, 16, 24, 0.97)) 94%, #08101d 6%);\\n    padding: 0.85rem;\\n    box-shadow: -18px 0 34px rgba(0, 0, 0, 0.3);\\n}\\n\\n.ai-wbr-memory-graph.rules-open .ai-wbr-memory-rule-drawer {\\n    transform: translateX(0);\\n}\\n\\n.ai-wbr-memory-rule-head {\\n    display: flex;\\n    align-items: flex-start;\\n    justify-content: space-between;\\n    gap: 0.75rem;\\n    padding-bottom: 0.7rem;\\n    border-bottom: 1px solid rgba(176, 225, 255, 0.14);\\n}\\n\\n.ai-wbr-memory-rule-head h3 {\\n    margin: 0.12rem 0 0.15rem;\\n    font-size: 1rem;\\n    font-weight: 800;\\n}\\n\\n.ai-wbr-memory-rule-head small,\\n.ai-wbr-memory-rule-card small {\\n    display: block;\\n    opacity: 0.72;\\n    line-height: 1.35;\\n}\\n\\n.ai-wbr-memory-rule-body {\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.65rem;\\n    padding-top: 0.75rem;\\n}\\n\\n.ai-wbr-memory-rule-master,\\n.ai-wbr-memory-rule-toolbar {\\n    display: flex;\\n    align-items: center;\\n    gap: 0.5rem;\\n    flex-wrap: wrap;\\n}\\n\\n.ai-wbr-memory-rule-list {\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.58rem;\\n}\\n\\n.ai-wbr-memory-rule-card {\\n    border: 1px solid rgba(176, 225, 255, 0.15);\\n    border-radius: 8px;\\n    padding: 0.62rem;\\n    background: rgba(255, 255, 255, 0.045);\\n}\\n\\n.ai-wbr-memory-rule-card-head {\\n    display: flex;\\n    align-items: center;\\n    justify-content: space-between;\\n    gap: 0.5rem;\\n    margin-bottom: 0.48rem;\\n}\\n\\n.ai-wbr-memory-rule-grid {\\n    display: grid;\\n    grid-template-columns: 4.8rem minmax(0, 1fr);\\n    gap: 0.42rem 0.5rem;\\n    align-items: start;\\n}\\n\\n.ai-wbr-memory-rule-grid label {\\n    font-size: 0.78rem;\\n    opacity: 0.74;\\n    padding-top: 0.35rem;\\n}\\n\\n.ai-wbr-memory-rule-grid .text_pole,\\n.ai-wbr-memory-rule-card textarea {\\n    width: 100%;\\n    min-width: 0;\\n}\\n\\n.ai-wbr-memory-rule-inline {\\n    display: grid;\\n    grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);\\n    align-items: center;\\n    gap: 0.35rem;\\n}\\n\\n.ai-wbr-memory-rule-inline span {\\n    opacity: 0.65;\\n    font-size: 0.78rem;\\n}\\n\\n.ai-wbr-memory-rule-preview-actions {\\n    display: flex;\\n    justify-content: flex-end;\\n    margin-top: 0.45rem;\\n}\\n\\n.ai-wbr-memory-rule-preview-result {\\n    display: flex;\\n    flex-wrap: wrap;\\n    gap: 0.35rem;\\n    margin-top: 0.5rem;\\n    font-size: 0.78rem;\\n    opacity: 0.92;\\n}\\n\\n.ai-wbr-memory-rule-preview-result span {\\n    border: 1px solid rgba(125, 212, 255, 0.22);\\n    border-radius: 999px;\\n    padding: 0.18rem 0.45rem;\\n    background: rgba(125, 212, 255, 0.08);\\n}\\n\\n.ai-wbr-memory-rule-preview-chip {\\n    display: inline-flex;\\n    flex-direction: column;\\n    align-items: flex-start;\\n    gap: 0.12rem;\\n    max-width: 100%;\\n    border-radius: 8px !important;\\n    white-space: normal;\\n    line-height: 1.25;\\n}\\n\\n.ai-wbr-memory-rule-preview-chip.accepted {\\n    border-color: rgba(84, 226, 166, 0.34);\\n    background: rgba(84, 226, 166, 0.1);\\n}\\n\\n.ai-wbr-memory-rule-preview-chip.skipped {\\n    border-color: rgba(255, 170, 110, 0.34);\\n    background: rgba(255, 170, 110, 0.08);\\n}\\n\\n.ai-wbr-memory-rule-preview-chip em {\\n    font-style: normal;\\n    opacity: 0.7;\\n    font-size: 0.72rem;\\n    word-break: break-word;\\n}\\n\\n.ai-wbr-memory-rule-preview-result b {\\n    margin-right: 0.28rem;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 86%, #9ee7ff 14%);\\n}\\n\\n.ai-wbr-memory-preview-head {\\n    display: flex;\\n    align-items: flex-start;\\n    justify-content: space-between;\\n    gap: 0.75rem;\\n    padding-bottom: 0.7rem;\\n    border-bottom: 1px solid rgba(176, 225, 255, 0.14);\\n}\\n\\n.ai-wbr-memory-preview-kicker {\\n    font-size: 0.72rem;\\n    opacity: 0.64;\\n    text-transform: uppercase;\\n    letter-spacing: 0.06em;\\n}\\n\\n.ai-wbr-memory-preview-title {\\n    margin-top: 0.12rem;\\n    font-size: 1rem;\\n    font-weight: 800;\\n}\\n\\n.ai-wbr-memory-preview-count {\\n    flex: 0 0 auto;\\n    border: 1px solid rgba(125, 212, 255, 0.22);\\n    border-radius: 999px;\\n    padding: 0.18rem 0.5rem;\\n    background: rgba(125, 212, 255, 0.09);\\n    font-size: 0.78rem;\\n}\\n\\n.ai-wbr-memory-preview-search {\\n    width: 100%;\\n    margin-top: 0.75rem;\\n}\\n\\n.ai-wbr-memory-preview-section {\\n    margin-top: 0.85rem;\\n}\\n\\n.ai-wbr-memory-preview-section-title {\\n    display: flex;\\n    align-items: center;\\n    justify-content: space-between;\\n    gap: 0.5rem;\\n    margin-bottom: 0.42rem;\\n    font-size: 0.82rem;\\n    font-weight: 800;\\n    opacity: 0.9;\\n}\\n\\n.ai-wbr-memory-preview-list {\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.4rem;\\n}\\n\\n.ai-wbr-memory-preview-card {\\n    display: block;\\n    width: 100%;\\n    text-align: left;\\n    border: 1px solid rgba(176, 225, 255, 0.14);\\n    border-radius: 8px;\\n    padding: 0.5rem 0.55rem;\\n    background: rgba(255, 255, 255, 0.045);\\n    color: var(--SmartThemeBodyColor);\\n    cursor: pointer;\\n}\\n\\n.ai-wbr-memory-preview-card:hover,\\n.ai-wbr-memory-preview-card.selected {\\n    border-color: rgba(125, 212, 255, 0.62);\\n    background: rgba(125, 212, 255, 0.1);\\n}\\n\\n.ai-wbr-memory-preview-card b {\\n    display: block;\\n    font-size: 0.86rem;\\n    line-height: 1.25;\\n    word-break: break-word;\\n}\\n\\n.ai-wbr-memory-preview-card small {\\n    display: block;\\n    margin-top: 0.2rem;\\n    opacity: 0.72;\\n    line-height: 1.35;\\n    word-break: break-word;\\n}\\n\\n.ai-wbr-memory-preview-meta {\\n    display: flex;\\n    align-items: center;\\n    justify-content: space-between;\\n    gap: 0.4rem;\\n    margin-top: 0.35rem;\\n    font-size: 0.72rem;\\n    opacity: 0.7;\\n}\\n\\n.ai-wbr-memory-preview-tags {\\n    display: flex;\\n    flex-wrap: wrap;\\n    gap: 0.25rem;\\n    margin-top: 0.35rem;\\n}\\n\\n.ai-wbr-memory-preview-tags span {\\n    border: 1px solid rgba(125, 212, 255, 0.18);\\n    border-radius: 999px;\\n    padding: 0.08rem 0.38rem;\\n    font-size: 0.68rem;\\n    opacity: 0.82;\\n}\\n\\n.ai-wbr-graph-workspace .ai-wbr-memory-graph {\\n    margin: 0;\\n    display: flex;\\n    flex-direction: column;\\n    height: 100%;\\n    min-height: 28rem;\\n    border: none;\\n    border-radius: 0;\\n    box-shadow: inset 0 0 38px rgba(125, 212, 255, 0.09);\\n}\\n\\n.ai-wbr-graph-workspace .ai-wbr-memory-graph::after {\\n    content: '';\\n    position: absolute;\\n    inset: 0;\\n    pointer-events: none;\\n    background: linear-gradient(90deg, rgba(0, 0, 0, 0.18), transparent 18%, transparent 82%, rgba(0, 0, 0, 0.18));\\n}\\n\\n.ai-wbr-memory-detail-drawer {\\n    position: absolute;\\n    z-index: 4;\\n    inset: 0 0 0 auto;\\n    width: min(24rem, 88%);\\n    transform: translateX(102%);\\n    transition: transform 160ms ease;\\n    min-width: 0;\\n    overflow: auto;\\n    border-left: 1px solid rgba(176, 225, 255, 0.18);\\n    background:\\n        linear-gradient(180deg, rgba(125, 212, 255, 0.06), rgba(183, 140, 255, 0.035)),\\n        color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(12, 16, 24, 0.96)) 94%, #08101d 6%);\\n    padding: 1rem;\\n    box-shadow: -18px 0 34px rgba(0, 0, 0, 0.28);\\n}\\n\\n.ai-wbr-memory-detail-drawer.open {\\n    transform: translateX(0);\\n}\\n\\n.ai-wbr-memory-detail-empty,\\n.ai-wbr-memory-detail-muted {\\n    opacity: 0.72;\\n    line-height: 1.5;\\n}\\n\\n.ai-wbr-memory-detail-head {\\n    display: flex;\\n    align-items: flex-start;\\n    justify-content: space-between;\\n    gap: 0.75rem;\\n    padding-bottom: 0.8rem;\\n    border-bottom: 1px solid rgba(176, 225, 255, 0.14);\\n}\\n\\n.ai-wbr-memory-detail-head h3 {\\n    margin: 0.12rem 0 0;\\n    font-size: 1.05rem;\\n    line-height: 1.28;\\n    word-break: break-word;\\n}\\n\\n.ai-wbr-memory-detail-kicker {\\n    font-size: 0.72rem;\\n    opacity: 0.68;\\n    text-transform: uppercase;\\n    letter-spacing: 0.05em;\\n}\\n\\n.ai-wbr-memory-detail-grid {\\n    display: grid;\\n    grid-template-columns: repeat(2, minmax(0, 1fr));\\n    gap: 0.55rem;\\n    margin-top: 0.85rem;\\n}\\n\\n.ai-wbr-memory-detail-meta {\\n    padding: 0.55rem;\\n    border: 1px solid rgba(176, 225, 255, 0.14);\\n    border-radius: 8px;\\n    background: rgba(255, 255, 255, 0.045);\\n}\\n\\n.ai-wbr-memory-detail-meta span {\\n    display: block;\\n    font-size: 0.72rem;\\n    opacity: 0.66;\\n}\\n\\n.ai-wbr-memory-detail-meta b {\\n    display: block;\\n    margin-top: 0.12rem;\\n    font-size: 0.84rem;\\n    word-break: break-word;\\n}\\n\\n.ai-wbr-memory-detail-section {\\n    margin-top: 0.95rem;\\n}\\n\\n.ai-wbr-memory-detail-section b {\\n    display: block;\\n    margin-bottom: 0.35rem;\\n}\\n\\n.ai-wbr-memory-detail-section p {\\n    margin: 0;\\n    line-height: 1.55;\\n    opacity: 0.88;\\n    white-space: pre-wrap;\\n    word-break: break-word;\\n}\\n\\n.ai-wbr-memory-detail-chips,\\n.ai-wbr-memory-detail-related,\\n.ai-wbr-memory-detail-actions {\\n    display: flex;\\n    gap: 0.38rem;\\n    flex-wrap: wrap;\\n}\\n\\n.ai-wbr-memory-detail-chips span {\\n    border: 1px solid rgba(125, 212, 255, 0.2);\\n    border-radius: 999px;\\n    padding: 0.16rem 0.48rem;\\n    background: rgba(125, 212, 255, 0.09);\\n    font-size: 0.75rem;\\n}\\n\\n.ai-wbr-memory-detail-actions {\\n    margin-top: 1rem;\\n    padding-top: 0.85rem;\\n    border-top: 1px solid rgba(176, 225, 255, 0.14);\\n}\\n\\n.ai-wbr-hidden-editor,\\n.ai-wbr-graph-list-collapsed {\\n    display: none !important;\\n}\\n\\n.ai-wbr-graph-section .ai-wbr-memory-node-editor,\\n.ai-wbr-graph-section .ai-wbr-memory-edge-editor,\\n.ai-wbr-graph-section .ai-wbr-memory-list {\\n    margin-top: 0.75rem;\\n}\\n\\n.ai-wbr-memory-dashboard {\\n    display: grid;\\n    grid-template-columns: repeat(4, minmax(0, 1fr));\\n    gap: 0.55rem;\\n    margin: 0.8rem 0 0.65rem;\\n}\\n\\n.ai-wbr-memory-metric,\\n.ai-wbr-memory-review-card {\\n    border: 1px solid rgba(151, 207, 255, 0.24);\\n    border-radius: 12px;\\n    background:\\n        linear-gradient(135deg, rgba(125, 212, 255, 0.08), rgba(183, 140, 255, 0.06)),\\n        color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(18, 20, 28, 0.92)) 94%, transparent 6%);\\n    box-shadow: 0 10px 24px rgba(0, 0, 0, 0.16);\\n}\\n\\n.ai-wbr-memory-metric {\\n    padding: 0.62rem;\\n    min-width: 0;\\n}\\n\\n.ai-wbr-memory-metric-label,\\n.ai-wbr-memory-metric-note {\\n    color: var(--SmartThemeQuoteColor);\\n    font-size: 0.76rem;\\n    line-height: 1.28;\\n    opacity: 0.88;\\n}\\n\\n.ai-wbr-memory-metric-value {\\n    margin: 0.12rem 0;\\n    font-size: 1.25rem;\\n    font-weight: 800;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 86%, #9edfff 14%);\\n    word-break: break-word;\\n}\\n\\n.ai-wbr-memory-review-queue {\\n    margin: 0.4rem 0 0.75rem;\\n}\\n\\n.ai-wbr-memory-review-card {\\n    padding: 0.65rem;\\n    margin-top: 0.45rem;\\n}\\n\\n.ai-wbr-memory-review-card-stale {\\n    opacity: 0.72;\\n    border-color: rgba(255, 196, 87, 0.35);\\n}\\n\\n.ai-wbr-memory-review-head {\\n    display: flex;\\n    align-items: flex-start;\\n    justify-content: space-between;\\n    gap: 0.55rem;\\n}\\n\\n.ai-wbr-memory-review-head small {\\n    display: block;\\n    margin-top: 0.12rem;\\n    color: var(--SmartThemeQuoteColor);\\n    font-size: 0.76rem;\\n}\\n\\n.ai-wbr-memory-review-actions {\\n    display: flex;\\n    gap: 0.35rem;\\n    flex-wrap: wrap;\\n    justify-content: flex-end;\\n}\\n\\n.ai-wbr-memory-review-chips {\\n    display: flex;\\n    gap: 0.35rem;\\n    flex-wrap: wrap;\\n    margin-top: 0.5rem;\\n}\\n\\n.ai-wbr-memory-review-chips span {\\n    padding: 0.14rem 0.45rem;\\n    border-radius: 999px;\\n    background: rgba(125, 212, 255, 0.12);\\n    border: 1px solid rgba(125, 212, 255, 0.22);\\n    font-size: 0.74rem;\\n}\\n\\n.ai-wbr-memory-review-summary {\\n    margin-top: 0.45rem;\\n    color: var(--SmartThemeBodyColor);\\n    opacity: 0.9;\\n    line-height: 1.45;\\n    word-break: break-word;\\n}\\n\\r\\n.ai-wbr-memory-graph-toolbar {\\n    position: relative;\\n    z-index: 3;\\n    display: flex;\\n    align-items: flex-start;\\n    gap: 0.42rem;\\n    flex-direction: column;\\n    pointer-events: auto;\\n    width: 100%;\\n    max-width: none;\\n    max-height: none;\\n    overflow: visible;\\n    padding: 0.55rem 0.65rem 0.5rem;\\n    border-bottom: 1px solid rgba(176, 225, 255, 0.16);\\n    background:\\n        linear-gradient(180deg, rgba(11, 16, 28, 0.88), rgba(11, 16, 28, 0.68)),\\n        color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(12, 16, 24, 0.96)) 88%, #07101f 12%);\\n    box-shadow: 0 10px 24px rgba(0, 0, 0, 0.18);\\n}\\n\\n.ai-wbr-memory-graph-toolbar .menu_button {\\n    width: auto;\\n    min-width: 2.2rem;\\n    padding: 0.25rem 0.45rem;\\n}\\n\\n.ai-wbr-memory-graph-row,\\n.ai-wbr-memory-graph-typebar {\\n    display: flex;\\n    align-items: center;\\n    gap: 0.35rem;\\n    flex-wrap: nowrap;\\n    width: 100%;\\n    overflow-x: auto;\\n    overflow-y: hidden;\\n    scrollbar-width: thin;\\n    padding-bottom: 0.08rem;\\n}\\n\\n.ai-wbr-memory-graph-statusbar {\\n    align-items: center;\\n}\\n\\n.ai-wbr-memory-graph-row > *,\\n.ai-wbr-memory-graph-typebar > * {\\n    flex: 0 0 auto;\\n}\\n\\n.ai-wbr-memory-graph-typebar small {\\n    margin-left: 0.2rem;\\n    opacity: 0.72;\\n    font-size: 0.72rem;\\n}\\n\\n.ai-wbr-memory-graph-search {\\n    width: min(15rem, 38vw);\\n    min-height: 1.8rem;\\n}\\n\\n.ai-wbr-memory-graph-canvas {\\n    position: relative;\\n    flex: 1 1 auto;\\n    min-height: 0;\\n    overflow: hidden;\\n}\\n\\n.ai-wbr-memory-graph-empty {\\n    display: grid;\\n    place-items: center;\\n    padding: 1rem;\\n}\\n\\n.ai-wbr-memory-timeline-view {\\n    position: relative;\\n    flex: 1 1 auto;\\n    min-height: 0;\\n    overflow: auto;\\n    padding: 1rem 1.2rem 1.2rem;\\n    background:\\n        linear-gradient(180deg, rgba(12, 18, 30, 0.22), rgba(12, 18, 30, 0.04)),\\n        color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(10, 12, 20, 0.96)) 92%, #07101f 8%);\\n}\\n\\n@media (max-width: 760px) {\\n    .ai-wbr-memory-graph-toolbar {\\n        max-width: none;\\n        gap: 0.25rem;\\n        padding: 0.42rem 0.45rem;\\n    }\\n\\n    .ai-wbr-memory-graph-toolbar .menu_button {\\n        min-width: 2rem;\\n        padding: 0.22rem 0.38rem;\\n        font-size: 0.78rem;\\n    }\\n\\n    .ai-wbr-memory-graph-search {\\n        width: 12rem;\\n        min-height: 1.65rem;\\n    }\\n\\n    .ai-wbr-memory-weight-filter,\\n    .ai-wbr-memory-graph-summary {\\n        font-size: 0.72rem;\\n        padding: 0.18rem 0.38rem;\\n    }\\n\\n    .ai-wbr-memory-link-weight {\\n        width: 4.8rem;\\n    }\\n\\n    .ai-wbr-memory-timeline-view {\\n        padding: 0.75rem 0.65rem 0.85rem;\\n    }\\n\\n    .ai-wbr-memory-timeline-head {\\n        align-items: flex-start;\\n        flex-direction: column;\\n        gap: 0.25rem;\\n    }\\n\\n    .ai-wbr-memory-timeline-rail {\\n        max-width: none;\\n    }\\n}\\n\\n.ai-wbr-memory-timeline-head {\\n    display: flex;\\n    align-items: flex-end;\\n    justify-content: space-between;\\n    gap: 1rem;\\n    margin-bottom: 1rem;\\n    padding-bottom: 0.75rem;\\n    border-bottom: 1px solid rgba(176, 225, 255, 0.16);\\n}\\n\\n.ai-wbr-memory-timeline-head h3 {\\n    margin: 0.1rem 0 0;\\n    font-size: 1.15rem;\\n}\\n\\n.ai-wbr-memory-timeline-head small {\\n    opacity: 0.7;\\n}\\n\\n.ai-wbr-memory-timeline-rail {\\n    position: relative;\\n    display: flex;\\n    flex-direction: column;\\n    gap: 1.05rem;\\n    max-width: 58rem;\\n}\\n\\n.ai-wbr-memory-timeline-group {\\n    position: relative;\\n    padding-left: 1.25rem;\\n}\\n\\n.ai-wbr-memory-timeline-group::before {\\n    content: '';\\n    position: absolute;\\n    left: 0.28rem;\\n    top: 1.7rem;\\n    bottom: 0.2rem;\\n    width: 2px;\\n    background: linear-gradient(180deg, rgba(125, 212, 255, 0.42), rgba(125, 212, 255, 0.06));\\n}\\n\\n.ai-wbr-memory-timeline-group-title {\\n    display: inline-flex;\\n    align-items: center;\\n    gap: 0.45rem;\\n    margin-bottom: 0.55rem;\\n    padding: 0.2rem 0.55rem;\\n    border: 1px solid rgba(125, 212, 255, 0.2);\\n    border-radius: 999px;\\n    background: rgba(125, 212, 255, 0.09);\\n    font-weight: 800;\\n    font-size: 0.84rem;\\n}\\n\\n.ai-wbr-memory-timeline-group-title span {\\n    opacity: 0.65;\\n    font-weight: 700;\\n}\\n\\n.ai-wbr-memory-timeline-items {\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.55rem;\\n}\\n\\n.ai-wbr-memory-timeline-card {\\n    position: relative;\\n    display: flex;\\n    width: 100%;\\n    gap: 0.65rem;\\n    padding: 0.68rem 0.75rem;\\n    border: 1px solid rgba(176, 225, 255, 0.15);\\n    border-radius: 10px;\\n    background: rgba(255, 255, 255, 0.048);\\n    color: var(--SmartThemeBodyColor);\\n    text-align: left;\\n    cursor: pointer;\\n}\\n\\n.ai-wbr-memory-timeline-card:hover,\\n.ai-wbr-memory-timeline-card.selected {\\n    border-color: rgba(125, 212, 255, 0.65);\\n    background: rgba(125, 212, 255, 0.105);\\n}\\n\\n.ai-wbr-memory-timeline-dot {\\n    position: absolute;\\n    left: -1.08rem;\\n    top: 0.88rem;\\n    width: 0.62rem;\\n    height: 0.62rem;\\n    border-radius: 999px;\\n    background: #7dd4ff;\\n    box-shadow: 0 0 0 4px rgba(125, 212, 255, 0.15);\\n}\\n\\n.ai-wbr-memory-timeline-card-body {\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.26rem;\\n    min-width: 0;\\n}\\n\\n.ai-wbr-memory-timeline-card b {\\n    font-size: 0.95rem;\\n    line-height: 1.25;\\n    word-break: break-word;\\n}\\n\\n.ai-wbr-memory-timeline-card small {\\n    opacity: 0.78;\\n    line-height: 1.42;\\n    word-break: break-word;\\n}\\n\\n.ai-wbr-memory-timeline-meta {\\n    font-size: 0.76rem;\\n    opacity: 0.66;\\n}\\n\\n.ai-wbr-memory-timeline-chips {\\n    display: flex;\\n    gap: 0.28rem;\\n    flex-wrap: wrap;\\n    margin-top: 0.12rem;\\n}\\n\\n.ai-wbr-memory-timeline-chips i {\\n    font-style: normal;\\n    border: 1px solid rgba(125, 212, 255, 0.18);\\n    border-radius: 999px;\\n    padding: 0.1rem 0.4rem;\\n    font-size: 0.7rem;\\n    opacity: 0.84;\\n}\\n\\n.ai-wbr-memory-timeline-empty {\\n    margin-top: 1rem;\\n}\\n\\n.ai-wbr-memory-weight-filter,\\n.ai-wbr-memory-graph-summary {\\n    display: inline-flex;\\n    align-items: center;\\n    gap: 0.35rem;\\n    padding: 0.22rem 0.5rem;\\n    border-radius: 999px;\\n    background: color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(20, 20, 24, 0.9)) 82%, transparent 18%);\\n    border: 1px solid rgba(125, 212, 255, 0.18);\\n    font-size: 0.78rem;\\n    line-height: 1.2;\\n}\\n\\n.ai-wbr-memory-link-weight {\\n    width: 6.5rem;\\n}\\n\\n.ai-wbr-memory-mode.active,\\n.ai-wbr-memory-type-filter.active {\\n    border-color: rgba(125, 212, 255, 0.72);\\n    box-shadow: 0 0 0 1px rgba(125, 212, 255, 0.22), 0 0 10px rgba(125, 212, 255, 0.18);\\n}\\n\\n.ai-wbr-memory-link-hint {\\n    font-size: 0.82rem;\\n    opacity: 0.76;\\n    padding: 0.22rem 0.45rem;\\r\\n    border-radius: 999px;\\r\\n    background: color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(20, 20, 24, 0.9)) 82%, transparent 18%);\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-graph svg {\\r\\n    width: 100%;\\r\\n    height: 100%;\\r\\n    display: block;\\r\\n    cursor: grab;\\r\\n    user-select: none;\\r\\n    touch-action: none;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-graph svg.ai-wbr-memory-panning {\\r\\n    cursor: grabbing;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-edge {\\n    fill: none;\\n    stroke: rgba(110, 208, 255, 0.92);\\n    stroke-width: 3.2;\\n    stroke-linecap: round;\\n    stroke-linejoin: round;\\n    cursor: pointer;\\n    pointer-events: stroke;\\n    vector-effect: non-scaling-stroke;\\n}\\n\\n.ai-wbr-memory-category-panel {\\n    pointer-events: none;\\n}\\n\\n.ai-wbr-memory-category-bg {\\n    fill: rgba(10, 16, 28, 0.42);\\n    stroke: rgba(125, 212, 255, 0.22);\\n    stroke-width: 1.2;\\n    stroke-dasharray: 0;\\n    vector-effect: non-scaling-stroke;\\n}\\n\\n.ai-wbr-memory-category-title {\\n    fill: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 88%, #9ddcff 12%);\\n    font-size: 0.82rem;\\n    font-weight: 800;\\n    pointer-events: none;\\n}\\n\\n.ai-wbr-memory-category-note {\\n    fill: color-mix(in srgb, var(--SmartThemeBodyColor, #e7eefc) 64%, rgba(157, 220, 255, 0.8) 36%);\\n    font-size: 0.64rem;\\n    opacity: 0.82;\\n    pointer-events: none;\\n}\\n\\n.ai-wbr-memory-edge-cross-category {\\n    stroke-dasharray: 7 8;\\n}\\n\\n.ai-wbr-memory-arrow {\\n    fill: currentColor;\\n    color: rgba(110, 208, 255, 0.92);\\n}\\n\\n.ai-wbr-memory-edge-involves,\\n.ai-wbr-memory-edge-mentions,\\n.ai-wbr-memory-edge-related {\\n    stroke: rgba(110, 208, 255, 0.88);\\n    color: rgba(110, 208, 255, 0.88);\\n}\\n\\n.ai-wbr-memory-edge-happens_at,\\n.ai-wbr-memory-edge-follows,\\n.ai-wbr-memory-edge-updates {\\n    stroke: rgba(120, 165, 255, 0.9);\\n    color: rgba(120, 165, 255, 0.9);\\n}\\n\\n.ai-wbr-memory-edge-part_of,\\n.ai-wbr-memory-edge-allied_with {\\n    stroke: rgba(84, 226, 166, 0.9);\\n    color: rgba(84, 226, 166, 0.9);\\n}\\n\\n.ai-wbr-memory-edge-causes,\\n.ai-wbr-memory-edge-opposes {\\n    stroke: rgba(255, 158, 116, 0.94);\\n    color: rgba(255, 158, 116, 0.94);\\n}\\n\\n.ai-wbr-memory-edge-hit {\\n    fill: none;\\n    stroke: transparent;\\n    stroke-width: 24;\\n    cursor: pointer;\\n    pointer-events: stroke;\\n}\\n\\r\\n.ai-wbr-memory-edge:hover,\\n.ai-wbr-memory-edge-selected {\\n    stroke: rgba(183, 131, 255, 0.98);\\n    stroke-width: 4.8;\\n}\\n\\r\\n.ai-wbr-memory-node {\\n    cursor: pointer;\\n    --memory-node-accent: #66e0ff;\\n    --memory-node-accent-soft: rgba(102, 224, 255, 0.18);\\n    will-change: transform;\\n}\\n\\n.ai-wbr-memory-node-card {\\n    fill: rgba(15, 22, 38, 0.92);\\n    stroke: rgba(104, 207, 255, 0.62);\\n    stroke-width: 1.5;\\n}\\n\\r\\n.ai-wbr-memory-node:hover .ai-wbr-memory-node-card {\\n    stroke: rgba(255, 255, 255, 0.95);\\n}\\n\\n.ai-wbr-memory-node-selected .ai-wbr-memory-node-card {\\n    stroke: rgba(255, 255, 255, 0.98);\\n    stroke-width: 2.4;\\n}\\n\\n.ai-wbr-memory-node-search-hit .ai-wbr-memory-node-card {\\n    stroke: rgba(255, 232, 140, 0.98);\\n    stroke-width: 2.2;\\n}\\n\\n.ai-wbr-memory-node .ai-wbr-memory-node-accent {\\n    fill: var(--memory-node-accent);\\n}\\n\\r\\n.ai-wbr-memory-node-location {\\r\\n    --memory-node-accent: #4ec9ff;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-node-character {\\r\\n    --memory-node-accent: #45e2a6;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-node-event,\\r\\n.ai-wbr-memory-node-quest {\\r\\n    --memory-node-accent: #ffbf69;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-node-faction,\\r\\n.ai-wbr-memory-node-concept {\\r\\n    --memory-node-accent: #b78cff;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-node-item {\\r\\n    --memory-node-accent: #ffd86b;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-node-rule {\\r\\n    --memory-node-accent: #ff8fb1;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-node-title {\\n    fill: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 92%, #ece7ff 8%);\\n    font-size: 0.86rem;\\n    font-weight: 800;\\n    pointer-events: none;\\n}\\n\\r\\n.ai-wbr-memory-node-subtitle {\\n    display: none;\\n}\\n\\n.ai-wbr-memory-node-subtitle-lines {\\n    fill: color-mix(in srgb, var(--SmartThemeBodyColor, #f4f6ff) 74%, rgba(206, 220, 255, 0.9) 26%);\\n    font-size: 0.68rem;\\n    opacity: 0.86;\\n    pointer-events: none;\\n}\\n\\n.ai-wbr-memory-node-chip rect,\\n.ai-wbr-memory-node-badge rect {\\n    fill: color-mix(in srgb, var(--memory-node-accent) 22%, rgba(12, 18, 30, 0.88) 78%);\\n    stroke: color-mix(in srgb, var(--memory-node-accent) 60%, rgba(255, 255, 255, 0.2) 40%);\\n    stroke-width: 1;\\n}\\n\\n.ai-wbr-memory-node-chip text,\\n.ai-wbr-memory-node-badge text {\\n    fill: color-mix(in srgb, var(--memory-node-accent) 88%, white 12%);\\n    font-size: 0.6rem;\\n    font-weight: 700;\\n    text-anchor: middle;\\n    pointer-events: none;\\n}\\n\\n.ai-wbr-memory-node-score {\\n    fill: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 70%, var(--memory-node-accent) 30%);\\n    font-size: 0.68rem;\\n    font-weight: 800;\\n    text-anchor: end;\\n    pointer-events: none;\\n}\\n\\n.ai-wbr-memory-node-popover {\\n    position: fixed;\\n    z-index: 30020;\\n    display: none;\\n    width: min(14rem, calc(100% - 1rem));\\r\\n    max-height: min(18rem, calc(100vh - 1rem));\\r\\n    overflow: auto;\\r\\n    padding: 0.45rem;\\r\\n    border: 1px solid rgba(218, 199, 255, 0.48);\\r\\n    border-radius: 10px;\\r\\n    background:\\r\\n        linear-gradient(135deg, rgba(117, 90, 170, 0.2), rgba(100, 174, 218, 0.08)),\\r\\n        color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(20, 20, 24, 0.96)) 94%, black 6%);\\r\\n    box-shadow:\\r\\n        0 18px 38px rgba(0, 0, 0, 0.32),\\r\\n        0 0 20px rgba(188, 151, 255, 0.16);\\r\\n    backdrop-filter: blur(10px);\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-node-popover label {\\r\\n    display: block;\\r\\n    font-size: 0.76rem;\\r\\n    margin-top: 0.28rem;\\r\\n    opacity: 0.92;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-node-popover input,\\r\\n.ai-wbr-memory-node-popover textarea {\\r\\n    width: 100%;\\r\\n    box-sizing: border-box;\\r\\n    margin-top: 0.2rem;\\r\\n    font-size: 0.84rem;\\r\\n    padding: 0.32rem 0.42rem;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-popover-title {\\r\\n    font-weight: 700;\\r\\n    margin-bottom: 0.25rem;\\r\\n    word-break: break-word;\\r\\n    font-size: 0.9rem;\\r\\n    line-height: 1.2;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-popover-actions {\\n    display: flex;\\n    gap: 0.25rem;\\n    flex-wrap: wrap;\\n    margin-top: 0.38rem;\\n}\\n\\n.ai-wbr-memory-fold {\\n    margin-top: 0.65rem;\\n    border: 1px solid rgba(151, 207, 255, 0.22);\\n    border-radius: 10px;\\n    background: color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(20,20,24,0.9)) 94%, transparent 6%);\\n    overflow: hidden;\\n}\\n\\n.ai-wbr-memory-fold[open] {\\n    border-color: rgba(151, 207, 255, 0.34);\\n    box-shadow: 0 10px 24px rgba(0, 0, 0, 0.12);\\n}\\n\\n.ai-wbr-memory-fold summary {\\n    display: grid;\\n    grid-template-columns: minmax(6rem, 0.8fr) 1fr;\\n    gap: 0.45rem 0.75rem;\\n    align-items: center;\\n    padding: 0.65rem 0.75rem;\\n    cursor: pointer;\\n    list-style: none;\\n    user-select: none;\\n}\\n\\n.ai-wbr-memory-fold summary::-webkit-details-marker {\\n    display: none;\\n}\\n\\n.ai-wbr-memory-fold summary::before {\\n    content: '▸';\\n    grid-column: 1;\\n    margin-right: 0.35rem;\\n    color: #8fd7ff;\\n    font-size: 0.9rem;\\n}\\n\\n.ai-wbr-memory-fold[open] summary::before {\\n    content: '▾';\\n}\\n\\n.ai-wbr-memory-fold summary span {\\n    grid-column: 1;\\n    grid-row: 1;\\n    padding-left: 1.05rem;\\n    font-weight: 700;\\n}\\n\\n.ai-wbr-memory-fold summary small {\\n    grid-column: 2;\\n    color: var(--SmartThemeQuoteColor);\\n    font-size: 0.78rem;\\n    line-height: 1.35;\\n}\\n\\n.ai-wbr-memory-fold-body {\\n    padding: 0 0.75rem 0.75rem;\\n}\\n\\n.ai-wbr-memory-fold-body.ai-wbr-grid,\\n.ai-wbr-memory-fold-body.ai-wbr-actions {\\n    margin-top: 0;\\n}\\n\\n.ai-wbr-memory-fold-attention {\\n    border-color: rgba(255, 196, 87, 0.55);\\n}\\n\\n.ai-wbr-memory-fold-attention summary {\\n    background: rgba(255, 196, 87, 0.08);\\n}\\n\\n.ai-wbr-memory-subtitle {\\n    margin-top: 0.8rem;\\n    margin-bottom: 0.45rem;\\n}\\n\\n.ai-wbr-memory-subtitle small {\\n    display: block;\\n    margin-top: 0.18rem;\\n    color: var(--SmartThemeQuoteColor);\\n    font-size: 0.78rem;\\n    font-weight: 400;\\n    line-height: 1.35;\\n}\\n\\n.ai-wbr-memory-history-import {\\n    margin-top: 0;\\n    padding: 0.75rem;\\n    border: 1px solid rgba(151, 207, 255, 0.24);\\n    border-radius: 10px;\\n    background:\\n        linear-gradient(135deg, rgba(125, 212, 255, 0.08), rgba(80, 255, 190, 0.05)),\\n        color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(20,20,24,0.9)) 94%, transparent 6%);\\n}\\n\\n.ai-wbr-memory-history-grid {\\n    display: grid;\\n    grid-template-columns: minmax(5rem, 0.8fr) minmax(6rem, 1fr);\\n    gap: 0.5rem 0.7rem;\\n    align-items: center;\\n}\\n\\n.ai-wbr-memory-history-actions {\\n    display: flex;\\n    flex-wrap: wrap;\\n    gap: 0.45rem;\\n    margin-top: 0.55rem;\\n}\\n\\n.ai-wbr-memory-history-status {\\n    margin-top: 0.55rem;\\n    color: var(--SmartThemeQuoteColor);\\n    font-size: 0.86rem;\\n    line-height: 1.4;\\n}\\n\\n.ai-wbr-memory-history-preview {\\n    margin-top: 0.5rem;\\n    display: grid;\\n    gap: 0.35rem;\\n}\\n\\n.ai-wbr-memory-history-preview-meta,\\n.ai-wbr-memory-history-preview-more {\\n    color: var(--SmartThemeQuoteColor);\\n    font-size: 0.78rem;\\n}\\n\\n.ai-wbr-memory-history-preview-item {\\n    display: grid;\\n    gap: 0.16rem;\\n    padding: 0.45rem 0.55rem;\\n    border: 1px solid rgba(151, 207, 255, 0.18);\\n    border-radius: 8px;\\n    background: rgba(255, 255, 255, 0.045);\\n}\\n\\n.ai-wbr-memory-history-preview-item b {\\n    font-size: 0.82rem;\\n}\\n\\n.ai-wbr-memory-history-preview-item span {\\n    color: var(--SmartThemeBodyColor);\\n    font-size: 0.84rem;\\n    line-height: 1.42;\\n    word-break: break-word;\\n}\\n\\n.ai-wbr-memory-state-desc {\\n    margin-top: 0.25rem;\\n    font-size: 0.75rem;\\n    font-weight: 400;\\n    line-height: 1.35;\\n    color: var(--SmartThemeQuoteColor);\\r\\n    opacity: 0.88;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-add-state {\\r\\n    display: grid;\\r\\n    grid-template-columns: 1fr 2fr auto;\\r\\n    gap: 0.5rem;\\r\\n    margin-bottom: 0.7rem;\\r\\n    align-items: center;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-node-editor-card {\\r\\n    padding: 0.75rem;\\r\\n    border: 1px solid var(--SmartThemeBorderColor);\\r\\n    border-radius: 10px;\\r\\n    background: color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(20,20,24,0.9)) 95%, transparent 5%);\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-node-editor-title {\\r\\n    font-weight: 700;\\r\\n    margin-bottom: 0.55rem;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-node-editor-grid {\\r\\n    display: grid;\\r\\n    grid-template-columns: repeat(2, minmax(0, 1fr));\\r\\n    gap: 0.6rem;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-node-editor-field {\\r\\n    display: flex;\\r\\n    flex-direction: column;\\r\\n    gap: 0.28rem;\\r\\n    font-size: 0.82rem;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-node-editor-field-wide {\\r\\n    grid-column: 1 / -1;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-table-wrap {\\r\\n    margin-top: 0.35rem;\\r\\n    overflow-x: auto;\\r\\n    border: 1px solid var(--SmartThemeBorderColor);\\r\\n    border-radius: 8px;\\r\\n    background: color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(20,20,24,0.88)) 94%, transparent 6%);\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-table {\\r\\n    width: 100%;\\r\\n    min-width: 42rem;\\r\\n    border-collapse: collapse;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-table th,\\r\\n.ai-wbr-memory-table td {\\r\\n    padding: 0.45rem 0.5rem;\\r\\n    border-bottom: 1px solid color-mix(in srgb, var(--SmartThemeBorderColor) 72%, transparent 28%);\\r\\n    vertical-align: top;\\r\\n    text-align: left;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-table thead th {\\r\\n    position: sticky;\\r\\n    top: 0;\\r\\n    z-index: 1;\\r\\n    background: color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(20,20,24,0.96)) 96%, black 4%);\\r\\n    font-weight: 700;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-table tbody tr:last-child th,\\r\\n.ai-wbr-memory-table tbody tr:last-child td {\\r\\n    border-bottom: none;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-table th[scope=\\\"row\\\"] {\\r\\n    width: 8.5rem;\\r\\n    min-width: 8.5rem;\\r\\n    color: var(--SmartThemeQuoteColor);\\r\\n    font-weight: 600;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-table .text_pole {\\r\\n    width: 100%;\\r\\n    box-sizing: border-box;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-event-actions {\\r\\n    display: grid;\\r\\n    gap: 0.35rem;\\r\\n}\\r\\n\\r\\n.m-t-05 {\\r\\n    margin-top: 0.35rem;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-list {\\r\\n    display: flex;\\r\\n    flex-direction: column;\\r\\n    gap: 0.5rem;\\r\\n    margin-top: 0.45rem;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-card,\\r\\n.ai-wbr-memory-link-card {\\r\\n    border: 1px solid var(--SmartThemeBorderColor);\\r\\n    border-radius: 8px;\\r\\n    padding: 0.55rem;\\r\\n    background: var(--SmartThemeBlurTintColor);\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-link-card-selected {\\r\\n    border-color: rgba(255, 214, 132, 0.8);\\r\\n    box-shadow: 0 0 0 1px rgba(255, 214, 132, 0.35) inset;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-card-head,\\r\\n.ai-wbr-memory-link-grid {\\r\\n    display: grid;\\r\\n    grid-template-columns: minmax(8rem, 1fr) minmax(5rem, 8rem) auto;\\r\\n    gap: 0.45rem;\\r\\n    align-items: center;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-type {\\r\\n    min-width: 5rem;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-content {\\r\\n    margin-top: 0.45rem;\\r\\n    resize: vertical;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-link-title {\\r\\n    margin-bottom: 0.4rem;\\r\\n    font-weight: 600;\\r\\n    opacity: 0.92;\\r\\n    word-break: break-word;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-json {\\r\\n    width: 100%;\\r\\n    box-sizing: border-box;\\r\\n    font-family: var(--monoFontFamily, monospace);\\r\\n    line-height: 1.35;\\r\\n    white-space: pre;\\r\\n    resize: vertical;\\r\\n}\\r\\n\\r\\n.ai-wbr-memory-debug-panel {\\r\\n    display: none;\\r\\n}\\r\\n\\r\\n.ai-wbr-prompt {\\r\\n    min-height: 7rem;\\r\\n    resize: vertical;\\r\\n}\\r\\n\\r\\n.ai-wbr-token-editor {\\r\\n    display: flex;\\r\\n    gap: 0.55rem;\\r\\n    align-items: center;\\r\\n    margin-top: 0.35rem;\\r\\n}\\r\\n\\r\\n.ai-wbr-token-list {\\r\\n    display: flex;\\r\\n    flex-wrap: wrap;\\r\\n    gap: 0.45rem;\\r\\n    margin-top: 0.6rem;\\r\\n}\\r\\n\\r\\n.ai-wbr-token-item {\\r\\n    display: inline-flex;\\r\\n    align-items: center;\\r\\n    gap: 0.45rem;\\r\\n    padding: 0.32rem 0.48rem 0.32rem 0.58rem;\\r\\n    border: 1px solid var(--SmartThemeBorderColor);\\r\\n    border-radius: 999px;\\r\\n    background: var(--SmartThemeBlurTintColor);\\r\\n    max-width: 100%;\\r\\n    box-sizing: border-box;\\r\\n}\\r\\n\\r\\n.ai-wbr-token-label {\\r\\n    white-space: normal;\\r\\n    word-break: break-word;\\r\\n    overflow-wrap: anywhere;\\r\\n    line-height: 1.2;\\r\\n}\\r\\n\\r\\n.ai-wbr-token-remove {\\r\\n    width: 1.35rem;\\r\\n    height: 1.35rem;\\r\\n    border: none;\\r\\n    border-radius: 999px;\\r\\n    background: rgba(255, 255, 255, 0.08);\\r\\n    color: inherit;\\r\\n    cursor: pointer;\\r\\n    display: inline-flex;\\r\\n    align-items: center;\\r\\n    justify-content: center;\\r\\n    padding: 0;\\r\\n    flex: 0 0 auto;\\r\\n}\\r\\n\\r\\n.ai-wbr-token-remove:hover {\\r\\n    background: rgba(255, 255, 255, 0.16);\\r\\n}\\r\\n\\r\\n.ai-wbr-token-empty {\\r\\n    opacity: 0.72;\\r\\n    font-size: 0.92em;\\r\\n}\\r\\n\\r\\n.ai-wbr-world-list {\\r\\n    display: flex;\\r\\n    flex-direction: column;\\r\\n    gap: 0.35rem;\\r\\n    margin-top: 0.55rem;\\r\\n    padding: 0.55rem 0.65rem;\\r\\n    border: 1px solid var(--SmartThemeBorderColor);\\r\\n    border-radius: 8px;\\r\\n    background: color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(20,20,24,0.88)) 94%, transparent 6%);\\r\\n}\\r\\n\\r\\n.ai-wbr-world-option {\\r\\n    display: flex;\\r\\n    align-items: center;\\r\\n    gap: 0.45rem;\\r\\n    flex-wrap: wrap;\\r\\n}\\r\\n\\r\\n.ai-wbr-world-option small {\\r\\n    opacity: 0.72;\\r\\n}\\r\\n\\r\\n.ai-wbr-debug {\\r\\n    margin-top: 0.75rem;\\r\\n}\\r\\n\\r\\n.ai-wbr-router-raw-block {\\r\\n    margin-top: 0.75rem;\\r\\n}\\r\\n\\r\\n.ai-wbr-router-raw {\\r\\n    margin-top: 0.45rem;\\r\\n    max-height: 14rem;\\r\\n    overflow: auto;\\r\\n    padding: 0.55rem 0.65rem;\\r\\n    border: 1px solid var(--SmartThemeBorderColor);\\r\\n    border-radius: 6px;\\r\\n    background-color: var(--SmartThemeBlurTintColor);\\r\\n    box-sizing: border-box;\\r\\n    white-space: pre-wrap;\\r\\n    word-break: break-word;\\r\\n    overflow-wrap: anywhere;\\r\\n    writing-mode: horizontal-tb;\\r\\n    font-family: var(--monoFontFamily, monospace);\\r\\n    font-size: 0.92em;\\r\\n    line-height: 1.35;\\r\\n}\\r\\n\\r\\n.ai-wbr-last-items {\\r\\n    display: flex;\\r\\n    flex-direction: column;\\r\\n    gap: 0.4rem;\\r\\n    margin-top: 0.5rem;\\r\\n    max-height: 14rem;\\r\\n    overflow: auto;\\r\\n    width: 100%;\\r\\n    writing-mode: horizontal-tb;\\r\\n}\\r\\n\\r\\n.ai-wbr-last-item {\\r\\n    border: 1px solid var(--SmartThemeBorderColor);\\r\\n    border-radius: 6px;\\r\\n    padding: 0.45rem 0.55rem;\\r\\n    background-color: var(--SmartThemeBlurTintColor);\\r\\n    width: 100%;\\r\\n    box-sizing: border-box;\\r\\n    writing-mode: horizontal-tb;\\r\\n    white-space: normal;\\r\\n    word-break: break-word;\\r\\n    overflow-wrap: anywhere;\\r\\n}\\r\\n\\r\\n.ai-wbr-last-item small {\\r\\n    opacity: 0.8;\\r\\n    display: block;\\r\\n    margin-top: 0.2rem;\\r\\n    writing-mode: horizontal-tb;\\r\\n    white-space: normal;\\r\\n    word-break: break-word;\\r\\n    overflow-wrap: anywhere;\\r\\n}\\r\\n\\r\\n#ai_wbr_last_summary {\\r\\n    width: 100%;\\r\\n    box-sizing: border-box;\\r\\n    writing-mode: horizontal-tb;\\r\\n    white-space: normal;\\r\\n    word-break: break-word;\\r\\n    overflow-wrap: anywhere;\\r\\n}\\r\\n\\r\\n@media (max-width: 640px) {\\n    .ai-wbr-grid {\\n        grid-template-columns: 1fr;\\n    }\\n\\n    .ai-wbr-memory-fold summary {\\n        grid-template-columns: 1fr;\\n    }\\n\\n    .ai-wbr-memory-fold summary small {\\n        grid-column: 1;\\n        padding-left: 1.05rem;\\n    }\\n\\n    .ai-wbr-memory-history-grid {\\n        grid-template-columns: 1fr;\\n    }\\n\\n    .ai-wbr-memory-history-actions .menu_button {\\n        flex: 1 1 auto;\\n    }\\n\\n    .ai-wbr-floating-window.ai-wbr-floating-window-graph {\\n        height: min(82dvh, 40rem);\\n        max-height: 82dvh;\\n    }\\n\\n    .ai-wbr-floating-window-graph .ai-wbr-floating-header,\\n    .ai-wbr-floating-window-graph .ai-wbr-console-tabs {\\n        flex: 0 0 auto;\\n    }\\n\\n    .ai-wbr-graph-topbar {\\n        align-items: flex-start;\\n        padding: 0.75rem 0.85rem;\\n    }\\n\\n    .ai-wbr-graph-topbar-actions {\\n        flex-wrap: nowrap;\\n        gap: 0.4rem;\\n    }\\n\\n    .ai-wbr-graph-topbar-actions .menu_button {\\n        min-width: 2.6rem;\\n        max-width: 3.2rem;\\n        padding: 0.35rem 0.45rem;\\n        line-height: 1.15;\\n        white-space: normal;\\n        word-break: keep-all;\\n    }\\n\\n    .ai-wbr-graph-shell {\\n        display: block;\\n        position: relative;\\n        flex: 1 1 auto;\\n        height: 100%;\\n        min-height: 0;\\n        overflow: hidden;\\n    }\\n\\n    .ai-wbr-graph-workspace .ai-wbr-memory-graph {\\n        width: 100%;\\n        height: 100%;\\n        min-height: 24rem;\\n    }\\n\\n    .ai-wbr-memory-detail-drawer {\\n        position: absolute;\\n        inset: 0 0 0 auto;\\n        z-index: 5;\\n        width: min(92vw, 24rem);\\n        max-width: calc(100vw - 1.5rem);\\n        height: 100%;\\n        border-left: 1px solid rgba(176, 225, 255, 0.22);\\n        box-shadow: -18px 0 34px rgba(0, 0, 0, 0.36);\\n        transform: translateX(105%);\\n        transition: transform 0.22s ease;\\n    }\\n\\n    .ai-wbr-memory-detail-drawer.open {\\n        transform: translateX(0);\\n    }\\n\\n    .ai-wbr-memory-detail-grid {\\n        grid-template-columns: 1fr;\\n    }\\n\\n    .ai-wbr-token-editor {\\n        flex-direction: column;\\n        align-items: stretch;\\n    }\\n\\r\\n    .ai-wbr-memory-card-head,\\n    .ai-wbr-memory-link-grid {\\n        grid-template-columns: 1fr;\\n    }\\n\\n    .ai-wbr-memory-dashboard {\\n        grid-template-columns: repeat(2, minmax(0, 1fr));\\n    }\\n\\n    .ai-wbr-memory-review-head {\\n        flex-direction: column;\\n    }\\n\\n    .ai-wbr-memory-review-actions {\\n        width: 100%;\\n        justify-content: stretch;\\n    }\\n\\n    .ai-wbr-memory-review-actions .menu_button {\\n        flex: 1 1 auto;\\n    }\\n}\\n\\r\\n@keyframes ai-wbr-book-pulse {\\r\\n    0%, 100% {\\r\\n        opacity: 0.62;\\r\\n        transform: translateX(-50%) scaleX(0.8) scaleY(0.84);\\r\\n    }\\r\\n\\r\\n    50% {\\r\\n        opacity: 1;\\r\\n        transform: translateX(-50%) scaleX(1.14) scaleY(1.22);\\r\\n    }\\r\\n}\\r\\n\\r\\n@keyframes ai-wbr-book-sheen {\\r\\n    0% {\\r\\n        opacity: 0;\\r\\n        transform: translate(-50%, -50%) translateX(-1.15rem);\\r\\n    }\\r\\n\\r\\n    16% {\\r\\n        opacity: 0.92;\\r\\n    }\\r\\n\\r\\n    60% {\\r\\n        opacity: 0.8;\\r\\n    }\\r\\n\\r\\n    100% {\\r\\n        opacity: 0;\\r\\n        transform: translate(-50%, -50%) translateX(1.15rem);\\r\\n    }\\r\\n}\\r\\n\\r\\n@keyframes ai-wbr-entry-burst {\\r\\n    0% {\\r\\n        opacity: 0;\\r\\n        transform: translate(-50%, -50%) translate3d(0, 0, 0) scale(0.45) rotate(0deg);\\r\\n    }\\r\\n\\r\\n    12% {\\r\\n        opacity: 1;\\r\\n        transform: translate(-50%, -50%) translate3d(0, -8px, 0) scale(0.92) rotate(0deg);\\r\\n    }\\r\\n\\r\\n    42% {\\r\\n        opacity: 0.98;\\r\\n        transform: translate(-50%, -50%) translate3d(var(--burst-x), var(--burst-y), 0) scale(1) rotate(var(--burst-tilt));\\r\\n    }\\r\\n\\r\\n    68% {\\r\\n        opacity: 0.94;\\r\\n        transform: translate(-50%, -50%) translate3d(calc(var(--burst-x) * 0.62), calc(var(--burst-y) * 0.8), 0) scale(0.96) rotate(calc(var(--burst-tilt) * 0.5));\\r\\n    }\\r\\n\\r\\n    100% {\\r\\n        opacity: 0;\\r\\n        transform: translate(-50%, -50%) translate3d(0, 0, 0) scale(0.38) rotate(0deg);\\r\\n    }\\r\\n}\\r\\n\\r\\n@keyframes ai-wbr-status-burst {\\r\\n    0% {\\r\\n        opacity: 0;\\r\\n        transform: translate(-50%, -50%) translate3d(0, 0, 0) scale(0.5) rotate(-18deg);\\r\\n    }\\r\\n\\r\\n    18% {\\r\\n        opacity: 1;\\r\\n        transform: translate(-50%, -50%) translate3d(0, -10px, 0) scale(1.1) rotate(10deg);\\r\\n    }\\r\\n\\r\\n    55% {\\r\\n        opacity: 1;\\r\\n        transform: translate(-50%, -50%) translate3d(0, 24px, 0) scale(1) rotate(-8deg);\\r\\n    }\\r\\n\\r\\n    100% {\\r\\n        opacity: 0;\\r\\n        transform: translate(-50%, -50%) translate3d(0, 56px, 0) scale(0.88) rotate(12deg);\\r\\n    }\\r\\n}\\r\\n\\r\\n@keyframes ai-wbr-memory-orbit-pulse {\\r\\n    0%, 100% {\\r\\n        opacity: 0.76;\\r\\n        transform: translate(-50%, -50%) scale(0.88) rotate(0deg);\\r\\n    }\\r\\n\\r\\n    50% {\\r\\n        opacity: 1;\\r\\n        transform: translate(-50%, -50%) scale(1.08) rotate(4deg);\\r\\n    }\\r\\n}\\r\\n\\r\\n@keyframes ai-wbr-memory-orbit-dot {\\r\\n    0% {\\r\\n        transform: rotate(0deg) translateX(1.65rem) scale(0.78);\\r\\n    }\\r\\n\\r\\n    50% {\\r\\n        transform: rotate(180deg) translateX(1.65rem) scale(1.18);\\r\\n    }\\r\\n\\r\\n    100% {\\r\\n        transform: rotate(360deg) translateX(1.65rem) scale(0.78);\\r\\n    }\\r\\n}\\r\\n\\r\\n@keyframes ai-wbr-memory-done {\\r\\n    0% {\\r\\n        opacity: 1;\\r\\n        transform: translate(-50%, -50%) scale(1);\\r\\n    }\\r\\n\\r\\n    100% {\\r\\n        opacity: 0;\\r\\n        transform: translate(-50%, -50%) scale(1.55);\\r\\n    }\\r\\n}\\r\\n\\r\\n@keyframes ai-wbr-error-flash {\\r\\n    0%, 100% {\\r\\n        opacity: 1;\\r\\n    }\\r\\n\\r\\n    50% {\\r\\n        opacity: 0.22;\\r\\n    }\\r\\n}\\r\\n\\n.ai-wbr-extension-entry {\\n    cursor: pointer;\\n}\\n\\n.ai-wbr-extension-row {\\n    align-items: center;\\n}\\n\\n.ai-wbr-extension-icon {\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 82%, #bfeeff 18%);\\n}\\n\\n.ai-wbr-extension-label {\\n    color: var(--SmartThemeBodyColor, #fff);\\n}\\n\\n.ai-wbr-bookshelf-panel {\\n    position: relative;\\n    overflow: hidden;\\n}\\n\\n.ai-wbr-bookshelf-app {\\n    position: relative;\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.95rem;\\n    min-height: 34rem;\\n    padding: 1.15rem;\\n    border: 1px solid color-mix(in srgb, var(--SmartThemeBorderColor, #d9d9d9) 42%, transparent 58%);\\n    border-radius: 18px;\\n    background: color-mix(in srgb, var(--SmartThemeBlurTintColor, #ffffff) 94%, #f6f3ee 6%);\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #222) 94%, #111 6%);\\n    font-family: system-ui, -apple-system, BlinkMacSystemFont, \\\"Segoe UI\\\", sans-serif;\\n}\\n\\n.ai-wbr-bookshelf-top {\\n    display: flex;\\n    align-items: center;\\n    justify-content: space-between;\\n    gap: 1rem;\\n}\\n\\n.ai-wbr-bookshelf-title {\\n    font-size: 2.15rem;\\n    line-height: 1.15;\\n    font-weight: 700;\\n    letter-spacing: 0;\\n}\\n\\n.ai-wbr-bookshelf-more {\\n    width: 3rem;\\n    height: 2.2rem;\\n    border: 0;\\n    border-radius: 999px;\\n    background: transparent;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #222) 86%, #111 14%);\\n    cursor: pointer;\\n    font-size: 1.75rem;\\n    font-weight: 700;\\n    line-height: 1;\\n}\\n\\n.ai-wbr-bookshelf-tabs {\\n    display: flex;\\n    align-items: center;\\n    justify-content: space-between;\\n    gap: 0.75rem;\\n    margin-top: 0.25rem;\\n}\\n\\n.ai-wbr-bookshelf-library-tabs {\\n    margin-top: 0.45rem;\\n}\\n\\n.ai-wbr-bookshelf-library-actions {\\n    display: inline-flex;\\n    align-items: center;\\n    gap: 0.8rem;\\n}\\n\\n.ai-wbr-bookshelf-tab-divider {\\n    width: 1px;\\n    height: 1.25rem;\\n    background: color-mix(in srgb, var(--SmartThemeBodyColor, #222) 18%, transparent 82%);\\n}\\n\\n.ai-wbr-bookshelf-mini-actions {\\n    display: flex;\\n    flex-wrap: wrap;\\n    gap: 0.55rem;\\n    margin-top: -0.25rem;\\n}\\n\\n.ai-wbr-bookshelf-pill-action {\\n    border: 1px solid color-mix(in srgb, var(--SmartThemeBorderColor, #d9d9d9) 52%, transparent 48%);\\n    border-radius: 999px;\\n    padding: 0.36rem 0.7rem;\\n    background: color-mix(in srgb, var(--SmartThemeBlurTintColor, #fff) 88%, #f3f4f6 12%);\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #222) 78%, transparent 22%);\\n    cursor: pointer;\\n    font-size: 0.84rem;\\n}\\n\\n.ai-wbr-bookshelf-read-badge {\\n    display: inline-flex;\\n    align-items: center;\\n    gap: 0.35rem;\\n    border-radius: 999px;\\n    padding: 0.5rem 0.9rem;\\n    background: color-mix(in srgb, var(--SmartThemeBodyColor, #222) 7%, transparent 93%);\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #222) 82%, transparent 18%);\\n    font-size: 0.98rem;\\n    font-weight: 600;\\n}\\n\\n.ai-wbr-bookshelf-read-badge::before {\\n    content: \\\"\\\";\\n    width: 0.7rem;\\n    height: 0.7rem;\\n    border-radius: 999px;\\n    background: #ffc64d;\\n    box-shadow: inset 0 0 0 0.18rem rgba(255, 255, 255, 0.5);\\n}\\n\\n.ai-wbr-bookshelf-link {\\n    border: 0;\\n    border-radius: 8px;\\n    background: transparent;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #222) 86%, transparent 14%);\\n    cursor: pointer;\\n    font-size: 1rem;\\n    white-space: nowrap;\\n}\\n\\n.ai-wbr-bookshelf-link:hover,\\n.ai-wbr-bookshelf-more:hover {\\n    background: color-mix(in srgb, var(--SmartThemeBodyColor, #222) 8%, transparent 92%);\\n}\\n\\n.ai-wbr-bookshelf-status {\\n    min-height: 1rem;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #222) 48%, transparent 52%);\\n    font-size: 0.78rem;\\n    line-height: 1.4;\\n}\\n\\n.ai-wbr-bookshelf-subtitle {\\n    margin-top: -0.35rem;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #222) 58%, transparent 42%);\\n    font-size: 0.9rem;\\n}\\n\\n.ai-wbr-bookshelf-top-actions {\\n    display: inline-flex;\\n    align-items: center;\\n    gap: 0.45rem;\\n}\\n\\n.ai-wbr-bookshelf-import-button {\\n    border: 1px solid color-mix(in srgb, var(--SmartThemeBorderColor, #d9d9d9) 58%, transparent 42%);\\n    border-radius: 999px;\\n    padding: 0.42rem 0.78rem;\\n    background: color-mix(in srgb, #7dd3fc 24%, var(--SmartThemeBlurTintColor, #fff) 76%);\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #222) 88%, #0ea5e9 12%);\\n    cursor: pointer;\\n    font-size: 0.88rem;\\n    font-weight: 700;\\n    white-space: nowrap;\\n}\\n\\n.ai-wbr-bookshelf-empty {\\n    grid-column: 1 / -1;\\n    display: flex;\\n    min-height: 12rem;\\n    flex-direction: column;\\n    align-items: center;\\n    justify-content: center;\\n    gap: 0.5rem;\\n    border: 1px dashed color-mix(in srgb, var(--SmartThemeBorderColor, #4b5563) 64%, transparent 36%);\\n    border-radius: 14px;\\n    padding: 1.25rem;\\n    background: color-mix(in srgb, var(--SmartThemeBodyColor, #ffffff) 5%, transparent 95%);\\n    text-align: center;\\n}\\n\\n.ai-wbr-bookshelf-empty b {\\n    font-size: 1.05rem;\\n}\\n\\n.ai-wbr-bookshelf-empty span {\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #222) 54%, transparent 46%);\\n    font-size: 0.9rem;\\n}\\n\\n.ai-wbr-bookshelf-toolbar,\\n.ai-wbr-bookshelf-quickbar,\\n.ai-wbr-bookshelf-section-title,\\n.ai-wbr-bookshelf-drawer-head {\\n    display: flex;\\n    gap: 0.75rem;\\n    align-items: center;\\n    justify-content: space-between;\\n}\\n\\n.ai-wbr-bookshelf-toolbar {\\n    border: 1px solid color-mix(in srgb, var(--SmartThemeBorderColor, #4b5563) 62%, transparent 38%);\\n    border-radius: 12px;\\n    padding: 0.75rem;\\n    background: color-mix(in srgb, var(--SmartThemeBlurTintColor, #111827) 84%, transparent 16%);\\n}\\n\\n.ai-wbr-bookshelf-toolbar b {\\n    display: block;\\n    font-size: 1rem;\\n}\\n\\n.ai-wbr-bookshelf-toolbar small,\\n.ai-wbr-bookshelf-drawer-head small,\\n.ai-wbr-bookshelf-section-title span {\\n    display: block;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 64%, transparent 36%);\\n    font-size: 0.82rem;\\n}\\n\\n.ai-wbr-bookshelf-toolbar-actions {\\n    display: flex;\\n    flex-wrap: wrap;\\n    gap: 0.45rem;\\n    justify-content: flex-end;\\n}\\n\\n.ai-wbr-bookshelf-quickbar {\\n    flex-wrap: wrap;\\n    justify-content: flex-start;\\n}\\n\\n.ai-wbr-bookshelf-quickbar .text_pole {\\n    min-width: 10rem;\\n    flex: 1 1 11rem;\\n}\\n\\n.ai-wbr-bookshelf-switches,\\n.ai-wbr-bookshelf-import,\\n.ai-wbr-bookshelf-actions {\\n    display: flex;\\n    flex-wrap: wrap;\\n    gap: 0.5rem;\\n    align-items: center;\\n}\\n\\n.ai-wbr-bookshelf-import .text_pole {\\n    min-width: 9rem;\\n    flex: 1 1 10rem;\\n}\\n\\n.ai-wbr-bookshelf-layout {\\n    display: grid;\\n    grid-template-columns: minmax(18rem, 1.25fr) minmax(18rem, 0.85fr);\\n    gap: 0.75rem;\\n    align-items: start;\\n}\\n\\n.ai-wbr-bookshelf-layout section,\\n.ai-wbr-bookshelf-test,\\n.ai-wbr-bookshelf-settings-drawer {\\n    border: 1px solid color-mix(in srgb, var(--SmartThemeBorderColor, #4b5563) 70%, transparent 30%);\\n    border-radius: 12px;\\n    padding: 0.75rem;\\n    background: color-mix(in srgb, var(--SmartThemeBlurTintColor, #111827) 78%, transparent 22%);\\n}\\n\\n.ai-wbr-bookshelf-main,\\n.ai-wbr-bookshelf-detail-pane {\\n    min-height: 26rem;\\n}\\n\\n.ai-wbr-bookshelf-scope,\\n.ai-wbr-bookshelf-books,\\n.ai-wbr-bookshelf-detail,\\n.ai-wbr-bookshelf-results {\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.55rem;\\n}\\n\\n.ai-wbr-bookshelf-books {\\n    display: grid;\\n    grid-template-columns: repeat(3, minmax(0, 1fr));\\n    gap: 1.45rem 1.15rem;\\n    align-items: start;\\n}\\n\\n.ai-wbr-bookshelf-scope-card,\\n.ai-wbr-bookshelf-book,\\n.ai-wbr-bookshelf-result,\\n.ai-wbr-bookshelf-chunk {\\n    border: 1px solid color-mix(in srgb, var(--SmartThemeBorderColor, #4b5563) 58%, transparent 42%);\\n    border-radius: 10px;\\n    padding: 0.65rem;\\n    background: color-mix(in srgb, var(--SmartThemeBodyColor, #ffffff) 7%, transparent 93%);\\n}\\n\\n.ai-wbr-bookshelf-book {\\n    position: relative;\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.35rem;\\n    min-width: 0;\\n    border: 0;\\n    padding: 0;\\n    background: transparent;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 92%, #d7f5ff 8%);\\n    cursor: pointer;\\n}\\n\\n.ai-wbr-bookshelf-book.selected {\\n    border-color: transparent;\\n    box-shadow: none;\\n}\\n\\n.ai-wbr-bookshelf-book.selected .ai-wbr-bookshelf-cover {\\n    outline: 2px solid rgba(125, 212, 255, 0.85);\\n    outline-offset: 3px;\\n}\\n\\n.ai-wbr-bookshelf-memory-book .ai-wbr-bookshelf-cover {\\n    border-radius: 10px;\\n    background:\\n        radial-gradient(circle at 18% 18%, rgba(255, 255, 255, 0.18), transparent 28%),\\n        linear-gradient(145deg, rgba(255, 248, 235, 0.2), rgba(0, 0, 0, 0.24)),\\n        linear-gradient(135deg, #ff6a2a, #d7263d 46%, #24111f);\\n    box-shadow: 0 12px 28px rgba(215, 38, 61, 0.22);\\n}\\n\\n.ai-wbr-bookshelf-worldbook .ai-wbr-bookshelf-cover {\\n    background:\\n        radial-gradient(circle at 18% 18%, rgba(255, 255, 255, 0.16), transparent 28%),\\n        linear-gradient(145deg, rgba(210, 255, 232, 0.17), rgba(0, 0, 0, 0.2)),\\n        linear-gradient(135deg, #0f766e, #134e4a 48%, #111827);\\n    box-shadow: 0 12px 28px rgba(20, 184, 166, 0.18);\\n}\\n\\n.ai-wbr-bookshelf-cover-mark {\\n    position: absolute;\\n    top: 0.55rem;\\n    left: 0.55rem;\\n    border-radius: 999px;\\n    padding: 0.12rem 0.42rem;\\n    background: rgba(255, 255, 255, 0.18);\\n    color: rgba(255, 255, 255, 0.92);\\n    font-size: 0.72rem;\\n    font-weight: 700;\\n}\\n\\n.ai-wbr-bookshelf-cover {\\n    position: relative;\\n    aspect-ratio: 3 / 4.35;\\n    width: 100%;\\n    overflow: hidden;\\n    border: 0;\\n    border-radius: 8px;\\n    padding: 0.72rem;\\n    background:\\n        linear-gradient(145deg, rgba(255, 255, 255, 0.08), rgba(0, 0, 0, 0.28)),\\n        linear-gradient(135deg, #2d3142, #784421 45%, #111827);\\n    box-shadow: 0 8px 18px rgba(0, 0, 0, 0.18);\\n    color: #fff;\\n    cursor: pointer;\\n    text-align: left;\\n}\\n\\n.ai-wbr-bookshelf-book:nth-child(3n + 2) .ai-wbr-bookshelf-cover {\\n    background:\\n        linear-gradient(145deg, rgba(255, 255, 255, 0.14), rgba(0, 0, 0, 0.2)),\\n        linear-gradient(135deg, #e8eef7, #8aa2b8 46%, #34495e);\\n}\\n\\n.ai-wbr-bookshelf-book:nth-child(3n) .ai-wbr-bookshelf-cover {\\n    background:\\n        linear-gradient(145deg, rgba(255, 255, 255, 0.08), rgba(0, 0, 0, 0.26)),\\n        linear-gradient(135deg, #451a1a, #dc3a1a 46%, #111827);\\n}\\n\\n.ai-wbr-bookshelf-book:nth-child(4n) .ai-wbr-bookshelf-cover {\\n    background:\\n        linear-gradient(145deg, rgba(255, 255, 255, 0.1), rgba(0, 0, 0, 0.24)),\\n        linear-gradient(135deg, #1f2937, #64748b 48%, #020617);\\n}\\n\\n.ai-wbr-bookshelf-cover b {\\n    position: absolute;\\n    inset: auto 0.65rem 1.9rem;\\n    display: -webkit-box;\\n    overflow: hidden;\\n    color: #fff;\\n    font-size: 1.05rem;\\n    font-weight: 800;\\n    line-height: 1.18;\\n    text-shadow: 0 2px 8px rgba(0, 0, 0, 0.45);\\n    -webkit-box-orient: vertical;\\n    -webkit-line-clamp: 4;\\n}\\n\\n.ai-wbr-bookshelf-cover small {\\n    position: absolute;\\n    right: 0.55rem;\\n    bottom: 0.55rem;\\n    left: 0.65rem;\\n    overflow: hidden;\\n    color: rgba(255, 255, 255, 0.86);\\n    font-size: 0.78rem;\\n    text-overflow: ellipsis;\\n    white-space: nowrap;\\n}\\n\\n.ai-wbr-bookshelf-cover-badge {\\n    position: absolute;\\n    top: 0.35rem;\\n    right: 0.35rem;\\n    border-radius: 4px;\\n    padding: 0.12rem 0.35rem;\\n    background: rgba(17, 17, 17, 0.78);\\n    color: #fff;\\n    font-size: 0.72rem;\\n    font-weight: 700;\\n}\\n\\n.ai-wbr-bookshelf-book-title {\\n    display: -webkit-box;\\n    overflow: hidden;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #222) 92%, transparent 8%);\\n    font-size: 1.02rem;\\n    line-height: 1.36;\\n    -webkit-box-orient: vertical;\\n    -webkit-line-clamp: 2;\\n}\\n\\n.ai-wbr-bookshelf-book-provider {\\n    display: none;\\n}\\n\\n.ai-wbr-bookshelf-scope-card b,\\n.ai-wbr-bookshelf-scope-card span {\\n    display: block;\\n}\\n\\n.ai-wbr-bookshelf-scope-card span,\\n.ai-wbr-bookshelf-book-meta,\\n.ai-wbr-bookshelf-result small {\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #222) 42%, transparent 58%);\\n    font-size: 0.88rem;\\n}\\n\\n.ai-wbr-bookshelf-book-head,\\n.ai-wbr-bookshelf-detail-head,\\n.ai-wbr-bookshelf-result-head,\\n.ai-wbr-bookshelf-chunk-head {\\n    display: flex;\\n    justify-content: space-between;\\n    gap: 0.5rem;\\n    align-items: center;\\n}\\n\\n.ai-wbr-bookshelf-book-head span,\\n.ai-wbr-bookshelf-detail-head span,\\n.ai-wbr-bookshelf-result-head span,\\n.ai-wbr-bookshelf-chunk-head span {\\n    font-size: 0.78rem;\\n    border-radius: 999px;\\n    padding: 0.15rem 0.45rem;\\n    background: color-mix(in srgb, #7dd3fc 18%, transparent 82%);\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 88%, #7dd3fc 12%);\\n    white-space: nowrap;\\n}\\n\\n.ai-wbr-bookshelf-tags {\\n    display: flex;\\n    flex-wrap: wrap;\\n    gap: 0.35rem;\\n    margin-top: 0.45rem;\\n}\\n\\n.ai-wbr-bookshelf-tags span {\\n    border-radius: 999px;\\n    padding: 0.12rem 0.45rem;\\n    font-size: 0.76rem;\\n    background: color-mix(in srgb, var(--SmartThemeQuoteColor, #6b7280) 22%, transparent 78%);\\n}\\n\\n.ai-wbr-bookshelf-book .ai-wbr-bookshelf-actions,\\n.ai-wbr-bookshelf-detail .ai-wbr-bookshelf-actions {\\n    margin-top: 0.55rem;\\n}\\n\\n.ai-wbr-bookshelf-detail {\\n    display: none;\\n    border: 1px solid rgba(125, 212, 255, 0.16);\\n    border-radius: 12px;\\n    padding: 0.75rem;\\n    background: rgba(255, 255, 255, 0.035);\\n}\\n\\n.ai-wbr-bookshelf-detail.open {\\n    display: flex;\\n}\\n\\n.ai-wbr-bookshelf-selected-detail {\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.55rem;\\n}\\n\\n.ai-wbr-bookshelf-source-summary {\\n    border-left: 3px solid rgba(125, 212, 255, 0.65);\\n    padding: 0.45rem 0.65rem;\\n    background: rgba(125, 212, 255, 0.08);\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 78%, transparent 22%);\\n    font-size: 0.86rem;\\n    line-height: 1.45;\\n}\\n\\n.ai-wbr-bookshelf-novel-hero {\\n    display: grid;\\n    grid-template-columns: minmax(7rem, 0.28fr) minmax(0, 1fr);\\n    gap: 0.9rem;\\n    align-items: stretch;\\n    border-radius: 14px;\\n    padding: 0.85rem;\\n    background:\\n        linear-gradient(135deg, rgba(255, 106, 42, 0.14), rgba(215, 38, 61, 0.1)),\\n        rgba(255, 255, 255, 0.045);\\n}\\n\\n.ai-wbr-bookshelf-novel-cover {\\n    position: relative;\\n    display: flex;\\n    min-height: 10rem;\\n    flex-direction: column;\\n    justify-content: flex-end;\\n    overflow: hidden;\\n    border-radius: 12px;\\n    padding: 0.8rem;\\n    background:\\n        radial-gradient(circle at 20% 20%, rgba(255, 255, 255, 0.18), transparent 30%),\\n        linear-gradient(145deg, rgba(255, 255, 255, 0.14), rgba(0, 0, 0, 0.3)),\\n        linear-gradient(135deg, #ff7a30, #df2f45 48%, #22111f);\\n    box-shadow: 0 14px 30px rgba(215, 38, 61, 0.22);\\n    color: #fff;\\n}\\n\\n.ai-wbr-bookshelf-novel-cover span {\\n    position: absolute;\\n    top: 0.65rem;\\n    left: 0.65rem;\\n    border-radius: 999px;\\n    padding: 0.12rem 0.46rem;\\n    background: rgba(255, 255, 255, 0.2);\\n    font-size: 0.72rem;\\n    font-weight: 800;\\n}\\n\\n.ai-wbr-bookshelf-novel-cover b {\\n    display: -webkit-box;\\n    overflow: hidden;\\n    font-size: 1.05rem;\\n    line-height: 1.18;\\n    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.42);\\n    -webkit-box-orient: vertical;\\n    -webkit-line-clamp: 4;\\n}\\n\\n.ai-wbr-bookshelf-novel-cover small {\\n    margin-top: 0.45rem;\\n    color: rgba(255, 255, 255, 0.82);\\n    font-size: 0.8rem;\\n}\\n\\n.ai-wbr-bookshelf-novel-info {\\n    display: flex;\\n    min-width: 0;\\n    flex-direction: column;\\n    justify-content: center;\\n    gap: 0.55rem;\\n}\\n\\n.ai-wbr-bookshelf-novel-info strong {\\n    display: block;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 94%, #ff9b65 6%);\\n    font-size: 1.25rem;\\n    line-height: 1.22;\\n}\\n\\n.ai-wbr-bookshelf-novel-info p {\\n    margin: 0;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 68%, transparent 32%);\\n    font-size: 0.9rem;\\n    line-height: 1.55;\\n}\\n\\n.ai-wbr-bookshelf-novel-stats {\\n    display: flex;\\n    flex-wrap: wrap;\\n    gap: 0.4rem;\\n}\\n\\n.ai-wbr-bookshelf-novel-stats span {\\n    border-radius: 999px;\\n    padding: 0.18rem 0.52rem;\\n    background: rgba(255, 106, 42, 0.14);\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 82%, #ff9b65 18%);\\n    font-size: 0.78rem;\\n}\\n\\n.ai-wbr-bookshelf-reader-overlay {\\n    position: fixed;\\n    inset: 0;\\n    z-index: 2147483647;\\n    box-sizing: border-box;\\n    display: flex;\\n    align-items: center;\\n    justify-content: center;\\n    width: 100vw;\\n    height: 100dvh;\\n    padding: 0;\\n    overflow: hidden;\\n    background: rgba(0, 0, 0, 0.34);\\n    backdrop-filter: blur(10px);\\n}\\n\\n.ai-wbr-bookshelf-reader-shell {\\n    box-sizing: border-box;\\n    display: grid;\\n    grid-template-rows: auto auto auto minmax(0, 1fr);\\n    gap: 0.8rem;\\n    width: min(54rem, 100vw);\\n    height: min(100dvh, 58rem);\\n    max-width: 100vw;\\n    max-height: 100dvh;\\n    min-height: 0;\\n    overflow: hidden;\\n    border: 0;\\n    border-radius: 0;\\n    padding: clamp(0.85rem, 2vw, 1.35rem);\\n    background:\\n        linear-gradient(180deg, rgba(255, 255, 255, 0.92), rgba(247, 243, 235, 0.96)),\\n        #f7f1e7;\\n    box-shadow: 0 24px 70px rgba(0, 0, 0, 0.32);\\n    color: #2b2520;\\n}\\n\\n.ai-wbr-bookshelf-reader-top {\\n    display: flex;\\n    align-items: center;\\n    justify-content: space-between;\\n    gap: 0.7rem;\\n    min-width: 0;\\n}\\n\\n.ai-wbr-bookshelf-reader-titlebar {\\n    flex: 1 1 auto;\\n    min-width: 0;\\n    text-align: center;\\n}\\n\\n.ai-wbr-bookshelf-reader-back,\\n.ai-wbr-bookshelf-reader-more {\\n    width: 2.35rem;\\n    height: 2.35rem;\\n    flex: 0 0 2.35rem;\\n    border: 0;\\n    border-radius: 999px;\\n    background: transparent;\\n    color: #2b2520;\\n    cursor: pointer;\\n    font-size: 1.45rem;\\n    line-height: 1;\\n}\\n\\n.ai-wbr-bookshelf-reader-menu-wrap {\\n    position: relative;\\n    display: flex;\\n    flex: 0 0 2.35rem;\\n    justify-content: flex-end;\\n}\\n\\n.ai-wbr-bookshelf-reader-menu {\\n    position: absolute;\\n    top: calc(100% + 0.45rem);\\n    right: 0;\\n    z-index: 2;\\n    display: none;\\n    min-width: 8.2rem;\\n    overflow: hidden;\\n    border: 1px solid rgba(43, 37, 32, 0.12);\\n    border-radius: 12px;\\n    background: rgba(255, 251, 244, 0.96);\\n    box-shadow: 0 16px 34px rgba(43, 37, 32, 0.18);\\n}\\n\\n.ai-wbr-bookshelf-reader-menu-wrap.open .ai-wbr-bookshelf-reader-menu {\\n    display: block;\\n}\\n\\n.ai-wbr-bookshelf-reader-menu-action {\\n    display: block;\\n    width: 100%;\\n    border: 0;\\n    padding: 0.72rem 0.9rem;\\n    background: transparent;\\n    color: #2b2520;\\n    cursor: pointer;\\n    font-size: 0.92rem;\\n    text-align: left;\\n}\\n\\n.ai-wbr-bookshelf-reader-menu-action:hover {\\n    background: rgba(43, 37, 32, 0.07);\\n}\\n\\n.ai-wbr-bookshelf-reader-back {\\n    font-size: 2.15rem;\\n    font-weight: 300;\\n}\\n\\n.ai-wbr-bookshelf-reader-back:hover,\\n.ai-wbr-bookshelf-reader-more:hover {\\n    background: rgba(43, 37, 32, 0.08);\\n}\\n\\n.ai-wbr-bookshelf-reader-top b,\\n.ai-wbr-bookshelf-reader-top small {\\n    display: block;\\n    overflow: hidden;\\n    text-overflow: ellipsis;\\n    white-space: nowrap;\\n}\\n\\n.ai-wbr-bookshelf-reader-top b {\\n    font-size: 1rem;\\n    font-weight: 700;\\n}\\n\\n.ai-wbr-bookshelf-reader-top small {\\n    color: rgba(43, 37, 32, 0.54);\\n    font-size: 0.78rem;\\n}\\n\\n.ai-wbr-bookshelf-reader-hero {\\n    display: grid;\\n    grid-template-columns: minmax(6.8rem, 9.5rem) minmax(0, 1fr);\\n    gap: 0.95rem;\\n    align-items: stretch;\\n    min-height: 0;\\n    border-radius: 14px;\\n    padding: 0.75rem 0;\\n    background: transparent;\\n}\\n\\n.ai-wbr-bookshelf-reader-cover {\\n    position: relative;\\n    display: flex;\\n    min-height: clamp(8.4rem, 18vh, 10.6rem);\\n    flex-direction: column;\\n    justify-content: flex-end;\\n    overflow: hidden;\\n    border-radius: 8px;\\n    padding: 0.9rem;\\n    background:\\n        radial-gradient(circle at 20% 18%, rgba(255, 255, 255, 0.2), transparent 30%),\\n        linear-gradient(145deg, rgba(255, 255, 255, 0.16), rgba(0, 0, 0, 0.3)),\\n        linear-gradient(135deg, #ff7a30, #df2f45 48%, #22111f);\\n    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.24);\\n    color: #fff;\\n}\\n\\n.ai-wbr-bookshelf-reader-cover span {\\n    position: absolute;\\n    top: 0.7rem;\\n    left: 0.7rem;\\n    border-radius: 999px;\\n    padding: 0.14rem 0.5rem;\\n    background: rgba(255, 255, 255, 0.2);\\n    font-size: 0.72rem;\\n    font-weight: 800;\\n}\\n\\n.ai-wbr-bookshelf-reader-cover strong {\\n    display: -webkit-box;\\n    overflow: hidden;\\n    font-size: 1.18rem;\\n    line-height: 1.2;\\n    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.42);\\n    -webkit-box-orient: vertical;\\n    -webkit-line-clamp: 4;\\n}\\n\\n.ai-wbr-bookshelf-reader-cover small {\\n    margin-top: 0.45rem;\\n    color: rgba(255, 255, 255, 0.82);\\n    font-size: 0.8rem;\\n}\\n\\n.ai-wbr-bookshelf-reader-intro {\\n    display: flex;\\n    min-width: 0;\\n    flex-direction: column;\\n    justify-content: center;\\n    gap: 0.55rem;\\n}\\n\\n.ai-wbr-bookshelf-reader-intro h3 {\\n    margin: 0;\\n    color: #1f1a16;\\n    font-size: 1.28rem;\\n    line-height: 1.22;\\n}\\n\\n.ai-wbr-bookshelf-reader-intro p {\\n    margin: 0;\\n    color: rgba(43, 37, 32, 0.64);\\n    font-size: 0.92rem;\\n    line-height: 1.55;\\n}\\n\\n.ai-wbr-bookshelf-reader-current {\\n    overflow: hidden;\\n    border-radius: 999px;\\n    padding: 0.45rem 0.8rem;\\n    background: rgba(43, 37, 32, 0.06);\\n    color: rgba(43, 37, 32, 0.68);\\n    font-size: 0.84rem;\\n    text-overflow: ellipsis;\\n    white-space: nowrap;\\n}\\n\\n.ai-wbr-bookshelf-reader-body {\\n    display: grid;\\n    grid-template-columns: minmax(10rem, 0.28fr) minmax(0, 1fr);\\n    gap: 0.9rem;\\n    min-height: 0;\\n    overflow: hidden;\\n}\\n\\n.ai-wbr-bookshelf-reader-toc,\\n.ai-wbr-bookshelf-reader-pages {\\n    min-height: 0;\\n    overflow: auto;\\n    overscroll-behavior: contain;\\n}\\n\\n.ai-wbr-bookshelf-reader-toc {\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.45rem;\\n    border: 1px solid rgba(43, 37, 32, 0.08);\\n    border-radius: 12px;\\n    padding: 0.65rem;\\n    background: rgba(255, 255, 255, 0.48);\\n    overflow-x: hidden;\\n}\\n\\n.ai-wbr-bookshelf-reader-toc > b {\\n    color: rgba(43, 37, 32, 0.84);\\n    font-size: 0.95rem;\\n}\\n\\n.ai-wbr-bookshelf-reader-toc-item {\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.14rem;\\n    border: 1px solid transparent;\\n    border-radius: 8px;\\n    padding: 0.5rem 0.55rem;\\n    background: transparent;\\n    color: rgba(43, 37, 32, 0.82);\\n    text-align: left;\\n    cursor: pointer;\\n}\\n\\n.ai-wbr-bookshelf-reader-toc-item.active,\\n.ai-wbr-bookshelf-reader-toc-item:hover {\\n    border-color: rgba(43, 37, 32, 0.12);\\n    background: rgba(43, 37, 32, 0.07);\\n}\\n\\n.ai-wbr-bookshelf-reader-toc-item small {\\n    color: rgba(43, 37, 32, 0.48);\\n    font-size: 0.76rem;\\n}\\n\\n.ai-wbr-bookshelf-reader-pages {\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.9rem;\\n    padding: 0 0.25rem 0.75rem 0;\\n    overflow-x: hidden;\\n    overflow-y: auto;\\n}\\n\\n.ai-wbr-bookshelf-reader-page {\\n    border: 1px solid rgba(43, 37, 32, 0.08);\\n    border-radius: 12px;\\n    padding: clamp(1rem, 2.4vw, 1.55rem);\\n    background:\\n        linear-gradient(180deg, rgba(255, 255, 255, 0.68), rgba(255, 251, 244, 0.88)),\\n        #fff9ef;\\n}\\n\\n.ai-wbr-bookshelf-reader-page-head {\\n    display: flex;\\n    align-items: center;\\n    justify-content: space-between;\\n    gap: 0.75rem;\\n}\\n\\n.ai-wbr-bookshelf-reader-page-head b {\\n    color: #1f1a16;\\n    font-size: 1.08rem;\\n}\\n\\n.ai-wbr-bookshelf-reader-page-head span {\\n    border-radius: 999px;\\n    padding: 0.14rem 0.46rem;\\n    background: rgba(43, 37, 32, 0.07);\\n    color: rgba(43, 37, 32, 0.58);\\n    font-size: 0.76rem;\\n    white-space: nowrap;\\n}\\n\\n.ai-wbr-bookshelf-reader-page p {\\n    margin: 0.75rem 0 0;\\n    color: #342c25;\\n    font-size: 1.04rem;\\n    line-height: 1.9;\\n    white-space: pre-wrap;\\n    overflow-wrap: anywhere;\\n}\\n\\n.ai-wbr-bookshelf-novel-reader {\\n    display: grid;\\n    grid-template-columns: minmax(9rem, 0.36fr) minmax(0, 1fr);\\n    gap: 0.75rem;\\n    margin-top: 0.75rem;\\n    min-height: 22rem;\\n}\\n\\n.ai-wbr-bookshelf-toc {\\n    position: sticky;\\n    top: 0.5rem;\\n    align-self: start;\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.4rem;\\n    max-height: 24rem;\\n    overflow: auto;\\n    border: 1px solid rgba(125, 212, 255, 0.16);\\n    border-radius: 10px;\\n    padding: 0.65rem;\\n    background: color-mix(in srgb, var(--SmartThemeBlurTintColor, #111827) 84%, rgba(255, 106, 42, 0.16) 16%);\\n}\\n\\n.ai-wbr-bookshelf-toc > b {\\n    font-size: 0.92rem;\\n    letter-spacing: 0;\\n}\\n\\n.ai-wbr-bookshelf-toc-item {\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.15rem;\\n    border: 1px solid transparent;\\n    border-radius: 8px;\\n    padding: 0.45rem 0.5rem;\\n    background: transparent;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 82%, transparent 18%);\\n    text-align: left;\\n    cursor: pointer;\\n}\\n\\n.ai-wbr-bookshelf-toc-item.active,\\n.ai-wbr-bookshelf-toc-item:hover {\\n    border-color: rgba(255, 106, 42, 0.38);\\n    background: rgba(255, 106, 42, 0.12);\\n}\\n\\n.ai-wbr-bookshelf-toc-item small {\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 54%, transparent 46%);\\n    font-size: 0.76rem;\\n}\\n\\n.ai-wbr-bookshelf-settings-drawer {\\n    position: absolute;\\n    inset: 0 0 0 auto;\\n    z-index: 4;\\n    width: min(30rem, 92%);\\n    max-height: 100%;\\n    overflow: auto;\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.75rem;\\n    box-shadow: -14px 0 32px rgba(0, 0, 0, 0.38);\\n    transform: translateX(106%);\\n    transition: transform 180ms ease;\\n    background: color-mix(in srgb, var(--SmartThemeBlurTintColor, #111827) 94%, #020617 6%);\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 92%, #d7f5ff 8%);\\n}\\n\\n.ai-wbr-bookshelf-settings-drawer.open {\\n    transform: translateX(0);\\n}\\n\\n.ai-wbr-bookshelf-drawer-backdrop {\\n    position: absolute;\\n    inset: 0;\\n    z-index: 3;\\n    display: none;\\n    background: rgba(0, 0, 0, 0.38);\\n}\\n\\n.ai-wbr-bookshelf-drawer-backdrop.open {\\n    display: block;\\n}\\n\\n.ai-wbr-bookshelf-book-action-backdrop {\\n    position: absolute;\\n    inset: 0;\\n    z-index: 5;\\n    display: none;\\n    background: rgba(0, 0, 0, 0.34);\\n}\\n\\n.ai-wbr-bookshelf-book-action-backdrop.open {\\n    display: block;\\n}\\n\\n.ai-wbr-bookshelf-book-action-sheet {\\n    position: absolute;\\n    right: 0;\\n    bottom: 0;\\n    left: 0;\\n    z-index: 6;\\n    max-height: min(82%, 30rem);\\n    overflow: auto;\\n    border: 1px solid color-mix(in srgb, var(--SmartThemeBorderColor, #4b5563) 70%, transparent 30%);\\n    border-radius: 18px 18px 0 0;\\n    padding: 0.85rem;\\n    background: color-mix(in srgb, var(--SmartThemeBlurTintColor, #111827) 96%, #020617 4%);\\n    box-shadow: 0 -18px 42px rgba(0, 0, 0, 0.42);\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 92%, #d7f5ff 8%);\\n    transform: translateY(110%);\\n    transition: transform 180ms ease;\\n}\\n\\n.ai-wbr-bookshelf-book-action-sheet.open {\\n    transform: translateY(0);\\n}\\n\\n.ai-wbr-bookshelf-book-action-head {\\n    display: flex;\\n    align-items: center;\\n    justify-content: space-between;\\n    gap: 0.75rem;\\n    margin-bottom: 0.75rem;\\n}\\n\\n.ai-wbr-bookshelf-book-action-head b,\\n.ai-wbr-bookshelf-book-action-head small {\\n    display: block;\\n}\\n\\n.ai-wbr-bookshelf-book-action-head small,\\n.ai-wbr-bookshelf-book-action-summary {\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 62%, transparent 38%);\\n    font-size: 0.84rem;\\n}\\n\\n.ai-wbr-bookshelf-book-action-body {\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.65rem;\\n}\\n\\n.ai-wbr-bookshelf-book-action-list {\\n    display: grid;\\n    grid-template-columns: repeat(2, minmax(0, 1fr));\\n    gap: 0.5rem;\\n    margin-top: 0.65rem;\\n}\\n\\n.ai-wbr-bookshelf-book-action-list .menu_button {\\n    width: 100%;\\n}\\n\\n.ai-wbr-bookshelf-book-action-list .menu_button.danger {\\n    border-color: rgba(248, 113, 113, 0.42);\\n    color: #fecaca;\\n}\\n\\n.ai-wbr-bookshelf-config {\\n    grid-template-columns: minmax(7.5rem, 0.7fr) minmax(12rem, 1.3fr);\\n}\\n\\n.ai-wbr-bookshelf-config .ai-wbr-bookshelf-fetch-models {\\n    justify-self: start;\\n    width: 100%;\\n    max-width: 12rem;\\n    min-width: 7.5rem;\\n    min-height: 2.35rem;\\n    padding: 0.45rem 0.8rem;\\n    line-height: 1.25;\\n    white-space: nowrap;\\n    writing-mode: horizontal-tb;\\n    text-orientation: mixed;\\n}\\n\\n.ai-wbr-bookshelf-model-hint {\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 72%, transparent 28%);\\n    font-size: 0.82rem;\\n    line-height: 1.35;\\n}\\n\\n.ai-wbr-bookshelf-import-options {\\n    display: grid;\\n    grid-template-columns: minmax(7.5rem, 0.7fr) minmax(12rem, 1.3fr);\\n    gap: 0.5rem 0.75rem;\\n    align-items: center;\\n}\\n\\n.ai-wbr-bookshelf-chunk-list {\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.5rem;\\n    margin-top: 0.75rem;\\n    max-height: 24rem;\\n    overflow: auto;\\n}\\n\\n.ai-wbr-bookshelf-novel-pages {\\n    margin-top: 0;\\n    max-height: 32rem;\\n    padding-right: 0.25rem;\\n}\\n\\n.ai-wbr-bookshelf-page {\\n    border-color: rgba(255, 106, 42, 0.16);\\n    border-radius: 14px;\\n    padding: 1rem 1.05rem;\\n    background:\\n        linear-gradient(180deg, rgba(255, 248, 235, 0.075), rgba(255, 255, 255, 0.025)),\\n        rgba(255, 255, 255, 0.035);\\n}\\n\\n.ai-wbr-bookshelf-page p {\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 86%, transparent 14%);\\n    font-size: 0.98rem;\\n    line-height: 1.72;\\n}\\n\\n.ai-wbr-bookshelf-chunk.disabled {\\n    opacity: 0.58;\\n}\\n\\n.ai-wbr-bookshelf-chunk p,\\n.ai-wbr-bookshelf-result p {\\n    margin: 0.35rem 0 0;\\n    white-space: pre-wrap;\\n    line-height: 1.45;\\n}\\n\\n.ai-wbr-bookshelf-test textarea {\\n    width: 100%;\\n    resize: vertical;\\n}\\n\\n.ai-wbr-bookshelf-test {\\n    display: none;\\n    background: rgba(255, 255, 255, 0.035);\\n}\\n\\n.ai-wbr-bookshelf-bind-note {\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 68%, transparent 32%);\\n    font-size: 0.86rem;\\n    line-height: 1.35;\\n}\\n\\n.ai-wbr-bookshelf-test.open {\\n    display: block;\\n}\\n\\n@media (max-width: 760px) {\\n    .ai-wbr-bookshelf-layout {\\n        grid-template-columns: 1fr;\\n    }\\n\\n    .ai-wbr-bookshelf-toolbar {\\n        align-items: stretch;\\n        flex-direction: column;\\n    }\\n\\n    .ai-wbr-bookshelf-toolbar-actions {\\n        justify-content: stretch;\\n    }\\n\\n    .ai-wbr-bookshelf-toolbar-actions .menu_button,\\n    .ai-wbr-bookshelf-quickbar .text_pole {\\n        width: 100%;\\n    }\\n\\n    .ai-wbr-bookshelf-books {\\n        grid-template-columns: repeat(3, minmax(0, 1fr));\\n        gap: 1.15rem 0.85rem;\\n    }\\n\\n    .ai-wbr-bookshelf-settings-drawer {\\n        width: 100%;\\n    }\\n\\n    .ai-wbr-bookshelf-switches,\\n    .ai-wbr-bookshelf-import,\\n    .ai-wbr-bookshelf-actions {\\n        align-items: stretch;\\n    }\\n\\n    .ai-wbr-bookshelf-import .text_pole,\\n    .ai-wbr-bookshelf-import .menu_button,\\n    .ai-wbr-bookshelf-actions .menu_button {\\n        width: 100%;\\n        flex-basis: 100%;\\n    }\\n\\n    .ai-wbr-bookshelf-book-head,\\n    .ai-wbr-bookshelf-detail-head,\\n    .ai-wbr-bookshelf-result-head,\\n    .ai-wbr-bookshelf-chunk-head {\\n        align-items: flex-start;\\n        flex-direction: column;\\n    }\\n\\n    .ai-wbr-bookshelf-novel-hero,\\n    .ai-wbr-bookshelf-novel-reader,\\n    .ai-wbr-bookshelf-reader-hero,\\n    .ai-wbr-bookshelf-reader-body {\\n        grid-template-columns: 1fr;\\n    }\\n\\n    .ai-wbr-bookshelf-toc {\\n        position: static;\\n        max-height: 14rem;\\n    }\\n\\n    .ai-wbr-bookshelf-reader-overlay {\\n        padding: 0;\\n    }\\n\\n    .ai-wbr-bookshelf-reader-shell {\\n        width: 100vw;\\n        height: 100dvh;\\n        max-width: 100vw;\\n        max-height: 100dvh;\\n        border-radius: 0;\\n        padding: 0.75rem;\\n    }\\n\\n    .ai-wbr-bookshelf-reader-toc {\\n        max-height: 10rem;\\n    }\\n}\\n\\n@media (max-width: 420px) {\\n    .ai-wbr-bookshelf-app {\\n        padding: 0.85rem;\\n    }\\n\\n    .ai-wbr-bookshelf-title {\\n        font-size: 1.8rem;\\n    }\\n\\n    .ai-wbr-bookshelf-book-title {\\n        font-size: 0.95rem;\\n    }\\n\\n    .ai-wbr-bookshelf-book-meta {\\n        font-size: 0.8rem;\\n    }\\n}\\n\\n/* ===== Memory & Graph panel restructure (v0.6.18) ===== */\\n\\n/* ----- Memory section: status band + activity area ----- */\\n.ai-wbr-memory-band {\\n    display: flex;\\n    flex-wrap: wrap;\\n    align-items: center;\\n    justify-content: space-between;\\n    gap: 0.6rem 0.8rem;\\n    padding: 0.65rem 0.8rem;\\n    margin-bottom: 0.7rem;\\n    border: 1px solid rgba(125, 212, 255, 0.32);\\n    border-radius: 12px;\\n    background:\\n        linear-gradient(135deg, rgba(85, 133, 201, 0.16), rgba(123, 216, 255, 0.07)),\\n        color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(12, 16, 24, 0.96)) 92%, #08101d 8%);\\n    box-shadow: inset 0 0 22px rgba(125, 212, 255, 0.08);\\n    position: sticky;\\n    top: 0;\\n    z-index: 6;\\n}\\n\\n.ai-wbr-memory-band-metrics {\\n    display: flex;\\n    flex-wrap: wrap;\\n    align-items: center;\\n    gap: 0.3rem 0.7rem;\\n    min-width: 0;\\n}\\n\\n.ai-wbr-memory-band-metrics .ai-wbr-status {\\n    font-weight: 700;\\n    font-size: 0.92rem;\\n    opacity: 1;\\n}\\n\\n.ai-wbr-memory-band-actions {\\n    display: flex;\\n    flex-wrap: wrap;\\n    gap: 0.35rem;\\n}\\n\\n.ai-wbr-memory-band-actions .menu_button {\\n    margin: 0;\\n}\\n\\n.ai-wbr-memory-band-primary {\\n    border-color: rgba(125, 212, 255, 0.6) !important;\\n    background: rgba(125, 212, 255, 0.14) !important;\\n}\\n\\n.ai-wbr-memory-band-muted {\\n    opacity: 0.7;\\n}\\n\\n.ai-wbr-memory-band-danger {\\n    border-color: rgba(255, 120, 120, 0.5) !important;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 80%, #ff9b9b 20%);\\n}\\n\\n.ai-wbr-memory-band-danger:hover {\\n    background: rgba(255, 100, 100, 0.16) !important;\\n}\\n\\n.ai-wbr-memory-activity {\\n    display: flex;\\n    flex-direction: column;\\n    gap: 0.7rem;\\n    margin-bottom: 0.7rem;\\n}\\n\\n.ai-wbr-memory-activity .ai-wbr-memory-review-fold {\\n    margin-top: 0;\\n}\\n\\n/* make the activity review queue prominent when there are pending items */\\n.ai-wbr-memory-activity .ai-wbr-memory-fold-attention {\\n    box-shadow: 0 0 0 1px rgba(255, 196, 87, 0.45), 0 8px 20px rgba(255, 160, 60, 0.12);\\n}\\n\\n/* settings group: visually separate the collapsed settings cluster */\\n.ai-wbr-memory-settings-fold {\\n    margin-top: 0.2rem;\\n}\\n\\n.ai-wbr-memory-settings-fold > summary span {\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 78%, #8fd7ff 22%);\\n}\\n\\n/* ----- Graph section: topbar slim + floating fit button + emphasis ----- */\\n.ai-wbr-graph-topbar-actions .menu_button {\\n    margin: 0;\\n}\\n\\n.ai-wbr-graph-topbar-primary {\\n    border-color: rgba(125, 212, 255, 0.6) !important;\\n    background: rgba(125, 212, 255, 0.14) !important;\\n}\\n\\n.ai-wbr-graph-fab-fit {\\n    position: absolute;\\n    z-index: 7;\\n    right: 0.6rem;\\n    bottom: 0.6rem;\\n    width: 2.2rem;\\n    height: 2.2rem;\\n    min-width: 0;\\n    padding: 0;\\n    display: inline-flex;\\n    align-items: center;\\n    justify-content: center;\\n    border-radius: 999px;\\n    border: 1px solid rgba(176, 225, 255, 0.42);\\n    background: color-mix(in srgb, var(--SmartThemeBlurTintColor, rgba(12, 16, 24, 0.9)) 80%, #08101d 20%);\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 88%, #d7f5ff 12%);\\n    box-shadow: 0 6px 16px rgba(0, 0, 0, 0.28);\\n    cursor: pointer;\\n    opacity: 0.78;\\n    transition: opacity 0.18s ease, transform 0.18s ease;\\n}\\n\\n.ai-wbr-graph-fab-fit:hover {\\n    opacity: 1;\\n    transform: scale(1.06);\\n}\\n\\n/* emphasize high-importance / recently-updated nodes so the graph has a focal point */\\n.ai-wbr-memory-node-emphasis .ai-wbr-memory-node-card {\\n    stroke-width: 2.4;\\n    stroke: color-mix(in srgb, var(--memory-node-accent, #66e0ff) 78%, #ffffff 22%);\\n    filter: drop-shadow(0 0 5px color-mix(in srgb, var(--memory-node-accent, #66e0ff) 55%, transparent 45%));\\n}\\n\\n.ai-wbr-memory-node-emphasis .ai-wbr-memory-node-title {\\n    font-weight: 800;\\n}\\n\\n@media (prefers-reduced-motion: no-preference) {\\n    .ai-wbr-memory-node-emphasis .ai-wbr-memory-node-card {\\n        animation: ai-wbr-memory-node-emphasis-pulse 2.6s ease-in-out infinite;\\n    }\\n\\n    @keyframes ai-wbr-memory-node-emphasis-pulse {\\n        0%, 100% { filter: drop-shadow(0 0 4px color-mix(in srgb, var(--memory-node-accent, #66e0ff) 45%, transparent 55%)); }\\n        50% { filter: drop-shadow(0 0 8px color-mix(in srgb, var(--memory-node-accent, #66e0ff) 70%, transparent 30%)); }\\n    }\\n}\\n\\n/* fullscreen floating fit button placement stays reachable */\\n.ai-wbr-graph-workspace.ai-wbr-memory-graph-fullscreen .ai-wbr-graph-fab-fit {\\n    right: 1rem;\\n    bottom: 1rem;\\n}\\n\\n/* narrow screens: band stacks vertically */\\n@media (max-width: 640px) {\\n    .ai-wbr-memory-band {\\n        flex-direction: column;\\n        align-items: stretch;\\n    }\\n    .ai-wbr-memory-band-actions {\\n        justify-content: center;\\n    }\\n}\\n\\n/* ===== Graph: compact single-row toolbar + filter drawer ===== */\\n.ai-wbr-memory-graph-toolbar-compact {\\n    position: relative;\\n    padding: 0.4rem 0.6rem;\\n    gap: 0.35rem;\\n}\\n\\n.ai-wbr-memory-graph-row-primary {\\n    display: flex;\\n    flex-wrap: wrap;\\n    align-items: center;\\n    gap: 0.4rem;\\n}\\n\\n.ai-wbr-memory-graph-modes {\\n    display: inline-flex;\\n    gap: 0.25rem;\\n}\\n\\n.ai-wbr-memory-graph-toolbar-compact .ai-wbr-memory-graph-summary {\\n    margin: 0;\\n    white-space: nowrap;\\n    opacity: 0.8;\\n    flex: 0 1 auto;\\n    min-width: 0;\\n    overflow: hidden;\\n    text-overflow: ellipsis;\\n}\\n\\n.ai-wbr-memory-graph-filter-toggle,\\n.ai-wbr-memory-rule-toggle {\\n    flex: 0 0 auto;\\n}\\n\\n.ai-wbr-memory-graph-toolbar-compact .ai-wbr-memory-graph-drawer {\\n    margin: 0;\\n}\\n\\n.ai-wbr-memory-graph-drawer-body {\\n    display: flex;\\n    flex-wrap: wrap;\\n    align-items: center;\\n    gap: 0.5rem;\\n    padding: 0.45rem 0 0.1rem;\\n    border-top: 1px solid rgba(176, 225, 255, 0.12);\\n    margin-top: 0.3rem;\\n}\\n\\n.ai-wbr-memory-graph-drawer-body .ai-wbr-memory-graph-typebar {\\n    margin: 0;\\n    flex: 1 1 100%;\\n}\\n\\n.ai-wbr-memory-graph-drawer-body .ai-wbr-memory-weight-filter {\\n    margin: 0;\\n}\\n\\n@media (max-width: 640px) {\\n    .ai-wbr-memory-graph-row-primary {\\n        gap: 0.3rem;\\n    }\\n    .ai-wbr-memory-graph-toolbar-compact .ai-wbr-memory-graph-search {\\n        flex: 1 1 8.2rem;\\n        width: min(13rem, 42vw);\\n    }\\n}\\n\\n/* touch: disable double-tap text selection interference on the svg canvas */\\n.ai-wbr-memory-graph svg {\\n    touch-action: none;\\n    -webkit-tap-highlight-color: transparent;\\n}\\n\\n/* ===== Graph: topbar removed, actions grouped in toolbar ===== */\\n.ai-wbr-memory-graph-divider {\\n    flex: 0 0 auto;\\n    width: 1px;\\n    height: 1.4rem;\\n    background: rgba(176, 225, 255, 0.22);\\n    margin: 0 0.15rem;\\n}\\n\\n.ai-wbr-memory-graph-actions {\\n    display: inline-flex;\\n    align-items: center;\\n    gap: 0.3rem;\\n    margin-left: auto;\\n    flex-wrap: nowrap;\\n    min-width: 0;\\n}\\n\\n.ai-wbr-memory-graph-actions .ai-wbr-memory-graph-search {\\n    flex: 0 0 4.9rem;\\n    width: 4.9rem;\\n    min-width: 4.9rem;\\n    max-width: 4.9rem;\\n    min-height: 0;\\n    height: 1.95rem;\\n    margin: 0;\\n    padding: 0.18rem 0.55rem;\\n    font-size: 0.82rem;\\n}\\n\\n.ai-wbr-memory-graph-toolbar-compact .ai-wbr-memory-graph-actions .ai-wbr-memory-graph-search {\\n    width: 4.9rem;\\n    min-width: 4.9rem;\\n    max-width: 4.9rem;\\n}\\n\\n.ai-wbr-memory-graph-actions .ai-wbr-memory-graph-search::placeholder {\\n    opacity: 0.78;\\n}\\n\\n.ai-wbr-memory-graph-close-fullscreen {\\n    position: fixed;\\n    top: max(0.7rem, env(safe-area-inset-top));\\n    right: max(0.75rem, env(safe-area-inset-right));\\n    z-index: 2147483647;\\n    width: 2.15rem;\\n    height: 2.15rem;\\n    min-width: 0;\\n    padding: 0;\\n    border-radius: 999px;\\n    border: 1px solid rgba(176, 225, 255, 0.32);\\n    background: rgba(8, 12, 22, 0.76);\\n    color: rgba(235, 248, 255, 0.94);\\n    font-size: 1.25rem;\\n    line-height: 1;\\n    cursor: pointer;\\n    box-shadow: 0 0.45rem 1.1rem rgba(0, 0, 0, 0.34);\\n    backdrop-filter: blur(10px);\\n}\\n\\n.ai-wbr-memory-graph-close-fullscreen:hover,\\n.ai-wbr-memory-graph-close-fullscreen:active {\\n    background: rgba(255, 100, 100, 0.22);\\n    border-color: rgba(255, 150, 150, 0.45);\\n}\\n\\n.ai-wbr-memory-graph-actions .menu_button i,\\n.ai-wbr-memory-graph-filter-toggle i,\\n.ai-wbr-memory-rule-toggle i {\\n    opacity: 0.78;\\n}\\n\\n.ai-wbr-memory-rule-toggle.active {\\n    border-color: rgba(125, 212, 255, 0.62);\\n    background: rgba(125, 212, 255, 0.12);\\n}\\n\\n/* push actions group to the right when summary wraps */\\n.ai-wbr-memory-graph-toolbar-compact .ai-wbr-memory-graph-row-primary {\\n    row-gap: 0.35rem;\\n}\\n\\n@media (max-width: 640px) {\\n    .ai-wbr-memory-graph-actions {\\n        margin-left: 0;\\n        flex-wrap: wrap;\\n    }\\n    .ai-wbr-memory-graph-divider {\\n        display: none;\\n    }\\n    .ai-wbr-memory-rule-drawer {\\n        width: min(24rem, 94vw);\\n    }\\n    .ai-wbr-memory-rule-grid {\\n        grid-template-columns: 1fr;\\n    }\\n    .ai-wbr-memory-rule-grid label {\\n        padding-top: 0;\\n    }\\n}\\n\\n/* card polish: softer corners + emphasis glow */\\n.ai-wbr-memory-node-card {\\n    rx: 16;\\n    ry: 16;\\n}\\n\\n.ai-wbr-memory-node-emphasis .ai-wbr-memory-node-card {\\n    stroke-width: 2.2;\\n    filter: drop-shadow(0 0 6px color-mix(in srgb, var(--memory-node-accent) 55%, transparent))\\n            drop-shadow(0 3px 7px rgba(0, 0, 0, 0.42));\\n}\\n\\n/* ===== Graph: preview panel close button + head layout ===== */\\n.ai-wbr-memory-preview-head-right {\\n    display: flex;\\n    align-items: center;\\n    gap: 0.5rem;\\n    flex: 0 0 auto;\\n}\\n\\n.ai-wbr-memory-preview-close {\\n    flex: 0 0 auto;\\n    width: 1.8rem;\\n    height: 1.8rem;\\n    min-width: 0;\\n    padding: 0;\\n    margin: 0;\\n    display: inline-flex;\\n    align-items: center;\\n    justify-content: center;\\n    border-radius: 999px;\\n    border: 1px solid rgba(176, 225, 255, 0.28);\\n    background: rgba(255, 255, 255, 0.05);\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 85%, #d7f5ff 15%);\\n    cursor: pointer;\\n    opacity: 0.72;\\n    transition: opacity 0.16s ease, transform 0.16s ease, background 0.16s ease;\\n}\\n\\n.ai-wbr-memory-preview-close:hover {\\n    opacity: 1;\\n    transform: scale(1.08);\\n    background: rgba(255, 100, 100, 0.18);\\n    border-color: rgba(255, 120, 120, 0.5);\\n}\\n\\n/* ===== Debug panel: merged memory + router groups ===== */\\n.ai-wbr-memory-debug-divider {\\n    height: 1px;\\n    background: rgba(176, 225, 255, 0.18);\\n    margin: 0.9rem 0 0.5rem;\\n}\\n\\n.ai-wbr-memory-debug-group {\\n    font-size: 0.78rem;\\n    font-weight: 800;\\n    opacity: 0.78;\\n    letter-spacing: 0.04em;\\n    text-transform: uppercase;\\n    margin-bottom: 0.45rem;\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 80%, #9ddcff 20%);\\n}\\n\\n.ai-wbr-api-stats-panel {\\n    display: grid;\\n    gap: 0.65rem;\\n    padding: 0.75rem;\\n    border: 1px solid rgba(125, 212, 255, 0.22);\\n    border-radius: 10px;\\n    background: rgba(12, 18, 28, 0.28);\\n}\\n\\n.ai-wbr-runtime-activity-panel {\\n    display: grid;\\n    gap: 0.7rem;\\n    padding: 0.75rem;\\n    border: 1px solid rgba(125, 212, 255, 0.24);\\n    border-radius: 10px;\\n    background:\\n        linear-gradient(135deg, rgba(125, 212, 255, 0.07), rgba(91, 214, 143, 0.035)),\\n        rgba(12, 18, 28, 0.28);\\n}\\n\\n.ai-wbr-runtime-activity-panel.running {\\n    border-color: rgba(125, 212, 255, 0.36);\\n}\\n\\n.ai-wbr-runtime-activity-panel.success {\\n    border-color: rgba(91, 214, 143, 0.34);\\n}\\n\\n.ai-wbr-runtime-activity-panel.failed {\\n    border-color: rgba(255, 100, 100, 0.34);\\n    background: rgba(44, 12, 18, 0.22);\\n}\\n\\n.ai-wbr-runtime-activity-head {\\n    display: grid;\\n    grid-template-columns: minmax(0, 1fr) auto;\\n    gap: 0.55rem;\\n    align-items: start;\\n}\\n\\n.ai-wbr-runtime-activity-head > div {\\n    display: grid;\\n    gap: 0.16rem;\\n    min-width: 0;\\n}\\n\\n.ai-wbr-runtime-activity-head b {\\n    font-size: 0.92rem;\\n}\\n\\n.ai-wbr-runtime-activity-head small {\\n    opacity: 0.74;\\n    font-size: 0.76rem;\\n    line-height: 1.35;\\n}\\n\\n.ai-wbr-runtime-activity-head > span {\\n    padding: 0.12rem 0.42rem;\\n    border-radius: 999px;\\n    background: rgba(255, 255, 255, 0.07);\\n    color: color-mix(in srgb, var(--SmartThemeBodyColor, #fff) 78%, #b9eaff 22%);\\n    font-size: 0.72rem;\\n    white-space: nowrap;\\n}\\n\\n.ai-wbr-runtime-activity-steps {\\n    display: grid;\\n    grid-template-columns: repeat(auto-fit, minmax(5.5rem, 1fr));\\n    gap: 0.45rem;\\n}\\n\\n.ai-wbr-runtime-step {\\n    display: grid;\\n    gap: 0.14rem;\\n    min-width: 0;\\n    padding: 0.5rem 0.55rem;\\n    border: 1px solid rgba(125, 212, 255, 0.14);\\n    border-radius: 8px;\\n    background: rgba(255, 255, 255, 0.035);\\n}\\n\\n.ai-wbr-runtime-step span {\\n    font-weight: 800;\\n    font-size: 0.78rem;\\n}\\n\\n.ai-wbr-runtime-step small {\\n    opacity: 0.68;\\n    font-size: 0.68rem;\\n    line-height: 1.25;\\n    overflow: hidden;\\n    text-overflow: ellipsis;\\n    white-space: nowrap;\\n}\\n\\n.ai-wbr-runtime-step.running,\\n.ai-wbr-runtime-step.current {\\n    border-color: rgba(125, 212, 255, 0.38);\\n    background: rgba(125, 212, 255, 0.08);\\n}\\n\\n.ai-wbr-runtime-step.success {\\n    border-color: rgba(91, 214, 143, 0.28);\\n    background: rgba(91, 214, 143, 0.07);\\n}\\n\\n.ai-wbr-runtime-step.failed {\\n    border-color: rgba(255, 100, 100, 0.34);\\n    background: rgba(255, 100, 100, 0.08);\\n}\\n\\n.ai-wbr-api-stats-head {\\n    display: flex;\\n    align-items: center;\\n    justify-content: space-between;\\n    gap: 0.75rem;\\n}\\n\\n.ai-wbr-api-stats-head > div {\\n    display: grid;\\n    gap: 0.18rem;\\n}\\n\\n.ai-wbr-api-stats-head small,\\n.ai-wbr-api-stats-item small {\\n    opacity: 0.72;\\n    font-size: 0.76rem;\\n}\\n\\n.ai-wbr-api-stats-grid {\\n    grid-template-columns: repeat(auto-fit, minmax(72px, 1fr));\\n}\\n\\n.ai-wbr-api-stats-list {\\n    display: grid;\\n    gap: 0.45rem;\\n}\\n\\n.ai-wbr-api-stats-item {\\n    display: grid;\\n    grid-template-columns: minmax(0, 1fr) auto;\\n    gap: 0.35rem 0.75rem;\\n    align-items: center;\\n    padding: 0.55rem 0.65rem;\\n    border: 1px solid rgba(125, 212, 255, 0.16);\\n    border-radius: 8px;\\n    background: rgba(255, 255, 255, 0.035);\\n}\\n\\n.ai-wbr-api-stats-item.failed {\\n    border-color: rgba(255, 112, 112, 0.32);\\n    background: rgba(255, 80, 80, 0.08);\\n}\\n\\n.ai-wbr-api-stats-item.running {\\n    border-color: rgba(255, 210, 120, 0.34);\\n    background: rgba(255, 196, 87, 0.08);\\n}\\n\\n.ai-wbr-api-stats-error {\\n    grid-column: 1 / -1;\\n    color: #ff9a9a;\\n}\\n\\n.ai-wbr-console-parking {\\n    display: none !important;\\n}\\n\\n.ai-wbr-park-anchor {\\n    display: none;\\n}\\n\\n/* ===== Fold summary icons ===== */\\n.ai-wbr-memory-fold summary span i {\\n    margin-right: 0.4rem;\\n    opacity: 0.72;\\n    font-size: 0.82rem;\\n    color: color-mix(in srgb, var(--SmartThemeQuoteColor, #7dd4ff) 88%, white 12%);\\n}\\n\";\nimport { extension_settings, getContext, renderExtensionTemplateAsync } from '../../../extensions.js';\r\nimport {\r\n    eventSource,\r\n    event_types,\r\n    extension_prompt_roles,\r\n    extension_prompt_types,\r\n    saveSettingsDebounced,\r\n    setExtensionPrompt,\r\n} from '../../../../script.js';\r\nimport {\r\n    METADATA_KEY,\r\n    convertCharacterBook,\r\n    loadWorldInfo,\r\n    selected_world_info,\r\n    world_info,\r\n} from '../../../world-info.js';\r\nimport { getCharaFilename } from '../../../utils.js';\r\nimport {\r\n    clampNumber,\r\n    countTermHits,\r\n    matchesBlockRule,\r\n    normalizeText,\r\n    normalizeUrl,\r\n    parseBlockRules,\r\n    splitIntoSentences,\r\n    truncateText,\r\n    uniqueStrings,\r\n} from './core/text-utils.js';\r\n\r\nconst MODULE_NAME = 'ai_worldbook_router';\r\nconst PROMPT_KEY = 'ai_worldbook_router_prompt';\r\nconst LOG_PREFIX = '[AI Worldbook Router]';\r\nconst MAX_MVU_CHARS = 1600;\r\nconst MAX_RECALL_TERMS = 32;\r\nconst MAX_ROUTER_CONTEXT_PREVIEW = 360;\r\nconst MAX_BURST_ITEMS = 5;\r\nconst MAX_MEMORY_CONTEXT_PREVIEW = 260;\r\nconst MAX_MEMORY_SELECTED = 4;\r\nconst CHAT_MEMORY_FIELD = 'AIWBR_ChatMemory';\r\nconst MEMORY_GRAPH_CANVAS_WIDTH = 840;\r\nconst MEMORY_GRAPH_CANVAS_HEIGHT = 360;\r\nconst MEMORY_GRAPH_NODE_WIDTH = 196;\r\nconst MEMORY_GRAPH_NODE_HEIGHT = 96;\r\nconst MEMORY_GRAPH_SAFE_PADDING = 12;\r\nconst MEMORY_GRAPH_TOP_SAFE_PADDING = 56;\r\nconst MEMORY_GRAPH_LAYOUT_PADDING = 112;\r\nconst MEMORY_GRAPH_TOUCH_TAP_THRESHOLD = 8;\r\nconst MEMORY_GRAPH_LONG_PRESS_MS = 520;\r\nconst MEMORY_NODE_TYPE_OPTIONS = [\n    { value: 'event', label: '\\u4e8b\\u4ef6' },\n    { value: 'character', label: '\\u89d2\\u8272' },\n    { value: 'location', label: '\\u5730\\u70b9' },\r\n    { value: 'faction', label: '\\u52bf\\u529b' },\r\n    { value: 'item', label: '\\u9053\\u5177' },\r\n    { value: 'concept', label: '\\u6982\\u5ff5' },\r\n    { value: 'rule', label: '\\u89c4\\u5219' },\r\n    { value: 'quest', label: '\\u4efb\\u52a1' },\n];\nconst MEMORY_TYPED_CARD_FIELDS = [\n    { field: 'characters', varName: 'memory_characters_json', type: 'character', label: '角色' },\n    { field: 'locations', varName: 'memory_locations_json', type: 'location', label: '地点' },\n    { field: 'items', varName: 'memory_items_json', type: 'item', label: '道具' },\n    { field: 'factions', varName: 'memory_factions_json', type: 'faction', label: '势力' },\n    { field: 'concepts', varName: 'memory_concepts_json', type: 'concept', label: '设定' },\n    { field: 'rules', varName: 'memory_rules_json', type: 'rule', label: '规则' },\n    { field: 'quests', varName: 'memory_quests_json', type: 'quest', label: '任务' },\n    { field: 'events', varName: 'memory_events_json', type: 'event', label: '事件' },\n];\nconst MEMORY_GRAPH_CATEGORY_DEFINITIONS = [\r\n    { key: 'character', label: '\\u89d2\\u8272', types: ['character'], note: '\\u4eba\\u7269\\u3001\\u8eab\\u4efd\\u3001\\u5173\\u7cfb' },\r\n    { key: 'location', label: '\\u5730\\u70b9', types: ['location'], note: '\\u573a\\u666f\\u3001\\u533a\\u57df\\u3001\\u7a7a\\u95f4' },\r\n    { key: 'item', label: '\\u9053\\u5177', types: ['item'], note: '\\u7269\\u54c1\\u3001\\u88c5\\u5907\\u3001\\u4fe1\\u7269' },\r\n    { key: 'faction', label: '\\u52bf\\u529b', types: ['faction'], note: '\\u7ec4\\u7ec7\\u3001\\u9635\\u8425\\u3001\\u5bb6\\u65cf' },\r\n    { key: 'setting', label: '\\u8bbe\\u5b9a', types: ['concept', 'rule'], note: '\\u4e16\\u754c\\u89c2\\u3001\\u89c4\\u5219\\u3001\\u80fd\\u529b' },\r\n    { key: 'quest', label: '\\u4efb\\u52a1', types: ['quest'], note: '\\u76ee\\u6807\\u3001\\u4f0f\\u7b14\\u3001\\u60ac\\u5ff5' },\r\n    { key: 'event', label: '\\u4e8b\\u4ef6', types: ['event'], note: '\\u5267\\u60c5\\u3001\\u884c\\u52a8\\u3001\\u65f6\\u95f4\\u7247\\u6bb5' },\r\n];\r\nconst MEMORY_GRAPH_CATEGORY_ORDER = MEMORY_GRAPH_CATEGORY_DEFINITIONS.map(item => item.key);\r\nconst MEMORY_GRAPH_KIND_DEFINITIONS = [\r\n    { key: 'plot', label: '\\u5267\\u60c5', types: ['event'], cues: /(剧情|进展|发生|遇到|发现|到达|离开|进入|战斗|决定|调查|plot|event|scene|timeline)/iu },\r\n    { key: 'relationship', label: '\\u5173\\u7cfb', types: ['character', 'faction'], cues: /(关系|信任|好感|敌意|同盟|合作|背叛|亲近|疏远|relationship|trust|ally|enemy|bond)/iu },\r\n    { key: 'state', label: '\\u72b6\\u6001', types: ['character'], cues: /(状态|情绪|受伤|能力|身份|处境|变化|疲惫|害怕|开心|state|mood|emotion|status|injured)/iu },\r\n    { key: 'goal', label: '\\u76ee\\u6807', types: ['quest'], cues: /(目标|任务|计划|待办|寻找|调查|解决|mission|goal|objective|task|quest)/iu },\r\n    { key: 'clue', label: '\\u4f0f\\u7b14', types: ['quest', 'item', 'concept'], cues: /(伏笔|线索|秘密|悬念|谜团|未解|异常|clue|secret|mystery|hint|foreshadow)/iu },\r\n    { key: 'context', label: '\\u4e0a\\u4e0b\\u6587', types: ['location', 'item', 'concept', 'rule'], cues: /(地点|道具|设定|规则|世界观|物品|场景|location|item|lore|rule|setting)/iu },\r\n];\r\nconst MEMORY_LINK_TYPE_OPTIONS = [\r\n    { value: 'INVOLVES', label: '\\u6d89\\u53ca' },\r\n    { value: 'PART_OF', label: '\\u5c5e\\u4e8e' },\r\n    { value: 'HAPPENS_AT', label: '\\u53d1\\u751f\\u4e8e' },\r\n    { value: 'FOLLOWS', label: '\\u540e\\u7eed\\u4e8e' },\r\n    { value: 'UPDATES', label: '\\u66f4\\u65b0' },\r\n    { value: 'OPPOSES', label: '\\u5bf9\\u7acb' },\r\n    { value: 'ALLIED_WITH', label: '\\u540c\\u76df' },\r\n    { value: 'CAUSES', label: '\\u5bfc\\u81f4' },\r\n    { value: 'RELATED', label: '\\u76f8\\u5173' },\r\n    { value: 'MENTIONS', label: '\\u63d0\\u53ca' },\r\n];\r\nconst MEMORY_LINK_TYPES = new Set([\r\n    'INVOLVES', 'PART_OF', 'HAPPENS_AT', 'FOLLOWS', 'UPDATES', 'OPPOSES',\r\n    'ALLIED_WITH', 'CAUSES', 'RELATED', 'MENTIONS',\r\n]);\r\nconst FETCH_FALLBACK_ENDPOINTS = [\r\n    '/api/backends/chat-completions/generate',\r\n    '/api/backends/text-completions/generate',\r\n    '/api/backends/kobold/generate',\r\n    '/api/novelai/generate',\r\n    '/api/horde/generate-text',\r\n];\r\nconst COMMON_QUERY_TERMS = new Set([\r\n    '\\u5982\\u679c', '\\u6709\\u4eba', '\\u8fd9\\u4e2a', '\\u90a3\\u4e2a', '\\u8fd9\\u91cc', '\\u90a3\\u91cc', '\\u4ec0\\u4e48', '\\u600e\\u4e48', '\\u4e3a\\u4f55', '\\u4e3a\\u4ec0\\u4e48', '\\u7136\\u540e',\r\n    '\\u53ef\\u4ee5', '\\u662f\\u4e0d\\u662f', '\\u5c31\\u662f', '\\u4e0d\\u662f', '\\u4e00\\u4e2a', '\\u4e00\\u4e0b\\u5b50', '\\u8fd9\\u6837', '\\u90a3\\u6837', '\\u4f1a\\u88ab', '\\u4f1a\\u4e0d\\u4f1a',\r\n    '\\u5230\\u5e95', '\\u771f\\u7684', '\\u5df2\\u7ecf', '\\u73b0\\u5728', '\\u4e4b\\u524d', '\\u4e4b\\u540e', '\\u800c\\u4e14', '\\u56e0\\u4e3a', '\\u6240\\u4ee5', '\\u90a3\\u4e2a\\u5730\\u65b9',\r\n]);\r\n\r\nconst defaultSystemPrompt = `\\u4f60\\u662f SillyTavern \\u7684\\u524d\\u7f6e\\u4e16\\u754c\\u4e66\\u8def\\u7531\\u5668\\u3002\r\n\\u53ea\\u505a\\u4e00\\u4ef6\\u4e8b\\uff1a\\u4ece\\u5019\\u9009 keys \\u4e2d\\u9009\\u62e9\\u672c\\u8f6e\\u771f\\u6b63\\u76f8\\u5173\\u7684\\u6761\\u76ee\\u3002\r\n\\u53ea\\u8f93\\u51fa\\u4e25\\u683c JSON\\u3002\r\n\\u7981\\u6b62 Markdown\\u3002\r\n\\u7981\\u6b62\\u89e3\\u91ca\\u3002\r\n\\u7981\\u6b62 reasoning\\u3002\r\n\\u7981\\u6b62\\u989d\\u5916\\u5b57\\u6bb5\\u3002\r\n\\u552f\\u4e00\\u5408\\u6cd5\\u683c\\u5f0f\\uff1a{\"selected\":[{\"key\":\"\\u547d\\u4e2d\\u7684 key\",\"reason\":\"\\u7b80\\u77ed\\u539f\\u56e0\"}]}`;\r\nconst defaultSettings = {\r\n    enabled: false,\r\n    debug: false,\r\n    entryDiagnostics: false,\r\n    floatingButtonEnabled: true,\r\n    routerUseSeparateModel: false,\r\n    routerApiUrl: '',\r\n    routerApiKey: '',\r\n    routerModel: '',\r\n    routerModels: [],\r\n    routerStatus: '???',\r\n    routerRequestMaxTokens: 10000,\r\n    maxCandidates: 24,\r\n    maxSelected: 5,\r\n    maxChars: 4000,\r\n    scanMessages: 8,\r\n    mainHistoryAiTurns: 0,\r\n    memoryEnabled: false,\r\n    memoryAutoRun: true,\r\n    memoryAutoRunInterval: 20,\r\n    memoryRealtimeEnabled: true,\r\n    memoryRealtimeScanMessages: 4,\r\n    memorySummaryEnabled: true,\r\n    memorySummaryIntervalMessages: 20,\r\n    memorySummaryScanMessages: 24,\r\n    memoryInjectToRouter: true,\r\n    memoryReviewRequired: false,\r\n    memoryDebug: false,\r\n    memoryScopeDebug: false,\r\n    memoryHistorySkipDone: true,\r\n    memoryHistoryMode: 'history',\r\n    memoryScanMessages: 6,\r\n    memoryRequestMaxTokens: 10000,\r\n    memoryRetries: 3,\r\n    memoryMaxNodes: 60,\r\n    memoryMaxLinks: 120,\r\n    memoryContainersByChat: {},\r\n    memoryLegacyGraphChatKeys: {},\r\n    memoryGraphsByChat: {},\r\n    memoryStatusesByChat: {},\r\n    memoryLastTurnSignaturesByChat: {},\r\n    memoryLastSourceByChat: {},\r\n    memoryInvalidatedSourcesByChat: {},\r\n    memoryLastPromptsByChat: {},\r\n    memoryLastRawByChat: {},\r\n    memoryLastErrorsByChat: {},\r\n    memoryGraph: null,\r\n    memoryLastTurnSignature: '',\r\n    memoryStatus: '???',\r\n    memoryLastPrompt: '',\r\n    memoryLastRaw: '',\n    memoryLastError: '',\n    memoryRuleConfig: null,\n    bookshelfEnabled: false,\n    bookshelfAutoInject: false,\n    bookshelfAutoMemoryBook: true,\n    bookshelfAutoMemoryBookVectorize: true,\n    bookshelfOnlyBound: true,\n    bookshelfAllowGlobal: false,\r\n    bookshelfMaxChunks: 3,\r\n    bookshelfMaxChunkChars: 500,\r\n    bookshelfMinScore: 0.35,\r\n    bookshelfMemoryVectorRecall: true,\r\n    bookshelfMemoryVectorMaxItems: 4,\r\n    bookshelfEmbeddingMode: 'api',\r\n    bookshelfApiUrl: '',\r\n    bookshelfApiKey: '',\r\n    bookshelfApiModel: '',\r\n    bookshelfApiModels: [],\r\n    bookshelfApiBatchSize: 8,\r\n    bookshelfLocalModelId: 'Xenova/paraphrase-multilingual-MiniLM-L12-v2',\r\n    bookshelfLocalModelStatus: 'not_loaded',\r\n    bookshelfLocalBatchSize: 2,\r\n    bookshelfLastTestQuery: '',\r\n    keywordRecall: true,\r\n    useMvu: false,\r\n    allowConstant: false,\r\n    titleBlocklist: '',\r\n    worldSelectionStates: {},\r\n    position: extension_prompt_types.IN_CHAT,\r\n    depth: 4,\r\n    role: extension_prompt_roles.SYSTEM,\r\n    aiResponseLength: 256,\r\n    routerRetries: 1,\r\n    systemPrompt: defaultSystemPrompt,\r\n};\r\n\r\nconst settings = structuredClone(defaultSettings);\nconst RUNTIME_ACTIVITY_STAGES = [\n    { key: 'recall', label: '召回' },\n    { key: 'preRefresh', label: '预刷新' },\n    { key: 'select', label: 'AI筛选' },\n    { key: 'inject', label: '注入' },\n    { key: 'graph', label: '图谱' },\n    { key: 'embedding', label: '向量' },\n];\n\nfunction createEmptyRuntimeActivity() {\n    return {\n        active: false,\n        stage: 'idle',\n        label: '等待中',\n        detail: '插件待命，下一轮生成后会显示运行流程。',\n        status: 'idle',\n        startedAt: 0,\n        updatedAt: 0,\n        finishedAt: 0,\n        stages: {},\n    };\n}\n\nfunction getRuntimeActivity() {\n    if (!lastRun.activity || typeof lastRun.activity !== 'object') {\n        lastRun.activity = createEmptyRuntimeActivity();\n    }\n    return lastRun.activity;\n}\n\nfunction getRuntimeActivityStageLabel(stage) {\n    return RUNTIME_ACTIVITY_STAGES.find(item => item.key === stage)?.label || '运行';\n}\n\nfunction getRuntimeActivityStatusLabel(status) {\n    return ({\n        running: '进行中',\n        success: '完成',\n        failed: '失败',\n        idle: '等待',\n    })[status] || '等待';\n}\n\nfunction renderRuntimeActivityPanel(target = null) {\n    const panel = target ? $(target) : $('#ai_wbr_runtime_activity_panel');\n    if (!panel.length) return;\n\n    const activity = getRuntimeActivity();\n    const elapsed = activity.startedAt\n        ? formatApiStatsDuration((activity.finishedAt || Date.now()) - activity.startedAt)\n        : '0ms';\n    panel.empty();\n    panel\n        .removeClass('idle running success failed')\n        .addClass(activity.status || 'idle');\n\n    panel.append(\n        $('<div class=\"ai-wbr-runtime-activity-head\"></div>')\n            .append($('<div></div>')\n                .append($('<b></b>').text(activity.label || '等待中'))\n                .append($('<small></small>').text(activity.detail || '插件待命。')))\n            .append($('<span></span>').text(`${getRuntimeActivityStatusLabel(activity.status)} · ${elapsed}`)),\n    );\n\n    const steps = $('<div class=\"ai-wbr-runtime-activity-steps\"></div>');\n    for (const stage of RUNTIME_ACTIVITY_STAGES) {\n        const state = activity.stages?.[stage.key] || {};\n        const status = state.status || (activity.stage === stage.key ? activity.status : 'idle');\n        steps.append(\n            $('<div class=\"ai-wbr-runtime-step\"></div>')\n                .addClass(status)\n                .toggleClass('current', activity.stage === stage.key)\n                .append($('<span></span>').text(stage.label))\n                .append($('<small></small>').text(state.detail || getRuntimeActivityStatusLabel(status))),\n        );\n    }\n    panel.append(steps);\n}\n\nfunction updateRuntimeActivityFab() {\n    const activity = getRuntimeActivity();\n    const status = activity.status || 'idle';\n    const stageLabel = activity.stage === 'idle' ? '' : getRuntimeActivityStageLabel(activity.stage);\n    ensureFloatingFabMounted();\n    $('#ai_wbr_fab')\n        .removeClass('ai-wbr-fab-runtime idle running success failed')\n        .addClass(`ai-wbr-fab-runtime ${status}`)\n        .attr('data-runtime-label', stageLabel)\n        .attr('title', activity.detail ? `世界书读取：${activity.label} · ${activity.detail}` : `世界书读取：${activity.label}`);\n    $('#ai_wbr_console_status')\n        .removeClass('idle ready active ok warn error running success failed')\n        .addClass(status)\n        .text(activity.label || '等待生成');\n    renderRuntimeActivityPanel();\n}\n\nfunction startRuntimeActivity(stage, label, detail = '') {\n    const now = Date.now();\n    const activity = getRuntimeActivity();\n    activity.active = true;\n    activity.stage = stage;\n    activity.label = label || `${getRuntimeActivityStageLabel(stage)}中`;\n    activity.detail = detail || activity.detail || '';\n    activity.status = 'running';\n    activity.startedAt = activity.startedAt || now;\n    activity.updatedAt = now;\n    activity.finishedAt = 0;\n    activity.stages[stage] = {\n        ...(activity.stages[stage] || {}),\n        status: 'running',\n        detail: detail || '进行中',\n        startedAt: activity.stages[stage]?.startedAt || now,\n        updatedAt: now,\n    };\n    updateRuntimeActivityFab();\n    return activity;\n}\n\nfunction updateRuntimeActivity(stage, detail = '', extra = {}) {\n    const activity = getRuntimeActivity();\n    const now = Date.now();\n    activity.stage = stage || activity.stage;\n    activity.detail = detail || activity.detail;\n    activity.updatedAt = now;\n    activity.stages[activity.stage] = {\n        ...(activity.stages[activity.stage] || {}),\n        status: activity.stages[activity.stage]?.status || 'running',\n        detail: detail || activity.stages[activity.stage]?.detail || '',\n        updatedAt: now,\n        ...extra,\n    };\n    updateRuntimeActivityFab();\n}\n\nfunction finishRuntimeActivityStage(stage, detail = '') {\n    const activity = getRuntimeActivity();\n    const now = Date.now();\n    activity.stages[stage] = {\n        ...(activity.stages[stage] || {}),\n        status: 'success',\n        detail: detail || '完成',\n        updatedAt: now,\n        finishedAt: now,\n    };\n    activity.updatedAt = now;\n    updateRuntimeActivityFab();\n}\n\nfunction failRuntimeActivity(stage, error) {\n    const activity = getRuntimeActivity();\n    const now = Date.now();\n    const detail = truncateText(error?.message || String(error || '未知错误'), 120);\n    activity.active = false;\n    activity.stage = stage || activity.stage || 'idle';\n    activity.label = `${getRuntimeActivityStageLabel(activity.stage)}失败`;\n    activity.detail = detail;\n    activity.status = 'failed';\n    activity.updatedAt = now;\n    activity.finishedAt = now;\n    activity.stages[activity.stage] = {\n        ...(activity.stages[activity.stage] || {}),\n        status: 'failed',\n        detail,\n        updatedAt: now,\n        finishedAt: now,\n    };\n    updateRuntimeActivityFab();\n}\n\nfunction completeRuntimeActivity(label = '本轮流程完成', detail = '') {\n    const activity = getRuntimeActivity();\n    const now = Date.now();\n    activity.active = false;\n    activity.label = label;\n    activity.detail = detail || activity.detail || '已完成。';\n    activity.status = 'success';\n    activity.updatedAt = now;\n    activity.finishedAt = now;\n    updateRuntimeActivityFab();\n}\n\nfunction resetRuntimeActivity(label = '等待中') {\n    lastRun.activity = createEmptyRuntimeActivity();\n    lastRun.activity.label = label;\n    if ($('#ai_wbr_fab').length || $('#ai_wbr_console_status').length || $('#ai_wbr_runtime_activity_panel').length) {\n        updateRuntimeActivityFab();\n    }\n}\n\nfunction createEmptyApiStats() {\n    return {\n        startedAt: 0,\n        updatedAt: 0,\n        label: '尚无本轮 API 调用',\n        totals: {\n            all: 0,\n            router: 0,\n            memory: 0,\n            preRefresh: 0,\n            embedding: 0,\n            retry: 0,\n            failed: 0,\n            durationMs: 0,\n        },\n        items: [],\n    };\n}\n\nfunction getApiStats() {\n    if (!lastRun.apiStats || typeof lastRun.apiStats !== 'object') {\n        lastRun.apiStats = createEmptyApiStats();\n    }\n    return lastRun.apiStats;\n}\n\nfunction beginApiStatsRound(label = '本轮 API 调用') {\n    lastRun.apiStats = createEmptyApiStats();\n    lastRun.apiStats.startedAt = Date.now();\n    lastRun.apiStats.updatedAt = lastRun.apiStats.startedAt;\n    lastRun.apiStats.label = label;\n    lastRun.activity = createEmptyRuntimeActivity();\n    renderApiStatsPanel();\n    renderRuntimeActivityPanel();\n    return lastRun.apiStats;\n}\n\nfunction resetApiStatsRound(label = '尚无本轮 API 调用') {\n    lastRun.apiStats = createEmptyApiStats();\n    lastRun.apiStats.label = label;\n    renderApiStatsPanel();\n    resetRuntimeActivity();\n}\n\nfunction recordApiCallStart(type, label, meta = {}) {\n    const stats = getApiStats();\n    const now = Date.now();\n    if (!stats.startedAt) {\n        stats.startedAt = now;\n        stats.label = meta.roundLabel || label || '本轮 API 调用';\n    }\n    const item = {\n        id: `api_${now}_${Math.random().toString(36).slice(2, 8)}`,\n        type: String(type || 'other'),\n        label: String(label || type || 'API 调用'),\n        startedAt: now,\n        durationMs: 0,\n        status: 'running',\n        retry: !!meta.retry,\n        source: String(meta.source || ''),\n    };\n    stats.items.push(item);\n    stats.updatedAt = now;\n    stats.totals.all += 1;\n    if (Object.hasOwn(stats.totals, item.type)) {\n        stats.totals[item.type] += 1;\n    }\n    if (item.retry) {\n        stats.totals.retry += 1;\n    }\n    renderApiStatsPanel();\n    return item;\n}\n\nfunction recordApiCallEnd(item, error = null) {\n    if (!item) return;\n    const stats = getApiStats();\n    item.durationMs = Math.max(0, Date.now() - Number(item.startedAt || Date.now()));\n    item.status = error ? 'failed' : 'success';\n    if (error) {\n        item.error = truncateText(error?.message || String(error), 120);\n        stats.totals.failed += 1;\n    }\n    stats.totals.durationMs = stats.items.reduce((sum, entry) => sum + Number(entry.durationMs || 0), 0);\n    stats.updatedAt = Date.now();\n    renderApiStatsPanel();\n}\n\nfunction recordApiRetry(type, label, source = '') {\n    const item = recordApiCallStart(type, label, { retry: true, source });\n    recordApiCallEnd(item);\n}\n\nfunction formatApiStatsDuration(ms) {\n    const value = Math.max(0, Number(ms || 0));\n    if (value >= 1000) {\n        return `${(value / 1000).toFixed(1)}s`;\n    }\n    return `${Math.round(value)}ms`;\n}\n\nfunction getApiStatsTypeLabel(type) {\n    return ({\n        router: '路由',\n        memory: '记忆',\n        preRefresh: '预刷新',\n        embedding: '向量',\n    })[type] || '其他';\n}\n\nfunction renderApiStatsPanel(target = null) {\n    const panel = target ? $(target) : $('#ai_wbr_api_stats_panel');\n    if (!panel.length) return;\n\n    const stats = getApiStats();\n    const totals = stats.totals || createEmptyApiStats().totals;\n    const updatedLabel = stats.updatedAt\n        ? new Date(stats.updatedAt).toLocaleTimeString()\n        : '尚无记录';\n    const items = Array.isArray(stats.items) ? stats.items : [];\n\n    panel.empty();\n    panel.append(\n        $('<div class=\"ai-wbr-api-stats-head\"></div>')\n            .append($('<div></div>')\n                .append($('<b></b>').text(stats.label || '本轮 API 调用'))\n                .append($('<small></small>').text(`最近更新：${updatedLabel}`)))\n            .append($('<button class=\"menu_button\" type=\"button\">清空统计</button>').on('click', () => {\n                resetApiStatsRound();\n                renderStandaloneConsole('settings');\n            })),\n    );\n    panel.append(\n        $('<div class=\"ai-wbr-console-stats ai-wbr-api-stats-grid\"></div>')\n            .append(createStandaloneStat('总调用', totals.all || 0))\n            .append(createStandaloneStat('路由', totals.router || 0))\n            .append(createStandaloneStat('记忆', totals.memory || 0))\n            .append(createStandaloneStat('预刷新', totals.preRefresh || 0))\n            .append(createStandaloneStat('向量', totals.embedding || 0))\n            .append(createStandaloneStat('重试', totals.retry || 0))\n            .append(createStandaloneStat('失败', totals.failed || 0))\n            .append(createStandaloneStat('耗时', formatApiStatsDuration(totals.durationMs || 0))),\n    );\n\n    const list = $('<div class=\"ai-wbr-api-stats-list\"></div>');\n    if (!items.length) {\n        list.append($('<div class=\"ai-wbr-console-empty\"></div>').text('尚无 API 调用记录。下一轮生成、记忆更新或向量召回后会自动显示。'));\n    } else {\n        for (const item of items.slice(-12).reverse()) {\n            const statusText = item.status === 'failed' ? '失败' : item.status === 'running' ? '进行中' : '成功';\n            list.append(\n                $('<div class=\"ai-wbr-api-stats-item\"></div>')\n                    .addClass(item.status || 'success')\n                    .append($('<div></div>')\n                        .append($('<b></b>').text(item.label || getApiStatsTypeLabel(item.type)))\n                        .append($('<small></small>').text(`${getApiStatsTypeLabel(item.type)}${item.retry ? ' · 重试' : ''}${item.source ? ` · ${item.source}` : ''}`)))\n                    .append($('<span></span>').text(`${statusText} · ${formatApiStatsDuration(item.durationMs || 0)}`))\n                    .append(item.error ? $('<small class=\"ai-wbr-api-stats-error\"></small>').text(item.error) : ''),\n            );\n        }\n    }\n    panel.append(list);\n}\n\nlet lastRun = {\n    candidates: [],\n    selected: [],\n    memoryCandidates: [],\n    selectedMemories: [],\n    bookshelfCandidates: [],\r\n    selectedBookshelf: [],\r\n    injectedChars: 0,\r\n    injectionText: '',\r\n    source: 'none',\r\n    error: '',\r\n    routerPrompt: '',\n    routerRaw: '',\n    pipeline: null,\n    apiStats: createEmptyApiStats(),\n    activity: createEmptyRuntimeActivity(),\n};\nlet burstCleanupTimer = null;\r\nlet routerBusyPromise = null;\r\nlet resolveRouterBusy = null;\r\nlet isGenerationActive = false;\r\nlet pendingCompatSend = false;\r\nlet compatFlushScheduled = false;\r\nlet suppressCompatReplay = false;\r\nlet compatHooksInstalled = false;\r\nlet compatHookRetryTimer = null;\r\nlet compatGenerateHookTimer = null;\r\nlet isCompatRouterRunning = false;\r\nlet isRouterSelectionRequest = false;\r\nlet fetchFallbackInstalled = false;\r\nlet lastRouteCompletedAt = 0;\r\nlet isMemoryWorkerRunning = false;\r\nlet memoryWorkerStartedAt = 0;\r\nlet memoryUpdateTimer = null;\r\nlet chatUiRefreshTimers = [];\r\nlet chatScopedUiPollTimer = null;\r\nlet lastObservedChatScopedUiSignature = '';\r\nlet memoryGraphView = { x: 0, y: 0, width: MEMORY_GRAPH_CANVAS_WIDTH, height: MEMORY_GRAPH_CANVAS_HEIGHT };\r\nlet memoryGraphDrag = null;\r\nlet memoryGraphPan = null;\r\nlet memoryGraphTouch = null;\r\nlet memoryGraphLinkSourceId = '';\r\nlet memoryGraphSelectedNodeId = '';\r\nlet memoryGraphSelectedLinkId = '';\r\nlet memoryGraphDetailMode = '';\r\nlet memoryGraphDisplayMode = 'overview';\r\nlet memoryGraphMinLinkWeight = 0.35;\r\nlet memoryGraphSearchText = '';\r\nlet memoryGraphVisibleTypes = new Set();\r\nlet memoryGraphSearchTimer = null;\nlet memoryGraphRenderFrame = null;\nlet memoryGraphPreviewRenderKey = '';\nlet memoryGraphDragFrame = null;\nlet memoryGraphFullscreenActive = false;\nlet memoryGraphSuppressNextNodeClick = false;\nlet memoryRuleDrawerOpen = false;\nlet memoryRuleSaveTimer = null;\nlet bookshelfDbPromise = null;\nlet selectedBookshelfBookId = '';\nlet bookshelfVirtualBooks = new Map();\nlet bookshelfLongPressTimer = null;\nlet bookshelfSuppressNextBookClick = false;\nlet bookshelfLastTestResults = [];\nlet bookshelfLastStatus = '';\nlet bookshelfLocalPipelinePromise = null;\r\nlet bookshelfVectorAbortRequested = false;\r\nlet bookshelfMemoryBookSyncTimer = null;\r\nconst BOOKSHELF_DB_NAME = 'AIWBR_VectorBookshelf';\r\nconst BOOKSHELF_DB_VERSION = 2;\r\nconst BOOKSHELF_MAX_IMPORT_CHUNKS = 2000;\r\nconst BOOKSHELF_LOCAL_TRANSFORMERS_CDN = 'https://cdn.jsdelivr.net/npm/@xenova/transformers@2.17.2';\r\nlet lastKnownChatFileName = '';\r\n\r\nfunction beginRouterBusy() {\r\n    if (routerBusyPromise) {\r\n        return () => { };\r\n    }\r\n\r\n    routerBusyPromise = new Promise((resolve) => {\r\n        resolveRouterBusy = resolve;\r\n    });\r\n\r\n    return () => {\r\n        if (!routerBusyPromise) {\r\n            return;\r\n        }\r\n\r\n        const resolve = resolveRouterBusy;\r\n        routerBusyPromise = null;\r\n        resolveRouterBusy = null;\r\n        resolve?.();\r\n        scheduleCompatFlush();\r\n    };\r\n}\r\n\r\nfunction clearChatUiRefreshTimers() {\r\n    for (const timer of chatUiRefreshTimers) {\r\n        clearTimeout(timer);\r\n    }\r\n    chatUiRefreshTimers = [];\r\n}\r\n\r\nfunction getChatScopedUiSignature(context = getContext()) {\r\n    const chat = Array.isArray(context?.chat) ? context.chat : [];\r\n    const first = chat[0];\r\n    const memoryRaw = first && typeof first === 'object' ? first[CHAT_MEMORY_FIELD] : '';\r\n    const memoryStamp = typeof memoryRaw === 'string'\r\n        ? memoryRaw.slice(0, 120)\r\n        : JSON.stringify(memoryRaw || {}).slice(0, 120);\r\n    const charaFile = String(getCharaFilename?.() || '');\r\n    const character = context?.characters?.[context?.characterId] || context?.character;\r\n    const worldStamp = getActiveWorldbookBindings(context)\r\n        .map(binding => `${binding.worldName}:${binding.source}:${getWorldSelectionState(binding.worldName) ? '1' : '0'}`)\r\n        .join(',');\r\n    return [\r\n        charaFile,\r\n        String(context?.characterId ?? context?.character_id ?? ''),\r\n        String(context?.groupId ?? context?.group_id ?? ''),\r\n        String(character?.avatar ?? character?.name ?? ''),\r\n        String(context?.chatId ?? context?.chat_id ?? context?.conversationId ?? context?.sessionId ?? ''),\r\n        String(context?.chatMetadata?.file_name ?? context?.chatMetadata?.main_chat ?? context?.chatMetadata?.name ?? ''),\r\n        String(chat.length),\r\n        memoryStamp,\r\n        worldStamp,\r\n    ].join('|');\r\n}\r\n\r\nfunction rememberChatIdentifierFromEvent(...args) {\r\n    for (const arg of args) {\r\n        const value = typeof arg === 'string'\r\n            ? arg\r\n            : (arg?.chatId || arg?.chat_id || arg?.file_name || arg?.filename || arg?.name || '');\r\n        const text = String(value || '').trim();\r\n        if (text && text !== 'null' && text !== 'undefined') {\r\n            lastKnownChatFileName = text;\r\n            return text;\r\n        }\r\n    }\r\n    return '';\r\n}\r\n\r\nfunction getMemoryGraphSummary(graph) {\r\n    const safeGraph = graph && typeof graph === 'object' ? graph : getDefaultMemoryGraph();\r\n    const state = safeGraph.state || {};\r\n    return {\r\n        location: state.current_location || '',\r\n        time: state.current_time || '',\r\n        objective: state.current_objective || '',\r\n        nodes: Array.isArray(safeGraph.nodes) ? safeGraph.nodes.length : 0,\r\n        links: Array.isArray(safeGraph.links) ? safeGraph.links.length : 0,\r\n        nodeTitles: (Array.isArray(safeGraph.nodes) ? safeGraph.nodes : []).slice(0, 8).map(node => `${node.type || 'memory'}:${node.title || node.id}`),\r\n        updatedAt: safeGraph.updatedAt || '',\r\n    };\r\n}\r\n\r\nfunction getMemoryScopeDebugSnapshot(context = getContext()) {\r\n    const chat = Array.isArray(context?.chat) ? context.chat : [];\r\n    const first = getChatMemoryFirstMessage(context);\r\n    const raw = first && typeof first === 'object' ? first[CHAT_MEMORY_FIELD] : undefined;\r\n    const parsed = typeof raw === 'string'\r\n        ? (() => {\r\n            try {\r\n                return JSON.parse(raw);\r\n            } catch {\r\n                return null;\r\n            }\r\n        })()\r\n        : raw;\r\n    return {\r\n        signature: getChatScopedUiSignature(context),\r\n        chatLength: chat.length,\r\n        firstMessageName: first?.name || '',\r\n        hasFirstMessage: !!first,\r\n        hasChatMemoryField: raw !== undefined,\r\n        rawType: raw === undefined ? 'undefined' : typeof raw,\r\n        rawPreview: typeof raw === 'string' ? raw.slice(0, 180) : JSON.stringify(raw || null).slice(0, 180),\r\n        parsedGraph: getMemoryGraphSummary(parsed?.graph || null),\r\n        contextIds: {\r\n            charaFile: String(getCharaFilename?.() || ''),\r\n            characterId: context?.characterId ?? context?.character_id ?? '',\r\n            groupId: context?.groupId ?? context?.group_id ?? '',\r\n            chatId: context?.chatId ?? context?.chat_id ?? '',\r\n            conversationId: context?.conversationId ?? '',\r\n            sessionId: context?.sessionId ?? '',\r\n            metadataFile: context?.chatMetadata?.file_name ?? context?.chatMetadata?.main_chat ?? context?.chatMetadata?.name ?? '',\r\n        },\r\n    };\r\n}\r\n\r\nfunction memoryScopeDebugLog(action, details = {}, context = getContext()) {\r\n    if (!settings.memoryScopeDebug) {\r\n        return;\r\n    }\r\n    try {\r\n        console.groupCollapsed(`${LOG_PREFIX} [MemoryScope] ${action}`);\r\n        console.debug('snapshot:', getMemoryScopeDebugSnapshot(context));\r\n        if (details && Object.keys(details).length) {\r\n            console.debug('details:', details);\r\n        }\r\n        console.groupEnd();\r\n    } catch (error) {\r\n        console.warn(`${LOG_PREFIX} [MemoryScope] debug log failed`, error);\r\n    }\r\n}\r\n\r\nif (typeof window !== 'undefined') {\r\n    window.aiWbrMemoryDebugDump = () => {\r\n        const snapshot = getMemoryScopeDebugSnapshot();\r\n        console.debug(`${LOG_PREFIX} [MemoryScope] manual dump`, snapshot);\r\n        return snapshot;\r\n    };\r\n}\r\n\r\nfunction clearMemoryUiForScopeSwitch() {\r\n    memoryScopeDebugLog('clearMemoryUiForScopeSwitch');\r\n    $('#ai_wbr_memory_node_popover').hide();\r\n    $('#ai_wbr_memory_graph').html('<div class=\"ai-wbr-token-empty\">正在切换聊天记忆...</div>');\r\n    $('#ai_wbr_memory_node_editor').empty().append('<div class=\"ai-wbr-token-empty\">点击上方图谱节点后，这里会显示该节点的可编辑信息。</div>');\r\n    $('#ai_wbr_memory_nodes').empty();\r\n    $('#ai_wbr_memory_links').empty();\r\n    $('#ai_wbr_memory_json').val('');\r\n}\r\n\r\nfunction safeRenderChatScopedPanels() {\r\n    if (memoryGraphDrag || memoryGraphPan) {\r\n        memoryScopeDebugLog('safeRenderChatScopedPanels skipped while dragging/panning');\r\n        return;\r\n    }\r\n    memoryScopeDebugLog('safeRenderChatScopedPanels before render');\r\n    renderDebugPanel();\r\n    renderMemoryPanel();\r\n    renderActiveWorldbookSelector();\r\n}\r\n\r\nfunction scheduleChatScopedUiRefresh() {\n    clearChatUiRefreshTimers();\n    const delays = [0, 40, 140, 360, 800];\n    memoryScopeDebugLog('scheduleChatScopedUiRefresh', { delays });\n    for (const delay of delays) {\n        const timer = setTimeout(() => {\n            safeRenderChatScopedPanels();\n            scheduleFloatingFabRecovery();\n        }, delay);\n        chatUiRefreshTimers.push(timer);\n    }\n}\n\r\nfunction startChatScopedUiPolling() {\r\n    if (chatScopedUiPollTimer) {\r\n        clearInterval(chatScopedUiPollTimer);\r\n    }\r\n    lastObservedChatScopedUiSignature = getChatScopedUiSignature();\r\n    chatScopedUiPollTimer = setInterval(() => {\r\n        if (memoryGraphDrag || memoryGraphPan) {\r\n            return;\r\n        }\r\n        const nextSignature = getChatScopedUiSignature();\r\n        if (nextSignature !== lastObservedChatScopedUiSignature) {\n            memoryScopeDebugLog('poll signature changed', {\n                previous: lastObservedChatScopedUiSignature,\n                next: nextSignature,\n            });\n            lastObservedChatScopedUiSignature = nextSignature;\n            safeRenderChatScopedPanels();\n        }\n        ensureFloatingFabMounted();\n    }, 500);\n}\n\r\nfunction handleChatScopedUiMaybeChanged() {\r\n    memoryScopeDebugLog('handleChatScopedUiMaybeChanged');\r\n    clearEntryBurst();\r\n    stopWorldInfoAnimation();\r\n    clearTimeout(memoryUpdateTimer);\r\n    clearTimeout(bookshelfMemoryBookSyncTimer);\r\n    clearChatUiRefreshTimers();\r\n    memoryGraphSelectedNodeId = '';\r\n    memoryGraphSelectedLinkId = '';\r\n    memoryGraphLinkSourceId = '';\r\n    selectedBookshelfBookId = '';\r\n    clearMemoryUiForScopeSwitch();\r\n    lastObservedChatScopedUiSignature = '';\r\n    scheduleChatScopedUiRefresh();\r\n    scheduleBookshelfMemoryBookSync(getContext(), { force: true, silent: true, delayMs: 250 });\r\n}\r\n\r\nfunction handleMessageDeletedOrSwiped() {\r\n    try {\r\n        const context = getContext();\r\n        const chatKey = getCurrentChatMemoryKey(context);\r\n        const lastSource = settings.memoryLastSourceByChat?.[chatKey];\r\n        if (lastSource?.id) {\r\n            settings.memoryInvalidatedSourcesByChat[chatKey] = uniqueStrings([\r\n                ...(Array.isArray(settings.memoryInvalidatedSourcesByChat?.[chatKey]) ? settings.memoryInvalidatedSourcesByChat[chatKey] : []),\r\n                String(lastSource.id),\r\n            ]).slice(-40);\r\n        }\r\n        const container = getChatMemoryContainer(context);\r\n        if (Array.isArray(container.reviewQueue) && container.reviewQueue.length) {\r\n            container.reviewQueue = container.reviewQueue.map(item => ({\r\n                ...item,\r\n                status: item.status === 'pending' ? 'stale' : item.status,\r\n                staleReason: item.staleReason || 'message_changed',\r\n            }));\r\n        }\r\n        if (container.graphBackup) {\r\n            memoryScopeDebugLog('Restoring memory graph from backup due to message deletion/swipe');\r\n            container.graph = cloneMemoryGraph(container.graphBackup);\r\n            container.graphBackup = null;\r\n            persistChatMemoryContainer(container, context);\r\n            settings.memoryGraph = container.graph;\r\n            Object.assign(extension_settings[MODULE_NAME], settings);\r\n            saveSettingsDebounced();\r\n            setCurrentMemoryLastTurnSignature('', context);\r\n            setMemoryStatus('\\u5df2\\u56de\\u9000\\u8bb0\\u5fc6\\u72b6\\u6001\\uff08\\u68c0\\u6d4b\\u5230\\u91cd\\u751f\\u6210/\\u5220\\u9664\\uff09', context);\r\n            scheduleBookshelfMemoryBookSync(context, { force: true, silent: true, delayMs: 200 });\r\n            \r\n            // Re-render UI if applicable\r\n            if (!memoryGraphDrag && !memoryGraphPan) {\r\n                scheduleChatScopedUiRefresh();\r\n            }\r\n        } else {\r\n            persistChatMemoryContainer(container, context);\r\n            setCurrentMemoryLastTurnSignature('', context);\r\n            scheduleBookshelfMemoryBookSync(context, { force: true, silent: true, delayMs: 200 });\r\n            scheduleChatScopedUiRefresh();\r\n        }\r\n    } catch (error) {\r\n        console.warn(`${LOG_PREFIX} Failed to restore memory graph backup`, error);\r\n    }\r\n}\r\n\r\nfunction installChatScopedUiRefreshEventHooks() {\r\n    const strictScopeEvents = [\r\n        'CHAT_CHANGED',\r\n        'CHAT_LOADED',\r\n    ];\r\n\r\n    for (const key of strictScopeEvents) {\r\n        const value = event_types?.[key];\r\n        if (!value) {\r\n            continue;\r\n        }\r\n        try {\r\n            eventSource.on(value, (...args) => {\r\n                rememberChatIdentifierFromEvent(...args);\r\n                memoryScopeDebugLog(`event ${key}`, {\r\n                    eventValue: value,\r\n                    argsPreview: args.slice(0, 2).map(arg => {\r\n                        try {\r\n                            return JSON.stringify(arg).slice(0, 240);\r\n                        } catch {\r\n                            return String(arg);\r\n                        }\r\n                    }),\r\n                });\r\n                handleChatScopedUiMaybeChanged();\r\n            });\r\n        } catch {\r\n            // no-op\r\n        }\r\n    }\r\n\r\n    const rollbackEvents = ['MESSAGE_DELETED', 'MESSAGE_SWIPED'];\r\n    for (const key of rollbackEvents) {\r\n        const value = event_types?.[key];\r\n        if (!value) {\r\n            continue;\r\n        }\r\n        try {\r\n            eventSource.on(value, () => {\r\n                handleMessageDeletedOrSwiped();\r\n            });\r\n        } catch {\r\n            // no-op\r\n        }\r\n    }\r\n}\r\n\r\nasync function waitForCompatIdle() {\r\n    if (routerBusyPromise) {\r\n        try {\r\n            await routerBusyPromise;\r\n        } catch {\r\n            // no-op\r\n        }\r\n    }\r\n\r\n    if (!isGenerationActive) {\r\n        return;\r\n    }\r\n\r\n    await new Promise((resolve) => {\r\n        let settled = false;\r\n        const done = () => {\r\n            if (settled) {\r\n                return;\r\n            }\r\n\r\n            settled = true;\r\n            resolve();\r\n        };\r\n\r\n        eventSource.once(event_types.GENERATION_ENDED, done);\r\n        eventSource.once(event_types.GENERATION_STOPPED, done);\r\n    });\r\n}\r\n\r\nfunction scheduleCompatFlush() {\r\n    if (compatFlushScheduled || !pendingCompatSend) {\r\n        return;\r\n    }\r\n\r\n    compatFlushScheduled = true;\r\n    (async () => {\r\n        try {\r\n            await waitForCompatIdle();\r\n            await new Promise(resolve => setTimeout(resolve, 250));\r\n\r\n            if (!pendingCompatSend || routerBusyPromise || isGenerationActive) {\r\n                return;\r\n            }\r\n\r\n            const sendButton = document.getElementById('send_but');\r\n            if (!sendButton) {\r\n                return;\r\n            }\r\n\r\n            pendingCompatSend = false;\r\n            suppressCompatReplay = true;\r\n            setTimeout(() => {\r\n                try {\r\n                    sendButton.click();\r\n                } finally {\r\n                    setTimeout(() => {\r\n                        suppressCompatReplay = false;\r\n                    }, 180);\r\n                }\r\n            }, 0);\r\n        } finally {\r\n            compatFlushScheduled = false;\r\n            if (pendingCompatSend && !compatFlushScheduled) {\r\n                scheduleCompatFlush();\r\n            }\r\n        }\r\n    })();\r\n}\r\n\r\nfunction queueCompatSend(event) {\r\n    if (!settings.enabled || suppressCompatReplay || !routerBusyPromise) {\r\n        return false;\r\n    }\r\n\r\n    pendingCompatSend = true;\r\n    event?.preventDefault?.();\r\n    event?.stopPropagation?.();\r\n    event?.stopImmediatePropagation?.();\r\n    debugLog('Queued competing send until router idle');\r\n    scheduleCompatFlush();\r\n    return true;\r\n}\r\n\r\nfunction handleCompatTextareaKeydown(event) {\r\n    const isEnter = event.key === 'Enter' || event.code === 'Enter' || event.keyCode === 13;\r\n    if (!isEnter || event.shiftKey || event.ctrlKey || event.altKey || event.metaKey || event.isComposing) {\r\n        return;\r\n    }\r\n\r\n    queueCompatSend(event);\r\n}\r\n\r\nfunction installCompatSendHooks() {\r\n    try {\r\n        const sendButton = document.getElementById('send_but');\r\n        const textarea = document.getElementById('send_textarea');\r\n\r\n        if (sendButton && !sendButton.dataset.aiWbrCompatHook) {\r\n            sendButton.addEventListener('click', queueCompatSend, true);\r\n            sendButton.dataset.aiWbrCompatHook = '1';\r\n        }\r\n\r\n        if (textarea && !textarea.dataset.aiWbrCompatHook) {\r\n            textarea.addEventListener('keydown', handleCompatTextareaKeydown, true);\r\n            textarea.dataset.aiWbrCompatHook = '1';\r\n        }\r\n\r\n        compatHooksInstalled = !!(sendButton && textarea);\r\n        if (!compatHooksInstalled && !compatHookRetryTimer) {\r\n            compatHookRetryTimer = setTimeout(() => {\r\n                compatHookRetryTimer = null;\r\n                installCompatSendHooks();\r\n            }, 1200);\r\n        }\r\n    } catch (error) {\r\n        console.warn(`${LOG_PREFIX} Failed to install compatibility send hooks`, error);\r\n    }\r\n}\r\n\r\nfunction ensureTavernHelperCompatHook() {\r\n    try {\r\n        const helper = globalThis.TavernHelper;\r\n        const original = helper?.generate;\r\n        if (!helper || typeof original !== 'function' || original.__aiWbrCompatWrapped) {\r\n            return false;\r\n        }\r\n\r\n        if (globalThis.original_TavernHelper_generate_ACU?.__aiWbrCompatWrapped) {\r\n            return false;\r\n        }\r\n\r\n        const wrapped = async function (...args) {\r\n            if (settings.enabled && !suppressCompatReplay && (routerBusyPromise || isGenerationActive)) {\r\n                debugLog('Waiting for router idle before TavernHelper.generate');\r\n                await waitForCompatIdle();\r\n                await new Promise(resolve => setTimeout(resolve, 250));\r\n            }\r\n\r\n            if (settings.enabled && !suppressCompatReplay && !isCompatRouterRunning) {\r\n                const routed = await runTavernHelperRoute(args);\r\n                if (routed) {\r\n                    await new Promise(resolve => setTimeout(resolve, 0));\r\n                }\r\n            }\r\n\r\n            return await original.apply(this, args);\r\n        };\r\n\r\n        Object.defineProperty(wrapped, '__aiWbrCompatWrapped', {\r\n            value: true,\r\n            configurable: false,\r\n            enumerable: false,\r\n            writable: false,\r\n        });\r\n\r\n        helper.generate = wrapped;\r\n        return true;\r\n    } catch (error) {\r\n        console.warn(`${LOG_PREFIX} Failed to install TavernHelper compatibility hook`, error);\r\n        return false;\r\n    }\r\n}\r\n\r\nfunction startCompatGenerateHookPolling() {\r\n    if (compatGenerateHookTimer) {\r\n        return;\r\n    }\r\n\r\n    compatGenerateHookTimer = setInterval(() => {\r\n        try {\r\n            ensureTavernHelperCompatHook();\r\n        } catch (error) {\r\n            console.warn(`${LOG_PREFIX} Compatibility polling failed`, error);\r\n        }\r\n    }, 1500);\r\n}\r\n\r\nfunction ensureSettings() {\r\n    if (!extension_settings[MODULE_NAME]) {\r\n        extension_settings[MODULE_NAME] = {};\r\n    }\r\n\r\n    // Deprecated: AI routing is now always on when the plugin itself is enabled.\r\n    if (Object.hasOwn(extension_settings[MODULE_NAME], 'useAi')) {\r\n        delete extension_settings[MODULE_NAME].useAi;\r\n    }\r\n\r\n    Object.assign(settings, defaultSettings, extension_settings[MODULE_NAME]);\r\n    if (!Object.hasOwn(extension_settings[MODULE_NAME], 'memoryRealtimeEnabled')) {\r\n        settings.memoryRealtimeEnabled = !!settings.memoryAutoRun;\r\n    }\r\n    if (!Object.hasOwn(extension_settings[MODULE_NAME], 'memorySummaryEnabled')) {\r\n        settings.memorySummaryEnabled = !!settings.memoryAutoRun;\r\n    }\r\n    if (!Object.hasOwn(extension_settings[MODULE_NAME], 'memorySummaryIntervalMessages')) {\r\n        settings.memorySummaryIntervalMessages = clampNumber(settings.memoryAutoRunInterval, defaultSettings.memorySummaryIntervalMessages, 1, 100);\r\n    }\r\n    if (!Object.hasOwn(extension_settings[MODULE_NAME], 'memoryRealtimeScanMessages')) {\r\n        settings.memoryRealtimeScanMessages = Math.min(6, clampNumber(settings.memoryScanMessages, defaultSettings.memoryRealtimeScanMessages, 2, 40));\r\n    }\r\n    if (!Object.hasOwn(extension_settings[MODULE_NAME], 'memorySummaryScanMessages')) {\r\n        settings.memorySummaryScanMessages = Math.max(8, clampNumber(settings.memoryScanMessages, defaultSettings.memorySummaryScanMessages, 2, 40));\r\n    }\r\n    settings.memoryContainersByChat = settings.memoryContainersByChat && typeof settings.memoryContainersByChat === 'object' ? settings.memoryContainersByChat : {};\r\n    settings.memoryLegacyGraphChatKeys = settings.memoryLegacyGraphChatKeys && typeof settings.memoryLegacyGraphChatKeys === 'object' ? settings.memoryLegacyGraphChatKeys : {};\r\n    settings.worldSelectionStates = settings.worldSelectionStates && typeof settings.worldSelectionStates === 'object' ? settings.worldSelectionStates : {};\r\n    settings.memoryGraphsByChat = settings.memoryGraphsByChat && typeof settings.memoryGraphsByChat === 'object' ? settings.memoryGraphsByChat : {};\r\n    settings.memoryStatusesByChat = settings.memoryStatusesByChat && typeof settings.memoryStatusesByChat === 'object' ? settings.memoryStatusesByChat : {};\r\n    settings.memoryLastTurnSignaturesByChat = settings.memoryLastTurnSignaturesByChat && typeof settings.memoryLastTurnSignaturesByChat === 'object' ? settings.memoryLastTurnSignaturesByChat : {};\r\n    settings.memoryLastSourceByChat = settings.memoryLastSourceByChat && typeof settings.memoryLastSourceByChat === 'object' ? settings.memoryLastSourceByChat : {};\r\n    settings.memoryInvalidatedSourcesByChat = settings.memoryInvalidatedSourcesByChat && typeof settings.memoryInvalidatedSourcesByChat === 'object' ? settings.memoryInvalidatedSourcesByChat : {};\r\n    settings.memoryLastPromptsByChat = settings.memoryLastPromptsByChat && typeof settings.memoryLastPromptsByChat === 'object' ? settings.memoryLastPromptsByChat : {};\r\n    settings.memoryLastRawByChat = settings.memoryLastRawByChat && typeof settings.memoryLastRawByChat === 'object' ? settings.memoryLastRawByChat : {};\r\n    settings.memoryLastErrorsByChat = settings.memoryLastErrorsByChat && typeof settings.memoryLastErrorsByChat === 'object' ? settings.memoryLastErrorsByChat : {};\r\n    if (!settings.memoryGraph || typeof settings.memoryGraph !== 'object') {\n        settings.memoryGraph = getDefaultMemoryGraph();\n    }\n    settings.memoryRuleConfig = normalizeMemoryRuleConfig(settings.memoryRuleConfig);\n    Object.assign(extension_settings[MODULE_NAME], settings);\n    try {\r\n        globalThis.AIWorldbookRouter?.setEntryDiagnostics?.(!!settings.entryDiagnostics);\r\n    } catch (_) {\r\n        // Bootstrap diagnostics sync is best-effort only.\r\n    }\r\n}\r\n\r\nfunction saveSetting(key, value) {\r\n    settings[key] = value;\r\n    Object.assign(extension_settings[MODULE_NAME], settings);\r\n    if (key === 'entryDiagnostics') {\r\n        try {\r\n            localStorage.setItem('AIWBR_entryDiagnosticsEnabled', value ? 'true' : 'false');\r\n            globalThis.AIWorldbookRouter?.setEntryDiagnostics?.(!!value);\r\n        } catch (_) {\r\n            // localStorage may be unavailable in some embedded webviews.\r\n        }\r\n    }\r\n    if (key === 'floatingButtonEnabled') {\n        if (value !== false && !$('#ai_wbr_fab').length) {\n            createFloatingMemoryWindow();\n        }\n        updateFloatingButtonVisibility();\n    }\n    saveSettingsDebounced();\r\n}\r\n\r\nfunction setRouterStatus(text) {\r\n    settings.routerStatus = String(text || '\\u672a\\u8fde\\u63a5');\r\n    $('#ai_wbr_router_status').text(settings.routerStatus);\r\n    Object.assign(extension_settings[MODULE_NAME], settings);\r\n    saveSettingsDebounced();\r\n    renderStandaloneConsole();\r\n}\r\n\r\nfunction getWorldInfoIcon() {\r\n    return $('#WIDrawerIcon');\r\n}\r\n\r\nfunction ensureFxLayer() {\r\n    let layer = $('#ai_wbr_fx_layer');\r\n    if (layer.length) {\r\n        return layer;\r\n    }\r\n\r\n    layer = $('<div id=\"ai_wbr_fx_layer\" aria-hidden=\"true\"></div>');\r\n    $('body').append(layer);\r\n    return layer;\r\n}\r\n\r\nfunction clearEntryBurst() {\r\n    clearTimeout(burstCleanupTimer);\r\n    burstCleanupTimer = null;\r\n    $('#ai_wbr_fx_layer .ai-wbr-entry-burst, #ai_wbr_fx_layer .ai-wbr-status-burst').remove();\r\n}\r\n\r\nfunction startWorldInfoAnimation() {\r\n    const icon = getWorldInfoIcon();\r\n    if (!icon.length) {\r\n        return;\r\n    }\r\n\r\n    const layer = ensureFxLayer();\r\n    const anchor = $('#WI-SP-button .drawer-toggle').first();\r\n    const anchorRect = (anchor.length ? anchor[0] : icon[0]).getBoundingClientRect();\r\n    const iconRect = icon[0].getBoundingClientRect();\r\n    let underline = layer.children('.ai-wbr-book-underline');\r\n    if (!underline.length) {\r\n        underline = $('<div class=\"ai-wbr-book-underline\"></div>');\r\n        layer.append(underline);\r\n    }\r\n\r\n    underline.css({\r\n        left: `${anchorRect.left + (anchorRect.width / 2)}px`,\r\n        top: `${iconRect.bottom - 44}px`,\r\n    });\r\n}\r\n\r\nfunction startMemoryAnimation() {\r\n    startWorldInfoAnimation();\r\n    const underline = $('#ai_wbr_fx_layer .ai-wbr-book-underline');\r\n    if (!underline.length) {\r\n        return;\r\n    }\r\n    underline.addClass('ai-wbr-book-underline-memory');\r\n}\r\n\r\nfunction stopWorldInfoAnimation() {\r\n    const underline = $('#ai_wbr_fx_layer .ai-wbr-book-underline');\r\n    if (!underline.length) {\r\n        return;\r\n    }\r\n\r\n    if (underline.hasClass('ai-wbr-book-underline-error')) {\r\n        setTimeout(() => underline.remove(), 980);\r\n        return;\r\n    }\r\n\r\n    underline.remove();\r\n}\r\n\r\nfunction flashWorldInfoError() {\r\n    const underline = $('#ai_wbr_fx_layer .ai-wbr-book-underline');\r\n    if (!underline.length) {\r\n        return;\r\n    }\r\n\r\n    underline.removeClass('ai-wbr-book-underline-error');\r\n    void underline[0].offsetWidth;\r\n    underline.addClass('ai-wbr-book-underline-error');\r\n    setTimeout(() => {\r\n        underline.removeClass('ai-wbr-book-underline-error');\r\n    }, 980);\r\n}\r\n\r\nasync function requestMemoryExtraction(context, prompt, systemPrompt, apiMeta = {}) {\n    const meta = {\n        type: 'memory',\n        label: '记忆整理',\n        roundLabel: '记忆 API 调用',\n        ...apiMeta,\n    };\n    if (settings.routerUseSeparateModel && settings.routerApiUrl && settings.routerApiKey && settings.routerModel) {\n        return sendSeparateMemoryRequest(context, prompt, {\n            systemPrompt,\n            maxTokens: getMemoryRequestMaxTokens(),\n            apiMeta: meta,\n        });\n    }\n\n    const apiItem = recordApiCallStart(meta.type, meta.label, meta);\n    try {\n        const raw = await context.generateRaw({\n            prompt,\n            systemPrompt,\n            responseLength: getMemoryRequestMaxTokens(),\n            trimNames: false,\n        });\n        recordApiCallEnd(apiItem);\n        return raw;\n    } catch (error) {\n        recordApiCallEnd(apiItem, error);\n        throw error;\n    }\n}\n\r\nfunction playStatusBurst(symbol, variant = 'retry') {\r\n    const icon = getWorldInfoIcon();\r\n    if (!icon.length) {\r\n        return;\r\n    }\r\n\r\n    const layer = ensureFxLayer();\r\n    const rect = icon[0].getBoundingClientRect();\r\n    const burst = $('<div class=\"ai-wbr-status-burst\"></div>')\r\n        .addClass(`ai-wbr-status-burst-${variant}`)\r\n        .text(symbol);\r\n\r\n    burst.css({\r\n        left: `${rect.left + (rect.width / 2)}px`,\r\n        top: `${rect.top + (rect.height / 2)}px`,\r\n    });\r\n\r\n    layer.append(burst);\r\n    setTimeout(() => burst.remove(), 1050);\r\n}\r\nfunction getEntryBurstLabel(entry) {\r\n    const comment = String(entry?.comment || '')\r\n        .replace(/^=+\\s*/u, '')\r\n        .replace(/\\s*=+$/u, '')\r\n        .trim();\r\n    if (comment) {\r\n        return truncateText(comment, 22);\r\n    }\r\n\r\n    const key = entry?.matchedKeys?.[0] || entry?.keys?.primary?.[0] || entry?.keys?.all?.[0] || String(entry?.uid || '');\r\n    return truncateText(key, 22);\r\n}\r\n\r\nfunction getMemoryBurstLabel(entry) {\r\n    const title = String(entry?.title || '').trim();\r\n    if (title) {\r\n        return truncateText(title, 22);\r\n    }\r\n    const content = String(entry?.content || '').trim();\r\n    if (content) {\r\n        return truncateText(content.split(/[。.!?\\n]/u)[0], 22);\r\n    }\r\n    return truncateText(String(entry?.id || '记忆更新'), 22);\r\n}\r\n\r\nfunction playEntryBurst(entries, {\r\n    variant = 'router',\r\n    labelGetter = getEntryBurstLabel,\r\n} = {}) {\r\n    const icon = getWorldInfoIcon();\r\n    if (!icon.length || !entries.length) {\r\n        return;\r\n    }\r\n\r\n    clearEntryBurst();\r\n    const layer = ensureFxLayer();\r\n    const rect = icon[0].getBoundingClientRect();\r\n    const originX = rect.left + (rect.width / 2);\r\n    const originY = rect.top + (rect.height / 2);\r\n    const burstEntries = entries.slice(0, MAX_BURST_ITEMS);\r\n\r\n    burstEntries.forEach((entry, index) => {\r\n        const chip = $('<div class=\"ai-wbr-entry-burst\"></div>')\r\n            .addClass(variant === 'memory' ? 'ai-wbr-entry-burst-memory' : '')\r\n            .text(labelGetter(entry));\r\n        const direction = index % 2 === 0 ? -1 : 1;\r\n        const spreadX = direction * (44 + (index * 18));\r\n        const spreadY = 54 + (index * 12);\r\n        const tilt = direction * (10 + (index * 4));\r\n        chip.css({\r\n            left: `${originX}px`,\r\n            top: `${originY}px`,\r\n            '--burst-x': `${spreadX}px`,\r\n            '--burst-y': `${spreadY}px`,\r\n            '--burst-tilt': `${tilt}deg`,\r\n            '--burst-delay': `${index * 70}ms`,\r\n        });\r\n        layer.append(chip);\r\n    });\r\n\r\n    burstCleanupTimer = setTimeout(() => {\r\n        clearEntryBurst();\r\n    }, 1900);\r\n}\r\n\r\nfunction playSelectedEntriesBurst(entries) {\r\n    playEntryBurst(entries, {\r\n        variant: 'router',\r\n        labelGetter: getEntryBurstLabel,\r\n    });\r\n}\r\n\r\nfunction extractActualUserInput(value) {\r\n    const text = String(value ?? '');\r\n    const match = text.match(/<\\u672c\\u8f6e\\u7528\\u6237\\u8f93\\u5165>\\s*([\\s\\S]*?)\\s*<\\/\\u672c\\u8f6e\\u7528\\u6237\\u8f93\\u5165>/i);\r\n    return (match ? match[1] : text).trim();\r\n}\r\n\r\nfunction extractQueryTerms(...texts) {\r\n    const terms = [];\r\n\r\n    for (const rawText of texts) {\r\n        const text = String(rawText ?? '').trim();\r\n        if (!text) {\r\n            continue;\r\n        }\r\n\r\n        const latinTokens = text\r\n            .replace(/[^\\p{L}\\p{N}\\s_-]+/gu, ' ')\r\n            .split(/\\s+/)\r\n            .map(token => token.trim())\r\n            .filter(token => token.length >= 3);\r\n        terms.push(...latinTokens);\r\n\r\n        const hanChunks = text.match(/[\\p{Script=Han}]{2,}/gu) || [];\r\n        for (const chunk of hanChunks) {\r\n            if (chunk.length <= 4) {\r\n                terms.push(chunk);\r\n                continue;\r\n            }\r\n\r\n            for (let size = 4; size >= 2; size -= 1) {\r\n                for (let index = 0; index <= chunk.length - size; index += 1) {\r\n                    terms.push(chunk.slice(index, index + size));\r\n                }\r\n            }\r\n        }\r\n    }\r\n\r\n    return uniqueStrings(terms\r\n        .map(normalizeText)\r\n        .filter(term => term.length >= 2)\r\n        .filter(term => !COMMON_QUERY_TERMS.has(term)))\r\n        .slice(0, MAX_RECALL_TERMS);\r\n}\r\n\r\nfunction getBlockedTitleRule(entry) {\r\n    const comment = String(entry?.comment || '').trim();\r\n    if (!comment) {\r\n        return '';\r\n    }\r\n\r\n    return parseBlockRules(settings.titleBlocklist).find(rule => matchesBlockRule(comment, rule)) || '';\r\n}\r\n\r\nfunction getWorldSelectionState(worldName) {\r\n    const name = String(worldName || '').trim();\r\n    if (!name) {\r\n        return true;\r\n    }\r\n    const state = settings.worldSelectionStates?.[name];\r\n    return state !== false;\r\n}\r\n\r\nfunction setWorldSelectionState(worldName, enabled) {\r\n    const name = String(worldName || '').trim();\r\n    if (!name) {\r\n        return;\r\n    }\r\n    const nextStates = {\r\n        ...(settings.worldSelectionStates || {}),\r\n        [name]: !!enabled,\r\n    };\r\n    saveSetting('worldSelectionStates', nextStates);\r\n}\r\n\r\nfunction getActiveWorldbookBindings(context = getContext()) {\r\n    const character = context.characters?.[context.characterId];\r\n    const worldBindings = [];\r\n    const seen = new Set();\r\n\r\n    const addWorld = (worldName, source) => {\r\n        const name = String(worldName || '').trim();\r\n        if (!name || seen.has(name)) {\r\n            return;\r\n        }\r\n        seen.add(name);\r\n        worldBindings.push({\r\n            worldName: name,\r\n            source,\r\n        });\r\n    };\r\n\r\n    for (const worldName of selected_world_info || []) {\r\n        addWorld(worldName, '全局');\r\n    }\r\n\r\n    addWorld(context.chatMetadata?.[METADATA_KEY], '聊天绑定');\r\n    addWorld(character?.data?.extensions?.world, '角色绑定');\r\n    addWorld(context.powerUserSettings?.persona_description_lorebook, '人格绑定');\r\n\r\n    try {\r\n        const fileName = context.characterId !== undefined ? getCharaFilename(context.characterId) : '';\r\n        const extraCharLore = world_info.charLore?.find(entry => entry.name === fileName);\r\n        for (const worldName of extraCharLore?.extraBooks || []) {\r\n            addWorld(worldName, '角色额外');\r\n        }\r\n    } catch (error) {\r\n        debugLog('Could not read active worldbook bindings', error);\r\n    }\r\n\r\n    const embeddedBook = character?.data?.character_book;\r\n    if (embeddedBook?.entries?.length) {\r\n        addWorld(embeddedBook.name || character?.name || 'embedded', '角色内嵌');\r\n    }\r\n\r\n    return worldBindings;\r\n}\r\n\r\nfunction getEntryId(entry, fallback) {\r\n    return entry.uid ?? entry.id ?? entry.displayIndex ?? fallback;\r\n}\r\n\r\nfunction getEntryKeys(entry = {}) {\r\n    const primary = Array.isArray(entry.key) ? entry.key : (Array.isArray(entry.keys) ? entry.keys : []);\r\n    const secondary = Array.isArray(entry.keysecondary)\r\n        ? entry.keysecondary\r\n        : (Array.isArray(entry.secondary_keys) ? entry.secondary_keys : []);\r\n    const triggers = Array.isArray(entry.triggers) ? entry.triggers : [];\r\n    return {\r\n        primary: primary.map(String).filter(Boolean),\r\n        secondary: secondary.map(String).filter(Boolean),\r\n        all: [...primary, ...secondary, ...triggers].map(String).filter(Boolean),\r\n    };\r\n}\r\n\r\nfunction getRecentMessages(chat) {\r\n    return chat\r\n        .filter(message => message && !message.is_system && message.mes)\r\n        .slice(-settings.scanMessages)\r\n        .map(message => ({\r\n            name: message.name || '',\r\n            text: message.is_user ? extractActualUserInput(message.mes) : String(message.mes || ''),\r\n            isUser: !!message.is_user,\r\n        }));\r\n}\r\n\r\nfunction getTavernHelperInput(options) {\r\n    if (!options || typeof options !== 'object') {\r\n        return '';\r\n    }\r\n\r\n    const injected = Array.isArray(options.injects)\r\n        ? options.injects.find(entry => entry && typeof entry.content === 'string' && entry.content.trim())?.content\r\n        : '';\r\n\r\n    return String(\r\n        injected\r\n        || options.user_input\r\n        || options.prompt\r\n        || options.message\r\n        || ''\r\n    );\r\n}\r\n\r\nfunction buildCompatRecentMessages(context, options) {\r\n    const chat = Array.isArray(context?.chat) ? context.chat : [];\r\n    const recentMessages = getRecentMessages(chat);\r\n    const input = extractActualUserInput(getTavernHelperInput(options));\r\n\r\n    if (!input) {\r\n        return recentMessages;\r\n    }\r\n\r\n    const last = recentMessages[recentMessages.length - 1];\r\n    if (last?.isUser && last.text.trim() === input) {\r\n        return recentMessages;\r\n    }\r\n\r\n    return [\r\n        ...recentMessages,\r\n        {\r\n            name: context?.name1 || 'User',\r\n            text: input,\r\n            isUser: true,\r\n        },\r\n    ].slice(-settings.scanMessages);\r\n}\r\n\r\nfunction shouldRouteTavernHelperGenerate(options) {\r\n    if (!options || typeof options !== 'object') {\r\n        return false;\r\n    }\r\n\r\n    if (options._ai_wbr_routed) {\r\n        return false;\r\n    }\r\n\r\n    if (options.quiet_prompt || options.quiet || options.automatic_trigger) {\r\n        return false;\r\n    }\r\n\r\n    return !!extractActualUserInput(getTavernHelperInput(options));\r\n}\r\n\r\nfunction getFetchUrl(input) {\r\n    if (typeof input === 'string') {\r\n        return input;\r\n    }\r\n\r\n    if (input instanceof URL) {\r\n        return input.pathname;\r\n    }\r\n\r\n    return String(input?.url || '');\r\n}\r\n\r\nfunction isMainGenerationFetch(input, init) {\r\n    if (isRouterSelectionRequest) {\r\n        return false;\r\n    }\r\n\r\n    const url = getFetchUrl(input);\r\n    if (!FETCH_FALLBACK_ENDPOINTS.some(endpoint => url.includes(endpoint))) {\r\n        return false;\r\n    }\r\n\r\n    const method = String(init?.method || input?.method || 'GET').toUpperCase();\r\n    return method === 'POST';\r\n}\r\n\r\nfunction contentToText(content) {\r\n    if (typeof content === 'string') {\r\n        return content;\r\n    }\r\n\r\n    if (Array.isArray(content)) {\r\n        return content\r\n            .map(part => {\r\n                if (typeof part === 'string') {\r\n                    return part;\r\n                }\r\n\r\n                return part?.text || part?.content || '';\r\n            })\r\n            .filter(Boolean)\r\n            .join('\\n');\r\n    }\r\n\r\n    return content == null ? '' : String(content);\r\n}\r\n\r\nfunction getPayloadBody(init) {\r\n    return typeof init?.body === 'string' ? init.body : '';\r\n}\r\n\r\nfunction buildFetchFallbackMessages(context, payload) {\r\n    if (Array.isArray(payload?.messages) && payload.messages.length) {\r\n        return payload.messages\r\n            .map(message => ({\r\n                name: message.name || message.role || '',\r\n                text: message.role === 'user' ? extractActualUserInput(contentToText(message.content)) : contentToText(message.content),\r\n                isUser: message.role === 'user',\r\n            }))\r\n            .filter(message => message.text)\r\n            .slice(-settings.scanMessages);\r\n    }\r\n\r\n    const chat = Array.isArray(context?.chat) ? getRecentMessages(context.chat) : [];\r\n    if (chat.length) {\r\n        return chat;\r\n    }\r\n\r\n    if (typeof payload?.prompt === 'string' && payload.prompt.trim()) {\r\n        return [{\r\n            name: context?.name1 || 'User',\r\n            text: extractActualUserInput(payload.prompt),\r\n            isUser: true,\r\n        }];\r\n    }\r\n\r\n    return [];\r\n}\r\n\r\nfunction injectIntoGenerationPayload(payload, injection) {\r\n    if (!injection) {\r\n        return false;\r\n    }\r\n\r\n    if (Array.isArray(payload.messages)) {\r\n        payload.messages.unshift({\r\n            role: 'system',\r\n            content: injection,\r\n        });\r\n        return true;\r\n    }\r\n\r\n    if (typeof payload.prompt === 'string') {\r\n        payload.prompt = `${injection}\\n\\n${payload.prompt}`;\r\n        return true;\r\n    }\r\n\r\n    return false;\r\n}\r\n\r\nfunction trimMainGenerationMessages(messages) {\r\n    if (!Array.isArray(messages)) {\r\n        return { changed: false, trimmedCount: 0 };\r\n    }\r\n\r\n    const maxAssistantTurns = clampNumber(settings.mainHistoryAiTurns, 0, 0, 100);\r\n    if (maxAssistantTurns <= 0) {\r\n        return { changed: false, trimmedCount: 0 };\r\n    }\r\n\r\n    const assistantIndexes = [];\r\n    for (let index = 0; index < messages.length; index += 1) {\r\n        if (String(messages[index]?.role || '').toLowerCase() === 'assistant') {\r\n            assistantIndexes.push(index);\r\n        }\r\n    }\r\n\r\n    if (assistantIndexes.length <= maxAssistantTurns) {\r\n        return { changed: false, trimmedCount: 0 };\r\n    }\r\n\r\n    let startIndex = assistantIndexes[assistantIndexes.length - maxAssistantTurns];\r\n    while (startIndex > 0 && String(messages[startIndex - 1]?.role || '').toLowerCase() === 'user') {\r\n        startIndex -= 1;\r\n    }\r\n\r\n    const trimmedMessages = messages.filter((message, index) => {\r\n        const role = String(message?.role || '').toLowerCase();\r\n        if (role !== 'user' && role !== 'assistant') {\r\n            return true;\r\n        }\r\n\r\n        return index >= startIndex;\r\n    });\r\n\r\n    const trimmedCount = messages.length - trimmedMessages.length;\r\n    if (trimmedCount <= 0) {\r\n        return { changed: false, trimmedCount: 0 };\r\n    }\r\n\r\n    messages.splice(0, messages.length, ...trimmedMessages);\r\n    return { changed: true, trimmedCount };\r\n}\r\n\r\nfunction installFetchFallbackHook() {\r\n    if (fetchFallbackInstalled || typeof globalThis.fetch !== 'function') {\r\n        return;\r\n    }\r\n\r\n    const originalFetch = globalThis.fetch.bind(globalThis);\r\n    const wrappedFetch = async function (input, init = undefined) {\r\n        if (!settings.enabled || !isMainGenerationFetch(input, init)) {\r\n            return originalFetch(input, init);\r\n        }\r\n\r\n        const body = getPayloadBody(init);\r\n        if (!body) {\r\n            return originalFetch(input, init);\r\n        }\r\n\r\n        let payload;\r\n        try {\r\n            payload = JSON.parse(body);\r\n        } catch {\r\n            return originalFetch(input, init);\r\n        }\r\n\r\n        const context = getContext();\r\n        const recentMessages = buildFetchFallbackMessages(context, payload);\r\n        const trimResult = trimMainGenerationMessages(payload.messages);\r\n        const shouldSkipRouting = body.includes('[本轮相关世界书]') || Date.now() - lastRouteCompletedAt < 3000;\r\n\r\n        if (shouldSkipRouting) {\r\n            if (trimResult.changed) {\r\n                debugLog('Trimmed main generation history without rerouting', {\r\n                    url: getFetchUrl(input),\r\n                    trimmedCount: trimResult.trimmedCount,\r\n                    remainingMessages: payload.messages?.length ?? 0,\r\n                });\r\n                return originalFetch(input, {\r\n                    ...init,\r\n                    body: JSON.stringify(payload),\r\n                });\r\n            }\r\n\r\n            return originalFetch(input, init);\r\n        }\r\n\r\n        const endRouterBusy = beginRouterBusy();\r\n        clearEntryBurst();\r\n        startWorldInfoAnimation();\r\n\r\n        try {\r\n            if (!recentMessages.length) {\r\n                return originalFetch(input, trimResult.changed ? {\r\n                    ...init,\r\n                    body: JSON.stringify(payload),\r\n                } : init);\r\n            }\r\n\r\n            const result = await routeWorldbookForMessages(context, recentMessages, 'fetch_fallback', {\r\n                type: 'fetch',\r\n                url: getFetchUrl(input),\r\n            });\r\n\r\n            if (result.selected.length && !result.source.includes('fallback')) {\r\n                playSelectedEntriesBurst(result.selected);\r\n            }\r\n\r\n            if (!injectIntoGenerationPayload(payload, result.injection)) {\r\n                return originalFetch(input, init);\r\n            }\r\n\r\n            debugLog('Injected through fetch fallback', {\r\n                url: getFetchUrl(input),\r\n                selected: result.selected.length,\r\n                chars: result.injection.length,\r\n                trimmedCount: trimResult.trimmedCount,\r\n            });\r\n\r\n            return originalFetch(input, {\r\n                ...init,\r\n                body: JSON.stringify(payload),\r\n            });\r\n        } catch (error) {\r\n            debugError(error);\r\n            console.error(`${LOG_PREFIX} Fetch fallback failed`, error);\r\n            return originalFetch(input, init);\r\n        } finally {\r\n            stopWorldInfoAnimation();\r\n            endRouterBusy();\r\n        }\r\n    };\r\n\r\n    Object.defineProperty(wrappedFetch, '__aiWbrFetchWrapped', {\r\n        value: true,\r\n        configurable: false,\r\n        enumerable: false,\r\n        writable: false,\r\n    });\r\n\r\n    globalThis.fetch = wrappedFetch;\r\n    fetchFallbackInstalled = true;\r\n}\r\n\r\nfunction getLastUserMessage(recentMessages) {\r\n    const text = [...recentMessages].reverse().find(message => message.isUser)?.text\r\n        || recentMessages.at(-1)?.text\r\n        || '';\r\n    return extractActualUserInput(text);\r\n}\r\n\r\nfunction summarizeMvuValue(value) {\r\n    if (value === undefined || value === null || value === '') {\r\n        return '';\r\n    }\r\n\r\n    if (typeof value === 'string') {\r\n        return truncateText(value, MAX_MVU_CHARS);\r\n    }\r\n\r\n    try {\r\n        return truncateText(JSON.stringify(value, null, 2), MAX_MVU_CHARS);\r\n    } catch {\r\n        return truncateText(String(value), MAX_MVU_CHARS);\r\n    }\r\n}\r\n\r\nfunction findNestedStatData(value, depth = 0, seen = new WeakSet()) {\r\n    if (!value || typeof value !== 'object' || depth > 4) {\r\n        return null;\r\n    }\r\n\r\n    if (seen.has(value)) {\r\n        return null;\r\n    }\r\n    seen.add(value);\r\n\r\n    if (Object.hasOwn(value, 'stat_data')) {\r\n        return value.stat_data;\r\n    }\r\n\r\n    for (const child of Object.values(value)) {\r\n        const found = findNestedStatData(child, depth + 1, seen);\r\n        if (found !== null && found !== undefined) {\r\n            return found;\r\n        }\r\n    }\r\n\r\n    return null;\r\n}\r\n\r\nfunction readVariableStore(store, keys) {\r\n    if (!store?.get) {\r\n        return '';\r\n    }\r\n\r\n    for (const key of keys) {\r\n        try {\r\n            const value = store.get(key);\r\n            const summary = summarizeMvuValue(value);\r\n            if (summary) {\r\n                return summary;\r\n            }\r\n        } catch {\r\n            // Some variable providers throw for missing keys.\r\n        }\r\n    }\r\n\r\n    return '';\r\n}\r\n\r\nfunction getMvuSummary(context) {\r\n    if (!settings.useMvu) {\r\n        return '';\r\n    }\r\n\r\n    const directMetadata = summarizeMvuValue(context.chatMetadata?.stat_data);\r\n    if (directMetadata) {\r\n        return directMetadata;\r\n    }\r\n\r\n    const nestedMetadata = summarizeMvuValue(findNestedStatData(context.chatMetadata));\r\n    if (nestedMetadata) {\r\n        return nestedMetadata;\r\n    }\r\n\r\n    const keys = ['stat_data', 'mvu_stat_data', 'tavern_helper_stat_data', 'MVU.stat_data'];\r\n    const localVariable = readVariableStore(context.variables?.local, keys);\r\n    if (localVariable) {\r\n        return localVariable;\r\n    }\r\n\r\n    const globalVariable = readVariableStore(context.variables?.global, keys);\r\n    if (globalVariable) {\r\n        return globalVariable;\r\n    }\r\n\r\n    for (const value of [\r\n        globalThis.TavernHelper?.stat_data,\r\n        globalThis.TavernHelper?.statData,\r\n        globalThis.MVU?.stat_data,\r\n        globalThis.MVU?.statData,\r\n        globalThis.stat_data,\r\n    ]) {\r\n        const summary = summarizeMvuValue(value);\r\n        if (summary) {\r\n            return summary;\r\n        }\r\n    }\r\n\r\n    return '';\r\n}\r\n\r\nfunction getDefaultMemoryGraph() {\r\n    return {\r\n        version: 1,\r\n        state: {\r\n            current_location: '',\r\n            current_time: '',\r\n            protagonist_status: '',\r\n            current_objective: '',\r\n            current_phase: '',\r\n            active_topics: [],\r\n            open_questions: [],\r\n            custom_values: {},\r\n        },\r\n        stateDefinitions: [],\r\n        nodes: [],\r\n        links: [],\r\n        lastSummary: '',\r\n        updatedAt: '',\r\n    };\r\n}\r\n\r\nfunction hashString(value) {\r\n    let hash = 0;\r\n    const text = String(value || '');\r\n    for (let i = 0; i < text.length; i += 1) {\r\n        hash = ((hash << 5) - hash) + text.charCodeAt(i);\r\n        hash |= 0;\r\n    }\r\n    return Math.abs(hash).toString(36);\r\n}\r\n\r\nfunction getStableChatScopeParts(context = getContext()) {\r\n    const cleanParts = (values) => {\r\n        const parts = [];\r\n        for (const candidate of values) {\r\n            const value = String(candidate ?? '').trim();\r\n            if (value && !parts.includes(value)) {\r\n                parts.push(value);\r\n            }\r\n        }\r\n        return parts;\r\n    };\r\n\r\n    const charaFile = String(getCharaFilename?.() || '').trim();\r\n    const scopeParts = cleanParts([\r\n        context?.groupId,\r\n        context?.group_id,\r\n        context?.characterId,\r\n        context?.character_id,\r\n        context?.name2,\r\n        context?.character?.avatar,\r\n        context?.character?.name,\r\n        charaFile,\r\n    ]);\r\n\r\n    const stableChatParts = cleanParts([\r\n        context?.chatId,\r\n        context?.chat_id,\r\n        context?.conversationId,\r\n        context?.sessionId,\r\n        context?.chatFileName,\r\n        context?.chat_filename,\r\n        lastKnownChatFileName,\r\n        context?.chatMetadata?.chat_id,\r\n        context?.chatMetadata?.session_id,\r\n        context?.chatMetadata?.filename,\r\n        context?.chatMetadata?.file_name,\r\n        context?.chatMetadata?.main_chat,\r\n        context?.chatMetadata?.mainChat,\r\n        context?.chatMetadata?.chat_name,\r\n        context?.chatMetadata?.name,\r\n        context?.chatName,\r\n        context?.chat_name,\r\n    ]);\r\n\r\n    return { scopeParts, stableChatParts };\r\n}\r\n\r\nfunction getCurrentChatMemoryKey(context = getContext()) {\r\n    const { scopeParts, stableChatParts } = getStableChatScopeParts(context);\r\n    if (stableChatParts.length) {\r\n        return `chat:v2:${[...scopeParts, ...stableChatParts].join('|')}`;\r\n    }\r\n\r\n    if (scopeParts.length) {\r\n        return `chat:v2:scope-only:${scopeParts.join('|')}`;\r\n    }\r\n\r\n    return 'chat:v2:default';\r\n}\r\n\r\nfunction idbRequestToPromise(request) {\r\n    return new Promise((resolve, reject) => {\r\n        request.onsuccess = () => resolve(request.result);\r\n        request.onerror = () => reject(request.error || new Error('IndexedDB request failed'));\r\n    });\r\n}\r\n\r\nfunction getBookshelfDb() {\r\n    if (bookshelfDbPromise) {\r\n        return bookshelfDbPromise;\r\n    }\r\n    if (!globalThis.indexedDB) {\r\n        bookshelfDbPromise = Promise.reject(new Error('Current WebView does not support IndexedDB; vector bookshelf is unavailable.'));\r\n        return bookshelfDbPromise;\r\n    }\r\n\r\n    bookshelfDbPromise = new Promise((resolve, reject) => {\r\n        const request = indexedDB.open(BOOKSHELF_DB_NAME, BOOKSHELF_DB_VERSION);\r\n        request.onupgradeneeded = () => {\r\n            const db = request.result;\r\n            if (!db.objectStoreNames.contains('books')) {\r\n                const books = db.createObjectStore('books', { keyPath: 'id' });\r\n                books.createIndex('updatedAt', 'updatedAt');\r\n            }\r\n            if (!db.objectStoreNames.contains('chunks')) {\r\n                const chunks = db.createObjectStore('chunks', { keyPath: 'id' });\r\n                chunks.createIndex('bookId', 'bookId');\r\n                chunks.createIndex('updatedAt', 'updatedAt');\r\n            }\r\n            if (!db.objectStoreNames.contains('memoryVectors')) {\r\n                const memoryVectors = db.createObjectStore('memoryVectors', { keyPath: 'id' });\r\n                memoryVectors.createIndex('chatKey', 'chatKey');\r\n                memoryVectors.createIndex('nodeId', 'nodeId');\r\n                memoryVectors.createIndex('updatedAt', 'updatedAt');\r\n            }\r\n        };\r\n        request.onsuccess = () => resolve(request.result);\r\n        request.onerror = () => reject(request.error || new Error('IndexedDB open failed'));\r\n    });\r\n    return bookshelfDbPromise;\r\n}\r\n\r\nasync function bookshelfGetAll(storeName) {\r\n    const db = await getBookshelfDb();\r\n    const tx = db.transaction(storeName, 'readonly');\r\n    return await idbRequestToPromise(tx.objectStore(storeName).getAll());\r\n}\r\n\r\nasync function bookshelfGet(storeName, key) {\r\n    const db = await getBookshelfDb();\r\n    const tx = db.transaction(storeName, 'readonly');\r\n    return await idbRequestToPromise(tx.objectStore(storeName).get(key));\r\n}\r\n\r\nasync function bookshelfPut(storeName, record) {\r\n    const db = await getBookshelfDb();\r\n    const tx = db.transaction(storeName, 'readwrite');\r\n    tx.objectStore(storeName).put(record);\r\n    return await new Promise((resolve, reject) => {\r\n        tx.oncomplete = () => resolve(record);\r\n        tx.onerror = () => reject(tx.error || new Error('IndexedDB write failed'));\r\n        tx.onabort = () => reject(tx.error || new Error('IndexedDB write aborted'));\r\n    });\r\n}\r\n\r\nasync function bookshelfPutMany(storeName, records) {\r\n    const db = await getBookshelfDb();\r\n    const tx = db.transaction(storeName, 'readwrite');\r\n    const store = tx.objectStore(storeName);\r\n    for (const record of records) {\r\n        store.put(record);\r\n    }\r\n    return await new Promise((resolve, reject) => {\r\n        tx.oncomplete = () => resolve(records);\r\n        tx.onerror = () => reject(tx.error || new Error('IndexedDB batch write failed'));\r\n        tx.onabort = () => reject(tx.error || new Error('IndexedDB batch write aborted'));\r\n    });\r\n}\r\n\r\nasync function bookshelfDelete(storeName, key) {\r\n    const db = await getBookshelfDb();\r\n    const tx = db.transaction(storeName, 'readwrite');\r\n    tx.objectStore(storeName).delete(key);\r\n    return await new Promise((resolve, reject) => {\r\n        tx.oncomplete = () => resolve(true);\r\n        tx.onerror = () => reject(tx.error || new Error('IndexedDB delete failed'));\r\n        tx.onabort = () => reject(tx.error || new Error('IndexedDB delete aborted'));\r\n    });\r\n}\r\n\r\nasync function bookshelfDeleteBook(bookId) {\r\n    const db = await getBookshelfDb();\r\n    const tx = db.transaction(['books', 'chunks'], 'readwrite');\r\n    tx.objectStore('books').delete(bookId);\r\n    const chunkStore = tx.objectStore('chunks');\r\n    const index = chunkStore.index('bookId');\r\n    const cursorRequest = index.openCursor(IDBKeyRange.only(bookId));\r\n    cursorRequest.onsuccess = () => {\r\n        const cursor = cursorRequest.result;\r\n        if (!cursor) return;\r\n        cursor.delete();\r\n        cursor.continue();\r\n    };\r\n    return await new Promise((resolve, reject) => {\r\n        tx.oncomplete = () => resolve(true);\r\n        tx.onerror = () => reject(tx.error || new Error('IndexedDB delete book failed'));\r\n        tx.onabort = () => reject(tx.error || new Error('IndexedDB delete book aborted'));\r\n    });\r\n}\r\n\r\nasync function bookshelfDeleteMemoryVectorsForChat(chatKey) {\r\n    const db = await getBookshelfDb();\r\n    const tx = db.transaction('memoryVectors', 'readwrite');\r\n    const store = tx.objectStore('memoryVectors');\r\n    const index = store.index('chatKey');\r\n    const cursorRequest = index.openCursor(IDBKeyRange.only(chatKey));\r\n    cursorRequest.onsuccess = () => {\r\n        const cursor = cursorRequest.result;\r\n        if (!cursor) return;\r\n        cursor.delete();\r\n        cursor.continue();\r\n    };\r\n    return await new Promise((resolve, reject) => {\r\n        tx.oncomplete = () => resolve(true);\r\n        tx.onerror = () => reject(tx.error || new Error('IndexedDB delete memory vectors failed'));\r\n        tx.onabort = () => reject(tx.error || new Error('IndexedDB delete memory vectors aborted'));\r\n    });\r\n}\r\n\r\nfunction getBookshelfScope(context = getContext()) {\r\n    const { scopeParts, stableChatParts } = getStableChatScopeParts(context);\r\n    const characterKey = scopeParts.length ? `character:${scopeParts.join('|')}` : 'character:default';\r\n    const chatKey = getCurrentChatMemoryKey(context);\r\n    const characterName = String(context?.name2 || context?.character?.name || context?.characterName || '当前角色').trim();\r\n    const chatName = String(\r\n        context?.chatName\r\n        || context?.chat_name\r\n        || context?.chatMetadata?.chat_name\r\n        || context?.chatMetadata?.name\r\n        || stableChatParts[0]\r\n        || '当前聊天',\r\n    ).trim();\r\n    return { characterKey, chatKey, characterName, chatName };\r\n}\r\n\r\nfunction normalizeBookshelfBindings(book) {\r\n    return Array.isArray(book?.bindings)\r\n        ? book.bindings.filter(item => item && item.type && item.key)\r\n        : [];\r\n}\r\n\r\nfunction isBookshelfBookBound(book, type, key) {\r\n    return normalizeBookshelfBindings(book).some(item => item.type === type && item.key === key);\r\n}\r\n\r\nfunction isBookshelfBookAvailable(book, context = getContext()) {\r\n    if (!book || book.enabled === false) return false;\r\n    if (String(book.status || '') !== 'ready' && Number(book.vectorizedCount || 0) <= 0) return false;\r\n    const scope = getBookshelfScope(context);\r\n    const bindings = normalizeBookshelfBindings(book);\r\n    if (bindings.some(item => item.type === 'chat' && item.key === scope.chatKey)) return true;\r\n    if (bindings.some(item => item.type === 'character' && item.key === scope.characterKey)) return true;\r\n    if (settings.bookshelfAllowGlobal && bindings.some(item => item.type === 'global')) return true;\r\n    return false;\r\n}\r\n\r\nfunction setBookshelfStatus(text) {\r\n    bookshelfLastStatus = String(text || '');\r\n    $('[id=\"ai_wbr_bookshelf_status\"]').text(bookshelfLastStatus || 'No books imported yet. Import TXT files to enable retrieval.');\r\n}\r\nfunction setBookshelfModelStatus(text) {\n    $('[id=\"ai_wbr_bookshelf_model_status\"]').text(text || 'Model not loaded');\n}\n\nfunction clearBookshelfApiModels(reason = 'API 配置已变更，请重新获取模型。') {\n    settings.bookshelfApiModels = [];\n    Object.assign(extension_settings[MODULE_NAME], settings);\n    saveSettingsDebounced();\n    renderBookshelfApiModelOptions();\n    setBookshelfModelStatus(reason);\n    setBookshelfStatus(reason);\n}\n\nfunction renderBookshelfApiModelOptions() {\n    const models = Array.isArray(settings.bookshelfApiModels)\n        ? uniqueStrings(settings.bookshelfApiModels.map(model => String(model || '').trim()).filter(Boolean))\n        : [];\n    const current = String(settings.bookshelfApiModel || '').trim();\n    $('[id=\"ai_wbr_bookshelf_api_model_select\"]').each(function () {\n        const select = $(this);\n        select.empty().append($('<option></option>', {\n            value: '',\n            text: models.length ? '请选择向量模型' : '先点击“获取模型”',\n        }));\n        for (const model of models) {\n            select.append($('<option></option>', {\n                value: model,\n                text: model,\n                selected: model === current,\n            }));\n        }\n        if (current && !models.includes(current)) {\n            select.append($('<option></option>', {\n                value: current,\n                text: `${current}（手动）`,\n                selected: true,\n            }));\n        }\n        select.val(current);\n    });\n    $('[id=\"ai_wbr_bookshelf_api_model\"]').each(function () {\n        const field = $(this);\n        const datalistId = 'ai_wbr_bookshelf_api_model_options';\n        let input = field;\n        if (!field.is('input')) {\n            input = $('<input id=\"ai_wbr_bookshelf_api_model\" class=\"text_pole\" type=\"text\" placeholder=\"例如 BAAI/bge-m3，可手动填写\" />');\n            field.replaceWith(input);\n        }\n        input.attr('list', datalistId).attr('placeholder', models.length ? '选择或手动输入向量模型名' : '例如 BAAI/bge-m3，可手动填写');\n        input.val(current);\n        let datalist = $(`#${datalistId}`);\n        if (!datalist.length) {\n            datalist = $(`<datalist id=\"${datalistId}\"></datalist>`);\n            input.after(datalist);\n        }\n        datalist.empty();\n        for (const model of models) {\n            datalist.append($('<option></option>', { value: model }));\n        }\n    });\n}\n\r\nfunction syncBookshelfProviderVisibility() {\n    const mode = getBookshelfEmbeddingMode();\r\n    const toggleField = (id, visible) => {\r\n        const rawId = id.replace(/^#/, '');\r\n        const field = $(`[id=\"${rawId}\"]`);\r\n        field.toggle(visible);\r\n        $(`label[for=\"${rawId}\"]`).toggle(visible);\r\n    };\r\n    toggleField('#ai_wbr_bookshelf_api_url', mode === 'api');\n    toggleField('#ai_wbr_bookshelf_api_key', mode === 'api');\n    toggleField('#ai_wbr_bookshelf_api_model_select', mode === 'api');\n    toggleField('#ai_wbr_bookshelf_api_model', mode === 'api');\n    $('[id=\"ai_wbr_bookshelf_fetch_models\"]').toggle(mode === 'api');\r\n    toggleField('#ai_wbr_bookshelf_local_model', mode === 'browser-local');\r\n    $('[id=\"ai_wbr_bookshelf_load_local\"]').toggle(mode === 'browser-local');\r\n    setBookshelfModelStatus(mode === 'browser-local'\r\n        ? `Local model: ${settings.bookshelfLocalModelStatus || 'not_loaded'}`\r\n        : (settings.bookshelfApiModel ? `API model: ${settings.bookshelfApiModel}` : 'API model not configured'));\r\n}\r\n\r\nfunction extractBookshelfModelIds(payload) {\n    const values = [];\n    const seenObjects = new WeakSet();\n    const pushModel = (item) => {\n        if (typeof item === 'string') {\n            values.push(item);\n            return;\n        }\n        if (!item || typeof item !== 'object') {\n            return;\n        }\n        values.push(item.id, item.name, item.model, item.model_name, item.modelName);\n    };\n    const visit = (item, depth = 0) => {\n        if (depth > 5 || item == null) return;\n        if (typeof item === 'string') {\n            values.push(item);\n            return;\n        }\n        if (Array.isArray(item)) {\n            item.forEach(entry => visit(entry, depth + 1));\n            return;\n        }\n        if (typeof item !== 'object') return;\n        if (seenObjects.has(item)) return;\n        seenObjects.add(item);\n        pushModel(item);\n        for (const key of ['data', 'models', 'model_list', 'available_models', 'availableModels', 'modelList', 'model_names', 'modelNames']) {\n            if (item[key] !== undefined) visit(item[key], depth + 1);\n        }\n        if (item.models && typeof item.models === 'object' && !Array.isArray(item.models)) {\n            Object.keys(item.models).forEach(model => values.push(model));\n        }\n    };\n\n    visit(payload);\n    if (Array.isArray(payload)) {\n        payload.forEach(pushModel);\n    }\n    if (Array.isArray(payload?.data)) {\n        payload.data.forEach(pushModel);\n    }\n    if (Array.isArray(payload?.models)) {\n        payload.models.forEach(pushModel);\n    }\n    if (Array.isArray(payload?.model_list)) {\n        payload.model_list.forEach(pushModel);\n    }\n    if (Array.isArray(payload?.available_models)) {\n        payload.available_models.forEach(pushModel);\n    }\n    if (payload?.models && typeof payload.models === 'object' && !Array.isArray(payload.models)) {\n        Object.keys(payload.models).forEach(model => values.push(model));\n    }\n\n    return uniqueStrings(values.map(model => String(model || '').trim()).filter(Boolean));\n}\n\nasync function fetchBookshelfModelsViaTavernBackend() {\n    const context = getContext();\n    const apiUrl = normalizeUrl(settings.bookshelfApiUrl || '');\n    const apiKey = String(settings.bookshelfApiKey || '').trim();\n    if (!apiUrl) {\n        throw new Error('请先填写书架向量模型的 API 地址。');\n    }\n    if (!apiKey) {\n        throw new Error('请先填写书架向量模型的 API Key。');\n    }\n\n    const response = await fetch('/api/backends/chat-completions/status', {\n        method: 'POST',\n        headers: typeof context.getRequestHeaders === 'function'\n            ? context.getRequestHeaders()\n            : { 'Content-Type': 'application/json' },\n        cache: 'no-cache',\n        body: JSON.stringify({\n            chat_completion_source: 'openai',\n            reverse_proxy: apiUrl,\n            proxy_password: apiKey,\n        }),\n    });\n    const data = await response.json().catch(() => ({}));\n    const models = extractBookshelfModelIds(data);\n    if (!response.ok || !models.length) {\n        throw new Error(data?.error?.message || data?.message || '没有拿到可用模型');\n    }\n    return models;\n}\n\nasync function fetchBookshelfApiModels() {\n    setBookshelfModelStatus('正在获取模型...');\n    setBookshelfStatus('正在通过酒馆后端获取书架向量模型列表...');\n    const models = await fetchBookshelfModelsViaTavernBackend();\n    if (!models.length) {\n        throw new Error('没有拿到可用模型');\n    }\n\n    settings.bookshelfApiModels = uniqueStrings(models);\n    const manualModel = String(settings.bookshelfApiModel || '').trim();\n    if (!manualModel) {\n        settings.bookshelfApiModel = settings.bookshelfApiModels[0] || '';\n    } else {\n        settings.bookshelfApiModel = manualModel;\n    }\n    Object.assign(extension_settings[MODULE_NAME], settings);\n    saveSettingsDebounced();\n    renderBookshelfApiModelOptions();\n    $('#ai_wbr_bookshelf_api_model').val(settings.bookshelfApiModel || '');\n    const modelLabel = settings.bookshelfApiModel\n        ? (settings.bookshelfApiModels.includes(settings.bookshelfApiModel) ? settings.bookshelfApiModel : `${settings.bookshelfApiModel}（手动）`)\n        : '未选择';\n    setBookshelfModelStatus(`已获取 ${settings.bookshelfApiModels.length} 个模型：${modelLabel}`);\n    setBookshelfStatus(`已获取 ${settings.bookshelfApiModels.length} 个向量模型，可从列表选择，也可继续使用手动填写的模型。`);\n    toastr?.success?.(`已获取 ${settings.bookshelfApiModels.length} 个模型`, 'AI Worldbook Router');\n}\n\r\nfunction readBookshelfTextFile(file) {\r\n    return new Promise((resolve, reject) => {\r\n        const reader = new FileReader();\r\n        reader.onload = () => resolve(String(reader.result || ''));\r\n        reader.onerror = () => reject(reader.error || new Error('TXT 读取失败'));\r\n        reader.readAsText(file);\r\n    });\r\n}\r\n\r\nfunction detectBookshelfChunkTitle(text, fallback = '') {\r\n    const firstLine = String(text || '').split('\\\\n').map(i => i.trim()).find(Boolean) || '';\r\n    if (/^(chapter|section|part|#{1,6}\\\\s+|[0-9]+[.)?\\\\s])/.test(firstLine.toLowerCase())) {\r\n        return truncateText(firstLine.replace(/^#{1,6}\\\\s+/, ''), 60);\r\n    }\r\n    return truncateText(fallback || firstLine || 'Untitled chunk', 60);\r\n}\r\nfunction splitTextIntoBookshelfChunks(text, options = {}) {\r\n    const chunkSize = clampNumber(options.chunkSize, 700, 200, 2000);\r\n    const overlap = clampNumber(options.overlap, 100, 0, Math.floor(chunkSize / 2));\r\n    const normalized = String(text || '').replace(/\\r\\n/g, '\\n').replace(/\\r/g, '\\n').trim();\r\n    if (!normalized) return [];\r\n\r\n    const paragraphs = normalized\r\n        .split(/\\n{2,}/)\r\n        .map(item => item.replace(/\\n+/g, '\\n').trim())\r\n        .filter(Boolean);\r\n    const chunks = [];\r\n    let current = '';\r\n    let currentTitle = '';\r\n\r\n    const pushCurrent = () => {\r\n        const content = current.trim();\r\n        if (!content) return;\r\n        chunks.push({\r\n            title: currentTitle || detectBookshelfChunkTitle(content, `片段 ${chunks.length + 1}`),\r\n            text: content,\r\n        });\r\n        current = '';\r\n    };\r\n\r\n    const pushLongText = (value, title) => {\r\n        const clean = String(value || '').trim();\r\n        if (!clean) return;\r\n        let start = 0;\r\n        while (start < clean.length && chunks.length < BOOKSHELF_MAX_IMPORT_CHUNKS) {\r\n            const part = clean.slice(start, start + chunkSize).trim();\r\n            if (part) {\r\n                chunks.push({ title: title || `片段 ${chunks.length + 1}`, text: part });\r\n            }\r\n            if (start + chunkSize >= clean.length) break;\r\n            start += Math.max(1, chunkSize - overlap);\r\n        }\r\n    };\r\n\r\n    for (const paragraph of paragraphs) {\r\n        if (chunks.length >= BOOKSHELF_MAX_IMPORT_CHUNKS) break;\r\n        const title = detectBookshelfChunkTitle(paragraph, '');\r\n        if (title && paragraph.length < 120) {\r\n            pushCurrent();\r\n            currentTitle = title;\r\n            continue;\r\n        }\r\n        if (paragraph.length > chunkSize * 1.5) {\r\n            pushCurrent();\r\n            pushLongText(paragraph, currentTitle || title);\r\n            continue;\r\n        }\r\n        if ((current + '\\n\\n' + paragraph).length > chunkSize) {\r\n            pushCurrent();\r\n        }\r\n        current = current ? `${current}\\n\\n${paragraph}` : paragraph;\r\n    }\r\n    pushCurrent();\r\n    return chunks.slice(0, BOOKSHELF_MAX_IMPORT_CHUNKS);\r\n}\r\n\r\nfunction getMemoryBookTypeConfig(type) {\r\n    const normalized = String(type || 'other').toLowerCase();\r\n    if (['character', 'person', 'npc', 'role'].includes(normalized)) {\r\n        return { key: 'character', type: 'character', title: '自动记忆书：人物档案' };\r\n    }\r\n    if (['event', 'plot', 'quest', 'scene', 'timeline'].includes(normalized)) {\r\n        return { key: 'plot', type: 'plot', title: '自动记忆书：剧情时间线' };\r\n    }\r\n    if (['rule', 'concept', 'world', 'faction', 'location', 'place', 'item'].includes(normalized)) {\r\n        return { key: 'world', type: normalized === 'rule' ? 'rule' : 'world', title: '自动记忆书：世界设定' };\r\n    }\r\n    return { key: 'memory', type: 'memory', title: '自动记忆书：综合记忆' };\r\n}\r\n\r\nfunction formatMemoryBookNodeChunk(node, graph = getMemoryGraph()) {\r\n    const relatedLinks = (Array.isArray(graph?.links) ? graph.links : [])\r\n        .filter(link => String(link?.source) === String(node?.id) || String(link?.target) === String(node?.id))\r\n        .slice(0, 6)\r\n        .map(link => {\r\n            const otherId = String(link.source) === String(node.id) ? link.target : link.source;\r\n            const other = (Array.isArray(graph?.nodes) ? graph.nodes : []).find(item => String(item.id) === String(otherId));\r\n            return `${link.type || 'RELATED'} -> ${other?.title || otherId}`;\r\n        });\r\n    const parts = [];\r\n    const push = (label, value) => {\r\n        const text = String(value || '').trim();\r\n        if (text) parts.push(`${label}：${text}`);\r\n    };\r\n    push('标题', node?.title || node?.id);\r\n    push('类型', node?.type);\r\n    push('摘要', node?.summary);\r\n    push('内容', node?.content);\r\n    push('地点', node?.location);\r\n    push('时间', node?.timeSpan);\r\n    if (Array.isArray(node?.tags) && node.tags.length) push('标签', node.tags.join('、'));\r\n    if (Array.isArray(node?.keys) && node.keys.length) push('关键词', node.keys.join('、'));\r\n    if (relatedLinks.length) push('关联', relatedLinks.join('；'));\r\n    return parts.join('\\n').trim();\r\n}\r\n\r\nfunction buildMemoryGraphBookshelfDefinitions(graph = getMemoryGraph(), context = getContext()) {\r\n    const nodes = (Array.isArray(graph?.nodes) ? graph.nodes : [])\r\n        .filter(node => node?.id && formatMemoryBookNodeChunk(node, graph));\r\n    const scope = getBookshelfScope(context);\r\n    const chapters = new Map([\r\n        ['prologue', { title: '序章：最近摘要', chunks: [] }],\r\n        ['character', { title: '第一章：人物档案', chunks: [] }],\r\n        ['plot', { title: '第二章：剧情时间线', chunks: [] }],\r\n        ['world', { title: '第三章：世界设定', chunks: [] }],\r\n        ['relations', { title: '第四章：关系网络', chunks: [] }],\r\n        ['state', { title: '第五章：当前状态', chunks: [] }],\r\n        ['memory', { title: '附录：综合记忆', chunks: [] }],\r\n    ]);\r\n\r\n    const pushChapterChunk = (chapterKey, title, text, extra = {}) => {\r\n        const chapter = chapters.get(chapterKey) || chapters.get('memory');\r\n        const clean = String(text || '').trim();\r\n        if (!clean) return;\r\n        chapter.chunks.push({\r\n            title: truncateText(title || `${chapter.title} ${chapter.chunks.length + 1}`, 70),\r\n            text: clean,\r\n            chapterKey,\r\n            chapterTitle: chapter.title,\r\n            ...extra,\r\n        });\r\n    };\r\n\r\n    if (graph?.lastSummary) {\r\n        pushChapterChunk('prologue', '最近剧情摘要', `最近摘要：${truncateText(graph.lastSummary, 1200)}`, { sourceType: 'summary' });\r\n    }\r\n\r\n    for (const node of nodes) {\r\n        const config = getMemoryBookTypeConfig(node.type);\r\n        pushChapterChunk(config.key, node.title || node.id, formatMemoryBookNodeChunk(node, graph), {\r\n            sourceNodeId: String(node.id || ''),\r\n            sourceType: String(node.type || ''),\r\n        });\r\n    }\r\n\r\n    const links = Array.isArray(graph?.links) ? graph.links : [];\r\n    if (links.length) {\r\n        const nodeById = new Map(nodes.map(node => [String(node.id), node]));\r\n        const relationLines = links.slice(-80).map(link => {\r\n            const source = nodeById.get(String(link.source));\r\n            const target = nodeById.get(String(link.target));\r\n            return `${source?.title || link.source} --${link.type || 'RELATED'}--> ${target?.title || link.target}`;\r\n        }).filter(Boolean);\r\n        if (relationLines.length) {\r\n            const relationText = relationLines.join('\\n');\r\n            splitTextIntoBookshelfChunks(relationText, { chunkSize: 520, overlap: 60 })\r\n                .forEach((chunk, index) => {\r\n                    pushChapterChunk('relations', chunk.title || `关系摘要 ${index + 1}`, chunk.text, { sourceType: 'relations' });\r\n                });\r\n        }\r\n    }\r\n\r\n    const stateParts = [];\r\n    const stateValues = graph?.state?.custom_values || graph?.custom_values || {};\r\n    for (const [key, value] of Object.entries(stateValues || {})) {\r\n        const text = String(value ?? '').trim();\r\n        if (text) stateParts.push(`${key}：${text}`);\r\n    }\r\n    if (stateParts.length) {\r\n        splitTextIntoBookshelfChunks(stateParts.join('\\n'), { chunkSize: 520, overlap: 60 })\r\n            .forEach((chunk, index) => {\r\n                pushChapterChunk('state', chunk.title || `状态摘要 ${index + 1}`, chunk.text, { sourceType: 'state' });\r\n            });\r\n    }\r\n\r\n    const chunks = [];\r\n    for (const [chapterKey, chapter] of chapters.entries()) {\r\n        chapter.chunks.slice(0, 80).forEach((chunk, index) => {\r\n            chunks.push({\r\n                ...chunk,\r\n                chapterKey,\r\n                chapterTitle: chapter.title,\r\n                title: `${chapter.title} · ${chunk.title || `片段 ${index + 1}`}`,\r\n            });\r\n        });\r\n    }\r\n    if (!chunks.length) return [];\r\n    return [{\r\n        key: 'chat_novel',\r\n        type: 'memory',\r\n        title: `记忆书：${scope.chatName || '当前聊天'}`,\r\n        chapters: Array.from(chapters.values())\r\n            .map(chapter => ({ title: chapter.title, count: chapter.chunks.length }))\r\n            .filter(chapter => chapter.count),\r\n        chunks: chunks.slice(0, 240),\r\n    }];\r\n}\r\n\r\nasync function syncMemoryGraphBookshelfBooks(graph = getMemoryGraph(), context = getContext(), options = {}) {\r\n    const scope = getBookshelfScope(context);\r\n    const chatKey = getCurrentChatMemoryKey(context);\r\n    const definitions = buildMemoryGraphBookshelfDefinitions(graph, context);\r\n    const [existingBooks, existingChunks] = await Promise.all([\r\n        bookshelfGetAll('books').catch(() => []),\r\n        bookshelfGetAll('chunks').catch(() => []),\r\n    ]);\r\n    const staleBooks = (existingBooks || []).filter(book => (\r\n        book?.autoGenerated\r\n        && book?.autoSource === 'memory-graph'\r\n        && book?.autoChatKey === chatKey\r\n    ));\r\n    const reusableChunks = new Map();\r\n    for (const book of staleBooks) {\r\n        for (const chunk of (existingChunks || []).filter(item => item.bookId === book.id)) {\r\n            const key = `${chunk.chapterTitle || ''}|${chunk.title || ''}|${chunk.textHash || hashString(chunk.text || '')}`;\r\n            if (!reusableChunks.has(key)) reusableChunks.set(key, chunk);\r\n        }\r\n    }\r\n    for (const book of staleBooks) {\r\n        await bookshelfDeleteBook(book.id);\r\n    }\r\n\r\n    const now = new Date().toISOString();\r\n    const provider = getBookshelfEmbeddingProviderMeta();\r\n    const createdBooks = [];\r\n    let totalChunks = 0;\r\n    for (const definition of definitions) {\r\n        const bookId = `auto_memory:${hashString(chatKey)}:${definition.key}`;\r\n        const bindings = [\r\n            { type: 'chat', key: scope.chatKey, label: scope.chatName || scope.chatKey, createdAt: now },\r\n        ];\r\n        const book = {\r\n            id: bookId,\r\n            title: definition.title,\r\n            fileName: `${definition.title}.memory`,\r\n            type: definition.type || 'memory',\r\n            enabled: true,\r\n            bindings,\r\n            status: definition.chunks.length ? 'pending_vectorization' : 'empty',\r\n            autoGenerated: true,\r\n            autoSource: 'memory-graph',\r\n            autoChatKey: chatKey,\r\n            autoScope: 'chat',\r\n            chapters: definition.chapters || [],\r\n            sourceSummary: `当前聊天专属记忆书：${definition.chapters?.length || 0} 章，${definition.chunks.length} 个摘要片段`,\r\n            embeddingMode: options.vectorize ? provider.mode : '',\r\n            embeddingModel: options.vectorize ? provider.model : '',\r\n            embeddingDim: 0,\r\n            chunkCount: definition.chunks.length,\r\n            vectorizedCount: 0,\r\n            createdAt: now,\r\n            updatedAt: now,\r\n        };\r\n        const chunks = definition.chunks.map((chunk, index) => {\r\n            const title = chunk.title || `${definition.title} #${index + 1}`;\r\n            const textHash = hashString(chunk.text);\r\n            const previous = reusableChunks.get(`${chunk.chapterTitle || ''}|${title}|${textHash}`);\r\n            const canReuseVector = previous\r\n                && previous.textHash === textHash\r\n                && previous.vectorStatus === 'ready'\r\n                && Array.isArray(previous.vector)\r\n                && previous.vector.length;\r\n            return {\r\n                id: `${bookId}:chunk:${index}`,\r\n                bookId,\r\n                index,\r\n                order: index + 1,\r\n                title,\r\n                text: chunk.text,\r\n                textHash,\r\n                chapterKey: chunk.chapterKey || '',\r\n                chapterTitle: chunk.chapterTitle || '',\r\n                sourceNodeId: chunk.sourceNodeId || '',\r\n                sourceType: chunk.sourceType || definition.key,\r\n                enabled: true,\r\n                vector: canReuseVector ? previous.vector : [],\r\n                vectorStatus: canReuseVector ? 'ready' : 'pending',\r\n                embeddingMode: canReuseVector ? previous.embeddingMode || '' : '',\r\n                embeddingModel: canReuseVector ? previous.embeddingModel || '' : '',\r\n                embeddingDim: canReuseVector ? previous.embeddingDim || previous.vector.length || 0 : 0,\r\n                createdAt: previous?.createdAt || now,\r\n                updatedAt: now,\r\n            };\r\n        });\r\n        const readyCount = chunks.filter(chunk => chunk.vectorStatus === 'ready').length;\r\n        book.status = chunks.length && readyCount === chunks.length ? 'ready' : readyCount > 0 ? 'partial_failed' : book.status;\r\n        book.vectorizedCount = readyCount;\r\n        const firstReady = chunks.find(chunk => chunk.vectorStatus === 'ready');\r\n        if (firstReady) {\r\n            book.embeddingMode = firstReady.embeddingMode || book.embeddingMode;\r\n            book.embeddingModel = firstReady.embeddingModel || book.embeddingModel;\r\n            book.embeddingDim = firstReady.embeddingDim || book.embeddingDim;\r\n        }\r\n        await bookshelfPut('books', book);\r\n        if (chunks.length) await bookshelfPutMany('chunks', chunks);\r\n        createdBooks.push(book);\r\n        totalChunks += chunks.length;\r\n    }\r\n\r\n    if (options.vectorize) {\r\n        for (const book of createdBooks) {\r\n            await rebuildBookshelfBookVectors(book.id);\r\n        }\r\n    }\r\n    return {\r\n        books: createdBooks.length,\r\n        chunks: totalChunks,\r\n        deleted: staleBooks.length,\r\n        bookIds: createdBooks.map(book => book.id),\r\n    };\r\n}\r\n\r\nfunction scheduleBookshelfMemoryBookSync(context = getContext(), options = {}) {\r\n    if (!settings.bookshelfAutoMemoryBook && !options.force) return;\r\n    clearTimeout(bookshelfMemoryBookSyncTimer);\r\n    const chatKey = getCurrentChatMemoryKey(context);\r\n    bookshelfMemoryBookSyncTimer = setTimeout(async () => {\r\n        try {\r\n            if (getCurrentChatMemoryKey() !== chatKey) return;\r\n            const graph = getMemoryGraph(context);\r\n            const autoVectorize = !!settings.bookshelfAutoMemoryBookVectorize;\n            const result = await syncMemoryGraphBookshelfBooks(graph, context, { vectorize: autoVectorize });\n            const orphanDeleted = await cleanupOrphanBookshelfMemoryBooks(context).catch(() => 0);\r\n            const deletedCount = Number(result.deleted || 0) + Number(orphanDeleted || 0);\r\n            if (!options.silent) {\r\n                setBookshelfStatus(result.books\n                    ? `记忆书已自动刷新${autoVectorize ? '并向量化' : ''}：${result.books} 本，${result.chunks} 个小节。`\n                    : `当前聊天暂无图谱内容，已清理旧记忆书 ${deletedCount} 本。`);\r\n            }\r\n            if ($('#ai_wbr_bookshelf_panel').length) {\r\n                renderBookshelfPanel();\r\n            }\r\n        } catch (error) {\r\n            console.warn(`${LOG_PREFIX} Auto memory book sync failed`, error);\r\n            if (!options.silent) {\r\n                setBookshelfStatus(`记忆书自动刷新失败：${error?.message || error}`);\r\n            }\r\n        }\r\n    }, clampNumber(options.delayMs, 900, 100, 8000));\r\n}\r\n\r\nasync function cleanupOrphanBookshelfMemoryBooks(context = getContext()) {\r\n    const currentChatKey = getCurrentChatMemoryKey(context);\r\n    const validChatKeys = new Set([currentChatKey]);\r\n    for (const [chatKey, container] of Object.entries(settings.memoryContainersByChat || {})) {\r\n        const graph = container?.graph || {};\r\n        if (graph?.nodes?.length || graph?.links?.length || graph?.lastSummary) {\r\n            validChatKeys.add(chatKey);\r\n        }\r\n    }\r\n    const books = await bookshelfGetAll('books').catch(() => []);\r\n    let deleted = 0;\r\n    for (const book of books || []) {\r\n        if (\r\n            book?.autoGenerated\r\n            && book?.autoSource === 'memory-graph'\r\n            && book?.autoChatKey\r\n            && !validChatKeys.has(book.autoChatKey)\r\n        ) {\r\n            await bookshelfDeleteBook(book.id);\r\n            deleted += 1;\r\n        }\r\n    }\r\n    return deleted;\r\n}\r\n\r\nfunction tokenizeBookshelfText(text) {\r\n    const normalized = normalizeText(text).toLowerCase();\r\n    const terms = extractQueryTerms(normalized).filter(item => item.length >= 2);\r\n    const compact = normalized.replace(/\\s+/g, '');\r\n    const grams = [];\r\n    const maxGrams = Math.min(compact.length - 1, 600);\r\n    for (let i = 0; i < maxGrams; i += 2) {\r\n        const gram = compact.slice(i, i + 2);\r\n        if (gram.trim()) grams.push(gram);\r\n    }\r\n    return uniqueStrings([...terms, ...grams]).slice(0, 900);\r\n}\r\n\r\nfunction getBookshelfEmbeddingMode() {\r\n    const mode = String(settings.bookshelfEmbeddingMode || 'api').trim();\r\n    return mode === 'browser-local' ? 'browser-local' : 'api';\r\n}\r\n\r\nfunction getBookshelfEmbeddingProviderMeta() {\r\n    const mode = getBookshelfEmbeddingMode();\r\n    if (mode === 'browser-local') {\r\n        return {\r\n            mode,\r\n            model: String(settings.bookshelfLocalModelId || defaultSettings.bookshelfLocalModelId).trim(),\r\n            label: 'Browser local embedding model',\r\n        };\r\n    }\r\n    return {\r\n        mode,\r\n        model: String(settings.bookshelfApiModel || '').trim(),\r\n        label: 'API embedding model',\r\n    };\r\n}\r\n\r\nfunction normalizeBookshelfVector(vector) {\r\n    const values = Array.from(vector || []).map(value => Number(value)).filter(value => Number.isFinite(value));\r\n    if (!values.length) {\r\n        throw new Error('Embedding model returned an empty vector.');\r\n    }\r\n    const norm = Math.sqrt(values.reduce((sum, value) => sum + (value * value), 0)) || 1;\r\n    return values.map(value => Number((value / norm).toFixed(8)));\r\n}\r\n\r\nfunction parseBookshelfEmbeddingResponse(json) {\r\n    const first = Array.isArray(json?.data) ? json.data[0] : null;\r\n    const vector = first?.embedding || json?.embedding || json?.vector;\r\n    return normalizeBookshelfVector(vector);\r\n}\r\n\r\nfunction getBookshelfEmbeddingApiUrl() {\r\n    const base = normalizeUrl(settings.bookshelfApiUrl || '');\r\n    if (!base) {\r\n        throw new Error('Please enter the bookshelf API URL first.');\r\n    }\r\n    if (/\\/embeddings$/i.test(base)) return base;\r\n    if (/\\/v1$/i.test(base)) return `${base}/embeddings`;\r\n    return `${base.replace(/\\/$/, '')}/v1/embeddings`;\r\n}\r\n\r\nasync function embedBookshelfTextByApi(text) {\n    const model = String(settings.bookshelfApiModel || '').trim();\n    if (!model) {\n        throw new Error('Please enter the bookshelf API URL first.');\n    }\n    const apiUrl = getBookshelfEmbeddingApiUrl();\n    const apiKey = String(settings.bookshelfApiKey || '').trim();\r\n    const headers = { 'Content-Type': 'application/json' };\r\n    if (apiKey) {\n        headers.Authorization = `Bearer ${apiKey}`;\n    }\n    const apiItem = recordApiCallStart('embedding', '书架 / 记忆向量', { roundLabel: '向量 API 调用' });\n    startRuntimeActivity('embedding', '向量化中', '正在调用向量模型生成可召回索引。');\n    try {\n        const response = await fetch(apiUrl, {\n            method: 'POST',\n            headers,\n            body: JSON.stringify({ model, input: String(text || '') }),\n        });\n        if (!response.ok) {\n            const body = await response.text().catch(() => '');\n            throw new Error(body || 'Please enter the bookshelf API URL first.');\n        }\n        const parsed = parseBookshelfEmbeddingResponse(await response.json());\n        recordApiCallEnd(apiItem);\n        finishRuntimeActivityStage('embedding', `向量完成 · ${parsed.length || 0} 维`);\n        return parsed;\n    } catch (error) {\n        recordApiCallEnd(apiItem, error);\n        failRuntimeActivity('embedding', error);\n        throw error;\n    }\n}\n\r\nasync function getBookshelfLocalEmbeddingPipeline() {\r\n    if (!bookshelfLocalPipelinePromise) {\r\n        saveSetting('bookshelfLocalModelStatus', 'loading');\r\n        bookshelfLocalPipelinePromise = import(`${BOOKSHELF_LOCAL_TRANSFORMERS_CDN}/dist/transformers.min.js`)\r\n            .then(async ({ pipeline, env }) => {\r\n                if (env?.allowLocalModels !== undefined) env.allowLocalModels = false;\r\n                if (env?.useBrowserCache !== undefined) env.useBrowserCache = true;\r\n                const modelId = String(settings.bookshelfLocalModelId || defaultSettings.bookshelfLocalModelId).trim();\r\n                if (!modelId) {\r\n                    throw new Error('Please enter the local embedding model ID first.');\r\n                }\r\n                const pipe = await pipeline('feature-extraction', modelId, { quantized: true });\r\n                saveSetting('bookshelfLocalModelStatus', 'ready');\r\n                return pipe;\r\n            })\r\n            .catch((error) => {\r\n                bookshelfLocalPipelinePromise = null;\r\n                saveSetting('bookshelfLocalModelStatus', 'failed');\r\n                throw new Error(`浏览器本地模型加载失败：${error?.message || error}`);\r\n            });\r\n    }\r\n    return bookshelfLocalPipelinePromise;\r\n}\r\n\r\nasync function embedBookshelfTextByBrowserLocal(text) {\r\n    const pipe = await getBookshelfLocalEmbeddingPipeline();\r\n    const output = await pipe(String(text || ''), { pooling: 'mean', normalize: true });\r\n    const values = output?.data || output?.tolist?.()?.[0] || output?.[0];\r\n    return normalizeBookshelfVector(values);\r\n}\r\n\r\nasync function embedBookshelfText(text) {\r\n    const mode = getBookshelfEmbeddingMode();\r\n    if (mode === 'browser-local') {\r\n        return await embedBookshelfTextByBrowserLocal(text);\r\n    }\r\n    return await embedBookshelfTextByApi(text);\r\n}\r\n\r\nfunction cosineSimilarity(a, b) {\r\n    if (!Array.isArray(a) || !Array.isArray(b) || !a.length || !b.length) return 0;\r\n    const length = Math.min(a.length, b.length);\r\n    let dot = 0;\r\n    let an = 0;\r\n    let bn = 0;\r\n    for (let i = 0; i < length; i += 1) {\r\n        const av = Number(a[i]) || 0;\r\n        const bv = Number(b[i]) || 0;\r\n        dot += av * bv;\r\n        an += av * av;\r\n        bn += bv * bv;\r\n    }\r\n    return an && bn ? dot / (Math.sqrt(an) * Math.sqrt(bn)) : 0;\r\n}\r\n\r\nfunction calcBookshelfKeywordScore(text, queryTerms) {\r\n    const body = normalizeText(text).toLowerCase();\r\n    const terms = uniqueStrings(queryTerms || []).filter(item => item.length >= 2);\r\n    if (!terms.length) return 0;\r\n    let hits = 0;\r\n    for (const term of terms) {\r\n        if (body.includes(String(term).toLowerCase())) hits += 1;\r\n    }\r\n    return hits / terms.length;\r\n}\r\n\r\nfunction buildBookshelfRecallQuery(recentMessages = [], mvuSummary = '') {\r\n    const parts = [];\r\n    for (const message of (recentMessages || []).slice(-6)) {\r\n        const text = contentToText(message?.content ?? message?.mes ?? message?.message ?? message?.text ?? message);\r\n        if (text) parts.push(text);\r\n    }\r\n    if (mvuSummary) parts.push(mvuSummary);\r\n    return parts.join('\\n').trim();\r\n}\r\n\r\nfunction tagRecallCandidate(item, sourceType) {\r\n    if (!item || typeof item !== 'object') return item;\r\n    const title = item.comment\r\n        || item.title\r\n        || item.book?.title\r\n        || item.book?.fileName\r\n        || item.keys?.primary?.[0]\r\n        || item.uid\r\n        || item.id\r\n        || '';\r\n    const primaryKeys = uniqueStrings([\r\n        ...(Array.isArray(item.keys?.primary) ? item.keys.primary : []),\r\n        title,\r\n        item.book?.title,\r\n        item.book?.fileName,\r\n    ].filter(Boolean));\r\n    const secondaryKeys = uniqueStrings([\r\n        ...(Array.isArray(item.keys?.secondary) ? item.keys.secondary : []),\r\n        sourceType,\r\n        item.source,\r\n        item.memoryType,\r\n        item.title,\r\n    ].filter(Boolean));\r\n    const allKeys = uniqueStrings([\r\n        ...(Array.isArray(item.keys?.all) ? item.keys.all : []),\r\n        ...primaryKeys,\r\n        ...secondaryKeys,\r\n    ]);\r\n    const score = Number(item.score || 0);\r\n    const semanticScore = Number(item.semanticScore || 0);\r\n    const keywordScore = Number(item.keywordScore || 0);\r\n    const matchedKeys = Array.isArray(item.matchedKeys) ? item.matchedKeys : [];\r\n    const reason = item.reason\r\n        || (sourceType === 'memory-vector' ? `图谱向量命中：语义 ${semanticScore.toFixed(2)}，关键词 ${keywordScore.toFixed(2)}` : '')\r\n        || (sourceType === 'bookshelf' ? `书架向量命中：语义 ${semanticScore.toFixed(2)}，关键词 ${keywordScore.toFixed(2)}` : '')\r\n        || (matchedKeys.length ? `关键词命中：${matchedKeys.join(', ')}` : '')\r\n        || `候选分数 ${score.toFixed(2)}`;\r\n    return {\r\n        ...item,\r\n        sourceType,\r\n        recallId: String(item.routerId || item.uid || item.id || title || ''),\r\n        recallTitle: String(title || ''),\r\n        recallScore: score,\r\n        recallReason: reason,\r\n        selectedBy: item.selectedBy || '',\r\n        keys: {\r\n            ...(item.keys || {}),\r\n            primary: primaryKeys,\r\n            secondary: secondaryKeys,\r\n            all: allKeys,\r\n        },\r\n    };\r\n}\r\n\r\nfunction tagRecallCandidates(items = [], sourceType) {\r\n    return (Array.isArray(items) ? items : []).map(item => tagRecallCandidate(item, sourceType));\r\n}\r\n\r\nfunction mergeMemoryVectorCandidates(keywordCandidates = [], vectorCandidates = []) {\r\n    const merged = new Map();\r\n    for (const item of [...keywordCandidates, ...vectorCandidates]) {\r\n        const key = String(item?.uid || item?.routerId || item?.comment || '');\r\n        if (!key) continue;\r\n        const previous = merged.get(key);\r\n        if (!previous || Number(item.score || 0) > Number(previous.score || 0)) {\r\n            merged.set(key, item);\r\n        }\r\n    }\r\n    return [...merged.values()]\r\n        .sort((a, b) => (Number(b.score || 0) - Number(a.score || 0)) || String(b.nodeRef?.updatedAt || '').localeCompare(String(a.nodeRef?.updatedAt || '')));\r\n}\r\n\r\nfunction mergeSelectedMemoryVectors(selectedMemories = [], selectedVectors = []) {\r\n    const maxVectorItems = clampNumber(settings.bookshelfMemoryVectorMaxItems, defaultSettings.bookshelfMemoryVectorMaxItems, 1, 12);\r\n    const merged = [...selectedMemories];\r\n    const seen = new Set(merged.map(item => String(item?.uid || item?.routerId || '')));\r\n    for (const item of selectedVectors.slice(0, maxVectorItems)) {\r\n        const key = String(item?.uid || item?.routerId || '');\r\n        if (!key || seen.has(key)) continue;\r\n        merged.push({\r\n            ...item,\r\n            reason: item.reason || `向量召回命中，分数 ${Number(item.score || 0).toFixed(2)}`,\r\n        });\r\n        seen.add(key);\r\n    }\r\n    return merged;\r\n}\r\n\r\nasync function recallBookshelfChunks(query, context = getContext(), options = {}) {\r\n    const force = !!options.force;\r\n    if (!force && (!settings.bookshelfEnabled || !settings.bookshelfAutoInject)) {\r\n        return { candidates: [], selected: [] };\r\n    }\r\n    const queryText = String(query || '').trim();\r\n    if (!queryText) return { candidates: [], selected: [] };\r\n\r\n    const [books, chunks] = await Promise.all([\r\n        bookshelfGetAll('books'),\r\n        bookshelfGetAll('chunks'),\r\n    ]);\r\n    const availableBooks = new Map((books || [])\r\n        .filter(book => isBookshelfBookAvailable(book, context))\r\n        .map(book => [book.id, book]));\r\n    if (!availableBooks.size) return { candidates: [], selected: [] };\r\n\r\n    const queryVector = await embedBookshelfText(queryText);\r\n    const queryTerms = tokenizeBookshelfText(queryText).slice(0, 40);\r\n    const minScore = clampNumber(settings.bookshelfMinScore, defaultSettings.bookshelfMinScore, 0, 1);\r\n    const maxChunks = clampNumber(settings.bookshelfMaxChunks, defaultSettings.bookshelfMaxChunks, 1, 12);\r\n\r\n    const candidates = (chunks || [])\r\n        .filter(chunk => chunk?.enabled !== false && availableBooks.has(chunk.bookId) && chunk.vectorStatus === 'ready' && Array.isArray(chunk.vector))\r\n        .map(chunk => {\r\n            const book = availableBooks.get(chunk.bookId);\r\n            const semanticScore = Math.max(0, cosineSimilarity(queryVector, chunk.vector));\r\n            const keywordScore = calcBookshelfKeywordScore(chunk.text, queryTerms);\r\n            const score = Math.min(1, (semanticScore * 0.78) + (keywordScore * 0.22) + ((Number(chunk.weight) || 1) - 1) * 0.05);\r\n            return {\r\n                ...chunk,\r\n                book,\r\n                source: 'bookshelf',\r\n                score,\r\n                semanticScore,\r\n                keywordScore,\r\n                comment: book?.title || chunk.title || chunk.id,\r\n                content: chunk.text,\r\n                uid: chunk.id,\r\n            };\r\n        })\r\n        .filter(item => item.score >= minScore)\r\n        .sort((a, b) => b.score - a.score);\r\n\r\n    return {\r\n        candidates,\r\n        selected: candidates.slice(0, maxChunks),\r\n    };\r\n}\r\n\r\nfunction buildMemoryVectorText(node, graph = getMemoryGraph()) {\r\n    const parts = [];\r\n    const push = (label, value) => {\r\n        const text = String(value || '').trim();\r\n        if (text) parts.push(`${label}: ${text}`);\r\n    };\r\n    push('Title', node?.title);\r\n    push('Type', node?.type);\r\n    push('Summary', node?.summary);\r\n    push('Content', node?.content);\r\n    push('Location', node?.location);\r\n    push('Time', node?.timeSpan);\r\n    if (Array.isArray(node?.tags) && node.tags.length) push('Tags', node.tags.join(', '));\r\n    if (Array.isArray(node?.keys) && node.keys.length) push('Keys', node.keys.join(', '));\r\n    if (graph?.lastSummary) push('Recent summary', truncateText(graph.lastSummary, 360));\r\n    return parts.join('\\n').trim();\r\n}\r\nfunction buildMemoryVectorRecord(node, graph = getMemoryGraph(), context = getContext(), provider = getBookshelfEmbeddingProviderMeta()) {\r\n    const chatKey = getCurrentChatMemoryKey(context);\r\n    const text = buildMemoryVectorText(node, graph);\r\n    const textHash = hashString(text);\r\n    return {\r\n        id: `memory:${chatKey}:${node.id}`,\r\n        chatKey,\r\n        nodeId: String(node.id || ''),\r\n        title: String(node.title || node.id || '记忆节点'),\r\n        memoryType: String(node.type || 'event'),\r\n        text,\r\n        textHash,\r\n        vector: [],\r\n        vectorStatus: text ? 'pending' : 'empty',\r\n        embeddingMode: provider.mode,\r\n        embeddingModel: provider.model,\r\n        embeddingDim: 0,\r\n        updatedAt: new Date().toISOString(),\r\n    };\r\n}\r\n\r\nasync function syncMemoryGraphVectors(graph = getMemoryGraph(), context = getContext(), options = {}) {\n    if (!settings.bookshelfMemoryVectorRecall && !options.force) {\n        return { total: 0, ready: 0, pending: 0, failed: 0, skipped: true };\n    }\n    startRuntimeActivity('embedding', '同步图谱向量中', '正在检查记忆节点向量状态。');\n    try {\n        const provider = getBookshelfEmbeddingProviderMeta();\n        if (!provider.model) {\n            throw new Error('Please enter the bookshelf API URL first.');\n        }\n        const chatKey = getCurrentChatMemoryKey(context);\n        const nodes = (Array.isArray(graph?.nodes) ? graph.nodes : [])\n            .filter(node => node?.id && buildMemoryVectorText(node, graph));\n        updateRuntimeActivity('embedding', `待同步记忆节点 ${nodes.length} 个`);\n        const existing = await bookshelfGetAll('memoryVectors').catch(() => []);\n        const existingById = new Map((existing || [])\n            .filter(item => item.chatKey === chatKey)\n            .map(item => [item.id, item]));\n        const liveIds = new Set(nodes.map(node => `memory:${chatKey}:${node.id}`));\n        const staleIds = (existing || [])\n            .filter(item => item.chatKey === chatKey && !liveIds.has(item.id))\n            .map(item => item.id);\n\n        for (const id of staleIds) {\n            await bookshelfDelete('memoryVectors', id);\n        }\n\n        let ready = 0;\n        let pending = 0;\n        let failed = 0;\n        const records = [];\n        for (const node of nodes) {\n            const base = buildMemoryVectorRecord(node, graph, context, provider);\n            const previous = existingById.get(base.id);\n            if (\n                previous\n                && previous.textHash === base.textHash\n                && previous.embeddingMode === provider.mode\n                && previous.embeddingModel === provider.model\n                && previous.vectorStatus === 'ready'\n                && Array.isArray(previous.vector)\n                && previous.vector.length\n                && !options.force\n            ) {\n                ready += 1;\n                continue;\n            }\n            try {\n                const vector = await embedBookshelfText(base.text);\n                records.push({\n                    ...base,\n                    vector,\n                    vectorStatus: 'ready',\n                    embeddingDim: vector.length,\n                    error: '',\n                });\n                ready += 1;\n            } catch (error) {\n                records.push({\n                    ...base,\n                    vector: [],\n                    vectorStatus: 'failed',\n                    error: error?.message || String(error),\n                });\n                failed += 1;\n            }\n            pending += 1;\n            if (records.length >= 6) {\n                await bookshelfPutMany('memoryVectors', records.splice(0, records.length));\n                await new Promise(resolve => setTimeout(resolve, 0));\n            }\n        }\n        if (records.length) {\n            await bookshelfPutMany('memoryVectors', records);\n        }\n        finishRuntimeActivityStage('embedding', `向量 ready ${ready} · failed ${failed}`);\n        return { total: nodes.length, ready, pending, failed, skipped: false };\n    } catch (error) {\n        failRuntimeActivity('embedding', error);\n        throw error;\n    }\n}\n\r\nasync function recallMemoryVectorChunks(query, memoryGraph = getMemoryGraph(), context = getContext(), options = {}) {\r\n    const force = !!options.force;\r\n    if (!force && !settings.bookshelfMemoryVectorRecall) {\r\n        return { candidates: [], selected: [], sync: null };\r\n    }\r\n    const queryText = String(query || '').trim();\r\n    if (!queryText) return { candidates: [], selected: [], sync: null };\r\n\r\n    const sync = await syncMemoryGraphVectors(memoryGraph, context, { force: !!options.forceRebuild });\r\n    const chatKey = getCurrentChatMemoryKey(context);\r\n    const [records, queryVector] = await Promise.all([\r\n        bookshelfGetAll('memoryVectors'),\r\n        embedBookshelfText(queryText),\r\n    ]);\r\n    const queryTerms = tokenizeBookshelfText(queryText).slice(0, 40);\r\n    const minScore = clampNumber(settings.bookshelfMinScore, defaultSettings.bookshelfMinScore, 0, 1);\r\n    const maxItems = clampNumber(settings.bookshelfMemoryVectorMaxItems, defaultSettings.bookshelfMemoryVectorMaxItems, 1, 12);\r\n    const nodeById = new Map((Array.isArray(memoryGraph?.nodes) ? memoryGraph.nodes : []).map(node => [String(node.id), node]));\r\n    const candidates = (records || [])\r\n        .filter(item => item.chatKey === chatKey && item.vectorStatus === 'ready' && Array.isArray(item.vector) && item.vector.length)\r\n        .map(item => {\r\n            const semanticScore = Math.max(0, cosineSimilarity(queryVector, item.vector));\r\n            const keywordScore = calcBookshelfKeywordScore(item.text, queryTerms);\r\n            const node = nodeById.get(String(item.nodeId)) || {};\r\n            const score = Math.min(1, (semanticScore * 0.82) + (keywordScore * 0.18));\r\n            return {\r\n                routerId: `memory-vector:${item.nodeId}`,\r\n                uid: String(item.nodeId || item.id),\r\n                source: 'memory-vector',\r\n                world: 'memory-vector',\r\n                comment: item.title || node.title || '图谱记忆',\r\n                content: node.content || node.summary || item.text,\r\n                keys: {\r\n                    primary: uniqueStrings([item.title, node.title].filter(Boolean)),\r\n                    secondary: uniqueStrings([\r\n                        item.memoryType,\r\n                        node.type,\r\n                        ...(Array.isArray(node.tags) ? node.tags : []),\r\n                        ...(Array.isArray(node.keys) ? node.keys : []),\r\n                    ].filter(Boolean)),\r\n                    all: uniqueStrings([\r\n                        item.title,\r\n                        node.title,\r\n                        item.memoryType,\r\n                        node.type,\r\n                        ...(Array.isArray(node.tags) ? node.tags : []),\r\n                        ...(Array.isArray(node.keys) ? node.keys : []),\r\n                    ].filter(Boolean)),\r\n                },\r\n                matchedKeys: [],\r\n                score,\r\n                semanticScore,\r\n                keywordScore,\r\n                memoryType: item.memoryType || node.type || 'event',\r\n                nodeRef: node,\r\n                vectorRecord: item,\r\n                reason: `向量召回：语义 ${semanticScore.toFixed(2)}，关键词 ${keywordScore.toFixed(2)}`,\r\n            };\r\n        })\r\n        .filter(item => item.score >= minScore)\r\n        .sort((a, b) => b.score - a.score);\r\n\r\n    return {\r\n        candidates,\r\n        selected: candidates.slice(0, maxItems),\r\n        sync,\r\n    };\r\n}\r\n\r\nasync function recallVectorMemoryAndBookshelf(query, memoryGraph = getMemoryGraph(), context = getContext(), options = {}) {\r\n    const [memoryResult, bookshelfResult] = await Promise.all([\r\n        recallMemoryVectorChunks(query, memoryGraph, context, options).catch(error => ({ candidates: [], selected: [], error })),\r\n        recallBookshelfChunks(query, context, options).catch(error => ({ candidates: [], selected: [], error })),\r\n    ]);\r\n    if (memoryResult.error) {\r\n        console.warn(`${LOG_PREFIX} Memory vector recall skipped.`, memoryResult.error);\r\n    }\r\n    if (bookshelfResult.error) {\r\n        console.warn(`${LOG_PREFIX} Bookshelf recall skipped.`, bookshelfResult.error);\r\n        setBookshelfStatus(`向量召回跳过：${bookshelfResult.error?.message || bookshelfResult.error}`);\r\n    }\r\n    return {\r\n        memoryCandidates: memoryResult.candidates || [],\r\n        selectedMemoryVectors: memoryResult.selected || [],\r\n        bookshelfCandidates: bookshelfResult.candidates || [],\r\n        selectedBookshelf: bookshelfResult.selected || [],\r\n        errors: [memoryResult.error, bookshelfResult.error].filter(Boolean),\r\n        sync: memoryResult.sync || null,\r\n    };\r\n}\r\n\r\nasync function buildUnifiedRecallBundle(context, recentMessages, options = {}) {\n    startRuntimeActivity('recall', '召回中', '正在召回世界书、记忆和书架候选。');\n    const mvuSummary = options.mvuSummary ?? getCombinedStateSummary(context);\n    const memoryGraph = options.memoryGraph || getMemoryGraph(context);\n    const entries = await getWorldbookEntries(context);\n    const wbCandidates = tagRecallCandidates(\r\n        recallCandidates(entries, recentMessages, mvuSummary),\r\n        'worldbook',\r\n    );\r\n    const keywordMemoryCandidates = tagRecallCandidates(\r\n        recallMemoryCandidates(memoryGraph, recentMessages, mvuSummary),\r\n        'memory-keyword',\r\n    );\r\n\r\n    let memoryCandidates = keywordMemoryCandidates;\r\n    let bookshelfCandidates = [];\r\n    let selectedBookshelf = [];\r\n    let selectedMemoryVectors = [];\r\n    let vectorErrors = [];\r\n    let vectorSync = null;\r\n\n    try {\n        updateRuntimeActivity('recall', '正在执行向量召回。');\n        const query = buildBookshelfRecallQuery(recentMessages, mvuSummary);\n        const vectorResult = await recallVectorMemoryAndBookshelf(query, memoryGraph, context, options);\n        const vectorMemoryCandidates = tagRecallCandidates(vectorResult.memoryCandidates || [], 'memory-vector');\r\n        memoryCandidates = mergeMemoryVectorCandidates(keywordMemoryCandidates, vectorMemoryCandidates);\r\n        selectedMemoryVectors = tagRecallCandidates(vectorResult.selectedMemoryVectors || [], 'memory-vector');\r\n        bookshelfCandidates = tagRecallCandidates(vectorResult.bookshelfCandidates || [], 'bookshelf');\r\n        selectedBookshelf = tagRecallCandidates(vectorResult.selectedBookshelf || [], 'bookshelf');\r\n        vectorErrors = vectorResult.errors || [];\r\n        vectorSync = vectorResult.sync || null;\r\n    } catch (error) {\r\n        vectorErrors = [error];\r\n        console.warn(`${LOG_PREFIX} Vector recall skipped.`, error);\n        setBookshelfStatus(`向量召回跳过：${error?.message || error}`);\n    }\n    finishRuntimeActivityStage('recall', `世界书 ${wbCandidates.length} · 记忆 ${memoryCandidates.length} · 书架 ${bookshelfCandidates.length}`);\n\n    return {\n        mvuSummary,\r\n        memoryGraph,\r\n        wbCandidates,\r\n        memoryCandidates,\r\n        bookshelfCandidates,\r\n        selectedBookshelf,\r\n        selectedMemoryVectors,\r\n        combinedCandidates: [...wbCandidates, ...memoryCandidates, ...bookshelfCandidates],\r\n        vectorErrors,\r\n        vectorSync,\r\n        pipeline: {\r\n            preRefresh: options.preRefresh || null,\r\n            graph: {\r\n                nodes: Array.isArray(memoryGraph?.nodes) ? memoryGraph.nodes.length : 0,\r\n                links: Array.isArray(memoryGraph?.links) ? memoryGraph.links.length : 0,\r\n                hasState: hasMemoryState(memoryGraph),\r\n                updatedAt: memoryGraph?.updatedAt || '',\r\n            },\r\n            recall: {\r\n                worldbook: wbCandidates.length,\r\n                memory: memoryCandidates.length,\r\n                bookshelf: bookshelfCandidates.length,\r\n                combined: wbCandidates.length + memoryCandidates.length + bookshelfCandidates.length,\r\n                selectedMemoryVectors: selectedMemoryVectors.length,\r\n                selectedBookshelf: selectedBookshelf.length,\r\n            },\r\n            vector: {\r\n                sync: vectorSync,\r\n                errors: vectorErrors.map(error => error?.message || String(error)),\r\n            },\r\n        },\r\n    };\r\n}\r\n\r\nasync function importBookshelfFiles(files) {\r\n    const list = Array.from(files || []);\r\n    if (!list.length) {\r\n        setBookshelfStatus('No files selected.');\r\n        return;\r\n    }\r\n\r\n    const context = getContext();\r\n    const scope = getBookshelfScope(context);\r\n    const type = String($('[id=\"ai_wbr_bookshelf_import_type\"]:visible').last().val() || $('[id=\"ai_wbr_bookshelf_import_type\"]').last().val() || 'other');\r\n    let imported = 0;\r\n    let totalChunks = 0;\r\n    setBookshelfStatus(`Importing ${list.length} TXT file(s)...`);\r\n\r\n    for (const file of list) {\r\n        const text = await readBookshelfTextFile(file);\r\n        const rawTitle = file.name.replace(/\\.[^.]+$/, '').trim() || 'Untitled book';\r\n        const now = new Date().toISOString();\r\n        const bookId = `book_${Date.now().toString(36)}_${hashString(`${file.name}:${file.size}:${text.slice(0, 200)}`)}`;\r\n        const chunks = splitTextIntoBookshelfChunks(text, { chunkSize: 700, overlap: 100 });\r\n        const bindings = [];\r\n\r\n        const book = {\r\n            id: bookId,\r\n            title: rawTitle,\r\n            fileName: file.name,\r\n            type,\r\n            enabled: true,\r\n            bindings,\r\n            status: chunks.length ? 'pending_vectorization' : 'empty',\r\n            embeddingMode: '',\r\n            embeddingModel: '',\r\n            embeddingDim: 0,\r\n            chunkCount: chunks.length,\r\n            vectorizedCount: 0,\r\n            createdAt: now,\r\n            updatedAt: now,\r\n        };\r\n        const records = chunks.map((chunk, index) => {\r\n            const textHash = hashString(chunk.text);\r\n            return {\r\n                id: `${bookId}:chunk:${index}` ,\r\n                bookId,\r\n                index,\r\n                title: chunk.title || `${rawTitle} #${index + 1}`,\r\n                text: chunk.text,\r\n                textHash,\r\n                vector: null,\r\n                embeddingMode: '',\r\n                embeddingModel: '',\r\n                embeddingDim: 0,\r\n                createdAt: now,\r\n                updatedAt: now,\r\n            };\r\n        });\r\n\r\n        if (settings.bookshelfOnlyBound) {\r\n            if (scope.chatKey) bindings.push({ type: 'chat', key: scope.chatKey, label: scope.chatName || scope.chatKey, createdAt: now });\r\n            else if (scope.characterKey) bindings.push({ type: 'character', key: scope.characterKey, label: scope.characterName || scope.characterKey, createdAt: now });\r\n        }\r\n\r\n        await bookshelfPut('books', book);\r\n        if (records.length) await bookshelfPutMany('chunks', records);\r\n        imported += 1;\r\n        totalChunks += records.length;\r\n    }\r\n\r\n    bookshelfLastStatus = `Imported ${imported} book(s), ${totalChunks} chunk(s).`;\r\n    setBookshelfStatus('Ready');\r\n    renderBookshelfPanel();\r\n}\r\nasync function updateBookshelfBook(bookId, mutator) {\r\n    const book = await bookshelfGet('books', bookId);\r\n    if (!book) return null;\r\n    const next = {\r\n        ...book,\r\n        ...(typeof mutator === 'function' ? mutator(book) : mutator || {}),\r\n        updatedAt: new Date().toISOString(),\r\n    };\r\n    await bookshelfPut('books', next);\r\n    return next;\r\n}\r\n\r\nasync function setBookshelfBinding(bookId, type) {\r\n    const scope = getBookshelfScope();\r\n    const binding = type === 'character'\r\n        ? { type: 'character', key: scope.characterKey, label: scope.characterName || scope.characterKey || 'Character' }\r\n        : type === 'chat'\r\n            ? { type: 'chat', key: scope.chatKey, label: scope.chatName || scope.chatKey || 'Chat' }\r\n            : { type: 'global', key: 'global', label: 'Global' };\r\n    await updateBookshelfBook(bookId, (book) => {\r\n        const bindings = normalizeBookshelfBindings(book).filter(item => !(item.type === binding.type && item.key === binding.key));\r\n        bindings.push({ ...binding, createdAt: new Date().toISOString() });\r\n        return { bindings };\r\n    });\r\n    renderBookshelfPanel();\r\n}\r\nasync function removeBookshelfBinding(bookId, type) {\r\n    const scope = getBookshelfScope();\r\n    const key = type === 'character' ? scope.characterKey : type === 'chat' ? scope.chatKey : 'global';\r\n    await updateBookshelfBook(bookId, (book) => ({\r\n        bindings: normalizeBookshelfBindings(book).filter(item => !(item.type === type && item.key === key)),\r\n    }));\r\n    renderBookshelfPanel();\r\n}\r\n\r\nasync function clearBookshelfBindings(bookId) {\r\n    if (!bookId) return;\r\n    await updateBookshelfBook(bookId, { bindings: [] });\r\n    renderBookshelfPanel();\r\n}\r\n\r\nasync function rebuildBookshelfBookVectors(bookId) {\r\n    const [book, chunks] = await Promise.all([\r\n        bookshelfGet('books', bookId),\r\n        bookshelfGetAll('chunks'),\r\n    ]);\r\n    if (!book) return;\r\n    const targets = (chunks || []).filter(chunk => chunk.bookId === bookId);\r\n    if (!targets.length) {\r\n        await updateBookshelfBook(bookId, { status: 'empty', vectorizedCount: 0, chunkCount: 0 });\r\n        setBookshelfStatus('This book has no chunks to vectorize.');\r\n        renderBookshelfPanel();\r\n        return;\r\n    }\r\n    const provider = getBookshelfEmbeddingProviderMeta();\r\n    if (!provider.model) {\r\n        throw new Error('Please configure an embedding model first.');\r\n    }\r\n    bookshelfVectorAbortRequested = false;\r\n    await updateBookshelfBook(bookId, {\r\n        status: 'vectorizing',\r\n        embeddingMode: provider.mode,\r\n        embeddingModel: provider.model,\r\n        vectorizedCount: targets.filter(chunk => chunk.vectorStatus === 'ready' && Array.isArray(chunk.vector) && chunk.vector.length).length,\r\n        chunkCount: targets.length,\r\n    });\r\n    setBookshelfStatus(`Vectorizing ${book.title || book.fileName}: 0 / ${targets.length} (${provider.label})`);\r\n    let done = 0;\r\n    let failed = 0;\r\n    let dim = 0;\r\n    const batch = [];\r\n    for (let index = 0; index < targets.length; index += 1) {\r\n        if (bookshelfVectorAbortRequested) break;\r\n        const chunk = targets[index];\r\n        try {\r\n            const vector = await embedBookshelfText(chunk.text);\r\n            dim = vector.length || dim;\r\n            batch.push({\r\n                ...chunk,\r\n                textHash: hashString(chunk.text),\r\n                vector,\r\n                vectorStatus: 'ready',\r\n                embeddingMode: provider.mode,\r\n                embeddingModel: provider.model,\r\n                embeddingDim: vector.length,\r\n                error: '',\r\n                updatedAt: new Date().toISOString(),\r\n            });\r\n            done += 1;\r\n        } catch (error) {\r\n            failed += 1;\r\n            batch.push({\r\n                ...chunk,\r\n                vector: [],\r\n                vectorStatus: 'failed',\r\n                embeddingMode: provider.mode,\r\n                embeddingModel: provider.model,\r\n                embeddingDim: 0,\r\n                error: error?.message || String(error),\r\n                updatedAt: new Date().toISOString(),\r\n            });\r\n        }\r\n        if (batch.length >= 8 || index === targets.length - 1) {\r\n            await bookshelfPutMany('chunks', batch.splice(0, batch.length));\r\n            await updateBookshelfBook(bookId, {\r\n                status: failed ? 'partial_failed' : 'vectorizing',\r\n                embeddingMode: provider.mode,\r\n                embeddingModel: provider.model,\r\n                embeddingDim: dim,\r\n                vectorizedCount: done,\r\n                chunkCount: targets.length,\r\n            });\r\n            setBookshelfStatus(`Vectorizing ${book.title || book.fileName}: ${done} / ${targets.length}${failed ? `, failed ${failed}` : ''}`);\r\n            await new Promise(resolve => setTimeout(resolve, 0));\r\n        }\r\n    }\r\n    const finalStatus = done === targets.length ? 'ready' : done > 0 ? 'partial_failed' : 'failed';\r\n    await updateBookshelfBook(bookId, {\r\n        status: finalStatus,\r\n        embeddingMode: provider.mode,\r\n        embeddingModel: provider.model,\r\n        embeddingDim: dim,\r\n        chunkCount: targets.length,\r\n        vectorizedCount: done,\r\n    });\r\n    setBookshelfStatus(`Vectorization complete for ${book.title || book.fileName}: ${done} / ${targets.length}${failed ? `, failed ${failed}` : ''}.`);\r\n    renderBookshelfPanel();\r\n}\r\n\r\nasync function clearBookshelfBookVectors(bookId) {\r\n    const [book, chunks] = await Promise.all([\r\n        bookshelfGet('books', bookId),\r\n        bookshelfGetAll('chunks'),\r\n    ]);\r\n    if (!book) return;\r\n    const now = new Date().toISOString();\r\n    const cleared = (chunks || []).filter(chunk => chunk.bookId === bookId).map(chunk => ({\r\n        ...chunk,\r\n        textHash: hashString(chunk.text),\r\n        vector: [],\r\n        vectorStatus: 'pending',\r\n        embeddingMode: '',\r\n        embeddingModel: '',\r\n        embeddingDim: 0,\r\n        error: '',\r\n        updatedAt: now,\r\n    }));\r\n    if (cleared.length) await bookshelfPutMany('chunks', cleared);\r\n    await updateBookshelfBook(bookId, {\r\n        status: cleared.length ? 'pending_vectorization' : 'empty',\r\n        embeddingMode: '',\r\n        embeddingModel: '',\r\n        embeddingDim: 0,\r\n        chunkCount: cleared.length,\r\n        vectorizedCount: 0,\r\n    });\r\n    setBookshelfStatus(`Reset ${book.title || book.fileName} to pending vectorization.`);\r\n    renderBookshelfPanel();\r\n}\r\nfunction getLegacyChatMemoryKeys(context = getContext()) {\r\n    const keys = [];\r\n    const add = (key) => {\r\n        const value = String(key || '').trim();\r\n        if (value && !keys.includes(value)) {\r\n            keys.push(value);\r\n        }\r\n    };\r\n\r\n    const { scopeParts, stableChatParts } = getStableChatScopeParts(context);\r\n    if (stableChatParts.length) {\r\n        add(`chat:${[...scopeParts, ...stableChatParts].join('|')}`);\r\n    }\r\n\r\n    const weakParts = [];\r\n    for (const candidate of [context?.chatMetadata?.chat_name, context?.chatMetadata?.name]) {\r\n        const value = String(candidate ?? '').trim();\r\n        if (value && !weakParts.includes(value)) {\r\n            weakParts.push(value);\r\n        }\r\n    }\r\n    if (weakParts.length) {\r\n        add(`chat:weak:${[...scopeParts, ...weakParts].join('|')}`);\r\n    }\r\n\r\n    const chat = Array.isArray(context?.chat) ? context.chat : [];\r\n    if (chat.length) {\r\n        const fingerprint = chat\r\n            .slice(0, 6)\r\n            .map(item => `${item?.is_user ? 'u' : 'a'}:${item?.name || ''}:${item?.mes || item?.text || ''}`)\r\n            .join('\\n');\r\n        if (fingerprint.trim()) {\r\n            add(`chat:fallback:${hashString(`${scopeParts.join('|')}|${fingerprint}`)}`);\r\n        }\r\n    }\r\n\r\n    return keys;\r\n}\r\n\r\nfunction hasUsefulMemoryContainer(container) {\r\n    if (!container || typeof container !== 'object' || Array.isArray(container)) {\r\n        return false;\r\n    }\r\n    const normalized = normalizeChatMemoryContainer(container);\r\n    return !!(\r\n        normalized.graph?.nodes?.length\r\n        || normalized.graph?.links?.length\r\n        || hasMemoryState(normalized.graph)\r\n        || normalized.reviewQueue?.length\r\n        || normalized.lastPrompt\r\n        || normalized.lastRaw\r\n        || normalized.lastError\r\n        || normalized.status\r\n    );\r\n}\r\n\r\nfunction hasUsefulMemoryGraph(graph) {\r\n    if (!graph || typeof graph !== 'object' || Array.isArray(graph)) {\r\n        return false;\r\n    }\r\n    const safeGraph = cloneMemoryGraph(graph);\r\n    return !!(\r\n        (Array.isArray(safeGraph.nodes) && safeGraph.nodes.length)\r\n        || (Array.isArray(safeGraph.links) && safeGraph.links.length)\r\n        || hasMemoryState(safeGraph)\r\n        || String(safeGraph.lastSummary || '').trim()\r\n    );\r\n}\r\n\r\nfunction cloneMemoryContainerForScope(container, chatKey) {\r\n    const normalized = normalizeChatMemoryContainer(container);\r\n    normalized.chatKey = chatKey;\r\n    normalized.migratedAt = normalized.migratedAt || new Date().toISOString();\r\n    normalized.migratedFrom = normalized.migratedFrom || String(container?.chatKey || 'legacy');\r\n    return normalized;\r\n}\r\n\r\nfunction cloneMemoryGraph(graph) {\r\n    try {\r\n        return JSON.parse(JSON.stringify(graph || getDefaultMemoryGraph()));\r\n    } catch {\r\n        return getDefaultMemoryGraph();\r\n    }\r\n}\r\n\r\nfunction normalizeMemoryStateDefinition(definition, index = 0) {\r\n    const rawLabel = String(definition?.label || definition?.name || '').trim();\r\n    const rawInstruction = String(definition?.instruction || definition?.desc || definition?.description || '').trim();\r\n    const label = truncateText(rawLabel || `自定义字段${index + 1}`, 40);\r\n    const keyBase = String(definition?.key || createMemoryId(rawLabel || rawInstruction || `custom_state_${index + 1}`, 'custom_state')).trim();\r\n    const key = keyBase.startsWith('custom_') ? keyBase : `custom_${keyBase}`;\r\n    return {\r\n        key,\r\n        label,\r\n        instruction: truncateText(rawInstruction, 140),\r\n    };\r\n}\r\n\r\nfunction getCustomMemoryStateDefinitions(graph) {\r\n    return (Array.isArray(graph?.stateDefinitions) ? graph.stateDefinitions : [])\r\n        .map((definition, index) => normalizeMemoryStateDefinition(definition, index))\r\n        .filter((definition, index, array) => definition.label && array.findIndex(item => item.key === definition.key) === index);\r\n}\r\n\r\nfunction getChatMemoryFirstMessage(context = getContext()) {\r\n    const chat = Array.isArray(context?.chat) ? context.chat : [];\r\n    const first = chat[0];\r\n    return first && typeof first === 'object' ? first : null;\r\n}\r\n\r\nfunction normalizeChatMemoryContainer(container) {\r\n    const normalized = container && typeof container === 'object' && !Array.isArray(container)\r\n        ? { ...container }\r\n        : {};\r\n    normalized.version = Number(normalized.version || 1);\r\n    normalized.graph = cloneMemoryGraph(normalized.graph || getDefaultMemoryGraph());\r\n    normalized.graph.stateDefinitions = getCustomMemoryStateDefinitions(normalized.graph);\r\n    normalized.graph.state = {\r\n        ...getDefaultMemoryGraph().state,\r\n        ...(normalized.graph.state && typeof normalized.graph.state === 'object' ? normalized.graph.state : {}),\r\n    };\r\n    normalized.graph.state.custom_values = normalized.graph.state.custom_values && typeof normalized.graph.state.custom_values === 'object'\r\n        ? { ...normalized.graph.state.custom_values }\r\n        : {};\r\n    normalized.graphBackup = normalized.graphBackup && typeof normalized.graphBackup === 'object'\r\n        ? cloneMemoryGraph(normalized.graphBackup)\r\n        : null;\r\n    normalized.chatKey = String(normalized.chatKey || '');\r\n    normalized.lastAutoMessageCount = clampNumber(normalized.lastAutoMessageCount, 0, 0, 1000000);\r\n    normalized.lastSummaryMessageCount = clampNumber(normalized.lastSummaryMessageCount, 0, 0, 1000000);\r\n    normalized.status = String(normalized.status || '');\r\n    normalized.lastTurnSignature = String(normalized.lastTurnSignature || '');\r\n    normalized.lastPrompt = String(normalized.lastPrompt || '');\r\n    normalized.lastRaw = String(normalized.lastRaw || '');\r\n    normalized.lastError = String(normalized.lastError || '');\r\n    normalized.historyImports = Array.isArray(normalized.historyImports)\r\n        ? normalized.historyImports.filter(item => item && typeof item === 'object').slice(0, 120)\r\n        : [];\r\n    normalized.reviewQueue = Array.isArray(normalized.reviewQueue)\r\n        ? normalized.reviewQueue.filter(item => item && typeof item === 'object').slice(0, 40)\r\n        : [];\r\n    return normalized;\r\n}\r\n\r\nfunction getChatMemoryContainer(context = getContext()) {\r\n    const currentChatKey = getCurrentChatMemoryKey(context);\r\n    const storedContainer = settings.memoryContainersByChat?.[currentChatKey];\r\n    if (storedContainer && typeof storedContainer === 'object' && !Array.isArray(storedContainer)) {\r\n        const normalizedStored = normalizeChatMemoryContainer(storedContainer);\r\n        normalizedStored.chatKey = currentChatKey;\r\n        if (hasUsefulMemoryContainer(normalizedStored)) {\r\n            memoryScopeDebugLog('getChatMemoryContainer from settings map', {\r\n                chatKey: currentChatKey,\r\n                graph: getMemoryGraphSummary(normalizedStored.graph),\r\n            }, context);\r\n            return normalizedStored;\r\n        }\r\n        memoryScopeDebugLog('getChatMemoryContainer ignored empty settings map container', {\r\n            chatKey: currentChatKey,\r\n            graph: getMemoryGraphSummary(normalizedStored.graph),\r\n        }, context);\r\n    }\r\n\r\n    const legacyChatKeys = getLegacyChatMemoryKeys(context);\r\n    for (const legacyKey of legacyChatKeys) {\r\n        const legacyContainer = settings.memoryContainersByChat?.[legacyKey];\r\n        if (!hasUsefulMemoryContainer(legacyContainer)) {\r\n            continue;\r\n        }\r\n        const migrated = cloneMemoryContainerForScope(legacyContainer, currentChatKey);\r\n        settings.memoryContainersByChat[currentChatKey] = migrated;\r\n        Object.assign(extension_settings[MODULE_NAME], settings);\r\n        saveSettingsDebounced();\r\n        memoryScopeDebugLog('getChatMemoryContainer migrated from legacy settings key', {\r\n            legacyKey,\r\n            currentChatKey,\r\n            graph: getMemoryGraphSummary(migrated.graph),\r\n        }, context);\r\n        return migrated;\r\n    }\r\n\r\n    const first = getChatMemoryFirstMessage(context);\r\n    if (!first) {\r\n        memoryScopeDebugLog('getChatMemoryContainer no first message', {}, context);\r\n        const empty = normalizeChatMemoryContainer(null);\r\n        empty.chatKey = currentChatKey;\r\n        return empty;\r\n    }\r\n\r\n    const raw = first[CHAT_MEMORY_FIELD];\r\n    const parsed = typeof raw === 'string'\r\n        ? (() => {\r\n            try {\r\n                return JSON.parse(raw);\r\n            } catch {\r\n                return null;\r\n            }\r\n        })()\r\n        : raw;\r\n\r\n    if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {\r\n        const normalized = normalizeChatMemoryContainer(parsed);\r\n        if (normalized.chatKey && normalized.chatKey !== currentChatKey) {\r\n            memoryScopeDebugLog('getChatMemoryContainer migrated mismatched chat[0] key', {\r\n                storedChatKey: normalized.chatKey,\r\n                currentChatKey,\r\n                graph: getMemoryGraphSummary(normalized.graph),\r\n            }, context);\r\n        }\r\n        if (!normalized.chatKey && (normalized.graph?.nodes?.length || normalized.graph?.links?.length || hasMemoryState(normalized.graph))) {\r\n            const legacySignature = hashString(JSON.stringify(getMemoryGraphSummary(normalized.graph)));\r\n            const legacyOwnerKey = settings.memoryLegacyGraphChatKeys?.[legacySignature];\r\n            if (legacyOwnerKey && legacyOwnerKey !== currentChatKey) {\r\n                memoryScopeDebugLog('getChatMemoryContainer reusing legacy graph for stable scope', {\r\n                    legacyOwnerKey,\r\n                    currentChatKey,\r\n                    graph: getMemoryGraphSummary(normalized.graph),\r\n                }, context);\r\n            }\r\n            settings.memoryLegacyGraphChatKeys[legacySignature] = currentChatKey;\r\n        }\r\n        normalized.chatKey = currentChatKey;\r\n        settings.memoryContainersByChat[currentChatKey] = normalized;\r\n        Object.assign(extension_settings[MODULE_NAME], settings);\r\n        saveSettingsDebounced();\r\n        memoryScopeDebugLog('getChatMemoryContainer from chat[0]', {\r\n            rawType: typeof raw,\r\n            chatKey: normalized.chatKey,\r\n            graph: getMemoryGraphSummary(normalized.graph),\r\n        }, context);\r\n        return normalized;\r\n    }\r\n\r\n    for (const legacyKey of [currentChatKey, ...legacyChatKeys]) {\r\n        const legacyGraph = settings.memoryGraphsByChat?.[legacyKey];\r\n        if (!hasUsefulMemoryGraph(legacyGraph)) {\r\n            continue;\r\n        }\r\n        const migrated = normalizeChatMemoryContainer({ graph: legacyGraph, chatKey: currentChatKey });\r\n        migrated.migratedAt = new Date().toISOString();\r\n        migrated.migratedFrom = `memoryGraphsByChat:${legacyKey}`;\r\n        settings.memoryContainersByChat[currentChatKey] = migrated;\r\n        Object.assign(extension_settings[MODULE_NAME], settings);\r\n        saveSettingsDebounced();\r\n        memoryScopeDebugLog('getChatMemoryContainer migrated from legacy graph map', {\r\n            legacyKey,\r\n            currentChatKey,\r\n            graph: getMemoryGraphSummary(migrated.graph),\r\n        }, context);\r\n        return migrated;\r\n    }\r\n\r\n    memoryScopeDebugLog('getChatMemoryContainer empty/default', {\r\n        rawType: raw === undefined ? 'undefined' : typeof raw,\r\n        rawPreview: typeof raw === 'string' ? raw.slice(0, 180) : JSON.stringify(raw || null).slice(0, 180),\r\n    }, context);\r\n    const empty = normalizeChatMemoryContainer(null);\r\n    empty.chatKey = currentChatKey;\r\n    return empty;\r\n}\r\n\r\nfunction persistChatMemoryContainer(container, context = getContext()) {\r\n    const chatKey = getCurrentChatMemoryKey(context);\r\n    const normalized = normalizeChatMemoryContainer(container);\r\n    normalized.chatKey = chatKey;\r\n    settings.memoryContainersByChat[chatKey] = normalized;\r\n    settings.memoryGraphsByChat[chatKey] = cloneMemoryGraph(normalized.graph);\r\n    settings.memoryGraph = normalized.graph;\r\n    Object.assign(extension_settings[MODULE_NAME], settings);\r\n    saveSettingsDebounced();\r\n\r\n    const first = getChatMemoryFirstMessage(context);\r\n    if (!first) {\r\n        memoryScopeDebugLog('persistChatMemoryContainer skipped no first message', {}, context);\r\n        return;\r\n    }\r\n\r\n    first[CHAT_MEMORY_FIELD] = normalized;\r\n    memoryScopeDebugLog('persistChatMemoryContainer wrote chat[0]', {\r\n        chatKey: normalized.chatKey,\r\n        graph: getMemoryGraphSummary(first[CHAT_MEMORY_FIELD]?.graph),\r\n        hasSaveChat: typeof context?.saveChat === 'function',\r\n    }, context);\r\n    if (typeof context?.saveChat === 'function') {\r\n        Promise.resolve(context.saveChat()).catch((error) => {\r\n            console.warn(`${LOG_PREFIX} Failed to save chat memory container`, error);\r\n        });\r\n    }\r\n}\r\n\r\nfunction getPerChatMemoryValue(storeName, fallbackValue, context = getContext()) {\r\n    const container = getChatMemoryContainer(context);\r\n    const fieldMap = {\r\n        memoryStatusesByChat: 'status',\r\n        memoryLastTurnSignaturesByChat: 'lastTurnSignature',\r\n        memoryLastPromptsByChat: 'lastPrompt',\r\n        memoryLastRawByChat: 'lastRaw',\r\n        memoryLastErrorsByChat: 'lastError',\r\n    };\r\n    const field = fieldMap[storeName];\r\n    if (!field) {\r\n        return fallbackValue;\r\n    }\r\n    return container[field] ?? fallbackValue;\r\n}\r\n\r\nfunction setPerChatMemoryValue(storeName, value, context = getContext()) {\r\n    const fieldMap = {\r\n        memoryStatusesByChat: 'status',\r\n        memoryLastTurnSignaturesByChat: 'lastTurnSignature',\r\n        memoryLastPromptsByChat: 'lastPrompt',\r\n        memoryLastRawByChat: 'lastRaw',\r\n        memoryLastErrorsByChat: 'lastError',\r\n    };\r\n    const field = fieldMap[storeName];\r\n    if (!field) {\r\n        return;\r\n    }\r\n    const container = getChatMemoryContainer(context);\r\n    container[field] = value;\r\n    persistChatMemoryContainer(container, context);\r\n}\r\n\r\nfunction getCurrentMemoryStatus(context = getContext()) {\r\n    return String(getPerChatMemoryValue('memoryStatusesByChat', settings.memoryStatus || '', context) || '');\r\n}\r\n\r\nfunction setCurrentMemoryStatus(text, context = getContext()) {\r\n    setPerChatMemoryValue('memoryStatusesByChat', String(text || ''), context);\r\n}\r\n\r\nfunction getCurrentMemoryLastTurnSignature(context = getContext()) {\r\n    return String(getPerChatMemoryValue('memoryLastTurnSignaturesByChat', settings.memoryLastTurnSignature || '', context) || '');\r\n}\r\n\r\nfunction setCurrentMemoryLastTurnSignature(value, context = getContext()) {\r\n    setPerChatMemoryValue('memoryLastTurnSignaturesByChat', String(value || ''), context);\r\n}\r\n\r\nfunction getCurrentMemoryLastPrompt(context = getContext()) {\r\n    return String(getPerChatMemoryValue('memoryLastPromptsByChat', settings.memoryLastPrompt || '', context) || '');\r\n}\r\n\r\nfunction setCurrentMemoryLastPrompt(value, context = getContext()) {\r\n    setPerChatMemoryValue('memoryLastPromptsByChat', String(value || ''), context);\r\n}\r\n\r\nfunction getCurrentMemoryLastRaw(context = getContext()) {\r\n    return String(getPerChatMemoryValue('memoryLastRawByChat', settings.memoryLastRaw || '', context) || '');\r\n}\r\n\r\nfunction setCurrentMemoryLastRaw(value, context = getContext()) {\r\n    setPerChatMemoryValue('memoryLastRawByChat', String(value || ''), context);\r\n}\r\n\r\nfunction getCurrentMemoryLastError(context = getContext()) {\r\n    return String(getPerChatMemoryValue('memoryLastErrorsByChat', settings.memoryLastError || '', context) || '');\r\n}\r\n\r\nfunction setCurrentMemoryLastError(value, context = getContext()) {\r\n    setPerChatMemoryValue('memoryLastErrorsByChat', String(value || ''), context);\r\n}\r\n\r\nfunction getMemoryGraph(context = getContext()) {\r\n    const container = getChatMemoryContainer(context);\r\n    const graph = container.graph || getDefaultMemoryGraph();\r\n    graph.version = Number(graph.version || 1);\r\n    graph.state = {\r\n        ...getDefaultMemoryGraph().state,\r\n        ...(graph.state && typeof graph.state === 'object' ? graph.state : {}),\r\n    };\r\n    graph.state.custom_values = graph.state.custom_values && typeof graph.state.custom_values === 'object'\r\n        ? { ...graph.state.custom_values }\r\n        : {};\r\n    graph.state.active_topics = uniqueStrings(Array.isArray(graph.state.active_topics) ? graph.state.active_topics : []);\r\n    graph.state.open_questions = uniqueStrings(Array.isArray(graph.state.open_questions) ? graph.state.open_questions : []);\r\n    graph.stateDefinitions = getCustomMemoryStateDefinitions(graph);\r\n    graph.nodes = Array.isArray(graph.nodes) ? graph.nodes : [];\r\n    let nodeTypeMigrated = false;\r\n    graph.nodes.forEach((node) => {\r\n        if (!node || typeof node !== 'object') {\r\n            return;\r\n        }\r\n        const type = String(node.type || '').trim().toLowerCase();\r\n        const inferredType = !type ? normalizeMemoryNodeType(node) : '';\r\n        if (!type && inferredType) {\r\n            node.type = inferredType;\r\n            nodeTypeMigrated = true;\r\n        } else if (!type) {\r\n            node.type = 'event';\r\n            nodeTypeMigrated = true;\r\n        }\r\n    });\r\n    graph.links = Array.isArray(graph.links) ? graph.links : [];\n    graph.lastSummary = String(graph.lastSummary || '');\r\n    graph.updatedAt = nodeTypeMigrated ? new Date().toISOString() : String(graph.updatedAt || '');\r\n    return graph;\r\n}\r\n\r\nfunction resetMemoryGraphRenderState(options = {}) {\r\n    memoryGraphPreviewRenderKey = '';\r\n    if (options.fit === true) {\r\n        memoryGraphView = { x: 0, y: 0, width: 0, height: 0 };\r\n    }\r\n}\r\n\r\nfunction refreshMemoryAndGraphPanels(context = getContext(), options = {}) {\r\n    const shouldFitGraph = options.fit === true;\r\n    resetMemoryGraphRenderState({ fit: shouldFitGraph });\r\n    renderMemoryPanel('all');\r\n\r\n    requestAnimationFrame(() => {\r\n        const activeTab = getStandaloneTabId();\r\n        if (activeTab !== 'memory' && activeTab !== 'graph') {\r\n            return;\r\n        }\r\n\r\n        renderMemoryPanel('all');\r\n        if (activeTab === 'graph' && $('#ai_wbr_memory_graph').is(':visible')) {\r\n            if (shouldFitGraph) {\r\n                fitMemoryGraphToContainer(getMemoryGraph(context));\r\n            }\r\n            renderMemoryPanel('graph');\r\n        }\r\n    });\r\n}\r\n\r\nfunction saveMemoryGraph(graph = getMemoryGraph(), context = getContext(), skipRender = false, renderOptions = {}) {\r\n    const container = getChatMemoryContainer(context);\r\n    container.graph = graph;\r\n    persistChatMemoryContainer(container, context);\r\n    settings.memoryGraph = graph;\r\n    Object.assign(extension_settings[MODULE_NAME], settings);\r\n    saveSettingsDebounced();\r\n    scheduleBookshelfMemoryBookSync(context, { silent: true, delayMs: 1200 });\r\n    if (!skipRender) {\r\n        refreshMemoryAndGraphPanels(context, renderOptions);\r\n    }\r\n}\r\n\r\nfunction getMemoryReviewQueue(context = getContext()) {\r\n    const container = getChatMemoryContainer(context);\r\n    return Array.isArray(container.reviewQueue) ? container.reviewQueue : [];\r\n}\r\n\r\nfunction summarizeMemoryUpdateProposal(update) {\r\n    const nodes = Array.isArray(update?.nodes) ? update.nodes : [];\r\n    const updates = Array.isArray(update?.updates) ? update.updates : [];\r\n    const links = Array.isArray(update?.links) ? update.links : [];\r\n    const removes = [\r\n        ...(Array.isArray(update?.remove_node_ids) ? update.remove_node_ids : []),\r\n        ...(Array.isArray(update?.remove_link_ids) ? update.remove_link_ids : []),\r\n    ];\r\n    const state = update?.state && typeof update.state === 'object' ? update.state : {};\r\n    const stateChanges = Object.entries(state).filter(([, value]) => {\r\n        if (Array.isArray(value)) return value.length > 0;\r\n        if (value && typeof value === 'object') return Object.keys(value).length > 0;\r\n        return String(value || '').trim();\r\n    }).length;\r\n    return {\r\n        nodes: nodes.length,\r\n        updates: updates.length,\r\n        links: links.length,\r\n        removes: removes.length,\r\n        stateChanges,\r\n        title: truncateText(update?.summary || nodes[0]?.title || updates[0]?.title || 'Pending memory update', 80),\r\n    };\r\n}\r\n\r\nfunction enqueueMemoryReview(update, meta = {}, context = getContext()) {\r\n    const container = getChatMemoryContainer(context);\r\n    const now = new Date().toISOString();\r\n    const summary = summarizeMemoryUpdateProposal(update);\r\n    const source = meta.source && typeof meta.source === 'object' ? meta.source : null;\r\n    const item = {\r\n        id: `review_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 7)}`,\r\n        status: 'pending',\r\n        reason: String(meta.reason || 'auto'),\r\n        signature: String(meta.signature || ''),\r\n        source,\r\n        createdAt: now,\r\n        title: summary.title,\r\n        summary,\r\n        update,\r\n        prompt: truncateText(meta.prompt || '', 4000),\r\n        raw: truncateText(meta.raw || '', 4000),\r\n    };\r\n    container.reviewQueue = [item, ...(Array.isArray(container.reviewQueue) ? container.reviewQueue : [])].slice(0, 40);\r\n    persistChatMemoryContainer(container, context);\r\n    return item;\r\n}\r\n\r\nfunction removeMemoryReviewItem(id, context = getContext()) {\r\n    const container = getChatMemoryContainer(context);\r\n    container.reviewQueue = getMemoryReviewQueue(context).filter(item => String(item.id) !== String(id));\r\n    persistChatMemoryContainer(container, context);\r\n}\r\n\r\nfunction acceptMemoryReviewItem(id, context = getContext()) {\r\n    const item = getMemoryReviewQueue(context).find(entry => String(entry.id) === String(id));\r\n    if (!item) return null;\r\n    const sourceInvalidated = item.source?.id && getInvalidatedMemorySourceIds(item.source.chatKey || getCurrentChatMemoryKey(context)).includes(String(item.source.id));\r\n    if (item.status === 'stale' || sourceInvalidated || (item.source?.chatKey && item.source.chatKey !== getCurrentChatMemoryKey(context))) {\r\n        const container = getChatMemoryContainer(context);\r\n        container.reviewQueue = getMemoryReviewQueue(context).map(entry => String(entry.id) === String(id)\r\n            ? { ...entry, status: 'stale', staleReason: entry.staleReason || 'source_changed' }\r\n            : entry);\r\n        persistChatMemoryContainer(container, context);\r\n        setMemoryStatus('记忆来源楼层已变化，本条待确认已失效', context);\r\n        return null;\r\n    }\r\n    const container = getChatMemoryContainer(context);\r\n    container.graphBackup = cloneMemoryGraph(getMemoryGraph(context));\r\n    persistChatMemoryContainer(container, context);\r\n    const result = applyMemoryGraphUpdate(item.update, context);\r\n    removeMemoryReviewItem(id, context);\r\n    return result;\r\n}\r\n\r\nfunction rejectMemoryReviewItem(id, context = getContext()) {\r\n    removeMemoryReviewItem(id, context);\r\n}\r\n\r\nfunction acceptAllMemoryReviews(context = getContext()) {\r\n    const queue = [...getMemoryReviewQueue(context)];\r\n    let lastResult = null;\r\n    let acceptedCount = 0;\r\n    for (const item of queue) {\r\n        const result = acceptMemoryReviewItem(item.id, context);\r\n        if (result) {\r\n            acceptedCount += 1;\r\n            lastResult = result;\r\n        }\r\n    }\r\n    return { count: acceptedCount, result: lastResult };\r\n}\r\n\r\nfunction clearMemoryReviewQueue(context = getContext()) {\r\n    const container = getChatMemoryContainer(context);\r\n    const count = getMemoryReviewQueue(context).length;\r\n    container.reviewQueue = [];\r\n    persistChatMemoryContainer(container, context);\r\n    return count;\r\n}\r\n\r\nfunction setMemoryStatus(text, context = getContext()) {\r\n    settings.memoryStatus = String(text || '');\r\n    setCurrentMemoryStatus(text, context);\r\n    $('#ai_wbr_memory_status').text(getCurrentMemoryStatus(context));\r\n}\r\n\r\nfunction createMemoryId(title, fallback = 'memory') {\r\n    const base = String(title || fallback)\r\n        .trim()\r\n        .toLowerCase()\r\n        .replace(/[^\\p{L}\\p{N}]+/gu, '_')\r\n        .replace(/^_+|_+$/gu, '')\r\n        .slice(0, 42);\r\n    return base || `${fallback}_${Date.now().toString(36)}`;\r\n}\r\n\r\nfunction normalizeMemoryNode(rawNode, fallbackIndex = 0) {\n    const title = truncateText(rawNode?.title || rawNode?.label || rawNode?.id || `记忆 ${fallbackIndex + 1}`, 80);\n    const aliases = uniqueStrings(Array.isArray(rawNode?.aliases)\n        ? rawNode.aliases\n        : String(rawNode?.aliases || '').split(/[,\\n;；、，|/]+/u)).slice(0, 12);\n    const history = Array.isArray(rawNode?.history)\n        ? rawNode.history\n            .filter(item => item && typeof item === 'object')\n            .slice(-8)\n            .map(item => ({\n                floor: Number.isFinite(Number(item.floor)) ? Number(item.floor) : undefined,\n                status: truncateText(item.status || '', 180),\n                summary: truncateText(item.summary || '', 180),\n                evidence: truncateText(item.evidence || '', 220),\n                at: truncateText(item.at || '', 40),\n            }))\n        : [];\n    return {\n        id: createMemoryId(rawNode?.id || title, `node_${fallbackIndex + 1}`),\n        title,\n        type: normalizeMemoryNodeType(rawNode),\n        content: truncateText(rawNode?.content || rawNode?.description || '', 1200),\n        summary: truncateText(rawNode?.summary || title, 120),\r\n        location: truncateText(rawNode?.location || rawNode?.scene || '', 120),\r\n        timeSpan: truncateText(rawNode?.timeSpan || rawNode?.time_span || rawNode?.time || '', 120),\r\n        keys: uniqueStrings(Array.isArray(rawNode?.keys)\r\n            ? rawNode.keys\r\n            : String(rawNode?.keys || '').split(/[,\\n;]+/u)),\r\n        tags: uniqueStrings(Array.isArray(rawNode?.tags)\n            ? rawNode.tags\n            : String(rawNode?.tags || '').split(/[,\\n;]+/u)),\n        aliases,\n        evidence: truncateText(rawNode?.evidence || rawNode?.source || rawNode?.quote || '', 260),\n        status: truncateText(rawNode?.status || rawNode?.state || rawNode?.current_status || '', 200),\n        firstSeenFloor: Number.isFinite(Number(rawNode?.firstSeenFloor ?? rawNode?.first_seen_floor))\n            ? Number(rawNode?.firstSeenFloor ?? rawNode?.first_seen_floor)\n            : undefined,\n        lastSeenFloor: Number.isFinite(Number(rawNode?.lastSeenFloor ?? rawNode?.last_seen_floor ?? rawNode?.floor))\n            ? Number(rawNode?.lastSeenFloor ?? rawNode?.last_seen_floor ?? rawNode?.floor)\n            : undefined,\n        history,\n        importance: clampNumber(rawNode?.importance, 0.6, 0, 1),\n        credibility: clampNumber(rawNode?.credibility, 0.8, 0, 1),\n        updatedAt: new Date().toISOString(),\n    };\n}\n\r\nfunction normalizeMemoryNodeType(rawNode = {}) {\r\n    const explicitType = String(rawNode?.type || rawNode?.category || rawNode?.kind || '').trim().toLowerCase();\r\n    const typeAliases = {\r\n        person: 'character',\r\n        people: 'character',\r\n        npc: 'character',\r\n        role: 'character',\r\n        char: 'character',\r\n        place: 'location',\r\n        scene: 'location',\r\n        area: 'location',\r\n        prop: 'item',\r\n        object: 'item',\r\n        equipment: 'item',\r\n        artifact: 'item',\r\n        organization: 'faction',\r\n        org: 'faction',\r\n        group: 'faction',\r\n        force: 'faction',\r\n        setting: 'concept',\r\n        lore: 'concept',\r\n        idea: 'concept',\r\n        law: 'rule',\r\n        constraint: 'rule',\r\n        mission: 'quest',\r\n        plot: 'event',\r\n        timeline: 'event',\r\n    };\r\n    const allowedTypes = new Set(MEMORY_NODE_TYPE_OPTIONS.map(option => option.value));\r\n    const normalizedExplicit = typeAliases[explicitType] || explicitType;\r\n    if (allowedTypes.has(normalizedExplicit)) {\r\n        return normalizedExplicit;\r\n    }\r\n    if (/^(人物|角色|主角|配角|npc|玩家)$/iu.test(explicitType)) return 'character';\r\n    if (/^(地点|场景|区域|位置|房间|城市|据点)$/iu.test(explicitType)) return 'location';\r\n    if (/^(道具|物品|装备|武器|线索|资料)$/iu.test(explicitType)) return 'item';\r\n    if (/^(势力|组织|阵营|队伍|家族|帮派)$/iu.test(explicitType)) return 'faction';\r\n    if (/^(概念|设定|世界观|能力|机制)$/iu.test(explicitType)) return 'concept';\r\n    if (/^(规则|法则|限制|约定)$/iu.test(explicitType)) return 'rule';\r\n    if (/^(任务|目标|悬念|问题)$/iu.test(explicitType)) return 'quest';\r\n\r\n    const text = normalizeText([\r\n        rawNode?.title,\r\n        rawNode?.label,\r\n        rawNode?.summary,\r\n        rawNode?.content,\r\n        rawNode?.description,\r\n        ...(Array.isArray(rawNode?.keys) ? rawNode.keys : []),\r\n        ...(Array.isArray(rawNode?.tags) ? rawNode.tags : []),\r\n    ].filter(Boolean).join(' '));\r\n\r\n    if (/\\b(character|person|npc|protagonist|hero|heroine|villain|ally|enemy|friend|teacher|student|doctor|captain|king|queen|girl|boy|man|woman)\\b/iu.test(text)) {\r\n        return 'character';\r\n    }\r\n    if (/(人物|角色|主角|配角|同伴|敌人|朋友|老师|学生|医生|队长|国王|女王|少女|少年|男人|女人|名字|身份)/u.test(text)) {\r\n        return 'character';\r\n    }\r\n    if (/\\b(place|location|scene|area|city|room|street|building|school|shop|base|station|home|house|castle|forest|village|bar|hotel)\\b/iu.test(text)) {\r\n        return 'location';\r\n    }\r\n    if (/(地点|场景|位置|区域|城市|房间|街道|建筑|学校|商店|据点|车站|家里|城堡|森林|村庄|酒馆|旅馆|医院|办公室|教室)/u.test(text)) {\r\n        return 'location';\r\n    }\r\n    if (/\\b(item|prop|tool|weapon|key|letter|book|phone|ring|card|token|medicine|artifact|equipment|sword|gun|map|note)\\b/iu.test(text)) {\r\n        return 'item';\r\n    }\r\n    if (/(道具|物品|工具|武器|钥匙|信件|书本|手机|戒指|卡片|令牌|药物|神器|装备|剑|枪|地图|笔记|线索)/u.test(text)) {\r\n        return 'item';\r\n    }\r\n    if (/\\b(faction|organization|guild|team|club|company|family|army|gang|group|order|kingdom|empire)\\b/iu.test(text)) {\r\n        return 'faction';\r\n    }\r\n    if (/(势力|组织|公会|队伍|社团|公司|家族|军队|帮派|集团|教团|王国|帝国|阵营)/u.test(text)) {\r\n        return 'faction';\r\n    }\r\n    if (/\\b(rule|law|constraint|taboo|protocol|contract|agreement|promise)\\b/iu.test(text)) {\r\n        return 'rule';\r\n    }\r\n    if (/(规则|法则|限制|禁忌|协议|契约|约定|承诺|条件)/u.test(text)) {\r\n        return 'rule';\r\n    }\r\n    if (/\\b(concept|setting|lore|system|ability|power|magic|skill|mechanism|curse|ritual)\\b/iu.test(text)) {\r\n        return 'concept';\r\n    }\r\n    if (/(概念|设定|世界观|系统|能力|力量|魔法|技能|机制|诅咒|仪式|背景)/u.test(text)) {\r\n        return 'concept';\r\n    }\r\n    if (/\\b(quest|mission|goal|objective|task|open question|problem)\\b/iu.test(text)) {\r\n        return 'quest';\r\n    }\r\n    if (/(任务|目标|目的|待办|悬念|未解|问题|谜团)/u.test(text)) {\r\n        return 'quest';\r\n    }\r\n    return 'event';\r\n}\r\n\r\nfunction looksLikeMemoryEventNode(node = {}) {\r\n    const type = String(node?.type || '').toLowerCase();\r\n    if (type !== 'character') {\r\n        return false;\r\n    }\r\n    const title = String(node?.title || '').trim();\r\n    const summary = String(node?.summary || '').trim();\r\n    const content = String(node?.content || '').trim();\r\n    const text = normalizeText([title, summary, content, ...(Array.isArray(node?.keys) ? node.keys : []), ...(Array.isArray(node?.tags) ? node.tags : [])].join(' '));\r\n    const hasEventTypeCue = /\\b(event|plot|scene|timeline|quest|mission|incident|memory|update|summary|conversation)\\b/iu.test(text)\r\n        || /(事件|剧情|场景|时间线|任务|进展|回忆|记忆|摘要|对话|发生|遇到|发现|到达|离开|战斗|决定|约定|计划|调查)/u.test(text);\r\n    const hasActionCue = /(发生|遇到|发现|到达|离开|进入|拿到|失去|告诉|承诺|决定|开始|结束|寻找|调查|战斗|争吵|合作|逃离|回忆|确认|揭露)/u.test(text)\r\n        || /\\b(met|found|arrived|left|entered|told|promised|decided|started|ended|searched|fought|escaped|revealed|confirmed|asked|answered)\\b/iu.test(text);\r\n    const longNarrative = content.length >= 80 || summary.length >= 60;\r\n    const shortNameLikeTitle = title.length > 0 && title.length <= 18 && !/[，。！？,.!?;；:：]/u.test(title);\r\n    return hasEventTypeCue || (hasActionCue && longNarrative) || (!shortNameLikeTitle && longNarrative && !!node?.timeSpan);\r\n}\r\n\r\nfunction rebalanceMemoryGraphNodeTypes(graph = getDefaultMemoryGraph()) {\r\n    const nodes = Array.isArray(graph?.nodes) ? graph.nodes : [];\r\n    if (nodes.length < 4) {\r\n        return false;\r\n    }\r\n    const typeCounts = nodes.reduce((acc, node) => {\r\n        const type = String(node?.type || 'event');\r\n        acc[type] = (acc[type] || 0) + 1;\r\n        return acc;\r\n    }, {});\r\n    const characterRatio = Number(typeCounts.character || 0) / Math.max(1, nodes.length);\r\n    if (characterRatio < 0.72) {\r\n        return false;\r\n    }\r\n    let changed = false;\r\n    for (const node of nodes) {\r\n        if (looksLikeMemoryEventNode(node)) {\r\n            node.type = 'event';\r\n            changed = true;\r\n        }\r\n    }\r\n    return changed;\r\n}\r\n\r\nfunction normalizeMemoryLink(rawLink, nodeIds, fallbackIndex = 0) {\r\n    const source = createMemoryId(rawLink?.source || rawLink?.sourceId || rawLink?.from || '');\r\n    const target = createMemoryId(rawLink?.target || rawLink?.targetId || rawLink?.to || '');\r\n    if (!source || !target || source === target || !nodeIds.has(source) || !nodeIds.has(target)) {\r\n        return null;\r\n    }\r\n\r\n    const type = String(rawLink?.type || rawLink?.label || 'RELATED').trim().toUpperCase();\r\n    return {\r\n        id: truncateText(rawLink?.id || `${source}_${type}_${target}_${fallbackIndex}`, 120),\r\n        source,\r\n        target,\r\n        type: MEMORY_LINK_TYPES.has(type) ? type : 'RELATED',\r\n        weight: clampNumber(rawLink?.weight, 0.7, 0, 1),\r\n        description: truncateText(rawLink?.description || rawLink?.reason || '', 480),\r\n        updatedAt: new Date().toISOString(),\r\n    };\r\n}\r\n\r\nfunction normalizeMemoryFacetValue(value, maxLength = 120) {\r\n    const text = String(value || '').replace(/\\s+/g, ' ').trim();\r\n    if (!text) {\r\n        return '';\r\n    }\r\n    const compact = normalizeText(text);\r\n    if (!compact || /^(unknown|none|null|n\\/a|na|undefined|not\\s+specified|unspecified)$/iu.test(compact)) {\r\n        return '';\r\n    }\r\n    return truncateText(text, maxLength);\r\n}\r\n\r\nfunction getMemoryNodeTemporalContext(rawNode = {}, graph = getMemoryGraph(), state = {}) {\r\n    const normalizedTitle = normalizeText(rawNode?.title || rawNode?.label || rawNode?.id || rawNode?.titleToUpdate || '');\r\n    const existing = normalizedTitle\r\n        ? (Array.isArray(graph?.nodes) ? graph.nodes : []).find(node => normalizeText(node.title) === normalizedTitle || normalizeText(node.id) === normalizedTitle)\r\n        : null;\r\n    const location = normalizeMemoryFacetValue(\r\n        rawNode?.location\r\n        || rawNode?.scene\r\n        || rawNode?.place\r\n        || existing?.location\r\n        || state?.current_location\r\n        || graph?.state?.current_location,\r\n    );\r\n    const timeSpan = normalizeMemoryFacetValue(\r\n        rawNode?.timeSpan\r\n        || rawNode?.time_span\r\n        || rawNode?.time\r\n        || rawNode?.period\r\n        || existing?.timeSpan\r\n        || state?.current_time\r\n        || graph?.state?.current_time\r\n        || state?.current_phase\r\n        || graph?.state?.current_phase,\r\n    );\r\n    return { location, timeSpan };\r\n}\r\n\r\nfunction applyMemoryTemporalContext(rawNode = {}, graph = getMemoryGraph(), state = {}) {\r\n    if (!rawNode || typeof rawNode !== 'object') {\r\n        return rawNode;\r\n    }\r\n    const temporal = getMemoryNodeTemporalContext(rawNode, graph, state);\r\n    return {\r\n        ...rawNode,\r\n        location: normalizeMemoryFacetValue(rawNode.location || rawNode.scene || rawNode.place) || temporal.location,\r\n        timeSpan: normalizeMemoryFacetValue(rawNode.timeSpan || rawNode.time_span || rawNode.time || rawNode.period) || temporal.timeSpan,\r\n    };\r\n}\r\n\r\nfunction buildMemoryAutoTemporalLinks(nodes = [], existingLinks = [], maxLinks = 10) {\r\n    const result = [];\r\n    const existingKeys = new Set((Array.isArray(existingLinks) ? existingLinks : []).map(link => {\r\n        const pair = [String(link.source || ''), String(link.target || '')].sort().join('|');\r\n        return `${pair}|${String(link.type || 'RELATED').toUpperCase()}`;\r\n    }));\r\n    const candidates = (Array.isArray(nodes) ? nodes : [])\r\n        .filter(node => node?.id)\r\n        .slice(0, 80);\r\n    for (let i = 0; i < candidates.length && result.length < maxLinks; i += 1) {\r\n        for (let j = i + 1; j < candidates.length && result.length < maxLinks; j += 1) {\r\n            const source = candidates[i];\r\n            const target = candidates[j];\r\n            const sameLocation = normalizeMemoryFacetValue(source.location) && normalizeText(source.location) === normalizeText(target.location);\r\n            const sameTime = normalizeMemoryFacetValue(source.timeSpan) && normalizeText(source.timeSpan) === normalizeText(target.timeSpan);\r\n            if (!sameLocation && !sameTime) {\r\n                continue;\r\n            }\r\n            const pair = [String(source.id), String(target.id)].sort().join('|');\r\n            const key = `${pair}|RELATED`;\r\n            if (existingKeys.has(key)) {\r\n                continue;\r\n            }\r\n            existingKeys.add(key);\r\n            result.push({\r\n                id: truncateText(`temporal_${source.id}_${target.id}_${result.length}`, 120),\r\n                source: source.id,\r\n                target: target.id,\r\n                type: 'RELATED',\r\n                weight: sameLocation && sameTime ? 0.58 : 0.42,\r\n                description: sameLocation && sameTime\r\n                    ? `Same scene and time: ${source.location} / ${source.timeSpan}`\r\n                    : sameLocation\r\n                        ? `Same location: ${source.location}`\r\n                        : `Same time: ${source.timeSpan}`,\r\n                updatedAt: new Date().toISOString(),\r\n                autoTemporal: true,\r\n            });\r\n        }\r\n    }\r\n    return result;\r\n}\r\n\r\nfunction getMemoryTemporalLayoutKey(node) {\r\n    const location = normalizeMemoryFacetValue(node?.location);\r\n    const timeSpan = normalizeMemoryFacetValue(node?.timeSpan);\r\n    if (location && timeSpan) {\r\n        return `${normalizeText(location)}::${normalizeText(timeSpan)}`;\r\n    }\r\n    if (location) {\r\n        return `loc::${normalizeText(location)}`;\r\n    }\r\n    if (timeSpan) {\r\n        return `time::${normalizeText(timeSpan)}`;\r\n    }\r\n    return '';\r\n}\r\n\r\nfunction isWeakMemoryTitle(title) {\r\n    const text = normalizeText(title);\r\n    if (!text || text.length < 2) return true;\r\n    return /^(本轮关键事件|关键事件|记忆|事件|摘要|本轮摘要|未命名|unknown|memory|event)$/iu.test(text);\r\n}\r\n\r\nfunction isValidMemoryNodeForApply(node) {\n    if (!node || isWeakMemoryTitle(node.title)) return false;\n    const body = normalizeText(`${node.content || ''} ${node.summary || ''}`);\n    const hasSubstance = body.length >= 4 || (Array.isArray(node.keys) && node.keys.join('').length >= 2);\n    return hasSubstance;\n}\n\nfunction normalizeMemoryStateScalar(value, maxLength = 80) {\n    const text = String(value || '')\n        .replace(/\\s+/gu, ' ')\n        .replace(/^[：:，,。.;；\\s]+/gu, '')\n        .trim();\n    if (!text) return '';\n    const firstClause = text\n        .split(/[。！？!?；;\\n]/u)\n        .map(part => part.trim())\n        .find(Boolean) || text;\n    return truncateText(firstClause, maxLength);\n}\n\nfunction normalizeMemoryStateList(value, maxItems = 8, maxLength = 80) {\n    const source = Array.isArray(value) ? value : String(value || '').split(/[,\\n;；、，]+/u);\n    return uniqueStrings(source\n        .map(item => normalizeMemoryStateScalar(item, maxLength))\n        .filter(Boolean))\n        .slice(0, maxItems);\n}\n\nfunction splitMemoryRuleKeywords(value) {\n    const source = Array.isArray(value) ? value : String(value || '').split(/[\\n,，、;；|/]+/u);\n    return uniqueStrings(source\n        .map(item => String(item || '').trim())\n        .filter(Boolean))\n        .slice(0, 32);\n}\n\nfunction escapeRegex(value) {\n    return String(value ?? '').replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');\n}\n\nfunction getDefaultMemoryRuleConfig() {\n    const commonBlocks = ['当前', '状态', '阶段', '摘要', '剧情', '本轮', '字段', '列表', '信息', '内容'];\n    const aiCardWritingGuide = [\n        '由 AI 在每回合实时记忆更新时直接填入记忆卡片；本地规则只负责约束、校验、净化和合并。',\n        '优先输出固定分栏字段：memory_characters_json、memory_locations_json、memory_items_json、memory_factions_json、memory_concepts_json、memory_rules_json、memory_quests_json、memory_events_json。',\n        '每次更新都按“角色→地点→道具→势力→设定→规则→任务→事件”的顺序巡检；事件只记录发生了什么，不承载全部信息。',\n        '角色标题只能写人名或称呼，例如“林悦”；不要把“提出入住”“是否会答应”等动作、疑问、状态写进角色标题。',\n        '地点标题只能写地点名；道具标题只能写物品名；势力标题只能写组织/家族/阵营名。',\n        '设定和规则只写会长期复用的世界观、能力、限制、契约、机制；一次性动作写进事件。',\n        '任务标题写未完成目标或待办，例如“请求入住林悦家”；已发生经过写事件。',\n        '同类型同标题必须 upsert 更新旧卡，不要重复新建近义卡。',\n        '证据不足时跳过该类型，不要为了填满分类而编造。'\n    ].join('\\n');\n    const titleCleaners = {\n        character: ['是否会', '能否', '是否', '想要', '准备', '打算', '需要', '希望', '说', '问', '回答', '表示', '提出', '同意', '拒绝', '解释', '告诉', '询问', '请求', '答应', '反对', '进入', '离开', '出现', '看向', '递给', '收留', '入住', '帮助', '攻击', '带领', '阻止', '会'],\n        location: ['前往', '进入', '来到', '离开', '抵达', '返回', '附近', '里面', '门口', '位于', '到达', '穿过'],\n        item: ['获得', '拿出', '使用', '交给', '递给', '打开', '丢失', '捡起', '发现', '收下', '藏起'],\n        faction: ['派出', '追捕', '加入', '隶属', '敌对', '合作', '控制', '掌控', '效忠', '代表'],\n        concept: ['必须', '不可', '需要', '可以', '依赖', '受到', '称为', '意味着', '分为', '源于'],\n        rule: ['必须', '不可', '需要', '只有', '否则', '禁止', '触发', '生效', '遵守', '违背'],\n        quest: ['调查', '寻找', '解决', '保护', '送达', '确认', '需要', '必须', '准备', '决定', '打算', '请求', '委托', '目标是'],\n    };\n    return {\n        enabled: true,\n        entityRules: [\n            {\n                id: 'character_default',\n                label: '角色卡片',\n                targetType: 'character',\n                enabled: true,\n                keywords: ['角色', '人物', 'NPC', '主角', '配角', '同伴', '敌人', '朋友', '老师', '学生', '姓名', '身份'],\n                positiveKeywords: ['说', '问', '回答', '表示', '提出', '是否会', '是否', '出现', '同意', '拒绝', '请求', '答应', '解释', '帮助', '攻击', '带领', '阻止', '告诉', '询问', '看向', '递给', '收留', '介绍', '名为', '叫做'],\n                negativeKeywords: ['角色设定', '人物关系', '当前状态', '主角目标', '身份设定', '任务目标', '地点状态', '角色栏', '人物栏'],\n                titleMinLength: 2,\n                titleMaxLength: 12,\n                minConfidence: 68,\n                lowConfidenceMode: 'skip',\n            },\n            {\n                id: 'location_default',\n                label: '地点卡片',\n                targetType: 'location',\n                enabled: true,\n                keywords: ['地点', '位置', '场景', '房间', '城市', '据点', '学校', '公寓', '宿舍', '家中', '客厅', '卧室', '门口', '入口', '街区', '森林', '村庄'],\n                positiveKeywords: ['进入', '前往', '来到', '离开', '抵达', '位于', '住在', '返回', '穿过', '靠近', '藏在'],\n                negativeKeywords: ['当前位置', '地点状态', '地图设定', '地点栏', '场景描述', '位置字段'],\n                titleMinLength: 2,\n                titleMaxLength: 18,\n                minConfidence: 64,\n                lowConfidenceMode: 'skip',\n            },\n            {\n                id: 'item_default',\n                label: '道具卡片',\n                targetType: 'item',\n                enabled: true,\n                keywords: ['道具', '物品', '装备', '武器', '线索', '资料', '信件', '钥匙', '令牌', '戒指', '卡片', '药物', '地图', '笔记', '包裹', '礼物'],\n                positiveKeywords: ['获得', '拿出', '使用', '交给', '递给', '捡起', '发现', '丢失', '打开', '收下', '藏起', '交换'],\n                negativeKeywords: ['道具栏', '任务道具', '道具设定', '物品列表', '装备栏', '线索栏'],\n                titleMinLength: 2,\n                titleMaxLength: 18,\n                minConfidence: 64,\n                lowConfidenceMode: 'skip',\n            },\n            {\n                id: 'faction_default',\n                label: '势力卡片',\n                targetType: 'faction',\n                enabled: true,\n                keywords: ['势力', '组织', '阵营', '队伍', '家族', '帮派', '集团', '公司', '社团', '教团', '王国', '帝国', '协会', '议会'],\n                positiveKeywords: ['加入', '隶属', '效忠', '敌对', '掌控', '派出', '追捕', '合作', '背叛', '来自', '代表'],\n                negativeKeywords: ['势力设定', '阵营关系', '势力分布', '组织结构', '势力栏', '阵营栏'],\n                titleMinLength: 2,\n                titleMaxLength: 20,\n                minConfidence: 66,\n                lowConfidenceMode: 'skip',\n            },\n            {\n                id: 'concept_default',\n                label: '设定卡片',\n                targetType: 'concept',\n                enabled: true,\n                keywords: ['设定', '概念', '世界观', '能力', '机制', '系统', '习俗', '背景', '身份设定', '关系设定', '货币', '历法', '等级'],\n                positiveKeywords: ['需要', '依赖', '不可', '可以', '必须', '受到', '源于', '遵循', '分为', '称为', '意味着'],\n                negativeKeywords: ['本轮事件', '剧情摘要', '普通动作', '临时状态', '任务目标'],\n                titleMinLength: 2,\n                titleMaxLength: 28,\n                minConfidence: 62,\n                lowConfidenceMode: 'skip',\n            },\n            {\n                id: 'rule_default',\n                label: '规则卡片',\n                targetType: 'rule',\n                enabled: true,\n                keywords: ['规则', '法则', '限制', '禁忌', '协议', '契约', '约定', '承诺', '条件'],\n                positiveKeywords: ['禁止', '必须', '不可', '需要', '一旦', '只有', '否则', '遵守', '违背', '触发', '生效'],\n                negativeKeywords: ['规则界面', '写入规则', '筛选规则', '插件规则', '规则卡片'],\n                titleMinLength: 2,\n                titleMaxLength: 30,\n                minConfidence: 64,\n                lowConfidenceMode: 'skip',\n            },\n            {\n                id: 'quest_default',\n                label: '任务卡片',\n                targetType: 'quest',\n                enabled: true,\n                keywords: ['任务', '目标', '目的', '待办', '悬念', '未解', '问题', '谜团', '调查', '寻找', '解决', '请求', '委托'],\n                positiveKeywords: ['需要', '必须', '准备', '打算', '决定', '承诺', '委托', '调查', '寻找', '解决', '送达', '保护', '确认'],\n                negativeKeywords: ['任务状态', '任务栏', '目标栏', '主线任务设定', '任务设定', '当前目标字段'],\n                titleMinLength: 2,\n                titleMaxLength: 32,\n                minConfidence: 66,\n                lowConfidenceMode: 'skip',\n            },\n        ],\n        blockKeywords: uniqueStrings([...commonBlocks, '当前目标', '当前位置', '地点状态', '任务状态', '角色设定', '人物关系', '道具栏', '势力设定', '事件', '特产', '鞋子', '公寓', '大学', '玄关', '客厅']),\n        aiCardWritingGuide,\n        titleCleaners,\n        exampleRules: [\n            { id: 'ex_character_statement', enabled: true, source: '林悦提出入住', targetType: 'character', expectedTitle: '林悦' },\n            { id: 'ex_character_question', enabled: true, source: '林悦是否会答应', targetType: 'character', expectedTitle: '林悦' },\n            { id: 'ex_location_move', enabled: true, source: '主角前往旧城区', targetType: 'location', expectedTitle: '旧城区' },\n            { id: 'ex_item_get', enabled: true, source: '获得银钥匙', targetType: 'item', expectedTitle: '银钥匙' },\n            { id: 'ex_faction_action', enabled: true, source: '白塔议会派人追捕主角', targetType: 'faction', expectedTitle: '白塔议会' },\n            { id: 'ex_concept_rule', enabled: true, source: '魔法必须依赖媒介', targetType: 'concept', expectedTitle: '魔法依赖媒介' },\n            { id: 'ex_quest_goal', enabled: true, source: '调查矿洞失踪事件', targetType: 'quest', expectedTitle: '调查矿洞失踪事件' },\n        ],\n    };\n}\n\nfunction getMemoryRuleTypePriority(type) {\n    const priorities = { rule: 90, concept: 84, quest: 78, location: 72, item: 66, faction: 60, character: 54, event: 10 };\n    return priorities[type] || 0;\n}\n\nfunction normalizeMemoryRuleNumber(value, fallback, min, max) {\n    const number = Number(value);\n    if (!Number.isFinite(number)) return fallback;\n    return Math.max(min, Math.min(max, Math.round(number)));\n}\n\nfunction normalizeMemoryRuleLowConfidenceMode(value, fallback = 'skip') {\n    const mode = String(value || fallback).trim();\n    return ['skip', 'review', 'write'].includes(mode) ? mode : fallback;\n}\n\nfunction getMemoryRuleTypeLabel(type) {\n    return getOptionLabel(MEMORY_NODE_TYPE_OPTIONS, type, type || '规则');\n}\n\nfunction getMemoryRuleAllowedTypes() {\n    return MEMORY_NODE_TYPE_OPTIONS.map(option => option.value).filter(type => type !== 'event');\n}\n\nfunction normalizeMemoryRuleTitleCleaners(value = null, fallback = null) {\n    const allowedTypes = getMemoryRuleAllowedTypes();\n    const fallbackMap = fallback && typeof fallback === 'object' ? fallback : {};\n    const source = value && typeof value === 'object' ? value : {};\n    const result = {};\n    for (const type of allowedTypes) {\n        const raw = Object.hasOwn(source, type) ? source[type] : fallbackMap[type];\n        result[type] = splitMemoryRuleKeywords(raw).slice(0, 48);\n    }\n    return result;\n}\n\nfunction parseMemoryRuleTitleCleaners(value, fallback = {}) {\n    const result = normalizeMemoryRuleTitleCleaners({}, fallback);\n    const lines = String(value || '').split(/\\n+/u).map(line => line.trim()).filter(Boolean);\n    for (const line of lines) {\n        const match = line.match(/^([^:：=]+)\\s*[:：=]\\s*(.+)$/u);\n        if (!match) continue;\n        const type = getMemoryEntityTypeFromLabel(match[1]) || String(match[1] || '').trim();\n        if (!getMemoryRuleAllowedTypes().includes(type)) continue;\n        result[type] = splitMemoryRuleKeywords(match[2]).slice(0, 48);\n    }\n    return result;\n}\n\nfunction serializeMemoryRuleTitleCleaners(cleaners = {}) {\n    const normalized = normalizeMemoryRuleTitleCleaners(cleaners);\n    return getMemoryRuleAllowedTypes()\n        .map(type => `${getMemoryRuleTypeLabel(type)}：${(normalized[type] || []).join('、')}`)\n        .join('\\n');\n}\n\nfunction normalizeMemoryRuleExampleRules(value = null, fallback = []) {\n    const allowedTypes = new Set(getMemoryRuleAllowedTypes());\n    const source = Array.isArray(value) && value.length ? value : (Array.isArray(fallback) ? fallback : []);\n    return source\n        .map((item, index) => {\n            const raw = item && typeof item === 'object' ? item : {};\n            const targetType = String(raw.targetType || raw.type || '').trim();\n            const safeType = allowedTypes.has(targetType) ? targetType : '';\n            const sourceText = truncateText(String(raw.source || raw.input || '').trim(), 80);\n            const expectedTitle = normalizeMemoryEntityTitle(raw.expectedTitle || raw.title || raw.output || '');\n            if (!safeType || !sourceText || !expectedTitle) return null;\n            return {\n                id: String(raw.id || `example_${safeType}_${index}`).replace(/[^\\w-]+/g, '_'),\n                enabled: raw.enabled !== false,\n                source: sourceText,\n                targetType: safeType,\n                expectedTitle,\n            };\n        })\n        .filter(Boolean)\n        .slice(0, 40);\n}\n\nfunction parseMemoryRuleExamples(value, fallback = []) {\n    const parsed = [];\n    const lines = String(value || '').split(/\\n+/u).map(line => line.trim()).filter(Boolean);\n    for (const line of lines) {\n        const enabled = !/^#/u.test(line);\n        const body = line.replace(/^#\\s*/u, '');\n        const match = body.match(/^(.+?)\\s*(?:=>|→|->)\\s*([^:：=]+)\\s*[:：=]\\s*(.+)$/u);\n        if (!match) continue;\n        const targetType = getMemoryEntityTypeFromLabel(match[2]) || String(match[2] || '').trim();\n        parsed.push({\n            enabled,\n            source: match[1].trim(),\n            targetType,\n            expectedTitle: match[3].trim(),\n        });\n    }\n    return normalizeMemoryRuleExampleRules(parsed, fallback);\n}\n\nfunction serializeMemoryRuleExamples(examples = []) {\n    return normalizeMemoryRuleExampleRules(examples)\n        .map(rule => `${rule.enabled ? '' : '# '}${rule.source} => ${getMemoryRuleTypeLabel(rule.targetType)}:${rule.expectedTitle}`)\n        .join('\\n');\n}\n\nfunction normalizeMemoryRuleGuideText(value, fallback = '') {\n    const text = String(value || fallback || '').trim();\n    return truncateText(text, 2400);\n}\n\nfunction normalizeMemoryRuleConfig(config = null) {\n    const defaults = getDefaultMemoryRuleConfig();\n    const raw = config && typeof config === 'object' ? config : {};\n    const byId = new Map(defaults.entityRules.map(rule => [rule.id, rule]));\n    const sourceRules = Array.isArray(raw.entityRules) && raw.entityRules.length ? raw.entityRules : defaults.entityRules;\n    const allowedTypes = new Set(MEMORY_NODE_TYPE_OPTIONS.map(option => option.value).filter(type => type !== 'event'));\n    const entityRules = sourceRules\n        .map((rule, index) => {\n            const rawRule = rule && typeof rule === 'object' ? rule : {};\n            const fallback = byId.get(String(rawRule.id || '')) || defaults.entityRules[index] || defaults.entityRules[0];\n            const targetType = String(rawRule.targetType || rawRule.type || fallback.targetType || '').trim();\n            const safeType = allowedTypes.has(targetType) ? targetType : fallback.targetType;\n            const id = String(rawRule.id || `${safeType}_${Date.now().toString(36)}_${index}`).replace(/[^\\w-]+/g, '_');\n            const keywordSource = Object.hasOwn(rawRule, 'keywords') ? rawRule.keywords : fallback.keywords;\n            const positiveSource = Object.hasOwn(rawRule, 'positiveKeywords') ? rawRule.positiveKeywords : fallback.positiveKeywords;\n            const negativeSource = Object.hasOwn(rawRule, 'negativeKeywords') ? rawRule.negativeKeywords : fallback.negativeKeywords;\n            const minLengthFallback = normalizeMemoryRuleNumber(fallback.titleMinLength, 2, 1, 20);\n            const maxLengthFallback = normalizeMemoryRuleNumber(fallback.titleMaxLength, 24, 2, 60);\n            const minLength = normalizeMemoryRuleNumber(rawRule.titleMinLength, minLengthFallback, 1, 20);\n            const maxLength = Math.max(minLength, normalizeMemoryRuleNumber(rawRule.titleMaxLength, maxLengthFallback, 2, 60));\n            return {\n                id,\n                label: truncateText(String(rawRule.label || fallback.label || getOptionLabel(MEMORY_NODE_TYPE_OPTIONS, safeType, safeType)).trim(), 32),\n                targetType: safeType,\n                enabled: rawRule.enabled !== false,\n                keywords: splitMemoryRuleKeywords(keywordSource),\n                positiveKeywords: splitMemoryRuleKeywords(positiveSource),\n                negativeKeywords: splitMemoryRuleKeywords(negativeSource),\n                titleMinLength: minLength,\n                titleMaxLength: maxLength,\n                minConfidence: normalizeMemoryRuleNumber(rawRule.minConfidence, normalizeMemoryRuleNumber(fallback.minConfidence, 64, 1, 100), 1, 100),\n                lowConfidenceMode: normalizeMemoryRuleLowConfidenceMode(rawRule.lowConfidenceMode, fallback.lowConfidenceMode || 'skip'),\n            };\n        })\n        .filter(rule => rule.targetType)\n        .slice(0, 24);\n    const blockKeywordSource = Object.hasOwn(raw, 'blockKeywords') ? raw.blockKeywords : defaults.blockKeywords;\n    return {\n        enabled: raw.enabled !== false,\n        entityRules: entityRules.length ? entityRules : defaults.entityRules,\n        blockKeywords: splitMemoryRuleKeywords(blockKeywordSource).slice(0, 80),\n        aiCardWritingGuide: normalizeMemoryRuleGuideText(raw.aiCardWritingGuide, defaults.aiCardWritingGuide),\n        titleCleaners: normalizeMemoryRuleTitleCleaners(raw.titleCleaners, defaults.titleCleaners),\n        exampleRules: normalizeMemoryRuleExampleRules(raw.exampleRules, defaults.exampleRules),\n    };\n}\n\nfunction getMemoryRuleConfig() {\n    settings.memoryRuleConfig = normalizeMemoryRuleConfig(settings.memoryRuleConfig);\n    return settings.memoryRuleConfig;\n}\n\nfunction saveMemoryRuleConfig(config) {\n    const normalized = normalizeMemoryRuleConfig(config);\n    saveSetting('memoryRuleConfig', normalized);\n    return normalized;\n}\n\nfunction collectMemoryRuleConfigFromDrawer() {\n    const drawer = $('#ai_wbr_memory_rule_drawer');\n    const current = getMemoryRuleConfig();\n    if (!drawer.length) {\n        return current;\n    }\n    return normalizeMemoryRuleConfig({\n        enabled: !!drawer.find('#ai_wbr_memory_rule_enabled').prop('checked'),\n        entityRules: current.entityRules,\n        blockKeywords: current.blockKeywords,\n        aiCardWritingGuide: normalizeMemoryRuleGuideText(drawer.find('#ai_wbr_memory_rule_ai_guide').val(), current.aiCardWritingGuide),\n        titleCleaners: current.titleCleaners,\n        exampleRules: current.exampleRules,\n    });\n}\n\nfunction saveMemoryRuleConfigFromDrawer({ rerender = false } = {}) {\n    const config = saveMemoryRuleConfig(collectMemoryRuleConfigFromDrawer());\n    if (rerender) {\n        renderMemoryGraphSvg(getMemoryGraph());\n    }\n    return config;\n}\n\nfunction scheduleMemoryRuleConfigSave() {\n    clearTimeout(memoryRuleSaveTimer);\n    memoryRuleSaveTimer = setTimeout(() => {\n        memoryRuleSaveTimer = null;\n        saveMemoryRuleConfigFromDrawer();\n    }, 220);\n}\n\nfunction addMemoryRuleConfigRow() {\n    const config = saveMemoryRuleConfigFromDrawer();\n    const type = 'concept';\n    config.entityRules.push({\n        id: `custom_${type}_${Date.now().toString(36)}`,\n        label: '自定义卡片',\n        targetType: type,\n        enabled: true,\n        keywords: ['自定义触发词'],\n        positiveKeywords: [],\n        negativeKeywords: [],\n        titleMinLength: 2,\n        titleMaxLength: 24,\n        minConfidence: 62,\n        lowConfidenceMode: 'skip',\n    });\n    saveMemoryRuleConfig(config);\n    renderMemoryGraphSvg(getMemoryGraph());\n}\n\nfunction getMemoryRuleTextWindow(text, index, length = 34) {\n    const source = String(text || '');\n    const start = Math.max(0, Number(index || 0) - length);\n    const end = Math.min(source.length, Number(index || 0) + length);\n    return source.slice(start, end);\n}\n\nfunction getMemoryRuleMatchedWords(text, words = []) {\n    const normalizedText = normalizeText(text);\n    return splitMemoryRuleKeywords(words).filter(word => {\n        const normalized = normalizeText(word);\n        return normalized && normalizedText.includes(normalized);\n    });\n}\n\nfunction getExistingMemoryTitleForCandidate(type, title, graph = getMemoryGraph()) {\n    const normalizedTitle = normalizeText(title);\n    if (!type || !normalizedTitle) return '';\n    const nodes = Array.isArray(graph?.nodes) ? graph.nodes : [];\n    const matches = nodes\n        .filter(node => normalizeMemoryNodeType(node) === type)\n        .map(node => String(node?.title || node?.label || '').trim())\n        .filter(Boolean)\n        .filter(existing => {\n            const normalizedExisting = normalizeText(existing);\n            return normalizedExisting\n                && normalizedExisting !== normalizedTitle\n                && (normalizedTitle.startsWith(normalizedExisting) || normalizedTitle.includes(normalizedExisting));\n        })\n        .sort((a, b) => normalizeText(b).length - normalizeText(a).length);\n    return matches[0] || '';\n}\n\nfunction cleanMemoryRuleTitleByType(rawTitle, type, options = {}) {\n    const config = options.config || getMemoryRuleConfig();\n    const graph = options.graph || getMemoryGraph();\n    const sourceTitle = stripMemoryEntityTail(rawTitle);\n    if (!sourceTitle) {\n        return { title: '', reason: '空标题', originalTitle: String(rawTitle || '') };\n    }\n    const reusedBefore = getExistingMemoryTitleForCandidate(type, sourceTitle, graph);\n    if (reusedBefore) {\n        return { title: reusedBefore, reason: `复用已有卡片：${reusedBefore}`, originalTitle: sourceTitle };\n    }\n    const cleaners = splitMemoryRuleKeywords(config?.titleCleaners?.[type]);\n    let cleaned = sourceTitle;\n    let reason = '';\n    for (const word of cleaners) {\n        const index = cleaned.indexOf(word);\n        if (index < 0) continue;\n        const before = cleaned.slice(0, index).trim();\n        const after = cleaned.slice(index + word.length).trim();\n        if (type === 'character' || type === 'faction') {\n            if (before.length >= 2) {\n                cleaned = before;\n                reason = `标题净化：遇到“${word}”截断`;\n                break;\n            }\n        } else if (type === 'location' || type === 'item') {\n            if (after.length >= 2) {\n                cleaned = after;\n                reason = `标题净化：移除动作“${word}”前缀`;\n                break;\n            }\n            if (before.length >= 2) {\n                cleaned = before;\n                reason = `标题净化：遇到“${word}”截断`;\n                break;\n            }\n        } else if (type === 'concept' || type === 'rule') {\n            if (before.length >= 2 && after.length >= 2) {\n                cleaned = `${before}${after}`;\n                reason = `标题净化：压缩规则词“${word}”`;\n                break;\n            }\n            if (after.length >= 2) {\n                cleaned = after;\n                reason = `标题净化：移除“${word}”前缀`;\n                break;\n            }\n        } else if (type === 'quest') {\n            if (after.length >= 2) {\n                cleaned = after;\n                reason = `标题净化：移除目标动词“${word}”`;\n                break;\n            }\n        }\n    }\n    cleaned = normalizeMemoryEntityTitle(cleaned);\n    const reusedAfter = getExistingMemoryTitleForCandidate(type, cleaned, graph);\n    if (reusedAfter) {\n        return { title: reusedAfter, reason: `复用已有卡片：${reusedAfter}`, originalTitle: sourceTitle };\n    }\n    return {\n        title: cleaned,\n        reason,\n        originalTitle: sourceTitle,\n    };\n}\n\nfunction getMemoryRuleCandidateTitle(rawTitle, rule, config = getMemoryRuleConfig()) {\n    const cleaned = cleanMemoryRuleTitleByType(rawTitle, rule?.targetType, { config });\n    if (!cleaned.title) return { title: '', originalTitle: String(rawTitle || ''), cleanReason: cleaned.reason || '' };\n    return {\n        title: truncateText(cleaned.title, normalizeMemoryRuleNumber(rule?.titleMaxLength, 24, 2, 60)),\n        originalTitle: cleaned.originalTitle,\n        cleanReason: cleaned.reason || '',\n    };\n}\n\nfunction scoreMemoryRuleCandidate({ rule, title, contextText = '', explicit = false, keyword = '' }) {\n    const safeTitle = normalizeMemoryEntityTitle(title);\n    const minLength = normalizeMemoryRuleNumber(rule?.titleMinLength, 2, 1, 20);\n    const maxLength = normalizeMemoryRuleNumber(rule?.titleMaxLength, 24, 2, 60);\n    const positiveMatches = getMemoryRuleMatchedWords(contextText, rule?.positiveKeywords);\n    const negativeMatches = uniqueStrings([\n        ...getMemoryRuleMatchedWords(contextText, rule?.negativeKeywords),\n        ...getMemoryRuleMatchedWords(safeTitle, rule?.negativeKeywords),\n    ]);\n    const globalBlocks = getMemoryRuleMatchedWords(safeTitle, getMemoryRuleConfig()?.blockKeywords);\n    let score = explicit ? 78 : 42;\n    if (keyword) score += 10;\n    score += Math.min(22, positiveMatches.length * 9);\n    score += Math.min(8, getMemoryRuleTypePriority(rule?.targetType) / 18);\n    if (safeTitle.length >= minLength && safeTitle.length <= maxLength) score += 10;\n    if (safeTitle.length < minLength || safeTitle.length > maxLength) score -= 26;\n    if (negativeMatches.length) score -= 38;\n    if (globalBlocks.length) score -= 34;\n    if (isWeakMemoryTitle(safeTitle)) score -= 35;\n    if (rule?.targetType === 'character' && /^(角色|人物|主角|配角|同伴|敌人|朋友|老师|学生|身份|姓名|关系|状态|目标)$/u.test(safeTitle)) score -= 45;\n    if (rule?.targetType === 'location' && /^(地点|位置|场景|房间|城市|据点|当前位置)$/u.test(safeTitle)) score -= 42;\n    if (rule?.targetType === 'item' && /^(道具|物品|装备|武器|线索|资料|任务道具)$/u.test(safeTitle)) score -= 42;\n    if (rule?.targetType === 'faction' && /^(势力|组织|阵营|队伍|家族|帮派|势力设定)$/u.test(safeTitle)) score -= 42;\n    if (rule?.targetType === 'quest' && /^(任务|目标|目的|问题|悬念|当前目标)$/u.test(safeTitle)) score -= 42;\n    return {\n        confidence: Math.max(0, Math.min(100, Math.round(score))),\n        positiveMatches,\n        negativeMatches: uniqueStrings([...negativeMatches, ...globalBlocks]),\n        reason: positiveMatches.length\n            ? `上下文命中：${positiveMatches.join('、')}`\n            : (explicit ? '显式标签命中' : '触发词命中'),\n    };\n}\n\nfunction addScoredMemoryEntityHint(hints, seen, type, title, meta = {}) {\n    const before = hints.length;\n    addMemoryEntityHint(hints, seen, type, title);\n    if (hints.length > before) {\n        hints[hints.length - 1] = {\n            ...hints[hints.length - 1],\n            confidence: meta.confidence,\n            matchedKeywords: meta.matchedKeywords || [],\n            positiveMatches: meta.positiveMatches || [],\n            negativeMatches: meta.negativeMatches || [],\n            ruleLabel: meta.ruleLabel || '',\n            reason: meta.reason || '',\n        };\n    }\n}\n\nfunction collectMemoryRuleCandidates(text, config = getMemoryRuleConfig()) {\n    if (!config?.enabled || !text) {\n        return [];\n    }\n    const candidates = [];\n    for (const example of normalizeMemoryRuleExampleRules(config.exampleRules)) {\n        if (!example.enabled || !text.includes(example.source)) continue;\n        const cleaned = cleanMemoryRuleTitleByType(example.expectedTitle, example.targetType, { config });\n        const title = cleaned.title || example.expectedTitle;\n        const rule = {\n            id: example.id,\n            label: '示例句规则',\n            targetType: example.targetType,\n            minConfidence: 72,\n            titleMinLength: 1,\n            titleMaxLength: 60,\n            positiveKeywords: [],\n            negativeKeywords: [],\n        };\n        candidates.push({\n            rule,\n            type: example.targetType,\n            title,\n            originalTitle: example.source,\n            keyword: '示例句',\n            contextText: example.source,\n            explicit: true,\n            confidence: 96,\n            positiveMatches: ['示例句'],\n            negativeMatches: [],\n            reason: `示例句命中：${example.source} → ${getMemoryRuleTypeLabel(example.targetType)}:${title}`,\n            cleanReason: cleaned.reason || '示例句期望标题',\n        });\n    }\n    const rules = (Array.isArray(config.entityRules) ? config.entityRules : [])\n        .filter(rule => rule?.enabled && rule.targetType)\n        .sort((a, b) => getMemoryRuleTypePriority(b.targetType) - getMemoryRuleTypePriority(a.targetType));\n    const ruleByType = new Map(rules.map(rule => [rule.targetType, rule]));\n    for (const [type, words] of Object.entries(normalizeMemoryRuleTitleCleaners(config.titleCleaners))) {\n        const rule = ruleByType.get(type);\n        if (!rule) continue;\n        const maxLength = normalizeMemoryRuleNumber(rule.titleMaxLength, 24, 2, 60);\n        for (const word of splitMemoryRuleKeywords(words)) {\n            const escaped = escapeRegex(word);\n            if (!escaped) continue;\n            const patterns = [];\n            if (type === 'character' || type === 'faction') {\n                const subjectLength = type === 'character' ? Math.min(maxLength, 8) : Math.min(maxLength, 18);\n                patterns.push(new RegExp(`(?:^|[\\\\s，。！？；;,.、：:「“])([\\\\p{Script=Han}A-Za-z0-9_·]{2,${Math.max(2, subjectLength)}})\\\\s*(?:${escaped})`, 'giu'));\n            } else if (type === 'location' || type === 'item' || type === 'quest') {\n                patterns.push(new RegExp(`(?:${escaped})\\\\s*([\\\\p{Script=Han}A-Za-z0-9_·《》“”「」『』]{2,${Math.max(2, maxLength + 4)}})`, 'giu'));\n            } else if (type === 'concept' || type === 'rule') {\n                patterns.push(new RegExp(`([\\\\p{Script=Han}A-Za-z0-9_·]{2,16})\\\\s*(?:${escaped})\\\\s*([\\\\p{Script=Han}A-Za-z0-9_·]{2,22})`, 'giu'));\n            }\n            for (const pattern of patterns) {\n                for (const match of text.matchAll(pattern)) {\n                    const contextText = getMemoryRuleTextWindow(text, match.index, 42);\n                    const rawTitle = type === 'concept' || type === 'rule'\n                        ? `${match[1] || ''}${word}${match[2] || ''}`\n                        : (type === 'character' || type === 'faction'\n                            ? `${match[1] || ''}${word}`\n                            : `${word}${match[1] || ''}`);\n                    const cleaned = getMemoryRuleCandidateTitle(rawTitle, rule, config);\n                    const scored = scoreMemoryRuleCandidate({ rule, title: cleaned.title, contextText, explicit: false, keyword: word });\n                    scored.confidence = Math.min(100, scored.confidence + 8);\n                    candidates.push({\n                        rule,\n                        type,\n                        title: cleaned.title,\n                        originalTitle: cleaned.originalTitle,\n                        cleanReason: cleaned.cleanReason || `标题净化词触发：${word}`,\n                        keyword: word,\n                        contextText,\n                        explicit: false,\n                        ...scored,\n                        reason: `标题净化词命中：${word}`,\n                    });\n                }\n            }\n        }\n    }\n    for (const rule of rules) {\n        const maxLength = normalizeMemoryRuleNumber(rule.titleMaxLength, 24, 2, 60);\n        for (const keyword of splitMemoryRuleKeywords(rule.keywords)) {\n            const escaped = escapeRegex(keyword);\n            if (!escaped) continue;\n            const explicitPattern = new RegExp(`(?:${escaped})\\\\s*[:：\\\\-为是叫名]\\\\s*([\\\\p{Script=Han}A-Za-z0-9_·《》“”「」『』]{2,${Math.max(2, maxLength + 4)}})`, 'giu');\n            for (const match of text.matchAll(explicitPattern)) {\n                const contextText = getMemoryRuleTextWindow(text, match.index, 38);\n                const cleaned = getMemoryRuleCandidateTitle(match[1], rule, config);\n                const scored = scoreMemoryRuleCandidate({ rule, title: cleaned.title, contextText, explicit: true, keyword });\n                candidates.push({ rule, type: rule.targetType, title: cleaned.title, originalTitle: cleaned.originalTitle, cleanReason: cleaned.cleanReason, keyword, contextText, explicit: true, ...scored });\n            }\n            const contextPattern = new RegExp(`([\\\\p{Script=Han}A-Za-z0-9_·]{2,${Math.max(2, maxLength)}})[^。！？；;,.，]{0,16}(?:${escaped})`, 'giu');\n            for (const match of text.matchAll(contextPattern)) {\n                const contextText = getMemoryRuleTextWindow(text, match.index, 42);\n                const cleaned = getMemoryRuleCandidateTitle(match[1], rule, config);\n                const scored = scoreMemoryRuleCandidate({ rule, title: cleaned.title, contextText, explicit: false, keyword });\n                candidates.push({ rule, type: rule.targetType, title: cleaned.title, originalTitle: cleaned.originalTitle, cleanReason: cleaned.cleanReason, keyword, contextText, explicit: false, ...scored });\n            }\n        }\n    }\n    return candidates\n        .filter(candidate => candidate.title)\n        .sort((a, b) => (b.confidence - a.confidence) || (getMemoryRuleTypePriority(b.type) - getMemoryRuleTypePriority(a.type)));\n}\n\nfunction runMemoryRulePreview() {\n    const drawer = $('#ai_wbr_memory_rule_drawer');\n    const resultBox = drawer.find('#ai_wbr_memory_rule_preview_result');\n    const text = String(drawer.find('#ai_wbr_memory_rule_preview_text').val() || '').trim();\n    const config = saveMemoryRuleConfigFromDrawer();\n    if (!text) {\n        resultBox.text('先输入一段剧情再试跑。');\n        return;\n    }\n    const sanitized = sanitizeMemoryMessageText(text);\n    const candidates = collectMemoryRuleCandidates(sanitized, config).slice(0, 24);\n    const hints = [];\n    const seen = new Set();\n    applyCustomMemoryEntityRules(sanitized, hints, seen, config);\n    for (const hint of extractMemoryEntityHintsFromText({ title: '规则试跑', summary: text, content: text }, getMemoryGraph().state)) {\n        addMemoryEntityHint(hints, seen, hint.type, hint.title);\n    }\n    if (!candidates.length && !hints.length) {\n        resultBox.html('<span class=\"ai-wbr-memory-rule-preview-chip skipped\">未拆出卡片。可以增加触发词、上下文词，或写成“类型：标题”。</span>');\n        return;\n    }\n    const acceptedKeys = new Set(hints.map(item => `${item.type}|${normalizeText(item.title)}`));\n    const rows = candidates.map(candidate => {\n        const threshold = normalizeMemoryRuleNumber(candidate.rule?.minConfidence, 64, 1, 100);\n        const accepted = acceptedKeys.has(`${candidate.type}|${normalizeText(candidate.title)}`) && candidate.confidence >= threshold;\n        const reason = candidate.negativeMatches.length\n            ? `拦截：${candidate.negativeMatches.join('、')}`\n            : `${candidate.reason}${candidate.cleanReason ? `；${candidate.cleanReason}` : ''}；阈值 ${threshold}`;\n        const titleLine = candidate.originalTitle && normalizeText(candidate.originalTitle) !== normalizeText(candidate.title)\n            ? `${candidate.originalTitle} → ${candidate.title}`\n            : candidate.title;\n        return `<span class=\"ai-wbr-memory-rule-preview-chip ${accepted ? 'accepted' : 'skipped'}\"><b>${escapeHtml(getOptionLabel(MEMORY_NODE_TYPE_OPTIONS, candidate.type, candidate.type))}</b>${escapeHtml(titleLine)}<em>${candidate.confidence}% · ${escapeHtml(reason)}</em></span>`;\n    });\n    for (const item of hints.slice(0, 18)) {\n        const key = `${item.type}|${normalizeText(item.title)}`;\n        if (candidates.some(candidate => `${candidate.type}|${normalizeText(candidate.title)}` === key)) continue;\n        rows.push(`<span class=\"ai-wbr-memory-rule-preview-chip accepted\"><b>${escapeHtml(getOptionLabel(MEMORY_NODE_TYPE_OPTIONS, item.type, item.type))}</b>${escapeHtml(item.title)}<em>内置拆分补充</em></span>`);\n    }\n    resultBox.html(rows.join(''));\n}\n\nfunction buildMemoryRulePromptGuide(config = getMemoryRuleConfig()) {\n    if (!config?.enabled) {\n        return 'AI 填卡说明已关闭。仍需由 AI 基于上下文直接输出固定分栏字段；本地按字段名强制映射类型、校验 title、合并同类型同标题卡片，并做最小标题保护。';\n    }\n    const aiGuide = normalizeMemoryRuleGuideText(config.aiCardWritingGuide, getDefaultMemoryRuleConfig().aiCardWritingGuide);\n    return `AI 填卡说明：\\n${aiGuide}\\n\\n执行方式：\\n- 基于最近剧情、当前状态和已有图谱，优先填写固定分栏字段，而不是只写 memory_cards_json。\\n- 字段名决定板块：characters=角色，locations=地点，items=道具，factions=势力，concepts=设定，rules=规则，quests=任务，events=事件。\\n- 每个分栏内的 title 必须符合该板块：角色只写人名，地点只写地点名，道具只写物品名，任务写待办目标，事件写本轮剧情进展。\\n- 本地代码按字段名强制映射 type，只负责保存、校验、合并重复卡片和最小标题保护，不再用关键词/示例句扩展生成新卡。\\n- 同类型同标题执行 upsert；证据不足就跳过该分栏，不要硬造卡。`;\n}\n\nfunction splitMemoryEntityTokens(value) {\n    if (Array.isArray(value)) {\n        return value.flatMap(splitMemoryEntityTokens);\n    }\n    return String(value || '')\n        .split(/[,\\n;；、，|/]+/u)\n        .map(item => item.trim())\n        .filter(Boolean);\n}\n\nfunction normalizeMemoryEntityTitle(value) {\n    return truncateText(String(value || '')\n        .replace(/^[#\\-*•\\s]+/u, '')\n        .replace(/^(角色|人物|地点|场景|道具|物品|势力|组织|设定|规则|任务|目标|线索|概念)\\s*[:：\\-]\\s*/u, '')\n        .trim(), 60);\n}\n\nfunction getMemoryEntityTypeFromLabel(label) {\n    const text = normalizeText(label);\n    if (/^(角色|人物|npc|主角|配角|同伴|敌人|玩家|character|person)$/iu.test(text)) return 'character';\n    if (/^(地点|场景|位置|区域|城市|据点|location|place|scene)$/iu.test(text)) return 'location';\n    if (/^(道具|物品|装备|武器|线索|资料|item|prop|object)$/iu.test(text)) return 'item';\n    if (/^(势力|组织|阵营|队伍|家族|帮派|faction|organization|org|group)$/iu.test(text)) return 'faction';\n    if (/^(规则|法则|限制|约定|rule|law)$/iu.test(text)) return 'rule';\n    if (/^(设定|概念|世界观|能力|机制|concept|setting|lore)$/iu.test(text)) return 'concept';\n    if (/^(任务|目标|悬念|问题|待办|quest|mission|goal|task)$/iu.test(text)) return 'quest';\n    return '';\n}\n\nfunction createMemoryEntityNode(type, title, sourceNode = null, state = {}, fallbackIndex = 0) {\n    const safeType = MEMORY_NODE_TYPE_OPTIONS.some(option => option.value === type) ? type : '';\n    const safeTitle = normalizeMemoryEntityTitle(title);\n    if (!safeType || isWeakMemoryTitle(safeTitle)) {\n        return null;\n    }\n    const sourceTitle = sourceNode?.title ? `（来源：${sourceNode.title}）` : '';\n    return {\n        id: createMemoryId(`${safeType}_${safeTitle}`, `${safeType}_${fallbackIndex + 1}`),\n        title: safeTitle,\n        type: safeType,\n        summary: safeType === 'quest'\n            ? `待跟进目标：${safeTitle}`\n            : `${getOptionLabel(MEMORY_NODE_TYPE_OPTIONS, safeType, safeType)}：${safeTitle}`,\n        content: truncateText(`${safeTitle}${sourceTitle}`, 500),\n        location: safeType === 'location' ? safeTitle : normalizeMemoryFacetValue(sourceNode?.location || state.current_location),\n        timeSpan: normalizeMemoryFacetValue(sourceNode?.timeSpan || state.current_time || state.current_phase),\n        keys: uniqueStrings([safeTitle]),\n        tags: uniqueStrings(['自动拆分', getOptionLabel(MEMORY_NODE_TYPE_OPTIONS, safeType, safeType)]),\n        importance: safeType === 'quest' ? 0.62 : 0.55,\n        credibility: 0.72,\n    };\n}\n\nfunction getMemoryCardArray(update = {}) {\n    const candidates = [\n        update?.memory_cards,\n        update?.memoryCards,\n        update?.cards,\n        update?.card_updates,\n        update?.memory_card_updates,\n    ];\n    for (const value of candidates) {\n        if (Array.isArray(value)) {\n            return value;\n        }\n    }\n    return [];\n}\n\nfunction normalizeTypedMemoryCards(update = {}) {\n    const typedSources = [\n        update?.typed_cards,\n        update?.memory_typed_cards,\n        update?.memoryTypedCards,\n    ].filter(value => value && typeof value === 'object' && !Array.isArray(value));\n    const cards = [];\n    const seen = new Set();\n    for (const definition of MEMORY_TYPED_CARD_FIELDS) {\n        const values = [\n            ...typedSources.map(source => source?.[definition.field]),\n            update?.[definition.field],\n            update?.[`memory_${definition.field}`],\n        ];\n        for (const value of values) {\n            if (!Array.isArray(value)) {\n                continue;\n            }\n            for (const card of value.slice(0, 16)) {\n                if (!card || typeof card !== 'object') {\n                    continue;\n                }\n                const rawTitle = card?.title || card?.name || card?.label || card?.id || '';\n                const key = `${definition.type}|${normalizeText(rawTitle)}|${normalizeText(card?.evidence || card?.summary || card?.status || '')}`;\n                if (!normalizeText(rawTitle) || seen.has(key)) {\n                    continue;\n                }\n                seen.add(key);\n                cards.push({\n                    ...card,\n                    type: definition.type,\n                    forcedType: definition.type,\n                    categoryLabel: definition.label,\n                });\n            }\n        }\n    }\n    return cards.slice(0, 64);\n}\n\nfunction expandTypedMemoryCardsIntoUpdate(update = {}) {\n    const typedCards = normalizeTypedMemoryCards(update);\n    if (!typedCards.length) {\n        return update;\n    }\n    const existingCards = getMemoryCardArray(update);\n    return {\n        ...update,\n        memory_cards: [...typedCards, ...existingCards].slice(0, 96),\n    };\n}\n\nfunction normalizeMemoryCardOperation(card = {}) {\n    const operation = String(card?.operation || card?.op || card?.action || 'upsert').trim().toLowerCase();\n    if (/^(delete|remove|删除|移除)$/u.test(operation)) return 'remove';\n    if (/^(skip|ignore|忽略|跳过)$/u.test(operation)) return 'skip';\n    return 'upsert';\n}\n\nfunction normalizeAiMemoryCardToNode(card = {}, state = {}, fallbackIndex = 0) {\n    const operation = normalizeMemoryCardOperation(card);\n    if (operation !== 'upsert') {\n        return null;\n    }\n    const explicitType = String(card?.forcedType || card?.type || card?.category || card?.kind || '').trim();\n    const type = getMemoryEntityTypeFromLabel(explicitType) || normalizeMemoryNodeType({ type: explicitType });\n    const rawTitle = card?.title || card?.name || card?.label || card?.id || '';\n    const cleaned = type && type !== 'event'\n        ? cleanMemoryRuleTitleByType(rawTitle, type, { config: getMemoryRuleConfig() })\n        : { title: stripMemoryEntityTail(rawTitle), originalTitle: rawTitle, reason: '' };\n    const title = truncateText(cleaned.title || rawTitle, 80);\n    if (isWeakMemoryTitle(title)) {\n        return null;\n    }\n    const status = truncateText(card?.status || card?.state || card?.current_status || '', 180);\n    const evidence = truncateText(card?.evidence || card?.source || card?.quote || card?.reason || '', 220);\n    const summary = truncateText(card?.summary || status || card?.content || card?.description || title, 160);\n    const aliases = uniqueStrings(Array.isArray(card?.aliases)\n        ? card.aliases\n        : String(card?.aliases || '').split(/[,\\n;；、，|/]+/u)).slice(0, 8);\n    const firstSeenFloor = Number.isFinite(Number(card?.first_seen_floor ?? card?.firstSeenFloor))\n        ? Number(card?.first_seen_floor ?? card?.firstSeenFloor)\n        : undefined;\n    const lastSeenFloor = Number.isFinite(Number(card?.last_seen_floor ?? card?.lastSeenFloor ?? card?.floor))\n        ? Number(card?.last_seen_floor ?? card?.lastSeenFloor ?? card?.floor)\n        : undefined;\n    const contentParts = [\n        card?.content || card?.description || summary,\n        status ? `状态：${status}` : '',\n        evidence ? `证据：${evidence}` : '',\n        aliases.length ? `别名：${aliases.join('、')}` : '',\n        lastSeenFloor ? `最近楼层：${lastSeenFloor}` : '',\n    ].filter(Boolean);\n    const keys = uniqueStrings([\n        ...(Array.isArray(card?.keys) ? card.keys : String(card?.keys || '').split(/[,\\n;；、，|/]+/u)),\n        evidence,\n        ...aliases,\n        cleaned.originalTitle,\n    ].filter(Boolean));\n    const tags = uniqueStrings([\n        ...(Array.isArray(card?.tags) ? card.tags : String(card?.tags || '').split(/[,\\n;；、，|/]+/u)),\n        'AI填卡',\n        getMemoryRuleTypeLabel(type),\n        cleaned.reason ? '标题净化' : '',\n    ].filter(Boolean));\n    return {\n        id: createMemoryId(`${type}_${title}`, `${type || 'card'}_${fallbackIndex + 1}`),\n        title,\n        type,\n        content: truncateText(contentParts.join('\\n'), 1200),\n        summary,\n        location: truncateText(card?.location || card?.scene || (type === 'location' ? title : state.current_location || ''), 120),\n        timeSpan: truncateText(card?.timeSpan || card?.time_span || card?.time || state.current_time || state.current_phase || '', 120),\n        keys,\n        tags,\n        aliases,\n        evidence,\n        status,\n        firstSeenFloor,\n        lastSeenFloor,\n        history: [{\n            floor: lastSeenFloor,\n            status,\n            summary,\n            evidence,\n            at: new Date().toISOString(),\n        }].filter(item => item.summary || item.status || item.evidence),\n        importance: clampNumber(card?.importance ?? card?.weight, type === 'event' ? 0.6 : 0.62, 0, 1),\n        credibility: clampNumber(card?.credibility ?? card?.confidence, 0.82, 0, 1),\n    };\n}\n\nfunction expandMemoryCardsIntoUpdate(update = {}) {\n    update = expandTypedMemoryCardsIntoUpdate(update);\n    const cards = getMemoryCardArray(update);\n    if (!cards.length) {\n        return update;\n    }\n    const state = update?.state && typeof update.state === 'object' ? update.state : {};\n    const nodes = Array.isArray(update?.nodes) ? [...update.nodes] : [];\n    const links = Array.isArray(update?.links) ? [...update.links] : [];\n    const removeNodeIds = Array.isArray(update?.remove_node_ids) ? [...update.remove_node_ids] : [];\n    const cardTitleByIndex = [];\n    for (const [index, card] of cards.slice(0, 32).entries()) {\n        const operation = normalizeMemoryCardOperation(card);\n        const title = card?.title || card?.name || card?.label || card?.id || '';\n        if (operation === 'remove') {\n            if (title) removeNodeIds.push(title);\n            continue;\n        }\n        if (operation === 'skip') {\n            continue;\n        }\n        const node = normalizeAiMemoryCardToNode(card, state, nodes.length + index);\n        if (!node) {\n            continue;\n        }\n        cardTitleByIndex[index] = node.title;\n        nodes.push(node);\n        const related = Array.isArray(card?.related)\n            ? card.related\n            : Array.isArray(card?.relatedTo)\n                ? card.relatedTo\n                : Array.isArray(card?.related_to)\n                    ? card.related_to\n                    : [];\n        for (const target of related.slice(0, 8)) {\n            const targetTitle = typeof target === 'object' ? target.title || target.target || target.id : target;\n            if (!targetTitle) continue;\n            links.push({\n                source: node.title,\n                target: targetTitle,\n                type: typeof target === 'object' ? (target.type || target.relation || 'RELATED') : 'RELATED',\n                weight: typeof target === 'object' ? target.weight : 0.68,\n                description: typeof target === 'object' ? target.description || '' : '',\n            });\n        }\n    }\n    for (const [index, card] of cards.slice(0, 32).entries()) {\n        const explicitLinks = Array.isArray(card?.links) ? card.links : [];\n        const sourceTitle = cardTitleByIndex[index] || card?.title || card?.name || card?.label || '';\n        for (const link of explicitLinks.slice(0, 8)) {\n            const target = link?.target || link?.to || link?.title || '';\n            if (!sourceTitle || !target) continue;\n            links.push({\n                source: link?.source || link?.from || sourceTitle,\n                target,\n                type: link?.type || link?.relation || 'RELATED',\n                weight: link?.weight ?? 0.7,\n                description: link?.description || '',\n            });\n        }\n    }\n    return {\n        ...update,\n        memory_cards: cards,\n        nodes,\n        links,\n        remove_node_ids: removeNodeIds,\n    };\n}\n\nfunction extractTypedMemoryEntityHints(rawNode = {}) {\n    const hints = [];\n    const tokens = [\n        ...splitMemoryEntityTokens(rawNode.keys),\n        ...splitMemoryEntityTokens(rawNode.tags),\n    ];\n    for (const token of tokens) {\n        const match = token.match(/^(角色|人物|地点|场景|道具|物品|势力|组织|设定|规则|任务|目标|线索|概念|character|person|location|place|item|prop|faction|organization|setting|rule|quest|goal)\\s*[:：\\-]\\s*(.+)$/iu);\n        if (!match) {\n            continue;\n        }\n        const labelType = getMemoryEntityTypeFromLabel(match[1]);\n        const title = normalizeMemoryEntityTitle(match[2]);\n        if (labelType && title) {\n            hints.push({ type: labelType, title });\n        }\n    }\n    return hints;\n}\n\nfunction addMemoryEntityHint(hints, seen, type, title) {\n    const safeType = MEMORY_NODE_TYPE_OPTIONS.some(option => option.value === type) ? type : '';\n    const safeTitle = normalizeMemoryEntityTitle(title);\n    if (!safeType || isWeakMemoryTitle(safeTitle)) {\n        return;\n    }\n    const key = `${safeType}|${normalizeText(safeTitle)}`;\n    if (seen.has(key)) {\n        return;\n    }\n    seen.add(key);\n    hints.push({ type: safeType, title: safeTitle });\n}\n\nfunction stripMemoryEntityTail(value) {\n    return normalizeMemoryEntityTitle(String(value || '')\n        .replace(/[，。！？；;,.!?].*$/u, '')\n        .replace(/^(和|与|及|以及|还有|并|向|给|对|在|到|从|把|将|因|因而|因为|确认|说明|请求|赠送|进入|前往|来到|拿到|获得|发现|遇见|见到|加入|属于|来自|关于|调查|寻找|解决|处理|更换|换上|借住|收留|反应)\\s*/u, '')\n        .replace(/\\s*(等|之一|这件事|此事|相关).*$/u, ''));\n}\n\nfunction isBlockedMemoryEntityTitle(title, config = getMemoryRuleConfig()) {\n    const text = normalizeText(title);\n    if (!text) return true;\n    const defaults = ['当前', '目标', '地点', '时间', '状态', '阶段', '问题', '任务', '特产', '鞋子', '公寓', '大学', '玄关', '客厅'];\n    const blockWords = uniqueStrings([...defaults, ...splitMemoryRuleKeywords(config?.blockKeywords)]);\n    return blockWords.some(word => {\n        const normalized = normalizeText(word);\n        return normalized && (text === normalized || text.includes(normalized));\n    });\n}\n\nfunction applyCustomMemoryEntityRules(text, hints, seen, config = getMemoryRuleConfig()) {\n    const candidates = collectMemoryRuleCandidates(text, config);\n    const acceptedByTitle = new Set();\n    for (const candidate of candidates) {\n        const minConfidence = normalizeMemoryRuleNumber(candidate.rule?.minConfidence, 64, 1, 100);\n        const mode = normalizeMemoryRuleLowConfidenceMode(candidate.rule?.lowConfidenceMode, 'skip');\n        const key = normalizeText(candidate.title);\n        if (!key || acceptedByTitle.has(key)) continue;\n        if (candidate.confidence < minConfidence && mode !== 'write') continue;\n        acceptedByTitle.add(key);\n        addScoredMemoryEntityHint(hints, seen, candidate.type, candidate.title, {\n            confidence: candidate.confidence,\n            matchedKeywords: [candidate.keyword].filter(Boolean),\n            positiveMatches: candidate.positiveMatches,\n            negativeMatches: candidate.negativeMatches,\n            ruleLabel: candidate.rule?.label,\n            reason: [candidate.reason, candidate.cleanReason].filter(Boolean).join('；'),\n        });\n    }\n}\n\nfunction extractMemoryEntityHintsFromText(rawNode = {}, state = {}) {\n    const hints = [];\n    const seen = new Set();\n    const text = sanitizeMemoryMessageText([\n        rawNode?.title,\n        rawNode?.summary,\n        rawNode?.content,\n        rawNode?.description,\n        Array.isArray(rawNode?.keys) ? rawNode.keys.join('，') : rawNode?.keys,\n        Array.isArray(rawNode?.tags) ? rawNode.tags.join('，') : rawNode?.tags,\n        state?.current_location,\n        state?.current_objective,\n        Array.isArray(state?.active_topics) ? state.active_topics.join('，') : '',\n        Array.isArray(state?.open_questions) ? state.open_questions.join('，') : '',\n    ].filter(Boolean).join('。'));\n\n    const typedPatterns = [\n        ['character', /(?:角色|人物|npc|主角|配角|同伴|敌人|朋友|老师|学生|房东|亲戚|家人|少女|少年|男子|女子|男人|女人|姓名|身份)[：:为是叫名]?\\s*([\\p{Script=Han}A-Za-z0-9_·]{2,18})/giu],\n        ['location', /(?:地点|位置|场景|房间|屋内|门口|入口|学校|大学|公寓|宿舍|家中|豪华公寓|玄关|客厅|卧室|城市|据点|秘境|森林|村庄)[：:为是在到至]?\\s*([\\p{Script=Han}A-Za-z0-9_·]{2,24})?/giu],\n        ['item', /(?:道具|物品|装备|武器|线索|资料|信件|钥匙|令牌|戒指|卡片|药物|鞋|鞋子|特产|土特产|包裹|礼物|手机|地图|笔记)[：:为是叫]?\\s*([\\p{Script=Han}A-Za-z0-9_·]{2,24})?/giu],\n        ['faction', /(?:势力|组织|阵营|队伍|家族|帮派|集团|公司|学校|大学|社团|教团|王国|帝国)[：:为是叫]?\\s*([\\p{Script=Han}A-Za-z0-9_·]{2,24})?/giu],\n        ['concept', /(?:设定|世界观|能力|机制|系统|规则|习俗|背景|身份设定|亲戚身份|借住关系)[：:为是叫]?\\s*([\\p{Script=Han}A-Za-z0-9_·]{2,30})?/giu],\n        ['rule', /(?:规则|法则|限制|禁忌|协议|契约|约定|承诺|条件)[：:为是叫]?\\s*([\\p{Script=Han}A-Za-z0-9_·]{2,30})?/giu],\n        ['quest', /(?:任务|目标|目的|待办|悬念|未解|问题|谜团|调查|寻找|解决|处理|请求|收留)[：:为是叫]?\\s*([\\p{Script=Han}A-Za-z0-9_·]{2,34})?/giu],\n    ];\n\n    for (const [type, pattern] of typedPatterns) {\n        for (const match of text.matchAll(pattern)) {\n            const title = stripMemoryEntityTail(match[1] || match[0]);\n            addMemoryEntityHint(hints, seen, type, title);\n        }\n    }\n\n    applyCustomMemoryEntityRules(text, hints, seen);\n\n    const namePatterns = [\n        /(?:向|对|给|问|告诉|请求|见到|遇见|认识|确认|说明|跟|和|与)([\\p{Script=Han}A-Za-z0-9_·]{2,12})(?:说|问|确认|说明|请求|交谈|解释|表示|提出|询问|回答|答应|赠送|借住|收留|反应)/giu,\n        /([\\p{Script=Han}A-Za-z0-9_·]{2,12})(?:同意|拒绝|反应|出现|离开|进入|询问|回答|表示|提出|是否会|能否|答应|解释|请求|告诉|赠送|收留)/giu,\n    ];\n    for (const pattern of namePatterns) {\n        for (const match of text.matchAll(pattern)) {\n            const title = stripMemoryEntityTail(match[1]);\n            if (!isBlockedMemoryEntityTitle(title)) {\n                addMemoryEntityHint(hints, seen, 'character', title);\n            }\n        }\n    }\n\n    const quotedItems = text.match(/[《“「『](.{2,24}?)[》”」』]/gu) || [];\n    for (const quoted of quotedItems) {\n        const title = stripMemoryEntityTail(quoted.replace(/[《》“”「」『』]/gu, ''));\n        const type = /(规则|约定|设定|身份|关系|能力|机制)/u.test(title)\n            ? 'concept'\n            : /(任务|目标|调查|寻找|解决)/u.test(title)\n                ? 'quest'\n                : 'item';\n        addMemoryEntityHint(hints, seen, type, title);\n    }\n\n    return hints.slice(0, 10);\n}\n\nfunction expandMemoryUpdateEntityNodes(update, graph = getMemoryGraph()) {\n    const state = update?.state && typeof update.state === 'object' ? update.state : {};\n    const incomingNodes = Array.isArray(update?.nodes) ? [...update.nodes] : [];\n    const incomingLinks = Array.isArray(update?.links) ? [...update.links] : [];\n    const existingTitles = new Set((Array.isArray(graph?.nodes) ? graph.nodes : [])\n        .map(node => `${String(node.type || '')}|${normalizeText(node.title)}`));\n    const incomingTitles = new Set(incomingNodes\n        .map(node => `${normalizeMemoryNodeType(node)}|${normalizeText(node?.title || node?.label || node?.id || '')}`));\n    const sourceIdByTitle = new Map(incomingNodes\n        .map(node => [normalizeText(node?.title || node?.label || node?.id || ''), createMemoryId(node?.id || node?.title || node?.label || '')]));\n\n    const addEntity = (type, title, sourceNode = null, linkType = 'RELATED') => {\n        const entity = createMemoryEntityNode(type, title, sourceNode, state, incomingNodes.length);\n        if (!entity) return null;\n        const key = `${entity.type}|${normalizeText(entity.title)}`;\n        if (existingTitles.has(key) || incomingTitles.has(key)) {\n            return null;\n        }\n        incomingNodes.push(entity);\n        incomingTitles.add(key);\n        const sourceTitleKey = normalizeText(sourceNode?.title || sourceNode?.label || sourceNode?.id || '');\n        const sourceId = sourceIdByTitle.get(sourceTitleKey) || createMemoryId(sourceNode?.id || sourceNode?.title || sourceNode?.label || '');\n        if (sourceId && sourceId !== entity.id) {\n            incomingLinks.push({\n                source: sourceId,\n                target: entity.id,\n                type: linkType,\n                weight: type === 'location' ? 0.78 : 0.64,\n                description: `${sourceNode?.title || '事件'} 关联 ${entity.title}`,\n            });\n        }\n        return entity;\n    };\n\n    for (const rawNode of incomingNodes.slice(0, 12)) {\n        const nodeType = normalizeMemoryNodeType(rawNode);\n        if (nodeType === 'event') {\n            if (rawNode?.location) {\n                addEntity('location', rawNode.location, rawNode, 'HAPPENS_AT');\n            }\n            for (const hint of extractTypedMemoryEntityHints(rawNode)) {\n                addEntity(hint.type, hint.title, rawNode, hint.type === 'location' ? 'HAPPENS_AT' : 'INVOLVES');\n            }\n            for (const hint of extractMemoryEntityHintsFromText(rawNode, state)) {\n                addEntity(hint.type, hint.title, rawNode, hint.type === 'location' ? 'HAPPENS_AT' : 'INVOLVES');\n            }\n        }\n    }\n\n    if (typeof state.current_location === 'string' && state.current_location.trim()) {\n        addEntity('location', state.current_location, null, 'HAPPENS_AT');\n    }\n    if (typeof state.current_objective === 'string' && state.current_objective.trim()) {\n        addEntity('quest', state.current_objective, null, 'RELATED');\n    }\n    for (const question of Array.isArray(state.open_questions) ? state.open_questions : []) {\n        addEntity('quest', question, null, 'RELATED');\n    }\n    for (const hint of extractMemoryEntityHintsFromText({}, state)) {\n        addEntity(hint.type, hint.title, null, 'RELATED');\n    }\n\n    return {\n        ...update,\n        nodes: incomingNodes,\n        links: incomingLinks,\n    };\n}\n\nfunction sanitizeMemoryUpdateForApply(update) {\n    update = expandMemoryCardsIntoUpdate(update);\n    const ruleConfig = getMemoryRuleConfig();\n    const sanitized = {\n        ...update,\n        nodes: [],\n        updates: [],\n        links: [],\n    };\n    const idRedirects = new Map();\n    const rememberRedirect = (from, to) => {\n        const raw = String(from || '').trim();\n        const target = String(to || '').trim();\n        if (!raw || !target) return;\n        idRedirects.set(normalizeText(raw), target);\n        idRedirects.set(createMemoryId(raw), target);\n    };\n    const redirectEndpoint = value => {\n        const raw = String(value || '').trim();\n        if (!raw) return '';\n        return idRedirects.get(normalizeText(raw)) || idRedirects.get(createMemoryId(raw)) || raw;\n    };\n    for (const rawNode of (Array.isArray(update?.nodes) ? update.nodes : []).slice(0, 32)) {\n        const nodeType = normalizeMemoryNodeType(rawNode);\n        const rawTitle = rawNode?.title || rawNode?.label || rawNode?.id || '';\n        const rawId = rawNode?.id || '';\n        const rawLabel = rawNode?.label || '';\n        const cleaned = nodeType && nodeType !== 'event'\n            ? cleanMemoryRuleTitleByType(rawTitle, nodeType, { config: ruleConfig })\n            : { title: stripMemoryEntityTail(rawTitle), originalTitle: rawTitle, reason: '' };\n        const title = truncateText(cleaned.title || rawTitle, 80);\n        const summary = truncateText(rawNode?.summary || rawNode?.content || rawNode?.description || '', 240);\n        const keys = Array.isArray(rawNode?.keys) ? rawNode.keys.join('') : String(rawNode?.keys || '');\n        if (isWeakMemoryTitle(title) || (normalizeText(summary).length < 4 && normalizeText(keys).length < 2)) continue;\n        const node = { ...rawNode, type: nodeType, title };\n        if (rawNode?.label || normalizeText(rawNode?.label || '') === normalizeText(rawTitle)) {\n            node.label = title;\n        }\n        if (!rawNode?.id || normalizeText(rawNode.id) === normalizeText(rawTitle) || normalizeText(rawNode.id).includes(normalizeText(rawTitle))) {\n            node.id = createMemoryId(`${nodeType}_${title}`);\n        }\n        if (cleaned.reason) {\n            node.tags = uniqueStrings([...(Array.isArray(rawNode?.tags) ? rawNode.tags : []), '标题净化']);\n            node.keys = uniqueStrings([...(Array.isArray(rawNode?.keys) ? rawNode.keys : []), cleaned.originalTitle].filter(Boolean));\n        }\n        rememberRedirect(rawId, node.id || createMemoryId(`${nodeType}_${title}`));\n        rememberRedirect(rawTitle, node.id || createMemoryId(`${nodeType}_${title}`));\n        rememberRedirect(rawLabel, node.id || createMemoryId(`${nodeType}_${title}`));\n        rememberRedirect(cleaned.originalTitle, node.id || createMemoryId(`${nodeType}_${title}`));\n        rememberRedirect(title, node.id || createMemoryId(`${nodeType}_${title}`));\n        sanitized.nodes.push(node);\n    }\n\n    for (const rawUpdate of (Array.isArray(update?.updates) ? update.updates : []).slice(0, 8)) {\n        const updateType = normalizeMemoryNodeType(rawUpdate);\n        const rawTitle = rawUpdate?.title || rawUpdate?.titleToUpdate || rawUpdate?.id || '';\n        const cleaned = updateType && updateType !== 'event'\n            ? cleanMemoryRuleTitleByType(rawTitle, updateType, { config: ruleConfig })\n            : { title: stripMemoryEntityTail(rawTitle), originalTitle: rawTitle, reason: '' };\n        const title = cleaned.title || rawTitle;\n        const content = rawUpdate?.content || rawUpdate?.newContent || rawUpdate?.summary || '';\n        const keys = Array.isArray(rawUpdate?.keys) ? rawUpdate.keys.join('') : String(rawUpdate?.keys || '');\n        if (isWeakMemoryTitle(title) || (normalizeText(content).length < 4 && normalizeText(keys).length < 2)) continue;\n        sanitized.updates.push({\n            ...rawUpdate,\n            title,\n            titleToUpdate: rawUpdate?.titleToUpdate ? title : rawUpdate?.titleToUpdate,\n            type: updateType,\n        });\n    }\n\r\n    for (const rawLink of (Array.isArray(update?.links) ? update.links : []).slice(0, 32)) {\n        const source = redirectEndpoint(rawLink?.source || rawLink?.sourceId || rawLink?.from || '');\n        const target = redirectEndpoint(rawLink?.target || rawLink?.targetId || rawLink?.to || '');\n        const sourceKey = normalizeText(source);\n        const targetKey = normalizeText(target);\n        if (!sourceKey || !targetKey || sourceKey === targetKey) continue;\n        sanitized.links.push({\n            ...rawLink,\n            source,\n            target,\n        });\n    }\n\r\n    return sanitized;\r\n}\r\n\r\nfunction memoryUpdateHasVisibleContent(update) {\r\n    if (!update || typeof update !== 'object') {\r\n        return false;\r\n    }\r\n    if (Array.isArray(update.nodes) && update.nodes.length) return true;\r\n    if (Array.isArray(update.updates) && update.updates.length) return true;\r\n    if (String(update.summary || '').trim()) return true;\r\n    const state = update.state && typeof update.state === 'object' ? update.state : {};\r\n    return Object.values(state).some((value) => {\r\n        if (Array.isArray(value)) return value.length > 0;\r\n        if (value && typeof value === 'object') return Object.keys(value).length > 0;\r\n        return String(value || '').trim();\r\n    });\r\n}\r\n\r\nfunction createMemoryFallbackSummaryFromMessages(messages, mode = 'realtime') {\r\n    const usefulMessages = (Array.isArray(messages) ? messages : [])\r\n        .map(message => sanitizeMemoryMessageText(message?.text || message?.mes || ''))\r\n        .filter(Boolean)\r\n        .slice(-4);\r\n    if (!usefulMessages.length) {\r\n        return '';\r\n    }\r\n    const label = mode === 'summary' || mode === 'history' ? '阶段整理' : '实时整理';\r\n    return truncateText(`${label}: ${usefulMessages.map(text => truncateText(text, 180)).join('\\n')}`, 900);\r\n}\r\n\r\nfunction ensureMemoryUpdateHasVisibleFallback(update, messages, mode = 'realtime') {\n    if (memoryUpdateHasVisibleContent(update)) {\n        return update;\n    }\n    const summary = createMemoryFallbackSummaryFromMessages(messages, mode);\n    if (!summary) {\r\n        return update;\r\n    }\r\n    return {\n        ...(update && typeof update === 'object' ? update : {}),\n        state: {\n            ...(update?.state && typeof update.state === 'object' ? update.state : {}),\n            current_location: update?.state?.current_location || '',\n            current_objective: update?.state?.current_objective || '',\n        },\n        summary,\n        nodes: [{\n            id: `event_${Date.now().toString(36)}`,\n            title: mode === 'summary' || mode === 'history' ? '阶段剧情整理' : '最新剧情进展',\n            type: 'event',\n            summary: truncateText(summary, 160),\r\n            content: summary,\r\n            tags: ['自动记忆'],\r\n            importance: 0.55,\r\n            credibility: 0.75,\r\n        }],\n    };\n}\n\r\nfunction createMemoryFallbackNodeFromSummary(summary, fallbackIndex = 0) {\r\n    const text = String(summary || '').trim();\r\n    if (normalizeText(text).length < 8) return null;\r\n    let title = truncateText(text.split(/[。.!?\\n]/u).find(part => normalizeText(part).length >= 4) || text, 48);\r\n    if (isWeakMemoryTitle(title)) {\r\n        title = `剧情进展 ${new Date().toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })}`;\r\n    }\r\n    return normalizeMemoryNode({\r\n        id: `event_${Date.now().toString(36)}_${fallbackIndex}`,\r\n        title,\r\n        type: 'event',\r\n        summary: title,\r\n        content: text,\r\n        tags: ['自动摘要'],\r\n        importance: 0.52,\r\n        credibility: 0.72,\r\n    }, fallbackIndex);\r\n}\r\n\r\nfunction getRecentMessagesByCount(chat, count) {\r\n    const limit = clampNumber(count, 6, 2, 40);\r\n    return chat\r\n        .map((message, index) => ({ message, index }))\r\n        .filter(({ message }) => message && !message.is_system && message.mes)\r\n        .slice(-limit)\r\n        .map(({ message, index }) => ({\r\n            index,\r\n            messageId: String(message.id || message.messageId || message.extra?.id || message.send_date || ''),\r\n            swipeId: String(message.swipe_id ?? message.swipeId ?? message.extra?.swipe_id ?? message.extra?.swipeId ?? ''),\r\n            name: message.name || '',\r\n            text: message.is_user ? extractActualUserInput(message.mes) : String(message.mes || ''),\r\n            isUser: !!message.is_user,\r\n        }));\r\n}\r\n\r\nfunction sanitizeMemoryMessageText(value) {\r\n    return String(value ?? '')\r\n        .replace(/\\[\\[AIWBR_MEMORY_VARS_BEGIN\\]\\][\\s\\S]*?\\[\\[AIWBR_MEMORY_VARS_END\\]\\]/gu, ' ')\r\n        .replace(/<draft_notes>[\\s\\S]*?<\\/draft_notes>/giu, ' ')\r\n        .replace(/<ai_last_output>[\\s\\S]*?<\\/ai_last_output>/giu, ' ')\r\n        .replace(/<\\/?peip>/giu, ' ')\r\n        .replace(/\\r/g, '')\r\n        .replace(/\\n{3,}/g, '\\n\\n')\r\n        .replace(/[ \\t]{2,}/g, ' ')\r\n        .trim();\r\n}\r\n\r\nfunction getMemoryRelevantMessages(chat, count) {\r\n    return getRecentMessagesByCount(chat, count)\r\n        .map(message => ({\r\n            ...message,\r\n            text: sanitizeMemoryMessageText(message.text),\r\n        }))\r\n        .filter(message => message.text);\r\n}\r\n\r\nfunction getAllMemoryMessages(chat) {\r\n    return (Array.isArray(chat) ? chat : [])\r\n        .map((message, index) => ({ message, index }))\r\n        .filter(({ message }) => message && !message.is_system && message.mes)\r\n        .map(({ message, index }) => ({\r\n            index,\r\n            floor: index + 1,\r\n            messageId: String(message.id || message.messageId || message.extra?.id || message.send_date || ''),\r\n            swipeId: String(message.swipe_id ?? message.swipeId ?? message.extra?.swipe_id ?? message.extra?.swipeId ?? ''),\r\n            name: message.name || '',\r\n            text: sanitizeMemoryMessageText(message.is_user ? extractActualUserInput(message.mes) : String(message.mes || '')),\r\n            isUser: !!message.is_user,\r\n        }))\r\n        .filter(message => message.text);\r\n}\r\n\r\nfunction getHistoryImportRangeFromUi(context = getContext()) {\r\n    const messages = getAllMemoryMessages(Array.isArray(context?.chat) ? context.chat : []);\r\n    const maxFloor = messages.length ? Math.max(...messages.map(message => message.floor)) : 0;\r\n    const startRaw = Number($('#ai_wbr_memory_history_start_floor').val());\r\n    const endRaw = Number($('#ai_wbr_memory_history_end_floor').val());\r\n    const startFloor = clampNumber(Number.isFinite(startRaw) ? startRaw : 1, 1, 1, Math.max(1, maxFloor));\r\n    const endFloor = clampNumber(Number.isFinite(endRaw) ? endRaw : maxFloor, maxFloor, 1, Math.max(1, maxFloor));\r\n    const first = Math.min(startFloor, endFloor);\r\n    const last = Math.max(startFloor, endFloor);\r\n    return {\r\n        startFloor: first,\r\n        endFloor: last,\r\n        maxFloor,\r\n        messages: messages.filter(message => message.floor >= first && message.floor <= last),\r\n    };\r\n}\r\n\r\nfunction buildHistoryImportPreview(rangeInfo, context = getContext()) {\r\n    const messages = Array.isArray(rangeInfo?.messages) ? rangeInfo.messages : [];\r\n    const startFloor = Number(rangeInfo?.startFloor || 0);\r\n    const endFloor = Number(rangeInfo?.endFloor || 0);\r\n    const imported = !!rangeInfo?.imported;\r\n    const floorLabel = startFloor && endFloor ? `${startFloor}-${endFloor}` : 'selected range';\r\n    if (!messages.length) {\r\n        return imported ? `Range ${floorLabel} has already been imported.` : `Range ${floorLabel} has no importable messages.`;\r\n    }\r\n    return `Selected ${floorLabel}; ${messages.length} message(s) can be imported${imported ? '; this range was imported before' : ''}.`;\r\n}\r\nfunction getHistoryImportSignature(messages, context = getContext()) {\r\n    const chatKey = getCurrentChatMemoryKey(context);\r\n    const sourceSignature = getMemorySourceSignature(messages);\r\n    const firstFloor = messages[0]?.floor || 0;\r\n    const lastFloor = messages[messages.length - 1]?.floor || 0;\r\n    return `hist_${hashString(`${chatKey}|${firstFloor}|${lastFloor}|${messages.length}|${sourceSignature}`)}`;\r\n}\r\n\r\nfunction getHistoryImportRecord(signature, context = getContext()) {\r\n    const container = getChatMemoryContainer(context);\r\n    return (Array.isArray(container.historyImports) ? container.historyImports : [])\r\n        .find(item => String(item.signature || '') === String(signature || ''));\r\n}\r\n\r\nfunction recordHistoryImport(signature, source, context = getContext()) {\r\n    if (!signature) {\r\n        return;\r\n    }\r\n    const container = getChatMemoryContainer(context);\r\n    const records = Array.isArray(container.historyImports) ? container.historyImports : [];\r\n    const nextRecord = {\r\n        signature,\r\n        source,\r\n        createdAt: new Date().toISOString(),\r\n    };\r\n    container.historyImports = [\r\n        nextRecord,\r\n        ...records.filter(item => String(item.signature || '') !== signature),\r\n    ].slice(0, 120);\r\n    persistChatMemoryContainer(container, context);\r\n}\r\n\r\nfunction setHistoryImportRange(startFloor, endFloor, context = getContext()) {\r\n    const messages = getAllMemoryMessages(Array.isArray(context?.chat) ? context.chat : []);\r\n    const maxFloor = messages.length ? Math.max(...messages.map(message => message.floor)) : 0;\r\n    if (!maxFloor) {\r\n        $('#ai_wbr_memory_history_start_floor').val('');\r\n        $('#ai_wbr_memory_history_end_floor').val('');\r\n        buildHistoryImportPreview({ startFloor: 1, endFloor: 1, maxFloor: 0, messages: [] }, context);\r\n        return;\r\n    }\r\n    const first = clampNumber(startFloor, 1, 1, maxFloor);\r\n    const last = clampNumber(endFloor, maxFloor, 1, maxFloor);\r\n    $('#ai_wbr_memory_history_start_floor').val(Math.min(first, last));\r\n    $('#ai_wbr_memory_history_end_floor').val(Math.max(first, last));\r\n    buildHistoryImportPreview(getHistoryImportRangeFromUi(context), context);\r\n}\r\n\r\nfunction getMemoryAutoRunMessageCount(context = getContext()) {\r\n    const chat = Array.isArray(context?.chat) ? context.chat : [];\r\n    return chat.filter(message => message && !message.is_system && message.mes).length;\r\n}\r\n\r\nfunction getMemoryTurnSignature(recentMessages) {\r\n    return recentMessages\r\n        .slice(-4)\r\n        .map(message => `${message.index ?? '?'}:${message.isUser ? 'U' : 'A'}:${message.name}:${message.messageId || ''}:${message.swipeId || ''}:${truncateText(message.text, 240)}`)\r\n        .join('\\n---\\n');\r\n}\r\n\r\nfunction getMemorySourceSignature(recentMessages) {\r\n    return recentMessages\r\n        .slice(-4)\r\n        .map(message => [\r\n            message.index ?? '?',\r\n            message.isUser ? 'U' : 'A',\r\n            message.name || '',\r\n            message.messageId || '',\r\n            message.swipeId || '',\r\n            hashString(message.text || ''),\r\n        ].join(':'))\r\n        .join('|');\r\n}\r\n\r\nfunction getMemorySourceSnapshot(context = getContext(), recentMessages = null, scanCount = settings.memoryRealtimeScanMessages || settings.memoryScanMessages) {\r\n    const messages = Array.isArray(recentMessages)\r\n        ? recentMessages\r\n        : getMemoryRelevantMessages(Array.isArray(context?.chat) ? context.chat : [], scanCount);\r\n    const last = messages[messages.length - 1] || {};\r\n    const chatKey = getCurrentChatMemoryKey(context);\r\n    const sourceSignature = getMemorySourceSignature(messages);\r\n    const messageCount = getMemoryAutoRunMessageCount(context);\r\n    return {\r\n        id: `src_${hashString(`${chatKey}|${messageCount}|${sourceSignature}`)}`,\r\n        chatKey,\r\n        messageCount,\r\n        indices: messages.map(message => message.index).filter(index => Number.isFinite(index)),\r\n        lastIndex: Number.isFinite(last.index) ? last.index : -1,\r\n        lastRole: last.isUser ? 'user' : 'assistant',\r\n        lastName: String(last.name || ''),\r\n        lastHash: hashString(last.text || ''),\r\n        sourceSignature,\r\n        createdAt: new Date().toISOString(),\r\n    };\r\n}\r\n\r\nfunction getInvalidatedMemorySourceIds(chatKey = getCurrentChatMemoryKey()) {\r\n    return Array.isArray(settings.memoryInvalidatedSourcesByChat?.[chatKey])\r\n        ? settings.memoryInvalidatedSourcesByChat[chatKey].map(value => String(value))\r\n        : [];\r\n}\r\n\r\nfunction setCurrentMemorySource(source, context = getContext()) {\r\n    if (!source?.id) {\r\n        return;\r\n    }\r\n    const chatKey = source.chatKey || getCurrentChatMemoryKey(context);\r\n    settings.memoryLastSourceByChat[chatKey] = source;\r\n    Object.assign(extension_settings[MODULE_NAME], settings);\r\n    saveSettingsDebounced();\r\n}\r\n\r\nfunction isMemorySourceCurrent(source, context = getContext()) {\r\n    if (!source?.id) {\r\n        return true;\r\n    }\r\n    const chatKey = getCurrentChatMemoryKey(context);\r\n    if (source.chatKey && source.chatKey !== chatKey) {\r\n        return false;\r\n    }\r\n    if (getInvalidatedMemorySourceIds(chatKey).includes(String(source.id))) {\r\n        return false;\r\n    }\r\n    const chat = Array.isArray(context?.chat) ? context.chat : [];\r\n    if (source.mode === 'history') {\r\n        const startFloor = Number(source.rangeStartFloor || 0);\r\n        const endFloor = Number(source.rangeEndFloor || 0);\r\n        if (!startFloor || !endFloor) {\r\n            return true;\r\n        }\r\n        const currentMessages = getAllMemoryMessages(chat)\r\n            .filter(message => message.floor >= Math.min(startFloor, endFloor) && message.floor <= Math.max(startFloor, endFloor));\r\n        const last = currentMessages[currentMessages.length - 1] || {};\r\n        return currentMessages.length === Number(source.scanMessages || 0)\r\n            && getMemorySourceSignature(currentMessages) === String(source.sourceSignature || '')\r\n            && Number(last.index ?? -1) === Number(source.lastIndex ?? -1)\r\n            && hashString(last.text || '') === String(source.lastHash || '');\r\n    }\r\n    const scanCount = clampNumber(source.scanMessages, settings.memoryRealtimeScanMessages || settings.memoryScanMessages, 2, 40);\r\n    const currentMessages = getMemoryRelevantMessages(chat, scanCount);\r\n    const current = getMemorySourceSnapshot(context, currentMessages);\r\n    return current.messageCount === Number(source.messageCount || 0)\r\n        && current.sourceSignature === String(source.sourceSignature || '')\r\n        && current.lastIndex === Number(source.lastIndex ?? -1)\r\n        && current.lastHash === String(source.lastHash || '');\r\n}\r\n\r\nfunction getMemorySourceLabel(source) {\r\n    if (!source) {\r\n        return '';\r\n    }\r\n    const indices = Array.isArray(source.indices) ? source.indices : [];\r\n    if (!indices.length) {\r\n        return '';\r\n    }\r\n    const floors = indices.map(index => Number(index) + 1).filter(index => Number.isFinite(index));\r\n    if (!floors.length) {\r\n        return '';\r\n    }\r\n    const first = floors[0];\r\n    const last = floors[floors.length - 1];\r\n    return first === last ? `Floor ${first}` : `Floor ${first}-${last}`;\r\n}\r\n\r\nfunction buildMemoryRouterSummary() {\r\n    if (!settings.memoryInjectToRouter) {\r\n        return '';\r\n    }\r\n\r\n    const graph = getMemoryGraph();\r\n    const state = graph.state || {};\r\n    const topNodes = [...graph.nodes]\r\n        .sort((a, b) => (Number(b.importance || 0) - Number(a.importance || 0)))\r\n        .slice(0, 8)\r\n        .map(node => `- ${node.title} (${node.type || 'memory'}): ${truncateText(node.content, 160)}`)\r\n        .join('\\n');\r\n    const topLinks = graph.links\r\n        .slice(-8)\r\n        .map(link => {\r\n            const source = graph.nodes.find(node => node.id === link.source)?.title || link.source;\r\n            const target = graph.nodes.find(node => node.id === link.target)?.title || link.target;\r\n            return `- ${source} ${link.type || 'RELATED'} ${target}${link.description ? `：${truncateText(link.description, 90)}` : ''}`;\r\n        })\r\n        .join('\\n');\r\n    const customStateLines = getCustomMemoryStateDefinitions(graph)\r\n        .map(definition => {\r\n            const value = String(state.custom_values?.[definition.key] || '').trim();\r\n            return value ? `${definition.label}：${value}` : '';\r\n        })\r\n        .filter(Boolean)\r\n        .join('\\n');\r\n\r\n    const parts = [\r\n        '[轻量记忆状态]',\r\n        state.current_location ? `当前地点：${state.current_location}` : '',\r\n        state.current_objective ? `当前目标：${state.current_objective}` : '',\r\n        state.active_topics?.length ? `活跃主题：${state.active_topics.join('、')}` : '',\r\n        state.open_questions?.length ? `未解问题：${state.open_questions.join('、')}` : '',\r\n        customStateLines,\r\n        graph.lastSummary ? `最近摘要：${truncateText(graph.lastSummary, 420)}` : '',\r\n        topNodes ? `关键记忆节点：\\n${topNodes}` : '',\r\n        topLinks ? `关键关系：\\n${topLinks}` : '',\r\n        '[/轻量记忆状态]',\r\n    ].filter(Boolean);\r\n\r\n    return parts.length > 2 ? truncateText(parts.join('\\n'), MAX_MVU_CHARS) : '';\r\n}\r\n\r\nfunction getCombinedStateSummary(context) {\r\n    return [getMvuSummary(context), buildMemoryRouterSummary()].filter(Boolean).join('\\n\\n');\r\n}\r\n\r\nfunction getMemoryExtractionSchema() {\n    const typedCardArraySchema = { type: 'array', items: { type: 'object' } };\n    return {\n        name: 'ai_worldbook_memory_graph_update',\n        value: {\n            type: 'object',\n            additionalProperties: true,\n            properties: {\n                state: { type: 'object' },\n                typed_cards: { type: 'object' },\n                memory_typed_cards: { type: 'object' },\n                memory_characters: typedCardArraySchema,\n                memory_locations: typedCardArraySchema,\n                memory_items: typedCardArraySchema,\n                memory_factions: typedCardArraySchema,\n                memory_concepts: typedCardArraySchema,\n                memory_rules: typedCardArraySchema,\n                memory_quests: typedCardArraySchema,\n                memory_events: typedCardArraySchema,\n                characters: typedCardArraySchema,\n                locations: typedCardArraySchema,\n                items: typedCardArraySchema,\n                factions: typedCardArraySchema,\n                concepts: typedCardArraySchema,\n                rules: typedCardArraySchema,\n                quests: typedCardArraySchema,\n                events: typedCardArraySchema,\n                memory_cards: { type: 'array', items: { type: 'object' } },\n                nodes: { type: 'array', items: { type: 'object' } },\n                updates: { type: 'array', items: { type: 'object' } },\n                links: { type: 'array', items: { type: 'object' } },\r\n                remove_node_ids: { type: 'array', items: { type: 'string' } },\r\n                remove_link_ids: { type: 'array', items: { type: 'string' } },\r\n                summary: { type: 'string' },\r\n            },\r\n        },\r\n        strict: false,\r\n    };\r\n}\r\n\r\nfunction buildMemoryExtractionPrompt(recentMessages, graph) {\r\n    const chatText = recentMessages\r\n        .map(message => `${message.isUser ? '用户' : '助手'} #${message.floor}: ${sanitizeMemoryMessageText(message.text)}`)\r\n        .join('\\n\\n');\r\n    const currentGraph = truncateText(JSON.stringify({\r\n        state: graph.state,\r\n        stateDefinitions: getCustomMemoryStateDefinitions(graph),\r\n        nodes: graph.nodes.slice(-8).map(node => ({\r\n            id: node.id,\r\n            title: node.title,\r\n            type: node.type,\r\n            location: node.location || '',\r\n            timeSpan: node.timeSpan || '',\r\n        })),\r\n        links: graph.links.slice(-12).map(link => ({ source: link.source, target: link.target, type: link.type })),\r\n    }, null, 2), 4200);\r\n    const customDefinitions = getCustomMemoryStateDefinitions(graph);\r\n    const customDefinitionLines = customDefinitions.length\n        ? customDefinitions.map(definition => `- ${definition.label} (${definition.key}): ${definition.instruction || 'Custom state field'}`).join('\\n')\n        : '- None';\n    const memoryRuleGuide = buildMemoryRulePromptGuide();\n\n    return `<role>你是 SillyTavern 的长期记忆整理器。你需要把几千楼聊天中的信息稳定拆进固定分栏记忆卡片，并保持图谱可检索、可合并、不卡成事件堆。</role>\n<task>只提取后续角色扮演会继续用到的稳定事实、剧情进展、人物关系、地点、物品、势力、设定、规则和未解决任务。只能返回下方变量块。</task>\n<rules>\n1. 除指定变量块外，不要返回任何 JSON、Markdown、解释或寒暄。\n2. 所有 title、summary、content、location、timeSpan、tags、keys、state 字段必须优先使用中文；只有专有名词、角色原名、英文地名或用户原文明确为英文时才保留英文。\n3. node id 保持稳定，使用小写 snake_case；id 可以是英文/拼音，但展示给用户的标题和摘要必须是中文。\n4. 优先更新已有节点，避免重复创建含义相同的节点。\n5. 每次更新都必须先做分栏巡检：角色 → 地点 → 道具 → 势力 → 设定 → 规则 → 任务 → 事件；不要只写事件。\n6. 必须优先填写固定分栏字段 memory_characters_json、memory_locations_json、memory_items_json、memory_factions_json、memory_concepts_json、memory_rules_json、memory_quests_json、memory_events_json；memory_cards_json 仅兼容旧格式。\n7. 字段名决定板块，本地会按字段名强制映射 type；不要依赖 memory_cards_json.type 来完成分类。\n8. 如果本轮出现人物、地点、道具、组织、设定、规则或待办任务，必须写入对应分栏；event 只记录发生了什么和结果。\n9. 如果有可用剧情内容，至少生成 1 个中文 memory_events_json；只有完全没有有效记忆时才返回空数组。\n10. 新增或更新的卡片应尽量填写 evidence、status、summary、last_seen_floor；能继承当前地点/时间时填写 location/timeSpan。\n11. 标题必须短而稳定：角色只写人名/称呼，地点只写地点名，道具只写物品名，势力只写组织名，设定/规则只写机制名，任务只写待办目标，事件写动作结果。\n12. 禁止把“林悦提出入住”“林悦是否会答应”写成角色标题；应写 characters 标题“林悦”，quests 标题“请求入住林悦家”，events 标题“主角向林悦提出入住请求”。\n13. 状态字段只写“当前状态”，不要把角色、地点、道具、势力、设定的完整信息塞进状态字段；这些信息必须进入对应分栏。\n14. memory_links_json 应优先连接 event -> character 用 INVOLVES，event -> location 用 HAPPENS_AT，event -> item 用 INVOLVES，event -> quest 用 UPDATES 或 RELATED。\n15. current_location 只写地点名，current_time 只写时间点，protagonist_status 只写主角身体/情绪/处境，current_objective 只写下一步目标，active_topics/open_questions 只写短条目。\n</rules>\n<typed_memory_cards_format>\n固定分栏字段中每项格式：\n{\n  \"operation\": \"upsert\",\n  \"title\": \"卡片标题\",\n  \"status\": \"当前状态，可空\",\n  \"summary\": \"短摘要\",\n  \"content\": \"可选详情\",\n  \"evidence\": \"来自本回合剧情的证据短句\",\n  \"aliases\": [\"别名或称呼，可空\"],\n  \"first_seen_floor\": 12,\n  \"last_seen_floor\": 18,\n  \"confidence\": 0.0-1.0,\n  \"related\": [{\"title\":\"相关卡片标题\",\"type\":\"INVOLVES|HAPPENS_AT|UPDATES|RELATED\",\"description\":\"关系说明\"}]\n}\n示例：“林悦是否会答应主角入住”应输出：\nmemory_characters_json=[{\"title\":\"林悦\",\"status\":\"正在考虑是否让主角入住\",\"summary\":\"林悦被主角提出入住请求，态度尚未确定。\",\"evidence\":\"林悦是否会答应主角入住\"}]\nmemory_quests_json=[{\"title\":\"请求入住林悦家\",\"status\":\"待确认\",\"summary\":\"主角向林悦提出入住请求，需要等待回应。\",\"evidence\":\"提出入住\"}]\nmemory_events_json=[{\"title\":\"主角向林悦提出入住请求\",\"summary\":\"主角提出希望入住林悦家，林悦尚未回应。\",\"evidence\":\"林悦是否会答应主角入住\"}]\n</typed_memory_cards_format>\n<node_type_guide>\n- character：人物、身份、关系、状态长期变化。\n- location：地点、场景、房间、城市、秘境、据点。\n- item：道具、物品、装备、线索、信件、钥匙、药物。\n- faction：组织、势力、家族、阵营、队伍。\n- concept/rule：世界观、能力、规则、限制、契约、系统机制。\n- quest：任务、目标、悬念、未解决问题、待调查事项。\n- event：本轮发生的剧情进展，只记录动作和结果。\n</node_type_guide>\n<memory_card_write_rules>\n以下是用户在图谱规则界面配置的 AI 记忆卡片写入规则。它们不是本地关键词分类器，而是告诉你应该如何填写固定分栏字段；不要只塞进 event、memory_cards_json、memory_nodes_json 或 state。\n${memoryRuleGuide}\n</memory_card_write_rules>\n<custom_state_definitions>\n${customDefinitionLines}\n</custom_state_definitions>\n<current_memory_graph>\r\n${currentGraph}\r\n</current_memory_graph>\r\n<recent_messages>\r\n${truncateText(chatText, 9000)}\r\n</recent_messages>\r\n<output_format>\n[[AIWBR_MEMORY_VARS_BEGIN]]\nmemory_state_current_location_json=\"\"\nmemory_state_current_time_json=\"\"\nmemory_state_protagonist_status_json=\"\"\nmemory_state_current_objective_json=\"\"\r\nmemory_state_current_phase_json=\"\"\r\nmemory_active_topics_json=[]\nmemory_open_questions_json=[]\nmemory_custom_state_json={}\nmemory_characters_json=[]\nmemory_locations_json=[]\nmemory_items_json=[]\nmemory_factions_json=[]\nmemory_concepts_json=[]\nmemory_rules_json=[]\nmemory_quests_json=[]\nmemory_events_json=[]\nmemory_cards_json=[]\nmemory_nodes_json=[]\nmemory_updates_json=[]\nmemory_links_json=[]\r\nmemory_remove_node_ids_json=[]\nmemory_last_summary_json=\"\"\n[[AIWBR_MEMORY_VARS_END]]\n</output_format>`;\n}\nfunction buildMemoryExtractionRetryPrompt(recentMessages, graph) {\r\n    const basePrompt = buildMemoryExtractionPrompt(recentMessages, graph);\r\n    return `${basePrompt}\r\n\r\n<retry_instruction>\r\n上一次返回无法解析。请只返回完整的 AIWBR 变量块，不要包含 Markdown、解释、注释或额外 JSON 包装。除 id 外，所有可读内容继续使用中文。\r\n</retry_instruction>`;\r\n}\r\nfunction getRouterRequestMaxTokens() {\r\n    return clampNumber(settings.routerRequestMaxTokens, defaultSettings.routerRequestMaxTokens, 32, 100000);\r\n}\r\n\r\nfunction getMemoryRequestMaxTokens() {\r\n    return clampNumber(settings.memoryRequestMaxTokens, defaultSettings.memoryRequestMaxTokens, 32, 100000);\r\n}\r\n\r\nfunction parseMemoryUpdate(rawResponse, prompt = '') {\r\n    const parseMemoryVariableBlock = (text) => {\r\n        const raw = String(text || '');\r\n        if (!raw.trim()) {\r\n            return null;\r\n        }\r\n\r\n        const blockMatch = raw.match(/\\[\\[AIWBR_MEMORY_VARS_BEGIN\\]\\]([\\s\\S]*?)\\[\\[AIWBR_MEMORY_VARS_END\\]\\]/u);\n        const body = (blockMatch ? blockMatch[1] : raw).trim();\n        const hasTypedCardVariable = MEMORY_TYPED_CARD_FIELDS.some(definition => body.includes(`${definition.varName}=`));\n        if (!hasTypedCardVariable && !body.includes('memory_cards_json=') && !body.includes('memory_nodes_json=') && !body.includes('memory_summary_json=')) {\n            return null;\n        }\n\r\n        const lines = body\r\n            .split(/\\r?\\n/u)\r\n            .map(line => line.trim())\r\n            .filter(Boolean);\r\n\r\n        const values = {};\r\n        for (const line of lines) {\r\n            const index = line.indexOf('=');\r\n            if (index <= 0) {\r\n                continue;\r\n            }\r\n            const key = line.slice(0, index).trim();\r\n            const valueText = line.slice(index + 1).trim();\r\n            values[key] = valueText;\r\n        }\r\n\r\n        const readJsonValue = (key, fallback) => {\n            const valueText = values[key];\n            if (valueText === undefined) {\n                return fallback;\n            }\r\n            try {\r\n                return JSON.parse(valueText);\r\n            } catch {\r\n                return fallback;\r\n            }\n        };\n        const typedCards = {};\n        for (const definition of MEMORY_TYPED_CARD_FIELDS) {\n            typedCards[definition.field] = readJsonValue(definition.varName, []);\n        }\n\n        return {\n            state: {\n                current_location: readJsonValue('memory_state_current_location_json', ''),\r\n                current_time: readJsonValue('memory_state_current_time_json', ''),\r\n                protagonist_status: readJsonValue('memory_state_protagonist_status_json', ''),\r\n                current_objective: readJsonValue('memory_state_current_objective_json', ''),\r\n                current_phase: readJsonValue('memory_state_current_phase_json', ''),\r\n                active_topics: readJsonValue('memory_active_topics_json', []),\r\n                open_questions: readJsonValue('memory_open_questions_json', []),\n                custom_state: readJsonValue('memory_custom_state_json', {}),\n            },\n            typed_cards: typedCards,\n            characters: typedCards.characters,\n            locations: typedCards.locations,\n            items: typedCards.items,\n            factions: typedCards.factions,\n            concepts: typedCards.concepts,\n            rules: typedCards.rules,\n            quests: typedCards.quests,\n            events: typedCards.events,\n            memory_cards: readJsonValue('memory_cards_json', []),\n            nodes: readJsonValue('memory_nodes_json', []),\n            updates: readJsonValue('memory_updates_json', []),\n            links: readJsonValue('memory_links_json', []),\r\n            remove_node_ids: readJsonValue('memory_remove_node_ids_json', []),\r\n            remove_link_ids: readJsonValue('memory_remove_link_ids_json', []),\r\n            summary: readJsonValue('memory_summary_json', readJsonValue('memory_last_summary_json', '')),\r\n        };\r\n    };\r\n\r\n    const looksLikeMemoryPayload = (value) => {\n        return value && typeof value === 'object' && !Array.isArray(value) && (\n            value.state !== undefined\n            || value.typed_cards !== undefined\n            || value.memory_typed_cards !== undefined\n            || MEMORY_TYPED_CARD_FIELDS.some(definition => value[definition.field] !== undefined || value[`memory_${definition.field}`] !== undefined)\n            || value.memory_cards !== undefined\n            || value.memoryCards !== undefined\n            || value.cards !== undefined\n            || value.nodes !== undefined\n            || value.updates !== undefined\n            || value.links !== undefined\n            || value.summary !== undefined\r\n        );\r\n    };\r\n\r\n    if (rawResponse && typeof rawResponse === 'object' && !Array.isArray(rawResponse)) {\r\n        if (looksLikeMemoryPayload(rawResponse)) {\r\n            return rawResponse;\r\n        }\r\n\r\n        if (looksLikeMemoryPayload(rawResponse.content)) {\r\n            return rawResponse.content;\r\n        }\r\n\r\n        if (looksLikeMemoryPayload(rawResponse.message?.content)) {\r\n            return rawResponse.message.content;\r\n        }\r\n\r\n        if (looksLikeMemoryPayload(rawResponse.choices?.[0]?.message?.content)) {\r\n            return rawResponse.choices[0].message.content;\r\n        }\r\n    }\r\n\r\n    const texts = collectRouterResponseTexts(rawResponse);\r\n    for (const text of texts) {\r\n        const variableParsed = parseMemoryVariableBlock(text);\r\n        if (variableParsed) {\r\n            return variableParsed;\r\n        }\r\n        const parsed = tryParseSelectionText(text);\r\n        if (parsed && !Array.isArray(parsed)) {\r\n            return parsed;\r\n        }\r\n        const rawText = String(text || '').trim();\r\n        const withoutFence = rawText.replace(/^```(?:json)?/i, '').replace(/```$/i, '').trim();\r\n        const firstBrace = withoutFence.indexOf('{');\r\n        const lastBrace = withoutFence.lastIndexOf('}');\r\n        const candidates = [\r\n            withoutFence,\r\n            firstBrace >= 0 && lastBrace > firstBrace ? withoutFence.slice(firstBrace, lastBrace + 1) : '',\r\n        ].filter(Boolean);\r\n        for (const candidate of candidates) {\r\n            try {\r\n                return JSON.parse(candidate);\r\n            } catch {\r\n                // keep trying\r\n            }\r\n        }\r\n    }\r\n\r\n    if (typeof rawResponse === 'string') {\r\n        const variableParsed = parseMemoryVariableBlock(rawResponse);\r\n        if (variableParsed) {\r\n            return variableParsed;\r\n        }\r\n    }\r\n\r\n    const error = new Error(`Memory graph JSON parse failed. Preview: ${truncateText(texts.join(' '), 220) || '(empty)'}`);\r\n    error.routerPrompt = prompt;\r\n    error.routerRaw = summarizeRouterResponse(rawResponse);\r\n    throw error;\r\n}\r\n\r\nfunction resolveMemoryNodeId(value, graph) {\n    const raw = String(value || '').trim();\n    if (!raw) {\n        return '';\n    }\r\n    const direct = createMemoryId(raw);\r\n    if (graph.nodes.some(node => node.id === direct)) {\r\n        return direct;\r\n    }\r\n    const byTitle = graph.nodes.find(node => normalizeText(node.title) === normalizeText(raw));\n    return byTitle?.id || direct;\n}\n\nfunction findExistingMemoryNodeForUpsert(graph, node, byId = new Map()) {\n    if (!node) {\n        return null;\n    }\n    const direct = byId.get(node.id);\n    if (direct) {\n        return direct;\n    }\n    const titleKey = normalizeText(node.title);\n    const typeKey = String(node.type || 'event');\n    return graph.nodes.find(item => String(item.type || 'event') === typeKey && normalizeText(item.title) === titleKey)\n        || null;\n}\n\nfunction mergeMemoryNodeLongTermFields(existing, node) {\n    const nextHistory = [\n        ...(Array.isArray(existing.history) ? existing.history : []),\n        ...(Array.isArray(node.history) ? node.history : []),\n    ].filter(item => item && typeof item === 'object')\n        .slice(-12);\n    return {\n        aliases: uniqueStrings([...(existing.aliases || []), ...(node.aliases || [])]).slice(0, 12),\n        evidence: node.evidence || existing.evidence || '',\n        status: node.status || existing.status || '',\n        firstSeenFloor: [existing.firstSeenFloor, node.firstSeenFloor, node.lastSeenFloor]\n            .map(value => Number(value))\n            .filter(value => Number.isFinite(value))\n            .reduce((min, value) => Math.min(min, value), Number.POSITIVE_INFINITY),\n        lastSeenFloor: [existing.lastSeenFloor, node.lastSeenFloor, node.firstSeenFloor]\n            .map(value => Number(value))\n            .filter(value => Number.isFinite(value))\n            .reduce((max, value) => Math.max(max, value), Number.NEGATIVE_INFINITY),\n        history: nextHistory,\n    };\n}\n\r\nfunction applyMemoryGraphUpdate(update, context = getContext()) {\r\n    update = sanitizeMemoryUpdateForApply(update);\r\n    const graph = getMemoryGraph(context);\r\n    const now = new Date().toISOString();\r\n    let addedOrUpdatedNodeCount = 0;\r\n    const touchedEntries = [];\r\n\r\n    const state = update?.state && typeof update.state === 'object' ? update.state : {};\n    if (typeof state.current_location === 'string') {\n        graph.state.current_location = normalizeMemoryStateScalar(state.current_location, 60);\n    }\n    if (typeof state.current_time === 'string') {\n        graph.state.current_time = normalizeMemoryStateScalar(state.current_time, 60);\n    }\n    if (typeof state.protagonist_status === 'string') {\n        graph.state.protagonist_status = normalizeMemoryStateScalar(state.protagonist_status, 100);\n    }\n    if (typeof state.current_objective === 'string') {\n        graph.state.current_objective = normalizeMemoryStateScalar(state.current_objective, 100);\n    }\n    if (typeof state.current_phase === 'string') {\n        graph.state.current_phase = normalizeMemoryStateScalar(state.current_phase, 70);\n    }\n    if (Array.isArray(state.active_topics)) {\n        graph.state.active_topics = normalizeMemoryStateList(state.active_topics, 8, 70);\n    }\n    if (Array.isArray(state.open_questions)) {\n        graph.state.open_questions = normalizeMemoryStateList(state.open_questions, 8, 90);\n    }\n    if (state.custom_state && typeof state.custom_state === 'object' && !Array.isArray(state.custom_state)) {\r\n        const allowedKeys = new Set(getCustomMemoryStateDefinitions(graph).map(definition => definition.key));\r\n        graph.state.custom_values = graph.state.custom_values && typeof graph.state.custom_values === 'object'\r\n            ? graph.state.custom_values\r\n            : {};\r\n        for (const [key, rawValue] of Object.entries(state.custom_state)) {\r\n            if (!allowedKeys.has(key)) {\r\n                continue;\r\n            }\r\n            graph.state.custom_values[key] = normalizeMemoryStateScalar(rawValue, 140);\n        }\n    }\n\r\n    const removeNodeIds = new Set((Array.isArray(update?.remove_node_ids) ? update.remove_node_ids : []).map(createMemoryId));\r\n    if (removeNodeIds.size) {\r\n        graph.nodes = graph.nodes.filter(node => !removeNodeIds.has(node.id));\r\n        graph.links = graph.links.filter(link => !removeNodeIds.has(link.source) && !removeNodeIds.has(link.target));\r\n    }\r\n\r\n    const byId = new Map(graph.nodes.map(node => [node.id, node]));\r\n    const incomingNodes = Array.isArray(update?.nodes) ? update.nodes : [];\n    for (const rawNode of incomingNodes.slice(0, 24)) {\n        const node = normalizeMemoryNode(applyMemoryTemporalContext(rawNode, graph, state), byId.size);\r\n        if (!isValidMemoryNodeForApply(node)) {\r\n            continue;\r\n        }\r\n        const existing = findExistingMemoryNodeForUpsert(graph, node, byId);\n        if (existing) {\n            const longTermFields = mergeMemoryNodeLongTermFields(existing, node);\n            Object.assign(existing, {\n                ...existing,\n                title: node.title || existing.title,\n                type: node.type || existing.type,\n                content: node.content || existing.content,\n                summary: node.summary || existing.summary,\r\n                location: node.location || existing.location,\r\n                timeSpan: node.timeSpan || existing.timeSpan,\r\n                keys: uniqueStrings([...(existing.keys || []), ...(node.keys || [])]),\n                tags: uniqueStrings([...(existing.tags || []), ...(node.tags || [])]),\n                aliases: longTermFields.aliases,\n                evidence: longTermFields.evidence,\n                status: longTermFields.status,\n                firstSeenFloor: Number.isFinite(longTermFields.firstSeenFloor) ? longTermFields.firstSeenFloor : existing.firstSeenFloor,\n                lastSeenFloor: Number.isFinite(longTermFields.lastSeenFloor) ? longTermFields.lastSeenFloor : existing.lastSeenFloor,\n                history: longTermFields.history,\n                importance: Math.max(Number(existing.importance || 0), Number(node.importance || 0)),\n                credibility: Math.max(Number(existing.credibility || 0), Number(node.credibility || 0)),\n                updatedAt: now,\n            });\n            byId.set(existing.id, existing);\r\n            addedOrUpdatedNodeCount += 1;\r\n            touchedEntries.push(existing);\r\n        } else {\r\n            graph.nodes.push(node);\r\n            byId.set(node.id, node);\r\n            addedOrUpdatedNodeCount += 1;\r\n            touchedEntries.push(node);\r\n        }\r\n    }\r\n\r\n    const updates = Array.isArray(update?.updates) ? update.updates : [];\n    for (const rawUpdate of updates.slice(0, 8)) {\n        const contextualUpdate = applyMemoryTemporalContext(rawUpdate, graph, state);\n        const resolvedId = resolveMemoryNodeId(contextualUpdate?.id || contextualUpdate?.title || contextualUpdate?.titleToUpdate, graph);\n        const updateTitle = contextualUpdate?.title || contextualUpdate?.titleToUpdate || '';\n        const updateType = normalizeMemoryNodeType(contextualUpdate);\n        const existing = byId.get(resolvedId)\n            || graph.nodes.find(item => normalizeText(item.title) === normalizeText(updateTitle) && String(item.type || 'event') === updateType);\n        if (!existing) {\n            continue;\n        }\n        if (contextualUpdate.title) {\r\n            existing.title = truncateText(contextualUpdate.title, 80);\r\n        }\r\n        if (contextualUpdate.content || contextualUpdate.newContent) {\r\n            existing.content = truncateText(contextualUpdate.content || contextualUpdate.newContent, 1400);\r\n        }\r\n        if (contextualUpdate.summary) {\r\n            existing.summary = truncateText(contextualUpdate.summary, 120);\r\n        }\r\n        if (contextualUpdate.location) {\r\n            existing.location = truncateText(contextualUpdate.location, 120);\r\n        }\r\n        if (contextualUpdate.timeSpan || contextualUpdate.time_span) {\r\n            existing.timeSpan = truncateText(contextualUpdate.timeSpan || contextualUpdate.time_span, 120);\r\n        }\r\n        if (contextualUpdate.keys) {\r\n            existing.keys = uniqueStrings([...(existing.keys || []), ...(Array.isArray(contextualUpdate.keys) ? contextualUpdate.keys : String(contextualUpdate.keys).split(/[,\\n;]+/u))]);\r\n        }\r\n        if (contextualUpdate.importance !== undefined) {\r\n            existing.importance = clampNumber(contextualUpdate.importance, existing.importance || 0.6, 0, 1);\r\n        }\r\n        if (contextualUpdate.credibility !== undefined) {\r\n            existing.credibility = clampNumber(contextualUpdate.credibility, existing.credibility || 0.8, 0, 1);\r\n        }\r\n        existing.updatedAt = now;\r\n        addedOrUpdatedNodeCount += 1;\r\n        touchedEntries.push(existing);\r\n    }\r\n\r\n    if (!addedOrUpdatedNodeCount) {\r\n        const fallbackNode = createMemoryFallbackNodeFromSummary(update?.summary, byId.size);\r\n        if (fallbackNode) {\r\n            const fallbackTemporal = getMemoryNodeTemporalContext(fallbackNode, graph, state);\r\n            fallbackNode.location = fallbackNode.location || fallbackTemporal.location;\r\n            fallbackNode.timeSpan = fallbackNode.timeSpan || fallbackTemporal.timeSpan;\r\n            graph.nodes.push(fallbackNode);\r\n            byId.set(fallbackNode.id, fallbackNode);\r\n            addedOrUpdatedNodeCount += 1;\r\n            touchedEntries.push(fallbackNode);\r\n        }\r\n    }\r\n\r\n    graph.nodes = graph.nodes\r\n        .sort((a, b) => (Number(b.importance || 0) - Number(a.importance || 0)) || String(b.updatedAt || '').localeCompare(String(a.updatedAt || '')))\r\n        .slice(0, clampNumber(settings.memoryMaxNodes, 60, 5, 200));\r\n\r\n    const nodeIds = new Set(graph.nodes.map(node => node.id));\r\n    const removeLinkIds = new Set((Array.isArray(update?.remove_link_ids) ? update.remove_link_ids : []).map(value => String(value)));\r\n    graph.links = graph.links.filter(link => !removeLinkIds.has(String(link.id)) && nodeIds.has(link.source) && nodeIds.has(link.target));\r\n\r\n    const incomingLinks = Array.isArray(update?.links) ? update.links : [];\r\n    const existingLinkKeys = new Set(graph.links.map(link => `${link.source}|${link.type}|${link.target}`));\r\n    for (const rawLink of incomingLinks.slice(0, 12)) {\r\n        const normalized = normalizeMemoryLink({\r\n            ...rawLink,\r\n            source: resolveMemoryNodeId(rawLink?.source || rawLink?.sourceId || rawLink?.from, graph),\r\n            target: resolveMemoryNodeId(rawLink?.target || rawLink?.targetId || rawLink?.to, graph),\r\n        }, nodeIds, graph.links.length);\r\n        if (!normalized) {\r\n            continue;\r\n        }\r\n        const key = `${normalized.source}|${normalized.type}|${normalized.target}`;\r\n        const existing = graph.links.find(link => `${link.source}|${link.type}|${link.target}` === key);\r\n        if (existing) {\r\n            existing.weight = Math.max(Number(existing.weight || 0), Number(normalized.weight || 0));\r\n            existing.description = normalized.description || existing.description;\r\n            existing.updatedAt = now;\r\n            continue;\r\n        }\r\n        if (!existingLinkKeys.has(key)) {\r\n            graph.links.push(normalized);\r\n            existingLinkKeys.add(key);\r\n        }\r\n    }\r\n    for (const temporalLink of buildMemoryAutoTemporalLinks(graph.nodes, graph.links, 14)) {\r\n        graph.links.push(temporalLink);\r\n    }\r\n\r\n    graph.links = graph.links\r\n        .filter(link => nodeIds.has(link.source) && nodeIds.has(link.target))\r\n        .slice(-clampNumber(settings.memoryMaxLinks, 120, 0, 400));\r\n    if (typeof update?.summary === 'string' && update.summary.trim()) {\r\n        graph.lastSummary = truncateText(update.summary, 900);\r\n    }\r\n    graph.updatedAt = now;\r\n    saveMemoryGraph(graph, context, false, { fit: true });\r\n    return {\r\n        graph,\r\n        touchedEntries: uniqueStrings(touchedEntries.map(entry => entry?.id))\r\n            .map(id => graph.nodes.find(node => node.id === id))\r\n            .filter(Boolean),\r\n    };\r\n}\r\n\r\nfunction stopMemoryAnimation(success = true) {\r\n    const underline = $('#ai_wbr_fx_layer .ai-wbr-book-underline');\r\n    if (!underline.length) {\r\n        return;\r\n    }\r\n    if (success) {\r\n        setTimeout(() => underline.remove(), 120);\r\n    } else {\r\n        underline.addClass('ai-wbr-book-underline-error');\r\n        setTimeout(() => underline.remove(), 980);\r\n    }\r\n}\r\n\r\nasync function runHistoryMemoryImport() {\r\n    if (!settings.memoryEnabled || isMemoryWorkerRunning || isRouterSelectionRequest) {\r\n        toastr?.warning?.('Please try again later.', 'AI Worldbook Router');\r\n        return false;\r\n    }\r\n\r\n    const context = getContext();\r\n    const memoryRunChatKey = getCurrentChatMemoryKey(context);\r\n    const rangeInfo = getHistoryImportRangeFromUi(context);\r\n    const recentMessages = rangeInfo.messages;\r\n    buildHistoryImportPreview(rangeInfo, context);\r\n\r\n    if (recentMessages.length < 2) {\r\n        setMemoryStatus('历史补录失败：所选楼层不足 2 条可整理消息', context);\r\n        toastr?.warning?.('Please try again later.', 'AI Worldbook Router');\r\n        return false;\r\n    }\r\n\r\n    const mode = String($('#ai_wbr_memory_history_mode').val() || settings.memoryHistoryMode || 'history');\r\n    saveSetting('memoryHistoryMode', mode);\r\n    const skipDone = !!$('#ai_wbr_memory_history_skip_done').prop('checked');\r\n    saveSetting('memoryHistorySkipDone', skipDone);\r\n\r\n    const signature = getHistoryImportSignature(recentMessages, context);\r\n    const existingRecord = getHistoryImportRecord(signature, context);\r\n    if (skipDone && existingRecord) {\r\n            setMemoryStatus('Importing history memory...', context);\r\n        toastr?.info?.('Done.', 'AI Worldbook Router');\r\n        return false;\r\n    }\r\n\r\n    const source = {\r\n        ...getMemorySourceSnapshot(context, recentMessages, recentMessages.length),\r\n        id: signature,\r\n        mode: 'history',\r\n        importMode: mode,\r\n        scanMessages: recentMessages.length,\r\n        rangeStartFloor: rangeInfo.startFloor,\r\n        rangeEndFloor: rangeInfo.endFloor,\r\n        indices: recentMessages.map(message => message.index).filter(index => Number.isFinite(index)),\r\n    };\r\n\r\n    isMemoryWorkerRunning = true;\r\n    startMemoryAnimation();\r\n            setMemoryStatus('Importing history memory...', context);\r\n\r\n    try {\r\n        const graph = getMemoryGraph(context);\r\n        const prompt = buildMemoryExtractionPrompt(recentMessages, graph)\r\n            + `\\n\\n<history_import_mode>${mode === 'summary' ? 'Summary mode: compress this range into durable plot, relationship, and open-question notes.' : 'History mode: extract reusable facts, relationships, settings, promises, and key events.'}</history_import_mode>`\r\n            + `\\n<history_import_range>Floors ${rangeInfo.startFloor}-${rangeInfo.endFloor}; ${recentMessages.length} valid messages.</history_import_range>`;\r\n        const maxAttempts = Math.max(1, Number(settings.memoryRetries || 0) + 1);\r\n        const baseSystemPrompt = 'Return only the requested AIWBR variable block. Do not explain, reason, use markdown, or wrap in JSON.';\r\n        const retrySystemPrompt = 'Your previous response was invalid. Return only the AIWBR variable block, with no prose or markdown.';\r\n        let raw = '';\r\n        let lastError = null;\r\n        setCurrentMemoryLastRaw('', context);\r\n        setCurrentMemoryLastError('', context);\r\n\r\n        for (let attempt = 1; attempt <= maxAttempts; attempt += 1) {\n            const useRetryPrompt = attempt > 1;\n            const activePrompt = useRetryPrompt ? buildMemoryExtractionRetryPrompt(recentMessages, graph) : prompt;\n            const activeSystemPrompt = useRetryPrompt ? retrySystemPrompt : baseSystemPrompt;\n            const promptLog = useRetryPrompt\r\n                ? `${prompt}\\n\\n----- HISTORY RETRY #${attempt - 1} PROMPT -----\\n\\n${activePrompt}`\r\n                : prompt;\r\n            setCurrentMemoryLastPrompt(promptLog, context);\r\n\r\n            try {\n                isRouterSelectionRequest = true;\n                raw = await requestMemoryExtraction(context, activePrompt, activeSystemPrompt, {\n                    type: 'memory',\n                    label: useRetryPrompt ? '历史补录记忆重试' : '历史补录记忆',\n                    roundLabel: '记忆 API 调用',\n                    retry: useRetryPrompt,\n                    source: mode,\n                });\n            } catch (error) {\n                lastError = error;\r\n                if (isGatewayLikeError(error) || attempt >= maxAttempts) {\r\n                    throw error;\r\n                }\r\n                playStatusBurst('retry', 'retry');\r\n                continue;\r\n            } finally {\r\n                isRouterSelectionRequest = false;\r\n            }\r\n\r\n            setCurrentMemoryLastRaw(summarizeRouterResponse(raw), context);\r\n\r\n            if (hasEmptyVisibleContentDueToLength(raw)) {\r\n                lastError = new Error('Memory extraction returned empty visible content due to length.');\r\n                if (attempt >= maxAttempts) {\r\n                    throw lastError;\r\n                }\r\n                playStatusBurst('retry', 'retry');\r\n                continue;\r\n            }\r\n\r\n            try {\r\n                let update = parseMemoryUpdate(raw, promptLog);\r\n                update = ensureMemoryUpdateHasVisibleFallback(update, recentMessages, mode);\r\n                setCurrentMemoryLastError('', context);\r\n                if (getCurrentChatMemoryKey() !== memoryRunChatKey) {\r\n                    debugLog('Discarded history memory import because chat changed before completion', {\r\n                        started: memoryRunChatKey,\r\n                        current: getCurrentChatMemoryKey(),\r\n                    });\r\n                    return false;\r\n                }\r\n\r\n                const container = getChatMemoryContainer(context);\r\n                container.graphBackup = cloneMemoryGraph(graph);\r\n                persistChatMemoryContainer(container, context);\r\n\r\n                if (settings.memoryReviewRequired) {\r\n                    const review = enqueueMemoryReview(update, {\r\n                        reason: 'history_import',\r\n                        signature,\r\n                        source,\r\n                        prompt: promptLog,\r\n                        raw: summarizeRouterResponse(raw),\r\n                    }, context);\r\n                    recordHistoryImport(signature, source, context);\r\n                    setCurrentMemorySource(source, context);\r\n            setMemoryStatus('Importing history memory...', context);\r\n                    playStatusBurst('ok', 'memory');\r\n                    toastr?.info?.('Done.', 'AI Worldbook Router');\r\n                    stopMemoryAnimation(true);\r\n                    return true;\r\n                }\r\n\r\n                const memoryResult = applyMemoryGraphUpdate(update, context);\r\n                recordHistoryImport(signature, source, context);\r\n                setCurrentMemorySource(source, context);\r\n            setMemoryStatus('Importing history memory...', context);\r\n                if (memoryResult.touchedEntries.length) {\r\n                    playEntryBurst(memoryResult.touchedEntries, {\r\n                        variant: 'memory',\r\n                        labelGetter: getMemoryBurstLabel,\r\n                    });\r\n                } else {\r\n                    playStatusBurst('ok', 'memory');\r\n                }\r\n                stopMemoryAnimation(true);\r\n                return true;\r\n            } catch (error) {\r\n                lastError = error;\r\n                setCurrentMemoryLastError(error?.message || String(error), context);\r\n                if (attempt >= maxAttempts) {\r\n                    throw error;\r\n                }\r\n                playStatusBurst('retry', 'retry');\r\n            }\r\n        }\r\n\r\n        throw lastError || new Error('History memory import failed.');\r\n    } catch (error) {\r\n        console.warn(`${LOG_PREFIX} History memory import failed`, error);\r\n        setCurrentMemoryLastError(error?.message || String(error), context);\r\n        if (error?.routerRaw && !getCurrentMemoryLastRaw(context)) {\r\n            setCurrentMemoryLastRaw(error.routerRaw, context);\r\n        }\r\n        if (error?.routerPrompt && !getCurrentMemoryLastPrompt(context)) {\r\n            setCurrentMemoryLastPrompt(error.routerPrompt, context);\r\n        }\r\n            setMemoryStatus('Importing history memory...', context);\r\n        playStatusBurst('fail', 'fail');\r\n        stopMemoryAnimation(false);\r\n        return false;\r\n    } finally {\r\n        isMemoryWorkerRunning = false;\r\n        buildHistoryImportPreview(getHistoryImportRangeFromUi(context), context);\r\n        renderMemoryPanel();\r\n    }\r\n}\r\n\r\nasync function runMemoryGraphUpdate(reason = 'realtime', options = {}) {\r\n    if (!settings.memoryEnabled) {\r\n        setMemoryStatus('实时整理未运行：记忆功能未启用。');\r\n        return false;\r\n    }\r\n    if (reason === 'manual' || reason === 'manual_summary') {\n        beginApiStatsRound(reason === 'manual_summary' ? '手动归纳 API 调用' : '手动记忆 API 调用');\n    }\n    if (isMemoryWorkerRunning) {\n        const busyMs = Date.now() - Number(memoryWorkerStartedAt || 0);\r\n        if (!options.force || busyMs < 120000) {\r\n            setMemoryStatus('实时整理跳过：上一轮记忆整理仍在运行。');\r\n            return false;\r\n        }\r\n        console.warn(`${LOG_PREFIX} Reset stale memory worker lock`, { busyMs });\r\n        isMemoryWorkerRunning = false;\r\n        memoryWorkerStartedAt = 0;\r\n    }\r\n    if (isRouterSelectionRequest && !options.force) {\r\n        setMemoryStatus('实时整理跳过：路由请求正在使用模型。');\r\n        return false;\r\n    }\r\n\r\n    const context = getContext();\r\n    const memoryRunChatKey = getCurrentChatMemoryKey(context);\r\n    const chat = Array.isArray(context?.chat) ? context.chat : [];\r\n    const mode = options.mode || (reason === 'summary' || reason === 'manual_summary' ? 'summary' : 'realtime');\r\n    const scanMessages = clampNumber(\r\n        options.scanMessages ?? (mode === 'summary' ? settings.memorySummaryScanMessages : settings.memoryRealtimeScanMessages),\r\n        mode === 'summary' ? defaultSettings.memorySummaryScanMessages : defaultSettings.memoryRealtimeScanMessages,\r\n        2,\r\n        40,\r\n    );\r\n    const recentMessages = getMemoryRelevantMessages(chat, scanMessages);\r\n    if (recentMessages.length < 2) {\r\n        setMemoryStatus('实时整理跳过：可整理消息不足 2 条。');\r\n        return false;\r\n    }\r\n\r\n    const signature = getMemoryTurnSignature(recentMessages);\r\n    const source = {\r\n        ...getMemorySourceSnapshot(context, recentMessages, scanMessages),\r\n        mode,\r\n        scanMessages,\r\n    };\r\n    if (mode === 'realtime' && reason !== 'manual' && !options.force && signature && signature === getCurrentMemoryLastTurnSignature(context)) {\r\n        setMemoryStatus('实时整理跳过：当前聊天图谱已是最新。');\r\n        return false;\r\n    }\r\n\r\n    isMemoryWorkerRunning = true;\n    memoryWorkerStartedAt = Date.now();\n    startMemoryAnimation();\n    setMemoryStatus(mode === 'summary' ? '间隔归纳整理中...' : '实时记忆整理中...');\n    startRuntimeActivity(\n        'graph',\n        reason === 'pre_route' ? '路由前更新图谱中' : '更新图谱中',\n        mode === 'summary' ? '正在归纳阶段记忆。' : '正在整理最新聊天记忆。',\n    );\n\n    try {\n        const graph = getMemoryGraph();\r\n        const prompt = buildMemoryExtractionPrompt(recentMessages, graph);\r\n        setCurrentMemoryLastRaw('', context);\r\n        setCurrentMemoryLastError('', context);\r\n        const maxAttempts = Math.max(1, Number(settings.memoryRetries || 0) + 1);\r\n        const baseSystemPrompt = 'Return only the requested AIWBR variable block. Do not explain, reason, use markdown, or wrap in JSON.';\r\n        const retrySystemPrompt = 'Your previous response was invalid. Return only the AIWBR variable block, with no prose or markdown.';\r\n        let raw = '';\r\n        let lastError = null;\r\n\r\n        for (let attempt = 1; attempt <= maxAttempts; attempt += 1) {\n            const useRetryPrompt = attempt > 1;\n            const activePrompt = useRetryPrompt ? buildMemoryExtractionRetryPrompt(recentMessages, graph) : prompt;\n            const activeSystemPrompt = useRetryPrompt ? retrySystemPrompt : baseSystemPrompt;\n            const promptLog = useRetryPrompt\r\n                ? `${prompt}\\n\\n----- RETRY #${attempt - 1} PROMPT -----\\n\\n${activePrompt}`\r\n                : prompt;\r\n            setCurrentMemoryLastPrompt(promptLog, context);\r\n\r\n            try {\n                isRouterSelectionRequest = true;\n                raw = await requestMemoryExtraction(context, activePrompt, activeSystemPrompt, {\n                    type: reason === 'pre_route' ? 'preRefresh' : 'memory',\n                    label: reason === 'pre_route'\n                        ? (useRetryPrompt ? '路由前记忆预刷新重试' : '路由前记忆预刷新')\n                        : (useRetryPrompt ? '实时记忆整理重试' : '实时记忆整理'),\n                    roundLabel: reason === 'pre_route' ? '预刷新 API 调用' : '记忆 API 调用',\n                    retry: useRetryPrompt,\n                    source: mode,\n                });\n            } catch (error) {\n                lastError = error;\r\n                if (isGatewayLikeError(error) || attempt >= maxAttempts) {\r\n                    throw error;\r\n                }\r\n                playStatusBurst('retry', 'retry');\r\n                continue;\r\n            } finally {\r\n                isRouterSelectionRequest = false;\r\n            }\r\n\r\n            setCurrentMemoryLastRaw(summarizeRouterResponse(raw), context);\r\n\r\n            if (hasEmptyVisibleContentDueToLength(raw)) {\r\n                lastError = new Error('Memory extraction returned empty visible content due to length.');\r\n                if (attempt >= maxAttempts) {\r\n                    throw lastError;\r\n                }\r\n                playStatusBurst('retry', 'retry');\r\n                continue;\r\n            }\r\n\r\n            try {\r\n                let update = parseMemoryUpdate(raw, promptLog);\r\n                update = ensureMemoryUpdateHasVisibleFallback(update, recentMessages, mode);\r\n                setCurrentMemoryLastError('', context);\r\n                if (getCurrentChatMemoryKey() !== memoryRunChatKey) {\r\n                    debugLog('Discarded memory update because chat changed before completion', {\r\n                        started: memoryRunChatKey,\r\n                        current: getCurrentChatMemoryKey(),\r\n                    });\r\n                    return false;\r\n                }\r\n                if (!isMemorySourceCurrent(source, context)) {\r\n                    debugLog('Discarded memory update because source floor changed before completion', {\r\n                        source,\r\n                        current: getMemorySourceSnapshot(context, null, scanMessages),\r\n                    });\r\n                    setCurrentMemoryLastTurnSignature('', context);\r\n            setMemoryStatus('Importing history memory...', context);\r\n                    stopMemoryAnimation(false);\r\n                    return false;\r\n                }\r\n                \r\n                // Backup graph before applying update\r\n                const container = getChatMemoryContainer(context);\r\n                container.graphBackup = cloneMemoryGraph(graph);\r\n                persistChatMemoryContainer(container, context);\r\n                \r\n                let memoryResult = null;\r\n                const shouldQueueReview = !!settings.memoryReviewRequired && options.review === true;\r\n                if (shouldQueueReview) {\r\n                    const review = enqueueMemoryReview(update, {\r\n                        reason,\r\n                        signature,\r\n                        source,\r\n                        prompt: promptLog,\r\n                        raw: summarizeRouterResponse(raw),\r\n                    }, context);\r\n                    setCurrentMemorySource(source, context);\r\n                    setCurrentMemoryLastTurnSignature(signature, context);\r\n                    const updatedContainer = getChatMemoryContainer(context);\r\n                    const messageCount = getMemoryAutoRunMessageCount(context);\r\n                    if (mode === 'summary') {\r\n                        updatedContainer.lastSummaryMessageCount = messageCount;\r\n                    } else {\r\n                        updatedContainer.lastAutoMessageCount = messageCount;\r\n                    }\r\n                    persistChatMemoryContainer(updatedContainer, context);\r\n                    setMemoryStatus(`已生成待确认记忆更新：${getMemoryReviewQueue(context).length} 条。确认后才会写入图谱。`, context);\r\n                    playStatusBurst('ok', 'memory');\n                    toastr?.info?.('Done.', 'AI Worldbook Router');\n                    stopMemoryAnimation(true);\n                    finishRuntimeActivityStage('graph', `待确认 ${getMemoryReviewQueue(context).length} 条`);\n                    return true;\n                }\n\n                memoryResult = applyMemoryGraphUpdate(update, context);\n                setCurrentMemorySource(source, context);\n                setCurrentMemoryLastTurnSignature(signature, context);\n                const updatedContainer = getChatMemoryContainer(context);\r\n                const messageCount = getMemoryAutoRunMessageCount(context);\r\n                if (mode === 'summary') {\r\n                    updatedContainer.lastSummaryMessageCount = messageCount;\r\n                } else {\r\n                    updatedContainer.lastAutoMessageCount = messageCount;\r\n                }\r\n                persistChatMemoryContainer(updatedContainer, context);\r\n                const nodeCount = memoryResult.graph.nodes.length;\n                const linkCount = memoryResult.graph.links.length;\n                finishRuntimeActivityStage('graph', `节点 ${nodeCount} · 关系 ${linkCount}`);\n                setMemoryStatus(`记忆图谱已更新：${nodeCount} 个节点，${linkCount} 条关系。`, context);\n                if (memoryResult.touchedEntries.length) {\r\n                    playEntryBurst(memoryResult.touchedEntries, {\r\n                        variant: 'memory',\r\n                        labelGetter: getMemoryBurstLabel,\r\n                    });\r\n                } else {\r\n                    playStatusBurst('ok', 'memory');\r\n                }\r\n                stopMemoryAnimation(true);\r\n                return true;\r\n            } catch (error) {\r\n                lastError = error;\r\n                setCurrentMemoryLastError(error?.message || String(error), context);\r\n                if (attempt >= maxAttempts) {\r\n                    throw error;\r\n                }\r\n                playStatusBurst('retry', 'retry');\r\n            }\r\n        }\r\n\r\n        throw lastError || new Error('Memory extraction failed.');\r\n    } catch (error) {\n        console.warn(`${LOG_PREFIX} Memory graph update failed`, error);\n        failRuntimeActivity('graph', error);\n        setCurrentMemoryLastError(error?.message || String(error), context);\n        if (error?.routerRaw && !getCurrentMemoryLastRaw(context)) {\r\n            setCurrentMemoryLastRaw(error.routerRaw, context);\r\n        }\r\n        if (error?.routerPrompt && !getCurrentMemoryLastPrompt(context)) {\r\n            setCurrentMemoryLastPrompt(error.routerPrompt, context);\r\n        }\r\n        setMemoryStatus(`记忆失败：${truncateText(error?.message || error, 80)}`);\r\n        playStatusBurst('fail', 'fail');\r\n        stopMemoryAnimation(false);\r\n        return false;\r\n    } finally {\r\n        isMemoryWorkerRunning = false;\r\n        memoryWorkerStartedAt = 0;\r\n        renderMemoryPanel();\r\n    }\r\n}\r\n\r\nfunction scheduleMemoryGraphUpdate(options = {}) {\n    if (!settings.memoryEnabled) {\n        setMemoryStatus('实时整理未运行：记忆功能未启用。');\n        return;\n    }\n\r\n    const context = getContext();\r\n    const currentCount = getMemoryAutoRunMessageCount(context);\r\n    const container = getChatMemoryContainer(context);\r\n    const shouldRunRealtime = !!settings.memoryRealtimeEnabled;\r\n    const summaryInterval = clampNumber(settings.memorySummaryIntervalMessages, defaultSettings.memorySummaryIntervalMessages, 1, 100);\r\n    const lastSummaryCount = clampNumber(container.lastSummaryMessageCount || container.lastAutoMessageCount, 0, 0, 1000000);\r\n    const shouldRunSummary = !!settings.memorySummaryEnabled && currentCount - lastSummaryCount >= summaryInterval;\r\n\r\n    if (!shouldRunRealtime && !shouldRunSummary) {\r\n        debugLog('Memory update skipped', { currentCount, lastSummaryCount, summaryInterval });\r\n        setMemoryStatus(`实时整理跳过：实时整理关闭，或摘要还差 ${Math.max(0, summaryInterval - (currentCount - lastSummaryCount))} 条消息。`);\r\n        return;\r\n    }\r\n\r\n    clearTimeout(memoryUpdateTimer);\n    memoryUpdateTimer = setTimeout(async () => {\n        if (shouldRunRealtime) {\n            const forceRealtime = String(getCurrentMemoryLastTurnSignature(context) || '') === '';\n            const updated = await runMemoryGraphUpdate(options.reason || 'realtime', {\n                mode: 'realtime',\n                scanMessages: settings.memoryRealtimeScanMessages,\n                force: forceRealtime || !!options.force,\n            });\n            if (!updated && (isMemoryWorkerRunning || isRouterSelectionRequest) && !options.retry) {\n                setTimeout(() => scheduleMemoryGraphUpdate({ ...options, retry: true }), 2200);\n            }\n        }\n        if (shouldRunSummary) {\n            if (shouldRunRealtime) {\n                await new Promise(resolve => setTimeout(resolve, 650));\n            }\n            await runMemoryGraphUpdate('summary', {\r\n                mode: 'summary',\r\n                scanMessages: settings.memorySummaryScanMessages,\r\n            });\r\n        }\r\n    }, 900);\r\n}\r\n\r\nasync function preRefreshMemoryForRoute(context = getContext()) {\r\n    const startedAt = Date.now();\r\n    const result = {\r\n        attempted: false,\r\n        completed: false,\r\n        timedOut: false,\r\n        updated: false,\r\n        skippedReason: '',\r\n        durationMs: 0,\r\n    };\r\n\r\n    if (!settings.memoryEnabled) {\r\n        result.skippedReason = 'memory-disabled';\r\n        return result;\r\n    }\r\n    if (!settings.memoryRealtimeEnabled) {\r\n        result.skippedReason = 'realtime-disabled';\r\n        return result;\r\n    }\r\n    if (isMemoryWorkerRunning || isRouterSelectionRequest) {\r\n        result.skippedReason = 'memory-busy';\r\n        return result;\r\n    }\r\n\r\n    const chat = Array.isArray(context?.chat) ? context.chat : [];\r\n    const scanMessages = clampNumber(settings.memoryRealtimeScanMessages, defaultSettings.memoryRealtimeScanMessages, 2, 40);\r\n    const recentMessages = getMemoryRelevantMessages(chat, scanMessages);\r\n    if (recentMessages.length < 2) {\r\n        result.skippedReason = 'not-enough-messages';\r\n        return result;\r\n    }\r\n\r\n    const signature = getMemoryTurnSignature(recentMessages);\r\n    if (signature && signature === getCurrentMemoryLastTurnSignature(context)) {\r\n        result.skippedReason = 'already-current';\r\n        return result;\r\n    }\r\n\r\n    result.attempted = true;\r\n    const updatePromise = runMemoryGraphUpdate('pre_route', {\r\n        mode: 'realtime',\r\n        scanMessages,\r\n    })\r\n        .then(updated => {\r\n            result.completed = true;\r\n            result.updated = !!updated;\r\n            return result;\r\n        })\r\n        .catch(error => {\r\n            result.completed = true;\r\n            result.skippedReason = error?.message || String(error);\r\n            console.warn(`${LOG_PREFIX} Pre-route memory refresh failed.`, error);\r\n            return result;\r\n        });\r\n\r\n    const timeoutMs = 1800;\r\n    const timeoutPromise = new Promise(resolve => {\r\n        setTimeout(() => {\r\n            result.timedOut = true;\r\n            result.skippedReason = 'timeout';\r\n            resolve(result);\r\n        }, timeoutMs);\r\n    });\r\n\r\n    const settled = await Promise.race([updatePromise, timeoutPromise]);\r\n    updatePromise.finally(() => renderDebugPanel()).catch(() => {});\r\n    settled.durationMs = Date.now() - startedAt;\r\n    return settled;\r\n}\r\n\r\nfunction normalizeWorldEntry(rawEntry, source, worldName, index) {\r\n    const id = getEntryId(rawEntry, index);\r\n    return {\r\n        ...rawEntry,\r\n        routerId: `${source}:${worldName || 'embedded'}:${id}`,\r\n        uid: id,\r\n        source,\r\n        world: worldName || '',\r\n        comment: String(rawEntry.comment || rawEntry.memo || ''),\r\n        content: String(rawEntry.content || ''),\r\n        constant: !!rawEntry.constant,\r\n        disable: !!rawEntry.disable || rawEntry.enabled === false,\r\n        order: Number(rawEntry.order ?? rawEntry.insertion_order ?? 0),\r\n        keys: getEntryKeys(rawEntry),\r\n    };\r\n}\r\n\r\nfunction worldEntriesFromData(data, source, worldName) {\r\n    if (!data?.entries) {\r\n        return [];\r\n    }\r\n\r\n    const entries = Array.isArray(data.entries) ? data.entries : Object.values(data.entries);\r\n    return entries.map((entry, index) => normalizeWorldEntry(entry, source, worldName, index));\r\n}\r\n\r\nasync function getEmbeddedCharacterEntries(context) {\r\n    const character = context.characters?.[context.characterId];\r\n    const book = character?.data?.character_book;\r\n    if (!book?.entries?.length) {\r\n        return [];\r\n    }\r\n\r\n    const worldName = book.name || character?.name || 'embedded';\r\n    if (!getWorldSelectionState(worldName)) {\r\n        return [];\r\n    }\r\n\r\n    const converted = convertCharacterBook(book);\r\n    return worldEntriesFromData(converted, 'character_book', worldName);\r\n}\r\n\r\nasync function getLinkedWorldEntries(context) {\r\n    const character = context.characters?.[context.characterId];\r\n    const worldSources = new Map();\r\n\r\n    const addWorld = (worldName, source) => {\r\n        if (worldName && !worldSources.has(worldName)) {\r\n            worldSources.set(worldName, source);\r\n        }\r\n    };\r\n\r\n    for (const worldName of selected_world_info || []) {\r\n        addWorld(worldName, 'global_world');\r\n    }\r\n\r\n    addWorld(context.chatMetadata?.[METADATA_KEY], 'chat_world');\r\n    addWorld(character?.data?.extensions?.world, 'character_world');\r\n    addWorld(context.powerUserSettings?.persona_description_lorebook, 'persona_world');\r\n\r\n    try {\r\n        const fileName = context.characterId !== undefined ? getCharaFilename(context.characterId) : '';\r\n        const extraCharLore = world_info.charLore?.find(entry => entry.name === fileName);\r\n        for (const worldName of extraCharLore?.extraBooks || []) {\r\n            addWorld(worldName, 'character_extra_world');\r\n        }\r\n    } catch (error) {\r\n        debugLog('Could not read character extra world bindings', error);\r\n    }\r\n\r\n    const allEntries = [];\r\n    for (const [worldName, source] of worldSources.entries()) {\r\n        if (!getWorldSelectionState(worldName)) {\r\n            debugLog('Skipped unselected worldbook', { worldName, source });\r\n            continue;\r\n        }\r\n        try {\r\n            const data = await loadWorldInfo(worldName);\r\n            allEntries.push(...worldEntriesFromData(data, source, worldName));\r\n        } catch (error) {\r\n            console.warn(`${LOG_PREFIX} Failed to load world info \"${worldName}\"`, error);\r\n        }\r\n    }\r\n\r\n    return allEntries;\r\n}\r\n\r\nasync function getWorldbookEntries(context) {\r\n    const [embedded, linked] = await Promise.all([\r\n        getEmbeddedCharacterEntries(context),\r\n        getLinkedWorldEntries(context),\r\n    ]);\r\n\r\n    const deduped = [];\r\n    const seen = new Set();\r\n    for (const entry of [...embedded, ...linked]) {\r\n        const key = `${entry.world}:${entry.uid}:${entry.content}`;\r\n        if (seen.has(key)) {\r\n            continue;\r\n        }\r\n        seen.add(key);\r\n        deduped.push(entry);\r\n    }\r\n\r\n    return deduped;\r\n}\r\n\r\nfunction scoreEntry(entry, matchText, lastUserText, recentText) {\r\n    let score = 0;\r\n    const matchedKeys = new Set();\r\n    const matchedSignals = new Set();\r\n    const lowerMatchText = normalizeText(matchText);\r\n    const lowerLastUserText = normalizeText(lastUserText);\r\n    const comment = normalizeText(entry.comment);\r\n    const content = normalizeText(truncateText(entry.content, 2400));\r\n    const haystack = `${comment}\\n${content}`;\r\n\r\n    for (const key of entry.keys.primary) {\r\n        const normalized = normalizeText(key).trim();\r\n        if (normalized && lowerMatchText.includes(normalized)) {\r\n            score += lowerLastUserText.includes(normalized) ? 14 : 10;\r\n            matchedKeys.add(key);\r\n        }\r\n    }\r\n\r\n    for (const key of entry.keys.secondary) {\r\n        const normalized = normalizeText(key).trim();\r\n        if (normalized && lowerMatchText.includes(normalized)) {\r\n            score += lowerLastUserText.includes(normalized) ? 6 : 4;\r\n            matchedKeys.add(key);\r\n        }\r\n    }\r\n\r\n    if (comment && lowerLastUserText && comment.includes(lowerLastUserText.slice(0, 16))) {\r\n        score += 2;\r\n    }\r\n\r\n    const lastUserTerms = extractQueryTerms(lastUserText);\r\n    const recentTerms = extractQueryTerms(recentText);\r\n\r\n    for (const term of lastUserTerms) {\r\n        const hits = countTermHits(haystack, term);\r\n        if (!hits) {\r\n            continue;\r\n        }\r\n\r\n        matchedSignals.add(term);\r\n        if (term.length >= 4) {\r\n            score += 6;\r\n        } else if (term.length === 3) {\r\n            score += 4;\r\n        } else {\r\n            score += 2;\r\n        }\r\n    }\r\n\r\n    for (const term of recentTerms) {\r\n        if (lastUserTerms.includes(term)) {\r\n            continue;\r\n        }\r\n\r\n        const hits = countTermHits(haystack, term);\r\n        if (!hits) {\r\n            continue;\r\n        }\r\n\r\n        matchedSignals.add(term);\r\n        score += term.length >= 3 ? 2 : 1;\r\n    }\r\n\r\n    if (!matchedKeys.size && matchedSignals.size && lowerLastUserText.includes('魔法') && haystack.includes('魔法')) {\r\n        score += 2;\r\n    }\r\n\r\n    if (entry.constant) {\r\n        score -= 3;\r\n    }\r\n\r\n    return { score, matchedKeys: [...matchedKeys], matchedSignals: [...matchedSignals] };\r\n}\r\n\r\nfunction recallCandidates(entries, recentMessages, mvuSummary) {\r\n    const lastUserMessage = getLastUserMessage(recentMessages);\r\n    const recentText = recentMessages.map(message => `${message.name}: ${message.text}`).join('\\n');\r\n    const matchText = [lastUserMessage, recentText, mvuSummary].filter(Boolean).join('\\n\\n');\r\n\r\n    const candidates = entries\r\n        .filter(entry => entry.content && !entry.disable)\r\n        .filter(entry => settings.allowConstant || !entry.constant)\r\n        .filter(entry => !getBlockedTitleRule(entry))\r\n        .map(entry => {\r\n            const { score, matchedKeys, matchedSignals } = scoreEntry(entry, matchText, lastUserMessage, recentText);\r\n            return { ...entry, score, matchedKeys, matchedSignals };\r\n        });\r\n\r\n    return candidates\r\n        .sort((a, b) => {\r\n            const aMatched = a.score > 0 ? 1 : 0;\r\n            const bMatched = b.score > 0 ? 1 : 0;\r\n            return (bMatched - aMatched) || (b.score - a.score) || (b.order - a.order);\r\n        })\r\n        .slice(0, settings.maxCandidates);\r\n}\r\n\r\nfunction buildAiPrompt(recentMessages, mvuSummary, candidates, maxSelectCount = settings.maxSelected) {\r\n    const lastUserMessage = getLastUserMessage(recentMessages);\r\n    const recentContext = recentMessages\r\n        .map(message => `${message.name || (message.isUser ? 'User' : 'Assistant')}: ${truncateText(message.text, MAX_ROUTER_CONTEXT_PREVIEW)}`)\r\n        .join('\\n\\n');\r\n    const candidateText = candidates.map(entry => {\r\n        const keys = entry.keys.all.length ? entry.keys.all.join(' / ') : '(无 keys)';\r\n        return `- ${keys}`;\r\n    }).join('\\n');\r\n\r\n    return `<task>\r\n从候选 keys 中选择最多 ${maxSelectCount} 条“本轮真正相关”的条目（世界书、动态记忆或资料片段）。\r\n</task>\r\n\r\n<rules>\r\n1. 只能从候选 keys 中选。\r\n2. 只输出严格 JSON。\r\n3. 禁止 Markdown、禁止解释、禁止 reasoning、禁止额外字段。\r\n4. 不要返回标题，不要返回 id，只返回命中的 key。\r\n5. 如果没有合适条目，输出 {\"selected\":[]}。\r\n</rules>\r\n\r\n<output_format>\r\n{\"selected\":[{\"key\":\"命中的 key\",\"reason\":\"简短原因\"}]}\r\n</output_format>\r\n\r\n<example>\r\n输入：\r\n最后用户消息：我想偷偷补完魔法阵然后逃跑\r\n候选 keys：\r\n- 魔法 / 魔法阵 / 画魔法\r\n- 奇夫利 / 老师\r\n- 魔法商品 / 魔法器\r\n输出：\r\n{\"selected\":[{\"key\":\"魔法阵\",\"reason\":\"用户正在补画魔法阵\"},{\"key\":\"奇夫利\",\"reason\":\"当前互动对象是奇夫利\"}]}\r\n</example>\r\n\r\n<last_user_message>\r\n${lastUserMessage || '(空)'}\r\n</last_user_message>\r\n\r\n<recent_context>\r\n${recentContext || '(空)'}\r\n</recent_context>\r\n\r\n<state_summary>\r\n${mvuSummary || '(未启用或未读取到)'}\r\n</state_summary>\r\n\r\n<candidate_keys>\r\n${candidateText || '(无)'}\r\n</candidate_keys>`;\r\n}\r\n\r\nfunction buildCompactAiPrompt(recentMessages, mvuSummary, candidates, maxSelectCount = settings.maxSelected) {\r\n    const lastUserMessage = truncateText(getLastUserMessage(recentMessages), 220);\r\n    const compactContext = recentMessages\r\n        .slice(-4)\r\n        .map(message => `${message.name || (message.isUser ? 'User' : 'Assistant')}: ${truncateText(message.text, 180)}`)\r\n        .join('\\n');\r\n    const compactSummary = truncateText(String(mvuSummary || ''), 320);\r\n    const candidateText = candidates\r\n        .slice(0, Math.min(candidates.length, 14))\r\n        .map(entry => `- ${(entry.keys.all.length ? entry.keys.all.join(' / ') : '(无 keys)')}`)\r\n        .join('\\n');\r\n\r\n    return `<task>从候选 keys 中选择最多 ${maxSelectCount} 条本轮相关条目（世界书、动态记忆或资料片段）。</task>\r\n<rules>只输出严格 JSON；禁止解释；禁止 reasoning；禁止额外字段；如果没有合适条目，输出 {\"selected\":[]}。</rules>\r\n<output>{\"selected\":[{\"key\":\"命中的 key\",\"reason\":\"简短原因\"}]}</output>\r\n<last_user_message>${lastUserMessage || '(空)'}</last_user_message>\r\n<recent_context>${compactContext || '(空)'}</recent_context>\r\n<state_summary>${compactSummary || '(无)'}</state_summary>\r\n<candidate_keys>\r\n${candidateText || '(无)'}\r\n</candidate_keys>`;\r\n}\r\n\r\nfunction getSelectionSchema() {\r\n    return {\r\n        name: 'ai_worldbook_router_selection',\r\n        value: {\r\n            type: 'object',\r\n            additionalProperties: false,\r\n            properties: {\r\n                selected: {\r\n                    type: 'array',\r\n                    items: {\r\n                        type: 'object',\r\n                        additionalProperties: false,\r\n                        properties: {\r\n                            key: { type: 'string' },\r\n                            reason: { type: 'string' },\r\n                        },\r\n                        required: ['key', 'reason'],\r\n                    },\r\n                },\r\n            },\r\n            required: ['selected'],\r\n        },\r\n        strict: true,\r\n    };\r\n}\r\n\r\nfunction normalizeSelectionPayload(payload) {\r\n    const selected = payload?.selected\r\n        ?? payload?.content?.selected\r\n        ?? payload?.message?.content?.selected;\r\n\r\n    if (!Array.isArray(selected)) {\r\n        throw new Error('AI selection JSON is missing selected[] or content.selected[]');\r\n    }\r\n\r\n    return selected;\r\n}\r\n\r\nfunction getEntrySelectionKeys(entry) {\r\n    return uniqueStrings(entry.keys.all.map(key => String(key).trim()).filter(Boolean));\r\n}\r\n\r\nfunction splitSelectionKey(value) {\r\n    return uniqueStrings(String(value ?? '')\r\n        .split(/[\\/|,，、；;\\n]+/u)\r\n        .map(part => part.trim())\r\n        .filter(Boolean));\r\n}\r\n\r\nfunction resolveEntryFromSelection(item, byId, byKey, candidates) {\r\n    const id = String(item?.id ?? '').trim();\r\n    if (id) {\r\n        const entryById = byId.get(id);\r\n        if (entryById) {\r\n            return entryById;\r\n        }\r\n    }\r\n\r\n    const rawKey = String(item?.key ?? '').trim();\r\n    if (!rawKey) {\r\n        return null;\r\n    }\r\n\r\n    const parts = splitSelectionKey(rawKey);\r\n    for (const part of parts) {\r\n        const entryByKey = byKey.get(normalizeText(part));\r\n        if (entryByKey) {\r\n            return entryByKey;\r\n        }\r\n    }\r\n\r\n    const normalizedRawKey = normalizeText(rawKey);\r\n    if (!normalizedRawKey) {\r\n        return null;\r\n    }\r\n\r\n    for (const candidate of candidates) {\r\n        const candidateKeys = getEntrySelectionKeys(candidate);\r\n        if (candidateKeys.some(key => normalizedRawKey.includes(normalizeText(key)))) {\r\n            return candidate;\r\n        }\r\n    }\r\n\r\n    return null;\r\n}\r\n\r\nfunction tryParseSelectionText(rawText) {\r\n    const text = String(rawText || '').trim();\r\n    if (!text) {\r\n        return null;\r\n    }\r\n\r\n    const withoutFence = text.replace(/^```(?:json)?/i, '').replace(/```$/i, '').trim();\r\n    const candidates = [withoutFence];\r\n    const firstBrace = withoutFence.indexOf('{');\r\n    const lastBrace = withoutFence.lastIndexOf('}');\r\n    if (firstBrace >= 0 && lastBrace > firstBrace) {\r\n        candidates.unshift(withoutFence.slice(firstBrace, lastBrace + 1));\r\n    }\r\n\r\n    for (const candidate of uniqueStrings(candidates)) {\r\n        try {\r\n            return normalizeSelectionPayload(JSON.parse(candidate));\r\n        } catch {\r\n            // Keep trying alternative text slices.\r\n        }\r\n    }\r\n\r\n    return null;\r\n}\r\n\r\nfunction collectRouterResponseTexts(rawResponse) {\r\n    if (rawResponse === null || rawResponse === undefined) {\r\n        return [];\r\n    }\r\n\r\n    if (typeof rawResponse === 'string') {\r\n        return [rawResponse];\r\n    }\r\n\r\n    const texts = [];\r\n    const stack = [rawResponse];\r\n    const seen = new WeakSet();\r\n\r\n    while (stack.length) {\r\n        const current = stack.pop();\r\n        if (!current || typeof current !== 'object') {\r\n            continue;\r\n        }\r\n\r\n        if (seen.has(current)) {\r\n            continue;\r\n        }\r\n        seen.add(current);\r\n\r\n        for (const [key, value] of Object.entries(current)) {\r\n            if (typeof value === 'string' && (\r\n                key === 'content'\r\n                || key === 'reasoning_content'\r\n                || key === 'text'\r\n                || key === 'output_text'\r\n            )) {\r\n                texts.push(value);\r\n            } else if (value && typeof value === 'object') {\r\n                stack.push(value);\r\n            }\r\n        }\r\n    }\r\n\r\n    return uniqueStrings(texts.map(text => text.trim()).filter(Boolean));\r\n}\r\n\r\nfunction stripReasoningForDisplay(text) {\r\n    return String(text ?? '')\r\n        .replace(/,\\s*\"reasoning\"\\s*:\\s*\"[\\s\\S]*?\"(?=\\s*[},])/gu, ', \"reasoning\":\"[filtered]\"')\r\n        .replace(/,\\s*\"reasoning_content\"\\s*:\\s*\"[\\s\\S]*?\"(?=\\s*[},])/gu, ', \"reasoning_content\":\"[filtered]\"')\r\n        .replace(/\"reasoning\"\\s*:\\s*\"[\\s\\S]*?\"(?=\\s*[},])/gu, '\"reasoning\":\"[filtered]\"')\r\n        .replace(/\"reasoning_content\"\\s*:\\s*\"[\\s\\S]*?\"(?=\\s*[},])/gu, '\"reasoning_content\":\"[filtered]\"');\r\n}\r\n\r\nfunction summarizeRouterResponse(rawResponse) {\r\n    if (rawResponse === null || rawResponse === undefined) {\r\n        return '(empty)';\r\n    }\r\n\r\n    if (typeof rawResponse === 'string') {\r\n        return stripReasoningForDisplay(rawResponse);\r\n    }\r\n\r\n    const texts = collectRouterResponseTexts(rawResponse)\r\n        .map(text => stripReasoningForDisplay(text))\r\n        .filter(text => String(text || '').trim());\r\n    if (texts.length) {\r\n        return texts.join('\\n\\n---\\n\\n');\r\n    }\r\n\r\n    try {\r\n        return stripReasoningForDisplay(JSON.stringify(rawResponse, null, 2));\r\n    } catch {\r\n        return stripReasoningForDisplay(String(rawResponse));\r\n    }\r\n}\r\n\r\nfunction createSelectionParseError(rawResponse, previewText = '', prompt = '') {\r\n    const error = new Error(`AI selection JSON parse failed. Preview: ${truncateText(previewText, 220) || '(empty)'}`);\r\n    error.routerPrompt = prompt;\r\n    error.routerRaw = summarizeRouterResponse(rawResponse);\r\n    return error;\r\n}\r\n\r\nfunction extractSelectionFromText(rawText, candidates) {\r\n    const text = String(rawText || '');\r\n    if (!text) {\r\n        return [];\r\n    }\r\n\r\n    const normalizedText = normalizeText(text);\r\n    const sentences = splitIntoSentences(text);\r\n    const positiveMarkers = [\r\n        'this is it', 'that is the answer', 'that\\'s the answer', 'bingo', 'perfect match',\r\n        'exact match', 'exactly', 'direct answer', 'most relevant', 'best match',\r\n        '正是', '就是这个', '答案', '最相关', '完全匹配', '直接回答',\r\n    ];\r\n    const negativeMarkers = [\r\n        'irrelevant', 'not relevant', 'no,', ' no ', 'not the answer', 'indirectly relevant',\r\n        'just a natural event', 'not what happens after', 'not about', 'irrelevant', 'not an answer',\r\n        '不是这个', '无关', '只是', '不对',\r\n    ];\r\n    const recovered = [];\r\n\r\n    for (const candidate of candidates) {\r\n        const entryKeys = getEntrySelectionKeys(candidate);\r\n        let score = 0;\r\n\r\n        for (const key of entryKeys) {\r\n            const normalizedKey = normalizeText(key);\r\n            const keyPattern = new RegExp(`[\"']?(?:key|keys?)[\"']?\\\\s*[:=]\\\\s*[\"']?${escapeRegex(key)}[\"']?`, 'u');\r\n            if (keyPattern.test(text)) {\r\n                score += 8;\r\n            }\r\n            if (normalizedKey && normalizedText.includes(normalizedKey)) {\r\n                score += normalizedKey.length >= 3 ? 4 : 2;\r\n            }\r\n        }\r\n\r\n        if (!score) {\r\n            continue;\r\n        }\r\n\r\n        for (const sentence of sentences) {\r\n            const normalizedSentence = normalizeText(sentence);\r\n            const mentionsCandidate = entryKeys.some(key => {\r\n                const normalizedKey = normalizeText(key);\r\n                return normalizedKey && normalizedSentence.includes(normalizedKey);\r\n            });\r\n            if (!mentionsCandidate) {\r\n                continue;\r\n            }\r\n\r\n            for (const marker of positiveMarkers) {\r\n                if (normalizedSentence.includes(marker)) {\r\n                    score += 6;\r\n                }\r\n            }\r\n            for (const marker of negativeMarkers) {\r\n                if (normalizedSentence.includes(marker)) {\r\n                    score -= 5;\r\n                }\r\n            }\r\n        }\r\n\r\n        recovered.push({\r\n            key: entryKeys[0] || '',\r\n            reason: 'Recovered from router response text.',\r\n            recoveryScore: score,\r\n        });\r\n    }\r\n\r\n    const positives = recovered\r\n        .filter(item => item.recoveryScore > 0)\r\n        .sort((a, b) => b.recoveryScore - a.recoveryScore);\r\n\r\n    if (!positives.length) {\r\n        return [];\r\n    }\r\n\r\n    const strongestScore = positives[0].recoveryScore;\r\n    const threshold = Math.max(6, strongestScore - 2);\r\n    return positives\r\n        .filter(item => item.recoveryScore >= threshold)\r\n        .slice(0, 2)\r\n        .map(({ recoveryScore, ...item }) => item);\r\n}\r\n\r\nfunction parseSelectionJson(rawResponse, candidates = [], prompt = '') {\r\n    if (rawResponse && typeof rawResponse === 'object') {\r\n        try {\r\n            return normalizeSelectionPayload(rawResponse);\r\n        } catch {\r\n            // Fall through to text extraction / recovery for provider-specific wrappers.\r\n        }\r\n    }\r\n\r\n    const texts = collectRouterResponseTexts(rawResponse);\r\n    for (const text of texts) {\r\n        const parsed = tryParseSelectionText(text);\r\n        if (parsed) {\r\n            return parsed;\r\n        }\r\n    }\r\n\r\n    const recovered = extractSelectionFromText(texts.join('\\n\\n'), candidates);\r\n    if (recovered.length) {\r\n        return recovered;\r\n    }\r\n\r\n    throw createSelectionParseError(rawResponse, texts.join(' '), prompt);\r\n}\r\n\r\nfunction getRouterMessages(prompt, systemPrompt = settings.systemPrompt) {\r\n    return [\r\n        { role: 'system', content: systemPrompt },\r\n        { role: 'user', content: prompt },\r\n    ];\r\n}\r\n\r\nfunction isEffectivelyEmptyStructuredResponse(rawResponse) {\r\n    if (rawResponse == null) {\r\n        return true;\r\n    }\r\n\r\n    if (typeof rawResponse === 'string') {\r\n        return !rawResponse.trim();\r\n    }\r\n\r\n    if (typeof rawResponse !== 'object') {\r\n        return false;\r\n    }\r\n\r\n    const content = rawResponse.content;\r\n    if (content && typeof content === 'object' && !Array.isArray(content) && !Object.keys(content).length) {\r\n        return true;\r\n    }\r\n\r\n    const texts = collectRouterResponseTexts(rawResponse);\r\n    if (!texts.length && !Object.keys(rawResponse).length) {\r\n        return true;\r\n    }\r\n\r\n    return false;\r\n}\r\n\r\nfunction hasEmptyVisibleContentDueToLength(rawResponse) {\r\n    const choice = rawResponse?.choices?.[0];\r\n    return !!choice\r\n        && choice?.finish_reason === 'length'\r\n        && typeof choice?.message?.content === 'string'\r\n        && !choice.message.content.trim();\r\n}\r\n\r\nfunction isGatewayLikeError(error) {\r\n    if (error?.isGatewayError) {\r\n        return true;\r\n    }\r\n    const text = `${error?.message || ''}\\n${String(error || '')}`.toLowerCase();\r\n    return /\\b(502|503|504)\\b/u.test(text)\r\n        || text.includes('gateway time-out')\r\n        || text.includes('gateway timeout')\r\n        || text.includes('endpoint failed');\r\n}\r\n\r\nfunction buildPlainSeparateChatPayload(prompt, {\r\n    systemPrompt = settings.systemPrompt,\r\n    maxTokens = getRouterRequestMaxTokens(),\r\n} = {}) {\r\n    return {\r\n        stream: false,\r\n        messages: getRouterMessages(prompt, systemPrompt),\r\n        model: settings.routerModel,\r\n        chat_completion_source: 'openai',\r\n        max_tokens: maxTokens,\r\n        temperature: 0,\r\n        reverse_proxy: normalizeUrl(settings.routerApiUrl),\r\n        proxy_password: String(settings.routerApiKey || ''),\r\n    };\r\n}\r\n\r\nasync function sendPlainSeparateChatRequest(context, payload, apiMeta = {}) {\n    const meta = {\n        type: 'router',\n        label: '独立模型路由',\n        roundLabel: '路由 API 调用',\n        ...apiMeta,\n    };\n    const apiItem = recordApiCallStart(meta.type, meta.label, meta);\n    try {\n        const response = await fetch('/api/backends/chat-completions/generate', {\n            method: 'POST',\n            headers: {\n                'Content-Type': 'application/json',\n                ...(typeof context.getRequestHeaders === 'function' ? context.getRequestHeaders() : {}),\n            },\n            cache: 'no-cache',\n            body: JSON.stringify(payload),\n        });\n\n        const text = await response.text();\n        if (!response.ok) {\n            let errorMessage = text;\n            try {\n                const parsed = JSON.parse(text);\n                errorMessage = parsed?.error?.message || parsed?.message || text;\n            } catch {\n                errorMessage = text.slice(0, 180);\n            }\n            const error = new Error(`Endpoint failed (${response.status}): ${errorMessage}`);\n            error.status = response.status;\n            error.isGatewayError = [502, 503, 504].includes(response.status);\n            throw error;\n        }\n\n        recordApiCallEnd(apiItem);\n        try {\n            return JSON.parse(text);\n        } catch {\n            return text;\n        }\n    } catch (error) {\n        recordApiCallEnd(apiItem, error);\n        throw error;\n    }\n}\n\r\nasync function sendSeparateRouterRequest(context, prompt, {\r\n    recentMessages = [],\r\n    mvuSummary = '',\r\n    candidates = [],\r\n    systemPrompt = settings.systemPrompt,\r\n    maxTokens = getRouterRequestMaxTokens(),\r\n    maxSelectCount = settings.maxSelected,\r\n} = {}) {\n    const firstRequest = buildPlainSeparateChatPayload(prompt, {\r\n        systemPrompt,\r\n        maxTokens,\r\n    });\r\n    let raw = await sendPlainSeparateChatRequest(context, firstRequest, {\n        type: 'router',\n        label: '前置路由选择',\n        roundLabel: '路由 API 调用',\n    });\n    if (!isEffectivelyEmptyStructuredResponse(raw) && !hasEmptyVisibleContentDueToLength(raw)) {\r\n        return { raw, usedRetry: false, usedCompactPrompt: false };\r\n    }\r\n\r\n    const compactPrompt = buildCompactAiPrompt(\r\n        recentMessages,\r\n        mvuSummary,\r\n        candidates,\r\n        maxSelectCount\r\n    );\r\n    const retryRequest = buildPlainSeparateChatPayload(compactPrompt, {\r\n        systemPrompt: 'Return strict JSON only: {\"selected\":[]}. Do not explain or include reasoning.',\r\n        maxTokens: getRouterRequestMaxTokens(),\r\n    });\r\n    raw = await sendPlainSeparateChatRequest(context, retryRequest, {\n        type: 'router',\n        label: '前置路由压缩重试',\n        roundLabel: '路由 API 调用',\n        retry: true,\n    });\n    return { raw, usedRetry: true, usedCompactPrompt: true, retryPrompt: compactPrompt };\r\n}\r\n\r\nasync function sendSeparateMemoryRequest(context, prompt, {\n    systemPrompt,\n    maxTokens = getMemoryRequestMaxTokens(),\n    apiMeta = {},\n} = {}) {\n    const requestData = buildPlainSeparateChatPayload(prompt, {\r\n        systemPrompt,\r\n        maxTokens,\r\n    });\r\n    return await sendPlainSeparateChatRequest(context, requestData, apiMeta);\n}\n\r\nfunction getRouterRequestData(context, prompt) {\r\n    return buildPlainSeparateChatPayload(prompt, {\r\n        systemPrompt: settings.systemPrompt,\r\n        maxTokens: getRouterRequestMaxTokens(),\r\n    });\r\n}\r\n\r\nasync function selectWithSeparateRouterModel(context, recentMessages, mvuSummary, candidates, maxSelectCount = settings.maxSelected) {\r\n    const prompt = buildAiPrompt(recentMessages, mvuSummary, candidates, maxSelectCount);\r\n    const result = await sendSeparateRouterRequest(context, prompt, {\r\n        recentMessages,\r\n        mvuSummary,\r\n        candidates,\r\n        systemPrompt: settings.systemPrompt,\r\n        maxTokens: getRouterRequestMaxTokens(),\r\n        maxSelectCount,\r\n    });\r\n    const promptForParse = result.usedCompactPrompt ? `${prompt}\\n\\n----- COMPACT RETRY PROMPT -----\\n\\n${result.retryPrompt}` : prompt;\r\n    const parsed = parseSelectionJson(result.raw, candidates, promptForParse);\r\n    return {\r\n        parsed,\r\n        prompt: promptForParse,\r\n        rawPreview: summarizeRouterResponse(result.raw),\r\n    };\r\n}\r\n\r\nasync function runSingleAiSelectionAttempt(context, recentMessages, mvuSummary, candidates, maxSelectCount = settings.maxSelected) {\r\n    let parsed;\r\n    let prompt = '';\r\n    let rawPreview = '';\r\n    if (settings.routerUseSeparateModel && settings.routerApiUrl && settings.routerApiKey && settings.routerModel) {\r\n        const result = await selectWithSeparateRouterModel(context, recentMessages, mvuSummary, candidates, maxSelectCount);\r\n        parsed = result.parsed;\r\n        prompt = result.prompt;\r\n        rawPreview = result.rawPreview;\r\n    } else {\r\n        prompt = buildAiPrompt(recentMessages, mvuSummary, candidates, maxSelectCount);\r\n        const apiItem = recordApiCallStart('router', '前置路由选择', { roundLabel: '路由 API 调用' });\n        let raw;\n        try {\n            raw = await context.generateRaw({\n                prompt,\n                systemPrompt: settings.systemPrompt,\n                responseLength: getRouterRequestMaxTokens(),\n                trimNames: false,\n                jsonSchema: getSelectionSchema(),\n            });\n            recordApiCallEnd(apiItem);\n        } catch (error) {\n            recordApiCallEnd(apiItem, error);\n            throw error;\n        }\n        parsed = parseSelectionJson(raw, candidates, prompt);\n        rawPreview = summarizeRouterResponse(raw);\n    }\r\n\r\n    return { parsed, prompt, rawPreview };\r\n}\r\n\r\nasync function selectWithAi(context, recentMessages, mvuSummary, candidates, maxSelectCount = settings.maxSelected) {\n    if (candidates.length === 0) {\n        return {\n            selected: [],\r\n            prompt: '',\r\n            rawPreview: '',\r\n        };\r\n    }\n\n    const maxAttempts = Math.max(1, Number(settings.routerRetries || 0) + 1);\n    startRuntimeActivity('select', 'AI筛选中', `候选 ${candidates.length} 条，最多命中 ${maxSelectCount} 条。`);\n    let parsed;\n    let prompt = '';\r\n    let rawPreview = '';\r\n    let lastError = null;\r\n\r\n    for (let attempt = 1; attempt <= maxAttempts; attempt += 1) {\r\n        try {\r\n            const result = await runSingleAiSelectionAttempt(context, recentMessages, mvuSummary, candidates, maxSelectCount);\r\n            parsed = result.parsed;\r\n            prompt = result.prompt;\r\n            rawPreview = result.rawPreview;\r\n            break;\r\n        } catch (error) {\r\n            lastError = error;\r\n            prompt = error?.routerPrompt || prompt || '';\r\n            rawPreview = error?.routerRaw || rawPreview || error?.message || String(error);\r\n            debugLog(`Router attempt ${attempt}/${maxAttempts} failed`, error);\r\n\r\n            if (!isGatewayLikeError(error) && attempt < maxAttempts) {\n                playStatusBurst('retry', 'retry');\n                updateRuntimeActivity('select', `AI筛选重试 ${attempt}/${maxAttempts}`);\n                continue;\n            }\n\n            playStatusBurst('fail', 'fail');\n            flashWorldInfoError();\n            failRuntimeActivity('select', error);\n            throw error;\n        }\n    }\n\n    if (!parsed) {\n        const error = lastError || new Error('Router selection failed without parsed result.');\n        failRuntimeActivity('select', error);\n        throw error;\n    }\r\n\r\n    const byId = new Map();\r\n    const byKey = new Map();\r\n    for (const entry of candidates) {\r\n        byId.set(String(entry.routerId), entry);\r\n        byId.set(String(entry.uid), entry);\r\n        for (const key of getEntrySelectionKeys(entry)) {\r\n            const normalizedKey = normalizeText(key);\r\n            if (normalizedKey && !byKey.has(normalizedKey)) {\r\n                byKey.set(normalizedKey, entry);\r\n            }\r\n        }\r\n    }\r\n    const selected = [];\r\n    const seen = new Set();\r\n\r\n    for (const item of parsed) {\r\n        const entry = resolveEntryFromSelection(item, byId, byKey, candidates);\r\n        if (!entry || seen.has(entry.routerId)) {\r\n            continue;\r\n        }\r\n\r\n        seen.add(entry.routerId);\r\n        selected.push({\r\n            ...entry,\r\n            reason: truncateText(item.reason || 'AI selected this entry.', 240),\r\n        });\r\n\r\n        if (selected.length >= maxSelectCount) {\r\n            break;\r\n        }\r\n    }\r\n\r\n    finishRuntimeActivityStage('select', `命中 ${selected.length} 条`);\n    return {\n        selected,\n        prompt,\n        rawPreview,\n    };\r\n}\r\n\r\nfunction selectWithFallback(candidates) {\r\n    return candidates.slice(0, settings.maxSelected).map(entry => ({\r\n        ...entry,\r\n        reason: entry.matchedKeys?.length\r\n            ? `关键词命中：${entry.matchedKeys.join(', ')}`\r\n            : `关键词评分 fallback：${entry.score}`,\r\n    }));\r\n}\r\n\r\nfunction hasMemoryState(graph) {\r\n    const state = graph?.state || {};\r\n    const customValues = state.custom_values && typeof state.custom_values === 'object' ? state.custom_values : {};\r\n    return !!(\r\n        String(state.current_location || '').trim()\r\n        || String(state.current_time || '').trim()\r\n        || String(state.protagonist_status || '').trim()\r\n        || String(state.current_objective || '').trim()\r\n        || String(state.current_phase || '').trim()\r\n        || (Array.isArray(state.active_topics) && state.active_topics.length)\r\n        || (Array.isArray(state.open_questions) && state.open_questions.length)\r\n        || Object.values(customValues).some(value => String(value || '').trim())\r\n    );\r\n}\r\n\r\nfunction collectMemoryKeywords(node) {\r\n    const keys = [];\r\n    const push = (value) => {\r\n        const text = String(value || '').trim();\r\n        if (text) {\r\n            keys.push(text);\r\n        }\r\n    };\r\n\r\n    push(node.id);\r\n    push(node.title);\r\n    push(node.summary);\r\n    push(node.location);\r\n    push(node.timeSpan);\r\n    for (const key of Array.isArray(node.keys) ? node.keys : []) {\r\n        push(key);\r\n    }\r\n    for (const tag of Array.isArray(node.tags) ? node.tags : []) {\r\n        push(tag);\r\n    }\r\n\r\n    const snippets = String(node.content || '')\r\n        .split(/[，,。.!?\\n；;：:]/u)\r\n        .map(part => part.trim())\r\n        .filter(part => part.length >= 2 && part.length <= 18)\r\n        .slice(0, 8);\r\n    for (const snippet of snippets) {\r\n        push(snippet);\r\n    }\r\n\r\n    return uniqueStrings(keys);\r\n}\r\n\r\nfunction buildMemoryCandidates(graph) {\r\n    const nodes = Array.isArray(graph?.nodes) ? graph.nodes : [];\r\n    return nodes.map((node, index) => {\r\n        const allKeys = collectMemoryKeywords(node);\r\n        return {\r\n            routerId: `memory:${node.id}`,\r\n            uid: node.id,\r\n            source: 'memory',\r\n            world: 'memory',\r\n            comment: node.title || `记忆 ${index + 1}`,\r\n            content: node.content || node.title || '',\r\n            order: index,\r\n            constant: false,\r\n            disable: false,\r\n            keys: {\r\n                primary: uniqueStrings([node.title, ...(Array.isArray(node.tags) ? node.tags.slice(0, 2) : [])].filter(Boolean)),\r\n                secondary: uniqueStrings(allKeys.slice(1)),\r\n                all: allKeys,\r\n            },\r\n            memoryType: String(node.type || 'event'),\r\n            nodeRef: node,\r\n        };\r\n    });\r\n}\r\n\r\nfunction isMemoryEventType(type) {\r\n    return ['event', 'quest'].includes(String(type || '').toLowerCase());\r\n}\r\n\r\nfunction getMemoryEventNodes(graph) {\r\n    return (Array.isArray(graph?.nodes) ? graph.nodes : [])\r\n        .filter(node => isMemoryEventType(node.type))\r\n        .sort((a, b) => String(b.updatedAt || '').localeCompare(String(a.updatedAt || '')));\r\n}\r\n\r\nfunction getMemoryNonEventNodes(graph) {\r\n    return (Array.isArray(graph?.nodes) ? graph.nodes : [])\r\n        .filter(node => !isMemoryEventType(node.type))\r\n        .sort((a, b) => String(b.updatedAt || '').localeCompare(String(a.updatedAt || '')));\r\n}\r\n\r\nfunction getMemoryStateRows(graph) {\r\n    const state = graph?.state || {};\r\n    const rows = [\r\n        { key: 'current_location', label: 'Current location', value: state.current_location || '' },\r\n        { key: 'current_time', label: 'Current time', value: state.current_time || '' },\r\n        { key: 'protagonist_status', label: 'Protagonist status', value: state.protagonist_status || '' },\r\n        { key: 'current_objective', label: 'Current objective', value: state.current_objective || '' },\r\n        { key: 'current_phase', label: 'Current phase', value: state.current_phase || '' },\r\n        { key: 'active_topics', label: 'Active topics', value: (state.active_topics || []).join(', ') },\r\n        { key: 'open_questions', label: 'Open questions', value: (state.open_questions || []).join(', ') },\r\n    ];\r\n    const customValues = state.custom_values && typeof state.custom_values === 'object' ? state.custom_values : {};\r\n    for (const definition of getCustomMemoryStateDefinitions(graph)) {\r\n        rows.push({\r\n            key: definition.key,\r\n            label: definition.label,\r\n            description: definition.instruction,\r\n            value: String(customValues[definition.key] || ''),\r\n            isCustom: true,\r\n        });\r\n    }\r\n    return rows;\r\n}\r\nfunction recallMemoryCandidates(graph, recentMessages, mvuSummary) {\r\n    const candidates = recallCandidates(buildMemoryCandidates(graph), recentMessages, mvuSummary)\r\n        .filter(entry => entry.score > 0 || ['event', 'quest', 'character', 'location'].includes(entry.memoryType));\r\n\r\n    return candidates\r\n        .sort((a, b) => (b.score - a.score) || String(b.nodeRef?.updatedAt || '').localeCompare(String(a.nodeRef?.updatedAt || '')))\r\n        .slice(0, MAX_MEMORY_SELECTED + 4);\r\n}\r\n\r\nfunction selectMemoryWithFallback(candidates) {\r\n    return candidates.slice(0, MAX_MEMORY_SELECTED).map(entry => ({\r\n        ...entry,\r\n        reason: entry.matchedKeys?.length\r\n            ? `记忆关键词命中：${entry.matchedKeys.join(', ')}`\r\n            : `近期记忆 fallback：${entry.score}`,\r\n    }));\r\n}\r\n\r\nfunction buildMemoryInjection(graph, selectedMemories) {\r\n    const state = graph?.state || {};\r\n    const hasState = hasMemoryState(graph);\r\n    const memoryItems = Array.isArray(selectedMemories) ? selectedMemories : [];\r\n    if (!hasState && !memoryItems.length) {\r\n        return '';\r\n    }\r\n\r\n    const parts = ['[本轮相关记忆]\\n以下内容来自当前聊天的动态记忆，仅用于本轮回复保持剧情连续性。\\n'];\r\n\r\n    if (String(state.current_location || '').trim()) {\r\n        parts.push(`\\n当前地点：${state.current_location.trim()}\\n`);\r\n    }\r\n    if (String(state.current_time || '').trim()) {\r\n        parts.push(`当前时间：${state.current_time.trim()}\\n`);\r\n    }\r\n    if (String(state.protagonist_status || '').trim()) {\r\n        parts.push(`主角状态：${state.protagonist_status.trim()}\\n`);\r\n    }\r\n    if (String(state.current_objective || '').trim()) {\r\n        parts.push(`当前目标：${state.current_objective.trim()}\\n`);\r\n    }\r\n    if (String(state.current_phase || '').trim()) {\r\n        parts.push(`当前阶段：${state.current_phase.trim()}\\n`);\r\n    }\r\n    if (Array.isArray(state.active_topics) && state.active_topics.length) {\r\n        parts.push(`活跃主题：${state.active_topics.join('、')}\\n`);\r\n    }\r\n    if (Array.isArray(state.open_questions) && state.open_questions.length) {\r\n        parts.push('未解问题：\\n');\r\n        for (const question of state.open_questions.slice(0, 6)) {\r\n            parts.push(`- ${question}\\n`);\r\n        }\r\n    }\r\n    for (const definition of getCustomMemoryStateDefinitions(graph)) {\r\n        const value = String(state.custom_values?.[definition.key] || '').trim();\r\n        if (!value) {\r\n            continue;\r\n        }\r\n        parts.push(`${definition.label}：${value}\\n`);\r\n    }\r\n\r\n    if (memoryItems.length) {\r\n        parts.push('\\n近期相关记忆：\\n');\r\n        for (const entry of memoryItems) {\r\n            const node = entry.nodeRef || {};\r\n            const prefix = [node.timeSpan, node.location].filter(Boolean).join(' | ');\r\n            parts.push(`- ${entry.comment || node.title || entry.uid}${prefix ? ` [${prefix}]` : ''}：${truncateText(node.summary || node.content || entry.content || '', 220)}\\n`);\r\n        }\r\n    }\r\n\r\n    parts.push('[/本轮相关记忆]');\r\n    return parts.join('');\r\n}\r\n\r\nfunction buildBookshelfInjection(selectedBookshelf = []) {\r\n    const items = Array.isArray(selectedBookshelf) ? selectedBookshelf : [];\r\n    if (!items.length) {\r\n        return '';\r\n    }\r\n\r\n    const header = '[本轮相关资料]\\n以下片段来自向量书架召回，仅用于补充本轮回复需要的外部资料。\\n';\r\n    const footer = '\\n[/本轮相关资料]';\r\n    const parts = [header];\r\n    let used = header.length + footer.length;\r\n    const maxChars = Math.max(600, Math.floor(clampNumber(settings.maxChars, defaultSettings.maxChars, 500, 50000) * 0.35));\r\n\r\n    for (const item of items) {\r\n        const bookTitle = item.book?.title || item.book?.fileName || item.bookTitle || '未命名资料';\r\n        const chunkTitle = item.title || item.chunk?.title || `片段 ${Number(item.index ?? item.chunk?.index ?? 0) + 1}`;\r\n        const score = Number(item.score || 0);\r\n        const text = String(item.content || item.text || item.chunk?.text || '').trim();\r\n        if (!text) continue;\r\n\r\n        const separator = `\\n--- ${bookTitle} / ${chunkTitle} | score ${score.toFixed(2)} ---\\n`;\r\n        const remaining = maxChars - used - separator.length - footer.length;\r\n        if (remaining <= 0) break;\r\n\r\n        const content = truncateText(text, remaining);\r\n        parts.push(separator, content, '\\n');\r\n        used += separator.length + content.length + 1;\r\n    }\r\n\r\n    if (parts.length === 1) {\r\n        return '';\r\n    }\r\n    parts.push(footer);\r\n    return parts.join('');\r\n}\r\nfunction buildInjection(selectedEntries, memoryGraph = null, selectedMemories = [], selectedBookshelf = []) {\r\n    const worldbookBlock = (() => {\r\n        if (!selectedEntries.length) {\r\n            return '';\r\n        }\r\n\r\n        const header = '[本轮相关世界书]\\n以下条目由前置路由器按当前用户消息、最近上下文和可选状态筛选，仅用于本轮回复保持设定一致。\\n';\r\n        const footer = '\\n[/本轮相关世界书]';\r\n        const parts = [header];\r\n        let used = header.length + footer.length;\r\n\r\n        for (const entry of selectedEntries) {\r\n            const title = entry.comment || entry.keys.primary[0] || entry.uid;\r\n            const separator = `\\n--- ${title} | ${entry.world || entry.source}#${entry.uid} ---\\n`;\r\n            const remaining = settings.maxChars - used - separator.length - footer.length;\r\n            if (remaining <= 0) {\r\n                break;\r\n            }\r\n\r\n            const content = truncateText(entry.content, remaining);\r\n            parts.push(separator, content, '\\n');\r\n            used += separator.length + content.length + 1;\r\n        }\r\n\r\n        parts.push(footer);\r\n        return truncateText(parts.join(''), settings.maxChars);\r\n    })();\r\n\r\n    const memoryBlock = buildMemoryInjection(memoryGraph || getMemoryGraph(), selectedMemories);\r\n    const bookshelfBlock = buildBookshelfInjection(selectedBookshelf);\r\n    if (!worldbookBlock && !memoryBlock && !bookshelfBlock) {\r\n        return '';\r\n    }\r\n    return [worldbookBlock, memoryBlock, bookshelfBlock].filter(Boolean).join('\\n\\n');\r\n}\r\n\r\nfunction renderDebugPanel() {\r\n    const candidates = Array.isArray(lastRun?.candidates) ? lastRun.candidates : [];\r\n    const selected = Array.isArray(lastRun?.selected) ? lastRun.selected : [];\r\n    const memoryCandidates = Array.isArray(lastRun?.memoryCandidates) ? lastRun.memoryCandidates : [];\r\n    const selectedMemories = Array.isArray(lastRun?.selectedMemories) ? lastRun.selectedMemories : [];\r\n    const bookshelfCandidates = Array.isArray(lastRun?.bookshelfCandidates) ? lastRun.bookshelfCandidates : [];\r\n    const selectedBookshelf = Array.isArray(lastRun?.selectedBookshelf) ? lastRun.selectedBookshelf : [];\r\n    const pipeline = lastRun?.pipeline && typeof lastRun.pipeline === 'object' ? lastRun.pipeline : null;\r\n    const summary = lastRun.error\r\n        ? `失败：${lastRun.error}`\r\n        : `世界书候选 ${candidates.length} 条，选择 ${selected.length} 条；记忆候选 ${memoryCandidates.length} 条，选择 ${selectedMemories.length} 条；书架候选 ${bookshelfCandidates.length} 条，选择 ${selectedBookshelf.length} 条；注入 ${lastRun.injectedChars} 字符，来源：${lastRun.source}`;\r\n    $('#ai_wbr_last_summary').text(summary);\r\n\r\n    const pipelineItems = [];\r\n    if (pipeline) {\r\n        const pre = pipeline.preRefresh || {};\r\n        const graph = pipeline.graph || {};\r\n        const recall = pipeline.recall || {};\r\n        const vector = pipeline.vector || {};\r\n        const sync = vector.sync || {};\r\n        const selectedStats = pipeline.selected || {};\r\n        const injection = pipeline.injection || {};\r\n        const preLabel = pre.attempted\r\n            ? (pre.timedOut ? `预刷新超时，后台继续整理 · ${pre.durationMs || 0}ms` : `预刷新${pre.updated ? '已更新' : '无变化'} · ${pre.durationMs || 0}ms`)\r\n            : `预刷新跳过：${pre.skippedReason || 'not-needed'}`;\r\n        pipelineItems.push(\r\n            $('<div class=\"ai-wbr-last-item ai-wbr-pipeline-item\"></div>')\r\n                .append($('<div></div>').text('[流水线] 记忆预刷新'))\r\n                .append($('<small></small>').text(preLabel)),\r\n            $('<div class=\"ai-wbr-last-item ai-wbr-pipeline-item\"></div>')\r\n                .append($('<div></div>').text('[流水线] 图谱状态'))\r\n                .append($('<small></small>').text(`节点 ${graph.nodes || 0} · 关系 ${graph.links || 0} · 状态 ${graph.hasState ? '有' : '无'}${graph.updatedAt ? ` · 更新 ${graph.updatedAt}` : ''}`)),\r\n            $('<div class=\"ai-wbr-last-item ai-wbr-pipeline-item\"></div>')\r\n                .append($('<div></div>').text('[流水线] 召回候选'))\r\n                .append($('<small></small>').text(`世界书 ${recall.worldbook || 0} · 记忆 ${recall.memory || 0} · 书架 ${recall.bookshelf || 0} · 合计 ${recall.combined || 0}`)),\r\n            $('<div class=\"ai-wbr-last-item ai-wbr-pipeline-item\"></div>')\r\n                .append($('<div></div>').text('[流水线] 向量同步'))\r\n                .append($('<small></small>').text(sync.skipped ? '跳过' : `总 ${sync.total || 0} · ready ${sync.ready || 0} · pending ${sync.pending || 0} · failed ${sync.failed || 0}${vector.errors?.length ? ` · 错误 ${vector.errors.join(' | ')}` : ''}`)),\r\n            $('<div class=\"ai-wbr-last-item ai-wbr-pipeline-item\"></div>')\r\n                .append($('<div></div>').text('[流水线] 最终注入'))\r\n                .append($('<small></small>').text(`世界书 ${selectedStats.worldbook || 0} · 记忆 ${selectedStats.memory || 0} · 资料 ${selectedStats.bookshelf || 0} · 字符 ${injection.chars || lastRun.injectedChars || 0}`)),\r\n        );\r\n    }\r\n\r\n    const worldbookItems = selected.map(entry => {\r\n        const title = entry.comment || entry.keys?.primary?.[0] || entry.uid;\r\n        const keys = entry.matchedKeys?.length ? ` | keys: ${entry.matchedKeys.join(', ')}` : '';\r\n        return $('<div class=\"ai-wbr-last-item\"></div>')\r\n            .append($('<div></div>').text(`${title} (${entry.world || entry.source}#${entry.uid})`))\r\n            .append($('<small></small>').text(`${entry.reason || entry.recallReason || ''}${keys}${entry.sourceType ? ` · ${entry.sourceType}` : ''}`));\r\n    });\r\n\r\n    const memoryItems = selectedMemories.map(entry => {\r\n        const title = entry.comment || entry.uid;\r\n        const keys = entry.matchedKeys?.length ? ` | keys: ${entry.matchedKeys.join(', ')}` : '';\r\n        return $('<div class=\"ai-wbr-last-item\"></div>')\r\n            .append($('<div></div>').text(`[记忆] ${title} (${entry.memoryType || 'memory'}#${entry.uid})`))\r\n            .append($('<small></small>').text(`${entry.reason || entry.recallReason || ''}${keys}${entry.sourceType ? ` · ${entry.sourceType}` : ''}`));\r\n    });\r\n\r\n    const bookshelfItems = selectedBookshelf.map(entry => (\r\n        $('<div class=\"ai-wbr-last-item\"></div>')\r\n            .append($('<div></div>').text(`[书架] 《${entry.book?.title || entry.book?.fileName || '书架'}》 / ${entry.title || entry.uid}`))\r\n            .append($('<small></small>').text(`${entry.recallReason || `分数 ${Number(entry.score || 0).toFixed(2)}`} · ${truncateText(entry.text || entry.content || '', 120)}`))\r\n    ));\r\n\r\n    $('#ai_wbr_last_items').empty().append([...pipelineItems, ...worldbookItems, ...memoryItems, ...bookshelfItems]);\r\n    $('#ai_wbr_injection_text').text(lastRun.injectionText || '尚无本轮注入记录');\r\n    $('#ai_wbr_router_prompt').text(lastRun.routerPrompt || '尚无前置 AI Prompt 记录');\r\n    $('#ai_wbr_router_raw').text(lastRun.routerRaw || '尚无前置 AI 返回记录');\r\n    renderStandaloneConsole();\r\n}\r\n\r\nfunction getStandaloneStatusMeta() {\r\n    if (!settings.enabled) {\r\n        return { label: 'Disabled', className: 'idle', icon: 'fa-pause-circle' };\r\n    }\r\n    if (lastRun.error) {\r\n        return { label: 'Route error', className: 'error', icon: 'fa-triangle-exclamation' };\r\n    }\r\n    if (lastRun.source && lastRun.source !== 'none') {\r\n        const isFallback = lastRun.source.includes('fallback');\r\n        return { label: isFallback ? 'Fallback route' : 'Route complete', className: isFallback ? 'warn' : 'ok', icon: isFallback ? 'fa-rotate-left' : 'fa-circle-check' };\r\n    }\r\n    if (settings.routerStatus && !['???', '???', 'Ready'].includes(settings.routerStatus)) {\r\n        return { label: settings.routerStatus, className: 'active', icon: 'fa-bolt' };\r\n    }\r\n    return { label: 'Waiting', className: 'ready', icon: 'fa-circle-dot' };\r\n}\r\nfunction createStandaloneStat(label, value) {\r\n    return $('<div class=\"ai-wbr-console-stat\"></div>')\r\n        .append($('<div class=\"ai-wbr-console-stat-value\"></div>').text(value))\r\n        .append($('<div class=\"ai-wbr-console-stat-label\"></div>').text(label));\r\n}\r\n\r\nfunction createStandaloneEntryCard(entry = {}, type = 'worldbook', selected = false) {\r\n    const keyData = entry.keys && typeof entry.keys === 'object' && !Array.isArray(entry.keys) && Array.isArray(entry.keys.all)\r\n        ? entry.keys\r\n        : getEntryKeys(entry);\r\n    const primaryKeys = Array.isArray(keyData.primary) ? keyData.primary : [];\r\n    const allKeys = Array.isArray(keyData.all) ? keyData.all : [];\r\n    const matchedKeys = Array.isArray(entry.matchedKeys)\r\n        ? entry.matchedKeys\r\n        : (entry.matchedKeys ? [entry.matchedKeys] : []);\r\n    const title = entry.comment\r\n        || entry.recallTitle\r\n        || primaryKeys[0]\r\n        || entry.title\r\n        || entry.uid\r\n        || entry.id\r\n        || 'Untitled entry';\r\n    const source = type === 'memory'\r\n        ? `${entry.memoryType || entry.sourceType || 'memory'}#${entry.uid || entry.id || ''}`\r\n        : type === 'bookshelf'\r\n            ? `${entry.book?.title || entry.book?.fileName || 'bookshelf'}#${entry.uid || entry.id || ''}`\r\n            : `${entry.world || entry.source || entry.sourceType || 'worldbook'}#${entry.uid || entry.id || ''}`;\r\n    const keys = matchedKeys.length ? matchedKeys.map(String).join(', ') : allKeys.map(String).join(', ');\r\n    const body = entry.content || entry.text || entry.chunk?.text || '';\r\n    const reason = entry.reason || entry.recallReason || '';\r\n\r\n    return $('<div class=\"ai-wbr-console-entry\"></div>')\r\n        .toggleClass('selected', !!selected)\r\n        .append($('<div class=\"ai-wbr-console-entry-head\"></div>')\r\n            .append($('<b></b>').text(title))\r\n            .append($('<span></span>').text(selected ? '已注入' : '候选')))\r\n        .append($('<div class=\"ai-wbr-console-entry-meta\"></div>').text(source))\r\n        .append(keys ? $('<div class=\"ai-wbr-console-entry-keys\"></div>').text(`关键词：${truncateText(keys, 160)}`) : '')\r\n        .append(reason ? $('<small></small>').text(reason) : '')\r\n        .append(body ? $('<p></p>').text(truncateText(body, 320)) : '');\r\n}\r\n\r\nfunction appendStandaloneEntryCard(list, entry, type, selected) {\r\n    try {\r\n        list.append(createStandaloneEntryCard(entry, type, selected));\r\n    } catch (error) {\r\n        console.warn(`${LOG_PREFIX} failed to render ${type} route candidate`, error, entry);\r\n        const title = entry?.comment || entry?.title || entry?.uid || entry?.id || 'Bad route candidate';\r\n        list.append($('<div class=\"ai-wbr-console-entry\"></div>')\r\n            .toggleClass('selected', !!selected)\r\n            .append($('<div class=\"ai-wbr-console-entry-head\"></div>')\r\n                .append($('<b></b>').text(title))\r\n                .append($('<span></span>').text(selected ? '已注入' : '候选')))\r\n            .append($('<div class=\"ai-wbr-console-entry-meta\"></div>').text(`${type}#${entry?.uid || entry?.id || ''}`))\r\n            .append($('<small></small>').text(`候选字段不完整，已跳过部分详情：${error?.message || error}`)));\r\n    }\r\n}\r\nfunction getStandaloneTabId() {\r\n    return String($('#ai_wbr_console_tabs .ai-wbr-console-tab.active').data('tab') || 'overview');\r\n}\r\n\r\nfunction createBookshelfStandaloneFold() {\n    return $(`\n        <details class=\"ai-wbr-memory-fold ai-wbr-bookshelf-fold\" open>\n            <summary>\n                <span>书架</span>\n                <small>导入 TXT，向量化后按当前剧情语义召回。</small>\r\n            </summary>\r\n            <div id=\"ai_wbr_bookshelf_panel\" class=\"ai-wbr-memory-fold-body ai-wbr-bookshelf-panel\">\r\n                <div class=\"ai-wbr-bookshelf-app\">\n                    <div class=\"ai-wbr-bookshelf-top\">\n                        <div>\n                            <div class=\"ai-wbr-bookshelf-title\">书架</div>\n                            <div class=\"ai-wbr-bookshelf-subtitle\">点击阅读，长按管理。</div>\n                        </div>\n                        <div class=\"ai-wbr-bookshelf-top-actions\">\n                            <button id=\"ai_wbr_bookshelf_import\" class=\"ai-wbr-bookshelf-import-button\" type=\"button\">＋ 导入TXT</button>\n                            <button id=\"ai_wbr_bookshelf_open_settings\" class=\"ai-wbr-bookshelf-more\" type=\"button\" title=\"书架设置\">•••</button>\n                        </div>\n                    </div>\n\n                    <input id=\"ai_wbr_bookshelf_file\" type=\"file\" accept=\".txt,text/plain\" multiple hidden />\n                    <div id=\"ai_wbr_bookshelf_status\" class=\"ai-wbr-bookshelf-status\">书架待命。点击书籍阅读，长按弹出书籍设置。</div>\n                    <div id=\"ai_wbr_bookshelf_books\" class=\"ai-wbr-bookshelf-books\"></div>\n                    <div id=\"ai_wbr_bookshelf_detail\" class=\"ai-wbr-bookshelf-detail\"></div>\n\r\n                    <div class=\"ai-wbr-bookshelf-drawer-backdrop\" id=\"ai_wbr_bookshelf_settings_backdrop\"></div>\r\n                    <aside class=\"ai-wbr-bookshelf-settings-drawer\" id=\"ai_wbr_bookshelf_settings_drawer\" aria-hidden=\"true\">\r\n                        <div class=\"ai-wbr-bookshelf-drawer-head\">\r\n                            <div>\r\n                                <b>书架设置</b>\r\n                                <small id=\"ai_wbr_bookshelf_model_status\">未测试</small>\r\n                            </div>\r\n                            <button id=\"ai_wbr_bookshelf_close_settings\" class=\"menu_button\" type=\"button\">关闭</button>\r\n                        </div>\r\n                        <div id=\"ai_wbr_bookshelf_scope\" class=\"ai-wbr-bookshelf-scope\"></div>\r\n                        <div class=\"ai-wbr-bookshelf-import-options\">\r\n                            <label for=\"ai_wbr_bookshelf_import_type\">导入分类</label>\r\n                            <select id=\"ai_wbr_bookshelf_import_type\" class=\"text_pole\">\r\n                                <option value=\"plot\">剧情记录</option>\r\n                                <option value=\"character\">人物档案</option>\r\n                                <option value=\"world\">世界观</option>\r\n                                <option value=\"rule\">规则设定</option>\r\n                                <option value=\"other\">其他资料</option>\r\n                            </select>\r\n                            <label>\\u7ed1\\u5b9a\\u903b\\u8f91</label>\r\n                            <div class=\"ai-wbr-bookshelf-bind-note\">\\u5bfc\\u5165 TXT \\u53ea\\u5165\\u5e93\\uff1b\\u5b8c\\u6210\\u5206\\u5272\\u5411\\u91cf\\u5316\\u540e\\uff0c\\u624b\\u52a8\\u7ed1\\u5b9a\\u5f53\\u524d\\u89d2\\u8272\\u5361\\u5373\\u53ef\\u5728\\u8be5\\u89d2\\u8272\\u7684\\u5404\\u4e2a\\u804a\\u5929\\u4e2d\\u81ea\\u52a8\\u751f\\u6548\\uff0c\\u4e5f\\u53ef\\u8bbe\\u4e3a\\u5168\\u5c40\\u6216\\u968f\\u65f6\\u53d6\\u6d88\\u7ed1\\u5b9a\\u3002</div>\r\n                        </div>\r\n                        <div class=\"ai-wbr-bookshelf-switches\">\r\n                            <label class=\"checkbox_label\" for=\"ai_wbr_bookshelf_enabled\"><input id=\"ai_wbr_bookshelf_enabled\" type=\"checkbox\" />启用向量召回</label>\r\n                            <label class=\"checkbox_label\" for=\"ai_wbr_bookshelf_auto_inject\"><input id=\"ai_wbr_bookshelf_auto_inject\" type=\"checkbox\" />生成前自动注入</label>\r\n                            <label class=\"checkbox_label\" for=\"ai_wbr_bookshelf_auto_memory_book\"><input id=\"ai_wbr_bookshelf_auto_memory_book\" type=\"checkbox\" />自动维护当前聊天记忆书</label>\n                            <label class=\"checkbox_label\" for=\"ai_wbr_bookshelf_auto_memory_book_vectorize\"><input id=\"ai_wbr_bookshelf_auto_memory_book_vectorize\" type=\"checkbox\" />记忆书生成后自动向量化</label>\n                            <label class=\"checkbox_label\" for=\"ai_wbr_bookshelf_memory_vector\"><input id=\"ai_wbr_bookshelf_memory_vector\" type=\"checkbox\" />图谱记忆参与召回</label>\r\n                            <label class=\"checkbox_label\" for=\"ai_wbr_bookshelf_only_bound\"><input id=\"ai_wbr_bookshelf_only_bound\" type=\"checkbox\" />仅召回绑定书籍</label>\r\n                            <label class=\"checkbox_label\" for=\"ai_wbr_bookshelf_allow_global\"><input id=\"ai_wbr_bookshelf_allow_global\" type=\"checkbox\" />允许全局书籍</label>\r\n                        </div>\r\n                        <div class=\"ai-wbr-grid ai-wbr-bookshelf-config\">\r\n                            <label for=\"ai_wbr_bookshelf_embedding_mode\">Embedding 模式</label>\r\n                            <select id=\"ai_wbr_bookshelf_embedding_mode\" class=\"text_pole\">\r\n                                <option value=\"api\">API 向量模型</option>\r\n                                <option value=\"browser-local\">浏览器本地模型</option>\r\n                            </select>\r\n                            <label for=\"ai_wbr_bookshelf_api_url\">API 地址</label>\r\n                            <input id=\"ai_wbr_bookshelf_api_url\" class=\"text_pole\" type=\"text\" placeholder=\"https://example.com/v1 或 /v1/embeddings\" />\r\n                            <label for=\"ai_wbr_bookshelf_api_key\">API Key</label>\r\n                            <input id=\"ai_wbr_bookshelf_api_key\" class=\"text_pole\" type=\"password\" placeholder=\"sk-...\" />\r\n                            <label for=\"ai_wbr_bookshelf_api_model_select\">API 模型</label>\n                            <select id=\"ai_wbr_bookshelf_api_model_select\" class=\"text_pole\">\n                                <option value=\"\">先点击“获取模型”</option>\n                            </select>\n                            <label for=\"ai_wbr_bookshelf_api_model\">手动模型</label>\n                            <input id=\"ai_wbr_bookshelf_api_model\" class=\"text_pole\" type=\"text\" list=\"ai_wbr_bookshelf_api_model_options\" placeholder=\"例如 BAAI/bge-m3，可手动填写\" />\n                            <datalist id=\"ai_wbr_bookshelf_api_model_options\"></datalist>\n                            <label></label>\n                            <div class=\"ai-wbr-bookshelf-model-hint\">获取不到模型时可直接填写模型名，例如 BAAI/bge-m3。</div>\n                            <label></label>\n                            <button id=\"ai_wbr_bookshelf_fetch_models\" class=\"menu_button ai-wbr-bookshelf-fetch-models\" type=\"button\">获取模型</button>\n                            <label for=\"ai_wbr_bookshelf_local_model\">本地模型 ID</label>\n                            <input id=\"ai_wbr_bookshelf_local_model\" class=\"text_pole\" type=\"text\" placeholder=\"Xenova/paraphrase-multilingual-MiniLM-L12-v2\" />\r\n                            <label for=\"ai_wbr_bookshelf_memory_vector_max\">图谱召回数量</label>\r\n                            <input id=\"ai_wbr_bookshelf_memory_vector_max\" class=\"text_pole\" type=\"number\" min=\"1\" max=\"12\" step=\"1\" />\r\n                            <label for=\"ai_wbr_bookshelf_max_chunks\">TXT 召回数量</label>\r\n                            <input id=\"ai_wbr_bookshelf_max_chunks\" class=\"text_pole\" type=\"number\" min=\"1\" max=\"12\" step=\"1\" />\r\n                            <label for=\"ai_wbr_bookshelf_max_chars\">每段最大字数</label>\r\n                            <input id=\"ai_wbr_bookshelf_max_chars\" class=\"text_pole\" type=\"number\" min=\"120\" max=\"2000\" step=\"20\" />\r\n                            <label for=\"ai_wbr_bookshelf_min_score\">最低相似度</label>\r\n                            <input id=\"ai_wbr_bookshelf_min_score\" class=\"text_pole\" type=\"number\" min=\"0\" max=\"1\" step=\"0.05\" />\r\n                        </div>\n                        <div class=\"ai-wbr-bookshelf-actions\">\n                            <button id=\"ai_wbr_bookshelf_test_open\" class=\"menu_button\" type=\"button\">召回测试</button>\n                            <button id=\"ai_wbr_bookshelf_test_provider\" class=\"menu_button\" type=\"button\">测试向量模型</button>\n                            <button id=\"ai_wbr_bookshelf_load_local\" class=\"menu_button\" type=\"button\">下载/加载本地模型</button>\n                            <button id=\"ai_wbr_bookshelf_build_memory_books\" class=\"menu_button\" type=\"button\">刷新当前聊天记忆书</button>\n                            <button id=\"ai_wbr_bookshelf_vectorize_memory\" class=\"menu_button\" type=\"button\">同步图谱向量</button>\n                            <button id=\"ai_wbr_bookshelf_reset_memory_vectors\" class=\"menu_button\" type=\"button\">重置图谱向量</button>\n                        </div>\n                        <div class=\"ai-wbr-bookshelf-test\" id=\"ai_wbr_bookshelf_test_panel\">\n                            <div class=\"ai-wbr-memory-subtitle\"><b>召回测试</b></div>\n                            <textarea id=\"ai_wbr_bookshelf_test_query\" class=\"text_pole\" rows=\"3\" placeholder=\"输入一句当前剧情问题，测试向量书架会召回哪些片段。\"></textarea>\n                            <div class=\"ai-wbr-bookshelf-actions\">\n                                <button id=\"ai_wbr_bookshelf_test\" class=\"menu_button\" type=\"button\">测试召回</button>\n                            </div>\n                            <div id=\"ai_wbr_bookshelf_results\" class=\"ai-wbr-bookshelf-results\"></div>\n                        </div>\n                    </aside>\n                    <div class=\"ai-wbr-bookshelf-book-action-backdrop\" id=\"ai_wbr_bookshelf_book_actions_backdrop\"></div>\n                    <aside class=\"ai-wbr-bookshelf-book-action-sheet\" id=\"ai_wbr_bookshelf_book_actions_sheet\" aria-hidden=\"true\">\n                        <div class=\"ai-wbr-bookshelf-book-action-head\">\n                            <div>\n                                <b id=\"ai_wbr_bookshelf_book_actions_title\">书籍设置</b>\n                                <small id=\"ai_wbr_bookshelf_book_actions_meta\">长按书籍打开</small>\n                            </div>\n                            <button id=\"ai_wbr_bookshelf_close_book_actions\" class=\"menu_button\" type=\"button\">关闭</button>\n                        </div>\n                        <div id=\"ai_wbr_bookshelf_book_actions_body\" class=\"ai-wbr-bookshelf-book-action-body\"></div>\n                    </aside>\n                </div>\n            </div>\n        </details>\n    `);\r\n}\r\n\r\nfunction ensureBookshelfStandaloneControls(section) {\n    const panel = section.find('#ai_wbr_bookshelf_panel');\n    if (!panel.length) return;\n    if (!panel.find('.ai-wbr-bookshelf-app').length) {\n        const rebuiltPanel = createBookshelfStandaloneFold().find('#ai_wbr_bookshelf_panel');\n        panel.empty().append(rebuiltPanel.children());\n    }\n    const switches = panel.find('.ai-wbr-bookshelf-switches').first();\r\n    if (switches.length && !panel.find('#ai_wbr_bookshelf_memory_vector').length) {\r\n        switches.find('#ai_wbr_bookshelf_auto_inject').closest('label').after(\r\n            $('<label class=\"checkbox_label\" for=\"ai_wbr_bookshelf_memory_vector\"></label>')\r\n                .append('<input id=\"ai_wbr_bookshelf_memory_vector\" type=\"checkbox\" />')\r\n                .append('图谱记忆参与向量召回'),\r\n        );\r\n    }\r\n    if (switches.length && !panel.find('#ai_wbr_bookshelf_auto_memory_book').length) {\n        switches.find('#ai_wbr_bookshelf_auto_inject').closest('label').after(\n            $('<label class=\"checkbox_label\" for=\"ai_wbr_bookshelf_auto_memory_book\"></label>')\n                .append('<input id=\"ai_wbr_bookshelf_auto_memory_book\" type=\"checkbox\" />')\n                .append('自动维护当前聊天记忆书'),\n        );\n    }\n    if (switches.length && !panel.find('#ai_wbr_bookshelf_auto_memory_book_vectorize').length) {\n        switches.find('#ai_wbr_bookshelf_auto_memory_book').closest('label').after(\n            $('<label class=\"checkbox_label\" for=\"ai_wbr_bookshelf_auto_memory_book_vectorize\"></label>')\n                .append('<input id=\"ai_wbr_bookshelf_auto_memory_book_vectorize\" type=\"checkbox\" />')\n                .append('记忆书生成后自动向量化'),\n        );\n    }\n    const config = panel.find('.ai-wbr-bookshelf-config').first();\r\n    if (config.length && !panel.find('#ai_wbr_bookshelf_memory_vector_max').length) {\r\n        config.find('#ai_wbr_bookshelf_max_chunks').prev('label').before('<label for=\"ai_wbr_bookshelf_memory_vector_max\">图谱召回数量</label>');\r\n        config.find('#ai_wbr_bookshelf_max_chunks').before('<input id=\"ai_wbr_bookshelf_memory_vector_max\" class=\"text_pole\" type=\"number\" min=\"1\" max=\"12\" step=\"1\" />');\r\n    }\r\n    const actions = panel.find('.ai-wbr-bookshelf-actions').first();\r\n    const tabs = panel.find('.ai-wbr-bookshelf-tabs').first();\r\n    if (tabs.length && !panel.find('#ai_wbr_bookshelf_build_memory_books').length) {\r\n        tabs.find('#ai_wbr_bookshelf_import').after('<button id=\"ai_wbr_bookshelf_build_memory_books\" class=\"ai-wbr-bookshelf-link\" type=\"button\">刷新记忆书</button>');\r\n    }\r\n    if (actions.length && !panel.find('#ai_wbr_bookshelf_build_memory_books').length) {\r\n        actions.find('#ai_wbr_bookshelf_load_local').after('<button id=\"ai_wbr_bookshelf_build_memory_books\" class=\"menu_button\" type=\"button\">刷新当前聊天记忆书</button>');\r\n    }\r\n    if (actions.length && !panel.find('#ai_wbr_bookshelf_vectorize_memory').length) {\r\n        actions.find('#ai_wbr_bookshelf_load_local').after('<button id=\"ai_wbr_bookshelf_vectorize_memory\" class=\"menu_button\" type=\"button\">同步图谱向量</button>');\r\n    }\r\n    if (actions.length && !panel.find('#ai_wbr_bookshelf_reset_memory_vectors').length) {\r\n        actions.find('#ai_wbr_bookshelf_vectorize_memory').after('<button id=\"ai_wbr_bookshelf_reset_memory_vectors\" class=\"menu_button\" type=\"button\">重置图谱向量</button>');\r\n    }\r\n    if (panel.find('#ai_wbr_bookshelf_api_model').is('select')) {\n        const currentValue = String(panel.find('#ai_wbr_bookshelf_api_model').val() || settings.bookshelfApiModel || '').trim();\n        panel.find('#ai_wbr_bookshelf_api_model').replaceWith(\n            $('<input id=\"ai_wbr_bookshelf_api_model\" class=\"text_pole\" type=\"text\" list=\"ai_wbr_bookshelf_api_model_options\" placeholder=\"例如 BAAI/bge-m3，可手动填写\" />').val(currentValue),\n        );\n    }\n    if (!panel.find('#ai_wbr_bookshelf_api_model_select').length) {\n        const modelInput = panel.find('#ai_wbr_bookshelf_api_model').last();\n        modelInput.prev('label[for=\"ai_wbr_bookshelf_api_model\"]').before(\n            '<label for=\"ai_wbr_bookshelf_api_model_select\">API 模型</label><select id=\"ai_wbr_bookshelf_api_model_select\" class=\"text_pole\"><option value=\"\">先点击“获取模型”</option></select>',\n        );\n        modelInput.prev('label[for=\"ai_wbr_bookshelf_api_model\"]').text('手动模型');\n    }\n    if (!panel.find('#ai_wbr_bookshelf_api_model_options').length) {\n        panel.find('#ai_wbr_bookshelf_api_model').after('<datalist id=\"ai_wbr_bookshelf_api_model_options\"></datalist>');\n    }\n    if (!panel.find('.ai-wbr-bookshelf-model-hint').length) {\n        const datalist = panel.find('#ai_wbr_bookshelf_api_model_options').last();\n        const target = datalist.length ? datalist : panel.find('#ai_wbr_bookshelf_api_model').last();\n        target.after('<label></label><div class=\"ai-wbr-bookshelf-model-hint\">获取不到模型时可直接填写模型名，例如 BAAI/bge-m3。</div>');\n    }\n    if (!panel.find('#ai_wbr_bookshelf_fetch_models').length) {\n        const modelHint = panel.find('.ai-wbr-bookshelf-model-hint').last();\n        const target = modelHint.length ? modelHint : panel.find('#ai_wbr_bookshelf_api_model').last();\n        target.after('<label></label><button id=\"ai_wbr_bookshelf_fetch_models\" class=\"menu_button ai-wbr-bookshelf-fetch-models\" type=\"button\">获取模型</button>');\n    } else {\n        panel.find('#ai_wbr_bookshelf_fetch_models').addClass('ai-wbr-bookshelf-fetch-models');\n    }\n}\n\r\nfunction ensureBookshelfStandaloneSection() {\r\n    let section = $('#ai_wbr_bookshelf_section');\r\n    if (!section.length) {\r\n        section = $('<div class=\"ai-wbr-section\" id=\"ai_wbr_bookshelf_section\"></div>');\r\n    }\r\n    const fold = $('.ai-wbr-bookshelf-fold');\r\n    if (fold.length && !section.find('#ai_wbr_bookshelf_panel').length && !fold.closest('#ai_wbr_memory_section').length) {\r\n        section.append(fold.detach());\r\n    }\r\n    if (!section.find('#ai_wbr_bookshelf_panel').length) {\r\n        section.empty().append(createBookshelfStandaloneFold());\r\n    }\r\n    ensureBookshelfStandaloneControls(section);\r\n    return section;\r\n}\r\n\r\nfunction getSettingsContentContainer() {\r\n    return $('#ai_worldbook_router_settings .inline-drawer-content').first();\r\n}\r\n\r\nfunction ensureBookshelfSettingsSection() {\r\n    const settingsContent = getSettingsContentContainer();\r\n    if (!settingsContent.length) return $();\r\n\r\n    const memorySection = settingsContent.find('#ai_wbr_memory_section').first();\r\n    const graphSection = settingsContent.find('#ai_wbr_memory_graph_section').first();\r\n    let bookshelfSection = settingsContent.find('#ai_wbr_bookshelf_section').first();\r\n    const bookshelfFold = $('#ai_wbr_bookshelf_panel').closest('.ai-wbr-bookshelf-fold');\r\n\r\n    if (!bookshelfSection.length) {\r\n        bookshelfSection = $('<div class=\"ai-wbr-section\" id=\"ai_wbr_bookshelf_section\"></div>');\r\n        if (graphSection.length) {\r\n            graphSection.before(bookshelfSection);\r\n        } else if (memorySection.length) {\r\n            memorySection.after(bookshelfSection);\r\n        } else {\r\n            settingsContent.append(bookshelfSection);\r\n        }\r\n    }\r\n\r\n    if (bookshelfFold.length && !bookshelfSection.find('#ai_wbr_bookshelf_panel').length) {\r\n        bookshelfSection.append(bookshelfFold.detach());\r\n    }\r\n    ensureBookshelfStandaloneControls(bookshelfSection);\r\n    return bookshelfSection;\r\n}\r\n\r\nfunction restoreStandalonePanelsToSettings() {\r\n    const settingsContent = getSettingsContentContainer();\r\n    if (!settingsContent.length) return;\r\n    ensureBookshelfSettingsSection();\r\n\r\n    // Restore each parked section to its original anchor position (kept in a\r\n    // placeholder while parked) instead of appending to the end, which was\r\n    // scrambling the section order in the settings page after console visits.\r\n    const sections = [\r\n        { sel: '#ai_wbr_memory_section', anchor: 'ai_wbr_memory_section' },\r\n        { sel: '#ai_wbr_memory_graph_section', anchor: 'ai_wbr_memory_graph_section' },\r\n        { sel: '#ai_wbr_bookshelf_section', anchor: 'ai_wbr_bookshelf_section' },\r\n    ];\r\n    for (const { sel, anchor } of sections) {\r\n        const section = $(sel);\r\n        if (!section.length || settingsContent.find(sel).length) {\r\n            continue;\r\n        }\r\n        const placeholder = settingsContent.find(`i.ai-wbr-park-anchor[data-anchor=\"${anchor}\"]`).first();\r\n        if (placeholder.length) {\r\n            placeholder.before(section.detach());\r\n            placeholder.remove();\r\n        } else {\r\n            settingsContent.append(section.detach());\r\n        }\r\n    }\r\n}\r\n\r\nfunction parkStandalonePanels(...selectors) {\r\n    let parking = $('#ai_wbr_console_parking');\r\n    if (!parking.length) {\r\n        parking = $('<div id=\"ai_wbr_console_parking\" class=\"ai-wbr-console-parking\"></div>');\r\n        $('body').append(parking);\r\n    }\r\n    ensureBookshelfStandaloneSection();\r\n    const targetSelectors = selectors.length ? selectors : ['#ai_wbr_memory_section', '#ai_wbr_memory_graph_section', '#ai_wbr_bookshelf_section'];\r\n    for (const sel of targetSelectors) {\r\n        const section = $(sel);\r\n        if (!section.length) {\r\n            continue;\r\n        }\r\n        // Drop a placeholder at the section's original spot so restore can put\r\n        // it back exactly where it was, instead of at the end of the list.\r\n        if (!section.prev('.ai-wbr-park-anchor').length) {\r\n            const anchor = $(`<i class=\"ai-wbr-park-anchor\" data-anchor=\"${section.attr('id') || ''}\"></i>`);\r\n            section.before(anchor);\r\n        }\r\n        section.detach().appendTo(parking);\r\n    }\r\n}\r\n\r\nfunction renderStandaloneOverview(container) {\n    const candidates = Array.isArray(lastRun?.candidates) ? lastRun.candidates : [];\r\n    const selected = Array.isArray(lastRun?.selected) ? lastRun.selected : [];\r\n    const memoryCandidates = Array.isArray(lastRun?.memoryCandidates) ? lastRun.memoryCandidates : [];\r\n    const selectedMemories = Array.isArray(lastRun?.selectedMemories) ? lastRun.selectedMemories : [];\r\n    const status = getStandaloneStatusMeta();\r\n\r\n    container.append($('<div class=\"ai-wbr-console-hero\"></div>')\r\n        .append($('<div></div>')\r\n            .append($('<div class=\"ai-wbr-console-kicker\"></div>').text('AI Worldbook Router'))\r\n            .append($('<h3></h3>').text('世界书读取控制台'))\r\n            .append($('<p></p>').text('生成前自动筛选相关世界书、记忆和状态，再注入当前提示词。')))\r\n        .append($('<div class=\"ai-wbr-console-status-pill\"></div>')\n            .addClass(status.className)\n            .append($(`<i class=\"fa-solid ${status.icon}\"></i>`))\n            .append($('<span></span>').text(status.label))));\n\n    const runtimePanel = $('<div id=\"ai_wbr_runtime_activity_panel\" class=\"ai-wbr-runtime-activity-panel\"></div>');\n    container.append(runtimePanel);\n    renderRuntimeActivityPanel(runtimePanel);\n\n    container.append($('<div class=\"ai-wbr-console-stats\"></div>')\n        .append(createStandaloneStat('世界书候选', candidates.length))\r\n        .append(createStandaloneStat('世界书命中', selected.length))\r\n        .append(createStandaloneStat('记忆候选', memoryCandidates.length))\r\n        .append(createStandaloneStat('记忆命中', selectedMemories.length))\r\n        .append(createStandaloneStat('注入字符', lastRun.injectedChars || 0))\r\n        .append(createStandaloneStat('路由来源', lastRun.source || 'none')));\r\n\r\n    container.append($('<div class=\"ai-wbr-console-actions\"></div>')\r\n        .append($('<button class=\"menu_button\" type=\"button\"></button>').text(settings.enabled ? '关闭路由' : '启用路由').on('click', () => {\r\n            saveSetting('enabled', !settings.enabled);\r\n            $('#ai_wbr_enabled').prop('checked', !!settings.enabled);\r\n            renderStandaloneConsole();\r\n        }))\r\n        .append($('<button class=\"menu_button\" type=\"button\">刷新状态</button>').on('click', () => {\r\n            renderActiveWorldbookSelector();\r\n            renderDebugPanel();\r\n            renderMemoryPanel();\r\n        }))\r\n        .append($('<button class=\"menu_button\" type=\"button\">复制注入文本</button>').on('click', async () => {\r\n            await navigator.clipboard?.writeText?.(lastRun.injectionText || '');\r\n            toastr?.success?.('Done.', 'AI Worldbook Router');\r\n        })));\r\n\r\n    if (lastRun.error) {\r\n        container.append($('<div class=\"ai-wbr-console-alert error\"></div>').text(lastRun.error));\r\n    }\r\n}\r\n\r\nfunction renderStandaloneRoutes(container) {\r\n    const selectedIds = new Set((lastRun.selected || []).map(entry => getEntryId(entry, entry.uid)));\r\n    const memorySelectedIds = new Set((lastRun.selectedMemories || []).map(entry => String(entry.uid)));\r\n    const bookshelfSelectedIds = new Set((lastRun.selectedBookshelf || []).map(entry => String(entry.uid || entry.id)));\r\n    const candidates = Array.isArray(lastRun.candidates) ? lastRun.candidates : [];\r\n    const memoryCandidates = Array.isArray(lastRun.memoryCandidates) ? lastRun.memoryCandidates : [];\r\n    const bookshelfCandidates = Array.isArray(lastRun.bookshelfCandidates) ? lastRun.bookshelfCandidates : [];\r\n    const list = $('<div class=\"ai-wbr-console-entry-list\"></div>');\r\n\r\n    container.append($('<div class=\"ai-wbr-console-section-title\"></div>').text('世界书路由结果'));\r\n    if (!candidates.length && !memoryCandidates.length && !bookshelfCandidates.length) {\r\n        list.append($('<div class=\"ai-wbr-console-empty\"></div>').text('尚无路由记录。下一次生成后会显示候选与命中条目。'));\r\n    }\r\n    for (const entry of candidates) {\r\n        appendStandaloneEntryCard(list, entry, 'worldbook', selectedIds.has(getEntryId(entry, entry.uid)));\r\n    }\r\n    for (const entry of memoryCandidates) {\r\n        appendStandaloneEntryCard(list, entry, 'memory', memorySelectedIds.has(String(entry.uid)));\r\n    }\r\n    for (const entry of bookshelfCandidates) {\r\n        appendStandaloneEntryCard(list, entry, 'bookshelf', bookshelfSelectedIds.has(String(entry.uid || entry.id)));\r\n    }\r\n    container.append(list);\r\n}\r\n\r\nfunction renderStandaloneInjection(container) {\r\n    container.append($('<div class=\"ai-wbr-console-section-head\"></div>')\r\n        .append($('<div class=\"ai-wbr-console-section-title\"></div>').text('本轮最终注入文本'))\r\n        .append($('<button class=\"menu_button\" type=\"button\">复制</button>').on('click', async () => {\r\n            await navigator.clipboard?.writeText?.(lastRun.injectionText || '');\r\n            toastr?.success?.('Done.', 'AI Worldbook Router');\r\n        })));\r\n    container.append($('<pre class=\"ai-wbr-console-pre\"></pre>').text(lastRun.injectionText || '尚无本轮注入记录'));\r\n}\r\n\r\nfunction renderStandaloneModel(container) {\r\n    container.append($('<div class=\"ai-wbr-console-section-title\"></div>').text('独立路由模型'));\r\n    const panel = $('<div class=\"ai-wbr-console-form\"></div>');\r\n    panel.append($('<label class=\"checkbox_label\"></label>')\r\n        .append($('<input type=\"checkbox\" />').prop('checked', !!settings.routerUseSeparateModel).on('input', function () {\r\n            saveSetting('routerUseSeparateModel', !!$(this).prop('checked'));\r\n            $('#ai_wbr_router_use_separate_model').prop('checked', !!settings.routerUseSeparateModel);\r\n            renderStandaloneConsole();\r\n        }))\r\n        .append($('<span></span>').text('启用独立路由模型')));\r\n    panel.append($('<label></label>').text('API URL'));\r\n    panel.append($('<input class=\"text_pole\" type=\"text\" />').val(settings.routerApiUrl || '').on('input', function () {\r\n        saveSetting('routerApiUrl', normalizeUrl($(this).val()));\r\n        $('#ai_wbr_router_api_url').val(settings.routerApiUrl);\r\n    }));\r\n    panel.append($('<label></label>').text('API Key'));\r\n    panel.append($('<input class=\"text_pole\" type=\"password\" />').val(settings.routerApiKey || '').on('input', function () {\r\n        saveSetting('routerApiKey', String($(this).val() || '').trim());\r\n        $('#ai_wbr_router_api_key').val(settings.routerApiKey);\r\n    }));\r\n    panel.append($('<label></label>').text('路由模型'));\r\n\r\n    const select = $('<select class=\"text_pole\"></select>').append('<option value=\"\">未选择</option>');\r\n    for (const modelId of settings.routerModels || []) {\r\n        select.append($('<option></option>', { value: modelId, text: modelId, selected: modelId === settings.routerModel }));\r\n    }\r\n    if (settings.routerModel && !(settings.routerModels || []).includes(settings.routerModel)) {\r\n        select.append($('<option></option>', { value: settings.routerModel, text: `${settings.routerModel} (手动)`, selected: true }));\r\n    }\r\n    panel.append(select.on('change', function () {\r\n        saveSetting('routerModel', String($(this).val() || ''));\r\n        $('#ai_wbr_router_model').val(settings.routerModel);\r\n    }));\r\n    panel.append($('<div class=\"ai-wbr-console-actions\"></div>')\r\n        .append($('<button class=\"menu_button\" type=\"button\">拉取模型</button>').on('click', fetchRouterModels))\r\n        .append($('<span class=\"ai-wbr-status\"></span>').text(settings.routerStatus || '未连接')));\r\n    container.append(panel);\r\n}\r\n\r\nfunction createStandaloneSettingsToggle(label, key, description = '', afterSave = null) {\r\n    return $('<label class=\"checkbox_label\"></label>')\r\n        .append($('<input type=\"checkbox\" />')\r\n            .prop('checked', !!settings[key])\r\n            .on('input', function () {\r\n                saveSetting(key, !!$(this).prop('checked'));\r\n                $(`#ai_wbr_${String(key).replace(/[A-Z]/g, letter => `_${letter.toLowerCase()}`)}`).prop('checked', !!settings[key]);\r\n                if (typeof afterSave === 'function') afterSave();\r\n                renderStandaloneConsole('settings');\r\n            }))\r\n        .append($('<span></span>').text(label))\r\n        .append(description ? $('<small></small>').text(description) : '');\r\n}\r\n\r\nfunction createStandaloneSettingsNav(label, tabId, description) {\r\n    return $('<button class=\"menu_button\" type=\"button\"></button>')\r\n        .append($('<b></b>').text(label))\r\n        .append(description ? $('<small></small>').text(description) : '')\r\n        .on('click', () => renderStandaloneConsole(tabId));\r\n}\r\n\r\nfunction renderStandaloneSettings(container) {\r\n    const settingsContent = getSettingsContentContainer();\r\n    const originalSections = settingsContent.find('.ai-wbr-section').length;\r\n    container.append($('<div class=\"ai-wbr-console-alert\"></div>').text(\r\n        originalSections\r\n            ? '完整设置仍保留在扩展设置页；这里提供常用开关和分区快捷入口。'\r\n            : '如果完整设置页未显示，请关闭并重新打开扩展设置，插件会自动还原被独立窗口临时移动的面板。',\r\n    ));\r\n    container.append($('<div class=\"ai-wbr-console-section-title\"></div>').text('核心设置'));\r\n    container.append(\r\n        $('<div class=\"ai-wbr-console-form\"></div>')\r\n            .append(createStandaloneSettingsToggle('启用前置 AI 世界书路由', 'enabled', '控制正式生成前是否筛选并注入世界书。'))\r\n            .append(createStandaloneSettingsToggle('启用记忆存储', 'memoryEnabled', '开启后才会整理聊天记忆和刷新图谱。'))\r\n            .append(createStandaloneSettingsToggle('实时整理聊天记忆', 'memoryRealtimeEnabled', '生成结束后自动整理最近消息。'))\r\n            .append(createStandaloneSettingsToggle('间隔归纳整理', 'memorySummaryEnabled', '按消息间隔压缩阶段剧情和关系变化。'))\r\n            .append(createStandaloneSettingsToggle('记忆状态参与路由', 'memoryInjectToRouter', '让图谱摘要辅助世界书路由命中。'))\n            .append(createStandaloneSettingsToggle('显示悬浮图谱按钮', 'floatingButtonEnabled', '控制右侧图谱悬浮按钮是否显示；回到 APP 前台时会自动恢复。'))\n            .append(createStandaloneSettingsToggle('启用书架补充召回', 'bookshelfEnabled', '允许书架和记忆书参与向量召回。'))\n            .append(createStandaloneSettingsToggle('自动维护当前聊天记忆书', 'bookshelfAutoMemoryBook', '从当前图谱自动生成聊天绑定记忆书。', () => {\n                if (settings.bookshelfAutoMemoryBook) {\n                    scheduleBookshelfMemoryBookSync(getContext(), { force: true, silent: false, delayMs: 100 });\n                }\n            }))\n            .append(createStandaloneSettingsToggle('记忆书生成后自动向量化', 'bookshelfAutoMemoryBookVectorize', '自动记忆书刷新后立即构建可召回向量。')),\n    );\r\n\r\n    container.append($('<div class=\"ai-wbr-console-section-title\"></div>').text('设置分区'));\r\n    container.append(\n        $('<div class=\"ai-wbr-console-actions ai-wbr-console-settings-nav\"></div>')\n            .append(createStandaloneSettingsNav('记忆设置', 'memory', '整理、历史补录、JSON 和调试。'))\n            .append(createStandaloneSettingsNav('图谱视图', 'graph', '全屏图谱、布局和详情面板。'))\n            .append(createStandaloneSettingsNav('书架设置', 'bookshelf', '记忆书、向量模型和召回测试。')),\n    );\n\n    container.append($('<div class=\"ai-wbr-console-section-title\"></div>').text('运行流程'));\n    const runtimePanel = $('<div id=\"ai_wbr_runtime_activity_panel\" class=\"ai-wbr-runtime-activity-panel\"></div>');\n    container.append(runtimePanel);\n    renderRuntimeActivityPanel(runtimePanel);\n\n    container.append($('<div class=\"ai-wbr-console-section-title\"></div>').text('API 调用统计'));\n    const apiStatsPanel = $('<div id=\"ai_wbr_api_stats_panel\" class=\"ai-wbr-api-stats-panel\"></div>');\n    container.append(apiStatsPanel);\n    renderApiStatsPanel(apiStatsPanel);\n\n    renderStandaloneModel(container);\n}\n\r\nfunction renderStandaloneConsole(tabId = getStandaloneTabId()) {\n    const body = $('#ai_wbr_console_body');\r\n    if (!body.length) {\r\n        return;\r\n    }\r\n\r\n    restoreStandalonePanelsToSettings();\r\n    const isGraphTab = tabId === 'graph';\r\n    if (!isGraphTab && memoryGraphFullscreenActive) {\r\n        setMemoryGraphFullscreen(false, { fit: false });\r\n    }\r\n    $('#ai_wbr_floating_window').toggleClass('ai-wbr-floating-window-graph', isGraphTab);\r\n    body.toggleClass('ai-wbr-console-body-graph', isGraphTab);\r\n    $('#ai_wbr_console_tabs .ai-wbr-console-tab').removeClass('active')\r\n        .filter(`[data-tab=\"${escapeCssSelector(tabId)}\"]`).addClass('active');\r\n    body.empty();\r\n\r\n    if (tabId === 'overview') {\r\n        renderStandaloneOverview(body);\r\n    } else if (tabId === 'routes') {\r\n        renderStandaloneRoutes(body);\r\n    } else if (tabId === 'injection') {\r\n        renderStandaloneInjection(body);\r\n    } else if (tabId === 'memory') {\r\n        parkStandalonePanels('#ai_wbr_memory_section');\r\n        body.append($('#ai_wbr_memory_section'));\r\n        renderMemoryPanel('memory');\r\n    } else if (tabId === 'graph') {\r\n        parkStandalonePanels('#ai_wbr_memory_graph_section');\r\n        body.append($('#ai_wbr_memory_graph_section'));\r\n        renderMemoryPanel('graph');\r\n        requestAnimationFrame(() => {\r\n            if (getStandaloneTabId() === 'graph' && $('#ai_wbr_memory_graph').is(':visible')) {\r\n                fitMemoryGraphToContainer();\r\n                renderMemoryPanel('graph');\r\n            }\r\n        });\r\n    } else if (tabId === 'bookshelf') {\r\n        parkStandalonePanels('#ai_wbr_bookshelf_section');\r\n        body.append(ensureBookshelfStandaloneSection());\r\n        syncBookshelfProviderVisibility();\r\n        renderBookshelfPanel();\r\n    } else if (tabId === 'settings') {\n        renderStandaloneSettings(body);\n    } else if (tabId === 'debug') {\n        renderStandaloneConsole('memory');\n        return;\n    }\n\r\n    const activity = getRuntimeActivity();\n    if (activity.status && activity.status !== 'idle') {\n        updateRuntimeActivityFab();\n    } else {\n        const status = getStandaloneStatusMeta();\n        $('#ai_wbr_fab')\n            .removeClass('ai-wbr-fab-runtime idle ready active ok warn error running success failed')\n            .removeAttr('data-runtime-label')\n            .addClass(status.className)\n            .attr('title', `世界书读取：${status.label}`);\n        $('#ai_wbr_console_status')\n            .removeClass('idle ready active ok warn error running success failed')\n            .addClass(status.className)\n            .text(status.label);\n    }\n}\n\r\nfunction escapeHtml(value) {\r\n    return String(value ?? '')\r\n        .replace(/&/g, '&amp;')\r\n        .replace(/</g, '&lt;')\r\n        .replace(/>/g, '&gt;')\r\n        .replace(/\"/g, '&quot;')\r\n        .replace(/'/g, '&#39;');\r\n}\r\n\r\nfunction escapeCssSelector(value) {\r\n    const text = String(value ?? '');\r\n    if (globalThis.CSS?.escape) {\r\n        return CSS.escape(text);\r\n    }\r\n\r\n    return text.replace(/[\"\\\\]/g, '\\\\$&');\r\n}\r\n\r\nfunction getMemoryGraphSearchHaystack(node) {\r\n    return [\r\n        node?.id,\r\n        node?.title,\r\n        node?.summary,\r\n        node?.content,\r\n        node?.location,\r\n        node?.timeSpan,\r\n        node?.type,\r\n        ...(Array.isArray(node?.keys) ? node.keys : []),\r\n        ...(Array.isArray(node?.tags) ? node.tags : []),\r\n    ].map(value => String(value || '').toLowerCase()).join('\\n');\r\n}\r\n\r\nfunction getMemoryGraphTextUnit(char) {\r\n    const value = String(char || '');\r\n    const code = value.codePointAt(0) || 0;\r\n    if (!value) return 0;\r\n    if (/\\s/u.test(value)) return 0.34;\r\n    if (/[\\u2e80-\\u9fff\\uac00-\\ud7af\\uff00-\\uffef]/u.test(value)) return 1;\r\n    if (/[A-Z]/.test(value)) return 0.68;\r\n    if (/[a-z]/.test(value)) return 0.56;\r\n    if (/[0-9]/.test(value)) return 0.58;\r\n    if (/[.,;:!?'\"`~，。；：！？、]/u.test(value)) return 0.36;\r\n    return code > 127 ? 0.82 : 0.62;\r\n}\r\n\r\nfunction getMemoryGraphTextUnits(value) {\r\n    return Array.from(String(value || '')).reduce((sum, char) => sum + getMemoryGraphTextUnit(char), 0);\r\n}\r\n\r\nfunction truncateMemoryGraphTextByUnits(value, maxUnits = 14) {\r\n    const chars = Array.from(String(value || '').replace(/\\s+/g, ' ').trim());\r\n    if (!chars.length) {\r\n        return '';\r\n    }\r\n    if (getMemoryGraphTextUnits(chars.join('')) <= maxUnits) {\r\n        return chars.join('');\r\n    }\r\n    const ellipsis = '...';\r\n    const ellipsisUnits = getMemoryGraphTextUnits(ellipsis);\r\n    let line = '';\r\n    let units = 0;\r\n    for (const char of chars) {\r\n        const nextUnits = getMemoryGraphTextUnit(char);\r\n        if (units + nextUnits + ellipsisUnits > maxUnits) {\r\n            break;\r\n        }\r\n        line += char;\r\n        units += nextUnits;\r\n    }\r\n    return `${line.trim()}${ellipsis}`;\r\n}\r\n\r\nfunction splitMemoryGraphSvgText(value, maxChars = 14, maxLines = 1) {\r\n    const text = String(value || '').replace(/\\s+/g, ' ').trim();\r\n    if (!text) {\r\n        return [];\r\n    }\r\n\r\n    const lines = [];\r\n    const chars = Array.from(text);\r\n    let line = '';\r\n    let units = 0;\r\n    let index = 0;\r\n    while (index < chars.length && lines.length < maxLines) {\r\n        const char = chars[index];\r\n        const nextUnits = getMemoryGraphTextUnit(char);\r\n        if (line && units + nextUnits > maxChars) {\r\n            lines.push(line.trim());\r\n            line = '';\r\n            units = 0;\r\n            continue;\r\n        }\r\n        line += char;\r\n        units += nextUnits;\r\n        index += 1;\r\n    }\r\n    if (line && lines.length < maxLines) {\r\n        lines.push(line.trim());\r\n    }\r\n    if (index < chars.length && lines.length) {\r\n        const lastIndex = lines.length - 1;\r\n        lines[lastIndex] = truncateMemoryGraphTextByUnits(lines[lastIndex], maxChars);\r\n    }\r\n\r\n    return lines.filter(Boolean);\r\n}\r\n\r\nfunction renderMemoryGraphSvgTextLines(lines, className, x, y, lineHeight = 14) {\r\n    const safeLines = Array.isArray(lines) && lines.length ? lines : ['暂无摘要'];\r\n    const tspans = safeLines.map((line, index) => (\r\n        `<tspan x=\"${x}\" dy=\"${index === 0 ? 0 : lineHeight}\">${escapeHtml(line)}</tspan>`\r\n    )).join('');\r\n    return `<text class=\"${className}\" x=\"${x}\" y=\"${y}\">${tspans}</text>`;\r\n}\r\n\r\nfunction inferMemoryGraphNodeKind(node = {}) {\r\n    const explicit = String(node?.memoryKind || node?.kind || node?.dimension || '').trim().toLowerCase();\r\n    const explicitMatch = MEMORY_GRAPH_KIND_DEFINITIONS.find(item => item.key === explicit || item.label === explicit);\r\n    if (explicitMatch) {\r\n        return explicitMatch;\r\n    }\r\n    const text = normalizeText([\r\n        node?.title,\r\n        node?.summary,\r\n        node?.content,\r\n        node?.description,\r\n        node?.location,\r\n        node?.timeSpan,\r\n        ...(Array.isArray(node?.keys) ? node.keys : []),\r\n        ...(Array.isArray(node?.tags) ? node.tags : []),\r\n    ].filter(Boolean).join(' '));\r\n    const cueMatch = MEMORY_GRAPH_KIND_DEFINITIONS.find(item => item.cues.test(text));\r\n    if (cueMatch) {\r\n        return cueMatch;\r\n    }\r\n    const type = String(node?.type || 'event').toLowerCase();\r\n    return MEMORY_GRAPH_KIND_DEFINITIONS.find(item => item.types.includes(type))\r\n        || MEMORY_GRAPH_KIND_DEFINITIONS[0];\r\n}\r\n\r\nfunction getMemoryGraphNodePreviewChips(node, typeLabel) {\r\n    const kind = inferMemoryGraphNodeKind(node);\r\n    const chips = uniqueStrings([\r\n        kind?.label || typeLabel,\r\n        node?.location,\r\n        node?.timeSpan,\r\n        typeLabel,\r\n        ...(Array.isArray(node?.tags) ? node.tags : []),\r\n        ...(Array.isArray(node?.keys) ? node.keys : []),\r\n    ].filter(Boolean)).slice(0, 5);\r\n    const visible = chips.slice(0, 3).map(chip => truncateMemoryGraphTextByUnits(chip, 8.4));\r\n    if (chips.length > visible.length) {\r\n        visible.push(`+${chips.length - visible.length}`);\r\n    }\r\n    return visible;\r\n}\r\n\r\nfunction renderMemoryGraphSvgChips(chips, x = 12, y = 58) {\r\n    let offset = 0;\r\n    return (Array.isArray(chips) ? chips : []).map((chip) => {\r\n        const label = String(chip || '').trim();\r\n        if (!label) {\r\n            return '';\r\n        }\r\n        const width = Math.min(78, Math.max(28, getMemoryGraphTextUnits(label) * 7 + 14));\r\n        const html = `<g class=\"ai-wbr-memory-node-chip\" transform=\"translate(${x + offset},${y})\">\r\n            <rect width=\"${width}\" height=\"17\" rx=\"8.5\" ry=\"8.5\"></rect>\r\n            <text x=\"${width / 2}\" y=\"11.5\">${escapeHtml(label)}</text>\r\n        </g>`;\r\n        offset += width + 5;\r\n        return html;\r\n    }).join('');\r\n}\r\n\r\nfunction getMemoryGraphNodeScore(node, degree = 0, index = 0) {\r\n    const importance = clampNumber(node?.importance, 0.5, 0, 1);\r\n    const updatedAt = Date.parse(node?.updatedAt || node?.createdAt || '') || 0;\r\n    const recencyBoost = updatedAt ? Math.min(0.35, Math.max(0, updatedAt / Math.max(1, Date.now()) * 0.35)) : 0;\r\n    return (degree * 1.6) + importance + recencyBoost + (1 / Math.max(10, index + 10));\r\n}\r\n\r\nfunction getMemoryGraphCategoryDefinition(key) {\r\n    const normalized = String(key || '').toLowerCase();\r\n    return MEMORY_GRAPH_CATEGORY_DEFINITIONS.find(item => item.key === normalized) || MEMORY_GRAPH_CATEGORY_DEFINITIONS.find(item => item.key === 'event');\r\n}\r\n\r\nfunction getMemoryGraphFocusedCategoryFromTypes(typeFilter) {\r\n    if (!(typeFilter instanceof Set) || typeFilter.size === 0) {\r\n        return '';\r\n    }\r\n    const selectedTypes = [...typeFilter].map(type => String(type || '').toLowerCase()).sort();\r\n    const match = MEMORY_GRAPH_CATEGORY_DEFINITIONS.find((definition) => {\r\n        const categoryTypes = definition.types.slice().sort();\r\n        return selectedTypes.length === categoryTypes.length\r\n            && selectedTypes.every((type, index) => type === categoryTypes[index]);\r\n    });\r\n    return match?.key || '';\r\n}\r\n\r\nfunction setMemoryGraphFocusedCategory(categoryKey) {\r\n    const definition = getMemoryGraphCategoryDefinition(categoryKey);\r\n    if (!definition || !definition.key || String(categoryKey || '') === 'all') {\r\n        memoryGraphVisibleTypes = new Set();\r\n        return '';\r\n    }\r\n    memoryGraphVisibleTypes = new Set(definition.types);\r\n    return definition.key;\r\n}\r\n\r\nfunction getMemoryGraphCategoryKey(type) {\r\n    const normalized = String(type || 'event').toLowerCase();\r\n    const definition = MEMORY_GRAPH_CATEGORY_DEFINITIONS.find(item => item.types.includes(normalized));\r\n    return definition?.key || 'event';\r\n}\r\n\r\nfunction getMemoryGraphNodeCategoryKey(node) {\r\n    return getMemoryGraphCategoryKey(node?.type || 'event');\r\n}\r\n\r\nfunction selectMemoryGraphVisibleNodes(scoredItems, nodeLimit, mode = 'overview') {\r\n    const sorted = (Array.isArray(scoredItems) ? scoredItems : [])\r\n        .slice()\r\n        .sort((a, b) => b.score - a.score);\r\n    const typeFilter = memoryGraphVisibleTypes instanceof Set ? memoryGraphVisibleTypes : new Set();\r\n    const focusedCategory = getMemoryGraphFocusedCategoryFromTypes(typeFilter);\r\n    if (mode !== 'overview' || sorted.length <= nodeLimit || focusedCategory) {\r\n        return sorted.slice(0, nodeLimit).map(item => item.node);\r\n    }\r\n\r\n    const visible = [];\r\n    const usedIds = new Set();\r\n    const reservePerCategory = nodeLimit >= 42 ? 4 : nodeLimit >= 30 ? 3 : 2;\r\n    for (const category of MEMORY_GRAPH_CATEGORY_ORDER) {\r\n        const picks = sorted\r\n            .filter(item => getMemoryGraphNodeCategoryKey(item.node) === category && !usedIds.has(String(item.node?.id || '')))\r\n            .slice(0, reservePerCategory);\r\n        for (const pick of picks) {\r\n            const id = String(pick.node?.id || '');\r\n            if (!id || usedIds.has(id) || visible.length >= nodeLimit) {\r\n                continue;\r\n            }\r\n            visible.push(pick.node);\r\n            usedIds.add(id);\r\n        }\r\n    }\r\n\r\n    for (const item of sorted) {\r\n        const id = String(item.node?.id || '');\r\n        if (!id || usedIds.has(id) || visible.length >= nodeLimit) {\r\n            continue;\r\n        }\r\n        visible.push(item.node);\r\n        usedIds.add(id);\r\n    }\r\n    return visible;\r\n}\r\n\r\nfunction buildMemoryGraphCategoryGroups(nodes, links = [], degree = new Map()) {\r\n    const nodeList = Array.isArray(nodes) ? nodes : [];\r\n    const nodeIds = new Set(nodeList.map(node => String(node.id || '')));\r\n    const nodeById = new Map(nodeList.map(node => [String(node.id || ''), node]));\r\n    const relatedCategoryByNode = new Map(nodeList.map(node => [String(node.id || ''), new Set()]));\r\n    for (const link of Array.isArray(links) ? links : []) {\r\n        const source = String(link?.source || '');\r\n        const target = String(link?.target || '');\r\n        if (!nodeIds.has(source) || !nodeIds.has(target)) {\r\n            continue;\r\n        }\r\n        const sourceNode = nodeById.get(source);\r\n        const targetNode = nodeById.get(target);\r\n        const sourceCategory = getMemoryGraphNodeCategoryKey(sourceNode);\r\n        const targetCategory = getMemoryGraphNodeCategoryKey(targetNode);\r\n        if (sourceCategory !== targetCategory) {\r\n            relatedCategoryByNode.get(source)?.add(targetCategory);\r\n            relatedCategoryByNode.get(target)?.add(sourceCategory);\r\n        }\r\n    }\r\n    return MEMORY_GRAPH_CATEGORY_DEFINITIONS\r\n        .map((definition) => {\r\n            const groupNodes = nodeList\r\n                .filter(node => getMemoryGraphNodeCategoryKey(node) === definition.key)\r\n                .slice()\r\n                .sort((a, b) => getMemoryGraphNodeScore(b, degree.get(String(b.id)) || 0) - getMemoryGraphNodeScore(a, degree.get(String(a.id)) || 0));\r\n            return {\r\n                ...definition,\r\n                nodes: groupNodes,\r\n                count: groupNodes.length,\r\n                relatedCategories: uniqueStrings(groupNodes.flatMap(node => [...(relatedCategoryByNode.get(String(node.id || '')) || [])])),\r\n            };\r\n        })\r\n        .filter(group => group.count > 0);\r\n}\r\n\r\nfunction buildMemoryGraphDisplayModel(graph) {\r\n    const allNodes = Array.isArray(graph?.nodes) ? graph.nodes : [];\r\n    const allLinks = Array.isArray(graph?.links) ? graph.links : [];\r\n    const nodeById = new Map(allNodes.map(node => [String(node.id), node]));\r\n    const selectedNodeId = String(memoryGraphSelectedNodeId || '');\r\n    const selectedLinkId = String(memoryGraphSelectedLinkId || '');\r\n    const query = String(memoryGraphSearchText || '').trim().toLowerCase();\r\n    const minWeight = clampNumber(memoryGraphMinLinkWeight, 0.35, 0, 1);\r\n    const typeFilter = memoryGraphVisibleTypes instanceof Set ? memoryGraphVisibleTypes : new Set();\r\n    const hasTypeFilter = typeFilter.size > 0;\r\n    const focusedCategory = getMemoryGraphFocusedCategoryFromTypes(typeFilter);\r\n    const degree = new Map(allNodes.map(node => [String(node.id), 0]));\r\n\r\n    for (const link of allLinks) {\r\n        const source = String(link?.source || '');\r\n        const target = String(link?.target || '');\r\n        if (!nodeById.has(source) || !nodeById.has(target)) {\r\n            continue;\r\n        }\r\n        const weight = clampNumber(link?.weight, 0.5, 0, 1);\r\n        degree.set(source, (degree.get(source) || 0) + weight);\r\n        degree.set(target, (degree.get(target) || 0) + weight);\r\n    }\r\n\r\n    const typeFilteredNodes = allNodes.filter((node) => {\r\n        const type = String(node?.type || 'event');\r\n        return !hasTypeFilter || typeFilter.has(type);\r\n    });\r\n    const typeFilteredIds = new Set(typeFilteredNodes.map(node => String(node.id)));\r\n    let mode = String(memoryGraphDisplayMode || 'overview');\r\n    if (mode === 'focus' && (!selectedNodeId || !nodeById.has(selectedNodeId))) {\r\n        mode = 'overview';\r\n    }\r\n\r\n    let candidateIds = new Set(typeFilteredIds);\r\n    if (query) {\r\n        const matchedIds = new Set(typeFilteredNodes\r\n            .filter(node => getMemoryGraphSearchHaystack(node).includes(query))\r\n            .map(node => String(node.id)));\r\n        for (const link of allLinks) {\r\n            const source = String(link?.source || '');\r\n            const target = String(link?.target || '');\r\n            if (matchedIds.has(source) && typeFilteredIds.has(target)) {\r\n                matchedIds.add(target);\r\n            }\r\n            if (matchedIds.has(target) && typeFilteredIds.has(source)) {\r\n                matchedIds.add(source);\r\n            }\r\n        }\r\n        candidateIds = matchedIds;\r\n        if (mode !== 'timeline') {\r\n            mode = 'search';\r\n        }\r\n    } else if (mode === 'focus') {\r\n        candidateIds = new Set([selectedNodeId]);\r\n        for (const link of allLinks) {\r\n            const source = String(link?.source || '');\r\n            const target = String(link?.target || '');\r\n            if (source === selectedNodeId && typeFilteredIds.has(target)) {\r\n                candidateIds.add(target);\r\n            }\r\n            if (target === selectedNodeId && typeFilteredIds.has(source)) {\r\n                candidateIds.add(source);\r\n            }\r\n        }\r\n    }\r\n\r\n    const nodeLimit = mode === 'full' ? 120 : mode === 'timeline' ? 120 : mode === 'focus' ? 54 : mode === 'search' ? 72 : 36;\r\n    const scoredVisibleNodes = typeFilteredNodes\r\n        .filter(node => candidateIds.has(String(node.id)))\r\n        .map((node, index) => ({\r\n            node,\r\n            score: getMemoryGraphNodeScore(node, degree.get(String(node.id)) || 0, index),\r\n        }))\r\n        .sort((a, b) => b.score - a.score);\r\n    const visibleNodes = selectMemoryGraphVisibleNodes(scoredVisibleNodes, nodeLimit, mode);\r\n    const visibleIds = new Set(visibleNodes.map(node => String(node.id)));\r\n\r\n    const linkLimit = mode === 'full' ? 180 : mode === 'overview' ? 80 : mode === 'timeline' ? 80 : 140;\r\n    const visibleLinks = allLinks\r\n        .filter((link) => {\r\n            const source = String(link?.source || '');\r\n            const target = String(link?.target || '');\r\n            if (!visibleIds.has(source) || !visibleIds.has(target)) {\r\n                return false;\r\n            }\r\n            if (String(link?.id || '') === selectedLinkId) {\r\n                return true;\r\n            }\r\n            if (mode === 'focus' && (source === selectedNodeId || target === selectedNodeId)) {\r\n                return true;\r\n            }\r\n            return clampNumber(link?.weight, 0.5, 0, 1) >= minWeight;\r\n        })\r\n        .map((link, index) => ({\r\n            link,\r\n            index,\r\n            score: (String(link?.id || '') === selectedLinkId ? 10 : 0)\r\n                + (mode === 'focus' && (String(link?.source || '') === selectedNodeId || String(link?.target || '') === selectedNodeId) ? 4 : 0)\r\n                + clampNumber(link?.weight, 0.5, 0, 1),\r\n        }))\r\n        .sort((a, b) => b.score - a.score || a.index - b.index)\r\n        .slice(0, linkLimit)\r\n        .map(item => item.link);\r\n    const categoryGroups = buildMemoryGraphCategoryGroups(visibleNodes, visibleLinks, degree);\r\n    const allCategoryGroups = buildMemoryGraphCategoryGroups(allNodes, allLinks, degree);\r\n    const visibleCategoryCounts = new Map(categoryGroups.map(group => [group.key, group.count]));\r\n    const totalCategoryCounts = new Map(allCategoryGroups.map(group => [group.key, group.count]));\r\n    const enrichedCategoryGroups = categoryGroups.map(group => ({\r\n        ...group,\r\n        totalCount: totalCategoryCounts.get(group.key) || group.count,\r\n        visibleCount: visibleCategoryCounts.get(group.key) || group.count,\r\n    }));\r\n\r\n    return {\r\n        nodes: visibleNodes,\r\n        links: visibleLinks,\r\n        categoryGroups: enrichedCategoryGroups,\r\n        allCategoryGroups,\r\n        focusedCategory,\r\n        mode,\r\n        query,\r\n        minWeight,\r\n        totalNodes: allNodes.length,\r\n        totalLinks: allLinks.length,\r\n        hiddenNodes: Math.max(0, allNodes.length - visibleNodes.length),\r\n        hiddenLinks: Math.max(0, allLinks.length - visibleLinks.length),\r\n    };\r\n}\r\n\r\nfunction renderMemoryGraphTypeFilters(graph) {\n    const nodes = Array.isArray(graph?.nodes) ? graph.nodes : [];\n    if (!nodes.length) {\n        return '';\n    }\n    const counts = new Map(MEMORY_GRAPH_CATEGORY_ORDER.map(key => [key, 0]));\r\n    for (const node of nodes) {\r\n        const category = getMemoryGraphNodeCategoryKey(node);\r\n        counts.set(category, (counts.get(category) || 0) + 1);\r\n    }\r\n    const focusedCategory = getMemoryGraphFocusedCategoryFromTypes(memoryGraphVisibleTypes);\r\n    return `<div class=\"ai-wbr-memory-graph-typebar\">\r\n        <button class=\"menu_button ai-wbr-memory-type-filter${!focusedCategory ? ' active' : ''}\" type=\"button\" data-memory-category=\"all\">全部 <small>${nodes.length}</small></button>\r\n        ${MEMORY_GRAPH_CATEGORY_DEFINITIONS.map((definition) => {\r\n            const count = counts.get(definition.key) || 0;\r\n            const active = focusedCategory === definition.key;\r\n            return `<button class=\"menu_button ai-wbr-memory-type-filter${active ? ' active' : ''}\" type=\"button\" data-memory-category=\"${escapeHtml(definition.key)}\" ${count ? '' : 'disabled'}>${escapeHtml(definition.label)} <small>${count}</small></button>`;\r\n        }).join('')}\n    </div>`;\n}\n\nfunction renderMemoryRuleDrawerHtml() {\n    const config = getMemoryRuleConfig();\n    return `\n        <aside id=\"ai_wbr_memory_rule_drawer\" class=\"ai-wbr-memory-rule-drawer\" aria-hidden=\"${memoryRuleDrawerOpen ? 'false' : 'true'}\">\n            <div class=\"ai-wbr-memory-rule-head\">\n                <div>\n                    <div class=\"ai-wbr-memory-preview-kicker\">Memory Rules</div>\n                    <h3>AI 填卡说明</h3>\n                    <small>只告诉 AI 怎么基于上下文填写固定分栏记忆卡片；板块由字段名决定。</small>\n                </div>\n                <button class=\"menu_button ai-wbr-memory-rule-close\" type=\"button\">关闭</button>\n            </div>\n            <div class=\"ai-wbr-memory-rule-body\">\n                <label class=\"checkbox_label ai-wbr-memory-rule-master\">\n                    <input id=\"ai_wbr_memory_rule_enabled\" type=\"checkbox\" ${config.enabled ? 'checked' : ''} />\n                    <span>启用 AI 填卡说明</span>\n                </label>\n                <div class=\"ai-wbr-memory-rule-card ai-wbr-memory-rule-ai-guide\">\n                    <b>AI 填卡说明</b>\n                    <small>这里写自然语言说明即可。实时更新时 AI 会优先输出角色、地点、道具、势力、设定、规则、任务、事件固定分栏；本地按字段名映射板块并合并同类型同标题卡。</small>\n                    <textarea id=\"ai_wbr_memory_rule_ai_guide\" class=\"text_pole\" rows=\"14\" placeholder=\"例如：角色标题只写人名；任务标题写未完成目标；事件只写本轮发生了什么；证据不足时跳过。\">${escapeHtml(config.aiCardWritingGuide || '')}</textarea>\n                    <div class=\"ai-wbr-memory-rule-preview-actions\">\n                        <button class=\"menu_button ai-wbr-memory-rule-reset\" type=\"button\">恢复默认说明</button>\n                    </div>\n                </div>\n            </div>\n        </aside>\n    `;\n}\n\nfunction getMemoryPreviewGroupKey(type) {\n    return getMemoryGraphCategoryKey(type);\n}\n\r\nfunction getMemoryPreviewGroupLabel(group) {\r\n    return getMemoryGraphCategoryDefinition(group)?.label || group;\r\n}\r\n\r\nfunction renderMemoryPreviewPanel(graph, displayModel = buildMemoryGraphDisplayModel(graph)) {\r\n    const panel = $('#ai_wbr_memory_preview_panel');\r\n    if (!panel.length) {\r\n        return;\r\n    }\r\n    const shell = panel.closest('.ai-wbr-graph-shell');\r\n    const isOpen = shell.hasClass('preview-open');\r\n    const selectedId = String(memoryGraphSelectedNodeId || '');\r\n    const renderKey = [\r\n        isOpen ? 'open' : 'closed',\r\n        displayModel?.mode || '',\r\n        displayModel?.query || '',\r\n        selectedId,\r\n        (displayModel?.nodes || []).map(node => `${node.id}:${node.updatedAt || ''}`).join('|'),\r\n    ].join('::');\r\n    if (!isOpen && panel.children().length && memoryGraphPreviewRenderKey === renderKey) {\r\n        return;\r\n    }\r\n    if (!isOpen && panel.children().length) {\r\n        memoryGraphPreviewRenderKey = renderKey;\r\n        return;\r\n    }\r\n    if (memoryGraphPreviewRenderKey === renderKey) {\r\n        return;\r\n    }\r\n    memoryGraphPreviewRenderKey = renderKey;\r\n\r\n    const nodes = Array.isArray(displayModel?.nodes) ? displayModel.nodes : [];\r\n    const links = Array.isArray(displayModel?.links) ? displayModel.links : [];\r\n    const degree = new Map(nodes.map(node => [String(node.id), 0]));\r\n    for (const link of links) {\r\n        degree.set(String(link.source || ''), (degree.get(String(link.source || '')) || 0) + 1);\r\n        degree.set(String(link.target || ''), (degree.get(String(link.target || '')) || 0) + 1);\r\n    }\r\n\r\n    const grouped = new Map();\r\n    for (const node of nodes) {\r\n        const group = getMemoryPreviewGroupKey(node.type);\r\n        if (!grouped.has(group)) {\r\n            grouped.set(group, []);\r\n        }\r\n        grouped.get(group).push(node);\r\n    }\r\n\r\n    const orderedGroups = MEMORY_GRAPH_CATEGORY_ORDER;\r\n    const createCard = (node) => {\r\n        const typeLabel = getOptionLabel(MEMORY_NODE_TYPE_OPTIONS, node.type, node.type || 'event');\r\n        const summary = node.summary || node.content || '';\r\n        const selected = String(node.id) === String(memoryGraphSelectedNodeId);\r\n        const score = Math.round(clampNumber(node.importance, 0.5, 0, 1) * 100);\r\n        const tags = uniqueStrings([...(Array.isArray(node.keys) ? node.keys : []), ...(Array.isArray(node.tags) ? node.tags : [])]).slice(0, 3);\r\n        const temporalMeta = [node.location, node.timeSpan].filter(Boolean).join(' / ');\r\n        return $('<button class=\"ai-wbr-memory-preview-card\" type=\"button\"></button>')\r\n            .toggleClass('selected', selected)\r\n            .attr('data-memory-node-id', node.id)\r\n            .append($('<b></b>').text(node.title || node.id))\r\n            .append($('<small></small>').text(truncateText(summary, 96) || '暂无摘要'))\r\n            .append($('<div class=\"ai-wbr-memory-preview-meta\"></div>')\r\n                .append($('<span></span>').text(typeLabel))\r\n                .append($('<span></span>').text(`${score}% · ${degree.get(String(node.id)) || 0} 关系`)))\r\n            .append(temporalMeta\r\n                ? $('<div class=\"ai-wbr-memory-preview-tags\"></div>').append($('<span></span>').text(truncateText(temporalMeta, 28)))\r\n                : '')\r\n            .append(tags.length\r\n                ? $('<div class=\"ai-wbr-memory-preview-tags\"></div>').append(tags.map(tag => $('<span></span>').text(tag)))\r\n                : '');\r\n    };\r\n\r\n    panel.empty().append(\r\n        $('<div class=\"ai-wbr-memory-preview-head\"></div>')\r\n            .append($('<div></div>')\r\n                .append($('<div class=\"ai-wbr-memory-preview-kicker\"></div>').text('Memory Preview'))\r\n                .append($('<div class=\"ai-wbr-memory-preview-title\"></div>').text('记忆预览')))\r\n            .append($('<div class=\"ai-wbr-memory-preview-head-right\"></div>')\r\n                .append($('<div class=\"ai-wbr-memory-preview-count\"></div>').text(`${nodes.length}/${displayModel.totalNodes || nodes.length}`))\r\n                .append($('<button class=\"ai-wbr-memory-preview-close\" type=\"button\" aria-label=\"关闭预览\"><i class=\"fa-solid fa-xmark\"></i></button>'))),\r\n        $('<input class=\"text_pole ai-wbr-memory-preview-search\" type=\"search\" placeholder=\"搜索记忆、人物、地点、关键词\" />').val(memoryGraphSearchText || ''),\r\n    );\r\n\r\n    if (!nodes.length) {\r\n        panel.append($('<div class=\"ai-wbr-token-empty m-t-1\"></div>').text('暂无可预览的记忆。清除筛选或继续生成后会显示。'));\r\n        return;\r\n    }\r\n\r\n    for (const group of orderedGroups) {\r\n        const groupNodes = (grouped.get(group) || [])\r\n            .slice()\r\n            .sort((a, b) => getMemoryGraphNodeScore(b, degree.get(String(b.id)) || 0) - getMemoryGraphNodeScore(a, degree.get(String(a.id)) || 0))\r\n            .slice(0, 8);\r\n        if (!groupNodes.length) {\r\n            continue;\r\n        }\r\n        panel.append(\r\n            $('<section class=\"ai-wbr-memory-preview-section\"></section>')\r\n                .append($('<div class=\"ai-wbr-memory-preview-section-title\"></div>')\r\n                    .append($('<span></span>').text(getMemoryPreviewGroupLabel(group)))\r\n                    .append($('<small></small>').text(`${grouped.get(group).length}`)))\r\n                .append($('<div class=\"ai-wbr-memory-preview-list\"></div>').append(groupNodes.map(createCard))),\r\n        );\r\n    }\r\n}\r\n\r\nfunction applyMemoryGraphPreviewLayout(displayModel, canvasWidth, canvasHeight) {\r\n    const nodes = Array.isArray(displayModel?.nodes) ? displayModel.nodes : [];\r\n    const links = Array.isArray(displayModel?.links) ? displayModel.links : [];\r\n    if (!nodes.length || displayModel.mode === 'full') {\r\n        return false;\r\n    }\r\n\r\n    const isNarrow = canvasWidth < 760;\r\n    const isMedium = canvasWidth >= 760 && canvasWidth < 1040;\r\n    const padding = isNarrow ? 38 : MEMORY_GRAPH_LAYOUT_PADDING;\r\n    const top = isNarrow ? 104 : Math.max(118, padding * 1.15);\r\n    const availableWidth = Math.max(360, canvasWidth - padding * 2);\r\n    const availableHeight = Math.max(260, canvasHeight - top - padding);\r\n    const selectedId = String(memoryGraphSelectedNodeId || '');\r\n    const nodeById = new Map(nodes.map(node => [String(node.id), node]));\r\n\r\n    if (displayModel.mode === 'focus' && selectedId && nodeById.has(selectedId)) {\r\n        const center = nodeById.get(selectedId);\r\n        center.x = (canvasWidth - MEMORY_GRAPH_NODE_WIDTH) / 2;\r\n        center.y = top + (availableHeight - MEMORY_GRAPH_NODE_HEIGHT) / 2;\r\n        const neighbors = nodes.filter(node => String(node.id) !== selectedId);\r\n        const innerIds = new Set();\r\n        for (const link of links) {\r\n            if (String(link.source) === selectedId) innerIds.add(String(link.target));\r\n            if (String(link.target) === selectedId) innerIds.add(String(link.source));\r\n        }\r\n        const inner = neighbors.filter(node => innerIds.has(String(node.id)));\r\n        const outer = neighbors.filter(node => !innerIds.has(String(node.id)));\r\n        const placeRing = (items, radiusX, radiusY, startAngle = -Math.PI / 2) => {\r\n            items.forEach((node, index) => {\r\n                const angle = startAngle + (Math.PI * 2 * index / Math.max(1, items.length));\r\n                node.x = (canvasWidth / 2) + Math.cos(angle) * radiusX - MEMORY_GRAPH_NODE_WIDTH / 2;\r\n                node.y = (top + availableHeight / 2) + Math.sin(angle) * radiusY - MEMORY_GRAPH_NODE_HEIGHT / 2;\r\n                const clamped = clampMemoryNodePosition(node.x, node.y, canvasWidth, canvasHeight, top);\r\n                node.x = clamped.x;\r\n                node.y = clamped.y;\r\n            });\r\n        };\r\n        placeRing(inner, Math.min(availableWidth * (isNarrow ? 0.42 : 0.34), isNarrow ? 230 : 320), Math.min(availableHeight * 0.28, isNarrow ? 170 : 210));\r\n        placeRing(outer, Math.min(availableWidth * (isNarrow ? 0.48 : 0.46), isNarrow ? 310 : 470), Math.min(availableHeight * 0.42, isNarrow ? 250 : 320), -Math.PI / 2 + 0.22);\r\n        return true;\r\n    }\r\n\r\n    const activeGroups = Array.isArray(displayModel.categoryGroups)\r\n        ? displayModel.categoryGroups.filter(group => group.nodes.length)\r\n        : buildMemoryGraphCategoryGroups(nodes, links);\r\n    const isFocusedCategory = !!displayModel.focusedCategory;\r\n    const columns = isFocusedCategory\r\n        ? 1\r\n        : Math.min(isNarrow ? 1 : isMedium ? 2 : 3, Math.max(1, activeGroups.length));\r\n    const rows = Math.ceil(activeGroups.length / columns);\r\n    const gapX = isNarrow ? 14 : 22;\r\n    const gapY = isNarrow ? 18 : 26;\r\n    const cellWidth = (availableWidth - gapX * Math.max(0, columns - 1)) / columns;\r\n    const cellHeight = (availableHeight - gapY * Math.max(0, rows - 1)) / Math.max(1, rows);\r\n\r\n    activeGroups.forEach((groupInfo, groupIndex) => {\r\n        const col = groupIndex % columns;\r\n        const row = Math.floor(groupIndex / columns);\r\n        const originX = padding + col * (cellWidth + gapX);\r\n        const originY = top + row * (cellHeight + gapY);\r\n        const innerX = originX + (isNarrow ? 10 : 16);\r\n        const innerY = originY + (isNarrow ? 34 : 42);\r\n        const localColumns = Math.max(1, Math.floor((cellWidth - (isNarrow ? 20 : 32)) / (MEMORY_GRAPH_NODE_WIDTH + (isNarrow ? 10 : 18))));\r\n        const maxVisibleRows = isFocusedCategory ? 7 : isNarrow ? 2 : 3;\r\n        const maxVisibleItems = Math.max(1, localColumns * maxVisibleRows);\r\n        const items = groupInfo.nodes\r\n            .slice()\r\n            .sort((a, b) => getMemoryGraphNodeScore(b) - getMemoryGraphNodeScore(a))\r\n            .slice(0, maxVisibleItems);\r\n        groupInfo.hiddenInLayout = Math.max(0, groupInfo.count - items.length);\r\n        groupInfo.nodes = items;\r\n        groupInfo.layout = {\r\n            x: originX,\r\n            y: originY,\r\n            width: cellWidth,\r\n            height: cellHeight,\r\n        };\r\n        const rowGap = isFocusedCategory ? 18 : isNarrow ? 12 : 16;\r\n        items.forEach((node, index) => {\r\n            const localCol = index % localColumns;\r\n            const localRow = Math.floor(index / localColumns);\r\n            node.x = innerX + localCol * (MEMORY_GRAPH_NODE_WIDTH + (isNarrow ? 10 : 18));\r\n            node.y = innerY + localRow * (MEMORY_GRAPH_NODE_HEIGHT + rowGap);\r\n            const maxX = Math.max(innerX, originX + cellWidth - MEMORY_GRAPH_NODE_WIDTH - (isNarrow ? 10 : 16));\r\n            const maxY = Math.max(innerY, originY + cellHeight - MEMORY_GRAPH_NODE_HEIGHT - (isNarrow ? 10 : 16));\r\n            node.x = Math.min(maxX, Math.max(innerX, node.x));\r\n            node.y = Math.min(maxY, Math.max(innerY, node.y));\r\n        });\r\n    });\r\n    return true;\r\n}\r\n\r\nfunction isMemoryTimelineNode(node) {\r\n    const type = String(node?.type || '').toLowerCase();\r\n    return type === 'event' || type === 'quest' || !!node?.timeSpan;\r\n}\r\n\r\nfunction getMemoryTimelineGroupLabel(node) {\r\n    const raw = String(node?.timeSpan || '').trim();\r\n    if (raw) {\r\n        return raw;\r\n    }\r\n    const dateText = String(node?.updatedAt || node?.createdAt || '').slice(0, 10);\r\n    return dateText || '未标记时间';\r\n}\r\n\r\nfunction getMemoryTimelineSortValue(node, index = 0) {\r\n    const timeText = String(node?.timeSpan || '').trim();\r\n    const explicitNumber = Number(timeText.match(/\\d+/)?.[0] || NaN);\r\n    if (Number.isFinite(explicitNumber)) {\r\n        return explicitNumber * 100000 + index;\r\n    }\r\n    const timestamp = Date.parse(node?.updatedAt || node?.createdAt || '');\r\n    return Number.isFinite(timestamp) ? timestamp : Number.MAX_SAFE_INTEGER - index;\r\n}\r\n\r\nfunction getMemoryTimelineRelatedNodes(graph, node, typeNames = []) {\r\n    const wanted = new Set(typeNames.map(type => String(type).toLowerCase()));\r\n    const nodes = Array.isArray(graph?.nodes) ? graph.nodes : [];\r\n    const links = Array.isArray(graph?.links) ? graph.links : [];\r\n    const nodeById = new Map(nodes.map(item => [String(item.id), item]));\r\n    const related = [];\r\n    for (const link of links) {\r\n        const source = String(link?.source || '');\r\n        const target = String(link?.target || '');\r\n        const otherId = source === String(node.id) ? target : target === String(node.id) ? source : '';\r\n        if (!otherId) {\r\n            continue;\r\n        }\r\n        const other = nodeById.get(otherId);\r\n        if (!other) {\r\n            continue;\r\n        }\r\n        if (!wanted.size || wanted.has(String(other.type || '').toLowerCase())) {\r\n            related.push(other);\r\n        }\r\n    }\r\n    return related;\r\n}\r\n\r\nfunction renderMemoryGraphToolbarHtml(graph, displayModel, nodes, edges) {\n    const fullscreenLabel = memoryGraphFullscreenActive ? '退出全屏' : '全屏图谱';\n    const hiddenHint = displayModel.hiddenNodes || displayModel.hiddenLinks ? `，收起 ${displayModel.hiddenNodes}/${displayModel.hiddenLinks}` : '';\n    const linkHint = memoryGraphLinkSourceId ? ` · 起点：${escapeHtml(truncateText(graph.nodes.find(node => node.id === memoryGraphLinkSourceId)?.title || memoryGraphLinkSourceId, 8))}` : '';\n    const ruleButtonClass = memoryRuleDrawerOpen ? ' active' : '';\n    const fullscreenCloseButton = memoryGraphFullscreenActive\n        ? '<button id=\"ai_wbr_memory_graph_close_fullscreen\" class=\"ai-wbr-memory-graph-close-fullscreen\" type=\"button\" title=\"关闭图谱\">×</button>'\n        : '';\n    return `\n        <div class=\"ai-wbr-memory-graph-toolbar ai-wbr-memory-graph-toolbar-compact\">\n            ${fullscreenCloseButton}\n            <div class=\"ai-wbr-memory-graph-row ai-wbr-memory-graph-row-primary\">\n                <div class=\"ai-wbr-memory-graph-modes\">\n                    <button class=\"menu_button ai-wbr-memory-mode ${displayModel.mode === 'overview' ? 'active' : ''}\" type=\"button\" data-memory-graph-mode=\"overview\">概览</button>\n                    <button class=\"menu_button ai-wbr-memory-mode ${displayModel.mode === 'focus' ? 'active' : ''}\" type=\"button\" data-memory-graph-mode=\"focus\" ${memoryGraphSelectedNodeId ? '' : 'disabled'}>聚焦</button>\n                    <button class=\"menu_button ai-wbr-memory-mode ${displayModel.mode === 'timeline' ? 'active' : ''}\" type=\"button\" data-memory-graph-mode=\"timeline\">时间线</button>\n                    <button class=\"menu_button ai-wbr-memory-mode ${displayModel.mode === 'full' ? 'active' : ''}\" type=\"button\" data-memory-graph-mode=\"full\">全量</button>\n                </div>\n                <div class=\"ai-wbr-memory-graph-summary\">显示 ${nodes.length}/${displayModel.totalNodes} · ${edges.length}/${displayModel.totalLinks}${hiddenHint}${linkHint}</div>\n                <span class=\"ai-wbr-memory-graph-divider\"></span>\n                <div class=\"ai-wbr-memory-graph-actions\">\n                    <button id=\"ai_wbr_memory_graph_preview_toggle\" class=\"menu_button\" type=\"button\"><i class=\"fa-solid fa-list-ul\"></i> 列表</button>\n                    <button id=\"ai_wbr_memory_graph_fit\" class=\"menu_button\" type=\"button\" title=\"适配视图：把所有节点铺满画布\"><i class=\"fa-solid fa-arrows-to-circle\"></i> 适配</button>\n                    <button id=\"ai_wbr_memory_graph_fullscreen\" class=\"menu_button ai-wbr-graph-topbar-primary\" type=\"button\"><i class=\"fa-solid fa-expand\"></i> ${fullscreenLabel}</button>\n                    <button class=\"menu_button ai-wbr-memory-graph-filter-toggle\" type=\"button\" aria-expanded=\"true\" title=\"查看下方类型与关系权重筛选\"><i class=\"fa-solid fa-sliders\"></i> 筛选</button>\n                    <button id=\"ai_wbr_memory_rule_toggle\" class=\"menu_button ai-wbr-memory-rule-toggle${ruleButtonClass}\" type=\"button\" aria-expanded=\"${memoryRuleDrawerOpen ? 'true' : 'false'}\"><i class=\"fa-solid fa-wand-magic-sparkles\"></i> 规则</button>\n                    <input class=\"text_pole ai-wbr-memory-graph-search\" type=\"search\" placeholder=\"搜索\" value=\"${escapeHtml(memoryGraphSearchText)}\" />\n                </div>\n            </div>\n            <div class=\"ai-wbr-memory-graph-drawer ai-wbr-memory-graph-drawer-open\">\n                <div class=\"ai-wbr-memory-graph-drawer-body\">\n                    ${renderMemoryGraphTypeFilters(graph)}\n                    <label class=\"ai-wbr-memory-weight-filter\">关系权重≥<span>${Math.round(displayModel.minWeight * 100)}%</span><input class=\"ai-wbr-memory-link-weight\" type=\"range\" min=\"0\" max=\"1\" step=\"0.05\" value=\"${displayModel.minWeight}\" /></label>\n                    <button class=\"menu_button ai-wbr-memory-clear-filters\" type=\"button\">清除筛选</button>\n                </div>\n            </div>\n        </div>\n    `;\n}\n\r\nfunction setMemoryGraphFullscreen(enabled, options = {}) {\r\n    const section = $('#ai_wbr_memory_graph_section');\r\n    if (!section.length) {\r\n        return;\r\n    }\r\n    memoryGraphFullscreenActive = !!enabled;\r\n    section.toggleClass('ai-wbr-memory-graph-fullscreen', memoryGraphFullscreenActive);\r\n    $('body').toggleClass('ai-wbr-memory-graph-fullscreen-open', memoryGraphFullscreenActive);\r\n    $('#ai_wbr_memory_graph_fullscreen').text(memoryGraphFullscreenActive ? '退出全屏' : '全屏图谱');\r\n    if (memoryGraphFullscreenActive) {\r\n        section.closest('.ai-wbr-graph-shell').removeClass('preview-open');\r\n    }\r\n    const shouldFit = options.fit !== false;\r\n    requestAnimationFrame(() => {\r\n        if (shouldFit) {\r\n            fitMemoryGraphToContainer(getMemoryGraph());\r\n        }\r\n        renderMemoryPanel('graph');\r\n    });\r\n}\r\n\r\nfunction openMemoryGraphCanvasFullscreen() {\r\n    const openConsole = globalThis.aiWbrOpenConsole;\r\n    if (typeof openConsole === 'function' && !$('#ai_wbr_memory_graph_section').is(':visible')) {\r\n        openConsole('graph', { mode: 'floating' });\r\n    }\r\n    setTimeout(() => setMemoryGraphFullscreen(true), 80);\r\n    setTimeout(() => setMemoryGraphFullscreen(true), 240);\r\n}\r\n\r\nfunction renderMemoryTimelineView(graph, displayModel) {\r\n    const container = $('#ai_wbr_memory_graph');\r\n    const nodes = Array.isArray(displayModel?.nodes) ? displayModel.nodes : [];\r\n    const eventNodes = nodes\r\n        .filter(isMemoryTimelineNode)\r\n        .map((node, index) => ({ node, index, sortValue: getMemoryTimelineSortValue(node, index) }))\r\n        .sort((a, b) => a.sortValue - b.sortValue || a.index - b.index)\r\n        .map(item => item.node);\r\n    const edges = Array.isArray(displayModel?.links) ? displayModel.links : [];\r\n    const grouped = new Map();\r\n    for (const node of eventNodes) {\r\n        const label = getMemoryTimelineGroupLabel(node);\r\n        if (!grouped.has(label)) {\r\n            grouped.set(label, []);\r\n        }\r\n        grouped.get(label).push(node);\r\n    }\r\n\r\n    const groupsHtml = eventNodes.length\r\n        ? [...grouped.entries()].map(([label, items]) => `\r\n            <section class=\"ai-wbr-memory-timeline-group\">\r\n                <div class=\"ai-wbr-memory-timeline-group-title\">${escapeHtml(label)} <span>${items.length}</span></div>\r\n                <div class=\"ai-wbr-memory-timeline-items\">\r\n                    ${items.map((node) => {\r\n                        const people = getMemoryTimelineRelatedNodes(graph, node, ['character']).slice(0, 4);\r\n                        const locations = getMemoryTimelineRelatedNodes(graph, node, ['location']).slice(0, 2);\r\n                        const settings = getMemoryTimelineRelatedNodes(graph, node, ['item', 'concept', 'rule', 'faction']).slice(0, 4);\r\n                        const selected = String(node.id) === String(memoryGraphSelectedNodeId);\r\n                        const chips = [\r\n                            ...people.map(item => `人物：${item.title || item.id}`),\r\n                            ...locations.map(item => `地点：${item.title || item.id}`),\r\n                            ...settings.map(item => getOptionLabel(MEMORY_NODE_TYPE_OPTIONS, item.type, item.type || '设定') + `：${item.title || item.id}`),\r\n                        ].slice(0, 6);\r\n                        return `\r\n                            <button class=\"ai-wbr-memory-timeline-card${selected ? ' selected' : ''}\" type=\"button\" data-memory-node-id=\"${escapeHtml(node.id)}\">\r\n                                <span class=\"ai-wbr-memory-timeline-dot\"></span>\r\n                                <span class=\"ai-wbr-memory-timeline-card-body\">\r\n                                    <b>${escapeHtml(node.title || node.id)}</b>\r\n                                    <small>${escapeHtml(truncateText(node.summary || node.content || '暂无摘要', 160))}</small>\r\n                                    <span class=\"ai-wbr-memory-timeline-meta\">\r\n                                        ${escapeHtml(node.location || locations[0]?.title || '地点未标记')}\r\n                                        ${node.updatedAt ? ` · ${escapeHtml(String(node.updatedAt).slice(0, 10))}` : ''}\r\n                                    </span>\r\n                                    ${chips.length ? `<span class=\"ai-wbr-memory-timeline-chips\">${chips.map(chip => `<i>${escapeHtml(chip)}</i>`).join('')}</span>` : ''}\r\n                                </span>\r\n                            </button>\r\n                        `;\r\n                    }).join('')}\r\n                </div>\r\n            </section>\r\n        `).join('')\r\n        : '<div class=\"ai-wbr-token-empty ai-wbr-memory-timeline-empty\">暂无事件时间线。事件/任务节点，或带有时间字段的记忆，会显示在这里。</div>';\r\n\r\n    container.html(`\n        ${renderMemoryGraphToolbarHtml(graph, displayModel, eventNodes, edges)}\n        <div class=\"ai-wbr-memory-timeline-view\">\n            <div class=\"ai-wbr-memory-timeline-head\">\r\n                <div>\r\n                    <div class=\"ai-wbr-memory-detail-kicker\">Timeline</div>\r\n                    <h3>剧情时间线</h3>\r\n                </div>\r\n                <small>按时间、章节或更新时间排列事件节点</small>\r\n            </div>\n            <div class=\"ai-wbr-memory-timeline-rail\">${groupsHtml}</div>\n        </div>\n        ${renderMemoryRuleDrawerHtml()}\n    `);\n    bindMemoryGraphSvgInteractions();\n}\n\r\nfunction renderMemoryDetailDrawer(graph = getMemoryGraph()) {\r\n    const drawer = $('#ai_wbr_memory_detail_drawer');\r\n    if (!drawer.length) {\r\n        return;\r\n    }\r\n\r\n    const createMeta = (label, value) => $('<div class=\"ai-wbr-memory-detail-meta\"></div>')\r\n        .append($('<span></span>').text(label))\r\n        .append($('<b></b>').text(value || '未记录'));\r\n    const createChips = (items) => {\r\n        const chips = $('<div class=\"ai-wbr-memory-detail-chips\"></div>');\r\n        for (const item of uniqueStrings(items || []).slice(0, 12)) {\r\n            chips.append($('<span></span>').text(item));\r\n        }\r\n        return chips.children().length ? chips : $('<div class=\"ai-wbr-memory-detail-muted\"></div>').text('暂无');\r\n    };\r\n\r\n    const selectedLink = memoryGraphDetailMode === 'link'\r\n        ? graph.links.find(link => String(link.id) === String(memoryGraphSelectedLinkId))\r\n        : null;\r\n    if (selectedLink) {\r\n        const source = graph.nodes.find(node => node.id === selectedLink.source);\r\n        const target = graph.nodes.find(node => node.id === selectedLink.target);\r\n        drawer.empty().addClass('open').append(\r\n            $('<div class=\"ai-wbr-memory-detail-head\"></div>')\r\n                .append($('<div></div>')\r\n                    .append($('<div class=\"ai-wbr-memory-detail-kicker\"></div>').text('关系详情'))\r\n                    .append($('<h3></h3>').text(`${source?.title || selectedLink.source} → ${target?.title || selectedLink.target}`)))\r\n                .append($('<button class=\"menu_button ai-wbr-memory-detail-close\" type=\"button\">关闭</button>')),\r\n            $('<div class=\"ai-wbr-memory-detail-grid\"></div>')\r\n                .append(createMeta('关系类型', getOptionLabel(MEMORY_LINK_TYPE_OPTIONS, selectedLink.type, selectedLink.type || 'RELATED')))\r\n                .append(createMeta('权重', String(selectedLink.weight ?? 0.7)))\r\n                .append(createMeta('更新时间', selectedLink.updatedAt || graph.updatedAt || '')),\r\n            $('<div class=\"ai-wbr-memory-detail-section\"></div>')\r\n                .append($('<b></b>').text('关系说明'))\r\n                .append($('<p></p>').text(selectedLink.description || '暂无关系说明。')),\r\n            $('<div class=\"ai-wbr-memory-detail-actions\"></div>')\r\n                .append($('<button class=\"menu_button ai-wbr-memory-detail-delete-link\" type=\"button\">删除这条关系</button>').attr('data-memory-link-id', selectedLink.id)),\r\n        );\r\n        return;\r\n    }\r\n\r\n    const selectedNode = memoryGraphDetailMode === 'node'\r\n        ? graph.nodes.find(node => node.id === memoryGraphSelectedNodeId)\r\n        : null;\r\n    if (selectedNode) {\r\n        const typeLabel = getOptionLabel(MEMORY_NODE_TYPE_OPTIONS, selectedNode.type, selectedNode.type || 'event');\r\n        const kindLabel = inferMemoryGraphNodeKind(selectedNode)?.label || typeLabel;\r\n        const relatedLinks = graph.links.filter(link => link.source === selectedNode.id || link.target === selectedNode.id);\r\n        const sourceTitle = memoryGraphLinkSourceId\r\n            ? graph.nodes.find(node => node.id === memoryGraphLinkSourceId)?.title || memoryGraphLinkSourceId\r\n            : '';\r\n        const canLinkToSource = memoryGraphLinkSourceId && memoryGraphLinkSourceId !== selectedNode.id;\r\n        const hasRelatedLink = !!(canLinkToSource && graph.links.some(item => item.source === memoryGraphLinkSourceId && item.target === selectedNode.id && item.type === 'RELATED'));\r\n\r\n        const relatedList = $('<div class=\"ai-wbr-memory-detail-related\"></div>');\r\n        if (relatedLinks.length) {\r\n            for (const link of relatedLinks.slice(0, 10)) {\r\n                const otherId = link.source === selectedNode.id ? link.target : link.source;\r\n                const other = graph.nodes.find(node => node.id === otherId);\r\n                relatedList.append($('<button class=\"menu_button ai-wbr-memory-detail-related-link\" type=\"button\"></button>')\r\n                    .attr('data-memory-link-id', link.id)\r\n                    .text(`${getOptionLabel(MEMORY_LINK_TYPE_OPTIONS, link.type, link.type || 'RELATED')}：${other?.title || otherId}`));\r\n            }\r\n        } else {\r\n            relatedList.append($('<div class=\"ai-wbr-memory-detail-muted\"></div>').text('暂无关联关系。'));\r\n        }\r\n\r\n        drawer.empty().addClass('open').append(\r\n            $('<div class=\"ai-wbr-memory-detail-head\"></div>')\r\n                .append($('<div></div>')\r\n                    .append($('<div class=\"ai-wbr-memory-detail-kicker\"></div>').text(`${kindLabel} · ${typeLabel}`))\r\n                    .append($('<h3></h3>').text(selectedNode.title || selectedNode.id)))\r\n                .append($('<button class=\"menu_button ai-wbr-memory-detail-close\" type=\"button\">关闭</button>')),\r\n            $('<div class=\"ai-wbr-memory-detail-grid\"></div>')\r\n                .append(createMeta('记忆维度', kindLabel))\r\n                .append(createMeta('节点类型', typeLabel))\r\n                .append(createMeta('重要度', `${Math.round(clampNumber(selectedNode.importance, 0.5, 0, 1) * 100)}%`))\r\n                .append(createMeta('可信度', `${Math.round(clampNumber(selectedNode.credibility, 0.8, 0, 1) * 100)}%`))\r\n                .append(createMeta('地点', selectedNode.location || ''))\r\n                .append(createMeta('时间', selectedNode.timeSpan || ''))\r\n                .append(createMeta('更新时间', selectedNode.updatedAt || graph.updatedAt || '')),\r\n            $('<div class=\"ai-wbr-memory-detail-section\"></div>')\r\n                .append($('<b></b>').text('摘要'))\r\n                .append($('<p></p>').text(selectedNode.summary || selectedNode.content || '暂无摘要。')),\r\n            $('<div class=\"ai-wbr-memory-detail-section\"></div>')\r\n                .append($('<b></b>').text('详细内容'))\r\n                .append($('<p></p>').text(selectedNode.content || selectedNode.summary || '暂无详细内容。')),\r\n            $('<div class=\"ai-wbr-memory-detail-section\"></div>')\r\n                .append($('<b></b>').text('关键词'))\r\n                .append(createChips(selectedNode.keys)),\r\n            $('<div class=\"ai-wbr-memory-detail-section\"></div>')\r\n                .append($('<b></b>').text('标签'))\r\n                .append(createChips(selectedNode.tags)),\r\n            $('<div class=\"ai-wbr-memory-detail-section\"></div>')\r\n                .append($('<b></b>').text('关联关系'))\r\n                .append(relatedList),\r\n            $('<div class=\"ai-wbr-memory-detail-actions\"></div>')\r\n                .append($('<button class=\"menu_button ai-wbr-memory-detail-set-link-source\" type=\"button\">设为连线起点</button>').attr('data-memory-node-id', selectedNode.id))\r\n                .append($(`<button class=\"menu_button ai-wbr-memory-detail-link-to-source\" type=\"button\" ${canLinkToSource ? '' : 'disabled'}>${hasRelatedLink ? '取消连接到起点' : '连接到起点'}${sourceTitle ? `：${escapeHtml(truncateText(sourceTitle, 10))}` : ''}</button>`).attr('data-memory-node-id', selectedNode.id))\r\n                .append($('<button class=\"menu_button ai-wbr-memory-detail-delete-node\" type=\"button\">删除节点</button>').attr('data-memory-node-id', selectedNode.id)),\r\n        );\r\n        return;\r\n    }\r\n\r\n    drawer.removeClass('open').html('<div class=\"ai-wbr-memory-detail-empty\">点击图谱中的记忆卡片或关系线查看详情。</div>');\r\n}\r\n\r\nfunction renderMemoryGraphSvg(graph) {\r\n    const container = $('#ai_wbr_memory_graph');\r\n    if (!container.length) {\r\n        return;\r\n    }\r\n\r\n    graph = graph && typeof graph === 'object' ? graph : getDefaultMemoryGraph();\r\n    graph.nodes = Array.isArray(graph.nodes) ? graph.nodes : [];\n    graph.links = Array.isArray(graph.links) ? graph.links : [];\n    container.toggleClass('rules-open', memoryRuleDrawerOpen);\n\n    let displayModel = buildMemoryGraphDisplayModel(graph);\n    if (!displayModel.nodes.length && graph.nodes.length) {\r\n        memoryGraphSearchText = '';\r\n        memoryGraphVisibleTypes = new Set();\r\n        if (memoryGraphDisplayMode === 'search' || memoryGraphDisplayMode === 'focus') {\r\n            memoryGraphDisplayMode = 'overview';\r\n        }\r\n        displayModel = buildMemoryGraphDisplayModel(graph);\r\n    }\r\n    renderMemoryPreviewPanel(graph, displayModel);\r\n    if (displayModel.mode === 'timeline') {\r\n        renderMemoryTimelineView(graph, displayModel);\r\n        return;\r\n    }\r\n    const nodes = displayModel.nodes;\r\n    if (!nodes.length) {\r\n        container.html(`\n            ${renderMemoryGraphToolbarHtml(graph, displayModel, [], [])}\n            <div class=\"ai-wbr-memory-graph-canvas ai-wbr-memory-graph-empty\">\n                <div class=\"ai-wbr-token-empty\">暂无可显示的记忆节点。可以清除搜索/类型筛选，或生成回复后让记忆图谱继续整理。</div>\n            </div>\n            ${renderMemoryRuleDrawerHtml()}\n        `);\n        bindMemoryGraphSvgInteractions();\n        return;\n    }\r\n\r\n    const viewport = getMemoryGraphViewportMetrics(container[0]);\r\n    const width = Math.max(viewport.baseWidth, Math.round(viewport.pixelWidth * 1.3));\r\n    const height = Math.max(viewport.baseHeight, Math.round(viewport.pixelHeight * 1.3));\r\n    const positions = new Map();\r\n    const usedPreviewLayout = applyMemoryGraphPreviewLayout(displayModel, width, height);\r\n    let layoutChanged = false;\r\n    if (!usedPreviewLayout) {\r\n        layoutChanged = normalizeMemoryGraphLayout(nodes, displayModel.links, width, height);\r\n    }\r\n    const layoutVisibleIds = usedPreviewLayout && Array.isArray(displayModel.categoryGroups)\r\n        ? new Set(displayModel.categoryGroups.flatMap(group => (group.nodes || []).map(node => String(node.id || ''))))\r\n        : new Set(nodes.map(node => String(node.id || '')));\r\n    const drawableNodes = nodes.filter(node => layoutVisibleIds.has(String(node.id || '')));\r\n    drawableNodes.forEach((node) => {\r\n        positions.set(node.id, {\r\n            x: Number(node.x || 0),\r\n            y: Number(node.y || 0),\r\n        });\r\n    });\r\n\r\n    if (layoutChanged) {\r\n        saveMemoryGraph(graph, getContext(), true);\r\n    }\r\n\r\n    const edges = displayModel.links.filter(link => positions.has(String(link.source || '')) && positions.has(String(link.target || '')));\r\n    const nodeById = new Map(drawableNodes.map(node => [String(node.id || ''), node]));\r\n    const categoryPanels = Array.isArray(displayModel.categoryGroups)\r\n        ? displayModel.categoryGroups\r\n            .filter(group => group.layout && group.nodes.length)\r\n            .map((group) => {\r\n                const layout = group.layout;\r\n                const hiddenCount = Math.max(0, group.hiddenInLayout || 0);\r\n                const widthValue = Math.max(120, layout.width);\r\n                const heightValue = Math.max(72, layout.height);\r\n                const totalCount = Math.max(group.totalCount || group.count || 0, group.count || 0);\r\n                const title = `${group.label} ${group.nodes.length}${hiddenCount || totalCount > group.nodes.length ? ` / ${totalCount}` : ''}`;\r\n                const subtitle = displayModel.focusedCategory\r\n                    ? group.note\r\n                    : `${group.note}${hiddenCount ? ` · 已收起 ${hiddenCount}` : ''}`;\r\n                return `<g class=\"ai-wbr-memory-category-panel ai-wbr-memory-category-${escapeHtml(group.key)}\" data-memory-category=\"${escapeHtml(group.key)}\">\r\n                    <rect class=\"ai-wbr-memory-category-bg\" x=\"${layout.x}\" y=\"${layout.y}\" width=\"${widthValue}\" height=\"${heightValue}\" rx=\"18\" ry=\"18\"></rect>\r\n                    <text class=\"ai-wbr-memory-category-title\" x=\"${layout.x + 16}\" y=\"${layout.y + 22}\">${escapeHtml(title)}</text>\r\n                    <text class=\"ai-wbr-memory-category-note\" x=\"${layout.x + 16}\" y=\"${layout.y + 40}\">${escapeHtml(truncateText(subtitle, 34))}</text>\r\n                </g>`;\r\n            })\r\n            .join('')\r\n        : '';\r\n    const pairBuckets = new Map();\r\n    edges.forEach((link) => {\r\n        const pairKey = [String(link.source || ''), String(link.target || '')].sort().join('||');\r\n        if (!pairBuckets.has(pairKey)) {\r\n            pairBuckets.set(pairKey, []);\r\n        }\r\n        pairBuckets.get(pairKey).push(link);\r\n    });\r\n    const markerDefs = `\r\n        <defs>\r\n            <marker id=\"ai-wbr-memory-arrow\" markerWidth=\"8\" markerHeight=\"8\" refX=\"7\" refY=\"4\" orient=\"auto\" markerUnits=\"strokeWidth\">\r\n                <path d=\"M 0 0 L 8 4 L 0 8 z\" class=\"ai-wbr-memory-arrow\"></path>\r\n            </marker>\r\n        </defs>\r\n    `;\r\n    const lines = edges.map(link => {\r\n        const source = positions.get(link.source);\r\n        const target = positions.get(link.target);\r\n        if (!source || !target) {\r\n            return '';\r\n        }\r\n        const sourceNode = nodeById.get(String(link.source || ''));\r\n        const targetNode = nodeById.get(String(link.target || ''));\r\n        const crossCategory = getMemoryGraphNodeCategoryKey(sourceNode) !== getMemoryGraphNodeCategoryKey(targetNode);\r\n        const opacity = Math.max(crossCategory ? 0.14 : 0.26, Math.min(crossCategory ? 0.46 : 0.85, Number(link.weight || 0.5)));\r\n        const selectedClass = String(link.id) === String(memoryGraphSelectedLinkId) ? ' ai-wbr-memory-edge-selected' : '';\r\n        const crossClass = crossCategory ? ' ai-wbr-memory-edge-cross-category' : '';\r\n        const typeClass = ` ai-wbr-memory-edge-${escapeHtml(String(link.type || 'RELATED').toLowerCase().replace(/[^a-z0-9_-]/g, '-'))}`;\r\n        const pairKey = [String(link.source || ''), String(link.target || '')].sort().join('||');\r\n        const siblings = (pairBuckets.get(pairKey) || []).slice().sort((a, b) => String(a.id || '').localeCompare(String(b.id || '')));\r\n        const siblingIndex = siblings.findIndex(item => String(item.id || '') === String(link.id || ''));\r\n        const offsetIndex = siblingIndex - ((siblings.length - 1) / 2);\r\n        const laneOffset = siblings.length > 1 ? offsetIndex * 26 : 0;\r\n        const path = buildMemoryEdgePath(source, target, laneOffset);\r\n        const linkId = escapeHtml(String(link.id || ''));\r\n        return `\r\n            <path d=\"${path}\" class=\"ai-wbr-memory-edge-hit\" data-memory-link-id=\"${linkId}\" data-source-id=\"${escapeHtml(link.source)}\" data-target-id=\"${escapeHtml(link.target)}\"></path>\r\n            <path d=\"${path}\" class=\"ai-wbr-memory-edge${typeClass}${crossClass}${selectedClass}\" data-memory-link-id=\"${linkId}\" data-source-id=\"${escapeHtml(link.source)}\" data-target-id=\"${escapeHtml(link.target)}\" style=\"opacity:${opacity}\" marker-end=\"url(#ai-wbr-memory-arrow)\"><title>${escapeHtml(getOptionLabel(MEMORY_LINK_TYPE_OPTIONS, link.type, link.type || 'RELATED'))}</title></path>\r\n        `;\r\n    }).join('');\r\n\r\n    const cards = drawableNodes.map(node => {\r\n        const position = positions.get(node.id);\r\n        const rawType = String(node.type || 'event');\r\n        const colorClass = `ai-wbr-memory-node-${escapeHtml(rawType.toLowerCase())}`;\r\n        const typeLabel = getOptionLabel(MEMORY_NODE_TYPE_OPTIONS, rawType, rawType);\r\n        const temporalMeta = [node.location, node.timeSpan].filter(Boolean).join(' · ');\r\n        const summaryText = node.summary || node.content || '';\r\n        const titleLines = splitMemoryGraphSvgText(node.title || node.id, 15.5, 2);\r\n        const subtitleLines = splitMemoryGraphSvgText(summaryText || '暂无摘要', 21, 2);\r\n        const chips = getMemoryGraphNodePreviewChips(node, typeLabel);\r\n        const selectedClass = String(node.id) === String(memoryGraphSelectedNodeId) ? ' ai-wbr-memory-node-selected' : '';\r\n        const searchClass = displayModel.query && getMemoryGraphSearchHaystack(node).includes(displayModel.query) ? ' ai-wbr-memory-node-search-hit' : '';\r\n        const importanceLabel = `${Math.round(clampNumber(node.importance, 0.5, 0, 1) * 100)}%`;\r\n        const emphasisScore = getMemoryGraphNodeScore(node, 0, 0);\r\n        const emphasisClass = emphasisScore >= 1.2 ? ' ai-wbr-memory-node-emphasis' : '';\r\n        const clipId = `ai-wbr-memory-node-clip-${escapeHtml(String(node.id || '').replace(/[^a-zA-Z0-9_-]/g, '-'))}`;\r\n        return `<g class=\"ai-wbr-memory-node ${colorClass}${selectedClass}${searchClass}${emphasisClass}\" data-memory-node-id=\"${escapeHtml(node.id)}\" transform=\"translate(${position.x},${position.y})\">\r\n            <clipPath id=\"${clipId}\"><rect x=\"3\" y=\"3\" width=\"${MEMORY_GRAPH_NODE_WIDTH - 6}\" height=\"${MEMORY_GRAPH_NODE_HEIGHT - 6}\" rx=\"12\" ry=\"12\"></rect></clipPath>\r\n            <rect class=\"ai-wbr-memory-node-card\" x=\"0\" y=\"0\" width=\"${MEMORY_GRAPH_NODE_WIDTH}\" height=\"${MEMORY_GRAPH_NODE_HEIGHT}\" rx=\"14\" ry=\"14\"></rect>\r\n            <g clip-path=\"url(#${clipId})\">\r\n                <circle class=\"ai-wbr-memory-node-accent\" cx=\"15\" cy=\"15\" r=\"4\"></circle>\r\n                ${renderMemoryGraphSvgTextLines(titleLines, 'ai-wbr-memory-node-title', 26, 18, 13)}\r\n                ${renderMemoryGraphSvgTextLines(subtitleLines, 'ai-wbr-memory-node-subtitle-lines', 12, 44, 12.5)}\r\n                <text class=\"ai-wbr-memory-node-subtitle\" x=\"12\" y=\"48\">${escapeHtml(truncateText(summaryText, 32) || '暂无摘要')}</text>\r\n                ${renderMemoryGraphSvgChips(chips, 12, 68)}\r\n                <text class=\"ai-wbr-memory-node-score\" x=\"184\" y=\"88\">${escapeHtml(importanceLabel)}</text>\r\n            </g>\r\n            <title>${escapeHtml(`${node.title || node.id}\\n${temporalMeta ? `${temporalMeta}\\n` : ''}${node.content || node.summary || ''}`)}</title>\r\n        </g>`;\r\n    }).join('');\r\n\r\n    if (!memoryGraphView\r\n        || !Number.isFinite(memoryGraphView.x)\r\n        || !Number.isFinite(memoryGraphView.y)\r\n        || !Number.isFinite(memoryGraphView.width)\r\n        || !Number.isFinite(memoryGraphView.height)\r\n        || memoryGraphView.width < 120\r\n        || memoryGraphView.height < 80) {\r\n        fitMemoryGraphToNodes(drawableNodes, container[0]);\r\n    }\r\n    syncMemoryGraphViewToContainerAspect(container[0]);\r\n\r\n    container.html(`\n        ${renderMemoryGraphToolbarHtml(graph, displayModel, nodes, edges)}\n        <div class=\"ai-wbr-memory-graph-canvas\">\n            <svg viewBox=\"${memoryGraphView.x} ${memoryGraphView.y} ${memoryGraphView.width} ${memoryGraphView.height}\" preserveAspectRatio=\"none\" role=\"img\" aria-label=\"记忆图谱\">${markerDefs}${categoryPanels}${lines}${cards}</svg>\n        </div>\n        ${renderMemoryRuleDrawerHtml()}\n    `);\n    bindMemoryGraphSvgInteractions();\n}\n\r\nfunction renderMemoryDashboard(graph) {\r\n    const dashboard = $('#ai_wbr_memory_dashboard').empty();\r\n    if (!dashboard.length) {\r\n        return;\r\n    }\r\n\r\n    const queueCount = getMemoryReviewQueue().length;\r\n    const nodes = Array.isArray(graph?.nodes) ? graph.nodes : [];\r\n    const links = Array.isArray(graph?.links) ? graph.links : [];\r\n    const nodeTypes = nodes.reduce((acc, node) => {\r\n        const type = String(node.type || 'memory');\r\n        acc[type] = (acc[type] || 0) + 1;\r\n        return acc;\r\n    }, {});\r\n    const topType = Object.entries(nodeTypes).sort((a, b) => b[1] - a[1])[0];\r\n    const activeTopics = Array.isArray(graph?.state?.active_topics)\r\n        ? graph.state.active_topics.slice(0, 3).join('，')\r\n        : '';\r\n    const cards = [\r\n        ['记忆节点', String(nodes.length), topType ? `最多：${topType[0]} × ${topType[1]}` : '等待写入'],\r\n        ['关系边', String(links.length), graph.updatedAt ? `更新：${new Date(graph.updatedAt).toLocaleString()}` : '暂无关系'],\r\n        ['待确认', String(queueCount), queueCount ? 'AI 更新等待确认' : '没有待处理更新'],\r\n        ['当前主题', activeTopics || '未识别', graph.lastSummary ? truncateText(graph.lastSummary, 64) : '等待整理剧情'],\r\n    ];\r\n\r\n    for (const [label, value, note] of cards) {\r\n        dashboard.append(\r\n            $('<div class=\"ai-wbr-memory-metric\"></div>')\r\n                .append($('<div class=\"ai-wbr-memory-metric-label\"></div>').text(label))\r\n                .append($('<div class=\"ai-wbr-memory-metric-value\"></div>').text(value))\r\n                .append($('<div class=\"ai-wbr-memory-metric-note\"></div>').text(note)),\r\n        );\r\n    }\r\n}\r\n\r\nfunction renderMemoryReviewQueue() {\r\n    const container = $('#ai_wbr_memory_review_queue').empty();\r\n    if (!container.length) {\r\n        return;\r\n    }\r\n\r\n    const queue = getMemoryReviewQueue();\r\n    const reviewFold = $('.ai-wbr-memory-review-fold');\r\n    $('#ai_wbr_memory_review_badge').text(queue.length ? `${queue.length} 条待确认，建议先处理。` : '暂无待确认。');\r\n    reviewFold.toggleClass('ai-wbr-memory-fold-attention', queue.length > 0);\r\n    if (queue.length) {\r\n        reviewFold.prop('open', true);\r\n    }\r\n\r\n    container.append($('<div class=\"ai-wbr-memory-subtitle\"><b>待确认记忆更新</b></div>'));\r\n    if (!queue.length) {\r\n        container.append('<div class=\"ai-wbr-token-empty\">暂无待确认更新。开启“记忆更新需要确认”后，AI 整理结果会先出现在这里。</div>');\r\n        return;\r\n    }\r\n\r\n    for (const item of queue) {\r\n        const summary = item.summary || summarizeMemoryUpdateProposal(item.update);\r\n        const sourceLabel = getMemorySourceLabel(item.source);\r\n        const sourceInvalidated = item.source?.id && getInvalidatedMemorySourceIds(item.source.chatKey || getCurrentChatMemoryKey()).includes(String(item.source.id));\r\n        const isStale = item.status === 'stale' || sourceInvalidated || (item.source?.chatKey && item.source.chatKey !== getCurrentChatMemoryKey());\r\n        const chips = [\r\n            `新增 ${summary.nodes || 0}`,\r\n            `修改 ${summary.updates || 0}`,\r\n            `关系 ${summary.links || 0}`,\r\n            `状态 ${summary.stateChanges || 0}`,\r\n            sourceLabel ? `来源 ${sourceLabel}` : '',\r\n            isStale ? '已失效' : '',\r\n            summary.removes ? `删除 ${summary.removes}` : '',\r\n        ].filter(Boolean);\r\n\r\n        container.append(\r\n            $('<div class=\"ai-wbr-memory-review-card\"></div>')\r\n                .toggleClass('ai-wbr-memory-review-card-stale', isStale)\r\n                .attr('data-memory-review-id', item.id)\r\n                .append($('<div class=\"ai-wbr-memory-review-head\"></div>')\r\n                    .append($('<div></div>')\r\n                        .append($('<b></b>').text(item.title || '待确认记忆更新'))\r\n                        .append($('<small></small>').text(` ${item.reason || 'auto'} · ${item.createdAt ? new Date(item.createdAt).toLocaleString() : ''}${sourceLabel ? ` · ${sourceLabel}` : ''}${isStale ? ' · roll/swipe 后已失效' : ''}`)))\r\n                    .append($('<div class=\"ai-wbr-memory-review-actions\"></div>')\r\n                        .append($('<button class=\"menu_button ai-wbr-memory-review-accept\" type=\"button\">确认写入</button>').prop('disabled', isStale))\r\n                        .append('<button class=\"menu_button ai-wbr-memory-review-reject\" type=\"button\">忽略</button>')))\r\n                .append($('<div class=\"ai-wbr-memory-review-chips\"></div>').append(chips.map(chip => $('<span></span>').text(chip))))\r\n                .append($('<div class=\"ai-wbr-memory-review-summary\"></div>').text(item.update?.summary || '无摘要')),\r\n        );\r\n    }\r\n}\r\n\r\nfunction getBookshelfTypeLabel(type) {\n    return ({\n        character: '人物档案',\n        world: '世界观',\n        worldbook: '世界书',\n        plot: '剧情记录',\n        rule: '规则设定',\n        memory: '自动记忆书',\n        other: '其他资料',\r\n    })[String(type || 'other')] || '其他资料';\r\n}\r\n\r\nfunction getBookshelfStatusLabel(book) {\r\n    if (!book) return '未知';\r\n    if (book.enabled === false) return '已禁用';\r\n    if (book.status === 'empty') return '无片段';\r\n    if (book.status === 'vectorizing') return `向量化中 ${book.vectorizedCount || 0}/${book.chunkCount || 0}`;\r\n    if (book.status === 'failed') return '向量化失败';\r\n    if (book.status === 'partial_failed') return `部分完成 ${book.vectorizedCount || 0}/${book.chunkCount || 0}`;\r\n    if (Number(book.vectorizedCount || 0) > 0 && Number(book.vectorizedCount || 0) >= Number(book.chunkCount || 0)) return '已向量化';\r\n    return '待向量化';\r\n}\r\n\r\nfunction getBookshelfBindingLabels(book) {\n    const labels = normalizeBookshelfBindings(book).map(item => {\r\n        if (item.type === 'character') return `角色：${item.label || '当前角色'}`;\r\n        if (item.type === 'chat') return `聊天：${item.label || '当前聊天'}`;\r\n        if (item.type === 'global') return '全局';\r\n        return item.label || item.type;\r\n    });\r\n    return labels.length ? labels : ['未绑定'];\n}\n\nfunction getBookshelfLibraryBadge(book) {\n    if (!book) return '资料';\n    if (book.virtualSource === 'worldbook') return '世界书';\n    if (book.enabled === false) return '停用';\n    if (book.autoGenerated && book.autoSource === 'memory-graph') return '连载';\n    if (book.status === 'failed' || book.status === 'partial_failed') return '异常';\n    if (Number(book.vectorizedCount || 0) > 0 && Number(book.vectorizedCount || 0) >= Number(book.chunkCount || 0)) return '完成';\n    if (Number(book.vectorizedCount || 0) > 0) return '更新';\n    return '待读';\n}\n\nfunction getBookshelfUnreadLabel(book) {\n    if (book?.virtualSource === 'worldbook') {\n        return `${book.chunkCount || 0}条设定 · ${book.sourceLabel || '世界书'}`;\n    }\n    if (book?.autoGenerated && book?.autoSource === 'memory-graph') {\n        const chapterCount = Array.isArray(book.chapters) ? book.chapters.length : 0;\n        return `${chapterCount || 1}章记忆 · ${book.chunkCount || 0}节`;\n    }\n    return `${book.chunkCount || 0}章未读`;\n}\n\nfunction createBookshelfBookCard(book, selected) {\n    const isAutoMemoryBook = book?.autoGenerated && book?.autoSource === 'memory-graph';\n    const isWorldbook = book?.virtualSource === 'worldbook';\n    const modeLabel = book.embeddingMode\n        ? `${book.embeddingMode}${book.embeddingModel ? ` / ${book.embeddingModel}` : ''}${book.embeddingDim ? ` / ${book.embeddingDim}维` : ''}`\n        : (isWorldbook ? (book.enabled === false ? '已停用世界书' : '当前世界书') : '待向量化');\n    const badge = getBookshelfLibraryBadge(book);\n    const unreadLabel = getBookshelfUnreadLabel(book);\n    return $('<div class=\"ai-wbr-bookshelf-book\"></div>')\n        .toggleClass('selected', !!selected)\n        .toggleClass('ai-wbr-bookshelf-memory-book', !!isAutoMemoryBook)\n        .toggleClass('ai-wbr-bookshelf-worldbook', !!isWorldbook)\n        .attr('data-bookshelf-book-id', book.id)\n        .append(\n            $('<button class=\"ai-wbr-bookshelf-cover ai-wbr-bookshelf-select\" type=\"button\"></button>')\n                .append($('<span class=\"ai-wbr-bookshelf-cover-badge\"></span>').text(badge))\n                .append(isAutoMemoryBook ? $('<span class=\"ai-wbr-bookshelf-cover-mark\"></span>').text('记忆书') : null)\n                .append($('<b></b>').text(book.title || book.fileName || '未命名'))\n                .append($('<small></small>').text(getBookshelfTypeLabel(book.type))),\n            $('<div class=\"ai-wbr-bookshelf-book-title\"></div>').text(book.title || book.fileName || '未命名'),\n            $('<div class=\"ai-wbr-bookshelf-book-meta\"></div>').text(unreadLabel),\n            $('<div class=\"ai-wbr-bookshelf-book-provider\"></div>').text(modeLabel),\n        );\n}\n\nasync function buildBookshelfWorldbookLibrary(context = getContext()) {\n    const bindings = getActiveWorldbookBindings(context);\n    if (!bindings.length) return [];\n    const entries = await getWorldbookEntries(context).catch(() => []);\n    const entriesByWorld = new Map();\n    for (const entry of entries || []) {\n        const worldName = String(entry.world || '').trim();\n        if (!worldName) continue;\n        if (!entriesByWorld.has(worldName)) entriesByWorld.set(worldName, []);\n        entriesByWorld.get(worldName).push(entry);\n    }\n    return bindings\n        .map((binding, index) => {\n            const worldName = String(binding.worldName || '').trim();\n            if (!worldName) return null;\n            const worldEntries = entriesByWorld.get(worldName) || [];\n            const id = `worldbook_${hashString(`${binding.source}:${worldName}`)}`;\n            const enabled = getWorldSelectionState(worldName);\n            const book = {\n                id,\n                title: worldName,\n                fileName: worldName,\n                type: 'worldbook',\n                virtualSource: 'worldbook',\n                worldName,\n                sourceLabel: binding.source || '世界书',\n                enabled,\n                status: worldEntries.length ? 'ready' : 'empty',\n                chunkCount: worldEntries.length,\n                vectorizedCount: worldEntries.length,\n                sourceSummary: `${binding.source || '世界书'} · ${enabled ? '已启用' : '已停用'} · ${worldEntries.length} 条设定`,\n                updatedAt: `worldbook_${String(9999 - index).padStart(4, '0')}`,\n            };\n            const chunks = worldEntries.map((entry, entryIndex) => ({\n                id: `${id}:entry:${entry.uid || entryIndex}`,\n                bookId: id,\n                index: entryIndex,\n                order: entryIndex + 1,\n                title: entry.comment || entry.keys?.primary?.[0] || `设定 ${entryIndex + 1}`,\n                text: entry.content || '',\n                chapterTitle: entry.source || binding.source || '世界书',\n                enabled: !entry.disable && enabled,\n                vectorStatus: !entry.disable && enabled ? 'ready' : 'disabled',\n            }));\n            return { book, chunks };\n        })\n        .filter(Boolean);\n}\n\nasync function getBookshelfDisplayBook(bookId) {\n    const id = String(bookId || '');\n    const virtual = bookshelfVirtualBooks.get(id);\n    if (virtual?.book) return virtual.book;\n    return await bookshelfGet('books', id);\n}\n\nfunction closeBookshelfBookActionSheet(panel = $('#ai_wbr_bookshelf_panel').last()) {\n    panel.find('#ai_wbr_bookshelf_book_actions_sheet').removeClass('open').attr('aria-hidden', 'true');\n    panel.find('#ai_wbr_bookshelf_book_actions_backdrop').removeClass('open');\n}\n\nfunction createBookshelfBookActionContent(book) {\n    const scope = getBookshelfScope();\n    const isAutoMemoryBook = book?.autoGenerated && book?.autoSource === 'memory-graph';\n    const isWorldbook = book?.virtualSource === 'worldbook';\n    const isCharBound = !isWorldbook && isBookshelfBookBound(book, 'character', scope.characterKey);\n    const isGlobalBound = !isWorldbook && isBookshelfBookBound(book, 'global', 'global');\n    const actions = $('<div class=\"ai-wbr-bookshelf-book-action-list\"></div>')\n        .append($('<button class=\"menu_button ai-wbr-bookshelf-open-reader\" type=\"button\"></button>').attr('data-bookshelf-book-id', book.id).text('打开阅读'));\n    if (isWorldbook) {\n        actions.append(\n            $('<button class=\"menu_button ai-wbr-bookshelf-world-toggle\" type=\"button\"></button>')\n                .attr('data-world-name', book.worldName || book.title || '')\n                .text(book.enabled === false ? '启用世界书' : '停用世界书'),\n        );\n        return $('<div></div>').append(\n            $('<div class=\"ai-wbr-bookshelf-book-action-summary\"></div>').text(book.sourceSummary || '当前生效世界书'),\n            actions,\n        );\n    }\n    actions\n        .append($('<button class=\"menu_button ai-wbr-bookshelf-rebuild\" type=\"button\"></button>').attr('data-bookshelf-book-id', book.id).text(Number(book.vectorizedCount || 0) ? '重新向量化' : '开始向量化'))\n        .append($('<button class=\"menu_button ai-wbr-bookshelf-toggle\" type=\"button\"></button>').attr('data-bookshelf-book-id', book.id).text(book.enabled === false ? '启用召回' : '停用召回'));\n    if (!isAutoMemoryBook) {\n        actions\n            .append($('<button class=\"menu_button ai-wbr-bookshelf-bind-character\" type=\"button\"></button>').attr('data-bookshelf-book-id', book.id).text(isCharBound ? '取消角色卡绑定' : '绑定当前角色卡'))\n            .append($('<button class=\"menu_button ai-wbr-bookshelf-bind-global\" type=\"button\"></button>').attr('data-bookshelf-book-id', book.id).text(isGlobalBound ? '取消全局' : '设为全局'))\n            .append($('<button class=\"menu_button ai-wbr-bookshelf-clear-bindings\" type=\"button\"></button>').attr('data-bookshelf-book-id', book.id).text('清空绑定'));\n    }\n    actions\n        .append($('<button class=\"menu_button ai-wbr-bookshelf-clear-vector\" type=\"button\"></button>').attr('data-bookshelf-book-id', book.id).text('重置向量'))\n        .append($('<button class=\"menu_button ai-wbr-bookshelf-delete danger\" type=\"button\"></button>').attr('data-bookshelf-book-id', book.id).text('删除书籍'));\n    return $('<div></div>').append(\n        $('<div class=\"ai-wbr-bookshelf-book-action-summary\"></div>').text(`${getBookshelfTypeLabel(book.type)} · ${getBookshelfStatusLabel(book)} · ${book.vectorizedCount || 0}/${book.chunkCount || 0}`),\n        $('<div class=\"ai-wbr-bookshelf-tags\"></div>').append(\n            getBookshelfBindingLabels(book).map(label => $('<span></span>').text(label)),\n            isAutoMemoryBook ? $('<span></span>').text('单聊天记录绑定') : null,\n        ),\n        actions,\n    );\n}\n\nasync function openBookshelfBookActionSheet(bookId) {\n    const panel = $('#ai_wbr_bookshelf_panel:visible').last().length\n        ? $('#ai_wbr_bookshelf_panel:visible').last()\n        : $('#ai_wbr_bookshelf_panel').last();\n    const book = await getBookshelfDisplayBook(bookId);\n    if (!panel.length || !book) return;\n    selectedBookshelfBookId = String(book.id || bookId || '');\n    panel.find('#ai_wbr_bookshelf_book_actions_title').text(book.title || book.fileName || '书籍设置');\n    panel.find('#ai_wbr_bookshelf_book_actions_meta').text(book.virtualSource === 'worldbook' ? '世界书设置' : '当前书籍设置');\n    panel.find('#ai_wbr_bookshelf_book_actions_body').empty().append(createBookshelfBookActionContent(book));\n    panel.find('#ai_wbr_bookshelf_book_actions_sheet').addClass('open').attr('aria-hidden', 'false');\n    panel.find('#ai_wbr_bookshelf_book_actions_backdrop').addClass('open');\n}\n\nfunction renderBookshelfResults(container, results = []) {\n    container.empty();\r\n    if (!results.length) {\r\n        container.append('<div class=\"ai-wbr-token-empty\">暂无召回结果。输入一句话后点“测试召回”。</div>');\r\n        return;\r\n    }\r\n    for (const item of results) {\r\n        const isMemoryVector = item.source === 'memory-vector';\r\n        const title = isMemoryVector\r\n            ? `图谱记忆 / ${item.comment || item.title || item.uid || '记忆节点'}`\r\n            : `《${item.book?.title || item.book?.fileName || '书架'}》 / ${item.title || `片段 ${item.order || ''}`}`;\r\n        container.append(\r\n            $('<div class=\"ai-wbr-bookshelf-result\"></div>')\r\n                .append($('<div class=\"ai-wbr-bookshelf-result-head\"></div>')\r\n                    .append($('<b></b>').text(title))\r\n                    .append($('<span></span>').text(`分数 ${Number(item.score || 0).toFixed(2)}`)))\r\n                .append($('<small></small>').text(`语义 ${Number(item.semanticScore || 0).toFixed(2)} · 关键词 ${Number(item.keywordScore || 0).toFixed(2)}`))\r\n                .append($('<p></p>').text(truncateText(item.text || item.content || '', 420))),\r\n        );\r\n    }\r\n}\r\n\r\nfunction buildBookshelfNovelParts(book, chunks = []) {\r\n    const selectedChunks = (chunks || []).sort((a, b) => Number(a.order || 0) - Number(b.order || 0));\r\n    const chapters = [];\r\n    const chapterSeen = new Set();\r\n    for (const chapter of Array.isArray(book?.chapters) ? book.chapters : []) {\r\n        const title = String(chapter?.title || '').trim();\r\n        if (title && !chapterSeen.has(title)) {\r\n            chapterSeen.add(title);\r\n            chapters.push({ title, count: Number(chapter.count || 0) });\r\n        }\r\n    }\r\n    for (const chunk of selectedChunks) {\r\n        const title = String(chunk.chapterTitle || '正文目录').trim();\r\n        if (!chapterSeen.has(title)) {\r\n            chapterSeen.add(title);\r\n            chapters.push({ title, count: selectedChunks.filter(item => String(item.chapterTitle || '正文目录') === title).length });\r\n        }\r\n    }\r\n    return {\r\n        chunks: selectedChunks,\r\n        chapters: chapters.length ? chapters : [{ title: '正文目录', count: selectedChunks.length }],\r\n    };\r\n}\r\n\r\nfunction createBookshelfNovelReader(book, chunks = []) {\n    const { chapters, chunks: selectedChunks } = buildBookshelfNovelParts(book, chunks);\n    const firstChapterTitle = chapters[0]?.title || '正文目录';\n    const readerLabel = book?.virtualSource === 'worldbook'\n        ? '世界书'\n        : (book?.autoGenerated && book?.autoSource === 'memory-graph' ? '记忆书' : 'TXT');\n    const overlay = $('<div class=\"ai-wbr-bookshelf-reader-overlay\" role=\"dialog\" aria-modal=\"true\"></div>');\n    const pages = $('<main class=\"ai-wbr-bookshelf-reader-pages\"></main>').append(\n        selectedChunks.slice(0, 160).map(chunk => (\n            $('<article class=\"ai-wbr-bookshelf-reader-page\"></article>')\n                .toggleClass('disabled', chunk.enabled === false)\r\n                .attr('data-bookshelf-chapter-title', chunk.chapterTitle || '正文目录')\r\n                .append($('<div class=\"ai-wbr-bookshelf-reader-page-head\"></div>')\r\n                    .append($('<b></b>').text(chunk.title || `第 ${chunk.order || ''} 节`))\r\n                    .append($('<span></span>').text(chunk.vectorStatus === 'ready' ? '已向量化' : chunk.vectorStatus === 'failed' ? '向量失败' : '待向量化')))\r\n                .append($('<p></p>').text(chunk.text || ''))\r\n        )),\r\n    );\r\n    overlay.append(\n        $('<div class=\"ai-wbr-bookshelf-reader-shell\"></div>')\n            .append(\n                $('<header class=\"ai-wbr-bookshelf-reader-top\"></header>')\n                    .append(\n                        $('<button class=\"ai-wbr-bookshelf-reader-back ai-wbr-bookshelf-reader-close\" type=\"button\"></button>').text('‹'),\n                        $('<div class=\"ai-wbr-bookshelf-reader-titlebar\"></div>')\n                            .append($('<b></b>').text(book.title || book.fileName || '记忆书'))\n                            .append($('<small></small>').text(`${chapters.length} 章 · ${selectedChunks.length} 小节`)),\n                    )\n                    .append(\n                        $('<div class=\"ai-wbr-bookshelf-reader-menu-wrap\"></div>')\n                            .append($('<button class=\"ai-wbr-bookshelf-reader-more\" type=\"button\" title=\"阅读设置\">•••</button>'))\n                            .append(\n                                $('<div class=\"ai-wbr-bookshelf-reader-menu\"></div>')\n                                    .append($('<button class=\"ai-wbr-bookshelf-reader-menu-action\" type=\"button\" data-reader-action=\"toc\"></button>').text('查看目录'))\n                                    .append($('<button class=\"ai-wbr-bookshelf-reader-menu-action\" type=\"button\" data-reader-action=\"content\"></button>').text('继续阅读'))\n                                    .append($('<button class=\"ai-wbr-bookshelf-reader-menu-action ai-wbr-bookshelf-reader-close\" type=\"button\" data-reader-action=\"close\"></button>').text('退出阅读')),\n                            ),\n                    ),\n                $('<section class=\"ai-wbr-bookshelf-reader-hero\"></section>')\n                    .append(\n                        $('<div class=\"ai-wbr-bookshelf-reader-cover\"></div>')\n                            .append($('<span></span>').text(readerLabel))\n                            .append($('<strong></strong>').text(book.title || '记忆书'))\n                            .append($('<small></small>').text(`${book.vectorizedCount || 0}/${book.chunkCount || 0} 已向量化`)),\r\n                        $('<div class=\"ai-wbr-bookshelf-reader-intro\"></div>')\r\n                            .append($('<h3></h3>').text(book.title || '记忆书'))\n                            .append($('<p></p>').text(book.sourceSummary || '从当前聊天图谱自动整理，按小说目录阅读，也可参与向量召回。'))\n                            .append($('<div class=\"ai-wbr-bookshelf-novel-stats\"></div>')\n                                .append($('<span></span>').text(`章节 ${chapters.length}`))\n                                .append($('<span></span>').text(`小节 ${selectedChunks.length}`))\n                                .append($('<span></span>').text(`向量 ${book.vectorizedCount || 0}/${book.chunkCount || 0}`))),\n                    ),\n                $('<div class=\"ai-wbr-bookshelf-reader-current\"></div>').text(firstChapterTitle),\n                $('<section class=\"ai-wbr-bookshelf-reader-body\"></section>')\n                    .append(\n                        $('<aside class=\"ai-wbr-bookshelf-reader-toc\"></aside>')\n                            .append($('<b></b>').text('目录'))\n                            .append(chapters.map((chapter, index) => (\r\n                                $('<button class=\"ai-wbr-bookshelf-reader-toc-item\" type=\"button\"></button>')\r\n                                    .attr('data-bookshelf-chapter-title', chapter.title)\r\n                                    .toggleClass('active', index === 0)\r\n                                    .append($('<span></span>').text(chapter.title))\r\n                                    .append($('<small></small>').text(`${chapter.count || 0} 节`))\r\n                            ))),\r\n                        pages,\r\n                    ),\r\n            ),\r\n    );\r\n    return overlay;\r\n}\r\n\r\nasync function openBookshelfNovelReader(bookId) {\n    const virtual = bookshelfVirtualBooks.get(String(bookId || ''));\n    if (virtual?.book) {\n        $('.ai-wbr-bookshelf-reader-overlay').remove();\n        $('body').append(createBookshelfNovelReader(virtual.book, virtual.chunks || []));\n        return;\n    }\n    const [book, chunks] = await Promise.all([\n        bookshelfGet('books', bookId),\n        bookshelfGetAll('chunks'),\n    ]);\r\n    if (!book) return;\r\n    $('.ai-wbr-bookshelf-reader-overlay').remove();\r\n    const reader = createBookshelfNovelReader(book, (chunks || []).filter(chunk => chunk.bookId === bookId));\r\n    $('body').append(reader);\r\n}\r\n\r\nasync function renderBookshelfPanel() {\r\n    if (!$('#ai_wbr_bookshelf_panel').length) {\r\n        ensureBookshelfStandaloneSection();\r\n    }\r\n    const panel = $('#ai_wbr_bookshelf_panel:visible').last().length\r\n        ? $('#ai_wbr_bookshelf_panel:visible').last()\r\n        : $('#ai_wbr_bookshelf_panel').last();\r\n    if (!panel.length) return;\r\n    ensureBookshelfStandaloneControls(panel.closest('#ai_wbr_bookshelf_section, .ai-wbr-bookshelf-fold'));\r\n\r\n    const scope = getBookshelfScope();\r\n    panel.find('#ai_wbr_bookshelf_enabled').prop('checked', !!settings.bookshelfEnabled);\n    panel.find('#ai_wbr_bookshelf_auto_inject').prop('checked', !!settings.bookshelfAutoInject);\n    panel.find('#ai_wbr_bookshelf_auto_memory_book').prop('checked', !!settings.bookshelfAutoMemoryBook);\n    panel.find('#ai_wbr_bookshelf_auto_memory_book_vectorize').prop('checked', !!settings.bookshelfAutoMemoryBookVectorize);\n    panel.find('#ai_wbr_bookshelf_memory_vector').prop('checked', !!settings.bookshelfMemoryVectorRecall);\n    panel.find('#ai_wbr_bookshelf_only_bound').prop('checked', !!settings.bookshelfOnlyBound);\r\n    panel.find('#ai_wbr_bookshelf_allow_global').prop('checked', !!settings.bookshelfAllowGlobal);\r\n    panel.find('#ai_wbr_bookshelf_memory_vector_max').val(settings.bookshelfMemoryVectorMaxItems);\r\n    panel.find('#ai_wbr_bookshelf_max_chunks').val(settings.bookshelfMaxChunks);\r\n    panel.find('#ai_wbr_bookshelf_max_chars').val(settings.bookshelfMaxChunkChars);\r\n    panel.find('#ai_wbr_bookshelf_min_score').val(settings.bookshelfMinScore);\r\n    panel.find('#ai_wbr_bookshelf_api_url').val(settings.bookshelfApiUrl || '');\r\n    panel.find('#ai_wbr_bookshelf_api_key').val(settings.bookshelfApiKey || '');\r\n    renderBookshelfApiModelOptions();\r\n    panel.find('#ai_wbr_bookshelf_api_model').val(settings.bookshelfApiModel || '');\r\n    panel.find('#ai_wbr_bookshelf_local_model').val(settings.bookshelfLocalModelId || defaultSettings.bookshelfLocalModelId);\r\n    panel.find('#ai_wbr_bookshelf_embedding_mode').val(getBookshelfEmbeddingMode());\r\n    panel.find('#ai_wbr_bookshelf_test_query').val(settings.bookshelfLastTestQuery || '');\r\n    setBookshelfStatus('Ready');\r\n\r\n    panel.find('#ai_wbr_bookshelf_scope').empty().append(\r\n        $('<div class=\"ai-wbr-bookshelf-scope-card\"></div>').append($('<b></b>').text('当前角色卡'), $('<span></span>').text(scope.characterName || '当前角色')),\r\n        $('<div class=\"ai-wbr-bookshelf-scope-card\"></div>').append($('<b></b>').text('当前聊天'), $('<span></span>').text(scope.chatName || '当前聊天')),\r\n        $('<div class=\"ai-wbr-bookshelf-scope-card\"></div>').append($('<b></b>').text('自动注入'), $('<span></span>').text(settings.bookshelfEnabled && settings.bookshelfAutoInject ? '已开启' : '关闭')),\n        $('<div class=\"ai-wbr-bookshelf-scope-card\"></div>').append($('<b></b>').text('记忆书'), $('<span></span>').text(settings.bookshelfAutoMemoryBook ? '自动维护当前聊天' : '手动刷新')),\n        $('<div class=\"ai-wbr-bookshelf-scope-card\"></div>').append($('<b></b>').text('记忆书向量'), $('<span></span>').text(settings.bookshelfAutoMemoryBookVectorize ? '生成后自动向量化' : '手动向量化')),\n        $('<div class=\"ai-wbr-bookshelf-scope-card\"></div>').append($('<b></b>').text('图谱向量'), $('<span></span>').text(settings.bookshelfMemoryVectorRecall ? `开启 · 最多 ${settings.bookshelfMemoryVectorMaxItems || defaultSettings.bookshelfMemoryVectorMaxItems} 条` : '关闭')),\n        $('<div class=\"ai-wbr-bookshelf-scope-card\"></div>').append($('<b></b>').text('向量模型'), $('<span></span>').text(`${getBookshelfEmbeddingProviderMeta().label}${settings.bookshelfApiModel ? ` · ${settings.bookshelfApiModel}` : ''}`)),\n    );\r\n\r\n    const booksBox = panel.find('#ai_wbr_bookshelf_books').empty().append('<div class=\"ai-wbr-token-empty\">正在读取书架...</div>');\r\n    const detailBox = panel.find('#ai_wbr_bookshelf_detail').empty();\r\n    const resultsBox = panel.find('#ai_wbr_bookshelf_results');\r\n\r\n    try {\n        const [books, chunks] = await Promise.all([\n            bookshelfGetAll('books'),\n            bookshelfGetAll('chunks'),\n        ]);\n        bookshelfVirtualBooks = new Map();\n        const localBooks = (books || [])\n            .filter(book => !(book?.autoGenerated && book?.autoSource === 'memory-graph' && book?.autoChatKey !== scope.chatKey))\n            .filter(book => book?.virtualSource !== 'worldbook' && book?.type !== 'worldbook')\n            .sort((a, b) => String(b.updatedAt || '').localeCompare(String(a.updatedAt || '')));\n        const allBooks = localBooks;\n\n        if (selectedBookshelfBookId && !allBooks.some(book => book.id === selectedBookshelfBookId)) {\n            selectedBookshelfBookId = '';\n        }\n\n        detailBox.removeClass('open').empty();\n        booksBox.empty();\n        if (!allBooks.length) {\n            booksBox.append(\n                $('<div class=\"ai-wbr-bookshelf-empty\"></div>')\n                    .append($('<b></b>').text('书架还是空的'))\n                    .append($('<span></span>').text('导入 TXT 后会像小说书籍一样显示在这里。'))\n                    .append($('<button id=\"ai_wbr_bookshelf_empty_import\" class=\"ai-wbr-bookshelf-import-button\" type=\"button\">＋ 导入TXT</button>')),\n            );\n        } else {\n            for (const book of allBooks) {\n                booksBox.append(createBookshelfBookCard(book, book.id === selectedBookshelfBookId));\n            }\n        }\n\n        renderBookshelfResults(resultsBox, bookshelfLastTestResults);\n        syncBookshelfProviderVisibility();\n        setBookshelfStatus(bookshelfLastStatus || `书架共 ${allBooks.length} 本；点击阅读，长按管理。`);\n    } catch (error) {\r\n        booksBox.empty().append($('<div class=\"ai-wbr-console-alert error\"></div>').text(error?.message || String(error)));\r\n        setBookshelfStatus(`书架读取失败：${error?.message || error}`);\r\n    }\r\n}\r\n\r\nfunction getMemoryGraphSvgPoint(svg, clientX, clientY) {\r\n    const point = svg.createSVGPoint();\r\n    point.x = clientX;\r\n    point.y = clientY;\r\n    return point.matrixTransform(svg.getScreenCTM().inverse());\r\n}\r\n\r\nfunction updateMemoryGraphViewBox(svg) {\r\n    if (!svg || !memoryGraphView) {\r\n        return;\r\n    }\r\n    svg.setAttribute('viewBox', `${memoryGraphView.x} ${memoryGraphView.y} ${memoryGraphView.width} ${memoryGraphView.height}`);\r\n}\r\n\r\nfunction zoomMemoryGraphAt(svg, clientX, clientY, factor) {\r\n    if (!svg || !memoryGraphView || !Number.isFinite(factor) || factor <= 0) {\r\n        return;\r\n    }\r\n    const svgPoint = getMemoryGraphSvgPoint(svg, clientX, clientY);\r\n    const nextWidth = Math.min(2400, Math.max(80, memoryGraphView.width * factor));\r\n    const nextHeight = Math.min(1900, Math.max(60, memoryGraphView.height * factor));\r\n    // At the zoom floor/ceiling, stop the zoom entirely instead of re-anchoring,\r\n    // which otherwise drifts the view when the finger keeps moving past the limit.\r\n    const atWidthLimit = (factor < 1 && nextWidth >= memoryGraphView.width) || (factor > 1 && nextWidth <= memoryGraphView.width);\r\n    const atHeightLimit = (factor < 1 && nextHeight >= memoryGraphView.height) || (factor > 1 && nextHeight <= memoryGraphView.height);\r\n    if (atWidthLimit && atHeightLimit) {\r\n        return;\r\n    }\r\n    const ratioX = (svgPoint.x - memoryGraphView.x) / memoryGraphView.width;\r\n    const ratioY = (svgPoint.y - memoryGraphView.y) / memoryGraphView.height;\r\n    memoryGraphView = {\r\n        x: svgPoint.x - (nextWidth * ratioX),\r\n        y: svgPoint.y - (nextHeight * ratioY),\r\n        width: nextWidth,\r\n        height: nextHeight,\r\n    };\r\n    updateMemoryGraphViewBox(svg);\r\n}\r\n\r\nfunction getMemoryGraphViewportMetrics(containerEl) {\r\n    const rect = containerEl?.getBoundingClientRect?.();\r\n    const parentRect = containerEl?.parentElement?.getBoundingClientRect?.();\r\n    const fallbackWidth = Math.max(320, Number(window.innerWidth || 960) - 420);\r\n    const fallbackHeight = Math.max(320, Number(window.innerHeight || 720) - 150);\r\n    const pixelWidth = Math.max(320, Number(rect?.width || parentRect?.width || fallbackWidth));\r\n    const pixelHeight = Math.max(260, Number(rect?.height || parentRect?.height || fallbackHeight));\r\n    const aspect = pixelWidth / pixelHeight;\r\n    const baseWidth = MEMORY_GRAPH_CANVAS_WIDTH;\r\n    const baseHeight = Math.max(MEMORY_GRAPH_CANVAS_HEIGHT, Math.min(980, MEMORY_GRAPH_CANVAS_WIDTH / aspect));\r\n    return {\r\n        aspect,\r\n        baseWidth,\r\n        baseHeight,\r\n        layoutWidth: baseWidth,\r\n        layoutHeight: baseHeight,\r\n        pixelWidth,\r\n        pixelHeight,\r\n    };\r\n}\r\n\r\nfunction getMemoryGraphNodesBounds(nodes) {\r\n    const valid = (Array.isArray(nodes) ? nodes : [])\r\n        .map(node => ({\r\n            x: Number(node?.x),\r\n            y: Number(node?.y),\r\n        }))\r\n        .filter(point => Number.isFinite(point.x) && Number.isFinite(point.y));\r\n    if (!valid.length) {\r\n        return null;\r\n    }\r\n\r\n    const minX = Math.min(...valid.map(point => point.x));\r\n    const minY = Math.min(...valid.map(point => point.y));\r\n    const maxX = Math.max(...valid.map(point => point.x + MEMORY_GRAPH_NODE_WIDTH));\r\n    const maxY = Math.max(...valid.map(point => point.y + MEMORY_GRAPH_NODE_HEIGHT));\r\n    return {\r\n        x: minX,\r\n        y: minY,\r\n        width: Math.max(1, maxX - minX),\r\n        height: Math.max(1, maxY - minY),\r\n    };\r\n}\r\n\r\nfunction fitMemoryGraphToNodes(nodes, containerEl) {\r\n    const viewport = getMemoryGraphViewportMetrics(containerEl);\r\n    const bounds = getMemoryGraphNodesBounds(nodes);\r\n    if (!bounds) {\r\n        memoryGraphView = { x: 0, y: 0, width: viewport.baseWidth, height: viewport.baseHeight };\r\n        return;\r\n    }\r\n\r\n    const padding = MEMORY_GRAPH_SAFE_PADDING;\r\n    let viewWidth = Math.max(bounds.width + padding * 2, MEMORY_GRAPH_NODE_WIDTH);\r\n    let viewHeight = Math.max(bounds.height + padding * 2, MEMORY_GRAPH_NODE_HEIGHT);\r\n\r\n    // Match the container aspect so the canvas fills edge-to-edge without\r\n    // letterboxing. Grow the shorter axis rather than shrinking the bounds,\r\n    // so dense graphs keep their cards large instead of being zoomed out.\r\n    const currentAspect = viewWidth / viewHeight;\r\n    if (currentAspect < viewport.aspect) {\r\n        viewWidth = viewHeight * viewport.aspect;\r\n    } else if (currentAspect > viewport.aspect) {\r\n        viewHeight = viewWidth / viewport.aspect;\r\n    }\r\n\r\n    viewWidth = Math.min(2200, Math.max(260, viewWidth));\r\n    viewHeight = Math.min(1800, Math.max(180, viewHeight));\r\n    const centerX = bounds.x + bounds.width / 2;\r\n    const centerY = bounds.y + bounds.height / 2;\r\n    memoryGraphView = {\r\n        x: centerX - viewWidth / 2,\r\n        y: centerY - viewHeight / 2,\r\n        width: viewWidth,\r\n        height: viewHeight,\r\n    };\r\n}\r\n\r\nfunction fitMemoryGraphToContainer(graph = getMemoryGraph()) {\r\n    const container = $('#ai_wbr_memory_graph');\r\n    if (!container.length) {\r\n        return;\r\n    }\r\n    const nodes = buildMemoryGraphDisplayModel(graph).nodes;\r\n    fitMemoryGraphToNodes(nodes, container[0]);\r\n}\r\n\r\nfunction normalizeMemoryGraphLayout(nodes, links = [], canvasWidth = MEMORY_GRAPH_CANVAS_WIDTH, canvasHeight = MEMORY_GRAPH_CANVAS_HEIGHT) {\r\n    if (!Array.isArray(nodes) || nodes.length === 0) {\r\n        return false;\r\n    }\r\n\r\n    const nodeIds = new Set(nodes.map(node => node.id));\r\n    const degree = new Map(nodes.map(node => [node.id, 0]));\r\n    for (const link of Array.isArray(links) ? links : []) {\r\n        if (!nodeIds.has(link?.source) || !nodeIds.has(link?.target)) {\r\n            continue;\r\n        }\r\n        const weight = clampNumber(link.weight, 0.7, 0, 1);\r\n        degree.set(link.source, (degree.get(link.source) || 0) + weight);\r\n        degree.set(link.target, (degree.get(link.target) || 0) + weight);\r\n    }\r\n\r\n    const isValidPosition = node => {\r\n        const x = Number(node?.x);\r\n        const y = Number(node?.y);\r\n        return Number.isFinite(x)\r\n            && Number.isFinite(y)\r\n            && x > -canvasWidth * 0.35\r\n            && y > -canvasHeight * 0.35\r\n            && x < canvasWidth * 1.35\r\n            && y < canvasHeight * 1.35;\r\n    };\r\n\r\n    const bounds = getMemoryGraphNodesBounds(nodes);\r\n    const invalidLayout = nodes.some(node => !isValidPosition(node));\r\n    const overextendedLayout = bounds\r\n        ? bounds.width > canvasWidth * 1.35\r\n            || bounds.height > canvasHeight * 1.35\r\n            || bounds.x < -canvasWidth * 0.25\r\n            || bounds.y < -canvasHeight * 0.25\r\n        : true;\r\n    const verticalStack = bounds && nodes.length > 3\r\n        && bounds.width < MEMORY_GRAPH_NODE_WIDTH * 1.85\r\n        && bounds.height > MEMORY_GRAPH_NODE_HEIGHT * 3.2;\r\n    const positions = new Map(nodes\r\n        .filter(node => Number.isFinite(Number(node.x)) && Number.isFinite(Number(node.y)))\r\n        .map(node => [node.id, { x: Number(node.x), y: Number(node.y) }]));\r\n    const visibleLinks = (Array.isArray(links) ? links : [])\r\n        .filter(link => positions.has(link?.source) && positions.has(link?.target));\r\n    const longVerticalLinks = visibleLinks.filter((link) => {\r\n        const source = positions.get(link.source);\r\n        const target = positions.get(link.target);\r\n        const dx = Math.abs((target.x + MEMORY_GRAPH_NODE_WIDTH / 2) - (source.x + MEMORY_GRAPH_NODE_WIDTH / 2));\r\n        const dy = Math.abs((target.y + MEMORY_GRAPH_NODE_HEIGHT / 2) - (source.y + MEMORY_GRAPH_NODE_HEIGHT / 2));\r\n        return dy > MEMORY_GRAPH_NODE_HEIGHT * 2.4 && dx < MEMORY_GRAPH_NODE_WIDTH * 0.72;\r\n    });\r\n    const awkwardConnections = visibleLinks.length >= 2\r\n        && longVerticalLinks.length / visibleLinks.length >= 0.45;\r\n\r\n    if (!invalidLayout && !overextendedLayout && !verticalStack && !awkwardConnections) {\r\n        return false;\r\n    }\r\n\r\n    const temporalGroupOrder = new Map();\r\n    for (const node of nodes) {\r\n        const key = getMemoryTemporalLayoutKey(node);\r\n        if (key && !temporalGroupOrder.has(key)) {\r\n            temporalGroupOrder.set(key, temporalGroupOrder.size);\r\n        }\r\n    }\r\n    const ranked = nodes.map((node, index) => {\r\n        const temporalKey = getMemoryTemporalLayoutKey(node);\r\n        return {\r\n            node,\r\n            index,\r\n            temporalOrder: temporalKey ? temporalGroupOrder.get(temporalKey) : -1,\r\n            score: (degree.get(node.id) || 0) + clampNumber(node.importance, 0.5, 0, 1) + ((nodes.length - index) / 1000),\r\n        };\r\n    }).sort((a, b) => {\r\n        if (a.temporalOrder !== b.temporalOrder) {\r\n            if (a.temporalOrder < 0) return 1;\r\n            if (b.temporalOrder < 0) return -1;\r\n            return a.temporalOrder - b.temporalOrder;\r\n        }\r\n        return (b.score - a.score) || (a.index - b.index);\r\n    });\r\n    const centerX = canvasWidth / 2;\r\n    const centerY = canvasHeight / 2;\r\n    const maxPerRing = 8;\r\n\r\n    ranked.forEach((item, index) => {\r\n        let x;\r\n        let y;\r\n        if (index === 0) {\r\n            x = centerX - MEMORY_GRAPH_NODE_WIDTH / 2;\r\n            y = centerY - MEMORY_GRAPH_NODE_HEIGHT / 2;\r\n        } else {\r\n            const ringIndex = Math.floor((index - 1) / maxPerRing);\r\n            const ringStart = 1 + ringIndex * maxPerRing;\r\n            const ringCount = Math.min(maxPerRing, ranked.length - ringStart);\r\n            const indexInRing = index - ringStart;\r\n            const angle = (-Math.PI / 2) + (Math.PI * 2 * indexInRing / Math.max(1, ringCount)) + (ringIndex * 0.28);\r\n            const radiusX = Math.max(MEMORY_GRAPH_NODE_WIDTH * 0.8, canvasWidth * (0.36 + ringIndex * 0.16));\r\n            const radiusY = Math.max(MEMORY_GRAPH_NODE_HEIGHT * 0.8, canvasHeight * (0.34 + ringIndex * 0.14));\r\n            x = centerX + Math.cos(angle) * radiusX - MEMORY_GRAPH_NODE_WIDTH / 2;\r\n            y = centerY + Math.sin(angle) * radiusY - MEMORY_GRAPH_NODE_HEIGHT / 2;\r\n        }\r\n\r\n        const clamped = clampMemoryNodePosition(x, y, canvasWidth, canvasHeight, MEMORY_GRAPH_SAFE_PADDING);\r\n        item.node.x = clamped.x;\r\n        item.node.y = clamped.y;\r\n    });\r\n    return true;\r\n}\r\n\r\nfunction syncMemoryGraphViewToContainerAspect(containerEl) {\r\n    if (!memoryGraphView || !Number.isFinite(memoryGraphView.width)) {\r\n        return;\r\n    }\r\n    const { aspect } = getMemoryGraphViewportMetrics(containerEl);\r\n    const nextHeight = Math.max(120, memoryGraphView.width / Math.max(0.2, aspect));\r\n    if (!Number.isFinite(nextHeight) || Math.abs(nextHeight - memoryGraphView.height) < 0.5) {\r\n        return;\r\n    }\r\n    const centerY = memoryGraphView.y + (memoryGraphView.height / 2);\r\n    memoryGraphView.height = nextHeight;\r\n    memoryGraphView.y = centerY - (nextHeight / 2);\r\n}\r\n\r\nfunction parseMemoryNodeTransform(transform) {\r\n    const text = String(transform || '');\r\n    const match = text.match(/translate\\(\\s*(-?\\d+(?:\\.\\d+)?)\\s*,\\s*(-?\\d+(?:\\.\\d+)?)\\s*\\)/i);\r\n    if (!match) {\r\n        return null;\r\n    }\r\n    return {\r\n        x: Number(match[1]),\r\n        y: Number(match[2]),\r\n    };\r\n}\r\n\r\nfunction getOptionLabel(options, value, fallback = '') {\r\n    const normalizedValue = String(value || '');\r\n    return options.find(option => option.value === normalizedValue)?.label || fallback || normalizedValue;\r\n}\r\n\r\nfunction clampMemoryNodePosition(x, y, canvasWidth = MEMORY_GRAPH_CANVAS_WIDTH, canvasHeight = MEMORY_GRAPH_CANVAS_HEIGHT, topPadding = MEMORY_GRAPH_TOP_SAFE_PADDING) {\r\n    return {\r\n        x: Math.min(\r\n            canvasWidth - MEMORY_GRAPH_NODE_WIDTH - MEMORY_GRAPH_SAFE_PADDING,\r\n            Math.max(MEMORY_GRAPH_SAFE_PADDING, Number(x || 0)),\r\n        ),\r\n        y: Math.min(\r\n            canvasHeight - MEMORY_GRAPH_NODE_HEIGHT - MEMORY_GRAPH_SAFE_PADDING,\r\n            Math.max(topPadding, Number(y || 0)),\r\n        ),\r\n    };\r\n}\r\n\r\nfunction clampMemoryNodePositionToView(x, y, view = memoryGraphView, topPadding = 0) {\r\n    const safeTop = Math.max(0, Number(topPadding || 0));\r\n    const overflowAllowanceX = MEMORY_GRAPH_NODE_WIDTH * 0.45;\r\n    const overflowAllowanceY = MEMORY_GRAPH_NODE_HEIGHT * 0.45;\r\n    const minX = Number(view?.x || 0) - overflowAllowanceX;\r\n    const minY = Number(view?.y || 0) + safeTop - overflowAllowanceY;\r\n    const maxX = Number(view?.x || 0) + Number(view?.width || MEMORY_GRAPH_CANVAS_WIDTH) - MEMORY_GRAPH_NODE_WIDTH + overflowAllowanceX;\r\n    const maxY = Number(view?.y || 0) + Number(view?.height || MEMORY_GRAPH_CANVAS_HEIGHT) - MEMORY_GRAPH_NODE_HEIGHT + overflowAllowanceY;\r\n    return {\r\n        x: Math.min(maxX, Math.max(minX, Number(x || 0))),\r\n        y: Math.min(maxY, Math.max(minY, Number(y || 0))),\r\n    };\r\n}\r\n\r\nfunction getMemoryNodeRect(position) {\r\n    return {\r\n        x: Number(position?.x || 0),\r\n        y: Number(position?.y || 0),\r\n        width: MEMORY_GRAPH_NODE_WIDTH,\r\n        height: MEMORY_GRAPH_NODE_HEIGHT,\r\n    };\r\n}\r\n\r\nfunction buildMemoryEdgePath(sourcePosition, targetPosition, laneOffset = 0) {\r\n    const sourceRect = getMemoryNodeRect(sourcePosition);\r\n    const targetRect = getMemoryNodeRect(targetPosition);\r\n    const sourceCenter = {\r\n        x: sourceRect.x + (sourceRect.width / 2),\r\n        y: sourceRect.y + (sourceRect.height / 2),\r\n    };\r\n    const targetCenter = {\r\n        x: targetRect.x + (targetRect.width / 2),\r\n        y: targetRect.y + (targetRect.height / 2),\r\n    };\r\n    const dx = targetCenter.x - sourceCenter.x;\r\n    const dy = targetCenter.y - sourceCenter.y;\r\n    const horizontal = Math.abs(dx) >= Math.abs(dy);\r\n\r\n    let startX;\r\n    let startY;\r\n    let endX;\r\n    let endY;\r\n    let control1X;\r\n    let control1Y;\r\n    let control2X;\r\n    let control2Y;\r\n\r\n    if (horizontal) {\r\n        const sourceToRight = dx >= 0;\r\n        startX = sourceToRight ? sourceRect.x + sourceRect.width : sourceRect.x;\r\n        startY = sourceRect.y + (sourceRect.height / 2);\r\n        endX = sourceToRight ? targetRect.x : targetRect.x + targetRect.width;\r\n        endY = targetRect.y + (targetRect.height / 2);\r\n        const curve = Math.max(42, Math.abs(endX - startX) * 0.35);\r\n        control1X = startX + (sourceToRight ? curve : -curve);\r\n        control1Y = startY + laneOffset;\r\n        control2X = endX + (sourceToRight ? -curve : curve);\r\n        control2Y = endY + laneOffset;\r\n        startY += laneOffset * 0.35;\r\n        endY += laneOffset * 0.35;\r\n    } else {\r\n        const sourceToBottom = dy >= 0;\r\n        startX = sourceRect.x + (sourceRect.width / 2);\r\n        startY = sourceToBottom ? sourceRect.y + sourceRect.height : sourceRect.y;\r\n        endX = targetRect.x + (targetRect.width / 2);\r\n        endY = sourceToBottom ? targetRect.y : targetRect.y + targetRect.height;\r\n        const curve = Math.max(34, Math.abs(endY - startY) * 0.35);\r\n        control1X = startX + laneOffset;\r\n        control1Y = startY + (sourceToBottom ? curve : -curve);\r\n        control2X = endX + laneOffset;\r\n        control2Y = endY + (sourceToBottom ? -curve : curve);\r\n        startX += laneOffset * 0.35;\r\n        endX += laneOffset * 0.35;\r\n    }\r\n\r\n    return `M ${startX} ${startY} C ${control1X} ${control1Y}, ${control2X} ${control2Y}, ${endX} ${endY}`;\r\n}\r\n\r\nfunction buildSelectOptionsHtml(options, currentValue) {\r\n    const normalizedValue = String(currentValue || '');\r\n    const known = options.some(option => option.value === normalizedValue);\r\n    const optionHtml = options.map(option => `<option value=\"${escapeHtml(option.value)}\" ${option.value === normalizedValue ? 'selected' : ''}>${escapeHtml(option.label)}</option>`).join('');\r\n    if (!known && normalizedValue) {\r\n        return `${optionHtml}<option value=\"${escapeHtml(normalizedValue)}\" selected>自定义（${escapeHtml(normalizedValue)}）</option>`;\r\n    }\r\n    return optionHtml;\r\n}\r\n\r\nfunction buildNodeTypeSelect(fieldName, currentValue, extraAttributes = '') {\r\n    return `<select class=\"text_pole\" ${extraAttributes} data-${fieldName}=\"type\">${buildSelectOptionsHtml(MEMORY_NODE_TYPE_OPTIONS, currentValue || 'event')}</select>`;\r\n}\r\n\r\nfunction buildLinkTypeSelect(fieldName, currentValue, extraAttributes = '') {\r\n    return `<select class=\"text_pole\" ${extraAttributes} data-${fieldName}=\"type\">${buildSelectOptionsHtml(MEMORY_LINK_TYPE_OPTIONS, currentValue || 'RELATED')}</select>`;\r\n}\r\n\r\nfunction createNodeTypeSelect(fieldAttrName, currentValue, extraData = {}) {\r\n    const select = $('<select class=\"text_pole\"></select>').attr(fieldAttrName, 'type');\r\n    for (const option of MEMORY_NODE_TYPE_OPTIONS) {\r\n        select.append($('<option></option>').attr('value', option.value).text(option.label));\r\n    }\r\n    const normalizedValue = String(currentValue || 'event');\r\n    if (!MEMORY_NODE_TYPE_OPTIONS.some(option => option.value === normalizedValue) && normalizedValue) {\r\n        select.append($('<option></option>').attr('value', normalizedValue).text(`自定义（${normalizedValue}）`));\r\n    }\r\n    select.val(normalizedValue);\r\n    for (const [key, value] of Object.entries(extraData)) {\r\n        select.attr(key, value);\r\n    }\r\n    return select;\r\n}\r\n\r\nfunction createLinkTypeSelect(fieldAttrName, currentValue, extraData = {}) {\r\n    const select = $('<select class=\"text_pole\"></select>').attr(fieldAttrName, 'type');\r\n    for (const option of MEMORY_LINK_TYPE_OPTIONS) {\r\n        select.append($('<option></option>').attr('value', option.value).text(option.label));\r\n    }\r\n    const normalizedValue = String(currentValue || 'RELATED');\r\n    if (!MEMORY_LINK_TYPE_OPTIONS.some(option => option.value === normalizedValue) && normalizedValue) {\r\n        select.append($('<option></option>').attr('value', normalizedValue).text(`自定义（${normalizedValue}）`));\r\n    }\r\n    select.val(normalizedValue);\r\n    for (const [key, value] of Object.entries(extraData)) {\r\n        select.attr(key, value);\r\n    }\r\n    return select;\r\n}\r\n\r\nfunction showMemoryNodePopover(nodeId, clientX, clientY) {\r\n    memoryGraphSelectedNodeId = String(nodeId || '');\r\n    renderMemoryPanel();\r\n\r\n    const graph = getMemoryGraph();\r\n    const node = graph.nodes.find(item => item.id === nodeId);\r\n    let popover = $('#ai_wbr_memory_node_popover');\r\n    if (!popover.length) {\r\n        popover = $('<div id=\"ai_wbr_memory_node_popover\" class=\"ai-wbr-memory-node-popover\"></div>');\r\n        $('body').append(popover);\r\n    }\r\n    if (!node || !popover.length) {\r\n        return;\r\n    }\r\n\r\n    const sourceTitle = memoryGraphLinkSourceId\r\n        ? graph.nodes.find(item => item.id === memoryGraphLinkSourceId)?.title || memoryGraphLinkSourceId\r\n        : '';\r\n\r\n    popover\r\n        .attr('data-memory-node-id', node.id)\r\n        .html(`\r\n            <div class=\"ai-wbr-memory-popover-title\">${escapeHtml(truncateText(node.title || node.id, 38))}</div>\r\n            <label>标题<input class=\"text_pole\" type=\"text\" data-popover-node-field=\"title\" value=\"${escapeHtml(node.title || '')}\" /></label>\r\n            <label>类型${buildNodeTypeSelect('popover-node-field', node.type || 'event')}</label>\r\n            <label>内容<textarea class=\"text_pole\" rows=\"2\" data-popover-node-field=\"content\">${escapeHtml(node.content || '')}</textarea></label>\r\n            <div class=\"ai-wbr-memory-popover-actions\">\r\n                <button class=\"menu_button ai-wbr-memory-popover-save\" type=\"button\">保存</button>\r\n                <button class=\"menu_button ai-wbr-memory-set-link-source\" type=\"button\">设为起点</button>\r\n                <button class=\"menu_button ai-wbr-memory-link-to-source\" type=\"button\" ${memoryGraphLinkSourceId && memoryGraphLinkSourceId !== node.id ? '' : 'disabled'}>连接到起点${sourceTitle ? `：${escapeHtml(truncateText(sourceTitle, 8))}` : ''}</button>\r\n                <button class=\"menu_button ai-wbr-memory-popover-delete\" type=\"button\">删除</button>\r\n            </div>\r\n        `)\r\n        .css({\r\n            left: `${Math.min(Math.max(clientX + 12, 8), window.innerWidth - 244)}px`,\r\n            top: `${Math.min(Math.max(clientY + 12, 8), window.innerHeight - 210)}px`,\r\n        })\r\n        .show();\r\n}\r\n\r\nfunction bindMemoryGraphSvgInteractions() {\r\n    const container = $('#ai_wbr_memory_graph');\r\n    const previewPanel = $('#ai_wbr_memory_preview_panel');\r\n    container.off('.memoryGraphSvg');\r\n    previewPanel.off('.memoryGraphPreview');\r\n    $(document).off('.memoryGraphSvg');\r\n\r\n    const renderCurrentGraph = (options = {}) => {\r\n        if (memoryGraphRenderFrame) {\r\n            cancelAnimationFrame(memoryGraphRenderFrame);\r\n        }\r\n        memoryGraphRenderFrame = requestAnimationFrame(() => {\r\n            memoryGraphRenderFrame = null;\r\n            const graph = getMemoryGraph();\r\n            if (options.fit) {\r\n                memoryGraphView = { x: 0, y: 0, width: 0, height: 0 };\r\n            }\r\n            renderMemoryGraphSvg(graph);\r\n        });\r\n    };\r\n\r\n    previewPanel.on('input.memoryGraphPreview', '.ai-wbr-memory-preview-search', function (event) {\r\n        event.stopPropagation();\r\n        const value = String($(this).val() || '');\r\n        clearTimeout(memoryGraphSearchTimer);\r\n        memoryGraphSearchTimer = setTimeout(() => {\r\n            memoryGraphSearchText = value;\r\n            renderCurrentGraph({ fit: true });\r\n        }, 180);\r\n    });\r\n\r\n    previewPanel.on('click.memoryGraphPreview', '.ai-wbr-memory-preview-card', function (event) {\r\n        event.preventDefault();\r\n        const nodeId = String($(this).data('memoryNodeId') || '');\r\n        if (!nodeId) {\r\n            return;\r\n        }\r\n        memoryGraphSelectedNodeId = nodeId;\r\n        memoryGraphSelectedLinkId = '';\r\n        memoryGraphDetailMode = 'node';\r\n        memoryGraphDisplayMode = 'focus';\r\n        previewPanel.closest('.ai-wbr-graph-shell').removeClass('preview-open');\r\n        renderMemoryPanel('graph');\r\n    });\r\n\r\n    container.on('click.memoryGraphSvg', '.ai-wbr-memory-timeline-card', function (event) {\r\n        event.preventDefault();\r\n        event.stopPropagation();\r\n        const nodeId = String($(this).data('memoryNodeId') || '');\r\n        if (!nodeId) {\r\n            return;\r\n        }\r\n        memoryGraphSelectedNodeId = nodeId;\r\n        memoryGraphSelectedLinkId = '';\r\n        memoryGraphDetailMode = 'node';\r\n        renderMemoryPanel('graph');\r\n    });\r\n\r\n    container.on('click.memoryGraphSvg', '.ai-wbr-memory-mode', function (event) {\r\n        event.preventDefault();\r\n        event.stopPropagation();\r\n        const mode = String($(this).data('memoryGraphMode') || 'overview');\r\n        if (mode === 'focus' && !memoryGraphSelectedNodeId) {\r\n            return;\r\n        }\r\n        memoryGraphDisplayMode = mode;\r\n        renderCurrentGraph({ fit: true });\r\n    });\r\n\r\n    container.on('input.memoryGraphSvg', '.ai-wbr-memory-graph-search', function (event) {\r\n        event.stopPropagation();\r\n        const value = String($(this).val() || '');\r\n        clearTimeout(memoryGraphSearchTimer);\r\n        memoryGraphSearchTimer = setTimeout(() => {\r\n            memoryGraphSearchText = value;\r\n            renderCurrentGraph({ fit: true });\r\n        }, 180);\r\n    });\r\n\r\n    container.on('input.memoryGraphSvg', '.ai-wbr-memory-link-weight', function (event) {\r\n        event.stopPropagation();\r\n        memoryGraphMinLinkWeight = clampNumber($(this).val(), 0.35, 0, 1);\r\n        clearTimeout(memoryGraphSearchTimer);\r\n        memoryGraphSearchTimer = setTimeout(() => renderCurrentGraph(), 120);\r\n    });\r\n\r\n    container.on('click.memoryGraphSvg', '.ai-wbr-memory-type-filter', function (event) {\r\n        event.preventDefault();\r\n        event.stopPropagation();\r\n        const category = String($(this).data('memoryCategory') || 'all');\r\n        if (!category) {\r\n            return;\r\n        }\r\n        setMemoryGraphFocusedCategory(category);\r\n        memoryGraphDisplayMode = 'overview';\r\n        renderCurrentGraph({ fit: true });\r\n    });\r\n\r\n    container.on('click.memoryGraphSvg', '.ai-wbr-memory-clear-filters', function (event) {\n        event.preventDefault();\n        event.stopPropagation();\n        memoryGraphDisplayMode = 'overview';\n        memoryGraphMinLinkWeight = 0.35;\n        memoryGraphSearchText = '';\r\n        clearTimeout(memoryGraphSearchTimer);\r\n        memoryGraphVisibleTypes.clear();\n        renderCurrentGraph({ fit: true });\n    });\n\n    container.on('click.memoryGraphSvg', '.ai-wbr-memory-graph-filter-toggle', function (event) {\n        event.preventDefault();\n        event.stopPropagation();\n        const drawer = container.find('.ai-wbr-memory-graph-drawer-body').first();\n        if (drawer.length) {\n            drawer[0].scrollIntoView({ block: 'nearest', inline: 'nearest', behavior: 'smooth' });\n        }\n    });\n\n    container.on('input.memoryGraphSvg change.memoryGraphSvg', '#ai_wbr_memory_rule_drawer input, #ai_wbr_memory_rule_drawer textarea, #ai_wbr_memory_rule_drawer select', function (event) {\n        event.stopPropagation();\n        scheduleMemoryRuleConfigSave();\n    });\n\n    container.on('click.memoryGraphSvg', '.ai-wbr-memory-rule-reset', function (event) {\n        event.preventDefault();\n        event.stopPropagation();\n        saveMemoryRuleConfig(getDefaultMemoryRuleConfig());\n        renderMemoryGraphSvg(getMemoryGraph());\n    });\n\n    container.on('click.memoryGraphSvg pointerup.memoryGraphSvg touchend.memoryGraphSvg', '.ai-wbr-memory-rule-close', function (event) {\n        event.preventDefault();\n        event.stopPropagation();\n        memoryRuleDrawerOpen = false;\n        renderMemoryGraphSvg(getMemoryGraph());\n    });\n\n    container.on('pointerdown.memoryGraphSvg wheel.memoryGraphSvg click.memoryGraphSvg', '#ai_wbr_memory_rule_drawer', function (event) {\n        event.stopPropagation();\n    });\n\n    const svg = container.find('svg')[0];\n    if (!svg) {\n        return;\n    }\n\r\n    container.on('wheel.memoryGraphSvg', 'svg', function (event) {\r\n        event.preventDefault();\r\n        const original = event.originalEvent;\r\n        const factor = original.deltaY < 0 ? 0.88 : 1.14;\r\n        zoomMemoryGraphAt(svg, original.clientX, original.clientY, factor);\r\n    });\r\n\r\n    let dblClickTimer = null;\r\n    container.on('dblclick.memoryGraphSvg', 'svg', function (event) {\r\n        event.preventDefault();\r\n        if (memoryGraphPan || memoryGraphDrag) {\r\n            return;\r\n        }\r\n        const original = event.originalEvent || event;\r\n        const baseWidth = MEMORY_GRAPH_CANVAS_WIDTH;\r\n        const zoomingIn = memoryGraphView.width > baseWidth * 0.7;\r\n        zoomMemoryGraphAt(svg, original.clientX, original.clientY, zoomingIn ? 0.5 : 2);\r\n        if (dblClickTimer) {\r\n            clearTimeout(dblClickTimer);\r\n        }\r\n        dblClickTimer = setTimeout(() => { dblClickTimer = null; }, 350);\r\n    });\r\n\r\n    const activePointers = new Map();\r\n    let pinchState = null;\r\n    let pinchFrame = null;\r\n\r\n    const applyPinchFrame = () => {\r\n        pinchFrame = null;\r\n        if (!pinchState) {\r\n            return;\r\n        }\r\n        const ps = pinchState;\r\n        const rect = svg.getBoundingClientRect();\r\n        if (!rect.width || !rect.height) {\r\n            return;\r\n        }\r\n\r\n        // Damped scale factor: clamp the per-frame raw ratio to a small band so\r\n        // finger jitter does not translate into view jumps; smooth toward it.\r\n        const rawFactor = ps.lastDist / Math.max(1, ps.startDist);\r\n        const clampedRaw = Math.min(1.18, Math.max(0.82, rawFactor));\r\n        const factor = ps.appliedFactor + (clampedRaw - ps.appliedFactor) * 0.5;\r\n        if (Math.abs(factor - 1) > 0.001) {\r\n            zoomMemoryGraphAt(svg, ps.lastMidX, ps.lastMidY, factor / Math.max(0.001, ps.appliedFactor || 1));\r\n            ps.appliedFactor = factor;\r\n        }\r\n\r\n        // Two-finger pan: move the view by the midpoint delta, in view units.\r\n        if (ps.panMoved) {\r\n            const dx = (ps.startMidX - ps.lastMidX) * (memoryGraphView.width / rect.width);\r\n            const dy = (ps.startMidY - ps.lastMidY) * (memoryGraphView.height / rect.height);\r\n            if (Number.isFinite(dx) && Number.isFinite(dy) && (Math.abs(dx) > 0.2 || Math.abs(dy) > 0.2)) {\r\n                memoryGraphView.x += dx;\r\n                memoryGraphView.y += dy;\r\n                updateMemoryGraphViewBox(svg);\r\n                ps.startMidX = ps.lastMidX;\r\n                ps.startMidY = ps.lastMidY;\r\n            }\r\n        }\r\n    };\r\n\r\n    container[0]?.addEventListener('pointerdown', (nativeEvent) => {\r\n        if (!nativeEvent.isTrusted && nativeEvent.pointerType === 'mouse') {\r\n            return;\r\n        }\r\n        activePointers.set(nativeEvent.pointerId, { x: nativeEvent.clientX, y: nativeEvent.clientY });\r\n        if (activePointers.size === 2) {\r\n            const [a, b] = [...activePointers.values()];\r\n            const dist = Math.hypot(a.x - b.x, a.y - b.y);\r\n            const midX = (a.x + b.x) / 2;\r\n            const midY = (a.y + b.y) / 2;\r\n            pinchState = {\r\n                startDist: dist,\r\n                startMidX: midX,\r\n                startMidY: midY,\r\n                lastDist: dist,\r\n                lastMidX: midX,\r\n                lastMidY: midY,\r\n                appliedFactor: 1,\r\n                panMoved: false,\r\n            };\r\n            if (memoryGraphPan) {\r\n                memoryGraphPan = null;\r\n                $(svg).removeClass('ai-wbr-memory-panning');\r\n            }\r\n            if (memoryGraphDrag?.longPressTimer) {\r\n                clearTimeout(memoryGraphDrag.longPressTimer);\r\n                memoryGraphDrag.longPressTimer = null;\r\n            }\r\n        }\r\n    }, { passive: true });\r\n\r\n    container[0]?.addEventListener('pointermove', (nativeEvent) => {\r\n        if (!activePointers.has(nativeEvent.pointerId)) {\r\n            activePointers.set(nativeEvent.pointerId, { x: nativeEvent.clientX, y: nativeEvent.clientY });\r\n            return;\r\n        }\r\n        activePointers.set(nativeEvent.pointerId, { x: nativeEvent.clientX, y: nativeEvent.clientY });\r\n        if (!pinchState || activePointers.size < 2) {\r\n            return;\r\n        }\r\n        const [a, b] = [...activePointers.values()];\r\n        const dist = Math.hypot(a.x - b.x, a.y - b.y);\r\n        const midX = (a.x + b.x) / 2;\r\n        const midY = (a.y + b.y) / 2;\r\n        pinchState.lastDist = dist;\r\n        pinchState.lastMidX = midX;\r\n        pinchState.lastMidY = midY;\r\n        if (Math.hypot(midX - pinchState.startMidX, midY - pinchState.startMidY) > MEMORY_GRAPH_TOUCH_TAP_THRESHOLD) {\r\n            pinchState.panMoved = true;\r\n        }\r\n        if (!pinchFrame) {\r\n            pinchFrame = requestAnimationFrame(applyPinchFrame);\r\n        }\r\n        nativeEvent.preventDefault?.();\r\n    }, { passive: false });\r\n\r\n    const releasePointer = (nativeEvent) => {\r\n        activePointers.delete(nativeEvent.pointerId);\r\n        if (activePointers.size < 2) {\r\n            pinchState = null;\r\n        }\r\n        if (pinchFrame) {\r\n            cancelAnimationFrame(pinchFrame);\r\n            pinchFrame = null;\r\n        }\r\n    };\r\n    container[0]?.addEventListener('pointerup', releasePointer, { passive: true });\r\n    container[0]?.addEventListener('pointercancel', releasePointer, { passive: true });\r\n    container[0]?.addEventListener('pointerleave', releasePointer, { passive: true });\r\n\r\n    container.on('pointerdown.memoryGraphSvg', '.ai-wbr-memory-node', function (event) {\r\n        const nodeId = String($(this).data('memoryNodeId'));\r\n        const original = event.originalEvent || event;\r\n        const start = getMemoryGraphSvgPoint(svg, original.clientX, original.clientY);\r\n        const graph = getMemoryGraph();\r\n        const node = graph.nodes.find(item => item.id === nodeId);\r\n        const transform = parseMemoryNodeTransform($(this).attr('transform'));\r\n        if (!node) {\r\n            return;\r\n        }\r\n\r\n        const nodePositions = new Map();\r\n        container.find('.ai-wbr-memory-node').each(function () {\r\n            const id = String($(this).data('memoryNodeId'));\r\n            const t = parseMemoryNodeTransform($(this).attr('transform'));\r\n            if (t) {\r\n                nodePositions.set(id, t);\r\n            }\r\n        });\r\n\r\n        const visibleIds = new Set(Array.from(nodePositions.keys()));\r\n        const edges = graph.links.filter(link => visibleIds.has(link.source) && visibleIds.has(link.target));\r\n        const pairBuckets = new Map();\r\n        edges.forEach((link) => {\r\n            const pairKey = [String(link.source || ''), String(link.target || '')].sort().join('||');\r\n            if (!pairBuckets.has(pairKey)) {\r\n                pairBuckets.set(pairKey, []);\r\n            }\r\n            pairBuckets.get(pairKey).push(link);\r\n        });\r\n\r\n        const linkOffsets = new Map();\r\n        edges.forEach((link) => {\r\n            const pairKey = [String(link.source || ''), String(link.target || '')].sort().join('||');\r\n            const siblings = (pairBuckets.get(pairKey) || []).slice().sort((a, b) => String(a.id || '').localeCompare(String(b.id || '')));\r\n            const siblingIndex = siblings.findIndex(item => String(item.id || '') === String(link.id || ''));\r\n            const offsetIndex = siblingIndex - ((siblings.length - 1) / 2);\r\n            linkOffsets.set(link.id, siblings.length > 1 ? offsetIndex * 18 : 0);\r\n        });\r\n\r\n        const longPressTimer = setTimeout(() => {\r\n            if (!memoryGraphDrag || memoryGraphDrag.nodeId !== nodeId || memoryGraphDrag.moved) {\r\n                return;\r\n            }\r\n            memoryGraphDrag.longPressed = true;\r\n            memoryGraphSuppressNextNodeClick = true;\r\n            memoryGraphSelectedNodeId = nodeId;\r\n            memoryGraphSelectedLinkId = '';\r\n            memoryGraphDetailMode = 'node';\r\n            renderMemoryDetailDrawer(getMemoryGraph());\r\n            $('#ai_wbr_memory_node_popover').hide();\r\n        }, MEMORY_GRAPH_LONG_PRESS_MS);\r\n\r\n        memoryGraphDrag = {\r\n            nodeId,\r\n            startX: start.x,\r\n            startY: start.y,\r\n            nodeX: Number.isFinite(transform?.x) ? transform.x : Number(node.x || 0),\r\n            nodeY: Number.isFinite(transform?.y) ? transform.y : Number(node.y || 0),\r\n            moved: false,\r\n            longPressed: false,\r\n            longPressTimer,\r\n            nodePositions,\r\n            linkOffsets,\r\n            links: edges.filter(link => link.source === nodeId || link.target === nodeId),\r\n            pointerId: original.pointerId,\r\n        };\r\n        this.setPointerCapture?.(original.pointerId);\r\n        event.preventDefault();\r\n        event.stopPropagation();\r\n    });\r\n\r\n    container.on('click.memoryGraphSvg', '.ai-wbr-memory-node', function (event) {\r\n        event.preventDefault();\r\n        event.stopPropagation();\r\n        if (memoryGraphSuppressNextNodeClick) {\r\n            memoryGraphSuppressNextNodeClick = false;\r\n            return;\r\n        }\r\n        const nodeId = String($(this).data('memoryNodeId') || '');\r\n        if (!nodeId) {\r\n            return;\r\n        }\r\n        memoryGraphSelectedNodeId = nodeId;\r\n        memoryGraphSelectedLinkId = '';\r\n        memoryGraphDetailMode = '';\r\n        memoryGraphDisplayMode = 'focus';\r\n        renderMemoryPanel('graph');\r\n        $('#ai_wbr_memory_node_popover').hide();\r\n    });\r\n\r\n    $(document).off('click.memoryGraphShellToggles')\n        .on('click.memoryGraphShellToggles', '#ai_wbr_memory_graph_preview_toggle', function (event) {\n            event.preventDefault();\n            $('#ai_wbr_memory_preview_panel').closest('.ai-wbr-graph-shell').toggleClass('preview-open');\n            memoryGraphPreviewRenderKey = '';\n            renderMemoryGraphSvg(getMemoryGraph());\n        })\n        .on('click.memoryGraphShellToggles', '.ai-wbr-memory-preview-close', function (event) {\n            event.preventDefault();\n            $('#ai_wbr_memory_preview_panel').closest('.ai-wbr-graph-shell').removeClass('preview-open');\n            memoryGraphPreviewRenderKey = '';\n            renderMemoryGraphSvg(getMemoryGraph());\n        })\n        .on('click.memoryGraphShellToggles', '#ai_wbr_memory_rule_toggle', function (event) {\n            event.preventDefault();\n            event.stopPropagation();\n            memoryRuleDrawerOpen = !memoryRuleDrawerOpen;\n            $('#ai_wbr_memory_preview_panel').closest('.ai-wbr-graph-shell').removeClass('preview-open');\n            renderMemoryGraphSvg(getMemoryGraph());\n        })\n        .on('click.memoryGraphShellToggles', '.ai-wbr-memory-rule-close', function (event) {\n            event.preventDefault();\n            event.stopPropagation();\n            memoryRuleDrawerOpen = false;\n            renderMemoryGraphSvg(getMemoryGraph());\n        });\n\r\n    container.on('click.memoryGraphSvg', '.ai-wbr-memory-edge, .ai-wbr-memory-edge-hit', function (event) {\r\n        event.preventDefault();\r\n        event.stopPropagation();\r\n        memoryGraphSelectedLinkId = String($(this).data('memoryLinkId') || '');\r\n        memoryGraphSelectedNodeId = '';\r\n        memoryGraphDetailMode = 'link';\r\n        renderMemoryPanel('graph');\r\n    });\r\n\r\n    container.on('pointerdown.memoryGraphSvg', '.ai-wbr-memory-edge, .ai-wbr-memory-edge-hit', function (event) {\r\n        event.preventDefault();\r\n        event.stopPropagation();\r\n    });\r\n\r\n    container.on('pointerdown.memoryGraphSvg', 'svg', function (event) {\r\n        if ($(event.target).closest('.ai-wbr-memory-node').length) {\r\n            return;\r\n        }\r\n        const original = event.originalEvent || event;\r\n\r\n        memoryGraphPan = {\r\n            startClientX: original.clientX,\r\n            startClientY: original.clientY,\r\n            viewX: memoryGraphView.x,\r\n            viewY: memoryGraphView.y,\r\n            moved: false,\r\n            pointerId: original.pointerId,\r\n        };\r\n        svg.setPointerCapture?.(original.pointerId);\r\n        $('#ai_wbr_memory_node_popover').hide();\r\n        $(svg).addClass('ai-wbr-memory-panning');\r\n        event.preventDefault();\r\n    });\r\n\r\n    $(document).on('pointermove.memoryGraphSvg', (event) => {\r\n        const original = event.originalEvent || event;\r\n        if (!memoryGraphDrag) {\r\n            if (memoryGraphPan) {\r\n                if (memoryGraphPan.pointerId !== undefined && original.pointerId !== undefined && memoryGraphPan.pointerId !== original.pointerId) {\r\n                    return;\r\n                }\r\n                const rect = svg.getBoundingClientRect();\r\n                const dx = (original.clientX - memoryGraphPan.startClientX) * (memoryGraphView.width / Math.max(1, rect.width));\r\n                const dy = (original.clientY - memoryGraphPan.startClientY) * (memoryGraphView.height / Math.max(1, rect.height));\r\n                if (Math.abs(dx) + Math.abs(dy) > MEMORY_GRAPH_TOUCH_TAP_THRESHOLD) {\r\n                    memoryGraphPan.moved = true;\r\n                }\r\n                memoryGraphView.x = memoryGraphPan.viewX - dx;\r\n                memoryGraphView.y = memoryGraphPan.viewY - dy;\r\n                updateMemoryGraphViewBox(svg);\r\n                event.preventDefault();\r\n            }\r\n            return;\r\n        }\r\n        if (memoryGraphDrag.pointerId !== undefined && original.pointerId !== undefined && memoryGraphDrag.pointerId !== original.pointerId) {\r\n            return;\r\n        }\r\n        const point = getMemoryGraphSvgPoint(svg, original.clientX, original.clientY);\r\n        const dx = point.x - memoryGraphDrag.startX;\r\n        const dy = point.y - memoryGraphDrag.startY;\r\n        if (Math.abs(dx) + Math.abs(dy) > MEMORY_GRAPH_TOUCH_TAP_THRESHOLD) {\r\n            memoryGraphDrag.moved = true;\r\n            if (memoryGraphDrag.longPressTimer) {\r\n                clearTimeout(memoryGraphDrag.longPressTimer);\r\n                memoryGraphDrag.longPressTimer = null;\r\n            }\r\n        }\r\n        \r\n        const clamped = clampMemoryNodePositionToView(memoryGraphDrag.nodeX + dx, memoryGraphDrag.nodeY + dy);\r\n        memoryGraphDrag.pendingPosition = clamped;\r\n        if (!memoryGraphDragFrame) {\r\n            memoryGraphDragFrame = requestAnimationFrame(() => {\r\n                memoryGraphDragFrame = null;\r\n                if (!memoryGraphDrag?.pendingPosition) {\r\n                    return;\r\n                }\r\n                const next = memoryGraphDrag.pendingPosition;\r\n                memoryGraphDrag.pendingPosition = null;\r\n                memoryGraphDrag.nodePositions.set(memoryGraphDrag.nodeId, { x: next.x, y: next.y });\r\n\r\n                const group = container.find(`.ai-wbr-memory-node[data-memory-node-id=\"${escapeCssSelector(memoryGraphDrag.nodeId)}\"]`);\r\n                group.attr('transform', `translate(${next.x},${next.y})`);\r\n\r\n                memoryGraphDrag.links.forEach((link) => {\r\n                    const line = container.find(`[data-memory-link-id=\"${escapeCssSelector(String(link.id || ''))}\"]`);\r\n                    const source = memoryGraphDrag.nodePositions.get(link.source);\r\n                    const target = memoryGraphDrag.nodePositions.get(link.target);\r\n                    const laneOffset = memoryGraphDrag.linkOffsets.get(link.id) || 0;\r\n                    if (source && target) {\r\n                        line.attr('d', buildMemoryEdgePath(source, target, laneOffset));\r\n                    }\r\n                });\r\n            });\r\n        }\r\n        event.preventDefault();\r\n    });\r\n\r\n    $(document).on('pointerup.memoryGraphSvg pointercancel.memoryGraphSvg', (event) => {\r\n        const original = event.originalEvent || event;\r\n        if (memoryGraphPan) {\r\n            if (memoryGraphPan.pointerId !== undefined && original.pointerId !== undefined && memoryGraphPan.pointerId !== original.pointerId) {\r\n                return;\r\n            }\r\n            memoryGraphPan = null;\r\n            $(svg).removeClass('ai-wbr-memory-panning');\r\n            lastObservedChatScopedUiSignature = getChatScopedUiSignature();\r\n            return;\r\n        }\r\n\r\n        if (!memoryGraphDrag) {\r\n            return;\r\n        }\r\n        if (memoryGraphDrag.pointerId !== undefined && original.pointerId !== undefined && memoryGraphDrag.pointerId !== original.pointerId) {\r\n            return;\r\n        }\r\n        const drag = memoryGraphDrag;\r\n        memoryGraphDrag = null;\r\n        if (drag.longPressTimer) {\r\n            clearTimeout(drag.longPressTimer);\r\n        }\r\n        if (memoryGraphDragFrame) {\r\n            cancelAnimationFrame(memoryGraphDragFrame);\r\n            memoryGraphDragFrame = null;\r\n        }\r\n        const graph = getMemoryGraph();\r\n        const node = graph.nodes.find(item => item.id === drag.nodeId);\r\n        if (node) {\r\n            const point = getMemoryGraphSvgPoint(svg, original.clientX, original.clientY);\r\n            const dx = point.x - drag.startX;\r\n            const dy = point.y - drag.startY;\r\n            const clamped = clampMemoryNodePositionToView(drag.nodeX + dx, drag.nodeY + dy);\r\n            node.x = clamped.x;\r\n            node.y = clamped.y;\r\n            node.updatedAt = new Date().toISOString();\r\n        }\r\n        graph.updatedAt = new Date().toISOString();\r\n        saveMemoryGraph(graph, getContext(), true);\r\n        lastObservedChatScopedUiSignature = getChatScopedUiSignature();\r\n\r\n        if (!drag.moved && !drag.longPressed) {\r\n            memoryGraphSelectedNodeId = drag.nodeId;\r\n            memoryGraphSelectedLinkId = '';\r\n            memoryGraphDetailMode = '';\r\n            memoryGraphDisplayMode = 'focus';\r\n            renderMemoryPanel('graph');\r\n            $('#ai_wbr_memory_node_popover').hide();\r\n        } else if (drag.longPressed) {\r\n            renderMemoryDetailDrawer(getMemoryGraph());\r\n        } else {\r\n            $('#ai_wbr_memory_json').val(JSON.stringify(graph, null, 2));\r\n        }\r\n    });\r\n\r\n    $(document).on('click.memoryGraphSvg', '#ai_wbr_memory_node_popover .ai-wbr-memory-popover-save', function () {\r\n        const graph = getMemoryGraph();\r\n        const popover = $('#ai_wbr_memory_node_popover');\r\n        const node = graph.nodes.find(item => item.id === String(popover.data('memoryNodeId')));\r\n        if (!node) {\r\n            return;\r\n        }\r\n        popover.find('[data-popover-node-field]').each(function () {\r\n            const field = String($(this).data('popoverNodeField'));\r\n            node[field] = String($(this).val() || '');\r\n        });\r\n        node.updatedAt = new Date().toISOString();\r\n        saveMemoryGraph(graph);\r\n        popover.hide();\r\n    });\r\n\r\n    $(document).on('click.memoryGraphSvg', '#ai_wbr_memory_node_popover .ai-wbr-memory-set-link-source', function () {\r\n        const nodeId = String($('#ai_wbr_memory_node_popover').data('memoryNodeId'));\r\n        memoryGraphLinkSourceId = nodeId;\r\n        renderMemoryGraphSvg(getMemoryGraph());\r\n    });\r\n\r\n    $(document).on('click.memoryGraphSvg', '#ai_wbr_memory_node_popover .ai-wbr-memory-link-to-source', function () {\r\n        const graph = getMemoryGraph();\r\n        const targetId = String($('#ai_wbr_memory_node_popover').data('memoryNodeId'));\r\n        if (!memoryGraphLinkSourceId || memoryGraphLinkSourceId === targetId) {\r\n            return;\r\n        }\r\n        const existingIndex = graph.links.findIndex(item => item.source === memoryGraphLinkSourceId && item.target === targetId && item.type === 'RELATED');\r\n        if (existingIndex >= 0) {\r\n            graph.links.splice(existingIndex, 1);\r\n            graph.updatedAt = new Date().toISOString();\r\n            saveMemoryGraph(graph, getContext(), false, { fit: true });\r\n            memoryGraphLinkSourceId = '';\r\n            $('#ai_wbr_memory_node_popover').hide();\r\n            return;\r\n        }\r\n        const link = normalizeMemoryLink({\r\n            source: memoryGraphLinkSourceId,\r\n            target: targetId,\r\n            type: 'RELATED',\r\n            weight: 0.7,\r\n            description: '手动创建的记忆关系',\r\n        }, new Set(graph.nodes.map(node => node.id)), graph.links.length);\r\n        if (link && !graph.links.some(item => item.source === link.source && item.target === link.target && item.type === link.type)) {\r\n            graph.links.push(link);\r\n            graph.updatedAt = new Date().toISOString();\r\n            saveMemoryGraph(graph, getContext(), false, { fit: true });\r\n        }\r\n        memoryGraphLinkSourceId = '';\r\n        $('#ai_wbr_memory_node_popover').hide();\r\n    });\r\n\r\n    $(document).on('click.memoryGraphSvg', '#ai_wbr_memory_node_popover .ai-wbr-memory-popover-delete', function () {\r\n        const graph = getMemoryGraph();\r\n        const nodeId = String($('#ai_wbr_memory_node_popover').data('memoryNodeId'));\r\n        graph.nodes = graph.nodes.filter(node => node.id !== nodeId);\r\n        graph.links = graph.links.filter(link => link.source !== nodeId && link.target !== nodeId);\r\n        saveMemoryGraph(graph, getContext(), false, { fit: true });\r\n        $('#ai_wbr_memory_node_popover').hide();\r\n    });\r\n\r\n    container.on('click.memoryGraphSvg', function (event) {\r\n        if ($(event.target).closest('.ai-wbr-memory-node, .ai-wbr-memory-edge, .ai-wbr-memory-edge-hit, .ai-wbr-memory-graph-toolbar').length) {\r\n            return;\r\n        }\r\n        memoryGraphSelectedNodeId = '';\r\n        memoryGraphSelectedLinkId = '';\r\n        memoryGraphDetailMode = '';\r\n        renderMemoryDetailDrawer(getMemoryGraph());\r\n        $('#ai_wbr_memory_node_popover').hide();\r\n    });\r\n}\r\n\r\nfunction renderMemoryPanel(scope = 'all') {\r\n    const hasMemoryPanel = $('#ai_wbr_memory_status, #ai_wbr_memory_review_queue, #ai_wbr_memory_state_editor').length > 0;\r\n    const hasGraphPanel = $('#ai_wbr_memory_graph').length > 0;\r\n    const hasGraphListPanel = $('#ai_wbr_memory_nodes, #ai_wbr_memory_links').length > 0;\r\n    if (!hasMemoryPanel && !hasGraphPanel) {\r\n        return;\r\n    }\r\n\r\n    const context = getContext();\r\n    const graph = getMemoryGraph(context);\r\n    memoryScopeDebugLog('renderMemoryPanel', {\r\n        graph: getMemoryGraphSummary(graph),\r\n        selectedNodeId: memoryGraphSelectedNodeId,\r\n        linkSourceId: memoryGraphLinkSourceId,\r\n    });\r\n    if (hasMemoryPanel) {\r\n        $('#ai_wbr_memory_status').text(getCurrentMemoryStatus() || (settings.memoryEnabled ? '待整理' : '未启用'));\r\n        $('#ai_wbr_memory_json').val(JSON.stringify(graph, null, 2));\r\n        const showMemoryDebugDetails = !!settings.memoryDebug || !!getCurrentMemoryLastError(context);\r\n        $('.ai-wbr-memory-debug-fold').prop('open', showMemoryDebugDetails);\r\n        $('#ai_wbr_memory_debug_panel .ai-wbr-memory-raw-block').toggle(showMemoryDebugDetails);\r\n        $('#ai_wbr_memory_prompt').text(getCurrentMemoryLastPrompt(context) || '尚无后置记忆 Prompt');\r\n        $('#ai_wbr_memory_raw').text(getCurrentMemoryLastRaw(context) || '尚无后置记忆返回');\r\n        $('#ai_wbr_memory_error').text(getCurrentMemoryLastError(context) || '尚无错误');\r\n        renderMemoryDashboard(graph);\r\n        renderMemoryReviewQueue();\r\n        renderBookshelfPanel();\r\n        if ($('#ai_wbr_memory_history_preview_box').length) {\r\n            buildHistoryImportPreview(getHistoryImportRangeFromUi(context), context);\r\n        }\r\n    }\r\n    renderMemoryGraphSvg(graph);\r\n    renderMemoryNodeEditor(graph);\r\n    renderMemoryEdgeEditor(graph);\r\n    renderMemoryDetailDrawer(graph);\r\n\r\n    if (hasMemoryPanel) {\r\n        const stateRows = getMemoryStateRows(graph);\r\n        const stateTable = $('<table class=\"ai-wbr-memory-table\"></table>');\r\n        stateTable.append('<thead><tr><th>状态字段</th><th>当前值</th></tr></thead>');\r\n        const stateBody = $('<tbody></tbody>');\r\n        for (const row of stateRows) {\r\n            stateBody.append(\r\n                $('<tr></tr>')\r\n                    .append($('<th scope=\"row\"></th>')\r\n                        .append($('<div></div>').text(row.label))\r\n                        .append(row.description ? $('<div class=\"ai-wbr-memory-state-desc\"></div>').text(row.description) : '')\r\n                        .append(row.isCustom\r\n                            ? $('<button class=\"menu_button ai-wbr-memory-delete-state-definition m-t-05\" type=\"button\">删除</button>')\r\n                                .attr('data-memory-state-definition-key', row.key)\r\n                            : ''))\r\n                    .append($('<td></td>').append(\r\n                        $('<input class=\"text_pole\" type=\"text\" />')\r\n                            .attr('data-memory-state-field', row.key)\r\n                            .val(row.value),\r\n                    )),\r\n            );\r\n        }\r\n        stateTable.append(stateBody);\r\n        $('#ai_wbr_memory_state_editor').empty().append(\r\n            $('<div class=\"ai-wbr-memory-subtitle\"><b>新增自定义状态字段</b></div>'),\r\n            $(`\r\n                <div class=\"ai-wbr-memory-add-state\">\r\n                    <input id=\"ai_wbr_memory_new_state_label\" class=\"text_pole\" type=\"text\" placeholder=\"字段名，例如：道具\" />\r\n                    <input id=\"ai_wbr_memory_new_state_instruction\" class=\"text_pole\" type=\"text\" placeholder=\"字段说明，例如：user背包里的道具\" />\r\n                    <button id=\"ai_wbr_memory_add_state_definition\" class=\"menu_button\" type=\"button\">添加字段</button>\r\n                </div>\r\n            `),\r\n            $('<div class=\"ai-wbr-memory-subtitle\"><b>状态表（固定更新）</b></div>'),\r\n            $('<div class=\"ai-wbr-memory-table-wrap\"></div>').append(stateTable),\r\n        );\r\n    }\r\n\r\n    if (!hasMemoryPanel && !hasGraphListPanel) {\r\n        return;\r\n    }\r\n\r\n    const eventNodes = getMemoryEventNodes(graph);\r\n    const nonEventNodes = getMemoryNonEventNodes(graph);\r\n    const nodesContainer = $('#ai_wbr_memory_nodes').empty();\r\n    nodesContainer.append($('<div class=\"ai-wbr-memory-subtitle\"><b>事件表（剧情推进会新增）</b></div>'));\r\n    if (!eventNodes.length) {\r\n        nodesContainer.append('<div class=\"ai-wbr-token-empty\">暂无事件记录</div>');\r\n    } else {\r\n        const eventTable = $('<table class=\"ai-wbr-memory-table ai-wbr-memory-event-table\"></table>');\r\n        eventTable.append('<thead><tr><th>标题</th><th>地点 / 时间</th><th>概要</th><th>详细纪要</th><th>关键词</th><th>操作</th></tr></thead>');\r\n        const eventBody = $('<tbody></tbody>');\r\n        for (const node of eventNodes) {\r\n            eventBody.append(\r\n                $('<tr></tr>')\r\n                    .attr('data-memory-node-id', node.id)\r\n                    .append($('<td></td>').append(\r\n                        $('<input class=\"text_pole\" type=\"text\" data-memory-node-field=\"title\" />').val(node.title || ''),\r\n                    ))\r\n                    .append($('<td></td>').append(\r\n                        $('<input class=\"text_pole\" type=\"text\" data-memory-node-field=\"location\" placeholder=\"地点\" />').val(node.location || ''),\r\n                        $('<input class=\"text_pole m-t-05\" type=\"text\" data-memory-node-field=\"timeSpan\" placeholder=\"时间跨度\" />').val(node.timeSpan || ''),\r\n                    ))\r\n                    .append($('<td></td>').append(\r\n                        $('<input class=\"text_pole\" type=\"text\" data-memory-node-field=\"summary\" />').val(node.summary || ''),\r\n                    ))\r\n                    .append($('<td></td>').append(\r\n                        $('<textarea class=\"text_pole ai-wbr-memory-content\" rows=\"3\" data-memory-node-field=\"content\"></textarea>').val(node.content || ''),\r\n                    ))\r\n                    .append($('<td></td>').append(\r\n                        $('<input class=\"text_pole\" type=\"text\" data-memory-node-field=\"keys\" placeholder=\"关键词，用逗号分隔\" />').val((node.keys || []).join('，')),\r\n                        $('<input class=\"text_pole m-t-05\" type=\"text\" data-memory-node-field=\"tags\" placeholder=\"标签，用逗号分隔\" />').val((node.tags || []).join('，')),\r\n                    ))\r\n                    .append($('<td></td>').append(\r\n                        $('<div class=\"ai-wbr-memory-event-actions\"></div>')\r\n                            .append(createNodeTypeSelect('data-memory-node-field', node.type || 'event'))\r\n                            .append($('<button class=\"menu_button ai-wbr-memory-delete-node\" type=\"button\">删除</button>')),\r\n                    )),\r\n            );\r\n        }\r\n        eventTable.append(eventBody);\r\n        nodesContainer.append($('<div class=\"ai-wbr-memory-table-wrap\"></div>').append(eventTable));\r\n    }\r\n\r\n    nodesContainer.append($('<div class=\"ai-wbr-memory-subtitle m-t-1\"><b>非事件节点（角色 / 地点 / 概念等）</b></div>'));\r\n    if (!nonEventNodes.length) {\r\n        nodesContainer.append('<div class=\"ai-wbr-token-empty\">暂无非事件节点</div>');\r\n    } else {\r\n        const nonEventTable = $('<table class=\"ai-wbr-memory-table ai-wbr-memory-entity-table\"></table>');\r\n        nonEventTable.append('<thead><tr><th>标题</th><th>类型</th><th>说明</th><th>关键词 / 标签</th><th>操作</th></tr></thead>');\r\n        const nonEventBody = $('<tbody></tbody>');\r\n        for (const node of nonEventNodes) {\r\n            nonEventBody.append(\r\n                $('<tr></tr>')\r\n                    .attr('data-memory-node-id', node.id)\r\n                    .append($('<td></td>').append(\r\n                        $('<input class=\"text_pole\" type=\"text\" data-memory-node-field=\"title\" />').val(node.title || ''),\r\n                    ))\r\n                    .append($('<td></td>').append(\r\n                        createNodeTypeSelect('data-memory-node-field', node.type || 'character'),\r\n                    ))\r\n                    .append($('<td></td>').append(\r\n                        $('<textarea class=\"text_pole ai-wbr-memory-content\" rows=\"3\" data-memory-node-field=\"content\"></textarea>').val(node.content || ''),\r\n                    ))\r\n                    .append($('<td></td>').append(\r\n                        $('<input class=\"text_pole\" type=\"text\" data-memory-node-field=\"keys\" placeholder=\"关键词，用逗号分隔\" />').val((node.keys || []).join('，')),\r\n                        $('<input class=\"text_pole m-t-05\" type=\"text\" data-memory-node-field=\"tags\" placeholder=\"标签，用逗号分隔\" />').val((node.tags || []).join('，')),\r\n                    ))\r\n                    .append($('<td></td>').append(\r\n                        $('<button class=\"menu_button ai-wbr-memory-delete-node\" type=\"button\">删除</button>'),\r\n                    )),\r\n            );\r\n        }\r\n        nonEventTable.append(nonEventBody);\r\n        nodesContainer.append($('<div class=\"ai-wbr-memory-table-wrap\"></div>').append(nonEventTable));\r\n    }\r\n\r\n    const linksContainer = $('#ai_wbr_memory_links').empty();\r\n    linksContainer.append($('<div class=\"ai-wbr-memory-subtitle\"><b>关系边</b></div>'));\r\n    if (!graph.links.length) {\r\n        linksContainer.append('<div class=\"ai-wbr-token-empty\">暂无关系</div>');\r\n    }\r\n    for (const link of graph.links) {\r\n        const source = graph.nodes.find(node => node.id === link.source)?.title || link.source;\r\n        const target = graph.nodes.find(node => node.id === link.target)?.title || link.target;\r\n        linksContainer.append(\r\n            $('<div class=\"ai-wbr-memory-link-card\"></div>')\r\n                .attr('data-memory-link-id', link.id)\r\n                .toggleClass('ai-wbr-memory-link-card-selected', String(link.id) === String(memoryGraphSelectedLinkId))\r\n                .append($('<div class=\"ai-wbr-memory-link-title\"></div>').text(`${source} → ${target}`))\r\n                .append($('<div class=\"ai-wbr-memory-link-grid\"></div>')\r\n                    .append(createLinkTypeSelect('data-memory-link-field', link.type || 'RELATED'))\r\n                    .append($('<input class=\"text_pole\" type=\"number\" min=\"0\" max=\"1\" step=\"0.05\" data-memory-link-field=\"weight\" />').val(link.weight ?? 0.7))\r\n                    .append($('<button class=\"menu_button ai-wbr-memory-delete-link\" type=\"button\">删除</button>')))\r\n                .append($('<input class=\"text_pole\" type=\"text\" data-memory-link-field=\"description\" placeholder=\"关系描述\" />').val(link.description || '')),\r\n        );\r\n    }\r\n}\r\n\r\nfunction renderMemoryNodeEditor(graph) {\r\n    const editor = $('#ai_wbr_memory_node_editor').empty();\r\n    const nodes = Array.isArray(graph?.nodes) ? graph.nodes : [];\r\n    const selectedNode = nodes.find(node => node.id === memoryGraphSelectedNodeId) || null;\r\n    const sourceTitle = memoryGraphLinkSourceId\r\n        ? nodes.find(item => item.id === memoryGraphLinkSourceId)?.title || memoryGraphLinkSourceId\r\n        : '';\r\n    const hasRelatedLink = !!(memoryGraphLinkSourceId && selectedNode && memoryGraphLinkSourceId !== selectedNode.id\r\n        && graph.links.some(item => item.source === memoryGraphLinkSourceId && item.target === selectedNode.id && item.type === 'RELATED'));\r\n\r\n    editor.append('<div class=\"ai-wbr-memory-subtitle\"><b>节点编辑器</b></div>');\r\n\r\n    if (!selectedNode) {\r\n        editor.append('<div class=\"ai-wbr-token-empty\">点击上方图谱节点后，这里会显示该节点的可编辑信息。</div>');\r\n        return;\r\n    }\r\n\r\n    editor\r\n        .attr('data-memory-node-id', selectedNode.id)\r\n        .append(\r\n            $('<div class=\"ai-wbr-memory-node-editor-card\"></div>')\r\n                .append($('<div class=\"ai-wbr-memory-node-editor-title\"></div>').text(selectedNode.title || selectedNode.id))\r\n                .append($('<div class=\"ai-wbr-memory-node-editor-grid\"></div>')\r\n                    .append(\r\n                        $('<label class=\"ai-wbr-memory-node-editor-field\"></label>')\r\n                            .append('<span>标题</span>')\r\n                            .append($('<input class=\"text_pole\" type=\"text\" data-memory-editor-field=\"title\" />').val(selectedNode.title || '')),\r\n                    )\r\n                    .append(\r\n                        $('<label class=\"ai-wbr-memory-node-editor-field\"></label>')\r\n                            .append('<span>类型</span>')\r\n                            .append(createNodeTypeSelect('data-memory-editor-field', selectedNode.type || 'event')),\r\n                    )\r\n                    .append(\r\n                        $('<label class=\"ai-wbr-memory-node-editor-field ai-wbr-memory-node-editor-field-wide\"></label>')\r\n                            .append('<span>内容</span>')\r\n                            .append($('<textarea class=\"text_pole\" rows=\"4\" data-memory-editor-field=\"content\"></textarea>').val(selectedNode.content || '')),\r\n                    )\r\n                    .append(\r\n                        $('<label class=\"ai-wbr-memory-node-editor-field\"></label>')\r\n                            .append('<span>关键词</span>')\r\n                            .append($('<input class=\"text_pole\" type=\"text\" data-memory-editor-field=\"keys\" placeholder=\"用逗号分隔\" />').val((selectedNode.keys || []).join('，'))),\r\n                    )\r\n                    .append(\r\n                        $('<label class=\"ai-wbr-memory-node-editor-field\"></label>')\r\n                            .append('<span>标签</span>')\r\n                            .append($('<input class=\"text_pole\" type=\"text\" data-memory-editor-field=\"tags\" placeholder=\"用逗号分隔\" />').val((selectedNode.tags || []).join('，'))),\r\n                    ),\r\n                )\r\n                .append(\r\n                    $('<div class=\"ai-wbr-memory-popover-actions\"></div>')\r\n                        .append('<button class=\"menu_button ai-wbr-memory-editor-save\" type=\"button\">保存</button>')\r\n                        .append('<button class=\"menu_button ai-wbr-memory-editor-set-link-source\" type=\"button\">设为起点</button>')\r\n                        .append($(`<button class=\"menu_button ai-wbr-memory-editor-link-to-source\" type=\"button\" ${memoryGraphLinkSourceId && memoryGraphLinkSourceId !== selectedNode.id ? '' : 'disabled'}>${hasRelatedLink ? '取消连线' : '连接到起点'}${sourceTitle ? `：${escapeHtml(truncateText(sourceTitle, 10))}` : ''}</button>`))\r\n                        .append('<button class=\"menu_button ai-wbr-memory-editor-delete\" type=\"button\">删除</button>'),\r\n                ),\r\n        );\r\n}\r\n\r\nfunction renderMemoryEdgeEditor(graph) {\r\n    const editor = $('#ai_wbr_memory_edge_editor').empty();\r\n    const links = Array.isArray(graph?.links) ? graph.links : [];\r\n    const selectedLink = links.find(link => String(link.id) === String(memoryGraphSelectedLinkId)) || null;\r\n    editor.append('<div class=\"ai-wbr-memory-subtitle\"><b>关系编辑器</b></div>');\r\n\r\n    if (!selectedLink) {\r\n        editor.append('<div class=\"ai-wbr-token-empty\">点击图上的关系线，或下方关系卡片后，这里会显示该关系的可编辑信息。</div>');\r\n        return;\r\n    }\r\n\r\n    const sourceTitle = graph.nodes.find(node => node.id === selectedLink.source)?.title || selectedLink.source;\r\n    const targetTitle = graph.nodes.find(node => node.id === selectedLink.target)?.title || selectedLink.target;\r\n\r\n    editor.append(\r\n        $('<div class=\"ai-wbr-memory-node-editor-card\"></div>')\r\n            .attr('data-memory-link-id', selectedLink.id)\r\n            .append($('<div class=\"ai-wbr-memory-node-editor-title\"></div>').text(`${sourceTitle} → ${targetTitle}`))\r\n                .append($('<div class=\"ai-wbr-memory-node-editor-grid\"></div>')\r\n                    .append(\r\n                        $('<label class=\"ai-wbr-memory-node-editor-field\"></label>')\r\n                            .append('<span>关系类型</span>')\r\n                            .append(createLinkTypeSelect('data-memory-edge-field', selectedLink.type || 'RELATED')),\r\n                    )\r\n                .append(\r\n                    $('<label class=\"ai-wbr-memory-node-editor-field\"></label>')\r\n                        .append('<span>权重</span>')\r\n                        .append($('<input class=\"text_pole\" type=\"number\" min=\"0\" max=\"1\" step=\"0.05\" data-memory-edge-field=\"weight\" />').val(selectedLink.weight ?? 0.7)),\r\n                )\r\n                .append(\r\n                    $('<label class=\"ai-wbr-memory-node-editor-field ai-wbr-memory-node-editor-field-wide\"></label>')\r\n                        .append('<span>关系描述</span>')\r\n                        .append($('<textarea class=\"text_pole\" rows=\"3\" data-memory-edge-field=\"description\"></textarea>').val(selectedLink.description || '')),\r\n                ),\r\n            )\r\n            .append(\r\n                $('<div class=\"ai-wbr-memory-popover-actions\"></div>')\r\n                    .append('<button class=\"menu_button ai-wbr-memory-edge-save\" type=\"button\">保存</button>')\r\n                    .append('<button class=\"menu_button ai-wbr-memory-edge-delete\" type=\"button\">删除这条线</button>'),\r\n            ),\r\n    );\r\n}\r\n\r\nfunction bindMemoryPanelActions() {\r\n    const bookshelfSection = $('#ai_wbr_bookshelf_panel').closest('#ai_wbr_bookshelf_section, .ai-wbr-bookshelf-fold');\r\n    if (bookshelfSection.length) {\r\n        ensureBookshelfStandaloneControls(bookshelfSection);\r\n    }\r\n\r\n    bindCheckbox('#ai_wbr_memory_enabled', 'memoryEnabled');\r\n    bindCheckbox('#ai_wbr_memory_auto_run', 'memoryRealtimeEnabled');\r\n    bindCheckbox('#ai_wbr_memory_realtime_enabled', 'memoryRealtimeEnabled');\r\n    bindCheckbox('#ai_wbr_memory_summary_enabled', 'memorySummaryEnabled');\r\n    bindCheckbox('#ai_wbr_memory_inject_to_router', 'memoryInjectToRouter');\r\n    bindCheckbox('#ai_wbr_memory_review_required', 'memoryReviewRequired');\r\n    bindCheckbox('#ai_wbr_memory_debug', 'memoryDebug');\r\n    bindCheckbox('#ai_wbr_memory_scope_debug', 'memoryScopeDebug');\r\n    bindCheckbox('#ai_wbr_memory_history_skip_done', 'memoryHistorySkipDone');\r\n    bindCheckbox('#ai_wbr_bookshelf_enabled', 'bookshelfEnabled');\n    bindCheckbox('#ai_wbr_bookshelf_auto_inject', 'bookshelfAutoInject');\n    bindCheckbox('#ai_wbr_bookshelf_auto_memory_book', 'bookshelfAutoMemoryBook');\n    bindCheckbox('#ai_wbr_bookshelf_auto_memory_book_vectorize', 'bookshelfAutoMemoryBookVectorize');\n    bindCheckbox('#ai_wbr_bookshelf_memory_vector', 'bookshelfMemoryVectorRecall');\n    bindCheckbox('#ai_wbr_bookshelf_only_bound', 'bookshelfOnlyBound');\r\n    bindCheckbox('#ai_wbr_bookshelf_allow_global', 'bookshelfAllowGlobal');\r\n    bindNumber('#ai_wbr_memory_auto_run_interval', 'memorySummaryIntervalMessages', 1, 100);\r\n    bindNumber('#ai_wbr_memory_scan_messages', 'memorySummaryScanMessages', 2, 40);\r\n    bindNumber('#ai_wbr_memory_realtime_scan_messages', 'memoryRealtimeScanMessages', 2, 40);\r\n    bindNumber('#ai_wbr_memory_summary_interval_messages', 'memorySummaryIntervalMessages', 1, 100);\r\n    bindNumber('#ai_wbr_memory_summary_scan_messages', 'memorySummaryScanMessages', 2, 40);\r\n    bindNumber('#ai_wbr_memory_retries', 'memoryRetries', 0, 10);\r\n    bindNumber('#ai_wbr_memory_max_nodes', 'memoryMaxNodes', 5, 200);\r\n    bindNumber('#ai_wbr_memory_max_links', 'memoryMaxLinks', 0, 400);\r\n    bindNumber('#ai_wbr_bookshelf_memory_vector_max', 'bookshelfMemoryVectorMaxItems', 1, 12);\r\n    bindNumber('#ai_wbr_bookshelf_max_chunks', 'bookshelfMaxChunks', 1, 12);\r\n    bindNumber('#ai_wbr_bookshelf_max_chars', 'bookshelfMaxChunkChars', 120, 2000);\r\n    bindNumber('#ai_wbr_bookshelf_min_score', 'bookshelfMinScore', 0, 1);\r\n\r\n    $('#ai_wbr_bookshelf_api_url').val(settings.bookshelfApiUrl || '');\n    $('#ai_wbr_bookshelf_api_key').val(settings.bookshelfApiKey || '');\n    $('[id=\"ai_wbr_bookshelf_api_model\"]').val(settings.bookshelfApiModel || '');\n    bindText('#ai_wbr_bookshelf_local_model', 'bookshelfLocalModelId', (value) => String(value).trim());\r\n\r\n    $('#ai_wbr_bookshelf_embedding_mode')\r\n        .val(getBookshelfEmbeddingMode())\r\n        .on('change', function () {\r\n            saveSetting('bookshelfEmbeddingMode', String($(this).val() || 'api') === 'browser-local' ? 'browser-local' : 'api');\r\n            syncBookshelfProviderVisibility();\r\n            renderBookshelfPanel();\r\n        });\r\n\r\n    $('#ai_wbr_bookshelf_enabled, #ai_wbr_bookshelf_auto_inject, #ai_wbr_bookshelf_auto_memory_book, #ai_wbr_bookshelf_auto_memory_book_vectorize, #ai_wbr_bookshelf_memory_vector, #ai_wbr_bookshelf_only_bound, #ai_wbr_bookshelf_allow_global, #ai_wbr_bookshelf_memory_vector_max, #ai_wbr_bookshelf_max_chunks, #ai_wbr_bookshelf_max_chars, #ai_wbr_bookshelf_min_score, #ai_wbr_bookshelf_local_model')\n        .on('input change', () => {\n            syncBookshelfProviderVisibility();\n            renderBookshelfPanel();\n        });\n    bindBookshelfDynamicSettingsActions();\r\n    syncBookshelfProviderVisibility();\r\n\r\n    $('#ai_wbr_memory_history_mode')\r\n        .val(settings.memoryHistoryMode || 'history')\r\n        .on('change', function () {\r\n            const nextMode = String($(this).val() || 'history');\r\n            saveSetting('memoryHistoryMode', ['history', 'summary'].includes(nextMode) ? nextMode : 'history');\r\n        });\r\n\r\n    $('[data-ai-wbr-history-range]').on('click', function (event) {\r\n        event.preventDefault();\r\n        const context = getContext();\r\n        const messages = getAllMemoryMessages(Array.isArray(context?.chat) ? context.chat : []);\r\n        const maxFloor = messages.length ? Math.max(...messages.map(message => message.floor)) : 0;\r\n        const range = String($(this).data('aiWbrHistoryRange') || '');\r\n        if (!maxFloor) {\r\n            setHistoryImportRange(1, 1, context);\r\n            return;\r\n        }\r\n        if (range === 'all') {\r\n            setHistoryImportRange(1, maxFloor, context);\r\n            return;\r\n        }\r\n        const count = clampNumber(Number(range), 20, 1, 100000);\r\n        setHistoryImportRange(Math.max(1, maxFloor - count + 1), maxFloor, context);\r\n    });\r\n\r\n    $('#ai_wbr_memory_history_start_floor, #ai_wbr_memory_history_end_floor').on('input change', () => {\r\n        buildHistoryImportPreview(getHistoryImportRangeFromUi(), getContext());\r\n    });\r\n\r\n    $('#ai_wbr_memory_history_preview').on('click', (event) => {\r\n        event.preventDefault();\r\n        buildHistoryImportPreview(getHistoryImportRangeFromUi(), getContext());\r\n    });\r\n\r\n    $('#ai_wbr_memory_history_import').on('click', async (event) => {\r\n        event.preventDefault();\r\n        await runHistoryMemoryImport();\r\n    });\r\n\r\n    $('#ai_wbr_memory_run_now').on('click', async (event) => {\r\n        event.preventDefault();\r\n        await runMemoryGraphUpdate('manual', {\r\n            mode: 'realtime',\r\n            scanMessages: settings.memoryRealtimeScanMessages,\r\n            force: true,\r\n            review: false,\r\n        });\r\n    });\r\n\r\n    $('#ai_wbr_memory_summary_now').on('click', async (event) => {\r\n        event.preventDefault();\r\n        await runMemoryGraphUpdate('manual_summary', {\r\n            mode: 'summary',\r\n            scanMessages: settings.memorySummaryScanMessages,\r\n            force: true,\r\n            review: false,\r\n        });\r\n    });\r\n\r\n    $('#ai_wbr_memory_accept_all').on('click', (event) => {\r\n        event.preventDefault();\r\n        const accepted = acceptAllMemoryReviews();\r\n        if (accepted.count) {\r\n            setMemoryStatus(`已确认 ${accepted.count} 条记忆更新`);\r\n            toastr?.success?.('Done.', 'AI Worldbook Router');\r\n        }\r\n        renderMemoryPanel();\r\n    });\r\n\r\n    $('#ai_wbr_memory_reject_all').on('click', (event) => {\r\n        event.preventDefault();\r\n        const count = clearMemoryReviewQueue();\r\n        if (count) {\r\n            setMemoryStatus(`已清空 ${count} 条待确认更新`);\r\n            toastr?.info?.('Done.', 'AI Worldbook Router');\r\n        }\r\n        renderMemoryPanel();\r\n    });\r\n\r\n    $('#ai_wbr_memory_save_json').on('click', (event) => {\r\n        event.preventDefault();\r\n        try {\r\n            const parsed = JSON.parse(String($('#ai_wbr_memory_json').val() || '{}'));\r\n            const nextGraph = {\r\n                ...getDefaultMemoryGraph(),\r\n                ...parsed,\r\n            };\r\n            saveMemoryGraph(nextGraph, getContext(), false, { fit: true });\r\n            toastr.success('记忆 JSON 已保存', '世界书读取');\r\n        } catch (error) {\r\n            toastr.error(`JSON 解析失败：${error.message || error}`, '世界书读取');\r\n        }\r\n    });\r\n\r\n    $('#ai_wbr_memory_clear').on('click', (event) => {\r\n        event.preventDefault();\r\n        if (!confirm('确定清空当前轻量记忆图谱？')) {\r\n            return;\r\n        }\r\n        saveMemoryGraph(getDefaultMemoryGraph(), getContext(), false, { fit: true });\r\n        setCurrentMemoryLastTurnSignature('');\r\n        setCurrentMemoryLastPrompt('');\r\n        setCurrentMemoryLastRaw('');\r\n        setCurrentMemoryLastError('');\r\n        setMemoryStatus('已清空');\r\n    });\r\n\r\n    $(document).off('click.aiWbrBookshelfImport', '#ai_wbr_bookshelf_import, #ai_wbr_bookshelf_empty_import').on('click.aiWbrBookshelfImport', '#ai_wbr_bookshelf_import, #ai_wbr_bookshelf_empty_import', (event) => {\n        event.preventDefault();\n        const panel = $(event.currentTarget).closest('#ai_wbr_bookshelf_panel');\n        const fileInput = panel.find('#ai_wbr_bookshelf_file').first();\n        (fileInput[0] || $('#ai_wbr_bookshelf_file')[0])?.click?.();\n    });\n\r\n    $(document).off('click.aiWbrBookshelfDrawer', '#ai_wbr_bookshelf_open_settings, #ai_wbr_bookshelf_close_settings, #ai_wbr_bookshelf_settings_backdrop')\n        .on('click.aiWbrBookshelfDrawer', '#ai_wbr_bookshelf_open_settings', (event) => {\n            event.preventDefault();\n            const panel = $(event.currentTarget).closest('#ai_wbr_bookshelf_panel');\n            closeBookshelfBookActionSheet(panel);\n            panel.find('#ai_wbr_bookshelf_settings_drawer').addClass('open').attr('aria-hidden', 'false');\n            panel.find('#ai_wbr_bookshelf_settings_backdrop').addClass('open');\n        })\n        .on('click.aiWbrBookshelfDrawer', '#ai_wbr_bookshelf_close_settings, #ai_wbr_bookshelf_settings_backdrop', (event) => {\r\n            event.preventDefault();\r\n            const panel = $(event.currentTarget).closest('#ai_wbr_bookshelf_panel');\r\n            panel.find('#ai_wbr_bookshelf_settings_drawer').removeClass('open').attr('aria-hidden', 'true');\r\n            panel.find('#ai_wbr_bookshelf_settings_backdrop').removeClass('open');\n        });\n\n    $(document).off('click.aiWbrBookshelfBookActionsDrawer', '#ai_wbr_bookshelf_close_book_actions, #ai_wbr_bookshelf_book_actions_backdrop')\n        .on('click.aiWbrBookshelfBookActionsDrawer', '#ai_wbr_bookshelf_close_book_actions, #ai_wbr_bookshelf_book_actions_backdrop', (event) => {\n            event.preventDefault();\n            closeBookshelfBookActionSheet($(event.currentTarget).closest('#ai_wbr_bookshelf_panel'));\n        });\n\r\n    $(document).off('change.aiWbrBookshelfFile', '#ai_wbr_bookshelf_file').on('change.aiWbrBookshelfFile', '#ai_wbr_bookshelf_file', async function () {\r\n        try {\r\n            await importBookshelfFiles(this.files);\r\n        } catch (error) {\r\n            setBookshelfStatus(`导入失败：${error?.message || error}`);\r\n            toastr?.error?.('Operation failed.', 'AI Worldbook Router');\r\n        } finally {\r\n            this.value = '';\r\n        }\r\n    });\r\n\r\n    $(document).off('click.aiWbrBookshelfHistoryOpen', '#ai_wbr_bookshelf_history_open').on('click.aiWbrBookshelfHistoryOpen', '#ai_wbr_bookshelf_history_open', (event) => {\n        event.preventDefault();\n        const panel = $(event.currentTarget).closest('#ai_wbr_bookshelf_panel');\n        panel.find('#ai_wbr_bookshelf_test_panel').addClass('open');\n        setBookshelfStatus('浏览历史暂用召回测试面板展示，可输入剧情问题查看命中片段。');\n    });\n\n    $(document).off('click.aiWbrBookshelfTestOpen', '#ai_wbr_bookshelf_test_open').on('click.aiWbrBookshelfTestOpen', '#ai_wbr_bookshelf_test_open', (event) => {\n        event.preventDefault();\n        const panel = $(event.currentTarget).closest('#ai_wbr_bookshelf_panel');\n        panel.find('#ai_wbr_bookshelf_test_panel').toggleClass('open');\n    });\r\n\r\n    $(document).off('click.aiWbrBookshelfTest', '#ai_wbr_bookshelf_test').on('click.aiWbrBookshelfTest', '#ai_wbr_bookshelf_test', async (event) => {\r\n        event.preventDefault();\r\n        const panel = $(event.currentTarget).closest('#ai_wbr_bookshelf_panel');\r\n        const query = String(panel.find('#ai_wbr_bookshelf_test_query').val() || '').trim();\r\n        saveSetting('bookshelfLastTestQuery', query);\r\n        if (!query) {\r\n            setBookshelfStatus('请输入测试句子。');\r\n            return;\r\n        }\r\n        try {\r\n            setBookshelfStatus('正在测试统一向量召回...');\r\n            const context = getContext();\r\n            const memoryGraph = getMemoryGraph(context);\r\n            const result = await recallVectorMemoryAndBookshelf(query, memoryGraph, context, { force: true });\r\n            bookshelfLastTestResults = [\r\n                ...(result.selectedMemoryVectors || []),\r\n                ...(result.selectedBookshelf || []),\r\n            ];\r\n            const graphHits = result.selectedMemoryVectors?.length || 0;\r\n            const txtHits = result.selectedBookshelf?.length || 0;\r\n            const graphCandidates = result.memoryCandidates?.length || 0;\r\n            const txtCandidates = result.bookshelfCandidates?.length || 0;\r\n            setBookshelfStatus(`测试完成：图谱候选 ${graphCandidates} 条，命中 ${graphHits} 条；TXT 候选 ${txtCandidates} 条，命中 ${txtHits} 条。`);\r\n            renderBookshelfPanel();\r\n        } catch (error) {\r\n            bookshelfLastTestResults = [];\r\n            setBookshelfStatus(`测试失败：${error?.message || error}`);\r\n            renderBookshelfPanel();\r\n        }\r\n    });\r\n\r\n    $(document).off('click.aiWbrBookshelfProvider', '#ai_wbr_bookshelf_test_provider').on('click.aiWbrBookshelfProvider', '#ai_wbr_bookshelf_test_provider', async (event) => {\r\n        event.preventDefault();\r\n        try {\r\n            setBookshelfModelStatus('正在测试...');\r\n            const vector = await embedBookshelfText('测试书架向量模型连接。');\r\n            setBookshelfModelStatus(`可用，维度 ${vector.length}`);\r\n            toastr?.success?.('Done.', 'AI Worldbook Router');\r\n        } catch (error) {\r\n            setBookshelfModelStatus(`失败：${error?.message || error}`);\r\n            toastr?.error?.('Operation failed.', 'AI Worldbook Router');\r\n        }\r\n    });\r\n\r\n    $(document).off('click.aiWbrBookshelfFetchModels', '#ai_wbr_bookshelf_fetch_models').on('click.aiWbrBookshelfFetchModels', '#ai_wbr_bookshelf_fetch_models', async (event) => {\n        event.preventDefault();\n        const button = $(event.currentTarget);\n        const originalText = button.text();\n        try {\n            button.prop('disabled', true).text('获取中...');\n            setBookshelfModelStatus('正在获取模型...');\n            setBookshelfStatus('正在获取书架向量模型列表...');\n            saveSetting('bookshelfEmbeddingMode', 'api');\n            $('#ai_wbr_bookshelf_embedding_mode').val('api');\n            await fetchBookshelfApiModels();\n            syncBookshelfProviderVisibility();\n        } catch (error) {\n            const message = `获取失败：${error?.message || error}。可直接在 API 模型框手动填写模型名。`;\n            setBookshelfModelStatus(message);\n            setBookshelfStatus(`向量模型${message}`);\n            toastr?.error?.(message, 'AI Worldbook Router');\n        } finally {\n            button.prop('disabled', false).text(originalText || '获取模型');\n        }\n    });\n\r\n    $(document).off('click.aiWbrBookshelfLocal', '#ai_wbr_bookshelf_load_local').on('click.aiWbrBookshelfLocal', '#ai_wbr_bookshelf_load_local', async (event) => {\r\n        event.preventDefault();\r\n        try {\r\n            saveSetting('bookshelfEmbeddingMode', 'browser-local');\r\n            $('#ai_wbr_bookshelf_embedding_mode').val('browser-local');\r\n            syncBookshelfProviderVisibility();\r\n            setBookshelfModelStatus('正在下载/加载本地模型...');\r\n            await getBookshelfLocalEmbeddingPipeline();\r\n            setBookshelfModelStatus('本地模型已缓存并可用');\r\n            toastr?.success?.('Done.', 'AI Worldbook Router');\r\n        } catch (error) {\r\n            setBookshelfModelStatus(`失败：${error?.message || error}`);\r\n            toastr?.error?.('Operation failed.', 'AI Worldbook Router');\r\n        }\r\n    });\r\n\r\n    $(document).off('click.aiWbrBookshelfMemoryVectorize', '#ai_wbr_bookshelf_vectorize_memory').on('click.aiWbrBookshelfMemoryVectorize', '#ai_wbr_bookshelf_vectorize_memory', async (event) => {\r\n        event.preventDefault();\r\n        try {\r\n            setBookshelfStatus('正在同步图谱记忆向量...');\r\n            const result = await syncMemoryGraphVectors(getMemoryGraph(), getContext(), { force: true });\r\n            setBookshelfStatus(`图谱向量同步完成：节点 ${result.total} 个，可用 ${result.ready} 个，失败 ${result.failed} 个。`);\r\n            toastr?.success?.('Done.', 'AI Worldbook Router');\r\n            renderBookshelfPanel();\r\n        } catch (error) {\r\n            setBookshelfStatus(`图谱向量同步失败：${error?.message || error}`);\r\n            toastr?.error?.('Operation failed.', 'AI Worldbook Router');\r\n            renderBookshelfPanel();\r\n        }\r\n    });\r\n\r\n    $(document).off('click.aiWbrBookshelfBuildMemoryBooks', '#ai_wbr_bookshelf_build_memory_books').on('click.aiWbrBookshelfBuildMemoryBooks', '#ai_wbr_bookshelf_build_memory_books', async (event) => {\n        event.preventDefault();\n        try {\n            setBookshelfStatus('正在从图谱拆分并生成自动记忆书...');\n            const autoVectorize = !!settings.bookshelfAutoMemoryBookVectorize;\n            const result = await syncMemoryGraphBookshelfBooks(getMemoryGraph(), getContext(), { vectorize: autoVectorize });\n            selectedBookshelfBookId = result.bookIds?.[0] || selectedBookshelfBookId;\n            bookshelfLastTestResults = [];\n            if (autoVectorize) {\n                setBookshelfStatus(`自动记忆书已生成并自动向量化：${result.books} 本，${result.chunks} 个摘要片段；已替换旧书 ${result.deleted} 本。`);\n            } else {\n                setBookshelfStatus(`自动记忆书已生成：${result.books} 本，${result.chunks} 个摘要片段；配置向量模型后点击书籍“开始向量化”。`);\n            }\n            toastr?.success?.('Memory books generated.', 'AI Worldbook Router');\r\n            renderBookshelfPanel();\r\n        } catch (error) {\r\n            setBookshelfStatus(`生成记忆书失败：${error?.message || error}`);\r\n            toastr?.error?.('Operation failed.', 'AI Worldbook Router');\r\n            renderBookshelfPanel();\n        }\n    });\n\n    $(document).off('click.aiWbrBookshelfBuildMemoryBooksQuick', '#ai_wbr_bookshelf_build_memory_books_quick').on('click.aiWbrBookshelfBuildMemoryBooksQuick', '#ai_wbr_bookshelf_build_memory_books_quick', (event) => {\n        event.preventDefault();\n        $('#ai_wbr_bookshelf_build_memory_books').first().trigger('click');\n    });\n\r\n    $(document).off('click.aiWbrBookshelfMemoryReset', '#ai_wbr_bookshelf_reset_memory_vectors').on('click.aiWbrBookshelfMemoryReset', '#ai_wbr_bookshelf_reset_memory_vectors', async (event) => {\r\n        event.preventDefault();\r\n        if (!confirm('确定重置当前聊天的图谱记忆向量索引？不会删除图谱本身。')) return;\r\n        try {\r\n            await bookshelfDeleteMemoryVectorsForChat(getCurrentChatMemoryKey());\r\n            setBookshelfStatus('当前聊天的图谱记忆向量已重置。');\r\n            bookshelfLastTestResults = [];\r\n            renderBookshelfPanel();\r\n        } catch (error) {\r\n            setBookshelfStatus(`图谱向量重置失败：${error?.message || error}`);\r\n            toastr?.error?.('Operation failed.', 'AI Worldbook Router');\r\n        }\r\n    });\r\n    function getBookshelfActionBookId(button) {\r\n        return String(\r\n            $(button).data('bookshelfBookId')\r\n            || $(button).closest('[data-bookshelf-book-id]').data('bookshelfBookId')\r\n            || selectedBookshelfBookId\r\n            || ''\r\n        );\r\n    }\r\n\r\n    $(document)\n        .off('.aiWbrBookshelf')\n        .on('pointerdown.aiWbrBookshelf', '.ai-wbr-bookshelf-book', function (event) {\n            if ($(event.target).closest('button, input, textarea, select').length && !$(event.target).closest('.ai-wbr-bookshelf-cover').length) return;\n            clearTimeout(bookshelfLongPressTimer);\n            bookshelfSuppressNextBookClick = false;\n            const bookId = String($(this).data('bookshelfBookId') || '');\n            bookshelfLongPressTimer = setTimeout(() => {\n                bookshelfSuppressNextBookClick = true;\n                openBookshelfBookActionSheet(bookId);\n            }, 520);\n        })\n        .on('pointerup.aiWbrBookshelf pointercancel.aiWbrBookshelf pointerleave.aiWbrBookshelf', '.ai-wbr-bookshelf-book', function () {\n            clearTimeout(bookshelfLongPressTimer);\n        })\n        .on('contextmenu.aiWbrBookshelf', '.ai-wbr-bookshelf-book', function (event) {\n            event.preventDefault();\n            clearTimeout(bookshelfLongPressTimer);\n            bookshelfSuppressNextBookClick = true;\n            openBookshelfBookActionSheet(String($(this).data('bookshelfBookId') || ''));\n        })\n        .on('click.aiWbrBookshelf', '.ai-wbr-bookshelf-book', async function (event) {\n            if ($(event.target).closest('button').length && !$(event.target).closest('.ai-wbr-bookshelf-cover').length) return;\n            event.preventDefault();\n            if (bookshelfSuppressNextBookClick) {\n                bookshelfSuppressNextBookClick = false;\n                return;\n            }\n            selectedBookshelfBookId = String($(this).data('bookshelfBookId') || '');\n            closeBookshelfBookActionSheet($(this).closest('#ai_wbr_bookshelf_panel'));\n            await openBookshelfNovelReader(selectedBookshelfBookId);\n        })\n        .on('click.aiWbrBookshelf', '.ai-wbr-bookshelf-select', async function (event) {\n            event.preventDefault();\n            event.stopPropagation();\n            if (bookshelfSuppressNextBookClick) {\n                bookshelfSuppressNextBookClick = false;\n                return;\n            }\n            selectedBookshelfBookId = String($(this).closest('[data-bookshelf-book-id]').data('bookshelfBookId') || '');\n            closeBookshelfBookActionSheet($(this).closest('#ai_wbr_bookshelf_panel'));\n            await openBookshelfNovelReader(selectedBookshelfBookId);\n        })\n        .on('click.aiWbrBookshelf', '.ai-wbr-bookshelf-open-reader', async function (event) {\n            event.preventDefault();\n            event.stopPropagation();\n            const bookId = getBookshelfActionBookId(this);\n            closeBookshelfBookActionSheet($(this).closest('#ai_wbr_bookshelf_panel'));\n            await openBookshelfNovelReader(bookId);\n        })\n        .on('click.aiWbrBookshelf', '.ai-wbr-bookshelf-reader-close, .ai-wbr-bookshelf-reader-overlay', function (event) {\n            if ($(event.target).closest('.ai-wbr-bookshelf-reader-shell').length && !$(event.target).closest('.ai-wbr-bookshelf-reader-close').length) return;\n            event.preventDefault();\n            $('.ai-wbr-bookshelf-reader-overlay').remove();\n        })\n        .on('click.aiWbrBookshelf', '.ai-wbr-bookshelf-reader-more', function (event) {\n            event.preventDefault();\n            event.stopPropagation();\n            $(this).closest('.ai-wbr-bookshelf-reader-menu-wrap').toggleClass('open');\n        })\n        .on('click.aiWbrBookshelf', '.ai-wbr-bookshelf-reader-menu-action', function (event) {\n            const action = String($(this).data('readerAction') || '');\n            const shell = $(this).closest('.ai-wbr-bookshelf-reader-shell');\n            const menu = $(this).closest('.ai-wbr-bookshelf-reader-menu-wrap');\n            if (action === 'close') return;\n            event.preventDefault();\n            event.stopPropagation();\n            menu.removeClass('open');\n            if (action === 'toc') {\n                shell.find('.ai-wbr-bookshelf-reader-toc').first().trigger('focus');\n                return;\n            }\n            if (action === 'content') {\n                const pages = shell.find('.ai-wbr-bookshelf-reader-pages');\n                pages.stop(true).animate({ scrollTop: 0 }, 160);\n            }\n        })\n        .on('click.aiWbrBookshelf', '.ai-wbr-bookshelf-reader-toc-item', function (event) {\r\n            event.preventDefault();\r\n            const button = $(this);\r\n            const chapterTitle = String(button.data('bookshelfChapterTitle') || '');\r\n            const shell = button.closest('.ai-wbr-bookshelf-reader-shell');\r\n            const pages = shell.find('.ai-wbr-bookshelf-reader-pages');\r\n            const target = pages.find('.ai-wbr-bookshelf-reader-page').filter(function () {\r\n                return String($(this).data('bookshelfChapterTitle') || '') === chapterTitle;\r\n            }).first();\r\n            button.siblings('.ai-wbr-bookshelf-reader-toc-item').removeClass('active');\r\n            button.addClass('active');\r\n            if (target.length && pages.length) {\r\n                pages.stop(true).animate({ scrollTop: pages.scrollTop() + target.position().top - 8 }, 160);\r\n            }\r\n        })\r\n        .on('click.aiWbrBookshelf', '.ai-wbr-bookshelf-toc-item', function (event) {\r\n            event.preventDefault();\r\n            const button = $(this);\r\n            const chapterTitle = String(button.data('bookshelfChapterTitle') || '');\r\n            const detail = button.closest('.ai-wbr-bookshelf-selected-detail');\r\n            const pages = detail.find('.ai-wbr-bookshelf-novel-pages');\r\n            const target = pages.find('.ai-wbr-bookshelf-page').filter(function () {\r\n                return String($(this).data('bookshelfChapterTitle') || '') === chapterTitle;\r\n            }).first();\r\n            button.siblings('.ai-wbr-bookshelf-toc-item').removeClass('active');\r\n            button.addClass('active');\r\n            if (target.length && pages.length) {\r\n                pages.stop(true).animate({ scrollTop: pages.scrollTop() + target.position().top - 8 }, 160);\r\n            }\r\n        })\r\n        .on('click.aiWbrBookshelf', '.ai-wbr-bookshelf-toggle', async function (event) {\n            event.preventDefault();\n            const bookId = getBookshelfActionBookId(this);\n            const book = await bookshelfGet('books', bookId);\n            if (!book) return;\n            closeBookshelfBookActionSheet($(this).closest('#ai_wbr_bookshelf_panel'));\n            await updateBookshelfBook(bookId, { enabled: book.enabled === false });\n            renderBookshelfPanel();\n        })\n        .on('click.aiWbrBookshelf', '.ai-wbr-bookshelf-rebuild', async function (event) {\n            event.preventDefault();\n            const bookId = getBookshelfActionBookId(this);\n            closeBookshelfBookActionSheet($(this).closest('#ai_wbr_bookshelf_panel'));\n            try {\n                await rebuildBookshelfBookVectors(bookId);\n            } catch (error) {\n                setBookshelfStatus(`向量化失败：${error?.message || error}`);\n            }\n        })\n        .on('click.aiWbrBookshelf', '.ai-wbr-bookshelf-clear-vector', async function (event) {\n            event.preventDefault();\n            const bookId = getBookshelfActionBookId(this);\n            if (!bookId || !confirm('确定重置这本书的所有向量并回到待向量化？')) return;\n            closeBookshelfBookActionSheet($(this).closest('#ai_wbr_bookshelf_panel'));\n            await clearBookshelfBookVectors(bookId);\n        })\n        .on('click.aiWbrBookshelf', '.ai-wbr-bookshelf-delete', async function (event) {\n            event.preventDefault();\n            event.stopPropagation();\n            const bookId = getBookshelfActionBookId(this);\n            if (!bookId) return;\n            closeBookshelfBookActionSheet($(this).closest('#ai_wbr_bookshelf_panel'));\n            setBookshelfStatus('\\u6b63\\u5728\\u5220\\u9664\\u4e66\\u7c4d...');\n            await bookshelfDeleteBook(bookId);\n            if (selectedBookshelfBookId === bookId) selectedBookshelfBookId = '';\r\n            bookshelfLastTestResults = [];\r\n            setBookshelfStatus('\\u4e66\\u7c4d\\u5df2\\u5220\\u9664\\u3002');\r\n            renderBookshelfPanel();\r\n        })\r\n        .on('click.aiWbrBookshelf', '.ai-wbr-bookshelf-bind-character', async function (event) {\n            event.preventDefault();\n            const bookId = getBookshelfActionBookId(this);\n            const book = await bookshelfGet('books', bookId);\n            const scope = getBookshelfScope();\n            closeBookshelfBookActionSheet($(this).closest('#ai_wbr_bookshelf_panel'));\n            if (isBookshelfBookBound(book, 'character', scope.characterKey)) {\n                await removeBookshelfBinding(bookId, 'character');\n            } else {\n                await setBookshelfBinding(bookId, 'character');\r\n            }\r\n        })\r\n        .on('click.aiWbrBookshelf', '.ai-wbr-bookshelf-bind-chat', async function (event) {\n            event.preventDefault();\n            const bookId = getBookshelfActionBookId(this);\n            const book = await bookshelfGet('books', bookId);\n            const scope = getBookshelfScope();\n            closeBookshelfBookActionSheet($(this).closest('#ai_wbr_bookshelf_panel'));\n            if (isBookshelfBookBound(book, 'chat', scope.chatKey)) {\n                await removeBookshelfBinding(bookId, 'chat');\n            } else {\n                await setBookshelfBinding(bookId, 'chat');\r\n            }\r\n        })\r\n        .on('click.aiWbrBookshelf', '.ai-wbr-bookshelf-bind-global', async function (event) {\n            event.preventDefault();\n            const bookId = getBookshelfActionBookId(this);\n            const book = await bookshelfGet('books', bookId);\n            closeBookshelfBookActionSheet($(this).closest('#ai_wbr_bookshelf_panel'));\n            if (isBookshelfBookBound(book, 'global', 'global')) {\n                await removeBookshelfBinding(bookId, 'global');\n            } else {\n                await setBookshelfBinding(bookId, 'global');\r\n            }\r\n        })\r\n        .on('click.aiWbrBookshelf', '.ai-wbr-bookshelf-clear-bindings', async function (event) {\n            event.preventDefault();\n            const bookId = getBookshelfActionBookId(this);\n            closeBookshelfBookActionSheet($(this).closest('#ai_wbr_bookshelf_panel'));\n            await clearBookshelfBindings(bookId);\n        })\n        .on('click.aiWbrBookshelf', '.ai-wbr-bookshelf-world-toggle', function (event) {\n            event.preventDefault();\n            const worldName = String($(this).data('worldName') || '').trim();\n            if (!worldName) return;\n            closeBookshelfBookActionSheet($(this).closest('#ai_wbr_bookshelf_panel'));\n            setWorldSelectionState(worldName, !getWorldSelectionState(worldName));\n            renderBookshelfPanel();\n        });\n\r\n    $(document)\r\n        .off('.aiWbrGraphWorkspace')\r\n        .on('click.aiWbrGraphWorkspace', '#ai_wbr_memory_graph_fit, .ai-wbr-graph-fit-trigger', (event) => {\r\n            event.preventDefault();\r\n            const graph = getMemoryGraph();\r\n            fitMemoryGraphToContainer(graph);\r\n            renderMemoryGraphSvg(graph);\r\n        })\r\n        .on('click.aiWbrGraphWorkspace', '#ai_wbr_memory_graph_fullscreen, .ai-wbr-memory-open-fullscreen', (event) => {\n            event.preventDefault();\n            event.stopPropagation();\n            setMemoryGraphFullscreen(!memoryGraphFullscreenActive);\n        })\n        .on('click.aiWbrGraphWorkspace', '#ai_wbr_memory_graph_close_fullscreen', (event) => {\n            event.preventDefault();\n            event.stopPropagation();\n            setMemoryGraphFullscreen(false, { fit: false });\n            if (typeof globalThis.aiWbrCloseConsole === 'function') {\n                globalThis.aiWbrCloseConsole();\n            }\n        })\n        .on('click.aiWbrGraphWorkspace', '#ai_wbr_memory_graph_close_detail, .ai-wbr-memory-detail-close', (event) => {\n            event.preventDefault();\n            memoryGraphSelectedNodeId = '';\n            memoryGraphSelectedLinkId = '';\r\n            memoryGraphDetailMode = '';\r\n            renderMemoryPanel('graph');\r\n        })\r\n        .on('click.aiWbrGraphWorkspace', '.ai-wbr-memory-detail-related-link', function (event) {\r\n            event.preventDefault();\r\n            memoryGraphSelectedLinkId = String($(this).data('memoryLinkId') || '');\r\n            memoryGraphSelectedNodeId = '';\r\n            memoryGraphDetailMode = 'link';\r\n            renderMemoryPanel('graph');\r\n        })\r\n        .on('click.aiWbrGraphWorkspace', '.ai-wbr-memory-detail-set-link-source', function (event) {\r\n            event.preventDefault();\r\n            memoryGraphLinkSourceId = String($(this).data('memoryNodeId') || '');\r\n            renderMemoryPanel('graph');\r\n        })\r\n        .on('click.aiWbrGraphWorkspace', '.ai-wbr-memory-detail-link-to-source', function (event) {\r\n            event.preventDefault();\r\n            const targetId = String($(this).data('memoryNodeId') || '');\r\n            if (!memoryGraphLinkSourceId || memoryGraphLinkSourceId === targetId) {\r\n                return;\r\n            }\r\n            const graph = getMemoryGraph();\r\n            const existingIndex = graph.links.findIndex(item => item.source === memoryGraphLinkSourceId && item.target === targetId && item.type === 'RELATED');\r\n            if (existingIndex >= 0) {\r\n                graph.links.splice(existingIndex, 1);\r\n            } else {\r\n                const link = normalizeMemoryLink({\r\n                    source: memoryGraphLinkSourceId,\r\n                    target: targetId,\r\n                    type: 'RELATED',\r\n                    weight: 0.7,\r\n                    description: '手动创建的记忆关系',\r\n                }, new Set(graph.nodes.map(node => node.id)), graph.links.length);\r\n                if (link) {\r\n                    graph.links.push(link);\r\n                }\r\n            }\r\n            memoryGraphLinkSourceId = '';\r\n            graph.updatedAt = new Date().toISOString();\r\n            saveMemoryGraph(graph, getContext(), false, { fit: true });\r\n        })\r\n        .on('click.aiWbrGraphWorkspace', '.ai-wbr-memory-detail-delete-node', function (event) {\r\n            event.preventDefault();\r\n            const nodeId = String($(this).data('memoryNodeId') || '');\r\n            const graph = getMemoryGraph();\r\n            graph.nodes = graph.nodes.filter(node => node.id !== nodeId);\r\n            graph.links = graph.links.filter(link => link.source !== nodeId && link.target !== nodeId);\r\n            memoryGraphSelectedNodeId = '';\r\n            memoryGraphDetailMode = '';\r\n            graph.updatedAt = new Date().toISOString();\r\n            saveMemoryGraph(graph, getContext(), false, { fit: true });\r\n        })\r\n        .on('click.aiWbrGraphWorkspace', '.ai-wbr-memory-detail-delete-link', function (event) {\r\n            event.preventDefault();\r\n            const linkId = String($(this).data('memoryLinkId') || '');\r\n            const graph = getMemoryGraph();\r\n            graph.links = graph.links.filter(link => String(link.id) !== linkId);\r\n            memoryGraphSelectedLinkId = '';\r\n            memoryGraphDetailMode = '';\r\n            graph.updatedAt = new Date().toISOString();\r\n            saveMemoryGraph(graph, getContext(), false, { fit: true });\r\n        });\r\n\r\n    $('#ai_worldbook_router_settings')\r\n        .on('click', '#ai_wbr_memory_node_popover, #ai_wbr_memory_node_popover *', function (event) {\r\n            event.stopPropagation();\r\n        })\r\n        .on('click', '.ai-wbr-memory-review-accept', function () {\r\n            const id = String($(this).closest('[data-memory-review-id]').data('memoryReviewId') || '');\r\n            const result = acceptMemoryReviewItem(id);\r\n            if (result) {\r\n                setMemoryStatus(`已确认：${result.graph.nodes.length} 节点 / ${result.graph.links.length} 关系`);\r\n                toastr?.success?.('Done.', 'AI Worldbook Router');\r\n            }\r\n            renderMemoryPanel();\r\n        })\r\n        .on('click', '.ai-wbr-memory-review-reject', function () {\r\n            const id = String($(this).closest('[data-memory-review-id]').data('memoryReviewId') || '');\r\n            rejectMemoryReviewItem(id);\r\n            setMemoryStatus('已忽略 1 条待确认更新');\r\n            renderMemoryPanel();\r\n        })\r\n        .on('input change', '[data-memory-state-field]', function () {\r\n            const graph = getMemoryGraph();\r\n            const field = String($(this).data('memoryStateField'));\r\n            const value = String($(this).val() || '').trim();\r\n            if (field === 'active_topics' || field === 'open_questions') {\r\n                graph.state[field] = uniqueStrings(value.split(/[,\\n，、]+/u)).slice(0, 12);\r\n            } else if (field.startsWith('custom_')) {\r\n                graph.state.custom_values = graph.state.custom_values && typeof graph.state.custom_values === 'object'\r\n                    ? graph.state.custom_values\r\n                    : {};\r\n                graph.state.custom_values[field] = truncateText(value, 220);\r\n            } else {\r\n                graph.state[field] = value;\r\n            }\r\n            graph.updatedAt = new Date().toISOString();\r\n            saveMemoryGraph(graph);\r\n        })\r\n        .on('click', '#ai_wbr_memory_add_state_definition', function () {\r\n            const graph = getMemoryGraph();\r\n            const label = String($('#ai_wbr_memory_new_state_label').val() || '').trim();\r\n            const instruction = String($('#ai_wbr_memory_new_state_instruction').val() || '').trim();\r\n            if (!label) {\r\n                toastr.warning('请先填写字段名', '世界书读取');\r\n                return;\r\n            }\r\n            const definition = normalizeMemoryStateDefinition({ label, instruction }, getCustomMemoryStateDefinitions(graph).length);\r\n            graph.stateDefinitions = getCustomMemoryStateDefinitions(graph);\r\n            if (graph.stateDefinitions.some(item => item.key === definition.key || item.label === definition.label)) {\r\n                toastr.warning('已存在同名或同 key 的状态字段', '世界书读取');\r\n                return;\r\n            }\r\n            graph.stateDefinitions.push(definition);\r\n            graph.state.custom_values = graph.state.custom_values && typeof graph.state.custom_values === 'object'\r\n                ? graph.state.custom_values\r\n                : {};\r\n            graph.state.custom_values[definition.key] = '';\r\n            graph.updatedAt = new Date().toISOString();\r\n            saveMemoryGraph(graph);\r\n            $('#ai_wbr_memory_new_state_label').val('');\r\n            $('#ai_wbr_memory_new_state_instruction').val('');\r\n        })\r\n        .on('click', '.ai-wbr-memory-delete-state-definition', function () {\r\n            const graph = getMemoryGraph();\r\n            const key = String($(this).data('memoryStateDefinitionKey') || '');\r\n            graph.stateDefinitions = getCustomMemoryStateDefinitions(graph).filter(item => item.key !== key);\r\n            if (graph.state.custom_values && typeof graph.state.custom_values === 'object') {\r\n                delete graph.state.custom_values[key];\r\n            }\r\n            graph.updatedAt = new Date().toISOString();\r\n            saveMemoryGraph(graph);\r\n        })\r\n        .on('input change', '[data-memory-node-field]', function () {\r\n            const graph = getMemoryGraph();\r\n            const card = $(this).closest('[data-memory-node-id]');\r\n            const node = graph.nodes.find(item => item.id === String(card.data('memoryNodeId')));\r\n            if (!node) {\r\n                return;\r\n            }\r\n            const field = String($(this).data('memoryNodeField'));\r\n            const value = String($(this).val() || '');\r\n            if (field === 'tags') {\r\n                node.tags = uniqueStrings(value.split(/[,\\n，、]+/u));\r\n            } else if (field === 'keys') {\r\n                node.keys = uniqueStrings(value.split(/[,\\n，、]+/u));\r\n            } else if (field === 'title' || field === 'type' || field === 'content') {\r\n                node[field] = value;\r\n            } else if (field === 'summary' || field === 'location' || field === 'timeSpan') {\r\n                node[field] = value.trim();\r\n            }\r\n            node.updatedAt = new Date().toISOString();\r\n            graph.updatedAt = node.updatedAt;\r\n            saveMemoryGraph(graph);\r\n        })\r\n        .on('input change', '[data-memory-link-field]', function () {\r\n            const graph = getMemoryGraph();\r\n            const card = $(this).closest('[data-memory-link-id]');\r\n            const link = graph.links.find(item => String(item.id) === String(card.data('memoryLinkId')));\r\n            if (!link) {\r\n                return;\r\n            }\r\n            const field = String($(this).data('memoryLinkField'));\r\n            if (field === 'weight') {\r\n                link.weight = clampNumber($(this).val(), link.weight || 0.7, 0, 1);\r\n            } else {\r\n                link[field] = String($(this).val() || '');\r\n            }\r\n            link.updatedAt = new Date().toISOString();\r\n            graph.updatedAt = link.updatedAt;\r\n            saveMemoryGraph(graph);\r\n        })\r\n        .on('click', '.ai-wbr-memory-link-card', function (event) {\r\n            if ($(event.target).closest('input, textarea, button, select').length) {\r\n                return;\r\n            }\r\n            memoryGraphSelectedLinkId = String($(this).data('memoryLinkId') || '');\r\n            renderMemoryPanel();\r\n        })\r\n        .on('input change', '[data-memory-edge-field]', function () {\r\n            const graph = getMemoryGraph();\r\n            const link = graph.links.find(item => String(item.id) === String(memoryGraphSelectedLinkId));\r\n            if (!link) {\r\n                return;\r\n            }\r\n            const field = String($(this).data('memoryEdgeField'));\r\n            if (field === 'weight') {\r\n                link.weight = clampNumber($(this).val(), link.weight || 0.7, 0, 1);\r\n            } else {\r\n                link[field] = String($(this).val() || '');\r\n            }\r\n            link.updatedAt = new Date().toISOString();\r\n            graph.updatedAt = link.updatedAt;\r\n            saveMemoryGraph(graph);\r\n        })\r\n        .on('click', '.ai-wbr-memory-edge-save', function () {\r\n            const graph = getMemoryGraph();\r\n            const link = graph.links.find(item => String(item.id) === String(memoryGraphSelectedLinkId));\r\n            if (!link) {\r\n                return;\r\n            }\r\n            link.updatedAt = new Date().toISOString();\r\n            graph.updatedAt = link.updatedAt;\r\n            saveMemoryGraph(graph);\r\n            toastr.success('关系已保存', '世界书读取');\r\n        })\r\n        .on('click', '.ai-wbr-memory-edge-delete', function () {\r\n            const graph = getMemoryGraph();\r\n            const id = String(memoryGraphSelectedLinkId || '');\r\n            graph.links = graph.links.filter(link => String(link.id) !== id);\r\n            memoryGraphSelectedLinkId = '';\r\n            graph.updatedAt = new Date().toISOString();\r\n            saveMemoryGraph(graph);\r\n        })\r\n        .on('input change', '[data-memory-editor-field]', function () {\r\n            const graph = getMemoryGraph();\r\n            const node = graph.nodes.find(item => item.id === memoryGraphSelectedNodeId);\r\n            if (!node) {\r\n                return;\r\n            }\r\n            const field = String($(this).data('memoryEditorField'));\r\n            const value = String($(this).val() || '');\r\n            if (field === 'tags') {\r\n                node.tags = uniqueStrings(value.split(/[,\\n，、]+/u));\r\n            } else if (field === 'keys') {\r\n                node.keys = uniqueStrings(value.split(/[,\\n，、]+/u));\r\n            } else {\r\n                node[field] = value;\r\n            }\r\n            node.updatedAt = new Date().toISOString();\r\n            graph.updatedAt = node.updatedAt;\r\n            saveMemoryGraph(graph);\r\n        })\r\n        .on('click', '.ai-wbr-memory-editor-save', function () {\r\n            const graph = getMemoryGraph();\r\n            const node = graph.nodes.find(item => item.id === memoryGraphSelectedNodeId);\r\n            if (!node) {\r\n                return;\r\n            }\r\n            node.updatedAt = new Date().toISOString();\r\n            graph.updatedAt = node.updatedAt;\r\n            saveMemoryGraph(graph);\r\n            toastr.success('节点已保存', '世界书读取');\r\n        })\r\n        .on('click', '.ai-wbr-memory-editor-set-link-source', function () {\r\n            if (!memoryGraphSelectedNodeId) {\r\n                return;\r\n            }\r\n            memoryGraphLinkSourceId = memoryGraphSelectedNodeId;\r\n            renderMemoryPanel();\r\n        })\r\n        .on('click', '.ai-wbr-memory-editor-link-to-source', function () {\r\n            const graph = getMemoryGraph();\r\n            const targetId = memoryGraphSelectedNodeId;\r\n            if (!memoryGraphLinkSourceId || !targetId || memoryGraphLinkSourceId === targetId) {\r\n                return;\r\n            }\r\n            const existingIndex = graph.links.findIndex(item => item.source === memoryGraphLinkSourceId && item.target === targetId && item.type === 'RELATED');\r\n            if (existingIndex >= 0) {\r\n                graph.links.splice(existingIndex, 1);\r\n                graph.updatedAt = new Date().toISOString();\r\n                saveMemoryGraph(graph, getContext(), false, { fit: true });\r\n                memoryGraphLinkSourceId = '';\r\n                renderMemoryPanel();\r\n                return;\r\n            }\r\n            const link = normalizeMemoryLink({\r\n                source: memoryGraphLinkSourceId,\r\n                target: targetId,\r\n                type: 'RELATED',\r\n                weight: 0.7,\r\n                description: '手动创建的记忆关系',\r\n            }, new Set(graph.nodes.map(node => node.id)), graph.links.length);\r\n            if (link && !graph.links.some(item => item.source === link.source && item.target === link.target && item.type === link.type)) {\r\n                graph.links.push(link);\r\n                graph.updatedAt = new Date().toISOString();\r\n                saveMemoryGraph(graph, getContext(), false, { fit: true });\r\n            }\r\n            memoryGraphLinkSourceId = '';\r\n            renderMemoryPanel();\r\n        })\r\n        .on('click', '.ai-wbr-memory-editor-delete', function () {\r\n            const graph = getMemoryGraph();\r\n            const id = memoryGraphSelectedNodeId;\r\n            if (!id) {\r\n                return;\r\n            }\r\n            graph.nodes = graph.nodes.filter(node => node.id !== id);\r\n            graph.links = graph.links.filter(link => link.source !== id && link.target !== id);\r\n            if (memoryGraphLinkSourceId === id) {\r\n                memoryGraphLinkSourceId = '';\r\n            }\r\n            memoryGraphSelectedNodeId = '';\r\n            saveMemoryGraph(graph, getContext(), false, { fit: true });\r\n        })\r\n        .on('click', '.ai-wbr-memory-delete-node', function () {\r\n            const graph = getMemoryGraph();\r\n            const id = String($(this).closest('[data-memory-node-id]').data('memoryNodeId'));\r\n            graph.nodes = graph.nodes.filter(node => node.id !== id);\r\n            graph.links = graph.links.filter(link => link.source !== id && link.target !== id);\r\n            if (memoryGraphSelectedNodeId === id) {\r\n                memoryGraphSelectedNodeId = '';\r\n            }\r\n            if (memoryGraphLinkSourceId === id) {\r\n                memoryGraphLinkSourceId = '';\r\n            }\r\n            saveMemoryGraph(graph, getContext(), false, { fit: true });\r\n        })\r\n        .on('click', '.ai-wbr-memory-delete-link', function () {\r\n            const graph = getMemoryGraph();\r\n            const id = String($(this).closest('[data-memory-link-id]').data('memoryLinkId'));\r\n            graph.links = graph.links.filter(link => String(link.id) !== id);\r\n            if (memoryGraphSelectedLinkId === id) {\r\n                memoryGraphSelectedLinkId = '';\r\n            }\r\n            saveMemoryGraph(graph, getContext(), false, { fit: true });\r\n        });\r\n}\r\n\r\nfunction debugLog(...args) {\r\n    if (settings.debug) {\r\n        console.debug(LOG_PREFIX, ...args);\r\n    }\r\n}\r\n\r\nfunction debugRun(candidates, selected, injection, source, routerPrompt = '', routerRaw = '', memoryCandidates = [], selectedMemories = [], bookshelfCandidates = [], selectedBookshelf = [], pipeline = null) {\n    const apiStats = getApiStats();\n    const activity = getRuntimeActivity();\n    lastRun = {\n        candidates,\n        selected,\n        memoryCandidates,\n        selectedMemories,\r\n        bookshelfCandidates,\r\n        selectedBookshelf,\r\n        injectedChars: injection.length,\r\n        injectionText: injection,\r\n        source,\r\n        error: '',\r\n        routerPrompt,\n        routerRaw,\n        pipeline,\n        apiStats,\n        activity,\n    };\n\r\n    if (settings.debug) {\r\n        console.groupCollapsed(`${LOG_PREFIX} routed wb ${selected.length}/${candidates.length}, memory ${selectedMemories.length}/${memoryCandidates.length}, bookshelf ${selectedBookshelf.length}/${bookshelfCandidates.length}`);\r\n        console.debug('Candidates:', candidates.map(entry => ({\r\n            id: entry.routerId,\r\n            world: entry.world,\r\n            comment: entry.comment,\r\n            keys: entry.keys.all,\r\n            matchedKeys: entry.matchedKeys,\r\n            score: entry.score,\r\n            constant: entry.constant,\r\n        })));\r\n        console.debug('Selected:', selected.map(entry => ({\r\n            id: entry.routerId,\r\n            reason: entry.reason,\r\n        })));\r\n        console.debug('Memory candidates:', memoryCandidates.map(entry => ({\r\n            id: entry.routerId,\r\n            title: entry.comment,\r\n            keys: entry.keys.all,\r\n            matchedKeys: entry.matchedKeys,\r\n            score: entry.score,\r\n            type: entry.memoryType,\r\n        })));\r\n        console.debug('Selected memories:', selectedMemories.map(entry => ({\r\n            id: entry.routerId,\r\n            reason: entry.reason,\r\n        })));\r\n        console.debug('Bookshelf candidates:', bookshelfCandidates.map(entry => ({\r\n            id: entry.id,\r\n            book: entry.book?.title || entry.book?.fileName,\r\n            title: entry.title,\r\n            score: entry.score,\r\n            semanticScore: entry.semanticScore,\r\n            keywordScore: entry.keywordScore,\r\n        })));\r\n        console.debug('Selected bookshelf:', selectedBookshelf.map(entry => ({\r\n            id: entry.id,\r\n            book: entry.book?.title || entry.book?.fileName,\r\n            title: entry.title,\r\n            score: entry.score,\r\n        })));\r\n        console.debug('Injection chars:', injection.length);\r\n        console.debug('Injection:', injection);\r\n        console.groupEnd();\r\n    }\r\n\r\n    renderDebugPanel();\r\n}\r\n\r\nfunction debugError(error) {\n    const apiStats = getApiStats();\n    const activity = getRuntimeActivity();\n    lastRun = {\n        candidates: [],\n        selected: [],\n        memoryCandidates: [],\r\n        selectedMemories: [],\r\n        bookshelfCandidates: [],\r\n        selectedBookshelf: [],\r\n        injectedChars: 0,\r\n        injectionText: '',\r\n        source: 'error',\r\n        error: error?.message || String(error),\r\n        routerPrompt: '',\n        routerRaw: '',\n        pipeline: null,\n        apiStats,\n        activity,\n    };\n    failRuntimeActivity(activity.stage || 'select', error);\n    renderDebugPanel();\n}\n\r\nasync function routeWorldbookForMessages(context, recentMessages, routeSource = 'generate_interceptor', logMeta = {}) {\n    const lastUserMessage = getLastUserMessage(recentMessages);\n    debugLog('Generation intercepted', { ...logMeta, routeSource, lastUserMessage });\n    beginApiStatsRound(`本轮生成 API 调用 · ${routeSource}`);\n    startRuntimeActivity('recall', '开始本轮流程', '准备召回世界书、记忆和书架资料。');\n\n    const preRefresh = await preRefreshMemoryForRoute(context);\n    if (preRefresh?.attempted) {\n        finishRuntimeActivityStage('preRefresh', preRefresh.updated ? '已预刷新记忆' : '预刷新无变化');\n    }\n    const recallBundle = await buildUnifiedRecallBundle(context, recentMessages, { preRefresh });\n    const {\r\n        mvuSummary,\r\n        memoryGraph,\r\n        wbCandidates,\r\n        memoryCandidates,\r\n        bookshelfCandidates,\r\n        selectedBookshelf: vectorSelectedBookshelf,\r\n        selectedMemoryVectors,\r\n        combinedCandidates,\r\n    } = recallBundle;\r\n    let selectedBookshelf = [];\r\n\r\n    if (combinedCandidates.length === 0 && !hasMemoryState(memoryGraph) && !vectorSelectedBookshelf.length) {\n        debugRun([], [], '', `none-${routeSource}`, '', '', [], [], bookshelfCandidates, [], recallBundle.pipeline);\n        completeRuntimeActivity('本轮流程完成', '没有需要注入的候选内容。');\n        lastRouteCompletedAt = Date.now();\n        return {\n            candidates: wbCandidates,\r\n            selected: [],\r\n            memoryCandidates,\r\n            selectedMemories: [],\r\n            bookshelfCandidates,\r\n            selectedBookshelf: [],\r\n            injection: '',\r\n            source: `none-${routeSource}`,\r\n        };\r\n    }\r\n\r\n    let selectedWb = [];\r\n    let selectedMem = [];\r\n    let routerPrompt = '';\r\n    let routerRaw = '';\r\n    let source = `ai-${routeSource}`;\r\n    let shouldFallback = false;\r\n\r\n    if (combinedCandidates.length) {\r\n        try {\r\n            isRouterSelectionRequest = true;\r\n            const maxSelectCount = settings.maxSelected + MAX_MEMORY_SELECTED + clampNumber(settings.bookshelfMaxChunks, defaultSettings.bookshelfMaxChunks, 1, 12);\r\n            const aiResult = await selectWithAi(context, recentMessages, mvuSummary, combinedCandidates, maxSelectCount);\r\n            \r\n            for (const item of aiResult.selected) {\r\n                if (item.sourceType === 'bookshelf' || item.source === 'bookshelf') {\r\n                    selectedBookshelf.push(item);\r\n                } else if (item.sourceType === 'memory-keyword' || item.sourceType === 'memory-vector' || item.source === 'memory' || item.source === 'memory-vector') {\r\n                    selectedMem.push(item);\r\n                } else {\r\n                    selectedWb.push(item);\r\n                }\r\n            }\r\n            \r\n            routerPrompt = aiResult.prompt;\r\n            routerRaw = aiResult.rawPreview;\r\n            source = (selectedWb.length || selectedMem.length || selectedBookshelf.length) ? `ai-${routeSource}` : `empty-ai-${routeSource}`;\r\n        } catch (error) {\r\n            source = `keyword-ai-fallback-${routeSource}`;\r\n            shouldFallback = true;\r\n            console.warn(`${LOG_PREFIX} AI selection failed; falling back to keyword score.`, error);\r\n            routerPrompt = error?.routerPrompt || '';\r\n            routerRaw = error?.routerRaw || error?.message || String(error);\r\n        } finally {\r\n            isRouterSelectionRequest = false;\r\n        }\r\n    }\r\n\r\n    if (shouldFallback && !selectedWb.length && !selectedMem.length && !selectedBookshelf.length) {\r\n        selectedWb = selectWithFallback(wbCandidates);\r\n        selectedMem = selectMemoryWithFallback(memoryCandidates);\r\n        selectedBookshelf = vectorSelectedBookshelf;\r\n    }\r\n    selectedMem = mergeSelectedMemoryVectors(selectedMem, selectedMemoryVectors);\r\n\r\n    if (!combinedCandidates.length && (selectedMem.length || hasMemoryState(memoryGraph))) {\r\n        source = `memory-${routeSource}`;\r\n    }\r\n    if (selectedMemoryVectors.length) {\r\n        source += '+memory-vector';\r\n    }\r\n    if (selectedBookshelf.length) {\r\n        source += '+bookshelf';\r\n    }\r\n\n    const injection = buildInjection(selectedWb, memoryGraph, selectedMem, selectedBookshelf);\n    startRuntimeActivity('inject', '注入中', `正在写入本轮上下文 · ${injection.length} 字`);\n    setExtensionPrompt(PROMPT_KEY, injection, settings.position, settings.depth, false, settings.role);\n    finishRuntimeActivityStage('inject', `已注入 ${injection.length} 字`);\n    const pipeline = {\n        ...recallBundle.pipeline,\r\n        selected: {\r\n            worldbook: selectedWb.length,\r\n            memory: selectedMem.length,\r\n            bookshelf: selectedBookshelf.length,\r\n        },\r\n        injection: {\r\n            chars: injection.length,\r\n            hasWorldbook: !!selectedWb.length,\r\n            hasMemory: !!(selectedMem.length || hasMemoryState(memoryGraph)),\r\n            hasBookshelf: !!selectedBookshelf.length,\r\n        },\r\n    };\n    debugRun(wbCandidates, selectedWb, injection, source, routerPrompt, routerRaw, memoryCandidates, selectedMem, bookshelfCandidates, selectedBookshelf, pipeline);\n    completeRuntimeActivity('本轮路由完成', `命中世界书 ${selectedWb.length} · 记忆 ${selectedMem.length} · 书架 ${selectedBookshelf.length}`);\n    lastRouteCompletedAt = Date.now();\n\r\n    return {\r\n        candidates: wbCandidates,\r\n        selected: selectedWb,\r\n        memoryCandidates,\r\n        selectedMemories: selectedMem,\r\n        bookshelfCandidates,\r\n        selectedBookshelf,\r\n        injection,\r\n        source,\r\n    };\r\n}\r\n\r\nasync function runTavernHelperRoute(args) {\r\n    const options = args?.[0];\r\n    if (!shouldRouteTavernHelperGenerate(options)) {\r\n        return false;\r\n    }\r\n\r\n    options._ai_wbr_routed = true;\r\n    isCompatRouterRunning = true;\r\n    const endRouterBusy = beginRouterBusy();\r\n    setExtensionPrompt(PROMPT_KEY, '', settings.position, settings.depth, false, settings.role);\r\n    clearEntryBurst();\r\n    startWorldInfoAnimation();\r\n\r\n    try {\r\n        const context = getContext();\r\n        const recentMessages = buildCompatRecentMessages(context, options);\r\n        const result = await routeWorldbookForMessages(context, recentMessages, 'tavernhelper_generate', {\r\n            type: 'tavernhelper',\r\n        });\r\n\r\n        stopWorldInfoAnimation();\r\n        if (result.selected.length && !result.source.includes('fallback')) {\r\n            playSelectedEntriesBurst(result.selected);\r\n        }\r\n\r\n        return true;\r\n    } catch (error) {\r\n        setExtensionPrompt(PROMPT_KEY, '', settings.position, settings.depth, false, settings.role);\r\n        debugError(error);\r\n        console.error(`${LOG_PREFIX} TavernHelper route failed`, error);\r\n        return false;\r\n    } finally {\n        stopWorldInfoAnimation();\n        endRouterBusy();\n        isCompatRouterRunning = false;\n        setTimeout(() => scheduleMemoryGraphUpdate({ reason: 'tavernhelper_post_generate' }), 3500);\n    }\n}\n\r\nfunction renderRouterModelOptions() {\r\n    const select = $('#ai_wbr_router_model');\r\n    if (!select.length) {\r\n        return;\r\n    }\r\n\r\n    select.empty();\r\n    select.append('<option value=\"\">未选择</option>');\r\n    for (const modelId of settings.routerModels || []) {\r\n        select.append($('<option></option>', {\r\n            value: modelId,\r\n            text: modelId,\r\n            selected: modelId === settings.routerModel,\r\n        }));\r\n    }\r\n\r\n    if (settings.routerModel && !settings.routerModels.includes(settings.routerModel)) {\r\n        select.append($('<option></option>', {\r\n            value: settings.routerModel,\r\n            text: `${settings.routerModel} (手动)`,\r\n            selected: true,\r\n        }));\r\n    }\r\n}\r\n\r\nasync function fetchRouterModels() {\r\n    const context = getContext();\r\n    const apiUrl = normalizeUrl(settings.routerApiUrl);\r\n    const apiKey = String(settings.routerApiKey || '').trim();\r\n\r\n    if (!apiUrl) {\r\n        toastr.warning('请先填写独立路由模型的 API URL。', '世界书读取');\r\n        return;\r\n    }\r\n\r\n    if (!apiKey) {\r\n        toastr.warning('请先填写独立路由模型的 API Key。', '世界书读取');\r\n        return;\r\n    }\r\n\r\n    setRouterStatus('正在拉取模型...');\r\n\r\n    try {\r\n        const response = await fetch('/api/backends/chat-completions/status', {\r\n            method: 'POST',\r\n            headers: context.getRequestHeaders(),\r\n            cache: 'no-cache',\r\n            body: JSON.stringify({\r\n                chat_completion_source: 'openai',\r\n                reverse_proxy: apiUrl,\r\n                proxy_password: apiKey,\r\n            }),\r\n        });\r\n\r\n        const data = await response.json();\r\n        const models = Array.isArray(data?.data)\r\n            ? data.data.map(model => String(model?.id || '')).filter(Boolean)\r\n            : [];\r\n\r\n        if (!response.ok || !models.length) {\r\n            throw new Error(data?.error?.message || data?.message || '没有拿到可用模型');\r\n        }\r\n\r\n        settings.routerModels = models;\r\n        if (!models.includes(settings.routerModel)) {\r\n            settings.routerModel = models[0];\r\n        }\r\n\r\n        Object.assign(extension_settings[MODULE_NAME], settings);\r\n        saveSettingsDebounced();\r\n        renderRouterModelOptions();\r\n        setRouterStatus(`已拉取 ${models.length} 个模型`);\r\n        toastr.success(`已拉取 ${models.length} 个模型`, '世界书读取');\r\n    } catch (error) {\r\n        setRouterStatus(`拉取失败：${error.message || error}`);\r\n        console.error(`${LOG_PREFIX} Failed to fetch router models`, error);\r\n        toastr.error(String(error.message || error), '世界书读取');\r\n    }\r\n}\r\n\r\nasync function interceptGeneration(chat, contextSize, abort, type) {\r\n    setExtensionPrompt(PROMPT_KEY, '', settings.position, settings.depth, false, settings.role);\r\n\r\n    if (!settings.enabled || type === 'quiet') {\r\n        return;\r\n    }\r\n\r\n    const context = getContext();\r\n    const endRouterBusy = beginRouterBusy();\r\n    clearEntryBurst();\r\n    startWorldInfoAnimation();\r\n    try {\r\n        const recentMessages = getRecentMessages(chat);\r\n        const result = await routeWorldbookForMessages(context, recentMessages, 'generate_interceptor', {\r\n            type,\r\n            contextSize,\r\n        });\r\n        stopWorldInfoAnimation();\r\n        if (result.selected.length && !result.source.includes('fallback')) {\r\n            playSelectedEntriesBurst(result.selected);\r\n        }\r\n    } catch (error) {\r\n        setExtensionPrompt(PROMPT_KEY, '', settings.position, settings.depth, false, settings.role);\r\n        debugError(error);\r\n        console.error(`${LOG_PREFIX} Interceptor failed`, error);\r\n    } finally {\r\n        stopWorldInfoAnimation();\r\n        endRouterBusy();\r\n    }\r\n}\r\n\r\nfunction bindCheckbox(id, key) {\r\n    $(id).prop('checked', !!settings[key]).on('input', function () {\r\n        saveSetting(key, !!$(this).prop('checked'));\r\n    });\r\n}\r\n\r\nfunction bindNumber(id, key, min, max) {\r\n    $(id).val(settings[key]).on('input', function () {\r\n        const value = clampNumber($(this).val(), defaultSettings[key], min, max);\r\n        saveSetting(key, value);\r\n        $(this).val(value);\r\n    });\r\n}\r\n\r\nfunction bindSelectNumber(id, key) {\r\n    $(id).val(String(settings[key])).on('change', function () {\r\n        saveSetting(key, Number($(this).val()));\r\n    });\r\n}\r\n\r\nfunction bindTextarea(id, key) {\r\n    $(id).val(settings[key]).on('input', function () {\r\n        saveSetting(key, String($(this).val()));\r\n    });\r\n}\r\n\r\nfunction bindText(id, key, normalizer = (value) => String(value)) {\r\n    $(id).val(settings[key]).on('input', function () {\r\n        saveSetting(key, normalizer($(this).val()));\r\n    });\r\n}\r\n\r\nfunction bindBookshelfDynamicSettingsActions() {\r\n    const selector = [\r\n        '#ai_wbr_bookshelf_enabled',\n        '#ai_wbr_bookshelf_auto_inject',\n        '#ai_wbr_bookshelf_auto_memory_book',\n        '#ai_wbr_bookshelf_auto_memory_book_vectorize',\n        '#ai_wbr_bookshelf_memory_vector',\n        '#ai_wbr_bookshelf_only_bound',\r\n        '#ai_wbr_bookshelf_allow_global',\r\n        '#ai_wbr_bookshelf_memory_vector_max',\r\n        '#ai_wbr_bookshelf_max_chunks',\r\n        '#ai_wbr_bookshelf_max_chars',\r\n        '#ai_wbr_bookshelf_min_score',\r\n        '#ai_wbr_bookshelf_api_url',\n        '#ai_wbr_bookshelf_api_key',\n        '#ai_wbr_bookshelf_api_model_select',\n        '#ai_wbr_bookshelf_api_model',\n        '#ai_wbr_bookshelf_local_model',\r\n        '#ai_wbr_bookshelf_embedding_mode',\r\n    ].join(', ');\r\n\r\n    $(document)\n        .off('input.aiWbrBookshelfSettings change.aiWbrBookshelfSettings', selector)\n        .on('input.aiWbrBookshelfSettings change.aiWbrBookshelfSettings', selector, function (event) {\n            const id = String(this.id || '');\n            let statusAfterSync = '';\n            if (id === 'ai_wbr_bookshelf_enabled') saveSetting('bookshelfEnabled', !!this.checked);\n            else if (id === 'ai_wbr_bookshelf_auto_inject') saveSetting('bookshelfAutoInject', !!this.checked);\r\n            else if (id === 'ai_wbr_bookshelf_auto_memory_book') {\n                saveSetting('bookshelfAutoMemoryBook', !!this.checked);\n                if (this.checked) scheduleBookshelfMemoryBookSync(getContext(), { force: true, silent: false, delayMs: 100 });\n            }\n            else if (id === 'ai_wbr_bookshelf_auto_memory_book_vectorize') saveSetting('bookshelfAutoMemoryBookVectorize', !!this.checked);\n            else if (id === 'ai_wbr_bookshelf_memory_vector') saveSetting('bookshelfMemoryVectorRecall', !!this.checked);\n            else if (id === 'ai_wbr_bookshelf_only_bound') saveSetting('bookshelfOnlyBound', !!this.checked);\r\n            else if (id === 'ai_wbr_bookshelf_allow_global') saveSetting('bookshelfAllowGlobal', !!this.checked);\r\n            else if (id === 'ai_wbr_bookshelf_memory_vector_max') saveSetting('bookshelfMemoryVectorMaxItems', clampNumber($(this).val(), defaultSettings.bookshelfMemoryVectorMaxItems, 1, 12));\r\n            else if (id === 'ai_wbr_bookshelf_max_chunks') saveSetting('bookshelfMaxChunks', clampNumber($(this).val(), defaultSettings.bookshelfMaxChunks, 1, 12));\r\n            else if (id === 'ai_wbr_bookshelf_max_chars') saveSetting('bookshelfMaxChunkChars', clampNumber($(this).val(), defaultSettings.bookshelfMaxChunkChars, 120, 2000));\r\n            else if (id === 'ai_wbr_bookshelf_min_score') saveSetting('bookshelfMinScore', clampNumber($(this).val(), defaultSettings.bookshelfMinScore, 0, 1));\r\n            else if (id === 'ai_wbr_bookshelf_api_url') {\n                const nextValue = normalizeUrl($(this).val());\n                if (nextValue !== settings.bookshelfApiUrl) {\n                    saveSetting('bookshelfApiUrl', nextValue);\n                    statusAfterSync = 'API 地址已变更，请重新获取模型。';\n                    clearBookshelfApiModels(statusAfterSync);\n                }\n            }\n            else if (id === 'ai_wbr_bookshelf_api_key') {\n                const nextValue = String($(this).val() || '').trim();\n                if (nextValue !== settings.bookshelfApiKey) {\n                    saveSetting('bookshelfApiKey', nextValue);\n                    statusAfterSync = 'API Key 已变更，请重新获取模型。';\n                    clearBookshelfApiModels(statusAfterSync);\n                }\n            }\n            else if (id === 'ai_wbr_bookshelf_api_model_select') {\n                const nextValue = String($(this).val() || '').trim();\n                saveSetting('bookshelfApiModel', nextValue);\n                $('[id=\"ai_wbr_bookshelf_api_model\"]').val(nextValue);\n                if (nextValue) {\n                    statusAfterSync = `已选择 API 模型：${nextValue}`;\n                    setBookshelfStatus(statusAfterSync);\n                }\n            }\n            else if (id === 'ai_wbr_bookshelf_api_model') {\n                const nextValue = String($(this).val() || '').trim();\n                saveSetting('bookshelfApiModel', nextValue);\n                $('[id=\"ai_wbr_bookshelf_api_model_select\"]').val(nextValue);\n                if (nextValue) {\n                    statusAfterSync = `已手动设置 API 模型：${nextValue}`;\n                    setBookshelfStatus(statusAfterSync);\n                }\n            }\n            else if (id === 'ai_wbr_bookshelf_local_model') saveSetting('bookshelfLocalModelId', String($(this).val() || '').trim());\r\n            else if (id === 'ai_wbr_bookshelf_embedding_mode' && event.type === 'change') {\r\n                saveSetting('bookshelfEmbeddingMode', String($(this).val() || 'api') === 'browser-local' ? 'browser-local' : 'api');\r\n            }\n            syncBookshelfProviderVisibility();\n            const textFieldWithoutImmediateRender = [\n                'ai_wbr_bookshelf_api_url',\n                'ai_wbr_bookshelf_api_key',\n                'ai_wbr_bookshelf_api_model_select',\n                'ai_wbr_bookshelf_api_model',\n                'ai_wbr_bookshelf_local_model',\n            ].includes(id);\n            if (!textFieldWithoutImmediateRender || event.type === 'change') {\n                renderBookshelfPanel();\n            }\n            if (statusAfterSync) {\n                setBookshelfModelStatus(statusAfterSync);\n                setBookshelfStatus(statusAfterSync);\n            }\n        });\n}\n\r\nfunction renderTitleBlocklistEditor() {\r\n    const container = $('#ai_wbr_title_block_items');\r\n    if (!container.length) {\r\n        return;\r\n    }\r\n\r\n    const rules = parseBlockRules(settings.titleBlocklist);\r\n    container.empty();\r\n\r\n    if (!rules.length) {\r\n        container.append('<div class=\"ai-wbr-token-empty\">暂无拦截标题</div>');\r\n        return;\r\n    }\r\n\r\n    for (const rule of rules) {\r\n        const item = $('<div class=\"ai-wbr-token-item\"></div>');\r\n        item.append($('<span class=\"ai-wbr-token-label\"></span>').text(rule));\r\n        item.append($('<button class=\"ai-wbr-token-remove\" type=\"button\" aria-label=\"删除\">×</button>')\r\n            .on('click', () => {\r\n                const nextRules = parseBlockRules(settings.titleBlocklist).filter(entry => entry !== rule);\r\n                saveSetting('titleBlocklist', nextRules.join('\\n'));\r\n                renderTitleBlocklistEditor();\r\n            }));\r\n        container.append(item);\r\n    }\r\n}\r\n\r\nfunction renderActiveWorldbookSelector(context = getContext()) {\r\n    const container = $('#ai_wbr_active_world_items');\r\n    if (!container.length) {\r\n        return;\r\n    }\r\n\r\n    const bindings = getActiveWorldbookBindings(context);\r\n    container.empty();\r\n\r\n    if (!bindings.length) {\r\n        container.append('<div class=\"ai-wbr-token-empty\">当前聊天下暂无可识别的生效世界书</div>');\r\n        return;\r\n    }\r\n\r\n    for (const binding of bindings) {\r\n        const item = $('<label class=\"checkbox_label ai-wbr-world-option\"></label>');\r\n        const checkbox = $('<input type=\"checkbox\" />')\r\n            .prop('checked', getWorldSelectionState(binding.worldName))\r\n            .attr('data-world-name', binding.worldName)\r\n            .on('input', function () {\r\n                setWorldSelectionState(binding.worldName, !!$(this).prop('checked'));\r\n            });\r\n        item.append(checkbox);\r\n        item.append($('<span></span>').text(binding.worldName));\r\n        item.append($('<small class=\"ai-wbr-world-source\"></small>').text(binding.source));\r\n        container.append(item);\r\n    }\r\n}\r\n\r\nfunction bindTitleBlocklistEditor() {\r\n    const input = $('#ai_wbr_title_block_input');\r\n    const button = $('#ai_wbr_title_block_add');\r\n    if (!input.length || !button.length) {\r\n        return;\r\n    }\r\n\r\n    const submit = () => {\r\n        const value = String(input.val() || '').trim();\r\n        if (!value) {\r\n            return;\r\n        }\r\n\r\n        const rules = parseBlockRules(settings.titleBlocklist);\r\n        if (!rules.includes(value)) {\r\n            rules.push(value);\r\n            saveSetting('titleBlocklist', rules.join('\\n'));\r\n        }\r\n\r\n        input.val('');\r\n        renderTitleBlocklistEditor();\r\n    };\r\n\r\n    button.on('click', submit);\r\n    input.on('keydown', (event) => {\r\n        if (event.key === 'Enter') {\r\n            event.preventDefault();\r\n            submit();\r\n        }\r\n    });\r\n\r\n    renderTitleBlocklistEditor();\r\n}\r\n\r\nfunction createFloatingMemoryWindow() {\r\n    if ($('#ai_wbr_fab').length && $('#ai_wbr_floating_window').length) {\r\n        updateFloatingButtonVisibility();\r\n        clampFloatingFabToViewport();\r\n        globalThis.aiWbrOpenConsole = (tabId = 'overview', options = {}) => {\r\n            const existingWin = $('#ai_wbr_floating_window');\r\n            if (!existingWin.length) {\r\n                return;\r\n            }\r\n            existingWin.removeClass('closing').addClass('open');\r\n            const mode = options?.mode || 'floating';\r\n            const node = existingWin[0];\r\n            node.hidden = false;\r\n            node.removeAttribute('aria-hidden');\r\n            node.dataset.aiWbrDisplayMode = mode;\r\n            node.style.setProperty('visibility', 'visible', 'important');\r\n            node.style.setProperty('opacity', '1', 'important');\r\n            node.style.setProperty('pointer-events', 'auto', 'important');\r\n            node.style.setProperty('z-index', '2147483646', 'important');\r\n            node.style.setProperty('display', 'flex', 'important');\r\n            node.style.setProperty('transform', 'none', 'important');\r\n            applyWindowDisplayMode(existingWin, mode);\r\n            renderStandaloneConsole(tabId || 'overview');\r\n            setTimeout(() => {\r\n                applyWindowDisplayMode(existingWin, node.dataset.aiWbrDisplayMode || mode);\r\n                renderStandaloneConsole(tabId || getStandaloneTabId());\r\n            }, 120);\r\n            setTimeout(() => {\r\n                applyWindowDisplayMode(existingWin, node.dataset.aiWbrDisplayMode || mode);\r\n                renderStandaloneConsole(tabId || getStandaloneTabId());\r\n            }, 340);\r\n        };\r\n        return;\r\n    }\r\n\r\n    $('#ai_wbr_fab').remove();\r\n    $('#ai_wbr_floating_window').remove();\r\n    $('#ai_wbr_emergency_fab').remove();\r\n\r\n    const fab = $('<div id=\"ai_wbr_fab\" class=\"ai-wbr-fab\" title=\"打开记忆图谱\"><i class=\"fa-solid fa-diagram-project\"></i></div>');\r\n    // 注意：局部变量命名为 win，避免遮蔽全局 window 对象（拖拽时需要用 window.innerWidth/innerHeight 取视口尺寸）\r\n    const win = $('<div id=\"ai_wbr_floating_window\" class=\"ai-wbr-floating-window\">' +\r\n        '<div class=\"ai-wbr-floating-header\" id=\"ai_wbr_floating_header\">' +\r\n            '<div class=\"ai-wbr-floating-title\"><i class=\"fa-solid fa-diagram-project\"></i> 记忆图谱 <span id=\"ai_wbr_console_status\" class=\"ai-wbr-console-status\">等待生成</span></div>' +\r\n            '<div class=\"ai-wbr-floating-close\" id=\"ai_wbr_floating_close\"><i class=\"fa-solid fa-times\"></i></div>' +\r\n        '</div>' +\r\n        '<div class=\"ai-wbr-console-tabs\" id=\"ai_wbr_console_tabs\">' +\r\n            '<button class=\"ai-wbr-console-tab active\" type=\"button\" data-tab=\"overview\">总览</button>' +\r\n            '<button class=\"ai-wbr-console-tab\" type=\"button\" data-tab=\"routes\">路由</button>' +\r\n            '<button class=\"ai-wbr-console-tab\" type=\"button\" data-tab=\"injection\">注入</button>' +\r\n            '<button class=\"ai-wbr-console-tab\" type=\"button\" data-tab=\"memory\">记忆</button>' +\n            '<button class=\"ai-wbr-console-tab\" type=\"button\" data-tab=\"graph\">图谱</button>' +\n            '<button class=\"ai-wbr-console-tab\" type=\"button\" data-tab=\"bookshelf\">书架</button>' +\n            '<button class=\"ai-wbr-console-tab\" type=\"button\" data-tab=\"settings\">设置</button>' +\n        '</div>' +\n        '<div class=\"ai-wbr-floating-content ai-wbr-console-body\" id=\"ai_wbr_console_body\"></div>' +\r\n    '</div>');\r\n\r\n    $('body').append(fab).append(win);\r\n    restoreStandalonePanelsToSettings();\r\n    updateFloatingButtonVisibility();\r\n    clampFloatingFabToViewport();\r\n\r\n    // 切换悬浮窗显隐\r\n    function clearForcedWindowStyles() {\r\n        const node = win?.[0];\r\n        if (!node) return;\r\n        [\r\n            'visibility',\r\n            'opacity',\r\n            'pointer-events',\r\n            'z-index',\r\n            'transform',\r\n            'position',\r\n            'inset',\r\n            'left',\r\n            'top',\r\n            'right',\r\n            'bottom',\r\n            'width',\r\n            'height',\r\n            'max-width',\r\n            'max-height',\r\n            'border-radius',\r\n            'display',\r\n        ].forEach((property) => node.style.removeProperty(property));\r\n    }\r\n\r\n    function applyWindowDisplayMode(targetWin, mode = 'floating') {\r\n        const node = targetWin?.[0];\r\n        if (!node) return;\r\n        const viewportWidth = Math.max(1, globalThis.visualViewport?.width || window.innerWidth || document.documentElement.clientWidth || 1);\r\n        const viewportHeight = Math.max(1, globalThis.visualViewport?.height || window.innerHeight || document.documentElement.clientHeight || 1);\r\n        const fullMode = mode === 'full';\r\n        const mobile = viewportWidth <= 640 || viewportHeight <= 520;\r\n        if (fullMode) {\r\n            node.style.setProperty('position', 'fixed', 'important');\r\n            node.style.setProperty('inset', '0', 'important');\r\n            node.style.setProperty('left', '0', 'important');\r\n            node.style.setProperty('top', '0', 'important');\r\n            node.style.setProperty('right', 'auto', 'important');\r\n            node.style.setProperty('bottom', 'auto', 'important');\r\n            node.style.setProperty('width', '100vw', 'important');\r\n            node.style.setProperty('height', '100dvh', 'important');\r\n            node.style.setProperty('max-width', '100vw', 'important');\r\n            node.style.setProperty('max-height', '100dvh', 'important');\r\n            node.style.setProperty('border-radius', '0', 'important');\r\n            return;\r\n        }\r\n\r\n        if (mobile) {\r\n            const margin = 8;\r\n            const width = Math.max(280, viewportWidth - margin * 2);\r\n            const height = Math.max(320, Math.min(viewportHeight * 0.82, viewportHeight - margin * 2));\r\n            const top = Math.max(margin, viewportHeight - height - margin);\r\n            node.style.setProperty('position', 'fixed', 'important');\r\n            node.style.setProperty('inset', 'auto', 'important');\r\n            node.style.setProperty('left', `${margin}px`, 'important');\r\n            node.style.setProperty('top', `${top}px`, 'important');\r\n            node.style.setProperty('right', 'auto', 'important');\r\n            node.style.setProperty('bottom', 'auto', 'important');\r\n            node.style.setProperty('width', `${width}px`, 'important');\r\n            node.style.setProperty('height', `${height}px`, 'important');\r\n            node.style.setProperty('max-width', `calc(100vw - ${margin * 2}px)`, 'important');\r\n            node.style.setProperty('max-height', `calc(100dvh - ${margin * 2}px)`, 'important');\r\n            node.style.setProperty('border-radius', '14px', 'important');\r\n            return;\r\n        }\r\n\r\n        const rect = node.getBoundingClientRect();\r\n        const width = Math.min(Math.max(320, rect.width || node.offsetWidth || 720), Math.max(320, viewportWidth - 16));\r\n        const height = Math.min(Math.max(360, rect.height || node.offsetHeight || 640), Math.max(360, viewportHeight - 16));\r\n        const nextLeft = Math.max(8, Math.min(Number.isFinite(rect.left) ? rect.left : viewportWidth - width - 24, viewportWidth - width - 8));\r\n        const nextTop = Math.max(8, Math.min(Number.isFinite(rect.top) ? rect.top : viewportHeight - height - 88, viewportHeight - height - 8));\r\n        node.style.setProperty('position', 'fixed', 'important');\r\n        node.style.setProperty('left', `${nextLeft}px`, 'important');\r\n        node.style.setProperty('top', `${nextTop}px`, 'important');\r\n        node.style.setProperty('right', 'auto', 'important');\r\n        node.style.setProperty('bottom', 'auto', 'important');\r\n        node.style.setProperty('width', `${width}px`, 'important');\r\n        node.style.setProperty('height', `${height}px`, 'important');\r\n        node.style.setProperty('max-width', 'calc(100vw - 16px)', 'important');\r\n        node.style.setProperty('max-height', 'calc(100dvh - 16px)', 'important');\r\n    }\r\n\r\n    function clampWindowToViewport(mode = 'floating') {\r\n        applyWindowDisplayMode(win, mode);\r\n    }\r\n\r\n    function forceWindowVisible(tabId = getStandaloneTabId(), options = {}) {\r\n        const node = win?.[0];\r\n        if (!node) return;\r\n        const mode = options?.mode || 'floating';\r\n        win.removeClass('closing').addClass('open');\r\n        node.hidden = false;\r\n        node.removeAttribute('aria-hidden');\r\n        node.dataset.aiWbrDisplayMode = mode;\r\n        node.style.setProperty('visibility', 'visible', 'important');\r\n        node.style.setProperty('opacity', '1', 'important');\r\n        node.style.setProperty('pointer-events', 'auto', 'important');\r\n        node.style.setProperty('z-index', '2147483646', 'important');\r\n        node.style.setProperty('display', 'flex', 'important');\r\n        node.style.setProperty('transform', 'none', 'important');\r\n        clampWindowToViewport(mode);\r\n        renderStandaloneConsole(tabId || 'overview');\r\n        // 动画结束后再渲染一次，确保记忆 SVG 取到动画终态的准确尺寸\r\n        setTimeout(() => {\r\n            clampWindowToViewport(node.dataset.aiWbrDisplayMode || mode);\r\n            renderStandaloneConsole(tabId || getStandaloneTabId());\r\n        }, 120);\r\n        setTimeout(() => {\r\n            clampWindowToViewport(node.dataset.aiWbrDisplayMode || mode);\r\n            renderStandaloneConsole(tabId || getStandaloneTabId());\r\n        }, 340);\r\n    }\r\n\r\n    function openWindow(tabId = getStandaloneTabId(), options = {}) {\r\n        forceWindowVisible(tabId || 'overview', options);\r\n    }\r\n\r\n    function closeWindow() {\r\n        if (memoryGraphFullscreenActive) {\r\n            setMemoryGraphFullscreen(false, { fit: false });\r\n        }\r\n        win.removeClass('open').addClass('closing');\r\n        restoreStandalonePanelsToSettings();\r\n        const node = win?.[0];\r\n        if (node) {\r\n            node.style.setProperty('visibility', 'hidden', 'important');\r\n            node.style.setProperty('opacity', '0', 'important');\r\n            node.style.setProperty('pointer-events', 'none', 'important');\r\n        }\r\n        setTimeout(() => {\r\n            win.removeClass('closing');\r\n            clearForcedWindowStyles();\r\n        }, 220); // 与 CSS 关闭动画时长一致\r\n    }\r\n\r\n    function toggleWindow(defaultTab = getStandaloneTabId()) {\r\n        const isOpen = win.hasClass('open');\r\n        if (isOpen) {\r\n            closeWindow();\r\n        } else {\r\n            openWindow(defaultTab || 'overview');\r\n        }\r\n    }\r\n\r\n    globalThis.aiWbrOpenConsole = (tabId = 'overview', options = {}) => {\r\n        openWindow(tabId, options);\r\n    };\r\n    globalThis.aiWbrCloseConsole = closeWindow;\r\n\r\n    // FAB 拖拽（区分点击与拖拽：移动超过 8px 视为拖拽，松手不触发开窗）\n    const FAB_DRAG_TAP_THRESHOLD = 8;\n    let fabDragging = false;\n    let fabMoved = false;\n    let fabStartX = 0, fabStartY = 0, fabInitialLeft = 0, fabInitialTop = 0;\r\n    let fabPointerId = null;\r\n    let fabSuppressNextClick = false;\r\n    let fabLastToggleAt = 0;\r\n    let fabLastTouchAt = 0;\r\n\r\n    function openFromFab(event, options = {}) {\n        const now = Date.now();\n        const minInterval = options.force ? 80 : 260;\n        if (now - fabLastToggleAt < minInterval) {\r\n            event?.preventDefault?.();\r\n            event?.stopPropagation?.();\r\n            return;\r\n        }\r\n        fabLastToggleAt = now;\n        event?.preventDefault?.();\n        event?.stopPropagation?.();\n        openWindow('graph', { mode: 'full' });\n        setMemoryGraphFullscreen(true, { fit: false });\n        setTimeout(() => {\n            if (!win.hasClass('open') || getStandaloneTabId() !== 'graph') {\n                openWindow('graph', { mode: 'full' });\n            }\n            setMemoryGraphFullscreen(true, { fit: true });\n        }, 120);\n    }\r\n\r\n    function openFromFabTouch(event) {\n        if (fabMoved || fabDragging || fabSuppressNextClick) {\n            event?.preventDefault?.();\n            event?.stopPropagation?.();\n            return;\n        }\n        fabLastTouchAt = Date.now();\n        fabMoved = false;\n        fabDragging = false;\n        fabPointerId = null;\r\n        $('body').css('user-select', '');\r\n        openFromFab(event, { force: true });\r\n    }\r\n\r\n    function finishFabPointer(e) {\r\n        if (!fabDragging) {\r\n            return;\r\n        }\r\n        if (fabPointerId !== null && e.pointerId !== undefined && e.pointerId !== fabPointerId) {\r\n            return;\r\n        }\r\n        const wasTap = !fabMoved && e.type === 'pointerup';\n        const wasDrag = fabMoved;\n        fabDragging = false;\n        fabPointerId = null;\n        clampFloatingFabToViewport();\n        $('body').css('user-select', '');\n        if (wasDrag) {\n            fabSuppressNextClick = true;\n            e?.preventDefault?.();\n            e?.stopPropagation?.();\n            setTimeout(() => {\n                fabSuppressNextClick = false;\n            }, 520);\n            return;\n        }\n        if (wasTap) {\n            fabSuppressNextClick = true;\n            openFromFab(e, { force: true });\r\n            setTimeout(() => {\r\n                fabSuppressNextClick = false;\r\n            }, 360);\r\n        }\r\n    }\r\n\r\n    fab.on('pointerdown', (e) => {\r\n        if (e.button !== undefined && e.button !== 0) return; // 仅左键\r\n        fabDragging = true;\r\n        fabMoved = false;\r\n        fabPointerId = e.pointerId;\r\n        fabStartX = e.clientX;\r\n        fabStartY = e.clientY;\r\n        // 把 right/bottom 定位转换为 left/top，便于拖拽\r\n        const rect = fab[0].getBoundingClientRect();\r\n        fabInitialLeft = rect.left;\r\n        fabInitialTop = rect.top;\r\n        fab.css({ right: 'auto', bottom: 'auto', left: fabInitialLeft + 'px', top: fabInitialTop + 'px' });\r\n        fab[0].setPointerCapture?.(e.pointerId);\r\n        $('body').css('user-select', 'none');\r\n    });\r\n\r\n    $(document).on('pointermove.fabDrag', (e) => {\r\n        if (!fabDragging) return;\n        const dx = e.clientX - fabStartX;\n        const dy = e.clientY - fabStartY;\n        if (!fabMoved && Math.hypot(dx, dy) < FAB_DRAG_TAP_THRESHOLD) return; // 阈值内仍视为点击\n        fabMoved = true;\n        const rect = fab[0].getBoundingClientRect();\r\n        const maxLeft = Math.max(0, window.innerWidth - rect.width);\r\n        const maxTop = Math.max(0, window.innerHeight - rect.height);\r\n        const newLeft = Math.max(0, Math.min(fabInitialLeft + dx, maxLeft));\r\n        const newTop = Math.max(0, Math.min(fabInitialTop + dy, maxTop));\r\n        fab.css({ left: newLeft + 'px', top: newTop + 'px' });\r\n        e.preventDefault();\r\n    });\r\n\r\n    fab.on('pointerup pointercancel', finishFabPointer);\r\n    $(document).on('pointerup.fabDrag pointercancel.fabDrag', finishFabPointer);\r\n\r\n    fab.on('click', (e) => {\r\n        if (Date.now() - fabLastTouchAt < 420) {\r\n            e.preventDefault();\r\n            e.stopPropagation();\r\n            return;\r\n        }\r\n        if (fabSuppressNextClick) {\r\n            e.preventDefault();\r\n            e.stopPropagation();\r\n            return;\r\n        }\r\n        if (fabMoved) {\r\n            // 拖拽结束，抑制本次开窗\r\n            fabMoved = false;\r\n            e.preventDefault();\r\n            e.stopPropagation();\r\n            return;\r\n        }\r\n        openFromFab(e);\r\n    });\r\n\r\n    fab.on('touchend', (e) => {\r\n        if (fabSuppressNextClick) {\r\n            e.preventDefault();\r\n            e.stopPropagation();\r\n            return;\r\n        }\r\n        openFromFabTouch(e);\r\n    });\r\n\r\n    fab[0]?.addEventListener('touchend', openFromFabTouch, { capture: true, passive: false });\r\n    fab[0]?.addEventListener('click', (event) => {\r\n        if (Date.now() - fabLastTouchAt < 420) {\r\n            event.preventDefault();\r\n            event.stopPropagation();\r\n            return;\r\n        }\r\n        openFromFab(event);\r\n    }, true);\r\n\r\n    $('#ai_wbr_floating_close').on('click touchend pointerup', (event) => {\r\n        event.preventDefault();\r\n        event.stopPropagation();\r\n        closeWindow();\r\n    });\r\n    $('#ai_wbr_console_tabs').on('click', '.ai-wbr-console-tab', function () {\r\n        renderStandaloneConsole(String($(this).data('tab') || 'overview'));\r\n    });\r\n\r\n    // ESC 关闭\r\n    $(document).on('keydown', (event) => {\r\n        if (event.key === 'Escape' && memoryGraphFullscreenActive) {\r\n            setMemoryGraphFullscreen(false);\r\n            return;\r\n        }\r\n        if (event.key === 'Escape' && win.hasClass('open')) {\r\n            toggleWindow();\r\n        }\r\n    });\r\n\r\n    // 拖拽逻辑（仅标题栏触发）\r\n    let isDragging = false;\r\n    let startX, startY, initialLeft, initialTop;\r\n\r\n    const header = $('#ai_wbr_floating_header');\r\n\r\n    header.on('pointerdown', (e) => {\r\n        if ($(e.target).closest('#ai_wbr_floating_close').length) return; // 排除关闭按钮\r\n        if (e.button !== undefined && e.button !== 0) return;\r\n        isDragging = true;\r\n        startX = e.clientX;\r\n        startY = e.clientY;\r\n\r\n        // 把 bottom/right 定位转换为 left/top，便于拖拽\r\n        const rect = win[0].getBoundingClientRect();\r\n        initialLeft = rect.left;\r\n        initialTop = rect.top;\r\n\r\n        win.css({\r\n            right: 'auto',\r\n            bottom: 'auto',\r\n            left: initialLeft + 'px',\r\n            top: initialTop + 'px'\r\n        });\r\n\r\n        win[0].style.removeProperty('inset');\r\n        win[0].style.removeProperty('right');\r\n        win[0].style.removeProperty('bottom');\r\n        header[0].setPointerCapture?.(e.pointerId);\r\n        $('body').css('user-select', 'none'); // 拖拽时禁止选中文本\r\n        e.preventDefault();\r\n    });\r\n\r\n    $(document).on('pointermove.aiWbrWindowDrag', (e) => {\r\n        if (!isDragging) return;\r\n\r\n        const dx = e.clientX - startX;\r\n        const dy = e.clientY - startY;\r\n\r\n        let newLeft = initialLeft + dx;\r\n        let newTop = initialTop + dy;\r\n\r\n        // 限制在视口范围内（用全局 window.innerWidth/innerHeight 取视口尺寸）\r\n        const rect = win[0].getBoundingClientRect();\r\n        const maxLeft = Math.max(0, window.innerWidth - rect.width);\r\n        const maxTop = Math.max(0, window.innerHeight - rect.height);\r\n\r\n        newLeft = Math.max(0, Math.min(newLeft, maxLeft));\r\n        newTop = Math.max(0, Math.min(newTop, maxTop));\r\n\r\n        win.css({\r\n            left: newLeft + 'px',\r\n            top: newTop + 'px'\r\n        });\r\n        e.preventDefault();\r\n    });\r\n\r\n    $(document).on('pointerup.aiWbrWindowDrag pointercancel.aiWbrWindowDrag', () => {\r\n        if (isDragging) {\r\n            isDragging = false;\r\n            $('body').css('user-select', '');\r\n        }\r\n    });\r\n\r\n    $(window).off('resize.aiWbrFabSafeArea orientationchange.aiWbrFabSafeArea')\r\n        .on('resize.aiWbrFabSafeArea orientationchange.aiWbrFabSafeArea', () => {\r\n            setTimeout(clampFloatingFabToViewport, 80);\r\n            setTimeout(clampFloatingFabToViewport, 360);\r\n            if (win.hasClass('open')) {\r\n                setTimeout(() => clampWindowToViewport(win[0]?.dataset.aiWbrDisplayMode || 'floating'), 80);\r\n                setTimeout(() => clampWindowToViewport(win[0]?.dataset.aiWbrDisplayMode || 'floating'), 360);\r\n            }\r\n        });\r\n\r\n    globalThis.visualViewport?.addEventListener?.('resize', () => {\r\n        clampFloatingFabToViewport();\r\n        if (win.hasClass('open')) clampWindowToViewport(win[0]?.dataset.aiWbrDisplayMode || 'floating');\r\n    }, { passive: true });\r\n    globalThis.visualViewport?.addEventListener?.('scroll', () => {\n        clampFloatingFabToViewport();\n        if (win.hasClass('open')) clampWindowToViewport(win[0]?.dataset.aiWbrDisplayMode || 'floating');\n    }, { passive: true });\n\n    function recoverFloatingFab() {\n        if (settings.floatingButtonEnabled === false) {\n            $('#ai_wbr_fab').hide();\n            return;\n        }\n        if (!$('#ai_wbr_fab').length || !$('#ai_wbr_floating_window').length) {\n            createFloatingMemoryWindow();\n            return;\n        }\n        updateFloatingButtonVisibility();\n    }\n\n    $(window).off('focus.aiWbrFabRecover pageshow.aiWbrFabRecover')\n        .on('focus.aiWbrFabRecover pageshow.aiWbrFabRecover', () => {\n            setTimeout(recoverFloatingFab, 80);\n            setTimeout(recoverFloatingFab, 420);\n        });\n\n    $(document).off('visibilitychange.aiWbrFabRecover')\n        .on('visibilitychange.aiWbrFabRecover', () => {\n            if (document.visibilityState === 'visible') {\n                setTimeout(recoverFloatingFab, 80);\n                setTimeout(recoverFloatingFab, 420);\n            }\n        });\n\n    if (!globalThis.aiWbrFloatingFabObserver) {\n        globalThis.aiWbrFloatingFabObserver = new MutationObserver(() => {\n            if (settings.floatingButtonEnabled === false) return;\n            if (!$('#ai_wbr_fab').length) {\n                setTimeout(recoverFloatingFab, 120);\n            }\n        });\n        globalThis.aiWbrFloatingFabObserver.observe(document.body, { childList: true, subtree: true });\n    }\n\n    renderStandaloneConsole('overview');\n}\n\r\nfunction updateFloatingButtonVisibility() {\n    const visible = settings.floatingButtonEnabled !== false;\n    $('#ai_wbr_emergency_fab').remove();\n    const fab = $('#ai_wbr_fab');\n    fab.toggle(visible);\n    if (visible) {\n        fab.each((_, node) => {\n            node.hidden = false;\n            node.removeAttribute('aria-hidden');\n            node.style.setProperty('display', 'flex', 'important');\n            node.style.setProperty('visibility', 'visible', 'important');\n            node.style.setProperty('opacity', '1', 'important');\n            node.style.setProperty('pointer-events', 'auto', 'important');\n            node.style.setProperty('z-index', '2147483647', 'important');\n        });\n        clampFloatingFabToViewport();\n    }\n}\n\nfunction ensureFloatingFabMounted() {\n    if (settings.floatingButtonEnabled === false) {\n        $('#ai_wbr_fab').hide();\n        return false;\n    }\n\n    const fab = $('#ai_wbr_fab');\n    const win = $('#ai_wbr_floating_window');\n    if (!fab.length || !win.length || !document.body.contains(fab[0]) || !document.body.contains(win[0])) {\n        createFloatingMemoryWindow();\n        return true;\n    }\n\n    $('#ai_wbr_emergency_fab').remove();\n    const node = fab[0];\n    node.hidden = false;\n    node.removeAttribute('aria-hidden');\n    node.style.setProperty('display', 'flex', 'important');\n    node.style.setProperty('visibility', 'visible', 'important');\n    node.style.setProperty('opacity', '1', 'important');\n    node.style.setProperty('pointer-events', 'auto', 'important');\n    node.style.setProperty('z-index', '2147483647', 'important');\n    clampFloatingFabToViewport();\n    return true;\n}\n\nfunction scheduleFloatingFabRecovery() {\n    if (settings.floatingButtonEnabled === false) {\n        return;\n    }\n    [0, 80, 260, 700, 1400].forEach(delay => {\n        setTimeout(ensureFloatingFabMounted, delay);\n    });\n}\n\r\nfunction clampFloatingFabToViewport() {\r\n    const fab = $('#ai_wbr_fab');\r\n    if (!fab.length || settings.floatingButtonEnabled === false || fab.css('display') === 'none') {\r\n        return;\r\n    }\r\n\r\n    const node = fab[0];\r\n    const rect = node.getBoundingClientRect();\r\n    const width = Math.max(44, rect.width || node.offsetWidth || 52);\r\n    const height = Math.max(44, rect.height || node.offsetHeight || 52);\r\n    const viewportWidth = Math.max(1, globalThis.visualViewport?.width || window.innerWidth || document.documentElement.clientWidth || 1);\r\n    const viewportHeight = Math.max(1, globalThis.visualViewport?.height || window.innerHeight || document.documentElement.clientHeight || 1);\r\n    const margin = Math.max(8, Math.min(16, Math.round(Math.min(viewportWidth, viewportHeight) * 0.025)));\r\n    const maxLeft = Math.max(margin, viewportWidth - width - margin);\r\n    const maxTop = Math.max(margin, viewportHeight - height - margin);\r\n    const hasInlinePosition = node.style.left || node.style.top;\r\n    const defaultLeft = Math.max(margin, viewportWidth - width - margin);\r\n    const defaultTop = Math.max(margin, viewportHeight - height - Math.max(margin, Math.min(88, viewportHeight * 0.12)));\r\n    const currentLeft = Number.isFinite(rect.left) && rect.width > 0 ? rect.left : defaultLeft;\r\n    const currentTop = Number.isFinite(rect.top) && rect.height > 0 ? rect.top : defaultTop;\r\n    const nextLeft = Math.max(margin, Math.min(hasInlinePosition ? currentLeft : defaultLeft, maxLeft));\r\n    const nextTop = Math.max(margin, Math.min(hasInlinePosition ? currentTop : defaultTop, maxTop));\r\n\r\n    fab.css({\r\n        left: `${nextLeft}px`,\r\n        top: `${nextTop}px`,\r\n        right: 'auto',\r\n        bottom: 'auto',\r\n    });\r\n}\r\n\r\nasync function loadSettingsHtml() {\r\n    if (typeof AI_WBR_SETTINGS_HTML === 'string' && AI_WBR_SETTINGS_HTML.trim()) {\r\n        return AI_WBR_SETTINGS_HTML;\r\n    }\r\n\r\n    const errors = [];\r\n    try {\r\n        const settingsUrl = new URL('./settings.html', import.meta.url);\r\n        const response = await fetch(settingsUrl.href, { cache: 'no-cache' });\r\n        if (response.ok) {\r\n            const html = await response.text();\r\n            if (html.includes('ai_worldbook_router_settings')) {\r\n                return html;\r\n            }\r\n            errors.push(`settings.html 内容不完整：${settingsUrl.href}`);\r\n        } else {\r\n            errors.push(`settings.html 请求失败 ${response.status}：${settingsUrl.href}`);\r\n        }\r\n    } catch (error) {\r\n        errors.push(`按当前脚本目录读取 settings.html 失败：${error?.message || error}`);\r\n    }\r\n\r\n    if (typeof renderExtensionTemplateAsync === 'function') {\r\n        try {\r\n            return await renderExtensionTemplateAsync('third-party/ai-worldbook-router', 'settings');\r\n        } catch (error) {\r\n            errors.push(`兼容旧目录模板读取失败：${error?.message || error}`);\r\n        }\r\n    }\r\n\r\n    throw new Error(`世界书读取设置界面加载失败：${errors.join('；') || '未知错误'}`);\r\n}\r\n\r\nasync function addSettingsUi() {\r\n    const html = await loadSettingsHtml();\r\n    $('#extensions_settings2').append(html);\r\n    ensureBookshelfSettingsSection();\r\n\r\n    bindCheckbox('#ai_wbr_enabled', 'enabled');\r\n    bindCheckbox('#ai_wbr_debug', 'debug');\r\n    bindCheckbox('#ai_wbr_entry_diagnostics', 'entryDiagnostics');\r\n    bindCheckbox('#ai_wbr_floating_button_enabled', 'floatingButtonEnabled');\r\n    bindCheckbox('#ai_wbr_router_use_separate_model', 'routerUseSeparateModel');\r\n    bindCheckbox('#ai_wbr_keyword_recall', 'keywordRecall');\r\n    bindCheckbox('#ai_wbr_use_mvu', 'useMvu');\r\n    bindCheckbox('#ai_wbr_allow_constant', 'allowConstant');\r\n\r\n    bindNumber('#ai_wbr_max_candidates', 'maxCandidates', 1, 100);\r\n    bindNumber('#ai_wbr_max_selected', 'maxSelected', 1, 50);\r\n    bindNumber('#ai_wbr_max_chars', 'maxChars', 100, 30000);\r\n    bindNumber('#ai_wbr_scan_messages', 'scanMessages', 1, 50);\r\n    bindNumber('#ai_wbr_main_history_ai_turns', 'mainHistoryAiTurns', 0, 100);\r\n    bindNumber('#ai_wbr_depth', 'depth', 0, 1000);\r\n    bindNumber('#ai_wbr_ai_response_length', 'aiResponseLength', 32, 16384);\r\n    bindNumber('#ai_wbr_router_retries', 'routerRetries', 0, 5);\r\n    bindNumber('#ai_wbr_router_request_max_tokens', 'routerRequestMaxTokens', 32, 100000);\r\n    bindNumber('#ai_wbr_memory_request_max_tokens', 'memoryRequestMaxTokens', 32, 100000);\r\n\r\n    bindSelectNumber('#ai_wbr_position', 'position');\r\n    bindSelectNumber('#ai_wbr_role', 'role');\r\n    bindTitleBlocklistEditor();\r\n    bindTextarea('#ai_wbr_system_prompt', 'systemPrompt');\r\n    bindText('#ai_wbr_router_api_url', 'routerApiUrl', normalizeUrl);\r\n    bindText('#ai_wbr_router_api_key', 'routerApiKey', (value) => String(value).trim());\r\n    bindMemoryPanelActions();\r\n\r\n    renderRouterModelOptions();\r\n    renderActiveWorldbookSelector();\r\n    $('#ai_wbr_router_model').val(settings.routerModel).on('change', function () {\r\n        saveSetting('routerModel', String($(this).val() || ''));\r\n    });\r\n    $('#ai_wbr_fetch_models').on('click', async (event) => {\r\n        event.preventDefault();\r\n        await fetchRouterModels();\r\n    });\r\n    $('#ai_wbr_router_status').text(settings.routerStatus || '未连接');\r\n\r\n    createFloatingMemoryWindow();\r\n\r\n    renderDebugPanel();\r\n    renderMemoryPanel();\r\n}\r\n\r\nglobalThis.ai_worldbook_router_intercept = interceptGeneration;\r\ninstallFetchFallbackHook();\r\n\r\nfunction createEmergencyEntryNative() {\r\n    document.getElementById('ai_wbr_emergency_fab')?.remove();\r\n}\r\n\r\ncreateEmergencyEntryNative();\r\n\r\nasync function bootAiWorldbookRouter() {\r\n    try {\r\n        ensureSettings();\r\n        createFloatingMemoryWindow();\r\n        try {\r\n            await addSettingsUi();\r\n        } catch (settingsError) {\r\n            lastRun.error = `设置面板加载失败，但独立控制台已启用：${settingsError?.message || settingsError}`;\r\n            console.error(`${LOG_PREFIX} Settings UI failed, standalone console remains available`, settingsError);\r\n            renderStandaloneConsole('overview');\r\n        }\r\n        installFetchFallbackHook();\r\n        installCompatSendHooks();\r\n        ensureTavernHelperCompatHook();\r\n        startCompatGenerateHookPolling();\r\n        startChatScopedUiPolling();\r\n        installChatScopedUiRefreshEventHooks();\r\n\r\n        eventSource.on(event_types.GENERATION_STARTED, () => {\n            isGenerationActive = true;\n            scheduleFloatingFabRecovery();\n        });\n        eventSource.on(event_types.GENERATION_ENDED, () => {\n            isGenerationActive = false;\n            scheduleCompatFlush();\n            scheduleMemoryGraphUpdate();\n            scheduleFloatingFabRecovery();\n        });\n        eventSource.on(event_types.GENERATION_STOPPED, () => {\n            isGenerationActive = false;\n            scheduleCompatFlush();\n            scheduleMemoryGraphUpdate({ reason: 'generation_stopped' });\n            scheduleFloatingFabRecovery();\n        });\n\r\n        eventSource.on(event_types.CHAT_CHANGED, (...args) => {\r\n            rememberChatIdentifierFromEvent(...args);\r\n            pendingCompatSend = false;\r\n            suppressCompatReplay = false;\r\n            lastRun = {\r\n                candidates: [],\r\n                selected: [],\r\n                memoryCandidates: [],\r\n                selectedMemories: [],\r\n                bookshelfCandidates: [],\r\n                selectedBookshelf: [],\r\n                injectedChars: 0,\r\n                injectionText: '',\r\n                source: 'none',\r\n                error: '',\n                routerPrompt: '',\n                routerRaw: '',\n                pipeline: null,\n                apiStats: createEmptyApiStats(),\n                activity: createEmptyRuntimeActivity(),\n            };\n            setExtensionPrompt(PROMPT_KEY, '', settings.position, settings.depth, false, settings.role);\n            renderActiveWorldbookSelector();\n            scheduleFloatingFabRecovery();\n        });\n\r\n        debugLog('Loaded');\r\n    } catch (error) {\r\n        console.error(`${LOG_PREFIX} Failed during initialization`, error);\r\n        try {\r\n            ensureSettings();\r\n            createEmergencyEntryNative();\r\n            createFloatingMemoryWindow();\r\n            lastRun.error = `初始化失败：${error?.message || error}`;\r\n            renderStandaloneConsole('overview');\r\n        } catch (fallbackError) {\r\n            console.error(`${LOG_PREFIX} Failed to create fallback console`, fallbackError);\r\n        }\r\n    }\r\n}\r\n\r\nconst readyRunner = globalThis.jQuery || globalThis.$;\r\nif (typeof readyRunner === 'function') {\r\n    readyRunner(bootAiWorldbookRouter);\r\n} else if (document.readyState === 'loading') {\r\n    document.addEventListener('DOMContentLoaded', bootAiWorldbookRouter, { once: true });\r\n} else {\r\n    setTimeout(bootAiWorldbookRouter, 0);\r\n}\r\n";
    const script = hostDocument.createElement('script');
    script.type = 'module';
    script.textContent = moduleSource;
    script.dataset.aiWorldbookRouterUserscript = 'true';
    hostDocument.head.appendChild(script);
})();
